/*bundlesConfig*/
System.bundles = {"bundles/bithub":["stealconfig","jquery","can/util/can","can/util/attr/attr","can/event/event","can/util/array/each","can/util/inserted/inserted","can/util/jquery/jquery","can/util/bind/bind","can/map/bubble","can/util/string/string","can/construct/construct","can/util/batch/batch","can/map/map","can/list/list","can/util/string/deparam/deparam","can/route/route","can/control/control","can/control/route/route","can/model/model","can/view/view","can/compute/compute","can/view/scope/scope","can/view/elements","can/view/callbacks/callbacks","can/view/scanner","can/view/node_lists/node_lists","can/view/parser/parser","can/view/live/live","can/view/render","can/view/mustache/mustache","can/observe/observe","can/view/bindings/bindings","can/component/component","can/can","can/view/mustache","can/view/mustache/system","bithub/homepage/homepage.mustache!can/view/mustache/system","bithub/homepage/event_list/views/init.mustache!can/view/mustache/system","bithub/homepage/event_list/views/latest.mustache!can/view/mustache/system","bithub/homepage/event_list/views/greatest.mustache!can/view/mustache/system","bithub/homepage/event_list/determine_event_partial","can/construct/proxy/proxy","can/construct/super/super","bithub/models/upvote","bithub/models/award","bithub/editpost/editpost.mustache!can/view/mustache/system","bithub/postform/postform.mustache!can/view/mustache/system","vendor/moment/moment.src","vendor/moment/moment","can/map/validations/validations","can/map/attributes/attributes","can/map/setter/setter","bithub/models/event","bithub/models/tag","bithub/models/postas_user","fileupload-load-image","jquery-widget","fileupload","fileupload-processing","bithub/postform/postform","bithub/editpost/editpost","bithub/homepage/event_list/handlers/handlers","ui/html_select/init.mustache!can/view/mustache/system","ui/html_select/html_select","bithub/homepage/event_list/spinner/spinner","bithub/homepage/event_list/flagsnapper/flagsnapper","bithub/homepage/event_list/post_render/post_render","bithub/homepage/event_list/latest_events_sorter","vendor/lodash/lodash.src","vendor/lodash/lodash","bithub/helpers/fun_helpers","bithub/helpers/mustacheHelpers","jquerypp/dom/compare/compare","jquerypp/dom/range/range","more","can/map/delegate/delegate","bithub/entities/entities","bithub/homepage/event_list/event_list","bithub/event_details/event_details.mustache!can/view/mustache/system","bithub/event_details/event_details","bithub/homepage/sidebar/sidebar.mustache!can/view/mustache/system","bithub/homepage/sidebar/leaderboard/leaderboard.mustache!can/view/mustache/system","bithub/models/identity","bithub/helpers/auth","bithub/helpers/github","bithub/helpers/data","bithub/models/user","bithub/homepage/sidebar/leaderboard/leaderboard","bithub/models/reward","bithub/models/accomplishment","bithub/homepage/sidebar/sidebar","bithub/homepage/homepage","bithub/profile/earnpoints/init.mustache!can/view/mustache/system","bithub/profile/earnpoints/earnpoints","bithub/profile/rewards/init.mustache!can/view/mustache/system","bithub/profile/rewards/rewards","bithub/profile/init.mustache!can/view/mustache/system","bithub/profile/info/info.mustache!can/view/mustache/system","ui/bootstrap_dropdown/init.mustache!can/view/mustache/system","ui/bootstrap_dropdown/bootstrap_dropdown","bithub/models/country","jquerypp/dom/form_params/form_params","bithub/profile/info/info","bithub/models/activity","bithub/profile/activities/activities.mustache!can/view/mustache/system","bithub/profile/activities/activities","bithub/profile/profile","bithub/admin/users/users.mustache!can/view/mustache/system","bithub/admin/users/form/form.mustache!can/view/mustache/system","bithub/admin/users/form/form","bithub/admin/paginator","bithub/admin/users/users","bithub/admin/rewards/rewards.mustache!can/view/mustache/system","bithub/admin/rewards/form/form.mustache!can/view/mustache/system","bootstrap-datepicker","bithub/admin/rewards/form/form","bithub/admin/rewards/rewards","bithub/admin/tags/tags.mustache!can/view/mustache/system","bithub/admin/tags/form/form.mustache!can/view/mustache/system","bithub/admin/tags/form/form","bithub/admin/tags/tags","bithub/admin/achievements/init.mustache!can/view/mustache/system","bithub/models/achievement","bithub/admin/achievements/achievements","bithub/admin/admin","bithub/leaderboard/leaderboard.mustache!can/view/mustache/system","bithub/leaderboard/leaderboard","bithub/user_profile/user_profile.mustache!can/view/mustache/system","bithub/user_profile/user_profile","bithub/pageswitcher/no_permissions.mustache!can/view/mustache/system","bithub/helpers/permission_checker","bithub/pageswitcher/pageswitcher","bithub/navigator/filterbar/filterbar.mustache!can/view/mustache/system","can/view/ejs/ejs","can/view/ejs","can/view/ejs/system","ui/radio_selector/init.ejs!can/view/ejs/system","ui/radio_selector/radio_selector","ui/dropdown_selector/init.ejs!can/view/ejs/system","bootstrap","ui/dropdown_selector/dropdown_selector","ui/lru_selector/init.ejs!can/view/ejs/system","jquerypp/lang/json/json","jquerypp/dom/cookie/cookie","ui/lru_selector/lru_selector","bithub/navigator/filterbar/filterbar","bithub/navigator/admin_navbar/admin_navbar","bithub/navigator/profile_navbar/init.mustache!can/view/mustache/system","bithub/navigator/profile_navbar/profile_navbar","bithub/navigator/navigator","bithub/login/login.mustache!can/view/mustache/system","bithub/login/login","bithub/newpost/init.mustache!can/view/mustache/system","bithub/newpost/typeahead","bithub/newpost/newpost","bithub/models/pagination","bithub/lib/paginator","bithub/lib/query_tracker","bithub/modals/init.mustache!can/view/mustache/system","bithub/modals/_twitter_modal.mustache!can/view/mustache/system","bithub/modals/modals","bithub/helpers/loadtime","jstz","ui/onbottom","can/route/pushstate/pushstate","bithub/bithub"]};
/*stealconfig*/
steal.config({map:{"jquery/jquery":"jquery","can/util/util":"can/util/jquery/jquery","bootstrap/bootstrap":"bootstrap","bootstrap-datepicker/bootstrap-datepicker":"bootstrap-datepicker","fileupload-load-image/fileupload-load-image":"fileupload-load-image","jquery-widget/jquery-widget":"jquery-widget","fileupload/fileupload":"fileupload","fileupload-processing/fileupload-processing":"fileupload-processing","more/more":"more","jstz/jstz":"jstz"},paths:{can:"can/can.js",jquery:"can/lib/jquery.1.9.1.js",bootstrap:"vendor/bootstrap/bootstrap.src.js","bootstrap-datepicker":"vendor/bootstrap-datepicker/bootstrap-datepicker.src.js","fileupload-load-image":"vendor/fileupload/load-image.js","jquery-widget":"vendor/fileupload/jquery.ui.widget.js",fileupload:"vendor/fileupload/fileupload.src.js","fileupload-processing":"vendor/fileupload/fileupload-fp.src.js",more:"ui/more/more.js",jstz:"vendor/jstz/jstz.js"},meta:{jquery:{exports:"jQuery"},bootstrap:{deps:["jquery"]},"bootstrap-datepicker":{deps:["jquery"]},"fileupload-load-image":{deps:["jquery"]},"jquery-widget":{deps:["fileupload-load-image"]},fileupload:{deps:["jquery-widget"]},"fileupload-processing":{deps:["fileupload"]},more:{deps:["jquery","jquerypp/dom/range/range"]},jstz:{exports:"jstz"}},ext:{ejs:"can/view/ejs/system",mustache:"can/view/mustache/system"}}),System.buildConfig={map:{"can/util/util":"can/util/domless/domless"}};
/*jquery*/
System.define("jquery",'/**\n * @documentjs-ignore\n */\n/*!\n * jQuery JavaScript Library v1.9.1\n * http://jquery.com/\n *\n * Includes Sizzle.js\n * http://sizzlejs.com/\n *\n * Copyright 2005, 2012 jQuery Foundation, Inc. and other contributors\n * Released under the MIT license\n * http://jquery.org/license\n *\n * Date: 2013-2-4\n */\n(function( window, undefined ) {\n\n// Can\'t do this because several apps including ASP.NET trace\n// the stack via arguments.caller.callee and Firefox dies if\n// you try to trace through "use strict" call chains. (#13335)\n// Support: Firefox 18+\n//"use strict";\nvar\n	// The deferred used on DOM ready\n	readyList,\n\n	// A central reference to the root jQuery(document)\n	rootjQuery,\n\n	// Support: IE<9\n	// For `typeof node.method` instead of `node.method !== undefined`\n	core_strundefined = typeof undefined,\n\n	// Use the correct document accordingly with window argument (sandbox)\n	document = window.document,\n	location = window.location,\n\n	// Map over jQuery in case of overwrite\n	_jQuery = window.jQuery,\n\n	// Map over the $ in case of overwrite\n	_$ = window.$,\n\n	// [[Class]] -> type pairs\n	class2type = {},\n\n	// List of deleted data cache ids, so we can reuse them\n	core_deletedIds = [],\n\n	core_version = "1.9.1",\n\n	// Save a reference to some core methods\n	core_concat = core_deletedIds.concat,\n	core_push = core_deletedIds.push,\n	core_slice = core_deletedIds.slice,\n	core_indexOf = core_deletedIds.indexOf,\n	core_toString = class2type.toString,\n	core_hasOwn = class2type.hasOwnProperty,\n	core_trim = core_version.trim,\n\n	// Define a local copy of jQuery\n	jQuery = function( selector, context ) {\n		// The jQuery object is actually just the init constructor \'enhanced\'\n		return new jQuery.fn.init( selector, context, rootjQuery );\n	},\n\n	// Used for matching numbers\n	core_pnum = /[+-]?(?:\\d*\\.|)\\d+(?:[eE][+-]?\\d+|)/.source,\n\n	// Used for splitting on whitespace\n	core_rnotwhite = /\\S+/g,\n\n	// Make sure we trim BOM and NBSP (here\'s looking at you, Safari 5.0 and IE)\n	rtrim = /^[\\s\\uFEFF\\xA0]+|[\\s\\uFEFF\\xA0]+$/g,\n\n	// A simple way to check for HTML strings\n	// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)\n	// Strict HTML recognition (#11290: must start with <)\n	rquickExpr = /^(?:(<[\\w\\W]+>)[^>]*|#([\\w-]*))$/,\n\n	// Match a standalone tag\n	rsingleTag = /^<(\\w+)\\s*\\/?>(?:<\\/\\1>|)$/,\n\n	// JSON RegExp\n	rvalidchars = /^[\\],:{}\\s]*$/,\n	rvalidbraces = /(?:^|:|,)(?:\\s*\\[)+/g,\n	rvalidescape = /\\\\(?:["\\\\\\/bfnrt]|u[\\da-fA-F]{4})/g,\n	rvalidtokens = /"[^"\\\\\\r\\n]*"|true|false|null|-?(?:\\d+\\.|)\\d+(?:[eE][+-]?\\d+|)/g,\n\n	// Matches dashed string for camelizing\n	rmsPrefix = /^-ms-/,\n	rdashAlpha = /-([\\da-z])/gi,\n\n	// Used by jQuery.camelCase as callback to replace()\n	fcamelCase = function( all, letter ) {\n		return letter.toUpperCase();\n	},\n\n	// The ready event handler\n	completed = function( event ) {\n\n		// readyState === "complete" is good enough for us to call the dom ready in oldIE\n		if ( document.addEventListener || event.type === "load" || document.readyState === "complete" ) {\n			detach();\n			jQuery.ready();\n		}\n	},\n	// Clean-up method for dom ready events\n	detach = function() {\n		if ( document.addEventListener ) {\n			document.removeEventListener( "DOMContentLoaded", completed, false );\n			window.removeEventListener( "load", completed, false );\n\n		} else {\n			document.detachEvent( "onreadystatechange", completed );\n			window.detachEvent( "onload", completed );\n		}\n	};\n\njQuery.fn = jQuery.prototype = {\n	// The current version of jQuery being used\n	jquery: core_version,\n\n	constructor: jQuery,\n	init: function( selector, context, rootjQuery ) {\n		var match, elem;\n\n		// HANDLE: $(""), $(null), $(undefined), $(false)\n		if ( !selector ) {\n			return this;\n		}\n\n		// Handle HTML strings\n		if ( typeof selector === "string" ) {\n			if ( selector.charAt(0) === "<" && selector.charAt( selector.length - 1 ) === ">" && selector.length >= 3 ) {\n				// Assume that strings that start and end with <> are HTML and skip the regex check\n				match = [ null, selector, null ];\n\n			} else {\n				match = rquickExpr.exec( selector );\n			}\n\n			// Match html or make sure no context is specified for #id\n			if ( match && (match[1] || !context) ) {\n\n				// HANDLE: $(html) -> $(array)\n				if ( match[1] ) {\n					context = context instanceof jQuery ? context[0] : context;\n\n					// scripts is true for back-compat\n					jQuery.merge( this, jQuery.parseHTML(\n						match[1],\n						context && context.nodeType ? context.ownerDocument || context : document,\n						true\n					) );\n\n					// HANDLE: $(html, props)\n					if ( rsingleTag.test( match[1] ) && jQuery.isPlainObject( context ) ) {\n						for ( match in context ) {\n							// Properties of context are called as methods if possible\n							if ( jQuery.isFunction( this[ match ] ) ) {\n								this[ match ]( context[ match ] );\n\n							// ...and otherwise set as attributes\n							} else {\n								this.attr( match, context[ match ] );\n							}\n						}\n					}\n\n					return this;\n\n				// HANDLE: $(#id)\n				} else {\n					elem = document.getElementById( match[2] );\n\n					// Check parentNode to catch when Blackberry 4.6 returns\n					// nodes that are no longer in the document #6963\n					if ( elem && elem.parentNode ) {\n						// Handle the case where IE and Opera return items\n						// by name instead of ID\n						if ( elem.id !== match[2] ) {\n							return rootjQuery.find( selector );\n						}\n\n						// Otherwise, we inject the element directly into the jQuery object\n						this.length = 1;\n						this[0] = elem;\n					}\n\n					this.context = document;\n					this.selector = selector;\n					return this;\n				}\n\n			// HANDLE: $(expr, $(...))\n			} else if ( !context || context.jquery ) {\n				return ( context || rootjQuery ).find( selector );\n\n			// HANDLE: $(expr, context)\n			// (which is just equivalent to: $(context).find(expr)\n			} else {\n				return this.constructor( context ).find( selector );\n			}\n\n		// HANDLE: $(DOMElement)\n		} else if ( selector.nodeType ) {\n			this.context = this[0] = selector;\n			this.length = 1;\n			return this;\n\n		// HANDLE: $(function)\n		// Shortcut for document ready\n		} else if ( jQuery.isFunction( selector ) ) {\n			return rootjQuery.ready( selector );\n		}\n\n		if ( selector.selector !== undefined ) {\n			this.selector = selector.selector;\n			this.context = selector.context;\n		}\n\n		return jQuery.makeArray( selector, this );\n	},\n\n	// Start with an empty selector\n	selector: "",\n\n	// The default length of a jQuery object is 0\n	length: 0,\n\n	// The number of elements contained in the matched element set\n	size: function() {\n		return this.length;\n	},\n\n	toArray: function() {\n		return core_slice.call( this );\n	},\n\n	// Get the Nth element in the matched element set OR\n	// Get the whole matched element set as a clean array\n	get: function( num ) {\n		return num == null ?\n\n			// Return a \'clean\' array\n			this.toArray() :\n\n			// Return just the object\n			( num < 0 ? this[ this.length + num ] : this[ num ] );\n	},\n\n	// Take an array of elements and push it onto the stack\n	// (returning the new matched element set)\n	pushStack: function( elems ) {\n\n		// Build a new jQuery matched element set\n		var ret = jQuery.merge( this.constructor(), elems );\n\n		// Add the old object onto the stack (as a reference)\n		ret.prevObject = this;\n		ret.context = this.context;\n\n		// Return the newly-formed element set\n		return ret;\n	},\n\n	// Execute a callback for every element in the matched set.\n	// (You can seed the arguments with an array of args, but this is\n	// only used internally.)\n	each: function( callback, args ) {\n		return jQuery.each( this, callback, args );\n	},\n\n	ready: function( fn ) {\n		// Add the callback\n		jQuery.ready.promise().done( fn );\n\n		return this;\n	},\n\n	slice: function() {\n		return this.pushStack( core_slice.apply( this, arguments ) );\n	},\n\n	first: function() {\n		return this.eq( 0 );\n	},\n\n	last: function() {\n		return this.eq( -1 );\n	},\n\n	eq: function( i ) {\n		var len = this.length,\n			j = +i + ( i < 0 ? len : 0 );\n		return this.pushStack( j >= 0 && j < len ? [ this[j] ] : [] );\n	},\n\n	map: function( callback ) {\n		return this.pushStack( jQuery.map(this, function( elem, i ) {\n			return callback.call( elem, i, elem );\n		}));\n	},\n\n	end: function() {\n		return this.prevObject || this.constructor(null);\n	},\n\n	// For internal use only.\n	// Behaves like an Array\'s method, not like a jQuery method.\n	push: core_push,\n	sort: [].sort,\n	splice: [].splice\n};\n\n// Give the init function the jQuery prototype for later instantiation\njQuery.fn.init.prototype = jQuery.fn;\n\njQuery.extend = jQuery.fn.extend = function() {\n	var src, copyIsArray, copy, name, options, clone,\n		target = arguments[0] || {},\n		i = 1,\n		length = arguments.length,\n		deep = false;\n\n	// Handle a deep copy situation\n	if ( typeof target === "boolean" ) {\n		deep = target;\n		target = arguments[1] || {};\n		// skip the boolean and the target\n		i = 2;\n	}\n\n	// Handle case when target is a string or something (possible in deep copy)\n	if ( typeof target !== "object" && !jQuery.isFunction(target) ) {\n		target = {};\n	}\n\n	// extend jQuery itself if only one argument is passed\n	if ( length === i ) {\n		target = this;\n		--i;\n	}\n\n	for ( ; i < length; i++ ) {\n		// Only deal with non-null/undefined values\n		if ( (options = arguments[ i ]) != null ) {\n			// Extend the base object\n			for ( name in options ) {\n				src = target[ name ];\n				copy = options[ name ];\n\n				// Prevent never-ending loop\n				if ( target === copy ) {\n					continue;\n				}\n\n				// Recurse if we\'re merging plain objects or arrays\n				if ( deep && copy && ( jQuery.isPlainObject(copy) || (copyIsArray = jQuery.isArray(copy)) ) ) {\n					if ( copyIsArray ) {\n						copyIsArray = false;\n						clone = src && jQuery.isArray(src) ? src : [];\n\n					} else {\n						clone = src && jQuery.isPlainObject(src) ? src : {};\n					}\n\n					// Never move original objects, clone them\n					target[ name ] = jQuery.extend( deep, clone, copy );\n\n				// Don\'t bring in undefined values\n				} else if ( copy !== undefined ) {\n					target[ name ] = copy;\n				}\n			}\n		}\n	}\n\n	// Return the modified object\n	return target;\n};\n\njQuery.extend({\n	noConflict: function( deep ) {\n		if ( window.$ === jQuery ) {\n			window.$ = _$;\n		}\n\n		if ( deep && window.jQuery === jQuery ) {\n			window.jQuery = _jQuery;\n		}\n\n		return jQuery;\n	},\n\n	// Is the DOM ready to be used? Set to true once it occurs.\n	isReady: false,\n\n	// A counter to track how many items to wait for before\n	// the ready event fires. See #6781\n	readyWait: 1,\n\n	// Hold (or release) the ready event\n	holdReady: function( hold ) {\n		if ( hold ) {\n			jQuery.readyWait++;\n		} else {\n			jQuery.ready( true );\n		}\n	},\n\n	// Handle when the DOM is ready\n	ready: function( wait ) {\n\n		// Abort if there are pending holds or we\'re already ready\n		if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {\n			return;\n		}\n\n		// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).\n		if ( !document.body ) {\n			return setTimeout( jQuery.ready );\n		}\n\n		// Remember that the DOM is ready\n		jQuery.isReady = true;\n\n		// If a normal DOM Ready event fired, decrement, and wait if need be\n		if ( wait !== true && --jQuery.readyWait > 0 ) {\n			return;\n		}\n\n		// If there are functions bound, to execute\n		readyList.resolveWith( document, [ jQuery ] );\n\n		// Trigger any bound ready events\n		if ( jQuery.fn.trigger ) {\n			jQuery( document ).trigger("ready").off("ready");\n		}\n	},\n\n	// See test/unit/core.js for details concerning isFunction.\n	// Since version 1.3, DOM methods and functions like alert\n	// aren\'t supported. They return false on IE (#2968).\n	isFunction: function( obj ) {\n		return jQuery.type(obj) === "function";\n	},\n\n	isArray: Array.isArray || function( obj ) {\n		return jQuery.type(obj) === "array";\n	},\n\n	isWindow: function( obj ) {\n		return obj != null && obj == obj.window;\n	},\n\n	isNumeric: function( obj ) {\n		return !isNaN( parseFloat(obj) ) && isFinite( obj );\n	},\n\n	type: function( obj ) {\n		if ( obj == null ) {\n			return String( obj );\n		}\n		return typeof obj === "object" || typeof obj === "function" ?\n			class2type[ core_toString.call(obj) ] || "object" :\n			typeof obj;\n	},\n\n	isPlainObject: function( obj ) {\n		// Must be an Object.\n		// Because of IE, we also have to check the presence of the constructor property.\n		// Make sure that DOM nodes and window objects don\'t pass through, as well\n		if ( !obj || jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {\n			return false;\n		}\n\n		try {\n			// Not own constructor property must be Object\n			if ( obj.constructor &&\n				!core_hasOwn.call(obj, "constructor") &&\n				!core_hasOwn.call(obj.constructor.prototype, "isPrototypeOf") ) {\n				return false;\n			}\n		} catch ( e ) {\n			// IE8,9 Will throw exceptions on certain host objects #9897\n			return false;\n		}\n\n		// Own properties are enumerated firstly, so to speed up,\n		// if last one is own, then all properties are own.\n\n		var key;\n		for ( key in obj ) {}\n\n		return key === undefined || core_hasOwn.call( obj, key );\n	},\n\n	isEmptyObject: function( obj ) {\n		var name;\n		for ( name in obj ) {\n			return false;\n		}\n		return true;\n	},\n\n	error: function( msg ) {\n		throw new Error( msg );\n	},\n\n	// data: string of html\n	// context (optional): If specified, the fragment will be created in this context, defaults to document\n	// keepScripts (optional): If true, will include scripts passed in the html string\n	parseHTML: function( data, context, keepScripts ) {\n		if ( !data || typeof data !== "string" ) {\n			return null;\n		}\n		if ( typeof context === "boolean" ) {\n			keepScripts = context;\n			context = false;\n		}\n		context = context || document;\n\n		var parsed = rsingleTag.exec( data ),\n			scripts = !keepScripts && [];\n\n		// Single tag\n		if ( parsed ) {\n			return [ context.createElement( parsed[1] ) ];\n		}\n\n		parsed = jQuery.buildFragment( [ data ], context, scripts );\n		if ( scripts ) {\n			jQuery( scripts ).remove();\n		}\n		return jQuery.merge( [], parsed.childNodes );\n	},\n\n	parseJSON: function( data ) {\n		// Attempt to parse using the native JSON parser first\n		if ( window.JSON && window.JSON.parse ) {\n			return window.JSON.parse( data );\n		}\n\n		if ( data === null ) {\n			return data;\n		}\n\n		if ( typeof data === "string" ) {\n\n			// Make sure leading/trailing whitespace is removed (IE can\'t handle it)\n			data = jQuery.trim( data );\n\n			if ( data ) {\n				// Make sure the incoming data is actual JSON\n				// Logic borrowed from http://json.org/json2.js\n				if ( rvalidchars.test( data.replace( rvalidescape, "@" )\n					.replace( rvalidtokens, "]" )\n					.replace( rvalidbraces, "")) ) {\n\n					return ( new Function( "return " + data ) )();\n				}\n			}\n		}\n\n		jQuery.error( "Invalid JSON: " + data );\n	},\n\n	// Cross-browser xml parsing\n	parseXML: function( data ) {\n		var xml, tmp;\n		if ( !data || typeof data !== "string" ) {\n			return null;\n		}\n		try {\n			if ( window.DOMParser ) { // Standard\n				tmp = new DOMParser();\n				xml = tmp.parseFromString( data , "text/xml" );\n			} else { // IE\n				xml = new ActiveXObject( "Microsoft.XMLDOM" );\n				xml.async = "false";\n				xml.loadXML( data );\n			}\n		} catch( e ) {\n			xml = undefined;\n		}\n		if ( !xml || !xml.documentElement || xml.getElementsByTagName( "parsererror" ).length ) {\n			jQuery.error( "Invalid XML: " + data );\n		}\n		return xml;\n	},\n\n	noop: function() {},\n\n	// Evaluates a script in a global context\n	// Workarounds based on findings by Jim Driscoll\n	// http://weblogs.java.net/blog/driscoll/archive/2009/09/08/eval-javascript-global-context\n	globalEval: function( data ) {\n		if ( data && jQuery.trim( data ) ) {\n			// We use execScript on Internet Explorer\n			// We use an anonymous function so that context is window\n			// rather than jQuery in Firefox\n			( window.execScript || function( data ) {\n				window[ "eval" ].call( window, data );\n			} )( data );\n		}\n	},\n\n	// Convert dashed to camelCase; used by the css and data modules\n	// Microsoft forgot to hump their vendor prefix (#9572)\n	camelCase: function( string ) {\n		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );\n	},\n\n	nodeName: function( elem, name ) {\n		return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();\n	},\n\n	// args is for internal usage only\n	each: function( obj, callback, args ) {\n		var value,\n			i = 0,\n			length = obj.length,\n			isArray = isArraylike( obj );\n\n		if ( args ) {\n			if ( isArray ) {\n				for ( ; i < length; i++ ) {\n					value = callback.apply( obj[ i ], args );\n\n					if ( value === false ) {\n						break;\n					}\n				}\n			} else {\n				for ( i in obj ) {\n					value = callback.apply( obj[ i ], args );\n\n					if ( value === false ) {\n						break;\n					}\n				}\n			}\n\n		// A special, fast, case for the most common use of each\n		} else {\n			if ( isArray ) {\n				for ( ; i < length; i++ ) {\n					value = callback.call( obj[ i ], i, obj[ i ] );\n\n					if ( value === false ) {\n						break;\n					}\n				}\n			} else {\n				for ( i in obj ) {\n					value = callback.call( obj[ i ], i, obj[ i ] );\n\n					if ( value === false ) {\n						break;\n					}\n				}\n			}\n		}\n\n		return obj;\n	},\n\n	// Use native String.trim function wherever possible\n	trim: core_trim && !core_trim.call("\\uFEFF\\xA0") ?\n		function( text ) {\n			return text == null ?\n				"" :\n				core_trim.call( text );\n		} :\n\n		// Otherwise use our own trimming functionality\n		function( text ) {\n			return text == null ?\n				"" :\n				( text + "" ).replace( rtrim, "" );\n		},\n\n	// results is for internal usage only\n	makeArray: function( arr, results ) {\n		var ret = results || [];\n\n		if ( arr != null ) {\n			if ( isArraylike( Object(arr) ) ) {\n				jQuery.merge( ret,\n					typeof arr === "string" ?\n					[ arr ] : arr\n				);\n			} else {\n				core_push.call( ret, arr );\n			}\n		}\n\n		return ret;\n	},\n\n	inArray: function( elem, arr, i ) {\n		var len;\n\n		if ( arr ) {\n			if ( core_indexOf ) {\n				return core_indexOf.call( arr, elem, i );\n			}\n\n			len = arr.length;\n			i = i ? i < 0 ? Math.max( 0, len + i ) : i : 0;\n\n			for ( ; i < len; i++ ) {\n				// Skip accessing in sparse arrays\n				if ( i in arr && arr[ i ] === elem ) {\n					return i;\n				}\n			}\n		}\n\n		return -1;\n	},\n\n	merge: function( first, second ) {\n		var l = second.length,\n			i = first.length,\n			j = 0;\n\n		if ( typeof l === "number" ) {\n			for ( ; j < l; j++ ) {\n				first[ i++ ] = second[ j ];\n			}\n		} else {\n			while ( second[j] !== undefined ) {\n				first[ i++ ] = second[ j++ ];\n			}\n		}\n\n		first.length = i;\n\n		return first;\n	},\n\n	grep: function( elems, callback, inv ) {\n		var retVal,\n			ret = [],\n			i = 0,\n			length = elems.length;\n		inv = !!inv;\n\n		// Go through the array, only saving the items\n		// that pass the validator function\n		for ( ; i < length; i++ ) {\n			retVal = !!callback( elems[ i ], i );\n			if ( inv !== retVal ) {\n				ret.push( elems[ i ] );\n			}\n		}\n\n		return ret;\n	},\n\n	// arg is for internal usage only\n	map: function( elems, callback, arg ) {\n		var value,\n			i = 0,\n			length = elems.length,\n			isArray = isArraylike( elems ),\n			ret = [];\n\n		// Go through the array, translating each of the items to their\n		if ( isArray ) {\n			for ( ; i < length; i++ ) {\n				value = callback( elems[ i ], i, arg );\n\n				if ( value != null ) {\n					ret[ ret.length ] = value;\n				}\n			}\n\n		// Go through every key on the object,\n		} else {\n			for ( i in elems ) {\n				value = callback( elems[ i ], i, arg );\n\n				if ( value != null ) {\n					ret[ ret.length ] = value;\n				}\n			}\n		}\n\n		// Flatten any nested arrays\n		return core_concat.apply( [], ret );\n	},\n\n	// A global GUID counter for objects\n	guid: 1,\n\n	// Bind a function to a context, optionally partially applying any\n	// arguments.\n	proxy: function( fn, context ) {\n		var args, proxy, tmp;\n\n		if ( typeof context === "string" ) {\n			tmp = fn[ context ];\n			context = fn;\n			fn = tmp;\n		}\n\n		// Quick check to determine if target is callable, in the spec\n		// this throws a TypeError, but we will just return undefined.\n		if ( !jQuery.isFunction( fn ) ) {\n			return undefined;\n		}\n\n		// Simulated bind\n		args = core_slice.call( arguments, 2 );\n		proxy = function() {\n			return fn.apply( context || this, args.concat( core_slice.call( arguments ) ) );\n		};\n\n		// Set the guid of unique handler to the same of original handler, so it can be removed\n		proxy.guid = fn.guid = fn.guid || jQuery.guid++;\n\n		return proxy;\n	},\n\n	// Multifunctional method to get and set values of a collection\n	// The value/s can optionally be executed if it\'s a function\n	access: function( elems, fn, key, value, chainable, emptyGet, raw ) {\n		var i = 0,\n			length = elems.length,\n			bulk = key == null;\n\n		// Sets many values\n		if ( jQuery.type( key ) === "object" ) {\n			chainable = true;\n			for ( i in key ) {\n				jQuery.access( elems, fn, i, key[i], true, emptyGet, raw );\n			}\n\n		// Sets one value\n		} else if ( value !== undefined ) {\n			chainable = true;\n\n			if ( !jQuery.isFunction( value ) ) {\n				raw = true;\n			}\n\n			if ( bulk ) {\n				// Bulk operations run against the entire set\n				if ( raw ) {\n					fn.call( elems, value );\n					fn = null;\n\n				// ...except when executing function values\n				} else {\n					bulk = fn;\n					fn = function( elem, key, value ) {\n						return bulk.call( jQuery( elem ), value );\n					};\n				}\n			}\n\n			if ( fn ) {\n				for ( ; i < length; i++ ) {\n					fn( elems[i], key, raw ? value : value.call( elems[i], i, fn( elems[i], key ) ) );\n				}\n			}\n		}\n\n		return chainable ?\n			elems :\n\n			// Gets\n			bulk ?\n				fn.call( elems ) :\n				length ? fn( elems[0], key ) : emptyGet;\n	},\n\n	now: function() {\n		return ( new Date() ).getTime();\n	}\n});\n\njQuery.ready.promise = function( obj ) {\n	if ( !readyList ) {\n\n		readyList = jQuery.Deferred();\n\n		// Catch cases where $(document).ready() is called after the browser event has already occurred.\n		// we once tried to use readyState "interactive" here, but it caused issues like the one\n		// discovered by ChrisS here: http://bugs.jquery.com/ticket/12282#comment:15\n		if ( document.readyState === "complete" ) {\n			// Handle it asynchronously to allow scripts the opportunity to delay ready\n			setTimeout( jQuery.ready );\n\n		// Standards-based browsers support DOMContentLoaded\n		} else if ( document.addEventListener ) {\n			// Use the handy event callback\n			document.addEventListener( "DOMContentLoaded", completed, false );\n\n			// A fallback to window.onload, that will always work\n			window.addEventListener( "load", completed, false );\n\n		// If IE event model is used\n		} else {\n			// Ensure firing before onload, maybe late but safe also for iframes\n			document.attachEvent( "onreadystatechange", completed );\n\n			// A fallback to window.onload, that will always work\n			window.attachEvent( "onload", completed );\n\n			// If IE and not a frame\n			// continually check to see if the document is ready\n			var top = false;\n\n			try {\n				top = window.frameElement == null && document.documentElement;\n			} catch(e) {}\n\n			if ( top && top.doScroll ) {\n				(function doScrollCheck() {\n					if ( !jQuery.isReady ) {\n\n						try {\n							// Use the trick by Diego Perini\n							// http://javascript.nwbox.com/IEContentLoaded/\n							top.doScroll("left");\n						} catch(e) {\n							return setTimeout( doScrollCheck, 50 );\n						}\n\n						// detach all dom ready events\n						detach();\n\n						// and execute any waiting functions\n						jQuery.ready();\n					}\n				})();\n			}\n		}\n	}\n	return readyList.promise( obj );\n};\n\n// Populate the class2type map\njQuery.each("Boolean Number String Function Array Date RegExp Object Error".split(" "), function(i, name) {\n	class2type[ "[object " + name + "]" ] = name.toLowerCase();\n});\n\nfunction isArraylike( obj ) {\n	var length = obj.length,\n		type = jQuery.type( obj );\n\n	if ( jQuery.isWindow( obj ) ) {\n		return false;\n	}\n\n	if ( obj.nodeType === 1 && length ) {\n		return true;\n	}\n\n	return type === "array" || type !== "function" &&\n		( length === 0 ||\n		typeof length === "number" && length > 0 && ( length - 1 ) in obj );\n}\n\n// All jQuery objects should point back to these\nrootjQuery = jQuery(document);\n// String to Object options format cache\nvar optionsCache = {};\n\n// Convert String-formatted options into Object-formatted ones and store in cache\nfunction createOptions( options ) {\n	var object = optionsCache[ options ] = {};\n	jQuery.each( options.match( core_rnotwhite ) || [], function( _, flag ) {\n		object[ flag ] = true;\n	});\n	return object;\n}\n\n/*\n * Create a callback list using the following parameters:\n *\n *	options: an optional list of space-separated options that will change how\n *			the callback list behaves or a more traditional option object\n *\n * By default a callback list will act like an event callback list and can be\n * "fired" multiple times.\n *\n * Possible options:\n *\n *	once:			will ensure the callback list can only be fired once (like a Deferred)\n *\n *	memory:			will keep track of previous values and will call any callback added\n *					after the list has been fired right away with the latest "memorized"\n *					values (like a Deferred)\n *\n *	unique:			will ensure a callback can only be added once (no duplicate in the list)\n *\n *	stopOnFalse:	interrupt callings when a callback returns false\n *\n */\njQuery.Callbacks = function( options ) {\n\n	// Convert options from String-formatted to Object-formatted if needed\n	// (we check in cache first)\n	options = typeof options === "string" ?\n		( optionsCache[ options ] || createOptions( options ) ) :\n		jQuery.extend( {}, options );\n\n	var // Flag to know if list is currently firing\n		firing,\n		// Last fire value (for non-forgettable lists)\n		memory,\n		// Flag to know if list was already fired\n		fired,\n		// End of the loop when firing\n		firingLength,\n		// Index of currently firing callback (modified by remove if needed)\n		firingIndex,\n		// First callback to fire (used internally by add and fireWith)\n		firingStart,\n		// Actual callback list\n		list = [],\n		// Stack of fire calls for repeatable lists\n		stack = !options.once && [],\n		// Fire callbacks\n		fire = function( data ) {\n			memory = options.memory && data;\n			fired = true;\n			firingIndex = firingStart || 0;\n			firingStart = 0;\n			firingLength = list.length;\n			firing = true;\n			for ( ; list && firingIndex < firingLength; firingIndex++ ) {\n				if ( list[ firingIndex ].apply( data[ 0 ], data[ 1 ] ) === false && options.stopOnFalse ) {\n					memory = false; // To prevent further calls using add\n					break;\n				}\n			}\n			firing = false;\n			if ( list ) {\n				if ( stack ) {\n					if ( stack.length ) {\n						fire( stack.shift() );\n					}\n				} else if ( memory ) {\n					list = [];\n				} else {\n					self.disable();\n				}\n			}\n		},\n		// Actual Callbacks object\n		self = {\n			// Add a callback or a collection of callbacks to the list\n			add: function() {\n				if ( list ) {\n					// First, we save the current length\n					var start = list.length;\n					(function add( args ) {\n						jQuery.each( args, function( _, arg ) {\n							var type = jQuery.type( arg );\n							if ( type === "function" ) {\n								if ( !options.unique || !self.has( arg ) ) {\n									list.push( arg );\n								}\n							} else if ( arg && arg.length && type !== "string" ) {\n								// Inspect recursively\n								add( arg );\n							}\n						});\n					})( arguments );\n					// Do we need to add the callbacks to the\n					// current firing batch?\n					if ( firing ) {\n						firingLength = list.length;\n					// With memory, if we\'re not firing then\n					// we should call right away\n					} else if ( memory ) {\n						firingStart = start;\n						fire( memory );\n					}\n				}\n				return this;\n			},\n			// Remove a callback from the list\n			remove: function() {\n				if ( list ) {\n					jQuery.each( arguments, function( _, arg ) {\n						var index;\n						while( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {\n							list.splice( index, 1 );\n							// Handle firing indexes\n							if ( firing ) {\n								if ( index <= firingLength ) {\n									firingLength--;\n								}\n								if ( index <= firingIndex ) {\n									firingIndex--;\n								}\n							}\n						}\n					});\n				}\n				return this;\n			},\n			// Check if a given callback is in the list.\n			// If no argument is given, return whether or not list has callbacks attached.\n			has: function( fn ) {\n				return fn ? jQuery.inArray( fn, list ) > -1 : !!( list && list.length );\n			},\n			// Remove all callbacks from the list\n			empty: function() {\n				list = [];\n				return this;\n			},\n			// Have the list do nothing anymore\n			disable: function() {\n				list = stack = memory = undefined;\n				return this;\n			},\n			// Is it disabled?\n			disabled: function() {\n				return !list;\n			},\n			// Lock the list in its current state\n			lock: function() {\n				stack = undefined;\n				if ( !memory ) {\n					self.disable();\n				}\n				return this;\n			},\n			// Is it locked?\n			locked: function() {\n				return !stack;\n			},\n			// Call all callbacks with the given context and arguments\n			fireWith: function( context, args ) {\n				args = args || [];\n				args = [ context, args.slice ? args.slice() : args ];\n				if ( list && ( !fired || stack ) ) {\n					if ( firing ) {\n						stack.push( args );\n					} else {\n						fire( args );\n					}\n				}\n				return this;\n			},\n			// Call all the callbacks with the given arguments\n			fire: function() {\n				self.fireWith( this, arguments );\n				return this;\n			},\n			// To know if the callbacks have already been called at least once\n			fired: function() {\n				return !!fired;\n			}\n		};\n\n	return self;\n};\njQuery.extend({\n\n	Deferred: function( func ) {\n		var tuples = [\n				// action, add listener, listener list, final state\n				[ "resolve", "done", jQuery.Callbacks("once memory"), "resolved" ],\n				[ "reject", "fail", jQuery.Callbacks("once memory"), "rejected" ],\n				[ "notify", "progress", jQuery.Callbacks("memory") ]\n			],\n			state = "pending",\n			promise = {\n				state: function() {\n					return state;\n				},\n				always: function() {\n					deferred.done( arguments ).fail( arguments );\n					return this;\n				},\n				then: function( /* fnDone, fnFail, fnProgress */ ) {\n					var fns = arguments;\n					return jQuery.Deferred(function( newDefer ) {\n						jQuery.each( tuples, function( i, tuple ) {\n							var action = tuple[ 0 ],\n								fn = jQuery.isFunction( fns[ i ] ) && fns[ i ];\n							// deferred[ done | fail | progress ] for forwarding actions to newDefer\n							deferred[ tuple[1] ](function() {\n								var returned = fn && fn.apply( this, arguments );\n								if ( returned && jQuery.isFunction( returned.promise ) ) {\n									returned.promise()\n										.done( newDefer.resolve )\n										.fail( newDefer.reject )\n										.progress( newDefer.notify );\n								} else {\n									newDefer[ action + "With" ]( this === promise ? newDefer.promise() : this, fn ? [ returned ] : arguments );\n								}\n							});\n						});\n						fns = null;\n					}).promise();\n				},\n				// Get a promise for this deferred\n				// If obj is provided, the promise aspect is added to the object\n				promise: function( obj ) {\n					return obj != null ? jQuery.extend( obj, promise ) : promise;\n				}\n			},\n			deferred = {};\n\n		// Keep pipe for back-compat\n		promise.pipe = promise.then;\n\n		// Add list-specific methods\n		jQuery.each( tuples, function( i, tuple ) {\n			var list = tuple[ 2 ],\n				stateString = tuple[ 3 ];\n\n			// promise[ done | fail | progress ] = list.add\n			promise[ tuple[1] ] = list.add;\n\n			// Handle state\n			if ( stateString ) {\n				list.add(function() {\n					// state = [ resolved | rejected ]\n					state = stateString;\n\n				// [ reject_list | resolve_list ].disable; progress_list.lock\n				}, tuples[ i ^ 1 ][ 2 ].disable, tuples[ 2 ][ 2 ].lock );\n			}\n\n			// deferred[ resolve | reject | notify ]\n			deferred[ tuple[0] ] = function() {\n				deferred[ tuple[0] + "With" ]( this === deferred ? promise : this, arguments );\n				return this;\n			};\n			deferred[ tuple[0] + "With" ] = list.fireWith;\n		});\n\n		// Make the deferred a promise\n		promise.promise( deferred );\n\n		// Call given func if any\n		if ( func ) {\n			func.call( deferred, deferred );\n		}\n\n		// All done!\n		return deferred;\n	},\n\n	// Deferred helper\n	when: function( subordinate /* , ..., subordinateN */ ) {\n		var i = 0,\n			resolveValues = core_slice.call( arguments ),\n			length = resolveValues.length,\n\n			// the count of uncompleted subordinates\n			remaining = length !== 1 || ( subordinate && jQuery.isFunction( subordinate.promise ) ) ? length : 0,\n\n			// the master Deferred. If resolveValues consist of only a single Deferred, just use that.\n			deferred = remaining === 1 ? subordinate : jQuery.Deferred(),\n\n			// Update function for both resolve and progress values\n			updateFunc = function( i, contexts, values ) {\n				return function( value ) {\n					contexts[ i ] = this;\n					values[ i ] = arguments.length > 1 ? core_slice.call( arguments ) : value;\n					if( values === progressValues ) {\n						deferred.notifyWith( contexts, values );\n					} else if ( !( --remaining ) ) {\n						deferred.resolveWith( contexts, values );\n					}\n				};\n			},\n\n			progressValues, progressContexts, resolveContexts;\n\n		// add listeners to Deferred subordinates; treat others as resolved\n		if ( length > 1 ) {\n			progressValues = new Array( length );\n			progressContexts = new Array( length );\n			resolveContexts = new Array( length );\n			for ( ; i < length; i++ ) {\n				if ( resolveValues[ i ] && jQuery.isFunction( resolveValues[ i ].promise ) ) {\n					resolveValues[ i ].promise()\n						.done( updateFunc( i, resolveContexts, resolveValues ) )\n						.fail( deferred.reject )\n						.progress( updateFunc( i, progressContexts, progressValues ) );\n				} else {\n					--remaining;\n				}\n			}\n		}\n\n		// if we\'re not waiting on anything, resolve the master\n		if ( !remaining ) {\n			deferred.resolveWith( resolveContexts, resolveValues );\n		}\n\n		return deferred.promise();\n	}\n});\njQuery.support = (function() {\n\n	var support, all, a,\n		input, select, fragment,\n		opt, eventName, isSupported, i,\n		div = document.createElement("div");\n\n	// Setup\n	div.setAttribute( "className", "t" );\n	div.innerHTML = "  <link/><table></table><a href=\'/a\'>a</a><input type=\'checkbox\'/>";\n\n	// Support tests won\'t run in some limited or non-browser environments\n	all = div.getElementsByTagName("*");\n	a = div.getElementsByTagName("a")[ 0 ];\n	if ( !all || !a || !all.length ) {\n		return {};\n	}\n\n	// First batch of tests\n	select = document.createElement("select");\n	opt = select.appendChild( document.createElement("option") );\n	input = div.getElementsByTagName("input")[ 0 ];\n\n	a.style.cssText = "top:1px;float:left;opacity:.5";\n	support = {\n		// Test setAttribute on camelCase class. If it works, we need attrFixes when doing get/setAttribute (ie6/7)\n		getSetAttribute: div.className !== "t",\n\n		// IE strips leading whitespace when .innerHTML is used\n		leadingWhitespace: div.firstChild.nodeType === 3,\n\n		// Make sure that tbody elements aren\'t automatically inserted\n		// IE will insert them into empty tables\n		tbody: !div.getElementsByTagName("tbody").length,\n\n		// Make sure that link elements get serialized correctly by innerHTML\n		// This requires a wrapper element in IE\n		htmlSerialize: !!div.getElementsByTagName("link").length,\n\n		// Get the style information from getAttribute\n		// (IE uses .cssText instead)\n		style: /top/.test( a.getAttribute("style") ),\n\n		// Make sure that URLs aren\'t manipulated\n		// (IE normalizes it by default)\n		hrefNormalized: a.getAttribute("href") === "/a",\n\n		// Make sure that element opacity exists\n		// (IE uses filter instead)\n		// Use a regex to work around a WebKit issue. See #5145\n		opacity: /^0.5/.test( a.style.opacity ),\n\n		// Verify style float existence\n		// (IE uses styleFloat instead of cssFloat)\n		cssFloat: !!a.style.cssFloat,\n\n		// Check the default checkbox/radio value ("" on WebKit; "on" elsewhere)\n		checkOn: !!input.value,\n\n		// Make sure that a selected-by-default option has a working selected property.\n		// (WebKit defaults to false instead of true, IE too, if it\'s in an optgroup)\n		optSelected: opt.selected,\n\n		// Tests for enctype support on a form (#6743)\n		enctype: !!document.createElement("form").enctype,\n\n		// Makes sure cloning an html5 element does not cause problems\n		// Where outerHTML is undefined, this still works\n		html5Clone: document.createElement("nav").cloneNode( true ).outerHTML !== "<:nav></:nav>",\n\n		// jQuery.support.boxModel DEPRECATED in 1.8 since we don\'t support Quirks Mode\n		boxModel: document.compatMode === "CSS1Compat",\n\n		// Will be defined later\n		deleteExpando: true,\n		noCloneEvent: true,\n		inlineBlockNeedsLayout: false,\n		shrinkWrapBlocks: false,\n		reliableMarginRight: true,\n		boxSizingReliable: true,\n		pixelPosition: false\n	};\n\n	// Make sure checked status is properly cloned\n	input.checked = true;\n	support.noCloneChecked = input.cloneNode( true ).checked;\n\n	// Make sure that the options inside disabled selects aren\'t marked as disabled\n	// (WebKit marks them as disabled)\n	select.disabled = true;\n	support.optDisabled = !opt.disabled;\n\n	// Support: IE<9\n	try {\n		delete div.test;\n	} catch( e ) {\n		support.deleteExpando = false;\n	}\n\n	// Check if we can trust getAttribute("value")\n	input = document.createElement("input");\n	input.setAttribute( "value", "" );\n	support.input = input.getAttribute( "value" ) === "";\n\n	// Check if an input maintains its value after becoming a radio\n	input.value = "t";\n	input.setAttribute( "type", "radio" );\n	support.radioValue = input.value === "t";\n\n	// #11217 - WebKit loses check when the name is after the checked attribute\n	input.setAttribute( "checked", "t" );\n	input.setAttribute( "name", "t" );\n\n	fragment = document.createDocumentFragment();\n	fragment.appendChild( input );\n\n	// Check if a disconnected checkbox will retain its checked\n	// value of true after appended to the DOM (IE6/7)\n	support.appendChecked = input.checked;\n\n	// WebKit doesn\'t clone checked state correctly in fragments\n	support.checkClone = fragment.cloneNode( true ).cloneNode( true ).lastChild.checked;\n\n	// Support: IE<9\n	// Opera does not clone events (and typeof div.attachEvent === undefined).\n	// IE9-10 clones events bound via attachEvent, but they don\'t trigger with .click()\n	if ( div.attachEvent ) {\n		div.attachEvent( "onclick", function() {\n			support.noCloneEvent = false;\n		});\n\n		div.cloneNode( true ).click();\n	}\n\n	// Support: IE<9 (lack submit/change bubble), Firefox 17+ (lack focusin event)\n	// Beware of CSP restrictions (https://developer.mozilla.org/en/Security/CSP), test/csp.php\n	for ( i in { submit: true, change: true, focusin: true }) {\n		div.setAttribute( eventName = "on" + i, "t" );\n\n		support[ i + "Bubbles" ] = eventName in window || div.attributes[ eventName ].expando === false;\n	}\n\n	div.style.backgroundClip = "content-box";\n	div.cloneNode( true ).style.backgroundClip = "";\n	support.clearCloneStyle = div.style.backgroundClip === "content-box";\n\n	// Run tests that need a body at doc ready\n	jQuery(function() {\n		var container, marginDiv, tds,\n			divReset = "padding:0;margin:0;border:0;display:block;box-sizing:content-box;-moz-box-sizing:content-box;-webkit-box-sizing:content-box;",\n			body = document.getElementsByTagName("body")[0];\n\n		if ( !body ) {\n			// Return for frameset docs that don\'t have a body\n			return;\n		}\n\n		container = document.createElement("div");\n		container.style.cssText = "border:0;width:0;height:0;position:absolute;top:0;left:-9999px;margin-top:1px";\n\n		body.appendChild( container ).appendChild( div );\n\n		// Support: IE8\n		// Check if table cells still have offsetWidth/Height when they are set\n		// to display:none and there are still other visible table cells in a\n		// table row; if so, offsetWidth/Height are not reliable for use when\n		// determining if an element has been hidden directly using\n		// display:none (it is still safe to use offsets if a parent element is\n		// hidden; don safety goggles and see bug #4512 for more information).\n		div.innerHTML = "<table><tr><td></td><td>t</td></tr></table>";\n		tds = div.getElementsByTagName("td");\n		tds[ 0 ].style.cssText = "padding:0;margin:0;border:0;display:none";\n		isSupported = ( tds[ 0 ].offsetHeight === 0 );\n\n		tds[ 0 ].style.display = "";\n		tds[ 1 ].style.display = "none";\n\n		// Support: IE8\n		// Check if empty table cells still have offsetWidth/Height\n		support.reliableHiddenOffsets = isSupported && ( tds[ 0 ].offsetHeight === 0 );\n\n		// Check box-sizing and margin behavior\n		div.innerHTML = "";\n		div.style.cssText = "box-sizing:border-box;-moz-box-sizing:border-box;-webkit-box-sizing:border-box;padding:1px;border:1px;display:block;width:4px;margin-top:1%;position:absolute;top:1%;";\n		support.boxSizing = ( div.offsetWidth === 4 );\n		support.doesNotIncludeMarginInBodyOffset = ( body.offsetTop !== 1 );\n\n		// Use window.getComputedStyle because jsdom on node.js will break without it.\n		if ( window.getComputedStyle ) {\n			support.pixelPosition = ( window.getComputedStyle( div, null ) || {} ).top !== "1%";\n			support.boxSizingReliable = ( window.getComputedStyle( div, null ) || { width: "4px" } ).width === "4px";\n\n			// Check if div with explicit width and no margin-right incorrectly\n			// gets computed margin-right based on width of container. (#3333)\n			// Fails in WebKit before Feb 2011 nightlies\n			// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right\n			marginDiv = div.appendChild( document.createElement("div") );\n			marginDiv.style.cssText = div.style.cssText = divReset;\n			marginDiv.style.marginRight = marginDiv.style.width = "0";\n			div.style.width = "1px";\n\n			support.reliableMarginRight =\n				!parseFloat( ( window.getComputedStyle( marginDiv, null ) || {} ).marginRight );\n		}\n\n		if ( typeof div.style.zoom !== core_strundefined ) {\n			// Support: IE<8\n			// Check if natively block-level elements act like inline-block\n			// elements when setting their display to \'inline\' and giving\n			// them layout\n			div.innerHTML = "";\n			div.style.cssText = divReset + "width:1px;padding:1px;display:inline;zoom:1";\n			support.inlineBlockNeedsLayout = ( div.offsetWidth === 3 );\n\n			// Support: IE6\n			// Check if elements with layout shrink-wrap their children\n			div.style.display = "block";\n			div.innerHTML = "<div></div>";\n			div.firstChild.style.width = "5px";\n			support.shrinkWrapBlocks = ( div.offsetWidth !== 3 );\n\n			if ( support.inlineBlockNeedsLayout ) {\n				// Prevent IE 6 from affecting layout for positioned elements #11048\n				// Prevent IE from shrinking the body in IE 7 mode #12869\n				// Support: IE<8\n				body.style.zoom = 1;\n			}\n		}\n\n		body.removeChild( container );\n\n		// Null elements to avoid leaks in IE\n		container = div = tds = marginDiv = null;\n	});\n\n	// Null elements to avoid leaks in IE\n	all = select = fragment = opt = a = input = null;\n\n	return support;\n})();\n\nvar rbrace = /(?:\\{[\\s\\S]*\\}|\\[[\\s\\S]*\\])$/,\n	rmultiDash = /([A-Z])/g;\n\nfunction internalData( elem, name, data, pvt /* Internal Use Only */ ){\n	if ( !jQuery.acceptData( elem ) ) {\n		return;\n	}\n\n	var thisCache, ret,\n		internalKey = jQuery.expando,\n		getByName = typeof name === "string",\n\n		// We have to handle DOM nodes and JS objects differently because IE6-7\n		// can\'t GC object references properly across the DOM-JS boundary\n		isNode = elem.nodeType,\n\n		// Only DOM nodes need the global jQuery cache; JS object data is\n		// attached directly to the object so GC can occur automatically\n		cache = isNode ? jQuery.cache : elem,\n\n		// Only defining an ID for JS objects if its cache already exists allows\n		// the code to shortcut on the same path as a DOM node with no cache\n		id = isNode ? elem[ internalKey ] : elem[ internalKey ] && internalKey;\n\n	// Avoid doing any more work than we need to when trying to get data on an\n	// object that has no data at all\n	if ( (!id || !cache[id] || (!pvt && !cache[id].data)) && getByName && data === undefined ) {\n		return;\n	}\n\n	if ( !id ) {\n		// Only DOM nodes need a new unique ID for each element since their data\n		// ends up in the global cache\n		if ( isNode ) {\n			elem[ internalKey ] = id = core_deletedIds.pop() || jQuery.guid++;\n		} else {\n			id = internalKey;\n		}\n	}\n\n	if ( !cache[ id ] ) {\n		cache[ id ] = {};\n\n		// Avoids exposing jQuery metadata on plain JS objects when the object\n		// is serialized using JSON.stringify\n		if ( !isNode ) {\n			cache[ id ].toJSON = jQuery.noop;\n		}\n	}\n\n	// An object can be passed to jQuery.data instead of a key/value pair; this gets\n	// shallow copied over onto the existing cache\n	if ( typeof name === "object" || typeof name === "function" ) {\n		if ( pvt ) {\n			cache[ id ] = jQuery.extend( cache[ id ], name );\n		} else {\n			cache[ id ].data = jQuery.extend( cache[ id ].data, name );\n		}\n	}\n\n	thisCache = cache[ id ];\n\n	// jQuery data() is stored in a separate object inside the object\'s internal data\n	// cache in order to avoid key collisions between internal data and user-defined\n	// data.\n	if ( !pvt ) {\n		if ( !thisCache.data ) {\n			thisCache.data = {};\n		}\n\n		thisCache = thisCache.data;\n	}\n\n	if ( data !== undefined ) {\n		thisCache[ jQuery.camelCase( name ) ] = data;\n	}\n\n	// Check for both converted-to-camel and non-converted data property names\n	// If a data property was specified\n	if ( getByName ) {\n\n		// First Try to find as-is property data\n		ret = thisCache[ name ];\n\n		// Test for null|undefined property data\n		if ( ret == null ) {\n\n			// Try to find the camelCased property\n			ret = thisCache[ jQuery.camelCase( name ) ];\n		}\n	} else {\n		ret = thisCache;\n	}\n\n	return ret;\n}\n\nfunction internalRemoveData( elem, name, pvt ) {\n	if ( !jQuery.acceptData( elem ) ) {\n		return;\n	}\n\n	var i, l, thisCache,\n		isNode = elem.nodeType,\n\n		// See jQuery.data for more information\n		cache = isNode ? jQuery.cache : elem,\n		id = isNode ? elem[ jQuery.expando ] : jQuery.expando;\n\n	// If there is already no cache entry for this object, there is no\n	// purpose in continuing\n	if ( !cache[ id ] ) {\n		return;\n	}\n\n	if ( name ) {\n\n		thisCache = pvt ? cache[ id ] : cache[ id ].data;\n\n		if ( thisCache ) {\n\n			// Support array or space separated string names for data keys\n			if ( !jQuery.isArray( name ) ) {\n\n				// try the string as a key before any manipulation\n				if ( name in thisCache ) {\n					name = [ name ];\n				} else {\n\n					// split the camel cased version by spaces unless a key with the spaces exists\n					name = jQuery.camelCase( name );\n					if ( name in thisCache ) {\n						name = [ name ];\n					} else {\n						name = name.split(" ");\n					}\n				}\n			} else {\n				// If "name" is an array of keys...\n				// When data is initially created, via ("key", "val") signature,\n				// keys will be converted to camelCase.\n				// Since there is no way to tell _how_ a key was added, remove\n				// both plain key and camelCase key. #12786\n				// This will only penalize the array argument path.\n				name = name.concat( jQuery.map( name, jQuery.camelCase ) );\n			}\n\n			for ( i = 0, l = name.length; i < l; i++ ) {\n				delete thisCache[ name[i] ];\n			}\n\n			// If there is no data left in the cache, we want to continue\n			// and let the cache object itself get destroyed\n			if ( !( pvt ? isEmptyDataObject : jQuery.isEmptyObject )( thisCache ) ) {\n				return;\n			}\n		}\n	}\n\n	// See jQuery.data for more information\n	if ( !pvt ) {\n		delete cache[ id ].data;\n\n		// Don\'t destroy the parent cache unless the internal data object\n		// had been the only thing left in it\n		if ( !isEmptyDataObject( cache[ id ] ) ) {\n			return;\n		}\n	}\n\n	// Destroy the cache\n	if ( isNode ) {\n		jQuery.cleanData( [ elem ], true );\n\n	// Use delete when supported for expandos or `cache` is not a window per isWindow (#10080)\n	} else if ( jQuery.support.deleteExpando || cache != cache.window ) {\n		delete cache[ id ];\n\n	// When all else fails, null\n	} else {\n		cache[ id ] = null;\n	}\n}\n\njQuery.extend({\n	cache: {},\n\n	// Unique for each copy of jQuery on the page\n	// Non-digits removed to match rinlinejQuery\n	expando: "jQuery" + ( core_version + Math.random() ).replace( /\\D/g, "" ),\n\n	// The following elements throw uncatchable exceptions if you\n	// attempt to add expando properties to them.\n	noData: {\n		"embed": true,\n		// Ban all objects except for Flash (which handle expandos)\n		"object": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",\n		"applet": true\n	},\n\n	hasData: function( elem ) {\n		elem = elem.nodeType ? jQuery.cache[ elem[jQuery.expando] ] : elem[ jQuery.expando ];\n		return !!elem && !isEmptyDataObject( elem );\n	},\n\n	data: function( elem, name, data ) {\n		return internalData( elem, name, data );\n	},\n\n	removeData: function( elem, name ) {\n		return internalRemoveData( elem, name );\n	},\n\n	// For internal use only.\n	_data: function( elem, name, data ) {\n		return internalData( elem, name, data, true );\n	},\n\n	_removeData: function( elem, name ) {\n		return internalRemoveData( elem, name, true );\n	},\n\n	// A method for determining if a DOM node can handle the data expando\n	acceptData: function( elem ) {\n		// Do not set data on non-element because it will not be cleared (#8335).\n		if ( elem.nodeType && elem.nodeType !== 1 && elem.nodeType !== 9 ) {\n			return false;\n		}\n\n		var noData = elem.nodeName && jQuery.noData[ elem.nodeName.toLowerCase() ];\n\n		// nodes accept data unless otherwise specified; rejection can be conditional\n		return !noData || noData !== true && elem.getAttribute("classid") === noData;\n	}\n});\n\njQuery.fn.extend({\n	data: function( key, value ) {\n		var attrs, name,\n			elem = this[0],\n			i = 0,\n			data = null;\n\n		// Gets all values\n		if ( key === undefined ) {\n			if ( this.length ) {\n				data = jQuery.data( elem );\n\n				if ( elem.nodeType === 1 && !jQuery._data( elem, "parsedAttrs" ) ) {\n					attrs = elem.attributes;\n					for ( ; i < attrs.length; i++ ) {\n						name = attrs[i].name;\n\n						if ( !name.indexOf( "data-" ) ) {\n							name = jQuery.camelCase( name.slice(5) );\n\n							dataAttr( elem, name, data[ name ] );\n						}\n					}\n					jQuery._data( elem, "parsedAttrs", true );\n				}\n			}\n\n			return data;\n		}\n\n		// Sets multiple values\n		if ( typeof key === "object" ) {\n			return this.each(function() {\n				jQuery.data( this, key );\n			});\n		}\n\n		return jQuery.access( this, function( value ) {\n\n			if ( value === undefined ) {\n				// Try to fetch any internally stored data first\n				return elem ? dataAttr( elem, key, jQuery.data( elem, key ) ) : null;\n			}\n\n			this.each(function() {\n				jQuery.data( this, key, value );\n			});\n		}, null, value, arguments.length > 1, null, true );\n	},\n\n	removeData: function( key ) {\n		return this.each(function() {\n			jQuery.removeData( this, key );\n		});\n	}\n});\n\nfunction dataAttr( elem, key, data ) {\n	// If nothing was found internally, try to fetch any\n	// data from the HTML5 data-* attribute\n	if ( data === undefined && elem.nodeType === 1 ) {\n\n		var name = "data-" + key.replace( rmultiDash, "-$1" ).toLowerCase();\n\n		data = elem.getAttribute( name );\n\n		if ( typeof data === "string" ) {\n			try {\n				data = data === "true" ? true :\n					data === "false" ? false :\n					data === "null" ? null :\n					// Only convert to a number if it doesn\'t change the string\n					+data + "" === data ? +data :\n					rbrace.test( data ) ? jQuery.parseJSON( data ) :\n						data;\n			} catch( e ) {}\n\n			// Make sure we set the data so it isn\'t changed later\n			jQuery.data( elem, key, data );\n\n		} else {\n			data = undefined;\n		}\n	}\n\n	return data;\n}\n\n// checks a cache object for emptiness\nfunction isEmptyDataObject( obj ) {\n	var name;\n	for ( name in obj ) {\n\n		// if the public data object is empty, the private is still empty\n		if ( name === "data" && jQuery.isEmptyObject( obj[name] ) ) {\n			continue;\n		}\n		if ( name !== "toJSON" ) {\n			return false;\n		}\n	}\n\n	return true;\n}\njQuery.extend({\n	queue: function( elem, type, data ) {\n		var queue;\n\n		if ( elem ) {\n			type = ( type || "fx" ) + "queue";\n			queue = jQuery._data( elem, type );\n\n			// Speed up dequeue by getting out quickly if this is just a lookup\n			if ( data ) {\n				if ( !queue || jQuery.isArray(data) ) {\n					queue = jQuery._data( elem, type, jQuery.makeArray(data) );\n				} else {\n					queue.push( data );\n				}\n			}\n			return queue || [];\n		}\n	},\n\n	dequeue: function( elem, type ) {\n		type = type || "fx";\n\n		var queue = jQuery.queue( elem, type ),\n			startLength = queue.length,\n			fn = queue.shift(),\n			hooks = jQuery._queueHooks( elem, type ),\n			next = function() {\n				jQuery.dequeue( elem, type );\n			};\n\n		// If the fx queue is dequeued, always remove the progress sentinel\n		if ( fn === "inprogress" ) {\n			fn = queue.shift();\n			startLength--;\n		}\n\n		hooks.cur = fn;\n		if ( fn ) {\n\n			// Add a progress sentinel to prevent the fx queue from being\n			// automatically dequeued\n			if ( type === "fx" ) {\n				queue.unshift( "inprogress" );\n			}\n\n			// clear up the last queue stop function\n			delete hooks.stop;\n			fn.call( elem, next, hooks );\n		}\n\n		if ( !startLength && hooks ) {\n			hooks.empty.fire();\n		}\n	},\n\n	// not intended for public consumption - generates a queueHooks object, or returns the current one\n	_queueHooks: function( elem, type ) {\n		var key = type + "queueHooks";\n		return jQuery._data( elem, key ) || jQuery._data( elem, key, {\n			empty: jQuery.Callbacks("once memory").add(function() {\n				jQuery._removeData( elem, type + "queue" );\n				jQuery._removeData( elem, key );\n			})\n		});\n	}\n});\n\njQuery.fn.extend({\n	queue: function( type, data ) {\n		var setter = 2;\n\n		if ( typeof type !== "string" ) {\n			data = type;\n			type = "fx";\n			setter--;\n		}\n\n		if ( arguments.length < setter ) {\n			return jQuery.queue( this[0], type );\n		}\n\n		return data === undefined ?\n			this :\n			this.each(function() {\n				var queue = jQuery.queue( this, type, data );\n\n				// ensure a hooks for this queue\n				jQuery._queueHooks( this, type );\n\n				if ( type === "fx" && queue[0] !== "inprogress" ) {\n					jQuery.dequeue( this, type );\n				}\n			});\n	},\n	dequeue: function( type ) {\n		return this.each(function() {\n			jQuery.dequeue( this, type );\n		});\n	},\n	// Based off of the plugin by Clint Helfers, with permission.\n	// http://blindsignals.com/index.php/2009/07/jquery-delay/\n	delay: function( time, type ) {\n		time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;\n		type = type || "fx";\n\n		return this.queue( type, function( next, hooks ) {\n			var timeout = setTimeout( next, time );\n			hooks.stop = function() {\n				clearTimeout( timeout );\n			};\n		});\n	},\n	clearQueue: function( type ) {\n		return this.queue( type || "fx", [] );\n	},\n	// Get a promise resolved when queues of a certain type\n	// are emptied (fx is the type by default)\n	promise: function( type, obj ) {\n		var tmp,\n			count = 1,\n			defer = jQuery.Deferred(),\n			elements = this,\n			i = this.length,\n			resolve = function() {\n				if ( !( --count ) ) {\n					defer.resolveWith( elements, [ elements ] );\n				}\n			};\n\n		if ( typeof type !== "string" ) {\n			obj = type;\n			type = undefined;\n		}\n		type = type || "fx";\n\n		while( i-- ) {\n			tmp = jQuery._data( elements[ i ], type + "queueHooks" );\n			if ( tmp && tmp.empty ) {\n				count++;\n				tmp.empty.add( resolve );\n			}\n		}\n		resolve();\n		return defer.promise( obj );\n	}\n});\nvar nodeHook, boolHook,\n	rclass = /[\\t\\r\\n]/g,\n	rreturn = /\\r/g,\n	rfocusable = /^(?:input|select|textarea|button|object)$/i,\n	rclickable = /^(?:a|area)$/i,\n	rboolean = /^(?:checked|selected|autofocus|autoplay|async|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped)$/i,\n	ruseDefault = /^(?:checked|selected)$/i,\n	getSetAttribute = jQuery.support.getSetAttribute,\n	getSetInput = jQuery.support.input;\n\njQuery.fn.extend({\n	attr: function( name, value ) {\n		return jQuery.access( this, jQuery.attr, name, value, arguments.length > 1 );\n	},\n\n	removeAttr: function( name ) {\n		return this.each(function() {\n			jQuery.removeAttr( this, name );\n		});\n	},\n\n	prop: function( name, value ) {\n		return jQuery.access( this, jQuery.prop, name, value, arguments.length > 1 );\n	},\n\n	removeProp: function( name ) {\n		name = jQuery.propFix[ name ] || name;\n		return this.each(function() {\n			// try/catch handles cases where IE balks (such as removing a property on window)\n			try {\n				this[ name ] = undefined;\n				delete this[ name ];\n			} catch( e ) {}\n		});\n	},\n\n	addClass: function( value ) {\n		var classes, elem, cur, clazz, j,\n			i = 0,\n			len = this.length,\n			proceed = typeof value === "string" && value;\n\n		if ( jQuery.isFunction( value ) ) {\n			return this.each(function( j ) {\n				jQuery( this ).addClass( value.call( this, j, this.className ) );\n			});\n		}\n\n		if ( proceed ) {\n			// The disjunction here is for better compressibility (see removeClass)\n			classes = ( value || "" ).match( core_rnotwhite ) || [];\n\n			for ( ; i < len; i++ ) {\n				elem = this[ i ];\n				cur = elem.nodeType === 1 && ( elem.className ?\n					( " " + elem.className + " " ).replace( rclass, " " ) :\n					" "\n				);\n\n				if ( cur ) {\n					j = 0;\n					while ( (clazz = classes[j++]) ) {\n						if ( cur.indexOf( " " + clazz + " " ) < 0 ) {\n							cur += clazz + " ";\n						}\n					}\n					elem.className = jQuery.trim( cur );\n\n				}\n			}\n		}\n\n		return this;\n	},\n\n	removeClass: function( value ) {\n		var classes, elem, cur, clazz, j,\n			i = 0,\n			len = this.length,\n			proceed = arguments.length === 0 || typeof value === "string" && value;\n\n		if ( jQuery.isFunction( value ) ) {\n			return this.each(function( j ) {\n				jQuery( this ).removeClass( value.call( this, j, this.className ) );\n			});\n		}\n		if ( proceed ) {\n			classes = ( value || "" ).match( core_rnotwhite ) || [];\n\n			for ( ; i < len; i++ ) {\n				elem = this[ i ];\n				// This expression is here for better compressibility (see addClass)\n				cur = elem.nodeType === 1 && ( elem.className ?\n					( " " + elem.className + " " ).replace( rclass, " " ) :\n					""\n				);\n\n				if ( cur ) {\n					j = 0;\n					while ( (clazz = classes[j++]) ) {\n						// Remove *all* instances\n						while ( cur.indexOf( " " + clazz + " " ) >= 0 ) {\n							cur = cur.replace( " " + clazz + " ", " " );\n						}\n					}\n					elem.className = value ? jQuery.trim( cur ) : "";\n				}\n			}\n		}\n\n		return this;\n	},\n\n	toggleClass: function( value, stateVal ) {\n		var type = typeof value,\n			isBool = typeof stateVal === "boolean";\n\n		if ( jQuery.isFunction( value ) ) {\n			return this.each(function( i ) {\n				jQuery( this ).toggleClass( value.call(this, i, this.className, stateVal), stateVal );\n			});\n		}\n\n		return this.each(function() {\n			if ( type === "string" ) {\n				// toggle individual class names\n				var className,\n					i = 0,\n					self = jQuery( this ),\n					state = stateVal,\n					classNames = value.match( core_rnotwhite ) || [];\n\n				while ( (className = classNames[ i++ ]) ) {\n					// check each className given, space separated list\n					state = isBool ? state : !self.hasClass( className );\n					self[ state ? "addClass" : "removeClass" ]( className );\n				}\n\n			// Toggle whole class name\n			} else if ( type === core_strundefined || type === "boolean" ) {\n				if ( this.className ) {\n					// store className if set\n					jQuery._data( this, "__className__", this.className );\n				}\n\n				// If the element has a class name or if we\'re passed "false",\n				// then remove the whole classname (if there was one, the above saved it).\n				// Otherwise bring back whatever was previously saved (if anything),\n				// falling back to the empty string if nothing was stored.\n				this.className = this.className || value === false ? "" : jQuery._data( this, "__className__" ) || "";\n			}\n		});\n	},\n\n	hasClass: function( selector ) {\n		var className = " " + selector + " ",\n			i = 0,\n			l = this.length;\n		for ( ; i < l; i++ ) {\n			if ( this[i].nodeType === 1 && (" " + this[i].className + " ").replace(rclass, " ").indexOf( className ) >= 0 ) {\n				return true;\n			}\n		}\n\n		return false;\n	},\n\n	val: function( value ) {\n		var ret, hooks, isFunction,\n			elem = this[0];\n\n		if ( !arguments.length ) {\n			if ( elem ) {\n				hooks = jQuery.valHooks[ elem.type ] || jQuery.valHooks[ elem.nodeName.toLowerCase() ];\n\n				if ( hooks && "get" in hooks && (ret = hooks.get( elem, "value" )) !== undefined ) {\n					return ret;\n				}\n\n				ret = elem.value;\n\n				return typeof ret === "string" ?\n					// handle most common string cases\n					ret.replace(rreturn, "") :\n					// handle cases where value is null/undef or number\n					ret == null ? "" : ret;\n			}\n\n			return;\n		}\n\n		isFunction = jQuery.isFunction( value );\n\n		return this.each(function( i ) {\n			var val,\n				self = jQuery(this);\n\n			if ( this.nodeType !== 1 ) {\n				return;\n			}\n\n			if ( isFunction ) {\n				val = value.call( this, i, self.val() );\n			} else {\n				val = value;\n			}\n\n			// Treat null/undefined as ""; convert numbers to string\n			if ( val == null ) {\n				val = "";\n			} else if ( typeof val === "number" ) {\n				val += "";\n			} else if ( jQuery.isArray( val ) ) {\n				val = jQuery.map(val, function ( value ) {\n					return value == null ? "" : value + "";\n				});\n			}\n\n			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];\n\n			// If set returns undefined, fall back to normal setting\n			if ( !hooks || !("set" in hooks) || hooks.set( this, val, "value" ) === undefined ) {\n				this.value = val;\n			}\n		});\n	}\n});\n\njQuery.extend({\n	valHooks: {\n		option: {\n			get: function( elem ) {\n				// attributes.value is undefined in Blackberry 4.7 but\n				// uses .value. See #6932\n				var val = elem.attributes.value;\n				return !val || val.specified ? elem.value : elem.text;\n			}\n		},\n		select: {\n			get: function( elem ) {\n				var value, option,\n					options = elem.options,\n					index = elem.selectedIndex,\n					one = elem.type === "select-one" || index < 0,\n					values = one ? null : [],\n					max = one ? index + 1 : options.length,\n					i = index < 0 ?\n						max :\n						one ? index : 0;\n\n				// Loop through all the selected options\n				for ( ; i < max; i++ ) {\n					option = options[ i ];\n\n					// oldIE doesn\'t update selected after form reset (#2551)\n					if ( ( option.selected || i === index ) &&\n							// Don\'t return options that are disabled or in a disabled optgroup\n							( jQuery.support.optDisabled ? !option.disabled : option.getAttribute("disabled") === null ) &&\n							( !option.parentNode.disabled || !jQuery.nodeName( option.parentNode, "optgroup" ) ) ) {\n\n						// Get the specific value for the option\n						value = jQuery( option ).val();\n\n						// We don\'t need an array for one selects\n						if ( one ) {\n							return value;\n						}\n\n						// Multi-Selects return an array\n						values.push( value );\n					}\n				}\n\n				return values;\n			},\n\n			set: function( elem, value ) {\n				var values = jQuery.makeArray( value );\n\n				jQuery(elem).find("option").each(function() {\n					this.selected = jQuery.inArray( jQuery(this).val(), values ) >= 0;\n				});\n\n				if ( !values.length ) {\n					elem.selectedIndex = -1;\n				}\n				return values;\n			}\n		}\n	},\n\n	attr: function( elem, name, value ) {\n		var hooks, notxml, ret,\n			nType = elem.nodeType;\n\n		// don\'t get/set attributes on text, comment and attribute nodes\n		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {\n			return;\n		}\n\n		// Fallback to prop when attributes are not supported\n		if ( typeof elem.getAttribute === core_strundefined ) {\n			return jQuery.prop( elem, name, value );\n		}\n\n		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );\n\n		// All attributes are lowercase\n		// Grab necessary hook if one is defined\n		if ( notxml ) {\n			name = name.toLowerCase();\n			hooks = jQuery.attrHooks[ name ] || ( rboolean.test( name ) ? boolHook : nodeHook );\n		}\n\n		if ( value !== undefined ) {\n\n			if ( value === null ) {\n				jQuery.removeAttr( elem, name );\n\n			} else if ( hooks && notxml && "set" in hooks && (ret = hooks.set( elem, value, name )) !== undefined ) {\n				return ret;\n\n			} else {\n				elem.setAttribute( name, value + "" );\n				return value;\n			}\n\n		} else if ( hooks && notxml && "get" in hooks && (ret = hooks.get( elem, name )) !== null ) {\n			return ret;\n\n		} else {\n\n			// In IE9+, Flash objects don\'t have .getAttribute (#12945)\n			// Support: IE9+\n			if ( typeof elem.getAttribute !== core_strundefined ) {\n				ret =  elem.getAttribute( name );\n			}\n\n			// Non-existent attributes return null, we normalize to undefined\n			return ret == null ?\n				undefined :\n				ret;\n		}\n	},\n\n	removeAttr: function( elem, value ) {\n		var name, propName,\n			i = 0,\n			attrNames = value && value.match( core_rnotwhite );\n\n		if ( attrNames && elem.nodeType === 1 ) {\n			while ( (name = attrNames[i++]) ) {\n				propName = jQuery.propFix[ name ] || name;\n\n				// Boolean attributes get special treatment (#10870)\n				if ( rboolean.test( name ) ) {\n					// Set corresponding property to false for boolean attributes\n					// Also clear defaultChecked/defaultSelected (if appropriate) for IE<8\n					if ( !getSetAttribute && ruseDefault.test( name ) ) {\n						elem[ jQuery.camelCase( "default-" + name ) ] =\n							elem[ propName ] = false;\n					} else {\n						elem[ propName ] = false;\n					}\n\n				// See #9699 for explanation of this approach (setting first, then removal)\n				} else {\n					jQuery.attr( elem, name, "" );\n				}\n\n				elem.removeAttribute( getSetAttribute ? name : propName );\n			}\n		}\n	},\n\n	attrHooks: {\n		type: {\n			set: function( elem, value ) {\n				if ( !jQuery.support.radioValue && value === "radio" && jQuery.nodeName(elem, "input") ) {\n					// Setting the type on a radio button after the value resets the value in IE6-9\n					// Reset value to default in case type is set after value during creation\n					var val = elem.value;\n					elem.setAttribute( "type", value );\n					if ( val ) {\n						elem.value = val;\n					}\n					return value;\n				}\n			}\n		}\n	},\n\n	propFix: {\n		tabindex: "tabIndex",\n		readonly: "readOnly",\n		"for": "htmlFor",\n		"class": "className",\n		maxlength: "maxLength",\n		cellspacing: "cellSpacing",\n		cellpadding: "cellPadding",\n		rowspan: "rowSpan",\n		colspan: "colSpan",\n		usemap: "useMap",\n		frameborder: "frameBorder",\n		contenteditable: "contentEditable"\n	},\n\n	prop: function( elem, name, value ) {\n		var ret, hooks, notxml,\n			nType = elem.nodeType;\n\n		// don\'t get/set properties on text, comment and attribute nodes\n		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {\n			return;\n		}\n\n		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );\n\n		if ( notxml ) {\n			// Fix name and attach hooks\n			name = jQuery.propFix[ name ] || name;\n			hooks = jQuery.propHooks[ name ];\n		}\n\n		if ( value !== undefined ) {\n			if ( hooks && "set" in hooks && (ret = hooks.set( elem, value, name )) !== undefined ) {\n				return ret;\n\n			} else {\n				return ( elem[ name ] = value );\n			}\n\n		} else {\n			if ( hooks && "get" in hooks && (ret = hooks.get( elem, name )) !== null ) {\n				return ret;\n\n			} else {\n				return elem[ name ];\n			}\n		}\n	},\n\n	propHooks: {\n		tabIndex: {\n			get: function( elem ) {\n				// elem.tabIndex doesn\'t always return the correct value when it hasn\'t been explicitly set\n				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/\n				var attributeNode = elem.getAttributeNode("tabindex");\n\n				return attributeNode && attributeNode.specified ?\n					parseInt( attributeNode.value, 10 ) :\n					rfocusable.test( elem.nodeName ) || rclickable.test( elem.nodeName ) && elem.href ?\n						0 :\n						undefined;\n			}\n		}\n	}\n});\n\n// Hook for boolean attributes\nboolHook = {\n	get: function( elem, name ) {\n		var\n			// Use .prop to determine if this attribute is understood as boolean\n			prop = jQuery.prop( elem, name ),\n\n			// Fetch it accordingly\n			attr = typeof prop === "boolean" && elem.getAttribute( name ),\n			detail = typeof prop === "boolean" ?\n\n				getSetInput && getSetAttribute ?\n					attr != null :\n					// oldIE fabricates an empty string for missing boolean attributes\n					// and conflates checked/selected into attroperties\n					ruseDefault.test( name ) ?\n						elem[ jQuery.camelCase( "default-" + name ) ] :\n						!!attr :\n\n				// fetch an attribute node for properties not recognized as boolean\n				elem.getAttributeNode( name );\n\n		return detail && detail.value !== false ?\n			name.toLowerCase() :\n			undefined;\n	},\n	set: function( elem, value, name ) {\n		if ( value === false ) {\n			// Remove boolean attributes when set to false\n			jQuery.removeAttr( elem, name );\n		} else if ( getSetInput && getSetAttribute || !ruseDefault.test( name ) ) {\n			// IE<8 needs the *property* name\n			elem.setAttribute( !getSetAttribute && jQuery.propFix[ name ] || name, name );\n\n		// Use defaultChecked and defaultSelected for oldIE\n		} else {\n			elem[ jQuery.camelCase( "default-" + name ) ] = elem[ name ] = true;\n		}\n\n		return name;\n	}\n};\n\n// fix oldIE value attroperty\nif ( !getSetInput || !getSetAttribute ) {\n	jQuery.attrHooks.value = {\n		get: function( elem, name ) {\n			var ret = elem.getAttributeNode( name );\n			return jQuery.nodeName( elem, "input" ) ?\n\n				// Ignore the value *property* by using defaultValue\n				elem.defaultValue :\n\n				ret && ret.specified ? ret.value : undefined;\n		},\n		set: function( elem, value, name ) {\n			if ( jQuery.nodeName( elem, "input" ) ) {\n				// Does not return so that setAttribute is also used\n				elem.defaultValue = value;\n			} else {\n				// Use nodeHook if defined (#1954); otherwise setAttribute is fine\n				return nodeHook && nodeHook.set( elem, value, name );\n			}\n		}\n	};\n}\n\n// IE6/7 do not support getting/setting some attributes with get/setAttribute\nif ( !getSetAttribute ) {\n\n	// Use this for any attribute in IE6/7\n	// This fixes almost every IE6/7 issue\n	nodeHook = jQuery.valHooks.button = {\n		get: function( elem, name ) {\n			var ret = elem.getAttributeNode( name );\n			return ret && ( name === "id" || name === "name" || name === "coords" ? ret.value !== "" : ret.specified ) ?\n				ret.value :\n				undefined;\n		},\n		set: function( elem, value, name ) {\n			// Set the existing or create a new attribute node\n			var ret = elem.getAttributeNode( name );\n			if ( !ret ) {\n				elem.setAttributeNode(\n					(ret = elem.ownerDocument.createAttribute( name ))\n				);\n			}\n\n			ret.value = value += "";\n\n			// Break association with cloned elements by also using setAttribute (#9646)\n			return name === "value" || value === elem.getAttribute( name ) ?\n				value :\n				undefined;\n		}\n	};\n\n	// Set contenteditable to false on removals(#10429)\n	// Setting to empty string throws an error as an invalid value\n	jQuery.attrHooks.contenteditable = {\n		get: nodeHook.get,\n		set: function( elem, value, name ) {\n			nodeHook.set( elem, value === "" ? false : value, name );\n		}\n	};\n\n	// Set width and height to auto instead of 0 on empty string( Bug #8150 )\n	// This is for removals\n	jQuery.each([ "width", "height" ], function( i, name ) {\n		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {\n			set: function( elem, value ) {\n				if ( value === "" ) {\n					elem.setAttribute( name, "auto" );\n					return value;\n				}\n			}\n		});\n	});\n}\n\n\n// Some attributes require a special call on IE\n// http://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx\nif ( !jQuery.support.hrefNormalized ) {\n	jQuery.each([ "href", "src", "width", "height" ], function( i, name ) {\n		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {\n			get: function( elem ) {\n				var ret = elem.getAttribute( name, 2 );\n				return ret == null ? undefined : ret;\n			}\n		});\n	});\n\n	// href/src property should get the full normalized URL (#10299/#12915)\n	jQuery.each([ "href", "src" ], function( i, name ) {\n		jQuery.propHooks[ name ] = {\n			get: function( elem ) {\n				return elem.getAttribute( name, 4 );\n			}\n		};\n	});\n}\n\nif ( !jQuery.support.style ) {\n	jQuery.attrHooks.style = {\n		get: function( elem ) {\n			// Return undefined in the case of empty string\n			// Note: IE uppercases css property names, but if we were to .toLowerCase()\n			// .cssText, that would destroy case senstitivity in URL\'s, like in "background"\n			return elem.style.cssText || undefined;\n		},\n		set: function( elem, value ) {\n			return ( elem.style.cssText = value + "" );\n		}\n	};\n}\n\n// Safari mis-reports the default selected property of an option\n// Accessing the parent\'s selectedIndex property fixes it\nif ( !jQuery.support.optSelected ) {\n	jQuery.propHooks.selected = jQuery.extend( jQuery.propHooks.selected, {\n		get: function( elem ) {\n			var parent = elem.parentNode;\n\n			if ( parent ) {\n				parent.selectedIndex;\n\n				// Make sure that it also works with optgroups, see #5701\n				if ( parent.parentNode ) {\n					parent.parentNode.selectedIndex;\n				}\n			}\n			return null;\n		}\n	});\n}\n\n// IE6/7 call enctype encoding\nif ( !jQuery.support.enctype ) {\n	jQuery.propFix.enctype = "encoding";\n}\n\n// Radios and checkboxes getter/setter\nif ( !jQuery.support.checkOn ) {\n	jQuery.each([ "radio", "checkbox" ], function() {\n		jQuery.valHooks[ this ] = {\n			get: function( elem ) {\n				// Handle the case where in Webkit "" is returned instead of "on" if a value isn\'t specified\n				return elem.getAttribute("value") === null ? "on" : elem.value;\n			}\n		};\n	});\n}\njQuery.each([ "radio", "checkbox" ], function() {\n	jQuery.valHooks[ this ] = jQuery.extend( jQuery.valHooks[ this ], {\n		set: function( elem, value ) {\n			if ( jQuery.isArray( value ) ) {\n				return ( elem.checked = jQuery.inArray( jQuery(elem).val(), value ) >= 0 );\n			}\n		}\n	});\n});\nvar rformElems = /^(?:input|select|textarea)$/i,\n	rkeyEvent = /^key/,\n	rmouseEvent = /^(?:mouse|contextmenu)|click/,\n	rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,\n	rtypenamespace = /^([^.]*)(?:\\.(.+)|)$/;\n\nfunction returnTrue() {\n	return true;\n}\n\nfunction returnFalse() {\n	return false;\n}\n\n/*\n * Helper functions for managing events -- not part of the public interface.\n * Props to Dean Edwards\' addEvent library for many of the ideas.\n */\njQuery.event = {\n\n	global: {},\n\n	add: function( elem, types, handler, data, selector ) {\n		var tmp, events, t, handleObjIn,\n			special, eventHandle, handleObj,\n			handlers, type, namespaces, origType,\n			elemData = jQuery._data( elem );\n\n		// Don\'t attach events to noData or text/comment nodes (but allow plain objects)\n		if ( !elemData ) {\n			return;\n		}\n\n		// Caller can pass in an object of custom data in lieu of the handler\n		if ( handler.handler ) {\n			handleObjIn = handler;\n			handler = handleObjIn.handler;\n			selector = handleObjIn.selector;\n		}\n\n		// Make sure that the handler has a unique ID, used to find/remove it later\n		if ( !handler.guid ) {\n			handler.guid = jQuery.guid++;\n		}\n\n		// Init the element\'s event structure and main handler, if this is the first\n		if ( !(events = elemData.events) ) {\n			events = elemData.events = {};\n		}\n		if ( !(eventHandle = elemData.handle) ) {\n			eventHandle = elemData.handle = function( e ) {\n				// Discard the second event of a jQuery.event.trigger() and\n				// when an event is called after a page has unloaded\n				return typeof jQuery !== core_strundefined && (!e || jQuery.event.triggered !== e.type) ?\n					jQuery.event.dispatch.apply( eventHandle.elem, arguments ) :\n					undefined;\n			};\n			// Add elem as a property of the handle fn to prevent a memory leak with IE non-native events\n			eventHandle.elem = elem;\n		}\n\n		// Handle multiple events separated by a space\n		// jQuery(...).bind("mouseover mouseout", fn);\n		types = ( types || "" ).match( core_rnotwhite ) || [""];\n		t = types.length;\n		while ( t-- ) {\n			tmp = rtypenamespace.exec( types[t] ) || [];\n			type = origType = tmp[1];\n			namespaces = ( tmp[2] || "" ).split( "." ).sort();\n\n			// If event changes its type, use the special event handlers for the changed type\n			special = jQuery.event.special[ type ] || {};\n\n			// If selector defined, determine special event api type, otherwise given type\n			type = ( selector ? special.delegateType : special.bindType ) || type;\n\n			// Update special based on newly reset type\n			special = jQuery.event.special[ type ] || {};\n\n			// handleObj is passed to all event handlers\n			handleObj = jQuery.extend({\n				type: type,\n				origType: origType,\n				data: data,\n				handler: handler,\n				guid: handler.guid,\n				selector: selector,\n				needsContext: selector && jQuery.expr.match.needsContext.test( selector ),\n				namespace: namespaces.join(".")\n			}, handleObjIn );\n\n			// Init the event handler queue if we\'re the first\n			if ( !(handlers = events[ type ]) ) {\n				handlers = events[ type ] = [];\n				handlers.delegateCount = 0;\n\n				// Only use addEventListener/attachEvent if the special events handler returns false\n				if ( !special.setup || special.setup.call( elem, data, namespaces, eventHandle ) === false ) {\n					// Bind the global event handler to the element\n					if ( elem.addEventListener ) {\n						elem.addEventListener( type, eventHandle, false );\n\n					} else if ( elem.attachEvent ) {\n						elem.attachEvent( "on" + type, eventHandle );\n					}\n				}\n			}\n\n			if ( special.add ) {\n				special.add.call( elem, handleObj );\n\n				if ( !handleObj.handler.guid ) {\n					handleObj.handler.guid = handler.guid;\n				}\n			}\n\n			// Add to the element\'s handler list, delegates in front\n			if ( selector ) {\n				handlers.splice( handlers.delegateCount++, 0, handleObj );\n			} else {\n				handlers.push( handleObj );\n			}\n\n			// Keep track of which events have ever been used, for event optimization\n			jQuery.event.global[ type ] = true;\n		}\n\n		// Nullify elem to prevent memory leaks in IE\n		elem = null;\n	},\n\n	// Detach an event or set of events from an element\n	remove: function( elem, types, handler, selector, mappedTypes ) {\n		var j, handleObj, tmp,\n			origCount, t, events,\n			special, handlers, type,\n			namespaces, origType,\n			elemData = jQuery.hasData( elem ) && jQuery._data( elem );\n\n		if ( !elemData || !(events = elemData.events) ) {\n			return;\n		}\n\n		// Once for each type.namespace in types; type may be omitted\n		types = ( types || "" ).match( core_rnotwhite ) || [""];\n		t = types.length;\n		while ( t-- ) {\n			tmp = rtypenamespace.exec( types[t] ) || [];\n			type = origType = tmp[1];\n			namespaces = ( tmp[2] || "" ).split( "." ).sort();\n\n			// Unbind all events (on this namespace, if provided) for the element\n			if ( !type ) {\n				for ( type in events ) {\n					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );\n				}\n				continue;\n			}\n\n			special = jQuery.event.special[ type ] || {};\n			type = ( selector ? special.delegateType : special.bindType ) || type;\n			handlers = events[ type ] || [];\n			tmp = tmp[2] && new RegExp( "(^|\\\\.)" + namespaces.join("\\\\.(?:.*\\\\.|)") + "(\\\\.|$)" );\n\n			// Remove matching events\n			origCount = j = handlers.length;\n			while ( j-- ) {\n				handleObj = handlers[ j ];\n\n				if ( ( mappedTypes || origType === handleObj.origType ) &&\n					( !handler || handler.guid === handleObj.guid ) &&\n					( !tmp || tmp.test( handleObj.namespace ) ) &&\n					( !selector || selector === handleObj.selector || selector === "**" && handleObj.selector ) ) {\n					handlers.splice( j, 1 );\n\n					if ( handleObj.selector ) {\n						handlers.delegateCount--;\n					}\n					if ( special.remove ) {\n						special.remove.call( elem, handleObj );\n					}\n				}\n			}\n\n			// Remove generic event handler if we removed something and no more handlers exist\n			// (avoids potential for endless recursion during removal of special event handlers)\n			if ( origCount && !handlers.length ) {\n				if ( !special.teardown || special.teardown.call( elem, namespaces, elemData.handle ) === false ) {\n					jQuery.removeEvent( elem, type, elemData.handle );\n				}\n\n				delete events[ type ];\n			}\n		}\n\n		// Remove the expando if it\'s no longer used\n		if ( jQuery.isEmptyObject( events ) ) {\n			delete elemData.handle;\n\n			// removeData also checks for emptiness and clears the expando if empty\n			// so use it instead of delete\n			jQuery._removeData( elem, "events" );\n		}\n	},\n\n	trigger: function( event, data, elem, onlyHandlers ) {\n		var handle, ontype, cur,\n			bubbleType, special, tmp, i,\n			eventPath = [ elem || document ],\n			type = core_hasOwn.call( event, "type" ) ? event.type : event,\n			namespaces = core_hasOwn.call( event, "namespace" ) ? event.namespace.split(".") : [];\n\n		cur = tmp = elem = elem || document;\n\n		// Don\'t do events on text and comment nodes\n		if ( elem.nodeType === 3 || elem.nodeType === 8 ) {\n			return;\n		}\n\n		// focus/blur morphs to focusin/out; ensure we\'re not firing them right now\n		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {\n			return;\n		}\n\n		if ( type.indexOf(".") >= 0 ) {\n			// Namespaced trigger; create a regexp to match event type in handle()\n			namespaces = type.split(".");\n			type = namespaces.shift();\n			namespaces.sort();\n		}\n		ontype = type.indexOf(":") < 0 && "on" + type;\n\n		// Caller can pass in a jQuery.Event object, Object, or just an event type string\n		event = event[ jQuery.expando ] ?\n			event :\n			new jQuery.Event( type, typeof event === "object" && event );\n\n		event.isTrigger = true;\n		event.namespace = namespaces.join(".");\n		event.namespace_re = event.namespace ?\n			new RegExp( "(^|\\\\.)" + namespaces.join("\\\\.(?:.*\\\\.|)") + "(\\\\.|$)" ) :\n			null;\n\n		// Clean up the event in case it is being reused\n		event.result = undefined;\n		if ( !event.target ) {\n			event.target = elem;\n		}\n\n		// Clone any incoming data and prepend the event, creating the handler arg list\n		data = data == null ?\n			[ event ] :\n			jQuery.makeArray( data, [ event ] );\n\n		// Allow special events to draw outside the lines\n		special = jQuery.event.special[ type ] || {};\n		if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {\n			return;\n		}\n\n		// Determine event propagation path in advance, per W3C events spec (#9951)\n		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)\n		if ( !onlyHandlers && !special.noBubble && !jQuery.isWindow( elem ) ) {\n\n			bubbleType = special.delegateType || type;\n			if ( !rfocusMorph.test( bubbleType + type ) ) {\n				cur = cur.parentNode;\n			}\n			for ( ; cur; cur = cur.parentNode ) {\n				eventPath.push( cur );\n				tmp = cur;\n			}\n\n			// Only add window if we got to document (e.g., not plain obj or detached DOM)\n			if ( tmp === (elem.ownerDocument || document) ) {\n				eventPath.push( tmp.defaultView || tmp.parentWindow || window );\n			}\n		}\n\n		// Fire handlers on the event path\n		i = 0;\n		while ( (cur = eventPath[i++]) && !event.isPropagationStopped() ) {\n\n			event.type = i > 1 ?\n				bubbleType :\n				special.bindType || type;\n\n			// jQuery handler\n			handle = ( jQuery._data( cur, "events" ) || {} )[ event.type ] && jQuery._data( cur, "handle" );\n			if ( handle ) {\n				handle.apply( cur, data );\n			}\n\n			// Native handler\n			handle = ontype && cur[ ontype ];\n			if ( handle && jQuery.acceptData( cur ) && handle.apply && handle.apply( cur, data ) === false ) {\n				event.preventDefault();\n			}\n		}\n		event.type = type;\n\n		// If nobody prevented the default action, do it now\n		if ( !onlyHandlers && !event.isDefaultPrevented() ) {\n\n			if ( (!special._default || special._default.apply( elem.ownerDocument, data ) === false) &&\n				!(type === "click" && jQuery.nodeName( elem, "a" )) && jQuery.acceptData( elem ) ) {\n\n				// Call a native DOM method on the target with the same name name as the event.\n				// Can\'t use an .isFunction() check here because IE6/7 fails that test.\n				// Don\'t do default actions on window, that\'s where global variables be (#6170)\n				if ( ontype && elem[ type ] && !jQuery.isWindow( elem ) ) {\n\n					// Don\'t re-trigger an onFOO event when we call its FOO() method\n					tmp = elem[ ontype ];\n\n					if ( tmp ) {\n						elem[ ontype ] = null;\n					}\n\n					// Prevent re-triggering of the same event, since we already bubbled it above\n					jQuery.event.triggered = type;\n					try {\n						elem[ type ]();\n					} catch ( e ) {\n						// IE<9 dies on focus/blur to hidden element (#1486,#12518)\n						// only reproducible on winXP IE8 native, not IE9 in IE8 mode\n					}\n					jQuery.event.triggered = undefined;\n\n					if ( tmp ) {\n						elem[ ontype ] = tmp;\n					}\n				}\n			}\n		}\n\n		return event.result;\n	},\n\n	dispatch: function( event ) {\n\n		// Make a writable jQuery.Event from the native event object\n		event = jQuery.event.fix( event );\n\n		var i, ret, handleObj, matched, j,\n			handlerQueue = [],\n			args = core_slice.call( arguments ),\n			handlers = ( jQuery._data( this, "events" ) || {} )[ event.type ] || [],\n			special = jQuery.event.special[ event.type ] || {};\n\n		// Use the fix-ed jQuery.Event rather than the (read-only) native event\n		args[0] = event;\n		event.delegateTarget = this;\n\n		// Call the preDispatch hook for the mapped type, and let it bail if desired\n		if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {\n			return;\n		}\n\n		// Determine handlers\n		handlerQueue = jQuery.event.handlers.call( this, event, handlers );\n\n		// Run delegates first; they may want to stop propagation beneath us\n		i = 0;\n		while ( (matched = handlerQueue[ i++ ]) && !event.isPropagationStopped() ) {\n			event.currentTarget = matched.elem;\n\n			j = 0;\n			while ( (handleObj = matched.handlers[ j++ ]) && !event.isImmediatePropagationStopped() ) {\n\n				// Triggered event must either 1) have no namespace, or\n				// 2) have namespace(s) a subset or equal to those in the bound event (both can have no namespace).\n				if ( !event.namespace_re || event.namespace_re.test( handleObj.namespace ) ) {\n\n					event.handleObj = handleObj;\n					event.data = handleObj.data;\n\n					ret = ( (jQuery.event.special[ handleObj.origType ] || {}).handle || handleObj.handler )\n							.apply( matched.elem, args );\n\n					if ( ret !== undefined ) {\n						if ( (event.result = ret) === false ) {\n							event.preventDefault();\n							event.stopPropagation();\n						}\n					}\n				}\n			}\n		}\n\n		// Call the postDispatch hook for the mapped type\n		if ( special.postDispatch ) {\n			special.postDispatch.call( this, event );\n		}\n\n		return event.result;\n	},\n\n	handlers: function( event, handlers ) {\n		var sel, handleObj, matches, i,\n			handlerQueue = [],\n			delegateCount = handlers.delegateCount,\n			cur = event.target;\n\n		// Find delegate handlers\n		// Black-hole SVG <use> instance trees (#13180)\n		// Avoid non-left-click bubbling in Firefox (#3861)\n		if ( delegateCount && cur.nodeType && (!event.button || event.type !== "click") ) {\n\n			for ( ; cur != this; cur = cur.parentNode || this ) {\n\n				// Don\'t check non-elements (#13208)\n				// Don\'t process clicks on disabled elements (#6911, #8165, #11382, #11764)\n				if ( cur.nodeType === 1 && (cur.disabled !== true || event.type !== "click") ) {\n					matches = [];\n					for ( i = 0; i < delegateCount; i++ ) {\n						handleObj = handlers[ i ];\n\n						// Don\'t conflict with Object.prototype properties (#13203)\n						sel = handleObj.selector + " ";\n\n						if ( matches[ sel ] === undefined ) {\n							matches[ sel ] = handleObj.needsContext ?\n								jQuery( sel, this ).index( cur ) >= 0 :\n								jQuery.find( sel, this, null, [ cur ] ).length;\n						}\n						if ( matches[ sel ] ) {\n							matches.push( handleObj );\n						}\n					}\n					if ( matches.length ) {\n						handlerQueue.push({ elem: cur, handlers: matches });\n					}\n				}\n			}\n		}\n\n		// Add the remaining (directly-bound) handlers\n		if ( delegateCount < handlers.length ) {\n			handlerQueue.push({ elem: this, handlers: handlers.slice( delegateCount ) });\n		}\n\n		return handlerQueue;\n	},\n\n	fix: function( event ) {\n		if ( event[ jQuery.expando ] ) {\n			return event;\n		}\n\n		// Create a writable copy of the event object and normalize some properties\n		var i, prop, copy,\n			type = event.type,\n			originalEvent = event,\n			fixHook = this.fixHooks[ type ];\n\n		if ( !fixHook ) {\n			this.fixHooks[ type ] = fixHook =\n				rmouseEvent.test( type ) ? this.mouseHooks :\n				rkeyEvent.test( type ) ? this.keyHooks :\n				{};\n		}\n		copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;\n\n		event = new jQuery.Event( originalEvent );\n\n		i = copy.length;\n		while ( i-- ) {\n			prop = copy[ i ];\n			event[ prop ] = originalEvent[ prop ];\n		}\n\n		// Support: IE<9\n		// Fix target property (#1925)\n		if ( !event.target ) {\n			event.target = originalEvent.srcElement || document;\n		}\n\n		// Support: Chrome 23+, Safari?\n		// Target should not be a text node (#504, #13143)\n		if ( event.target.nodeType === 3 ) {\n			event.target = event.target.parentNode;\n		}\n\n		// Support: IE<9\n		// For mouse/key events, metaKey==false if it\'s undefined (#3368, #11328)\n		event.metaKey = !!event.metaKey;\n\n		return fixHook.filter ? fixHook.filter( event, originalEvent ) : event;\n	},\n\n	// Includes some event props shared by KeyEvent and MouseEvent\n	props: "altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),\n\n	fixHooks: {},\n\n	keyHooks: {\n		props: "char charCode key keyCode".split(" "),\n		filter: function( event, original ) {\n\n			// Add which for key events\n			if ( event.which == null ) {\n				event.which = original.charCode != null ? original.charCode : original.keyCode;\n			}\n\n			return event;\n		}\n	},\n\n	mouseHooks: {\n		props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),\n		filter: function( event, original ) {\n			var body, eventDoc, doc,\n				button = original.button,\n				fromElement = original.fromElement;\n\n			// Calculate pageX/Y if missing and clientX/Y available\n			if ( event.pageX == null && original.clientX != null ) {\n				eventDoc = event.target.ownerDocument || document;\n				doc = eventDoc.documentElement;\n				body = eventDoc.body;\n\n				event.pageX = original

\n			}\n\n			// Add relatedTarget, if necessary\n			if ( !event.relatedTarget && fromElement ) {\n				event.relatedTarget = fromElement === event.target ? original.toElement : fromElement;\n			}\n\n			// Add which for click: 1 === left; 2 === middle; 3 === right\n			// Note: button is not normalized, so don\'t use it\n			if ( !event.which && button !== undefined ) {\n				event.which = ( button & 1 ? 1 : ( button & 2 ? 3 : ( button & 4 ? 2 : 0 ) ) );\n			}\n\n			return event;\n		}\n	},\n\n	special: {\n		load: {\n			// Prevent triggered image.load events from bubbling to window.load\n			noBubble: true\n		},\n		click: {\n			// For checkbox, fire native event so checked state will be right\n			trigger: function() {\n				if ( jQuery.nodeName( this, "input" ) && this.type === "checkbox" && this.click ) {\n					this.click();\n					return false;\n				}\n			}\n		},\n		focus: {\n			// Fire native event if possible so blur/focus sequence is correct\n			trigger: function() {\n				if ( this !== document.activeElement && this.focus ) {\n					try {\n						this.focus();\n						return false;\n					} catch ( e ) {\n						// Support: IE<9\n						// If we error on focus to hidden element (#1486, #12518),\n						// let .trigger() run the handlers\n					}\n				}\n			},\n			delegateType: "focusin"\n		},\n		blur: {\n			trigger: function() {\n				if ( this === document.activeElement && this.blur ) {\n					this.blur();\n					return false;\n				}\n			},\n			delegateType: "focusout"\n		},\n\n		beforeunload: {\n			postDispatch: function( event ) {\n\n				// Even when returnValue equals to undefined Firefox will still show alert\n				if ( event.result !== undefined ) {\n					event.originalEvent.returnValue = event.result;\n				}\n			}\n		}\n	},\n\n	simulate: function( type, elem, event, bubble ) {\n		// Piggyback on a donor event to simulate a different one.\n		// Fake originalEvent to avoid donor\'s stopPropagation, but if the\n		// simulated event prevents default then we do the same on the donor.\n		var e = jQuery.extend(\n			new jQuery.Event(),\n			event,\n			{ type: type,\n				isSimulated: true,\n				originalEvent: {}\n			}\n		);\n		if ( bubble ) {\n			jQuery.event.trigger( e, null, elem );\n		} else {\n			jQuery.event.dispatch.call( elem, e );\n		}\n		if ( e.isDefaultPrevented() ) {\n			event.preventDefault();\n		}\n	}\n};\n\njQuery.removeEvent = document.removeEventListener ?\n	function( elem, type, handle ) {\n		if ( elem.removeEventListener ) {\n			elem.removeEventListener( type, handle, false );\n		}\n	} :\n	function( elem, type, handle ) {\n		var name = "on" + type;\n\n		if ( elem.detachEvent ) {\n\n			// #8545, #7054, preventing memory leaks for custom events in IE6-8\n			// detachEvent needed property on element, by name of that event, to properly expose it to GC\n			if ( typeof elem[ name ] === core_strundefined ) {\n				elem[ name ] = null;\n			}\n\n			elem.detachEvent( name, handle );\n		}\n	};\n\njQuery.Event = function( src, props ) {\n	// Allow instantiation without the \'new\' keyword\n	if ( !(this instanceof jQuery.Event) ) {\n		return new jQuery.Event( src, props );\n	}\n\n	// Event object\n	if ( src && src.type ) {\n		this.originalEvent = src;\n		this.type = src.type;\n\n		// Events bubbling up the document may have been marked as prevented\n		// by a handler lower down the tree; reflect the correct value.\n		this.isDefaultPrevented = ( src.defaultPrevented || src.returnValue === false ||\n			src.getPreventDefault && src.getPreventDefault() ) ? returnTrue : returnFalse;\n\n	// Event type\n	} else {\n		this.type = src;\n	}\n\n	// Put explicitly provided properties onto the event object\n	if ( props ) {\n		jQuery.extend( this, props );\n	}\n\n	// Create a timestamp if incoming event doesn\'t have one\n	this.timeStamp = src && src.timeStamp || jQuery.now();\n\n	// Mark it as fixed\n	this[ jQuery.expando ] = true;\n};\n\n// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding\n// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html\njQuery.Event.prototype = {\n	isDefaultPrevented: returnFalse,\n	isPropagationStopped: returnFalse,\n	isImmediatePropagationStopped: returnFalse,\n\n	preventDefault: function() {\n		var e = this.originalEvent;\n\n		this.isDefaultPrevented = returnTrue;\n		if ( !e ) {\n			return;\n		}\n\n		// If preventDefault exists, run it on the original event\n		if ( e.preventDefault ) {\n			e.preventDefault();\n\n		// Support: IE\n		// Otherwise set the returnValue property of the original event to false\n		} else {\n			e.returnValue = false;\n		}\n	},\n	stopPropagation: function() {\n		var e = this.originalEvent;\n\n		this.isPropagationStopped = returnTrue;\n		if ( !e ) {\n			return;\n		}\n		// If stopPropagation exists, run it on the original event\n		if ( e.stopPropagation ) {\n			e.stopPropagation();\n		}\n\n		// Support: IE\n		// Set the cancelBubble property of the original event to true\n		e.cancelBubble = true;\n	},\n	stopImmediatePropagation: function() {\n		this.isImmediatePropagationStopped = returnTrue;\n		this.stopPropagation();\n	}\n};\n\n// Create mouseenter/leave events using mouseover/out and event-time checks\njQuery.each({\n	mouseenter: "mouseover",\n	mouseleave: "mouseout"\n}, function( orig, fix ) {\n	jQuery.event.special[ orig ] = {\n		delegateType: fix,\n		bindType: fix,\n\n		handle: function( event ) {\n			var ret,\n				target = this,\n				related = event.relatedTarget,\n				handleObj = event.handleObj;\n\n			// For mousenter/leave call the handler if related is outside the target.\n			// NB: No relatedTarget if the mouse left/entered the browser window\n			if ( !related || (related !== target && !jQuery.contains( target, related )) ) {\n				event.type = handleObj.origType;\n				ret = handleObj.handler.apply( this, arguments );\n				event.type = fix;\n			}\n			return ret;\n		}\n	};\n});\n\n// IE submit delegation\nif ( !jQuery.support.submitBubbles ) {\n\n	jQuery.event.special.submit = {\n		setup: function() {\n			// Only need this for delegated form submit events\n			if ( jQuery.nodeName( this, "form" ) ) {\n				return false;\n			}\n\n			// Lazy-add a submit handler when a descendant form may potentially be submitted\n			jQuery.event.add( this, "click._submit keypress._submit", function( e ) {\n				// Node name check avoids a VML-related crash in IE (#9807)\n				var elem = e.target,\n					form = jQuery.nodeName( elem, "input" ) || jQuery.nodeName( elem, "button" ) ? elem.form : undefined;\n				if ( form && !jQuery._data( form, "submitBubbles" ) ) {\n					jQuery.event.add( form, "submit._submit", function( event ) {\n						event._submit_bubble = true;\n					});\n					jQuery._data( form, "submitBubbles", true );\n				}\n			});\n			// return undefined since we don\'t need an event listener\n		},\n\n		postDispatch: function( event ) {\n			// If form was submitted by the user, bubble the event up the tree\n			if ( event._submit_bubble ) {\n				delete event._submit_bubble;\n				if ( this.parentNode && !event.isTrigger ) {\n					jQuery.event.simulate( "submit", this.parentNode, event, true );\n				}\n			}\n		},\n\n		teardown: function() {\n			// Only need this for delegated form submit events\n			if ( jQuery.nodeName( this, "form" ) ) {\n				return false;\n			}\n\n			// Remove delegated handlers; cleanData eventually reaps submit handlers attached above\n			jQuery.event.remove( this, "._submit" );\n		}\n	};\n}\n\n// IE change delegation and checkbox/radio fix\nif ( !jQuery.support.changeBubbles ) {\n\n	jQuery.event.special.change = {\n\n		setup: function() {\n\n			if ( rformElems.test( this.nodeName ) ) {\n				// IE doesn\'t fire change on a check/radio until blur; trigger it on click\n				// after a propertychange. Eat the blur-change in special.change.handle.\n				// This still fires onchange a second time for check/radio after blur.\n				if ( this.type === "checkbox" || this.type === "radio" ) {\n					jQuery.event.add( this, "propertychange._change", function( event ) {\n						if ( event.originalEvent.propertyName === "checked" ) {\n							this._just_changed = true;\n						}\n					});\n					jQuery.event.add( this, "click._change", function( event ) {\n						if ( this._just_changed && !event.isTrigger ) {\n							this._just_changed = false;\n						}\n						// Allow triggered, simulated change events (#11500)\n						jQuery.event.simulate( "change", this, event, true );\n					});\n				}\n				return false;\n			}\n			// Delegated event; lazy-add a change handler on descendant inputs\n			jQuery.event.add( this, "beforeactivate._change", function( e ) {\n				var elem = e.target;\n\n				if ( rformElems.test( elem.nodeName ) && !jQuery._data( elem, "changeBubbles" ) ) {\n					jQuery.event.add( elem, "change._change", function( event ) {\n						if ( this.parentNode && !event.isSimulated && !event.isTrigger ) {\n							jQuery.event.simulate( "change", this.parentNode, event, true );\n						}\n					});\n					jQuery._data( elem, "changeBubbles", true );\n				}\n			});\n		},\n\n		handle: function( event ) {\n			var elem = event.target;\n\n			// Swallow native change events from checkbox/radio, we already triggered them above\n			if ( this !== elem || event.isSimulated || event.isTrigger || (elem.type !== "radio" && elem.type !== "checkbox") ) {\n				return event.handleObj.handler.apply( this, arguments );\n			}\n		},\n\n		teardown: function() {\n			jQuery.event.remove( this, "._change" );\n\n			return !rformElems.test( this.nodeName );\n		}\n	};\n}\n\n// Create "bubbling" focus and blur events\nif ( !jQuery.support.focusinBubbles ) {\n	jQuery.each({ focus: "focusin", blur: "focusout" }, function( orig, fix ) {\n\n		// Attach a single capturing handler while someone wants focusin/focusout\n		var attaches = 0,\n			handler = function( event ) {\n				jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ), true );\n			};\n\n		jQuery.event.special[ fix ] = {\n			setup: function() {\n				if ( attaches++ === 0 ) {\n					document.addEventListener( orig, handler, true );\n				}\n			},\n			teardown: function() {\n				if ( --attaches === 0 ) {\n					document.removeEventListener( orig, handler, true );\n				}\n			}\n		};\n	});\n}\n\njQuery.fn.extend({\n\n	on: function( types, selector, data, fn, /*INTERNAL*/ one ) {\n		var type, origFn;\n\n		// Types can be a map of types/handlers\n		if ( typeof types === "object" ) {\n			// ( types-Object, selector, data )\n			if ( typeof selector !== "string" ) {\n				// ( types-Object, data )\n				data = data || selector;\n				selector = undefined;\n			}\n			for ( type in types ) {\n				this.on( type, selector, data, types[ type ], one );\n			}\n			return this;\n		}\n\n		if ( data == null && fn == null ) {\n			// ( types, fn )\n			fn = selector;\n			data = selector = undefined;\n		} else if ( fn == null ) {\n			if ( typeof selector === "string" ) {\n				// ( types, selector, fn )\n				fn = data;\n				data = undefined;\n			} else {\n				// ( types, data, fn )\n				fn = data;\n				data = selector;\n				selector = undefined;\n			}\n		}\n		if ( fn === false ) {\n			fn = returnFalse;\n		} else if ( !fn ) {\n			return this;\n		}\n\n		if ( one === 1 ) {\n			origFn = fn;\n			fn = function( event ) {\n				// Can use an empty set, since event contains the info\n				jQuery().off( event );\n				return origFn.apply( this, arguments );\n			};\n			// Use same guid so caller can remove using origFn\n			fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );\n		}\n		return this.each( function() {\n			jQuery.event.add( this, types, fn, data, selector );\n		});\n	},\n	one: function( types, selector, data, fn ) {\n		return this.on( types, selector, data, fn, 1 );\n	},\n	off: function( types, selector, fn ) {\n		var handleObj, type;\n		if ( types && types.preventDefault && types.handleObj ) {\n			// ( event )  dispatched jQuery.Event\n			handleObj = types.handleObj;\n			jQuery( types.delegateTarget ).off(\n				handleObj.namespace ? handleObj.origType + "." + handleObj.namespace : handleObj.origType,\n				handleObj.selector,\n				handleObj.handler\n			);\n			return this;\n		}\n		if ( typeof types === "object" ) {\n			// ( types-object [, selector] )\n			for ( type in types ) {\n				this.off( type, selector, types[ type ] );\n			}\n			return this;\n		}\n		if ( selector === false || typeof selector === "function" ) {\n			// ( types [, fn] )\n			fn = selector;\n			selector = undefined;\n		}\n		if ( fn === false ) {\n			fn = returnFalse;\n		}\n		return this.each(function() {\n			jQuery.event.remove( this, types, fn, selector );\n		});\n	},\n\n	bind: function( types, data, fn ) {\n		return this.on( types, null, data, fn );\n	},\n	unbind: function( types, fn ) {\n		return this.off( types, null, fn );\n	},\n\n	delegate: function( selector, types, data, fn ) {\n		return this.on( types, selector, data, fn );\n	},\n	undelegate: function( selector, types, fn ) {\n		// ( namespace ) or ( selector, types [, fn] )\n		return arguments.length === 1 ? this.off( selector, "**" ) : this.off( types, selector || "**", fn );\n	},\n\n	trigger: function( type, data ) {\n		return this.each(function() {\n			jQuery.event.trigger( type, data, this );\n		});\n	},\n	triggerHandler: function( type, data ) {\n		var elem = this[0];\n		if ( elem ) {\n			return jQuery.event.trigger( type, data, elem, true );\n		}\n	}\n});\n/*!\n * Sizzle CSS Selector Engine\n * Copyright 2012 jQuery Foundation and other contributors\n * Released under the MIT license\n * http://sizzlejs.com/\n */\n(function( window, undefined ) {\n\nvar i,\n	cachedruns,\n	Expr,\n	getText,\n	isXML,\n	compile,\n	hasDuplicate,\n	outermostContext,\n\n	// Local document vars\n	setDocument,\n	document,\n	docElem,\n	documentIsXML,\n	rbuggyQSA,\n	rbuggyMatches,\n	matches,\n	contains,\n	sortOrder,\n\n	// Instance-specific data\n	expando = "sizzle" + -(new Date()),\n	preferredDoc = window.document,\n	support = {},\n	dirruns = 0,\n	done = 0,\n	classCache = createCache(),\n	tokenCache = createCache(),\n	compilerCache = createCache(),\n\n	// General-purpose constants\n	strundefined = typeof undefined,\n	MAX_NEGATIVE = 1 << 31,\n\n	// Array methods\n	arr = [],\n	pop = arr.pop,\n	push = arr.push,\n	slice = arr.slice,\n	// Use a stripped-down indexOf if we can\'t use a native one\n	indexOf = arr.indexOf || function( elem ) {\n		var i = 0,\n			len = this.length;\n		for ( ; i < len; i++ ) {\n			if ( this[i] === elem ) {\n				return i;\n			}\n		}\n		return -1;\n	},\n\n\n	// Regular expressions\n\n	// Whitespace characters http://www.w3.org/TR/css3-selectors/#whitespace\n	whitespace = "[\\\\x20\\\\t\\\\r\\\\n\\\\f]",\n	// http://www.w3.org/TR/css3-syntax/#characters\n	characterEncoding = "(?:\\\\\\\\.|[\\\\w-]|[^\\\\x00-\\\\xa0])+",\n\n	// Loosely modeled on CSS identifier characters\n	// An unquoted value should be a CSS identifier http://www.w3.org/TR/css3-selectors/#attribute-selectors\n	// Proper syntax: http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier\n	identifier = characterEncoding.replace( "w", "w#" ),\n\n	// Acceptable operators http://www.w3.org/TR/selectors/#attribute-selectors\n	operators = "([*^$|!~]?=)",\n	attributes = "\\\\[" + whitespace + "*(" + characterEncoding + ")" + whitespace +\n		"*(?:" + operators + whitespace + "*(?:([\'\\"])((?:\\\\\\\\.|[^\\\\\\\\])*?)\\\\3|(" + identifier + ")|)|)" + whitespace + "*\\\\]",\n\n	// Prefer arguments quoted,\n	//   then not containing pseudos/brackets,\n	//   then attribute selectors/non-parenthetical expressions,\n	//   then anything else\n	// These preferences are here to reduce the number of selectors\n	//   needing tokenize in the PSEUDO preFilter\n	pseudos = ":(" + characterEncoding + ")(?:\\\\((([\'\\"])((?:\\\\\\\\.|[^\\\\\\\\])*?)\\\\3|((?:\\\\\\\\.|[^\\\\\\\\()[\\\\]]|" + attributes.replace( 3, 8 ) + ")*)|.*)\\\\)|)",\n\n	// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter\n	rtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\\\\\])(?:\\\\\\\\.)*)" + whitespace + "+$", "g" ),\n\n	rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),\n	rcombinators = new RegExp( "^" + whitespace + "*([\\\\x20\\\\t\\\\r\\\\n\\\\f>+~])" + whitespace + "*" ),\n	rpseudo = new RegExp( pseudos ),\n	ridentifier = new RegExp( "^" + identifier + "$" ),\n\n	matchExpr = {\n		"ID": new RegExp( "^#(" + characterEncoding + ")" ),\n		"CLASS": new RegExp( "^\\\\.(" + characterEncoding + ")" ),\n		"NAME": new RegExp( "^\\\\[name=[\'\\"]?(" + characterEncoding + ")[\'\\"]?\\\\]" ),\n		"TAG": new RegExp( "^(" + characterEncoding.replace( "w", "w*" ) + ")" ),\n		"ATTR": new RegExp( "^" + attributes ),\n		"PSEUDO": new RegExp( "^" + pseudos ),\n		"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\\\(" + whitespace +\n			"*(even|odd|(([+-]|)(\\\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +\n			"*(\\\\d+)|))" + whitespace + "*\\\\)|)", "i" ),\n		// For use in libraries implementing .is()\n		// We use this for POS matching in `select`\n		"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\\\(" +\n			whitespace + "*((?:-\\\\d)?\\\\d*)" + whitespace + "*\\\\)|)(?=[^-]|$)", "i" )\n	},\n\n	rsibling = /[\\x20\\t\\r\\n\\f]*[+~]/,\n\n	rnative = /^[^{]+\\{\\s*\\[native code/,\n\n	// Easily-parseable/retrievable ID or TAG or CLASS selectors\n	rquickExpr = /^(?:#([\\w-]+)|(\\w+)|\\.([\\w-]+))$/,\n\n	rinputs = /^(?:input|select|textarea|button)$/i,\n	rheader = /^h\\d$/i,\n\n	rescape = /\'|\\\\/g,\n	rattributeQuotes = /\\=[\\x20\\t\\r\\n\\f]*([^\'"\\]]*)[\\x20\\t\\r\\n\\f]*\\]/g,\n\n	// CSS escapes http://www.w3.org/TR/CSS21/syndata.html#escaped-characters\n	runescape = /\\\\([\\da-fA-F]{1,6}[\\x20\\t\\r\\n\\f]?|.)/g,\n	funescape = function( _, escaped ) {\n		var high = "0x" + escaped - 0x10000;\n		// NaN means non-codepoint\n		return high !== high ?\n			escaped :\n			// BMP codepoint\n			high < 0 ?\n				String.fromCharCode( high + 0x10000 ) :\n				// Supplemental Plane codepoint (surrogate pair)\n				String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );\n	};\n\n// Use a stripped-down slice if we can\'t use a native one\ntry {\n	slice.call( preferredDoc.documentElement.childNodes, 0 )[0].nodeType;\n} catch ( e ) {\n	slice = function( i ) {\n		var elem,\n			results = [];\n		while ( (elem = this[i++]) ) {\n			results.push( elem );\n		}\n		return results;\n	};\n}\n\n/**\n * For feature detection\n * @param {Function} fn The function to test for native support\n */\nfunction isNative( fn ) {\n	return rnative.test( fn + "" );\n}\n\n/**\n * Create key-value caches of limited size\n * @returns {Function(string, Object)} Returns the Object data after storing it on itself with\n *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)\n *	deleting the oldest entry\n */\nfunction createCache() {\n	var cache,\n		keys = [];\n\n	return (cache = function( key, value ) {\n		// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)\n		if ( keys.push( key += " " ) > Expr.cacheLength ) {\n			// Only keep the most recent entries\n			delete cache[ keys.shift() ];\n		}\n		return (cache[ key ] = value);\n	});\n}\n\n/**\n * Mark a function for special use by Sizzle\n * @param {Function} fn The function to mark\n */\nfunction markFunction( fn ) {\n	fn[ expando ] = true;\n	return fn;\n}\n\n/**\n * Support testing using an element\n * @param {Function} fn Passed the created div and expects a boolean result\n */\nfunction assert( fn ) {\n	var div = document.createElement("div");\n\n	try {\n		return fn( div );\n	} catch (e) {\n		return false;\n	} finally {\n		// release memory in IE\n		div = null;\n	}\n}\n\nfunction Sizzle( selector, context, results, seed ) {\n	var match, elem, m, nodeType,\n		// QSA vars\n		i, groups, old, nid, newContext, newSelector;\n\n	if ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {\n		setDocument( context );\n	}\n\n	context = context || document;\n	results = results || [];\n\n	if ( !selector || typeof selector !== "string" ) {\n		return results;\n	}\n\n	if ( (nodeType = context.nodeType) !== 1 && nodeType !== 9 ) {\n		return [];\n	}\n\n	if ( !documentIsXML && !seed ) {\n\n		// Shortcuts\n		if ( (match = rquickExpr.exec( selector )) ) {\n			// Speed-up: Sizzle("#ID")\n			if ( (m = match[1]) ) {\n				if ( nodeType === 9 ) {\n					elem = context.getElementById( m );\n					// Check parentNode to catch when Blackberry 4.6 returns\n					// nodes that are no longer in the document #6963\n					if ( elem && elem.parentNode ) {\n						// Handle the case where IE, Opera, and Webkit return items\n						// by name instead of ID\n						if ( elem.id === m ) {\n							results.push( elem );\n							return results;\n						}\n					} else {\n						return results;\n					}\n				} else {\n					// Context is not a document\n					if ( context.ownerDocument && (elem = context.ownerDocument.getElementById( m )) &&\n						contains( context, elem ) && elem.id === m ) {\n						results.push( elem );\n						return results;\n					}\n				}\n\n			// Speed-up: Sizzle("TAG")\n			} else if ( match[2] ) {\n				push.apply( results, slice.call(context.getElementsByTagName( selector ), 0) );\n				return results;\n\n			// Speed-up: Sizzle(".CLASS")\n			} else if ( (m = match[3]) && support.getByClassName && context.getElementsByClassName ) {\n				push.apply( results, slice.call(context.getElementsByClassName( m ), 0) );\n				return results;\n			}\n		}\n\n		// QSA path\n		if ( support.qsa && !rbuggyQSA.test(selector) ) {\n			old = true;\n			nid = expando;\n			newContext = context;\n			newSelector = nodeType === 9 && selector;\n\n			// qSA works strangely on Element-rooted queries\n			// We can work around this by specifying an extra ID on the root\n			// and working up from there (Thanks to Andrew Dupont for the technique)\n			// IE 8 doesn\'t work on object elements\n			if ( nodeType === 1 && context.nodeName.toLowerCase() !== "object" ) {\n				groups = tokenize( selector );\n\n				if ( (old = context.getAttribute("id")) ) {\n					nid = old.replace( rescape, "\\\\$&" );\n				} else {\n					context.setAttribute( "id", nid );\n				}\n				nid = "[id=\'" + nid + "\'] ";\n\n				i = groups.length;\n				while ( i-- ) {\n					groups[i] = nid + toSelector( groups[i] );\n				}\n				newContext = rsibling.test( selector ) && context.parentNode || context;\n				newSelector = groups.join(",");\n			}\n\n			if ( newSelector ) {\n				try {\n					push.apply( results, slice.call( newContext.querySelectorAll(\n						newSelector\n					), 0 ) );\n					return results;\n				} catch(qsaError) {\n				} finally {\n					if ( !old ) {\n						context.removeAttribute("id");\n					}\n				}\n			}\n		}\n	}\n\n	// All others\n	return select( selector.replace( rtrim, "$1" ), context, results, seed );\n}\n\n/**\n * Detect xml\n * @param {Element|Object} elem An element or a document\n */\nisXML = Sizzle.isXML = function( elem ) {\n	// documentElement is verified for cases where it doesn\'t yet exist\n	// (such as loading iframes in IE - #4833)\n	var documentElement = elem && (elem.ownerDocument || elem).documentElement;\n	return documentElement ? documentElement.nodeName !== "HTML" : false;\n};\n\n/**\n * Sets document-related variables once based on the current document\n * @param {Element|Object} [doc] An element or document object to use to set the document\n * @returns {Object} Returns the current document\n */\nsetDocument = Sizzle.setDocument = function( node ) {\n	var doc = node ? node.ownerDocument || node : preferredDoc;\n\n	// If no document and documentElement is available, return\n	if ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {\n		return document;\n	}\n\n	// Set our document\n	document = doc;\n	docElem = doc.documentElement;\n\n	// Support tests\n	documentIsXML = isXML( doc );\n\n	// Check if getElementsByTagName("*") returns only elements\n	support.tagNameNoComments = assert(function( div ) {\n		div.appendChild( doc.createComment("") );\n		return !div.getElementsByTagName("*").length;\n	});\n\n	// Check if attributes should be retrieved by attribute nodes\n	support.attributes = assert(function( div ) {\n		div.innerHTML = "<select></select>";\n		var type = typeof div.lastChild.getAttribute("multiple");\n		// IE8 returns a string for some attributes even when not present\n		return type !== "boolean" && type !== "string";\n	});\n\n	// Check if getElementsByClassName can be trusted\n	support.getByClassName = assert(function( div ) {\n		// Opera can\'t find a second classname (in 9.6)\n		div.innerHTML = "<div class=\'hidden e\'></div><div class=\'hidden\'></div>";\n		if ( !div.getElementsByClassName || !div.getElementsByClassName("e").length ) {\n			return false;\n		}\n\n		// Safari 3.2 caches class attributes and doesn\'t catch changes\n		div.lastChild.className = "e";\n		return div.getElementsByClassName("e").length === 2;\n	});\n\n	// Check if getElementById returns elements by name\n	// Check if getElementsByName privileges form controls or returns elements by ID\n	support.getByName = assert(function( div ) {\n		// Inject content\n		div.id = expando + 0;\n		div.innerHTML = "<a name=\'" + expando + "\'></a><div name=\'" + expando + "\'></div>";\n		docElem.insertBefore( div, docElem.firstChild );\n\n		// Test\n		var pass = doc.getElementsByName &&\n			// buggy browsers will return fewer than the correct 2\n			doc.getElementsByName( expando ).length === 2 +\n			// buggy browsers will return more than the correct 0\n			doc.getElementsByName( expando + 0 ).length;\n		support.getIdNotName = !doc.getElementById( expando );\n\n		// Cleanup\n		docElem.removeChild( div );\n\n		return pass;\n	});\n\n	// IE6/7 return modified attributes\n	Expr.attrHandle = assert(function( div ) {\n		div.innerHTML = "<a href=\'#\'></a>";\n		return div.firstChild && typeof div.firstChild.getAttribute !== strundefined &&\n			div.firstChild.getAttribute("href") === "#";\n	}) ?\n		{} :\n		{\n			"href": function( elem ) {\n				return elem.getAttribute( "href", 2 );\n			},\n			"type": function( elem ) {\n				return elem.getAttribute("type");\n			}\n		};\n\n	// ID find and filter\n	if ( support.getIdNotName ) {\n		Expr.find["ID"] = function( id, context ) {\n			if ( typeof context.getElementById !== strundefined && !documentIsXML ) {\n				var m = context.getElementById( id );\n				// Check parentNode to catch when Blackberry 4.6 returns\n				// nodes that are no longer in the document #6963\n				return m && m.parentNode ? [m] : [];\n			}\n		};\n		Expr.filter["ID"] = function( id ) {\n			var attrId = id.replace( runescape, funescape );\n			return function( elem ) {\n				return elem.getAttribute("id") === attrId;\n			};\n		};\n	} else {\n		Expr.find["ID"] = function( id, context ) {\n			if ( typeof context.getElementById !== strundefined && !documentIsXML ) {\n				var m = context.getElementById( id );\n\n				return m ?\n					m.id === id || typeof m.getAttributeNode !== strundefined && m.getAttributeNode("id").value === id ?\n						[m] :\n						undefined :\n					[];\n			}\n		};\n		Expr.filter["ID"] =  function( id ) {\n			var attrId = id.replace( runescape, funescape );\n			return function( elem ) {\n				var node = typeof elem.getAttributeNode !== strundefined && elem.getAttributeNode("id");\n				return node && node.value === attrId;\n			};\n		};\n	}\n\n	// Tag\n	Expr.find["TAG"] = support.tagNameNoComments ?\n		function( tag, context ) {\n			if ( typeof context.getElementsByTagName !== strundefined ) {\n				return context.getElementsByTagName( tag );\n			}\n		} :\n		function( tag, context ) {\n			var elem,\n				tmp = [],\n				i = 0,\n				results = context.getElementsByTagName( tag );\n\n			// Filter out possible comments\n			if ( tag === "*" ) {\n				while ( (elem = results[i++]) ) {\n					if ( elem.nodeType === 1 ) {\n						tmp.push( elem );\n					}\n				}\n\n				return tmp;\n			}\n			return results;\n		};\n\n	// Name\n	Expr.find["NAME"] = support.getByName && function( tag, context ) {\n		if ( typeof context.getElementsByName !== strundefined ) {\n			return context.getElementsByName( name );\n		}\n	};\n\n	// Class\n	Expr.find["CLASS"] = support.getByClassName && function( className, context ) {\n		if ( typeof context.getElementsByClassName !== strundefined && !documentIsXML ) {\n			return context.getElementsByClassName( className );\n		}\n	};\n\n	// QSA and matchesSelector support\n\n	// matchesSelector(:active) reports false when true (IE9/Opera 11.5)\n	rbuggyMatches = [];\n\n	// qSa(:focus) reports false when true (Chrome 21),\n	// no need to also add to buggyMatches since matches checks buggyQSA\n	// A support test would require too much code (would include document ready)\n	rbuggyQSA = [ ":focus" ];\n\n	if ( (support.qsa = isNative(doc.querySelectorAll)) ) {\n		// Build QSA regex\n		// Regex strategy adopted from Diego Perini\n		assert(function( div ) {\n			// Select is set to empty string on purpose\n			// This is to test IE\'s treatment of not explictly\n			// setting a boolean content attribute,\n			// since its presence should be enough\n			// http://bugs.jquery.com/ticket/12359\n			div.innerHTML = "<select><option selected=\'\'></option></select>";\n\n			// IE8 - Some boolean attributes are not treated correctly\n			if ( !div.querySelectorAll("[selected]").length ) {\n				rbuggyQSA.push( "\\\\[" + whitespace + "*(?:checked|disabled|ismap|multiple|readonly|selected|value)" );\n			}\n\n			// Webkit/Opera - :checked should return selected option elements\n			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n			// IE8 throws error here and will not see later tests\n			if ( !div.querySelectorAll(":checked").length ) {\n				rbuggyQSA.push(":checked");\n			}\n		});\n\n		assert(function( div ) {\n\n			// Opera 10-12/IE8 - ^= $= *= and empty values\n			// Should not select anything\n			div.innerHTML = "<input type=\'hidden\' i=\'\'/>";\n			if ( div.querySelectorAll("[i^=\'\']").length ) {\n				rbuggyQSA.push( "[*^$]=" + whitespace + "*(?:\\"\\"|\'\')" );\n			}\n\n			// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)\n			// IE8 throws error here and will not see later tests\n			if ( !div.querySelectorAll(":enabled").length ) {\n				rbuggyQSA.push( ":enabled", ":disabled" );\n			}\n\n			// Opera 10-11 does not throw on post-comma invalid pseudos\n			div.querySelectorAll("*,:x");\n			rbuggyQSA.push(",.*:");\n		});\n	}\n\n	if ( (support.matchesSelector = isNative( (matches = docElem.matchesSelector ||\n		docElem.mozMatchesSelector ||\n		docElem.webkitMatchesSelector ||\n		docElem.oMatchesSelector ||\n		docElem.msMatchesSelector) )) ) {\n\n		assert(function( div ) {\n			// Check to see if it\'s possible to do matchesSelector\n			// on a disconnected node (IE 9)\n			support.disconnectedMatch = matches.call( div, "div" );\n\n			// This should fail with an exception\n			// Gecko does not error, returns false instead\n			matches.call( div, "[s!=\'\']:x" );\n			rbuggyMatches.push( "!=", pseudos );\n		});\n	}\n\n	rbuggyQSA = new RegExp( rbuggyQSA.join("|") );\n	rbuggyMatches = new RegExp( rbuggyMatches.join("|") );\n\n	// Element contains another\n	// Purposefully does not implement inclusive descendent\n	// As in, an element does not contain itself\n	contains = isNative(docElem.contains) || docElem.compareDocumentPosition ?\n		function( a, b ) {\n			var adown = a.nodeType === 9 ? a.documentElement : a,\n				bup = b && b.parentNode;\n			return a === bup || !!( bup && bup.nodeType === 1 && (\n				adown.contains ?\n					adown.contains( bup ) :\n					a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16\n			));\n		} :\n		function( a, b ) {\n			if ( b ) {\n				while ( (b = b.parentNode) ) {\n					if ( b === a ) {\n						return true;\n					}\n				}\n			}\n			return false;\n		};\n\n	// Document order sorting\n	sortOrder = docElem.compareDocumentPosition ?\n	function( a, b ) {\n		var compare;\n\n		if ( a === b ) {\n			hasDuplicate = true;\n			return 0;\n		}\n\n		if ( (compare = b.compareDocumentPosition && a.compareDocumentPosition && a.compareDocumentPosition( b )) ) {\n			if ( compare & 1 || a.parentNode && a.parentNode.nodeType === 11 ) {\n				if ( a === doc || contains( preferredDoc, a ) ) {\n					return -1;\n				}\n				if ( b === doc || contains( preferredDoc, b ) ) {\n					return 1;\n				}\n				return 0;\n			}\n			return compare & 4 ? -1 : 1;\n		}\n\n		return a.compareDocumentPosition ? -1 : 1;\n	} :\n	function( a, b ) {\n		var cur,\n			i = 0,\n			aup = a.parentNode,\n			bup = b.parentNode,\n			ap = [ a ],\n			bp = [ b ];\n\n		// Exit early if the nodes are identical\n		if ( a === b ) {\n			hasDuplicate = true;\n			return 0;\n\n		// Parentless nodes are either documents or disconnected\n		} else if ( !aup || !bup ) {\n			return a === doc ? -1 :\n				b === doc ? 1 :\n				aup ? -1 :\n				bup ? 1 :\n				0;\n\n		// If the nodes are siblings, we can do a quick check\n		} else if ( aup === bup ) {\n			return siblingCheck( a, b );\n		}\n\n		// Otherwise we need full lists of their ancestors for comparison\n		cur = a;\n		while ( (cur = cur.parentNode) ) {\n			ap.unshift( cur );\n		}\n		cur = b;\n		while ( (cur = cur.parentNode) ) {\n			bp.unshift( cur );\n		}\n\n		// Walk down the tree looking for a discrepancy\n		while ( ap[i] === bp[i] ) {\n			i++;\n		}\n\n		return i ?\n			// Do a sibling check if the nodes have a common ancestor\n			siblingCheck( ap[i], bp[i] ) :\n\n			// Otherwise nodes in our document sort first\n			ap[i] === preferredDoc ? -1 :\n			bp[i] === preferredDoc ? 1 :\n			0;\n	};\n\n	// Always assume the presence of duplicates if sort doesn\'t\n	// pass them to our comparison function (as in Google Chrome).\n	hasDuplicate = false;\n	[0, 0].sort( sortOrder );\n	support.detectDuplicates = hasDuplicate;\n\n	return document;\n};\n\nSizzle.matches = function( expr, elements ) {\n	return Sizzle( expr, null, null, elements );\n};\n\nSizzle.matchesSelector = function( elem, expr ) {\n	// Set document vars if needed\n	if ( ( elem.ownerDocument || elem ) !== document ) {\n		setDocument( elem );\n	}\n\n	// Make sure that attribute selectors are quoted\n	expr = expr.replace( rattributeQuotes, "=\'$1\']" );\n\n	// rbuggyQSA always contains :focus, so no need for an existence check\n	if ( support.matchesSelector && !documentIsXML && (!rbuggyMatches || !rbuggyMatches.test(expr)) && !rbuggyQSA.test(expr) ) {\n		try {\n			var ret = matches.call( elem, expr );\n\n			// IE 9\'s matchesSelector returns false on disconnected nodes\n			if ( ret || support.disconnectedMatch ||\n					// As well, disconnected nodes are said to be in a document\n					// fragment in IE 9\n					elem.document && elem.document.nodeType !== 11 ) {\n				return ret;\n			}\n		} catch(e) {}\n	}\n\n	return Sizzle( expr, document, null, [elem] ).length > 0;\n};\n\nSizzle.contains = function( context, elem ) {\n	// Set document vars if needed\n	if ( ( context.ownerDocument || context ) !== document ) {\n		setDocument( context );\n	}\n	return contains( context, elem );\n};\n\nSizzle.attr = function( elem, name ) {\n	var val;\n\n	// Set document vars if needed\n	if ( ( elem.ownerDocument || elem ) !== document ) {\n		setDocument( elem );\n	}\n\n	if ( !documentIsXML ) {\n		name = name.toLowerCase();\n	}\n	if ( (val = Expr.attrHandle[ name ]) ) {\n		return val( elem );\n	}\n	if ( documentIsXML || support.attributes ) {\n		return elem.getAttribute( name );\n	}\n	return ( (val = elem.getAttributeNode( name )) || elem.getAttribute( name ) ) && elem[ name ] === true ?\n		name :\n		val && val.specified ? val.value : null;\n};\n\nSizzle.error = function( msg ) {\n	throw new Error( "Syntax error, unrecognized expression: " + msg );\n};\n\n// Document sorting and removing duplicates\nSizzle.uniqueSort = function( results ) {\n	var elem,\n		duplicates = [],\n		i = 1,\n		j = 0;\n\n	// Unless we *know* we can detect duplicates, assume their presence\n	hasDuplicate = !support.detectDuplicates;\n	results.sort( sortOrder );\n\n	if ( hasDuplicate ) {\n		for ( ; (elem = results[i]); i++ ) {\n			if ( elem === results[ i - 1 ] ) {\n				j = duplicates.push( i );\n			}\n		}\n		while ( j-- ) {\n			results.splice( duplicates[ j ], 1 );\n		}\n	}\n\n	return results;\n};\n\nfunction siblingCheck( a, b ) {\n	var cur = b && a,\n		diff = cur && ( ~b.sourceIndex || MAX_NEGATIVE ) - ( ~a.sourceIndex || MAX_NEGATIVE );\n\n	// Use IE sourceIndex if available on both nodes\n	if ( diff ) {\n		return diff;\n	}\n\n	// Check if b follows a\n	if ( cur ) {\n		while ( (cur = cur.nextSibling) ) {\n			if ( cur === b ) {\n				return -1;\n			}\n		}\n	}\n\n	return a ? 1 : -1;\n}\n\n// Returns a function to use in pseudos for input types\nfunction createInputPseudo( type ) {\n	return function( elem ) {\n		var name = elem.nodeName.toLowerCase();\n		return name === "input" && elem.type === type;\n	};\n}\n\n// Returns a function to use in pseudos for buttons\nfunction createButtonPseudo( type ) {\n	return function( elem ) {\n		var name = elem.nodeName.toLowerCase();\n		return (name === "input" || name === "button") && elem.type === type;\n	};\n}\n\n// Returns a function to use in pseudos for positionals\nfunction createPositionalPseudo( fn ) {\n	return markFunction(function( argument ) {\n		argument = +argument;\n		return markFunction(function( seed, matches ) {\n			var j,\n				matchIndexes = fn( [], seed.length, argument ),\n				i = matchIndexes.length;\n\n			// Match elements found at the specified indexes\n			while ( i-- ) {\n				if ( seed[ (j = matchIndexes[i]) ] ) {\n					seed[j] = !(matches[j] = seed[j]);\n				}\n			}\n		});\n	});\n}\n\n/**\n * Utility function for retrieving the text value of an array of DOM nodes\n * @param {Array|Element} elem\n */\ngetText = Sizzle.getText = function( elem ) {\n	var node,\n		ret = "",\n		i = 0,\n		nodeType = elem.nodeType;\n\n	if ( !nodeType ) {\n		// If no nodeType, this is expected to be an array\n		for ( ; (node = elem[i]); i++ ) {\n			// Do not traverse comment nodes\n			ret += getText( node );\n		}\n	} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {\n		// Use textContent for elements\n		// innerText usage removed for consistency of new lines (see #11153)\n		if ( typeof elem.textContent === "string" ) {\n			return elem.textContent;\n		} else {\n			// Traverse its children\n			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n				ret += getText( elem );\n			}\n		}\n	} else if ( nodeType === 3 || nodeType === 4 ) {\n		return elem.nodeValue;\n	}\n	// Do not include comment or processing instruction nodes\n\n	return ret;\n};\n\nExpr = Sizzle.selectors = {\n\n	// Can be adjusted by the user\n	cacheLength: 50,\n\n	createPseudo: markFunction,\n\n	match: matchExpr,\n\n	find: {},\n\n	relative: {\n		">": { dir: "parentNode", first: true },\n		" ": { dir: "parentNode" },\n		"+": { dir: "previousSibling", first: true },\n		"~": { dir: "previousSibling" }\n	},\n\n	preFilter: {\n		"ATTR": function( match ) {\n			match[1] = match[1].replace( runescape, funescape );\n\n			// Move the given value to match[3] whether quoted or unquoted\n			match[3] = ( match[4] || match[5] || "" ).replace( runescape, funescape );\n\n			if ( match[2] === "~=" ) {\n				match[3] = " " + match[3] + " ";\n			}\n\n			return match.slice( 0, 4 );\n		},\n\n		"CHILD": function( match ) {\n			/* matches from matchExpr["CHILD"]\n				1 type (only|nth|...)\n				2 what (child|of-type)\n				3 argument (even|odd|\\d*|\\d*n([+-]\\d+)?|...)\n				4 xn-component of xn+y argument ([+-]?\\d*n|)\n				5 sign of xn-component\n				6 x of xn-component\n				7 sign of y-component\n				8 y of y-component\n			*/\n			match[1] = match[1].toLowerCase();\n\n			if ( match[1].slice( 0, 3 ) === "nth" ) {\n				// nth-* requires argument\n				if ( !match[3] ) {\n					Sizzle.error( match[0] );\n				}\n\n				// numeric x and y parameters for Expr.filter.CHILD\n				// remember that false/true cast respectively to 0/1\n				match[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );\n				match[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );\n\n			// other types prohibit arguments\n			} else if ( match[3] ) {\n				Sizzle.error( match[0] );\n			}\n\n			return match;\n		},\n\n		"PSEUDO": function( match ) {\n			var excess,\n				unquoted = !match[5] && match[2];\n\n			if ( matchExpr["CHILD"].test( match[0] ) ) {\n				return null;\n			}\n\n			// Accept quoted arguments as-is\n			if ( match[4] ) {\n				match[2] = match[4];\n\n			// Strip excess characters from unquoted arguments\n			} else if ( unquoted && rpseudo.test( unquoted ) &&\n				// Get excess from tokenize (recursively)\n				(excess = tokenize( unquoted, true )) &&\n				// advance to the next closing parenthesis\n				(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {\n\n				// excess is a negative index\n				match[0] = match[0].slice( 0, excess );\n				match[2] = unquoted.slice( 0, excess );\n			}\n\n			// Return only captures needed by the pseudo filter method (type and argument)\n			return match.slice( 0, 3 );\n		}\n	},\n\n	filter: {\n\n		"TAG": function( nodeName ) {\n			if ( nodeName === "*" ) {\n				return function() { return true; };\n			}\n\n			nodeName = nodeName.replace( runescape, funescape ).toLowerCase();\n			return function( elem ) {\n				return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;\n			};\n		},\n\n		"CLASS": function( className ) {\n			var pattern = classCache[ className + " " ];\n\n			return pattern ||\n				(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&\n				classCache( className, function( elem ) {\n					return pattern.test( elem.className || (typeof elem.getAttribute !== strundefined && elem.getAttribute("class")) || "" );\n				});\n		},\n\n		"ATTR": function( name, operator, check ) {\n			return function( elem ) {\n				var result = Sizzle.attr( elem, name );\n\n				if ( result == null ) {\n					return operator === "!=";\n				}\n				if ( !operator ) {\n					return true;\n				}\n\n				result += "";\n\n				return operator === "=" ? result === check :\n					operator === "!=" ? result !== check :\n					operator === "^=" ? check && result.indexOf( check ) === 0 :\n					operator === "*=" ? check && result.indexOf( check ) > -1 :\n					operator === "$=" ? check && result.slice( -check.length ) === check :\n					operator === "~=" ? ( " " + result + " " ).indexOf( check ) > -1 :\n					operator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :\n					false;\n			};\n		},\n\n		"CHILD": function( type, what, argument, first, last ) {\n			var simple = type.slice( 0, 3 ) !== "nth",\n				forward = type.slice( -4 ) !== "last",\n				ofType = what === "of-type";\n\n			return first === 1 && last === 0 ?\n\n				// Shortcut for :nth-*(n)\n				function( elem ) {\n					return !!elem.parentNode;\n				} :\n\n				function( elem, context, xml ) {\n					var cache, outerCache, node, diff, nodeIndex, start,\n						dir = simple !== forward ? "nextSibling" : "previousSibling",\n						parent = elem.parentNode,\n						name = ofType && elem.nodeName.toLowerCase(),\n						useCache = !xml && !ofType;\n\n					if ( parent ) {\n\n						// :(first|last|only)-(child|of-type)\n						if ( simple ) {\n							while ( dir ) {\n								node = elem;\n								while ( (node = node[ dir ]) ) {\n									if ( ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1 ) {\n										return false;\n									}\n								}\n								// Reverse direction for :only-* (if we haven\'t yet done so)\n								start = dir = type === "only" && !start && "nextSibling";\n							}\n							return true;\n						}\n\n						start = [ forward ? parent.firstChild : parent.lastChild ];\n\n						// non-xml :nth-child(...) stores cache data on `parent`\n						if ( forward && useCache ) {\n							// Seek `elem` from a previously-cached index\n							outerCache = parent[ expando ] || (parent[ expando ] = {});\n							cache = outerCache[ type ] || [];\n							nodeIndex = cache[0] === dirruns && cache[1];\n							diff = cache[0] === dirruns && cache[2];\n							node = nodeIndex && parent.childNodes[ nodeIndex ];\n\n							while ( (node = ++nodeIndex && node && node[ dir ] ||\n\n								// Fallback to seeking `elem` from the start\n								(diff = nodeIndex = 0) || start.pop()) ) {\n\n								// When found, cache indexes on `parent` and break\n								if ( node.nodeType === 1 && ++diff && node === elem ) {\n									outerCache[ type ] = [ dirruns, nodeIndex, diff ];\n									break;\n								}\n							}\n\n						// Use previously-cached element index if available\n						} else if ( useCache && (cache = (elem[ expando ] || (elem[ expando ] = {}))[ type ]) && cache[0] === dirruns ) {\n							diff = cache[1];\n\n						// xml :nth-child(...) or :nth-last-child(...) or :nth(-last)?-of-type(...)\n						} else {\n							// Use the same loop as above to seek `elem` from the start\n							while ( (node = ++nodeIndex && node && node[ dir ] ||\n								(diff = nodeIndex = 0) || start.pop()) ) {\n\n								if ( ( ofType ? node.nodeName.toLowerCase() === name : node.nodeType === 1 ) && ++diff ) {\n									// Cache the index of each encountered element\n									if ( useCache ) {\n										(node[ expando ] || (node[ expando ] = {}))[ type ] = [ dirruns, diff ];\n									}\n\n									if ( node === elem ) {\n										break;\n									}\n								}\n							}\n						}\n\n						// Incorporate the offset, then check against cycle size\n						diff -= last;\n						return diff === first || ( diff % first === 0 && diff / first >= 0 );\n					}\n				};\n		},\n\n		"PSEUDO": function( pseudo, argument ) {\n			// pseudo-class names are case-insensitive\n			// http://www.w3.org/TR/selectors/#pseudo-classes\n			// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters\n			// Remember that setFilters inherits from pseudos\n			var args,\n				fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||\n					Sizzle.error( "unsupported pseudo: " + pseudo );\n\n			// The user may use createPseudo to indicate that\n			// arguments are needed to create the filter function\n			// just as Sizzle does\n			if ( fn[ expando ] ) {\n				return fn( argument );\n			}\n\n			// But maintain support for old signatures\n			if ( fn.length > 1 ) {\n				args = [ pseudo, pseudo, "", argument ];\n				return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?\n					markFunction(function( seed, matches ) {\n						var idx,\n							matched = fn( seed, argument ),\n							i = matched.length;\n						while ( i-- ) {\n							idx = indexOf.call( seed, matched[i] );\n							seed[ idx ] = !( matches[ idx ] = matched[i] );\n						}\n					}) :\n					function( elem ) {\n						return fn( elem, 0, args );\n					};\n			}\n\n			return fn;\n		}\n	},\n\n	pseudos: {\n		// Potentially complex pseudos\n		"not": markFunction(function( selector ) {\n			// Trim the selector passed to compile\n			// to avoid treating leading and trailing\n			// spaces as combinators\n			var input = [],\n				results = [],\n				matcher = compile( selector.replace( rtrim, "$1" ) );\n\n			return matcher[ expando ] ?\n				markFunction(function( seed, matches, context, xml ) {\n					var elem,\n						unmatched = matcher( seed, null, xml, [] ),\n						i = seed.length;\n\n					// Match elements unmatched by `matcher`\n					while ( i-- ) {\n						if ( (elem = unmatched[i]) ) {\n							seed[i] = !(matches[i] = elem);\n						}\n					}\n				}) :\n				function( elem, context, xml ) {\n					input[0] = elem;\n					matcher( input, null, xml, results );\n					return !results.pop();\n				};\n		}),\n\n		"has": markFunction(function( selector ) {\n			return function( elem ) {\n				return Sizzle( selector, elem ).length > 0;\n			};\n		}),\n\n		"contains": markFunction(function( text ) {\n			return function( elem ) {\n				return ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;\n			};\n		}),\n\n		// "Whether an element is represented by a :lang() selector\n		// is based solely on the element\'s language value\n		// being equal to the identifier C,\n		// or beginning with the identifier C immediately followed by "-".\n		// The matching of C against the element\'s language value is performed case-insensitively.\n		// The identifier C does not have to be a valid language name."\n		// http://www.w3.org/TR/selectors/#lang-pseudo\n		"lang": markFunction( function( lang ) {\n			// lang value must be a valid identifider\n			if ( !ridentifier.test(lang || "") ) {\n				Sizzle.error( "unsupported lang: " + lang );\n			}\n			lang = lang.replace( runescape, funescape ).toLowerCase();\n			return function( elem ) {\n				var elemLang;\n				do {\n					if ( (elemLang = documentIsXML ?\n						elem.getAttribute("xml:lang") || elem.getAttribute("lang") :\n						elem.lang) ) {\n\n						elemLang = elemLang.toLowerCase();\n						return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;\n					}\n				} while ( (elem = elem.parentNode) && elem.nodeType === 1 );\n				return false;\n			};\n		}),\n\n		// Miscellaneous\n		"target": function( elem ) {\n			var hash = window.location && window.location.hash;\n			return hash && hash.slice( 1 ) === elem.id;\n		},\n\n		"root": function( elem ) {\n			return elem === docElem;\n		},\n\n		"focus": function( elem ) {\n			return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);\n		},\n\n		// Boolean properties\n		"enabled": function( elem ) {\n			return elem.disabled === false;\n		},\n\n		"disabled": function( elem ) {\n			return elem.disabled === true;\n		},\n\n		"checked": function( elem ) {\n			// In CSS3, :checked should return both checked and selected elements\n			// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked\n			var nodeName = elem.nodeName.toLowerCase();\n			return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);\n		},\n\n		"selected": function( elem ) {\n			// Accessing this property makes selected-by-default\n			// options in Safari work properly\n			if ( elem.parentNode ) {\n				elem.parentNode.selectedIndex;\n			}\n\n			return elem.selected === true;\n		},\n\n		// Contents\n		"empty": function( elem ) {\n			// http://www.w3.org/TR/selectors/#empty-pseudo\n			// :empty is only affected by element nodes and content nodes(including text(3), cdata(4)),\n			//   not comment, processing instructions, or others\n			// Thanks to Diego Perini for the nodeName shortcut\n			//   Greater than "@" means alpha characters (specifically not starting with "#" or "?")\n			for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {\n				if ( elem.nodeName > "@" || elem.nodeType === 3 || elem.nodeType === 4 ) {\n					return false;\n				}\n			}\n			return true;\n		},\n\n		"parent": function( elem ) {\n			return !Expr.pseudos["empty"]( elem );\n		},\n\n		// Element/input types\n		"header": function( elem ) {\n			return rheader.test( elem.nodeName );\n		},\n\n		"input": function( elem ) {\n			return rinputs.test( elem.nodeName );\n		},\n\n		"button": function( elem ) {\n			var name = elem.nodeName.toLowerCase();\n			return name === "input" && elem.type === "button" || name === "button";\n		},\n\n		"text": function( elem ) {\n			var attr;\n			// IE6 and 7 will map elem.type to \'text\' for new HTML5 types (search, etc)\n			// use getAttribute instead to test this case\n			return elem.nodeName.toLowerCase() === "input" &&\n				elem.type === "text" &&\n				( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === elem.type );\n		},\n\n		// Position-in-collection\n		"first": createPositionalPseudo(function() {\n			return [ 0 ];\n		}),\n\n		"last": createPositionalPseudo(function( matchIndexes, length ) {\n			return [ length - 1 ];\n		}),\n\n		"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {\n			return [ argument < 0 ? argument + length : argument ];\n		}),\n\n		"even": createPositionalPseudo(function( matchIndexes, length ) {\n			var i = 0;\n			for ( ; i < length; i += 2 ) {\n				matchIndexes.push( i );\n			}\n			return matchIndexes;\n		}),\n\n		"odd": createPositionalPseudo(function( matchIndexes, length ) {\n			var i = 1;\n			for ( ; i < length; i += 2 ) {\n				matchIndexes.push( i );\n			}\n			return matchIndexes;\n		}),\n\n		"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n			var i = argument < 0 ? argument + length : argument;\n			for ( ; --i >= 0; ) {\n				matchIndexes.push( i );\n			}\n			return matchIndexes;\n		}),\n\n		"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {\n			var i = argument < 0 ? argument + length : argument;\n			for ( ; ++i < length; ) {\n				matchIndexes.push( i );\n			}\n			return matchIndexes;\n		})\n	}\n};\n\n// Add button/input type pseudos\nfor ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {\n	Expr.pseudos[ i ] = createInputPseudo( i );\n}\nfor ( i in { submit: true, reset: true } ) {\n	Expr.pseudos[ i ] = createButtonPseudo( i );\n}\n\nfunction tokenize( selector, parseOnly ) {\n	var matched, match, tokens, type,\n		soFar, groups, preFilters,\n		cached = tokenCache[ selector + " " ];\n\n	if ( cached ) {\n		return parseOnly ? 0 : cached.slice( 0 );\n	}\n\n	soFar = selector;\n	groups = [];\n	preFilters = Expr.preFilter;\n\n	while ( soFar ) {\n\n		// Comma and first run\n		if ( !matched || (match = rcomma.exec( soFar )) ) {\n			if ( match ) {\n				// Don\'t consume trailing commas as valid\n				soFar = soFar.slice( match[0].length ) || soFar;\n			}\n			groups.push( tokens = [] );\n		}\n\n		matched = false;\n\n		// Combinators\n		if ( (match = rcombinators.exec( soFar )) ) {\n			matched = match.shift();\n			tokens.push( {\n				value: matched,\n				// Cast descendant combinators to space\n				type: match[0].replace( rtrim, " " )\n			} );\n			soFar = soFar.slice( matched.length );\n		}\n\n		// Filters\n		for ( type in Expr.filter ) {\n			if ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||\n				(match = preFilters[ type ]( match ))) ) {\n				matched = match.shift();\n				tokens.push( {\n					value: matched,\n					type: type,\n					matches: match\n				} );\n				soFar = soFar.slice( matched.length );\n			}\n		}\n\n		if ( !matched ) {\n			break;\n		}\n	}\n\n	// Return the length of the invalid excess\n	// if we\'re just parsing\n	// Otherwise, throw an error or return tokens\n	return parseOnly ?\n		soFar.length :\n		soFar ?\n			Sizzle.error( selector ) :\n			// Cache the tokens\n			tokenCache( selector, groups ).slice( 0 );\n}\n\nfunction toSelector( tokens ) {\n	var i = 0,\n		len = tokens.length,\n		selector = "";\n	for ( ; i < len; i++ ) {\n		selector += tokens[i].value;\n	}\n	return selector;\n}\n\nfunction addCombinator( matcher, combinator, base ) {\n	var dir = combinator.dir,\n		checkNonElements = base && dir === "parentNode",\n		doneName = done++;\n\n	return combinator.first ?\n		// Check against closest ancestor/preceding element\n		function( elem, context, xml ) {\n			while ( (elem = elem[ dir ]) ) {\n				if ( elem.nodeType === 1 || checkNonElements ) {\n					return matcher( elem, context, xml );\n				}\n			}\n		} :\n\n		// Check against all ancestor/preceding elements\n		function( elem, context, xml ) {\n			var data, cache, outerCache,\n				dirkey = dirruns + " " + doneName;\n\n			// We can\'t set arbitrary data on XML nodes, so they don\'t benefit from dir caching\n			if ( xml ) {\n				while ( (elem = elem[ dir ]) ) {\n					if ( elem.nodeType === 1 || checkNonElements ) {\n						if ( matcher( elem, context, xml ) ) {\n							return true;\n						}\n					}\n				}\n			} else {\n				while ( (elem = elem[ dir ]) ) {\n					if ( elem.nodeType === 1 || checkNonElements ) {\n						outerCache = elem[ expando ] || (elem[ expando ] = {});\n						if ( (cache = outerCache[ dir ]) && cache[0] === dirkey ) {\n							if ( (data = cache[1]) === true || data === cachedruns ) {\n								return data === true;\n							}\n						} else {\n							cache = outerCache[ dir ] = [ dirkey ];\n							cache[1] = matcher( elem, context, xml ) || cachedruns;\n							if ( cache[1] === true ) {\n								return true;\n							}\n						}\n					}\n				}\n			}\n		};\n}\n\nfunction elementMatcher( matchers ) {\n	return matchers.length > 1 ?\n		function( elem, context, xml ) {\n			var i = matchers.length;\n			while ( i-- ) {\n				if ( !matchers[i]( elem, context, xml ) ) {\n					return false;\n				}\n			}\n			return true;\n		} :\n		matchers[0];\n}\n\nfunction condense( unmatched, map, filter, context, xml ) {\n	var elem,\n		newUnmatched = [],\n		i = 0,\n		len = unmatched.length,\n		mapped = map != null;\n\n	for ( ; i < len; i++ ) {\n		if ( (elem = unmatched[i]) ) {\n			if ( !filter || filter( elem, context, xml ) ) {\n				newUnmatched.push( elem );\n				if ( mapped ) {\n					map.push( i );\n				}\n			}\n		}\n	}\n\n	return newUnmatched;\n}\n\nfunction setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {\n	if ( postFilter && !postFilter[ expando ] ) {\n		postFilter = setMatcher( postFilter );\n	}\n	if ( postFinder && !postFinder[ expando ] ) {\n		postFinder = setMatcher( postFinder, postSelector );\n	}\n	return markFunction(function( seed, results, context, xml ) {\n		var temp, i, elem,\n			preMap = [],\n			postMap = [],\n			preexisting = results.length,\n\n			// Get initial elements from seed or context\n			elems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),\n\n			// Prefilter to get matcher input, preserving a map for seed-results synchronization\n			matcherIn = preFilter && ( seed || !selector ) ?\n				condense( elems, preMap, preFilter, context, xml ) :\n				elems,\n\n			matcherOut = matcher ?\n				// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,\n				postFinder || ( seed ? preFilter : preexisting || postFilter ) ?\n\n					// ...intermediate processing is necessary\n					[] :\n\n					// ...otherwise use results directly\n					results :\n				matcherIn;\n\n		// Find primary matches\n		if ( matcher ) {\n			matcher( matcherIn, matcherOut, context, xml );\n		}\n\n		// Apply postFilter\n		if ( postFilter ) {\n			temp = condense( matcherOut, postMap );\n			postFilter( temp, [], context, xml );\n\n			// Un-match failing elements by moving them back to matcherIn\n			i = temp.length;\n			while ( i-- ) {\n				if ( (elem = temp[i]) ) {\n					matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);\n				}\n			}\n		}\n\n		if ( seed ) {\n			if ( postFinder || preFilter ) {\n				if ( postFinder ) {\n					// Get the final matcherOut by condensing this intermediate into postFinder contexts\n					temp = [];\n					i = matcherOut.length;\n					while ( i-- ) {\n						if ( (elem = matcherOut[i]) ) {\n							// Restore matcherIn since elem is not yet a final match\n							temp.push( (matcherIn[i] = elem) );\n						}\n					}\n					postFinder( null, (matcherOut = []), temp, xml );\n				}\n\n				// Move matched elements from seed to results to keep them synchronized\n				i = matcherOut.length;\n				while ( i-- ) {\n					if ( (elem = matcherOut[i]) &&\n						(temp = postFinder ? indexOf.call( seed, elem ) : preMap[i]) > -1 ) {\n\n						seed[temp] = !(results[temp] = elem);\n					}\n				}\n			}\n\n		// Add elements to results, through postFinder if defined\n		} else {\n			matcherOut = condense(\n				matcherOut === results ?\n					matcherOut.splice( preexisting, matcherOut.length ) :\n					matcherOut\n			);\n			if ( postFinder ) {\n				postFinder( null, results, matcherOut, xml );\n			} else {\n				push.apply( results, matcherOut );\n			}\n		}\n	});\n}\n\nfunction matcherFromTokens( tokens ) {\n	var checkContext, matcher, j,\n		len = tokens.length,\n		leadingRelative = Expr.relative[ tokens[0].type ],\n		implicitRelative = leadingRelative || Expr.relative[" "],\n		i = leadingRelative ? 1 : 0,\n\n		// The foundational matcher ensures that elements are reachable from top-level context(s)\n		matchContext = addCombinator( function( elem ) {\n			return elem === checkContext;\n		}, implicitRelative, true ),\n		matchAnyContext = addCombinator( function( elem ) {\n			return indexOf.call( checkContext, elem ) > -1;\n		}, implicitRelative, true ),\n		matchers = [ function( elem, context, xml ) {\n			return ( !leadingRelative && ( xml || context !== outermostContext ) ) || (\n				(checkContext = context).nodeType ?\n					matchContext( elem, context, xml ) :\n					matchAnyContext( elem, context, xml ) );\n		} ];\n\n	for ( ; i < len; i++ ) {\n		if ( (matcher = Expr.relative[ tokens[i].type ]) ) {\n			matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];\n		} else {\n			matcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );\n\n			// Return special upon seeing a positional matcher\n			if ( matcher[ expando ] ) {\n				// Find the next relative operator (if any) for proper handling\n				j = ++i;\n				for ( ; j < len; j++ ) {\n					if ( Expr.relative[ tokens[j].type ] ) {\n						break;\n					}\n				}\n				return setMatcher(\n					i > 1 && elementMatcher( matchers ),\n					i > 1 && toSelector( tokens.slice( 0, i - 1 ) ).replace( rtrim, "$1" ),\n					matcher,\n					i < j && matcherFromTokens( tokens.slice( i, j ) ),\n					j < len && matcherFromTokens( (tokens = tokens.slice( j )) ),\n					j < len && toSelector( tokens )\n				);\n			}\n			matchers.push( matcher );\n		}\n	}\n\n	return elementMatcher( matchers );\n}\n\nfunction matcherFromGroupMatchers( elementMatchers, setMatchers ) {\n	// A counter to specify which element is currently being matched\n	var matcherCachedRuns = 0,\n		bySet = setMatchers.length > 0,\n		byElement = elementMatchers.length > 0,\n		superMatcher = function( seed, context, xml, results, expandContext ) {\n			var elem, j, matcher,\n				setMatched = [],\n				matchedCount = 0,\n				i = "0",\n				unmatched = seed && [],\n				outermost = expandContext != null,\n				contextBackup = outermostContext,\n				// We must always have either seed elements or context\n				elems = seed || byElement && Expr.find["TAG"]( "*", expandContext && context.parentNode || context ),\n				// Use integer dirruns iff this is the outermost matcher\n				dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1);\n\n			if ( outermost ) {\n				outermostContext = context !== document && context;\n				cachedruns = matcherCachedRuns;\n			}\n\n			// Add elements passing elementMatchers directly to results\n			// Keep `i` a string if there are no elements so `matchedCount` will be "00" below\n			for ( ; (elem = elems[i]) != null; i++ ) {\n				if ( byElement && elem ) {\n					j = 0;\n					while ( (matcher = elementMatchers[j++]) ) {\n						if ( matcher( elem, context, xml ) ) {\n							results.push( elem );\n							break;\n						}\n					}\n					if ( outermost ) {\n						dirruns = dirrunsUnique;\n						cachedruns = ++matcherCachedRuns;\n					}\n				}\n\n				// Track unmatched elements for set filters\n				if ( bySet ) {\n					// They will have gone through all possible matchers\n					if ( (elem = !matcher && elem) ) {\n						matchedCount--;\n					}\n\n					// Lengthen the array for every element, matched or not\n					if ( seed ) {\n						unmatched.push( elem );\n					}\n				}\n			}\n\n			// Apply set filters to unmatched elements\n			matchedCount += i;\n			if ( bySet && i !== matchedCount ) {\n				j = 0;\n				while ( (matcher = setMatchers[j++]) ) {\n					matcher( unmatched, setMatched, context, xml );\n				}\n\n				if ( seed ) {\n					// Reintegrate element matches to eliminate the need for sorting\n					if ( matchedCount > 0 ) {\n						while ( i-- ) {\n							if ( !(unmatched[i] || setMatched[i]) ) {\n								setMatched[i] = pop.call( results );\n							}\n						}\n					}\n\n					// Discard index placeholder values to get only actual matches\n					setMatched = condense( setMatched );\n				}\n\n				// Add matches to results\n				push.apply( results, setMatched );\n\n				// Seedless set matches succeeding multiple successful matchers stipulate sorting\n				if ( outermost && !seed && setMatched.length > 0 &&\n					( matchedCount + setMatchers.length ) > 1 ) {\n\n					Sizzle.uniqueSort( results );\n				}\n			}\n\n			// Override manipulation of globals by nested matchers\n			if ( outermost ) {\n				dirruns = dirrunsUnique;\n				outermostContext = contextBackup;\n			}\n\n			return unmatched;\n		};\n\n	return bySet ?\n		markFunction( superMatcher ) :\n		superMatcher;\n}\n\ncompile = Sizzle.compile = function( selector, group /* Internal Use Only */ ) {\n	var i,\n		setMatchers = [],\n		elementMatchers = [],\n		cached = compilerCache[ selector + " " ];\n\n	if ( !cached ) {\n		// Generate a function of recursive functions that can be used to check each element\n		if ( !group ) {\n			group = tokenize( selector );\n		}\n		i = group.length;\n		while ( i-- ) {\n			cached = matcherFromTokens( group[i] );\n			if ( cached[ expando ] ) {\n				setMatchers.push( cached );\n			} else {\n				elementMatchers.push( cached );\n			}\n		}\n\n		// Cache the compiled function\n		cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );\n	}\n	return cached;\n};\n\nfunction multipleContexts( selector, contexts, results ) {\n	var i = 0,\n		len = contexts.length;\n	for ( ; i < len; i++ ) {\n		Sizzle( selector, contexts[i], results );\n	}\n	return results;\n}\n\nfunction select( selector, context, results, seed ) {\n	var i, tokens, token, type, find,\n		match = tokenize( selector );\n\n	if ( !seed ) {\n		// Try to minimize operations if there is only one group\n		if ( match.length === 1 ) {\n\n			// Take a shortcut and set the context if the root selector is an ID\n			tokens = match[0] = match[0].slice( 0 );\n			if ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&\n					context.nodeType === 9 && !documentIsXML &&\n					Expr.relative[ tokens[1].type ] ) {\n\n				context = Expr.find["ID"]( token.matches[0].replace( runescape, funescape ), context )[0];\n				if ( !context ) {\n					return results;\n				}\n\n				selector = selector.slice( tokens.shift().value.length );\n			}\n\n			// Fetch a seed set for right-to-left matching\n			i = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;\n			while ( i-- ) {\n				token = tokens[i];\n\n				// Abort if we hit a combinator\n				if ( Expr.relative[ (type = token.type) ] ) {\n					break;\n				}\n				if ( (find = Expr.find[ type ]) ) {\n					// Search, expanding context for leading sibling combinators\n					if ( (seed = find(\n						token.matches[0].replace( runescape, funescape ),\n						rsibling.test( tokens[0].type ) && context.parentNode || context\n					)) ) {\n\n						// If seed is empty or no tokens remain, we can return early\n						tokens.splice( i, 1 );\n						selector = seed.length && toSelector( tokens );\n						if ( !selector ) {\n							push.apply( results, slice.call( seed, 0 ) );\n							return results;\n						}\n\n						break;\n					}\n				}\n			}\n		}\n	}\n\n	// Compile and execute a filtering function\n	// Provide `match` to avoid retokenization if we modified the selector above\n	compile( selector, match )(\n		seed,\n		context,\n		documentIsXML,\n		results,\n		rsibling.test( selector )\n	);\n	return results;\n}\n\n// Deprecated\nExpr.pseudos["nth"] = Expr.pseudos["eq"];\n\n// Easy API for creating new setFilters\nfunction setFilters() {}\nExpr.filters = setFilters.prototype = Expr.pseudos;\nExpr.setFilters = new setFilters();\n\n// Initialize with the default document\nsetDocument();\n\n// Override sizzle attribute retrieval\nSizzle.attr = jQuery.attr;\njQuery.find = Sizzle;\njQuery.expr = Sizzle.selectors;\njQuery.expr[":"] = jQuery.expr.pseudos;\njQuery.unique = Sizzle.uniqueSort;\njQuery.text = Sizzle.getText;\njQuery.isXMLDoc = Sizzle.isXML;\njQuery.contains = Sizzle.contains;\n\n\n})( window );\nvar runtil = /Until$/,\n	rparentsprev = /^(?:parents|prev(?:Until|All))/,\n	isSimple = /^.[^:#\\[\\.,]*$/,\n	rneedsContext = jQuery.expr.match.needsContext,\n	// methods guaranteed to produce a unique set when starting from a unique set\n	guaranteedUnique = {\n		children: true,\n		contents: true,\n		next: true,\n		prev: true\n	};\n\njQuery.fn.extend({\n	find: function( selector ) {\n		var i, ret, self,\n			len = this.length;\n\n		if ( typeof selector !== "string" ) {\n			self = this;\n			return this.pushStack( jQuery( selector ).filter(function() {\n				for ( i = 0; i < len; i++ ) {\n					if ( jQuery.contains( self[ i ], this ) ) {\n						return true;\n					}\n				}\n			}) );\n		}\n\n		ret = [];\n		for ( i = 0; i < len; i++ ) {\n			jQuery.find( selector, this[ i ], ret );\n		}\n\n		// Needed because $( selector, context ) becomes $( context ).find( selector )\n		ret = this.pushStack( len > 1 ? jQuery.unique( ret ) : ret );\n		ret.selector = ( this.selector ? this.selector + " " : "" ) + selector;\n		return ret;\n	},\n\n	has: function( target ) {\n		var i,\n			targets = jQuery( target, this ),\n			len = targets.length;\n\n		return this.filter(function() {\n			for ( i = 0; i < len; i++ ) {\n				if ( jQuery.contains( this, targets[i] ) ) {\n					return true;\n				}\n			}\n		});\n	},\n\n	not: function( selector ) {\n		return this.pushStack( winnow(this, selector, false) );\n	},\n\n	filter: function( selector ) {\n		return this.pushStack( winnow(this, selector, true) );\n	},\n\n	is: function( selector ) {\n		return !!selector && (\n			typeof selector === "string" ?\n				// If this is a positional/relative selector, check membership in the returned set\n				// so $("p:first").is("p:last") won\'t return true for a doc with two "p".\n				rneedsContext.test( selector ) ?\n					jQuery( selector, this.context ).index( this[0] ) >= 0 :\n					jQuery.filter( selector, this ).length > 0 :\n				this.filter( selector ).length > 0 );\n	},\n\n	closest: function( selectors, context ) {\n		var cur,\n			i = 0,\n			l = this.length,\n			ret = [],\n			pos = rneedsContext.test( selectors ) || typeof selectors !== "string" ?\n				jQuery( selectors, context || this.context ) :\n				0;\n\n		for ( ; i < l; i++ ) {\n			cur = this[i];\n\n			while ( cur && cur.ownerDocument && cur !== context && cur.nodeType !== 11 ) {\n				if ( pos ? pos.index(cur) > -1 : jQuery.find.matchesSelector(cur, selectors) ) {\n					ret.push( cur );\n					break;\n				}\n				cur = cur.parentNode;\n			}\n		}\n\n		return this.pushStack( ret.length > 1 ? jQuery.unique( ret ) : ret );\n	},\n\n	// Determine the position of an element within\n	// the matched set of elements\n	index: function( elem ) {\n\n		// No argument, return index in parent\n		if ( !elem ) {\n			return ( this[0] && this[0].parentNode ) ? this.first().prevAll().length : -1;\n		}\n\n		// index in selector\n		if ( typeof elem === "string" ) {\n			return jQuery.inArray( this[0], jQuery( elem ) );\n		}\n\n		// Locate the position of the desired element\n		return jQuery.inArray(\n			// If it receives a jQuery object, the first element is used\n			elem.jquery ? elem[0] : elem, this );\n	},\n\n	add: function( selector, context ) {\n		var set = typeof selector === "string" ?\n				jQuery( selector, context ) :\n				jQuery.makeArray( selector && selector.nodeType ? [ selector ] : selector ),\n			all = jQuery.merge( this.get(), set );\n\n		return this.pushStack( jQuery.unique(all) );\n	},\n\n	addBack: function( selector ) {\n		return this.add( selector == null ?\n			this.prevObject : this.prevObject.filter(selector)\n		);\n	}\n});\n\njQuery.fn.andSelf = jQuery.fn.addBack;\n\nfunction sibling( cur, dir ) {\n	do {\n		cur = cur[ dir ];\n	} while ( cur && cur.nodeType !== 1 );\n\n	return cur;\n}\n\njQuery.each({\n	parent: function( elem ) {\n		var parent = elem.parentNode;\n		return parent && parent.nodeType !== 11 ? parent : null;\n	},\n	parents: function( elem ) {\n		return jQuery.dir( elem, "parentNode" );\n	},\n	parentsUntil: function( elem, i, until ) {\n		return jQuery.dir( elem, "parentNode", until );\n	},\n	next: function( elem ) {\n		return sibling( elem, "nextSibling" );\n	},\n	prev: function( elem ) {\n		return sibling( elem, "previousSibling" );\n	},\n	nextAll: function( elem ) {\n		return jQuery.dir( elem, "nextSibling" );\n	},\n	prevAll: function( elem ) {\n		return jQuery.dir( elem, "previousSibling" );\n	},\n	nextUntil: function( elem, i, until ) {\n		return jQuery.dir( elem, "nextSibling", until );\n	},\n	prevUntil: function( elem, i, until ) {\n		return jQuery.dir( elem, "previousSibling", until );\n	},\n	siblings: function( elem ) {\n		return jQuery.sibling( ( elem.parentNode || {} ).firstChild, elem );\n	},\n	children: function( elem ) {\n		return jQuery.sibling( elem.firstChild );\n	},\n	contents: function( elem ) {\n		return jQuery.nodeName( elem, "iframe" ) ?\n			elem.contentDocument || elem.contentWindow.document :\n			jQuery.merge( [], elem.childNodes );\n	}\n}, function( name, fn ) {\n	jQuery.fn[ name ] = function( until, selector ) {\n		var ret = jQuery.map( this, fn, until );\n\n		if ( !runtil.test( name ) ) {\n			selector = until;\n		}\n\n		if ( selector && typeof selector === "string" ) {\n			ret = jQuery.filter( selector, ret );\n		}\n\n		ret = this.length > 1 && !guaranteedUnique[ name ] ? jQuery.unique( ret ) : ret;\n\n		if ( this.length > 1 && rparentsprev.test( name ) ) {\n			ret = ret.reverse();\n		}\n\n		return this.pushStack( ret );\n	};\n});\n\njQuery.extend({\n	filter: function( expr, elems, not ) {\n		if ( not ) {\n			expr = ":not(" + expr + ")";\n		}\n\n		return elems.length === 1 ?\n			jQuery.find.matchesSelector(elems[0], expr) ? [ elems[0] ] : [] :\n			jQuery.find.matches(expr, elems);\n	},\n\n	dir: function( elem, dir, until ) {\n		var matched = [],\n			cur = elem[ dir ];\n\n		while ( cur && cur.nodeType !== 9 && (until === undefined || cur.nodeType !== 1 || !jQuery( cur ).is( until )) ) {\n			if ( cur.nodeType === 1 ) {\n				matched.push( cur );\n			}\n			cur = cur[dir];\n		}\n		return matched;\n	},\n\n	sibling: function( n, elem ) {\n		var r = [];\n\n		for ( ; n; n = n.nextSibling ) {\n			if ( n.nodeType === 1 && n !== elem ) {\n				r.push( n );\n			}\n		}\n\n		return r;\n	}\n});\n\n// Implement the identical functionality for filter and not\nfunction winnow( elements, qualifier, keep ) {\n\n	// Can\'t pass null or undefined to indexOf in Firefox 4\n	// Set to 0 to skip string check\n	qualifier = qualifier || 0;\n\n	if ( jQuery.isFunction( qualifier ) ) {\n		return jQuery.grep(elements, function( elem, i ) {\n			var retVal = !!qualifier.call( elem, i, elem );\n			return retVal === keep;\n		});\n\n	} else if ( qualifier.nodeType ) {\n		return jQuery.grep(elements, function( elem ) {\n			return ( elem === qualifier ) === keep;\n		});\n\n	} else if ( typeof qualifier === "string" ) {\n		var filtered = jQuery.grep(elements, function( elem ) {\n			return elem.nodeType === 1;\n		});\n\n		if ( isSimple.test( qualifier ) ) {\n			return jQuery.filter(qualifier, filtered, !keep);\n		} else {\n			qualifier = jQuery.filter( qualifier, filtered );\n		}\n	}\n\n	return jQuery.grep(elements, function( elem ) {\n		return ( jQuery.inArray( elem, qualifier ) >= 0 ) === keep;\n	});\n}\nfunction createSafeFragment( document ) {\n	var list = nodeNames.split( "|" ),\n		safeFrag = document.createDocumentFragment();\n\n	if ( safeFrag.createElement ) {\n		while ( list.length ) {\n			safeFrag.createElement(\n				list.pop()\n			);\n		}\n	}\n	return safeFrag;\n}\n\nvar nodeNames = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|" +\n		"header|hgroup|mark|meter|nav|output|progress|section|summary|time|video",\n	rinlinejQuery = / jQuery\\d+="(?:null|\\d+)"/g,\n	rnoshimcache = new RegExp("<(?:" + nodeNames + ")[\\\\s/>]", "i"),\n	rleadingWhitespace = /^\\s+/,\n	rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([\\w:]+)[^>]*)\\/>/gi,\n	rtagName = /<([\\w:]+)/,\n	rtbody = /<tbody/i,\n	rhtml = /<|&#?\\w+;/,\n	rnoInnerhtml = /<(?:script|style|link)/i,\n	manipulation_rcheckableType = /^(?:checkbox|radio)$/i,\n	// checked="checked" or checked\n	rchecked = /checked\\s*(?:[^=]|=\\s*.checked.)/i,\n	rscriptType = /^$|\\/(?:java|ecma)script/i,\n	rscriptTypeMasked = /^true\\/(.*)/,\n	rcleanScript = /^\\s*<!(?:\\[CDATA\\[|--)|(?:\\]\\]|--)>\\s*$/g,\n\n	// We have to close these tags to support XHTML (#13200)\n	wrapMap = {\n		option: [ 1, "<select multiple=\'multiple\'>", "</select>" ],\n		legend: [ 1, "<fieldset>", "</fieldset>" ],\n		area: [ 1, "<map>", "</map>" ],\n		param: [ 1, "<object>", "</object>" ],\n		thead: [ 1, "<table>", "</table>" ],\n		tr: [ 2, "<table><tbody>", "</tbody></table>" ],\n		col: [ 2, "<table><tbody></tbody><colgroup>", "</colgroup></table>" ],\n		td: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],\n\n		// IE6-8 can\'t serialize link, script, style, or any html5 (NoScope) tags,\n		// unless wrapped in a div with non-breaking characters in front of it.\n		_default: jQuery.support.htmlSerialize ? [ 0, "", "" ] : [ 1, "X<div>", "</div>"  ]\n	},\n	safeFragment = createSafeFragment( document ),\n	fragmentDiv = safeFragment.appendChild( document.createElement("div") );\n\nwrapMap.optgroup = wrapMap.option;\nwrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;\nwrapMap.th = wrapMap.td;\n\njQuery.fn.extend({\n	text: function( value ) {\n		return jQuery.access( this, function( value ) {\n			return value === undefined ?\n				jQuery.text( this ) :\n				this.empty().append( ( this[0] && this[0].ownerDocument || document ).createTextNode( value ) );\n		}, null, value, arguments.length );\n	},\n\n	wrapAll: function( html ) {\n		if ( jQuery.isFunction( html ) ) {\n			return this.each(function(i) {\n				jQuery(this).wrapAll( html.call(this, i) );\n			});\n		}\n\n		if ( this[0] ) {\n			// The elements to wrap the target around\n			var wrap = jQuery( html, this[0].ownerDocument ).eq(0).clone(true);\n\n			if ( this[0].parentNode ) {\n				wrap.insertBefore( this[0] );\n			}\n\n			wrap.map(function() {\n				var elem = this;\n\n				while ( elem.firstChild && elem.firstChild.nodeType === 1 ) {\n					elem = elem.firstChild;\n				}\n\n				return elem;\n			}).append( this );\n		}\n\n		return this;\n	},\n\n	wrapInner: function( html ) {\n		if ( jQuery.isFunction( html ) ) {\n			return this.each(function(i) {\n				jQuery(this).wrapInner( html.call(this, i) );\n			});\n		}\n\n		return this.each(function() {\n			var self = jQuery( this ),\n				contents = self.contents();\n\n			if ( contents.length ) {\n				contents.wrapAll( html );\n\n			} else {\n				self.append( html );\n			}\n		});\n	},\n\n	wrap: function( html ) {\n		var isFunction = jQuery.isFunction( html );\n\n		return this.each(function(i) {\n			jQuery( this ).wrapAll( isFunction ? html.call(this, i) : html );\n		});\n	},\n\n	unwrap: function() {\n		return this.parent().each(function() {\n			if ( !jQuery.nodeName( this, "body" ) ) {\n				jQuery( this ).replaceWith( this.childNodes );\n			}\n		}).end();\n	},\n\n	append: function() {\n		return this.domManip(arguments, true, function( elem ) {\n			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n				this.appendChild( elem );\n			}\n		});\n	},\n\n	prepend: function() {\n		return this.domManip(arguments, true, function( elem ) {\n			if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {\n				this.insertBefore( elem, this.firstChild );\n			}\n		});\n	},\n\n	before: function() {\n		return this.domManip( arguments, false, function( elem ) {\n			if ( this.parentNode ) {\n				this.parentNode.insertBefore( elem, this );\n			}\n		});\n	},\n\n	after: function() {\n		return this.domManip( arguments, false, function( elem ) {\n			if ( this.parentNode ) {\n				this.parentNode.insertBefore( elem, this.nextSibling );\n			}\n		});\n	},\n\n	// keepData is for internal use only--do not document\n	remove: function( selector, keepData ) {\n		var elem,\n			i = 0;\n\n		for ( ; (elem = this[i]) != null; i++ ) {\n			if ( !selector || jQuery.filter( selector, [ elem ] ).length > 0 ) {\n				if ( !keepData && elem.nodeType === 1 ) {\n					jQuery.cleanData( getAll( elem ) );\n				}\n\n				if ( elem.parentNode ) {\n					if ( keepData && jQuery.contains( elem.ownerDocument, elem ) ) {\n						setGlobalEval( getAll( elem, "script" ) );\n					}\n					elem.parentNode.removeChild( elem );\n				}\n			}\n		}\n\n		return this;\n	},\n\n	empty: function() {\n		var elem,\n			i = 0;\n\n		for ( ; (elem = this[i]) != null; i++ ) {\n			// Remove element nodes and prevent memory leaks\n			if ( elem.nodeType === 1 ) {\n				jQuery.cleanData( getAll( elem, false ) );\n			}\n\n			// Remove any remaining nodes\n			while ( elem.firstChild ) {\n				elem.removeChild( elem.firstChild );\n			}\n\n			// If this is a select, ensure that it displays empty (#12336)\n			// Support: IE<9\n			if ( elem.options && jQuery.nodeName( elem, "select" ) ) {\n				elem.options.length = 0;\n			}\n		}\n\n		return this;\n	},\n\n	clone: function( dataAndEvents, deepDataAndEvents ) {\n		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;\n		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;\n\n		return this.map( function () {\n			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );\n		});\n	},\n\n	html: function( value ) {\n		return jQuery.access( this, function( value ) {\n			var elem = this[0] || {},\n				i = 0,\n				l = this.length;\n\n			if ( value === undefined ) {\n				return elem.nodeType === 1 ?\n					elem.innerHTML.replace( rinlinejQuery, "" ) :\n					undefined;\n			}\n\n			// See if we can take a shortcut and just use innerHTML\n			if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&\n				( jQuery.support.htmlSerialize || !rnoshimcache.test( value )  ) &&\n				( jQuery.support.leadingWhitespace || !rleadingWhitespace.test( value ) ) &&\n				!wrapMap[ ( rtagName.exec( value ) || ["", ""] )[1].toLowerCase() ] ) {\n\n				value = value.replace( rxhtmlTag, "<$1></$2>" );\n\n				try {\n					for (; i < l; i++ ) {\n						// Remove element nodes and prevent memory leaks\n						elem = this[i] || {};\n						if ( elem.nodeType === 1 ) {\n							jQuery.cleanData( getAll( elem, false ) );\n							elem.innerHTML = value;\n						}\n					}\n\n					elem = 0;\n\n				// If using innerHTML throws an exception, use the fallback method\n				} catch(e) {}\n			}\n\n			if ( elem ) {\n				this.empty().append( value );\n			}\n		}, null, value, arguments.length );\n	},\n\n	replaceWith: function( value ) {\n		var isFunc = jQuery.isFunction( value );\n\n		// Make sure that the elements are removed from the DOM before they are inserted\n		// this can help fix replacing a parent with child elements\n		if ( !isFunc && typeof value !== "string" ) {\n			value = jQuery( value ).not( this ).detach();\n		}\n\n		return this.domManip( [ value ], true, function( elem ) {\n			var next = this.nextSibling,\n				parent = this.parentNode;\n\n			if ( parent ) {\n				jQuery( this ).remove();\n				parent.insertBefore( elem, next );\n			}\n		});\n	},\n\n	detach: function( selector ) {\n		return this.remove( selector, true );\n	},\n\n	domManip: function( args, table, callback ) {\n\n		// Flatten any nested arrays\n		args = core_concat.apply( [], args );\n\n		var first, node, hasScripts,\n			scripts, doc, fragment,\n			i = 0,\n			l = this.length,\n			set = this,\n			iNoClone = l - 1,\n			value = args[0],\n			isFunction = jQuery.isFunction( value );\n\n		// We can\'t cloneNode fragments that contain checked, in WebKit\n		if ( isFunction || !( l <= 1 || typeof value !== "string" || jQuery.support.checkClone || !rchecked.test( value ) ) ) {\n			return this.each(function( index ) {\n				var self = set.eq( index );\n				if ( isFunction ) {\n					args[0] = value.call( this, index, table ? self.html() : undefined );\n				}\n				self.domManip( args, table, callback );\n			});\n		}\n\n		if ( l ) {\n			fragment = jQuery.buildFragment( args, this[ 0 ].ownerDocument, false, this );\n			first = fragment.firstChild;\n\n			if ( fragment.childNodes.length === 1 ) {\n				fragment = first;\n			}\n\n			if ( first ) {\n				table = table && jQuery.nodeName( first, "tr" );\n				scripts = jQuery.map( getAll( fragment, "script" ), disableScript );\n				hasScripts = scripts.length;\n\n				// Use the original fragment for the last item instead of the first because it can end up\n				// being emptied incorrectly in certain situations (#8070).\n				for ( ; i < l; i++ ) {\n					node = fragment;\n\n					if ( i !== iNoClone ) {\n						node = jQuery.clone( node, true, true );\n\n						// Keep references to cloned scripts for later restoration\n						if ( hasScripts ) {\n							jQuery.merge( scripts, getAll( node, "script" ) );\n						}\n					}\n\n					callback.call(\n						table && jQuery.nodeName( this[i], "table" ) ?\n							findOrAppend( this[i], "tbody" ) :\n							this[i],\n						node,\n						i\n					);\n				}\n\n				if ( hasScripts ) {\n					doc = scripts[ scripts.length - 1 ].ownerDocument;\n\n					// Reenable scripts\n					jQuery.map( scripts, restoreScript );\n\n					// Evaluate executable scripts on first document insertion\n					for ( i = 0; i < hasScripts; i++ ) {\n						node = scripts[ i ];\n						if ( rscriptType.test( node.type || "" ) &&\n							!jQuery._data( node, "globalEval" ) && jQuery.contains( doc, node ) ) {\n\n							if ( node.src ) {\n								// Hope ajax is available...\n								jQuery.ajax({\n									url: node.src,\n									type: "GET",\n									dataType: "script",\n									async: false,\n									global: false,\n									"throws": true\n								});\n							} else {\n								jQuery.globalEval( ( node.text || node.textContent || node.innerHTML || "" ).replace( rcleanScript, "" ) );\n							}\n						}\n					}\n				}\n\n				// Fix #11809: Avoid leaking memory\n				fragment = first = null;\n			}\n		}\n\n		return this;\n	}\n});\n\nfunction findOrAppend( elem, tag ) {\n	return elem.getElementsByTagName( tag )[0] || elem.appendChild( elem.ownerDocument.createElement( tag ) );\n}\n\n// Replace/restore the type attribute of script elements for safe DOM manipulation\nfunction disableScript( elem ) {\n	var attr = elem.getAttributeNode("type");\n	elem.type = ( attr && attr.specified ) + "/" + elem.type;\n	return elem;\n}\nfunction restoreScript( elem ) {\n	var match = rscriptTypeMasked.exec( elem.type );\n	if ( match ) {\n		elem.type = match[1];\n	} else {\n		elem.removeAttribute("type");\n	}\n	return elem;\n}\n\n// Mark scripts as having already been evaluated\nfunction setGlobalEval( elems, refElements ) {\n	var elem,\n		i = 0;\n	for ( ; (elem = elems[i]) != null; i++ ) {\n		jQuery._data( elem, "globalEval", !refElements || jQuery._data( refElements[i], "globalEval" ) );\n	}\n}\n\nfunction cloneCopyEvent( src, dest ) {\n\n	if ( dest.nodeType !== 1 || !jQuery.hasData( src ) ) {\n		return;\n	}\n\n	var type, i, l,\n		oldData = jQuery._data( src ),\n		curData = jQuery._data( dest, oldData ),\n		events = oldData.events;\n\n	if ( events ) {\n		delete curData.handle;\n		curData.events = {};\n\n		for ( type in events ) {\n			for ( i = 0, l = events[ type ].length; i < l; i++ ) {\n				jQuery.event.add( dest, type, events[ type ][ i ] );\n			}\n		}\n	}\n\n	// make the cloned public data object a copy from the original\n	if ( curData.data ) {\n		curData.data = jQuery.extend( {}, curData.data );\n	}\n}\n\nfunction fixCloneNodeIssues( src, dest ) {\n	var nodeName, e, data;\n\n	// We do not need to do anything for non-Elements\n	if ( dest.nodeType !== 1 ) {\n		return;\n	}\n\n	nodeName = dest.nodeName.toLowerCase();\n\n	// IE6-8 copies events bound via attachEvent when using cloneNode.\n	if ( !jQuery.support.noCloneEvent && dest[ jQuery.expando ] ) {\n		data = jQuery._data( dest );\n\n		for ( e in data.events ) {\n			jQuery.removeEvent( dest, e, data.handle );\n		}\n\n		// Event data gets referenced instead of copied if the expando gets copied too\n		dest.removeAttribute( jQuery.expando );\n	}\n\n	// IE blanks contents when cloning scripts, and tries to evaluate newly-set text\n	if ( nodeName === "script" && dest.text !== src.text ) {\n		disableScript( dest ).text = src.text;\n		restoreScript( dest );\n\n	// IE6-10 improperly clones children of object elements using classid.\n	// IE10 throws NoModificationAllowedError if parent is null, #12132.\n	} else if ( nodeName === "object" ) {\n		if ( dest.parentNode ) {\n			dest.outerHTML = src.outerHTML;\n		}\n\n		// This path appears unavoidable for IE9. When cloning an object\n		// element in IE9, the outerHTML strategy above is not sufficient.\n		// If the src has innerHTML and the destination does not,\n		// copy the src.innerHTML into the dest.innerHTML. #10324\n		if ( jQuery.support.html5Clone && ( src.innerHTML && !jQuery.trim(dest.innerHTML) ) ) {\n			dest.innerHTML = src.innerHTML;\n		}\n\n	} else if ( nodeName === "input" && manipulation_rcheckableType.test( src.type ) ) {\n		// IE6-8 fails to persist the checked state of a cloned checkbox\n		// or radio button. Worse, IE6-7 fail to give the cloned element\n		// a checked appearance if the defaultChecked value isn\'t also set\n\n		dest.defaultChecked = dest.checked = src.checked;\n\n		// IE6-7 get confused and end up setting the value of a cloned\n		// checkbox/radio button to an empty string instead of "on"\n		if ( dest.value !== src.value ) {\n			dest.value = src.value;\n		}\n\n	// IE6-8 fails to return the selected option to the default selected\n	// state when cloning options\n	} else if ( nodeName === "option" ) {\n		dest.defaultSelected = dest.selected = src.defaultSelected;\n\n	// IE6-8 fails to set the defaultValue to the correct value when\n	// cloning other types of input fields\n	} else if ( nodeName === "input" || nodeName === "textarea" ) {\n		dest.defaultValue = src.defaultValue;\n	}\n}\n\njQuery.each({\n	appendTo: "append",\n	prependTo: "prepend",\n	insertBefore: "before",\n	insertAfter: "after",\n	replaceAll: "replaceWith"\n}, function( name, original ) {\n	jQuery.fn[ name ] = function( selector ) {\n		var elems,\n			i = 0,\n			ret = [],\n			insert = jQuery( selector ),\n			last = insert.length - 1;\n\n		for ( ; i <= last; i++ ) {\n			elems = i === last ? this : this.clone(true);\n			jQuery( insert[i] )[ original ]( elems );\n\n			// Modern browsers can apply jQuery collections as arrays, but oldIE needs a .get()\n			core_push.apply( ret, elems.get() );\n		}\n\n		return this.pushStack( ret );\n	};\n});\n\nfunction getAll( context, tag ) {\n	var elems, elem,\n		i = 0,\n		found = typeof context.getElementsByTagName !== core_strundefined ? context.getElementsByTagName( tag || "*" ) :\n			typeof context.querySelectorAll !== core_strundefined ? context.querySelectorAll( tag || "*" ) :\n			undefined;\n\n	if ( !found ) {\n		for ( found = [], elems = context.childNodes || context; (elem = elems[i]) != null; i++ ) {\n			if ( !tag || jQuery.nodeName( elem, tag ) ) {\n				found.push( elem );\n			} else {\n				jQuery.merge( found, getAll( elem, tag ) );\n			}\n		}\n	}\n\n	return tag === undefined || tag && jQuery.nodeName( context, tag ) ?\n		jQuery.merge( [ context ], found ) :\n		found;\n}\n\n// Used in buildFragment, fixes the defaultChecked property\nfunction fixDefaultChecked( elem ) {\n	if ( manipulation_rcheckableType.test( elem.type ) ) {\n		elem.defaultChecked = elem.checked;\n	}\n}\n\njQuery.extend({\n	clone: function( elem, dataAndEvents, deepDataAndEvents ) {\n		var destElements, node, clone, i, srcElements,\n			inPage = jQuery.contains( elem.ownerDocument, elem );\n\n		if ( jQuery.support.html5Clone || jQuery.isXMLDoc(elem) || !rnoshimcache.test( "<" + elem.nodeName + ">" ) ) {\n			clone = elem.cloneNode( true );\n\n		// IE<=8 does not properly clone detached, unknown element nodes\n		} else {\n			fragmentDiv.innerHTML = elem.outerHTML;\n			fragmentDiv.removeChild( clone = fragmentDiv.firstChild );\n		}\n\n		if ( (!jQuery.support.noCloneEvent || !jQuery.support.noCloneChecked) &&\n				(elem.nodeType === 1 || elem.nodeType === 11) && !jQuery.isXMLDoc(elem) ) {\n\n			// We eschew Sizzle here for performance reasons: http://jsperf.com/getall-vs-sizzle/2\n			destElements = getAll( clone );\n			srcElements = getAll( elem );\n\n			// Fix all IE cloning issues\n			for ( i = 0; (node = srcElements[i]) != null; ++i ) {\n				// Ensure that the destination node is not null; Fixes #9587\n				if ( destElements[i] ) {\n					fixCloneNodeIssues( node, destElements[i] );\n				}\n			}\n		}\n\n		// Copy the events from the original to the clone\n		if ( dataAndEvents ) {\n			if ( deepDataAndEvents ) {\n				srcElements = srcElements || getAll( elem );\n				destElements = destElements || getAll( clone );\n\n				for ( i = 0; (node = srcElements[i]) != null; i++ ) {\n					cloneCopyEvent( node, destElements[i] );\n				}\n			} else {\n				cloneCopyEvent( elem, clone );\n			}\n		}\n\n		// Preserve script evaluation history\n		destElements = getAll( clone, "script" );\n		if ( destElements.length > 0 ) {\n			setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );\n		}\n\n		destElements = srcElements = node = null;\n\n		// Return the cloned set\n		return clone;\n	},\n\n	buildFragment: function( elems, context, scripts, selection ) {\n		var j, elem, contains,\n			tmp, tag, tbody, wrap,\n			l = elems.length,\n\n			// Ensure a safe fragment\n			safe = createSafeFragment( context ),\n\n			nodes = [],\n			i = 0;\n\n		for ( ; i < l; i++ ) {\n			elem = elems[ i ];\n\n			if ( elem || elem === 0 ) {\n\n				// Add nodes directly\n				if ( jQuery.type( elem ) === "object" ) {\n					jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );\n\n				// Convert non-html into a text node\n				} else if ( !rhtml.test( elem ) ) {\n					nodes.push( context.createTextNode( elem ) );\n\n				// Convert html into DOM nodes\n				} else {\n					tmp = tmp || safe.appendChild( context.createElement("div") );\n\n					// Deserialize a standard representation\n					tag = ( rtagName.exec( elem ) || ["", ""] )[1].toLowerCase();\n					wrap = wrapMap[ tag ] || wrapMap._default;\n\n					tmp.innerHTML = wrap[1] + elem.replace( rxhtmlTag, "<$1></$2>" ) + wrap[2];\n\n					// Descend through wrappers to the right content\n					j = wrap[0];\n					while ( j-- ) {\n						tmp = tmp.lastChild;\n					}\n\n					// Manually add leading whitespace removed by IE\n					if ( !jQuery.support.leadingWhitespace && rleadingWhitespace.test( elem ) ) {\n						nodes.push( context.createTextNode( rleadingWhitespace.exec( elem )[0] ) );\n					}\n\n					// Remove IE\'s autoinserted <tbody> from table fragments\n					if ( !jQuery.support.tbody ) {\n\n						// String was a <table>, *may* have spurious <tbody>\n						elem = tag === "table" && !rtbody.test( elem ) ?\n							tmp.firstChild :\n\n							// String was a bare <thead> or <tfoot>\n							wrap[1] === "<table>" && !rtbody.test( elem ) ?\n								tmp :\n								0;\n\n						j = elem && elem.childNodes.length;\n						while ( j-- ) {\n							if ( jQuery.nodeName( (tbody = elem.childNodes[j]), "tbody" ) && !tbody.childNodes.length ) {\n								elem.removeChild( tbody );\n							}\n						}\n					}\n\n					jQuery.merge( nodes, tmp.childNodes );\n\n					// Fix #12392 for WebKit and IE > 9\n					tmp.textContent = "";\n\n					// Fix #12392 for oldIE\n					while ( tmp.firstChild ) {\n						tmp.removeChild( tmp.firstChild );\n					}\n\n					// Remember the top-level container for proper cleanup\n					tmp = safe.lastChild;\n				}\n			}\n		}\n\n		// Fix #11356: Clear elements from fragment\n		if ( tmp ) {\n			safe.removeChild( tmp );\n		}\n\n		// Reset defaultChecked for any radios and checkboxes\n		// about to be appended to the DOM in IE 6/7 (#8060)\n		if ( !jQuery.support.appendChecked ) {\n			jQuery.grep( getAll( nodes, "input" ), fixDefaultChecked );\n		}\n\n		i = 0;\n		while ( (elem = nodes[ i++ ]) ) {\n\n			// #4087 - If origin and destination elements are the same, and this is\n			// that element, do not do anything\n			if ( selection && jQuery.inArray( elem, selection ) !== -1 ) {\n				continue;\n			}\n\n			contains = jQuery.contains( elem.ownerDocument, elem );\n\n			// Append to fragment\n			tmp = getAll( safe.appendChild( elem ), "script" );\n\n			// Preserve script evaluation history\n			if ( contains ) {\n				setGlobalEval( tmp );\n			}\n\n			// Capture executables\n			if ( scripts ) {\n				j = 0;\n				while ( (elem = tmp[ j++ ]) ) {\n					if ( rscriptType.test( elem.type || "" ) ) {\n						scripts.push( elem );\n					}\n				}\n			}\n		}\n\n		tmp = null;\n\n		return safe;\n	},\n\n	cleanData: function( elems, /* internal */ acceptData ) {\n		var elem, type, id, data,\n			i = 0,\n			internalKey = jQuery.expando,\n			cache = jQuery.cache,\n			deleteExpando = jQuery.support.deleteExpando,\n			special = jQuery.event.special;\n\n		for ( ; (elem = elems[i]) != null; i++ ) {\n\n			if ( acceptData || jQuery.acceptData( elem ) ) {\n\n				id = elem[ internalKey ];\n				data = id && cache[ id ];\n\n				if ( data ) {\n					if ( data.events ) {\n						for ( type in data.events ) {\n							if ( special[ type ] ) {\n								jQuery.event.remove( elem, type );\n\n							// This is a shortcut to avoid jQuery.event.remove\'s overhead\n							} else {\n								jQuery.removeEvent( elem, type, data.handle );\n							}\n						}\n					}\n\n					// Remove cache only if it was not already removed by jQuery.event.remove\n					if ( cache[ id ] ) {\n\n						delete cache[ id ];\n\n						// IE does not allow us to delete expando properties from nodes,\n						// nor does it have a removeAttribute function on Document nodes;\n						// we must handle all of these cases\n						if ( deleteExpando ) {\n							delete elem[ internalKey ];\n\n						} else if ( typeof elem.removeAttribute !== core_strundefined ) {\n							elem.removeAttribute( internalKey );\n\n						} else {\n							elem[ internalKey ] = null;\n						}\n\n						core_deletedIds.push( id );\n					}\n				}\n			}\n		}\n	}\n});\nvar iframe, getStyles, curCSS,\n	ralpha = /alpha\\([^)]*\\)/i,\n	ropacity = /opacity\\s*=\\s*([^)]*)/,\n	rposition = /^(top|right|bottom|left)$/,\n	// swappable if display is none or starts with table except "table", "table-cell", or "table-caption"\n	// see here for display values: https://developer.mozilla.org/en-US/docs/CSS/display\n	rdisplayswap = /^(none|table(?!-c[ea]).+)/,\n	rmargin = /^margin/,\n	rnumsplit = new RegExp( "^(" + core_pnum + ")(.*)$", "i" ),\n	rnumnonpx = new RegExp( "^(" + core_pnum + ")(?!px)[a-z%]+$", "i" ),\n	rrelNum = new RegExp( "^([+-])=(" + core_pnum + ")", "i" ),\n	elemdisplay = { BODY: "block" },\n\n	cssShow = { position: "absolute", visibility: "hidden", display: "block" },\n	cssNormalTransform = {\n		letterSpacing: 0,\n		fontWeight: 400\n	},\n\n	cssExpand = [ "Top", "Right", "Bottom", "Left" ],\n	cssPrefixes = [ "Webkit", "O", "Moz", "ms" ];\n\n// return a css property mapped to a potentially vendor prefixed property\nfunction vendorPropName( style, name ) {\n\n	// shortcut for names that are not vendor prefixed\n	if ( name in style ) {\n		return name;\n	}\n\n	// check for vendor prefixed names\n	var capName = name.charAt(0).toUpperCase() + name.slice(1),\n		origName = name,\n		i = cssPrefixes.length;\n\n	while ( i-- ) {\n		name = cssPrefixes[ i ] + capName;\n		if ( name in style ) {\n			return name;\n		}\n	}\n\n	return origName;\n}\n\nfunction isHidden( elem, el ) {\n	// isHidden might be called from jQuery#filter function;\n	// in that case, element will be second argument\n	elem = el || elem;\n	return jQuery.css( elem, "display" ) === "none" || !jQuery.contains( elem.ownerDocument, elem );\n}\n\nfunction showHide( elements, show ) {\n	var display, elem, hidden,\n		values = [],\n		index = 0,\n		length = elements.length;\n\n	for ( ; index < length; index++ ) {\n		elem = elements[ index ];\n		if ( !elem.style ) {\n			continue;\n		}\n\n		values[ index ] = jQuery._data( elem, "olddisplay" );\n		display = elem.style.display;\n		if ( show ) {\n			// Reset the inline display of this element to learn if it is\n			// being hidden by cascaded rules or not\n			if ( !values[ index ] && display === "none" ) {\n				elem.style.display = "";\n			}\n\n			// Set elements which have been overridden with display: none\n			// in a stylesheet to whatever the default browser style is\n			// for such an element\n			if ( elem.style.display === "" && isHidden( elem ) ) {\n				values[ index ] = jQuery._data( elem, "olddisplay", css_defaultDisplay(elem.nodeName) );\n			}\n		} else {\n\n			if ( !values[ index ] ) {\n				hidden = isHidden( elem );\n\n				if ( display && display !== "none" || !hidden ) {\n					jQuery._data( elem, "olddisplay", hidden ? display : jQuery.css( elem, "display" ) );\n				}\n			}\n		}\n	}\n\n	// Set the display of most of the elements in a second loop\n	// to avoid the constant reflow\n	for ( index = 0; index < length; index++ ) {\n		elem = elements[ index ];\n		if ( !elem.style ) {\n			continue;\n		}\n		if ( !show || elem.style.display === "none" || elem.style.display === "" ) {\n			elem.style.display = show ? values[ index ] || "" : "none";\n		}\n	}\n\n	return elements;\n}\n\njQuery.fn.extend({\n	css: function( name, value ) {\n		return jQuery.access( this, function( elem, name, value ) {\n			var len, styles,\n				map = {},\n				i = 0;\n\n			if ( jQuery.isArray( name ) ) {\n				styles = getStyles( elem );\n				len = name.length;\n\n				for ( ; i < len; i++ ) {\n					map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );\n				}\n\n				return map;\n			}\n\n			return value !== undefined ?\n				jQuery.style( elem, name, value ) :\n				jQuery.css( elem, name );\n		}, name, value, arguments.length > 1 );\n	},\n	show: function() {\n		return showHide( this, true );\n	},\n	hide: function() {\n		return showHide( this );\n	},\n	toggle: function( state ) {\n		var bool = typeof state === "boolean";\n\n		return this.each(function() {\n			if ( bool ? state : isHidden( this ) ) {\n				jQuery( this ).show();\n			} else {\n				jQuery( this ).hide();\n			}\n		});\n	}\n});\n\njQuery.extend({\n	// Add in style property hooks for overriding the default\n	// behavior of getting and setting a style property\n	cssHooks: {\n		opacity: {\n			get: function( elem, computed ) {\n				if ( computed ) {\n					// We should always get a number back from opacity\n					var ret = curCSS( elem, "opacity" );\n					return ret === "" ? "1" : ret;\n				}\n			}\n		}\n	},\n\n	// Exclude the following css properties to add px\n	cssNumber: {\n		"columnCount": true,\n		"fillOpacity": true,\n		"fontWeight": true,\n		"lineHeight": true,\n		"opacity": true,\n		"orphans": true,\n		"widows": true,\n		"zIndex": true,\n		"zoom": true\n	},\n\n	// Add in properties whose names you wish to fix before\n	// setting or getting the value\n	cssProps: {\n		// normalize float css property\n		"float": jQuery.support.cssFloat ? "cssFloat" : "styleFloat"\n	},\n\n	// Get and set the style property on a DOM Node\n	style: function( elem, name, value, extra ) {\n		// Don\'t set styles on text and comment nodes\n		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {\n			return;\n		}\n\n		// Make sure that we\'re working with the right name\n		var ret, type, hooks,\n			origName = jQuery.camelCase( name ),\n			style = elem.style;\n\n		name = jQuery.cssProps[ origName ] || ( jQuery.cssProps[ origName ] = vendorPropName( style, origName ) );\n\n		// gets hook for the prefixed version\n		// followed by the unprefixed version\n		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n		// Check if we\'re setting a value\n		if ( value !== undefined ) {\n			type = typeof value;\n\n			// convert relative number strings (+= or -=) to relative numbers. #7345\n			if ( type === "string" && (ret = rrelNum.exec( value )) ) {\n				value = ( ret[1] + 1 ) * ret[2] + parseFloat( jQuery.css( elem, name ) );\n				// Fixes bug #9237\n				type = "number";\n			}\n\n			// Make sure that NaN and null values aren\'t set. See: #7116\n			if ( value == null || type === "number" && isNaN( value ) ) {\n				return;\n			}\n\n			// If a number was passed in, add \'px\' to the (except for certain CSS properties)\n			if ( type === "number" && !jQuery.cssNumber[ origName ] ) {\n				value += "px";\n			}\n\n			// Fixes #8908, it can be done more correctly by specifing setters in cssHooks,\n			// but it would mean to define eight (for every problematic property) identical functions\n			if ( !jQuery.support.clearCloneStyle && value === "" && name.indexOf("background") === 0 ) {\n				style[ name ] = "inherit";\n			}\n\n			// If a hook was provided, use that value, otherwise just set the specified value\n			if ( !hooks || !("set" in hooks) || (value = hooks.set( elem, value, extra )) !== undefined ) {\n\n				// Wrapped to prevent IE from throwing errors when \'invalid\' values are provided\n				// Fixes bug #5509\n				try {\n					style[ name ] = value;\n				} catch(e) {}\n			}\n\n		} else {\n			// If a hook was provided get the non-computed value from there\n			if ( hooks && "get" in hooks && (ret = hooks.get( elem, false, extra )) !== undefined ) {\n				return ret;\n			}\n\n			// Otherwise just get the value from the style object\n			return style[ name ];\n		}\n	},\n\n	css: function( elem, name, extra, styles ) {\n		var num, val, hooks,\n			origName = jQuery.camelCase( name );\n\n		// Make sure that we\'re working with the right name\n		name = jQuery.cssProps[ origName ] || ( jQuery.cssProps[ origName ] = vendorPropName( elem.style, origName ) );\n\n		// gets hook for the prefixed version\n		// followed by the unprefixed version\n		hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];\n\n		// If a hook was provided get the computed value from there\n		if ( hooks && "get" in hooks ) {\n			val = hooks.get( elem, true, extra );\n		}\n\n		// Otherwise, if a way to get the computed value exists, use that\n		if ( val === undefined ) {\n			val = curCSS( elem, name, styles );\n		}\n\n		//convert "normal" to computed value\n		if ( val === "normal" && name in cssNormalTransform ) {\n			val = cssNormalTransform[ name ];\n		}\n\n		// Return, converting to number if forced or a qualifier was provided and val looks numeric\n		if ( extra === "" || extra ) {\n			num = parseFloat( val );\n			return extra === true || jQuery.isNumeric( num ) ? num || 0 : val;\n		}\n		return val;\n	},\n\n	// A method for quickly swapping in/out CSS properties to get correct calculations\n	swap: function( elem, options, callback, args ) {\n		var ret, name,\n			old = {};\n\n		// Remember the old values, and insert the new ones\n		for ( name in options ) {\n			old[ name ] = elem.style[ name ];\n			elem.style[ name ] = options[ name ];\n		}\n\n		ret = callback.apply( elem, args || [] );\n\n		// Revert the old values\n		for ( name in options ) {\n			elem.style[ name ] = old[ name ];\n		}\n\n		return ret;\n	}\n});\n\n// NOTE: we\'ve included the "window" in window.getComputedStyle\n// because jsdom on node.js will break without it.\nif ( window.getComputedStyle ) {\n	getStyles = function( elem ) {\n		return window.getComputedStyle( elem, null );\n	};\n\n	curCSS = function( elem, name, _computed ) {\n		var width, minWidth, maxWidth,\n			computed = _computed || getStyles( elem ),\n\n			// getPropertyValue is only needed for .css(\'filter\') in IE9, see #12537\n			ret = computed ? computed.getPropertyValue( name ) || computed[ name ] : undefined,\n			style = elem.style;\n\n		if ( computed ) {\n\n			if ( ret === "" && !jQuery.contains( elem.ownerDocument, elem ) ) {\n				ret = jQuery.style( elem, name );\n			}\n\n			// A tribute to the "awesome hack by Dean Edwards"\n			// Chrome < 17 and Safari 5.0 uses "computed value" instead of "used value" for margin-right\n			// Safari 5.1.7 (at least) returns percentage for a larger set of values, but width seems to be reliably pixels\n			// this is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values\n			if ( rnumnonpx.test( ret ) && rmargin.test( name ) ) {\n\n				// Remember the original values\n				width = style.width;\n				minWidth = style.minWidth;\n				maxWidth = style.maxWidth;\n\n				// Put in the new values to get a computed value out\n				style.minWidth = style.maxWidth = style.width = ret;\n				ret = computed.width;\n\n				// Revert the changed values\n				style.width = width;\n				style.minWidth = minWidth;\n				style.maxWidth = maxWidth;\n			}\n		}\n\n		return ret;\n	};\n} else if ( document.documentElement.currentStyle ) {\n	getStyles = function( elem ) {\n		return elem.currentStyle;\n	};\n\n	curCSS = function( elem, name, _computed ) {\n		var left, rs, rsLeft,\n			computed = _computed || getStyles( elem ),\n			ret = computed ? computed[ name ] : undefined,\n			style = elem.style;\n\n		// Avoid setting ret to empty string here\n		// so we don\'t default to auto\n		if ( ret == null && style && style[ name ] ) {\n			ret = style[ name ];\n		}\n\n		// From the awesome hack by Dean Edwards\n		// http://erik.eae.net/archives/2007/07/27/18.54.15/#comment-102291\n\n		// If we\'re not dealing with a regular pixel number\n		// but a number that has a weird ending, we need to convert it to pixels\n		// but not position css attributes, as those are proportional to the parent element instead\n		// and we can\'t measure the parent instead because it might trigger a "stacking dolls" problem\n		if ( rnumnonpx.test( ret ) && !rposition.test( name ) ) {\n\n			// Remember the original values\n			left = style.left;\n			rs = elem.runtimeStyle;\n			rsLeft = rs && rs.left;\n\n			// Put in the new values to get a computed value out\n			if ( rsLeft ) {\n				rs.left = elem.currentStyle.left;\n			}\n			style.left = name === "fontSize" ? "1em" : ret;\n			ret = style.pixelLeft + "px";\n\n			// Revert the changed values\n			style.left = left;\n			if ( rsLeft ) {\n				rs.left = rsLeft;\n			}\n		}\n\n		return ret === "" ? "auto" : ret;\n	};\n}\n\nfunction setPositiveNumber( elem, value, subtract ) {\n	var matches = rnumsplit.exec( value );\n	return matches ?\n		// Guard against undefined "subtract", e.g., when used as in cssHooks\n		Math.max( 0, matches[ 1 ] - ( subtract || 0 ) ) + ( matches[ 2 ] || "px" ) :\n		value;\n}\n\nfunction augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {\n	var i = extra === ( isBorderBox ? "border" : "content" ) ?\n		// If we already have the right measurement, avoid augmentation\n		4 :\n		// Otherwise initialize for horizontal or vertical properties\n		name === "width" ? 1 : 0,\n\n		val = 0;\n\n	for ( ; i < 4; i += 2 ) {\n		// both box models exclude margin, so add it if we want it\n		if ( extra === "margin" ) {\n			val += jQuery.css( elem, extra + cssExpand[ i ], true, styles );\n		}\n\n		if ( isBorderBox ) {\n			// border-box includes padding, so remove it if we want content\n			if ( extra === "content" ) {\n				val -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n			}\n\n			// at this point, extra isn\'t border nor margin, so remove border\n			if ( extra !== "margin" ) {\n				val -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n			}\n		} else {\n			// at this point, extra isn\'t content, so add padding\n			val += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );\n\n			// at this point, extra isn\'t content nor padding, so add border\n			if ( extra !== "padding" ) {\n				val += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );\n			}\n		}\n	}\n\n	return val;\n}\n\nfunction getWidthOrHeight( elem, name, extra ) {\n\n	// Start with offset property, which is equivalent to the border-box value\n	var valueIsBorderBox = true,\n		val = name === "width" ? elem.offsetWidth : elem.offsetHeight,\n		styles = getStyles( elem ),\n		isBorderBox = jQuery.support.boxSizing && jQuery.css( elem, "boxSizing", false, styles ) === "border-box";\n\n	// some non-html elements return undefined for offsetWidth, so check for null/undefined\n	// svg - https://bugzilla.mozilla.org/show_bug.cgi?id=649285\n	// MathML - https://bugzilla.mozilla.org/show_bug.cgi?id=491668\n	if ( val <= 0 || val == null ) {\n		// Fall back to computed then uncomputed css if necessary\n		val = curCSS( elem, name, styles );\n		if ( val < 0 || val == null ) {\n			val = elem.style[ name ];\n		}\n\n		// Computed unit is not pixels. Stop here and return.\n		if ( rnumnonpx.test(val) ) {\n			return val;\n		}\n\n		// we need the check for style in case a browser which returns unreliable values\n		// for getComputedStyle silently falls back to the reliable elem.style\n		valueIsBorderBox = isBorderBox && ( jQuery.support.boxSizingReliable || val === elem.style[ name ] );\n\n		// Normalize "", auto, and prepare for extra\n		val = parseFloat( val ) || 0;\n	}\n\n	// use the active box-sizing model to add/subtract irrelevant styles\n	return ( val +\n		augmentWidthOrHeight(\n			elem,\n			name,\n			extra || ( isBorderBox ? "border" : "content" ),\n			valueIsBorderBox,\n			styles\n		)\n	) + "px";\n}\n\n// Try to determine the default display value of an element\nfunction css_defaultDisplay( nodeName ) {\n	var doc = document,\n		display = elemdisplay[ nodeName ];\n\n	if ( !display ) {\n		display = actualDisplay( nodeName, doc );\n\n		// If the simple way fails, read from inside an iframe\n		if ( display === "none" || !display ) {\n			// Use the already-created iframe if possible\n			iframe = ( iframe ||\n				jQuery("<iframe frameborder=\'0\' width=\'0\' height=\'0\'/>")\n				.css( "cssText", "display:block !important" )\n			).appendTo( doc.documentElement );\n\n			// Always write a new HTML skeleton so Webkit and Firefox don\'t choke on reuse\n			doc = ( iframe[0].contentWindow || iframe[0].contentDocument ).document;\n			doc.write("<!doctype html><html><body>");\n			doc.close();\n\n			display = actualDisplay( nodeName, doc );\n			iframe.detach();\n		}\n\n		// Store the correct default display\n		elemdisplay[ nodeName ] = display;\n	}\n\n	return display;\n}\n\n// Called ONLY from within css_defaultDisplay\nfunction actualDisplay( name, doc ) {\n	var elem = jQuery( doc.createElement( name ) ).appendTo( doc.body ),\n		display = jQuery.css( elem[0], "display" );\n	elem.remove();\n	return display;\n}\n\njQuery.each([ "height", "width" ], function( i, name ) {\n	jQuery.cssHooks[ name ] = {\n		get: function( elem, computed, extra ) {\n			if ( computed ) {\n				// certain elements can have dimension info if we invisibly show them\n				// however, it must have a current display style that would benefit from this\n				return elem.offsetWidth === 0 && rdisplayswap.test( jQuery.css( elem, "display" ) ) ?\n					jQuery.swap( elem, cssShow, function() {\n						return getWidthOrHeight( elem, name, extra );\n					}) :\n					getWidthOrHeight( elem, name, extra );\n			}\n		},\n\n		set: function( elem, value, extra ) {\n			var styles = extra && getStyles( elem );\n			return setPositiveNumber( elem, value, extra ?\n				augmentWidthOrHeight(\n					elem,\n					name,\n					extra,\n					jQuery.support.boxSizing && jQuery.css( elem, "boxSizing", false, styles ) === "border-box",\n					styles\n				) : 0\n			);\n		}\n	};\n});\n\nif ( !jQuery.support.opacity ) {\n	jQuery.cssHooks.opacity = {\n		get: function( elem, computed ) {\n			// IE uses filters for opacity\n			return ropacity.test( (computed && elem.currentStyle ? elem.currentStyle.filter : elem.style.filter) || "" ) ?\n				( 0.01 * parseFloat( RegExp.$1 ) ) + "" :\n				computed ? "1" : "";\n		},\n\n		set: function( elem, value ) {\n			var style = elem.style,\n				currentStyle = elem.currentStyle,\n				opacity = jQuery.isNumeric( value ) ? "alpha(opacity=" + value * 100 + ")" : "",\n				filter = currentStyle && currentStyle.filter || style.filter || "";\n\n			// IE has trouble with opacity if it does not have layout\n			// Force it by setting the zoom level\n			style.zoom = 1;\n\n			// if setting opacity to 1, and no other filters exist - attempt to remove filter attribute #6652\n			// if value === "", then remove inline opacity #12685\n			if ( ( value >= 1 || value === "" ) &&\n					jQuery.trim( filter.replace( ralpha, "" ) ) === "" &&\n					style.removeAttribute ) {\n\n				// Setting style.filter to null, "" & " " still leave "filter:" in the cssText\n				// if "filter:" is present at all, clearType is disabled, we want to avoid this\n				// style.removeAttribute is IE Only, but so apparently is this code path...\n				style.removeAttribute( "filter" );\n\n				// if there is no filter style applied in a css rule or unset inline opacity, we are done\n				if ( value === "" || currentStyle && !currentStyle.filter ) {\n					return;\n				}\n			}\n\n			// otherwise, set new filter values\n			style.filter = ralpha.test( filter ) ?\n				filter.replace( ralpha, opacity ) :\n				filter + " " + opacity;\n		}\n	};\n}\n\n// These hooks cannot be added until DOM ready because the support test\n// for it is not run until after DOM ready\njQuery(function() {\n	if ( !jQuery.support.reliableMarginRight ) {\n		jQuery.cssHooks.marginRight = {\n			get: function( elem, computed ) {\n				if ( computed ) {\n					// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right\n					// Work around by temporarily setting element display to inline-block\n					return jQuery.swap( elem, { "display": "inline-block" },\n						curCSS, [ elem, "marginRight" ] );\n				}\n			}\n		};\n	}\n\n	// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084\n	// getComputedStyle returns percent when specified for top/left/bottom/right\n	// rather than make the css module depend on the offset module, we just check for it here\n	if ( !jQuery.support.pixelPosition && jQuery.fn.position ) {\n		jQuery.each( [ "top", "left" ], function( i, prop ) {\n			jQuery.cssHooks[ prop ] = {\n				get: function( elem, computed ) {\n					if ( computed ) {\n						computed = curCSS( elem, prop );\n						// if curCSS returns percentage, fallback to offset\n						return rnumnonpx.test( computed ) ?\n							jQuery( elem ).position()[ prop ] + "px" :\n							computed;\n					}\n				}\n			};\n		});\n	}\n\n});\n\nif ( jQuery.expr && jQuery.expr.filters ) {\n	jQuery.expr.filters.hidden = function( elem ) {\n		// Support: Opera <= 12.12\n		// Opera reports offsetWidths and offsetHeights less than zero on some elements\n		return elem.offsetWidth <= 0 && elem.offsetHeight <= 0 ||\n			(!jQuery.support.reliableHiddenOffsets && ((elem.style && elem.style.display) || jQuery.css( elem, "display" )) === "none");\n	};\n\n	jQuery.expr.filters.visible = function( elem ) {\n		return !jQuery.expr.filters.hidden( elem );\n	};\n}\n\n// These hooks are used by animate to expand properties\njQuery.each({\n	margin: "",\n	padding: "",\n	border: "Width"\n}, function( prefix, suffix ) {\n	jQuery.cssHooks[ prefix + suffix ] = {\n		expand: function( value ) {\n			var i = 0,\n				expanded = {},\n\n				// assumes a single number if not a string\n				parts = typeof value === "string" ? value.split(" ") : [ value ];\n\n			for ( ; i < 4; i++ ) {\n				expanded[ prefix + cssExpand[ i ] + suffix ] =\n					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];\n			}\n\n			return expanded;\n		}\n	};\n\n	if ( !rmargin.test( prefix ) ) {\n		jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;\n	}\n});\nvar r20 = /%20/g,\n	rbracket = /\\[\\]$/,\n	rCRLF = /\\r?\\n/g,\n	rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,\n	rsubmittable = /^(?:input|select|textarea|keygen)/i;\n\njQuery.fn.extend({\n	serialize: function() {\n		return jQuery.param( this.serializeArray() );\n	},\n	serializeArray: function() {\n		return this.map(function(){\n			// Can add propHook for "elements" to filter or add form elements\n			var elements = jQuery.prop( this, "elements" );\n			return elements ? jQuery.makeArray( elements ) : this;\n		})\n		.filter(function(){\n			var type = this.type;\n			// Use .is(":disabled") so that fieldset[disabled] works\n			return this.name && !jQuery( this ).is( ":disabled" ) &&\n				rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&\n				( this.checked || !manipulation_rcheckableType.test( type ) );\n		})\n		.map(function( i, elem ){\n			var val = jQuery( this ).val();\n\n			return val == null ?\n				null :\n				jQuery.isArray( val ) ?\n					jQuery.map( val, function( val ){\n						return { name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n					}) :\n					{ name: elem.name, value: val.replace( rCRLF, "\\r\\n" ) };\n		}).get();\n	}\n});\n\n//Serialize an array of form elements or a set of\n//key/values into a query string\njQuery.param = function( a, traditional ) {\n	var prefix,\n		s = [],\n		add = function( key, value ) {\n			// If value is a function, invoke it and return its value\n			value = jQuery.isFunction( value ) ? value() : ( value == null ? "" : value );\n			s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );\n		};\n\n	// Set traditional to true for jQuery <= 1.3.2 behavior.\n	if ( traditional === undefined ) {\n		traditional = jQuery.ajaxSettings && jQuery.ajaxSettings.traditional;\n	}\n\n	// If an array was passed in, assume that it is an array of form elements.\n	if ( jQuery.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {\n		// Serialize the form elements\n		jQuery.each( a, function() {\n			add( this.name, this.value );\n		});\n\n	} else {\n		// If traditional, encode the "old" way (the way 1.3.2 or older\n		// did it), otherwise encode params recursively.\n		for ( prefix in a ) {\n			buildParams( prefix, a[ prefix ], traditional, add );\n		}\n	}\n\n	// Return the resulting serialization\n	return s.join( "&" ).replace( r20, "+" );\n};\n\nfunction buildParams( prefix, obj, traditional, add ) {\n	var name;\n\n	if ( jQuery.isArray( obj ) ) {\n		// Serialize array item.\n		jQuery.each( obj, function( i, v ) {\n			if ( traditional || rbracket.test( prefix ) ) {\n				// Treat each array item as a scalar.\n				add( prefix, v );\n\n			} else {\n				// Item is non-scalar (array or object), encode its numeric index.\n				buildParams( prefix + "[" + ( typeof v === "object" ? i : "" ) + "]", v, traditional, add );\n			}\n		});\n\n	} else if ( !traditional && jQuery.type( obj ) === "object" ) {\n		// Serialize object item.\n		for ( name in obj ) {\n			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );\n		}\n\n	} else {\n		// Serialize scalar item.\n		add( prefix, obj );\n	}\n}\njQuery.each( ("blur focus focusin focusout load resize scroll unload click dblclick " +\n	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +\n	"change select submit keydown keypress keyup error contextmenu").split(" "), function( i, name ) {\n\n	// Handle event binding\n	jQuery.fn[ name ] = function( data, fn ) {\n		return arguments.length > 0 ?\n			this.on( name, null, data, fn ) :\n			this.trigger( name );\n	};\n});\n\njQuery.fn.hover = function( fnOver, fnOut ) {\n	return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );\n};\nvar\n	// Document location\n	ajaxLocParts,\n	ajaxLocation,\n	ajax_nonce = jQuery.now(),\n\n	ajax_rquery = /\\?/,\n	rhash = /#.*$/,\n	rts = /([?&])_=[^&]*/,\n	rheaders = /^(.*?):[ \\t]*([^\\r\\n]*)\\r?$/mg, // IE leaves an \\r character at EOL\n	// #7653, #8125, #8152: local protocol detection\n	rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,\n	rnoContent = /^(?:GET|HEAD)$/,\n	rprotocol = /^\\/\\//,\n	rurl = /^([\\w.+-]+:)(?:\\/\\/([^\\/?#:]*)(?::(\\d+)|)|)/,\n\n	// Keep a copy of the old load method\n	_load = jQuery.fn.load,\n\n	/* Prefilters\n	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)\n	 * 2) These are called:\n	 *    - BEFORE asking for a transport\n	 *    - AFTER param serialization (s.data is a string if s.processData is true)\n	 * 3) key is the dataType\n	 * 4) the catchall symbol "*" can be used\n	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed\n	 */\n	prefilters = {},\n\n	/* Transports bindings\n	 * 1) key is the dataType\n	 * 2) the catchall symbol "*" can be used\n	 * 3) selection will start with transport dataType and THEN go to "*" if needed\n	 */\n	transports = {},\n\n	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression\n	allTypes = "*/".concat("*");\n\n// #8138, IE may throw an exception when accessing\n// a field from window.location if document.domain has been set\ntry {\n	ajaxLocation = location.href;\n} catch( e ) {\n	// Use the href attribute of an A element\n	// since IE will modify it given document.location\n	ajaxLocation = document.createElement( "a" );\n	ajaxLocation.href = "";\n	ajaxLocation = ajaxLocation.href;\n}\n\n// Segment location into parts\najaxLocParts = rurl.exec( ajaxLocation.toLowerCase() ) || [];\n\n// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport\nfunction addToPrefiltersOrTransports( structure ) {\n\n	// dataTypeExpression is optional and defaults to "*"\n	return function( dataTypeExpression, func ) {\n\n		if ( typeof dataTypeExpression !== "string" ) {\n			func = dataTypeExpression;\n			dataTypeExpression = "*";\n		}\n\n		var dataType,\n			i = 0,\n			dataTypes = dataTypeExpression.toLowerCase().match( core_rnotwhite ) || [];\n\n		if ( jQuery.isFunction( func ) ) {\n			// For each dataType in the dataTypeExpression\n			while ( (dataType = dataTypes[i++]) ) {\n				// Prepend if requested\n				if ( dataType[0] === "+" ) {\n					dataType = dataType.slice( 1 ) || "*";\n					(structure[ dataType ] = structure[ dataType ] || []).unshift( func );\n\n				// Otherwise append\n				} else {\n					(structure[ dataType ] = structure[ dataType ] || []).push( func );\n				}\n			}\n		}\n	};\n}\n\n// Base inspection function for prefilters and transports\nfunction inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {\n\n	var inspected = {},\n		seekingTransport = ( structure === transports );\n\n	function inspect( dataType ) {\n		var selected;\n		inspected[ dataType ] = true;\n		jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {\n			var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );\n			if( typeof dataTypeOrTransport === "string" && !seekingTransport && !inspected[ dataTypeOrTransport ] ) {\n				options.dataTypes.unshift( dataTypeOrTransport );\n				inspect( dataTypeOrTransport );\n				return false;\n			} else if ( seekingTransport ) {\n				return !( selected = dataTypeOrTransport );\n			}\n		});\n		return selected;\n	}\n\n	return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );\n}\n\n// A special extend for ajax options\n// that takes "flat" options (not to be deep extended)\n// Fixes #9887\nfunction ajaxExtend( target, src ) {\n	var deep, key,\n		flatOptions = jQuery.ajaxSettings.flatOptions || {};\n\n	for ( key in src ) {\n		if ( src[ key ] !== undefined ) {\n			( flatOptions[ key ] ? target : ( deep || (deep = {}) ) )[ key ] = src[ key ];\n		}\n	}\n	if ( deep ) {\n		jQuery.extend( true, target, deep );\n	}\n\n	return target;\n}\n\njQuery.fn.load = function( url, params, callback ) {\n	if ( typeof url !== "string" && _load ) {\n		return _load.apply( this, arguments );\n	}\n\n	var selector, response, type,\n		self = this,\n		off = url.indexOf(" ");\n\n	if ( off >= 0 ) {\n		selector = url.slice( off, url.length );\n		url = url.slice( 0, off );\n	}\n\n	// If it\'s a function\n	if ( jQuery.isFunction( params ) ) {\n\n		// We assume that it\'s the callback\n		callback = params;\n		params = undefined;\n\n	// Otherwise, build a param string\n	} else if ( params && typeof params === "object" ) {\n		type = "POST";\n	}\n\n	// If we have elements to modify, make the request\n	if ( self.length > 0 ) {\n		jQuery.ajax({\n			url: url,\n\n			// if "type" variable is undefined, then "GET" method will be used\n			type: type,\n			dataType: "html",\n			data: params\n		}).done(function( responseText ) {\n\n			// Save response for use in complete callback\n			response = arguments;\n\n			self.html( selector ?\n\n				// If a selector was specified, locate the right elements in a dummy div\n				// Exclude scripts to avoid IE \'Permission Denied\' errors\n				jQuery("<div>").append( jQuery.parseHTML( responseText ) ).find( selector ) :\n\n				// Otherwise use the full result\n				responseText );\n\n		}).complete( callback && function( jqXHR, status ) {\n			self.each( callback, response || [ jqXHR.responseText, status, jqXHR ] );\n		});\n	}\n\n	return this;\n};\n\n// Attach a bunch of functions for handling common AJAX events\njQuery.each( [ "ajaxStart", "ajaxStop", "ajaxComplete", "ajaxError", "ajaxSuccess", "ajaxSend" ], function( i, type ){\n	jQuery.fn[ type ] = function( fn ){\n		return this.on( type, fn );\n	};\n});\n\njQuery.each( [ "get", "post" ], function( i, method ) {\n	jQuery[ method ] = function( url, data, callback, type ) {\n		// shift arguments if data argument was omitted\n		if ( jQuery.isFunction( data ) ) {\n			type = type || callback;\n			callback = data;\n			data = undefined;\n		}\n\n		return jQuery.ajax({\n			url: url,\n			type: method,\n			dataType: type,\n			data: data,\n			success: callback\n		});\n	};\n});\n\njQuery.extend({\n\n	// Counter for holding the number of active queries\n	active: 0,\n\n	// Last-Modified header cache for next request\n	lastModified: {},\n	etag: {},\n\n	ajaxSettings: {\n		url: ajaxLocation,\n		type: "GET",\n		isLocal: rlocalProtocol.test( ajaxLocParts[ 1 ] ),\n		global: true,\n		processData: true,\n		async: true,\n		contentType: "application/x-www-form-urlencoded; charset=UTF-8",\n		/*\n		timeout: 0,\n		data: null,\n		dataType: null,\n		username: null,\n		password: null,\n		cache: null,\n		throws: false,\n		traditional: false,\n		headers: {},\n		*/\n\n		accepts: {\n			"*": allTypes,\n			text: "text/plain",\n			html: "text/html",\n			xml: "application/xml, text/xml",\n			json: "application/json, text/javascript"\n		},\n\n		contents: {\n			xml: /xml/,\n			html: /html/,\n			json: /json/\n		},\n\n		responseFields: {\n			xml: "responseXML",\n			text: "responseText"\n		},\n\n		// Data converters\n		// Keys separate source (or catchall "*") and destination types with a single space\n		converters: {\n\n			// Convert anything to text\n			"* text": window.String,\n\n			// Text to html (true = no transformation)\n			"text html": true,\n\n			// Evaluate text as a json expression\n			"text json": jQuery.parseJSON,\n\n			// Parse text as xml\n			"text xml": jQuery.parseXML\n		},\n\n		// For options that shouldn\'t be deep extended:\n		// you can add your own custom options here if\n		// and when you create one that shouldn\'t be\n		// deep extended (see ajaxExtend)\n		flatOptions: {\n			url: true,\n			context: true\n		}\n	},\n\n	// Creates a full fledged settings object into target\n	// with both ajaxSettings and settings fields.\n	// If target is omitted, writes into ajaxSettings.\n	ajaxSetup: function( target, settings ) {\n		return settings ?\n\n			// Building a settings object\n			ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :\n\n			// Extending ajaxSettings\n			ajaxExtend( jQuery.ajaxSettings, target );\n	},\n\n	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),\n	ajaxTransport: addToPrefiltersOrTransports( transports ),\n\n	// Main method\n	ajax: function( url, options ) {\n\n		// If url is an object, simulate pre-1.5 signature\n		if ( typeof url === "object" ) {\n			options = url;\n			url = undefined;\n		}\n\n		// Force options to be an object\n		options = options || {};\n\n		var // Cross-domain detection vars\n			parts,\n			// Loop variable\n			i,\n			// URL without anti-cache param\n			cacheURL,\n			// Response headers as string\n			responseHeadersString,\n			// timeout handle\n			timeoutTimer,\n\n			// To know if global events are to be dispatched\n			fireGlobals,\n\n			transport,\n			// Response headers\n			responseHeaders,\n			// Create the final options object\n			s = jQuery.ajaxSetup( {}, options ),\n			// Callbacks context\n			callbackContext = s.context || s,\n			// Context for global events is callbackContext if it is a DOM node or jQuery collection\n			globalEventContext = s.context && ( callbackContext.nodeType || callbackContext.jquery ) ?\n				jQuery( callbackContext ) :\n				jQuery.event,\n			// Deferreds\n			deferred = jQuery.Deferred(),\n			completeDeferred = jQuery.Callbacks("once memory"),\n			// Status-dependent callbacks\n			statusCode = s.statusCode || {},\n			// Headers (they are sent all at once)\n			requestHeaders = {},\n			requestHeadersNames = {},\n			// The jqXHR state\n			state = 0,\n			// Default abort message\n			strAbort = "canceled",\n			// Fake xhr\n			jqXHR = {\n				readyState: 0,\n\n				// Builds headers hashtable if needed\n				getResponseHeader: function( key ) {\n					var match;\n					if ( state === 2 ) {\n						if ( !responseHeaders ) {\n							responseHeaders = {};\n							while ( (match = rheaders.exec( responseHeadersString )) ) {\n								responseHeaders[ match[1].toLowerCase() ] = match[ 2 ];\n							}\n						}\n						match = responseHeaders[ key.toLowerCase() ];\n					}\n					return match == null ? null : match;\n				},\n\n				// Raw string\n				getAllResponseHeaders: function() {\n					return state === 2 ? responseHeadersString : null;\n				},\n\n				// Caches the header\n				setRequestHeader: function( name, value ) {\n					var lname = name.toLowerCase();\n					if ( !state ) {\n						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;\n						requestHeaders[ name ] = value;\n					}\n					return this;\n				},\n\n				// Overrides response content-type header\n				overrideMimeType: function( type ) {\n					if ( !state ) {\n						s.mimeType = type;\n					}\n					return this;\n				},\n\n				// Status-dependent callbacks\n				statusCode: function( map ) {\n					var code;\n					if ( map ) {\n						if ( state < 2 ) {\n							for ( code in map ) {\n								// Lazy-add the new callback in a way that preserves old ones\n								statusCode[ code ] = [ statusCode[ code ], map[ code ] ];\n							}\n						} else {\n							// Execute the appropriate callbacks\n							jqXHR.always( map[ jqXHR.status ] );\n						}\n					}\n					return this;\n				},\n\n				// Cancel the request\n				abort: function( statusText ) {\n					var finalText = statusText || strAbort;\n					if ( transport ) {\n						transport.abort( finalText );\n					}\n					done( 0, finalText );\n					return this;\n				}\n			};\n\n		// Attach deferreds\n		deferred.promise( jqXHR ).complete = completeDeferred.add;\n		jqXHR.success = jqXHR.done;\n		jqXHR.error = jqXHR.fail;\n\n		// Remove hash character (#7531: and string promotion)\n		// Add protocol if not provided (#5866: IE7 issue with protocol-less urls)\n		// Handle falsy url in the settings object (#10093: consistency with old signature)\n		// We also use the url parameter if available\n		s.url = ( ( url || s.url || ajaxLocation ) + "" ).replace( rhash, "" ).replace( rprotocol, ajaxLocParts[ 1 ] + "//" );\n\n		// Alias method option to type as per ticket #12004\n		s.type = options.method || options.type || s.method || s.type;\n\n		// Extract dataTypes list\n		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().match( core_rnotwhite ) || [""];\n\n		// A cross-domain request is in order when we have a protocol:host:port mismatch\n		if ( s.crossDomain == null ) {\n			parts = rurl.exec( s.url.toLowerCase() );\n			s.crossDomain = !!( parts &&\n				( parts[ 1 ] !== ajaxLocParts[ 1 ] || parts[ 2 ] !== ajaxLocParts[ 2 ] ||\n					( parts[ 3 ] || ( parts[ 1 ] === "http:" ? 80 : 443 ) ) !=\n						( ajaxLocParts[ 3 ] || ( ajaxLocParts[ 1 ] === "http:" ? 80 : 443 ) ) )\n			);\n		}\n\n		// Convert data if not already a string\n		if ( s.data && s.processData && typeof s.data !== "string" ) {\n			s.data = jQuery.param( s.data, s.traditional );\n		}\n\n		// Apply prefilters\n		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );\n\n		// If request was aborted inside a prefilter, stop there\n		if ( state === 2 ) {\n			return jqXHR;\n		}\n\n		// We can fire global events as of now if asked to\n		fireGlobals = s.global;\n\n		// Watch for a new set of requests\n		if ( fireGlobals && jQuery.active++ === 0 ) {\n			jQuery.event.trigger("ajaxStart");\n		}\n\n		// Uppercase the type\n		s.type = s.type.toUpperCase();\n\n		// Determine if request has content\n		s.hasContent = !rnoContent.test( s.type );\n\n		// Save the URL in case we\'re toying with the If-Modified-Since\n		// and/or If-None-Match header later on\n		cacheURL = s.url;\n\n		// More options handling for requests with no content\n		if ( !s.hasContent ) {\n\n			// If data is available, append data to url\n			if ( s.data ) {\n				cacheURL = ( s.url += ( ajax_rquery.test( cacheURL ) ? "&" : "?" ) + s.data );\n				// #9682: remove data so that it\'s not used in an eventual retry\n				delete s.data;\n			}\n\n			// Add anti-cache in url if needed\n			if ( s.cache === false ) {\n				s.url = rts.test( cacheURL ) ?\n\n					// If there is already a \'_\' parameter, set its value\n					cacheURL.replace( rts, "$1_=" + ajax_nonce++ ) :\n\n					// Otherwise add one to the end\n					cacheURL + ( ajax_rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ajax_nonce++;\n			}\n		}\n\n		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n		if ( s.ifModified ) {\n			if ( jQuery.lastModified[ cacheURL ] ) {\n				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );\n			}\n			if ( jQuery.etag[ cacheURL ] ) {\n				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );\n			}\n		}\n\n		// Set the correct header, if data is being sent\n		if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {\n			jqXHR.setRequestHeader( "Content-Type", s.contentType );\n		}\n\n		// Set the Accepts header for the server, depending on the dataType\n		jqXHR.setRequestHeader(\n			"Accept",\n			s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[0] ] ?\n				s.accepts[ s.dataTypes[0] ] + ( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :\n				s.accepts[ "*" ]\n		);\n\n		// Check for headers option\n		for ( i in s.headers ) {\n			jqXHR.setRequestHeader( i, s.headers[ i ] );\n		}\n\n		// Allow custom headers/mimetypes and early abort\n		if ( s.beforeSend && ( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {\n			// Abort if not done already and return\n			return jqXHR.abort();\n		}\n\n		// aborting is no longer a cancellation\n		strAbort = "abort";\n\n		// Install callbacks on deferreds\n		for ( i in { success: 1, error: 1, complete: 1 } ) {\n			jqXHR[ i ]( s[ i ] );\n		}\n\n		// Get transport\n		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );\n\n		// If no transport, we auto-abort\n		if ( !transport ) {\n			done( -1, "No Transport" );\n		} else {\n			jqXHR.readyState = 1;\n\n			// Send global event\n			if ( fireGlobals ) {\n				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );\n			}\n			// Timeout\n			if ( s.async && s.timeout > 0 ) {\n				timeoutTimer = setTimeout(function() {\n					jqXHR.abort("timeout");\n				}, s.timeout );\n			}\n\n			try {\n				state = 1;\n				transport.send( requestHeaders, done );\n			} catch ( e ) {\n				// Propagate exception as error if not done\n				if ( state < 2 ) {\n					done( -1, e );\n				// Simply rethrow otherwise\n				} else {\n					throw e;\n				}\n			}\n		}\n\n		// Callback for when everything is done\n		function done( status, nativeStatusText, responses, headers ) {\n			var isSuccess, success, error, response, modified,\n				statusText = nativeStatusText;\n\n			// Called once\n			if ( state === 2 ) {\n				return;\n			}\n\n			// State is "done" now\n			state = 2;\n\n			// Clear timeout if it exists\n			if ( timeoutTimer ) {\n				clearTimeout( timeoutTimer );\n			}\n\n			// Dereference transport for early garbage collection\n			// (no matter how long the jqXHR object will be used)\n			transport = undefined;\n\n			// Cache response headers\n			responseHeadersString = headers || "";\n\n			// Set readyState\n			jqXHR.readyState = status > 0 ? 4 : 0;\n\n			// Get response data\n			if ( responses ) {\n				response = ajaxHandleResponses( s, jqXHR, responses );\n			}\n\n			// If successful, handle type chaining\n			if ( status >= 200 && status < 300 || status === 304 ) {\n\n				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.\n				if ( s.ifModified ) {\n					modified = jqXHR.getResponseHeader("Last-Modified");\n					if ( modified ) {\n						jQuery.lastModified[ cacheURL ] = modified;\n					}\n					modified = jqXHR.getResponseHeader("etag");\n					if ( modified ) {\n						jQuery.etag[ cacheURL ] = modified;\n					}\n				}\n\n				// if no content\n				if ( status === 204 ) {\n					isSuccess = true;\n					statusText = "nocontent";\n\n				// if not modified\n				} else if ( status === 304 ) {\n					isSuccess = true;\n					statusText = "notmodified";\n\n				// If we have data, let\'s convert it\n				} else {\n					isSuccess = ajaxConvert( s, response );\n					statusText = isSuccess.state;\n					success = isSuccess.data;\n					error = isSuccess.error;\n					isSuccess = !error;\n				}\n			} else {\n				// We extract error from statusText\n				// then normalize statusText and status for non-aborts\n				error = statusText;\n				if ( status || !statusText ) {\n					statusText = "error";\n					if ( status < 0 ) {\n						status = 0;\n					}\n				}\n			}\n\n			// Set data for the fake xhr object\n			jqXHR.status = status;\n			jqXHR.statusText = ( nativeStatusText || statusText ) + "";\n\n			// Success/Error\n			if ( isSuccess ) {\n				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );\n			} else {\n				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );\n			}\n\n			// Status-dependent callbacks\n			jqXHR.statusCode( statusCode );\n			statusCode = undefined;\n\n			if ( fireGlobals ) {\n				globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",\n					[ jqXHR, s, isSuccess ? success : error ] );\n			}\n\n			// Complete\n			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );\n\n			if ( fireGlobals ) {\n				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );\n				// Handle the global AJAX counter\n				if ( !( --jQuery.active ) ) {\n					jQuery.event.trigger("ajaxStop");\n				}\n			}\n		}\n\n		return jqXHR;\n	},\n\n	getScript: function( url, callback ) {\n		return jQuery.get( url, undefined, callback, "script" );\n	},\n\n	getJSON: function( url, data, callback ) {\n		return jQuery.get( url, data, callback, "json" );\n	}\n});\n\n/* Handles responses to an ajax request:\n * - sets all responseXXX fields accordingly\n * - finds the right dataType (mediates between content-type and expected dataType)\n * - returns the corresponding response\n */\nfunction ajaxHandleResponses( s, jqXHR, responses ) {\n	var firstDataType, ct, finalDataType, type,\n		contents = s.contents,\n		dataTypes = s.dataTypes,\n		responseFields = s.responseFields;\n\n	// Fill responseXXX fields\n	for ( type in responseFields ) {\n		if ( type in responses ) {\n			jqXHR[ responseFields[type] ] = responses[ type ];\n		}\n	}\n\n	// Remove auto dataType and get content-type in the process\n	while( dataTypes[ 0 ] === "*" ) {\n		dataTypes.shift();\n		if ( ct === undefined ) {\n			ct = s.mimeType || jqXHR.getResponseHeader("Content-Type");\n		}\n	}\n\n	// Check if we\'re dealing with a known content-type\n	if ( ct ) {\n		for ( type in contents ) {\n			if ( contents[ type ] && contents[ type ].test( ct ) ) {\n				dataTypes.unshift( type );\n				break;\n			}\n		}\n	}\n\n	// Check to see if we have a response for the expected dataType\n	if ( dataTypes[ 0 ] in responses ) {\n		finalDataType = dataTypes[ 0 ];\n	} else {\n		// Try convertible dataTypes\n		for ( type in responses ) {\n			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[0] ] ) {\n				finalDataType = type;\n				break;\n			}\n			if ( !firstDataType ) {\n				firstDataType = type;\n			}\n		}\n		// Or just use first one\n		finalDataType = finalDataType || firstDataType;\n	}\n\n	// If we found a dataType\n	// We add the dataType to the list if needed\n	// and return the corresponding response\n	if ( finalDataType ) {\n		if ( finalDataType !== dataTypes[ 0 ] ) {\n			dataTypes.unshift( finalDataType );\n		}\n		return responses[ finalDataType ];\n	}\n}\n\n// Chain conversions given the request and the original response\nfunction ajaxConvert( s, response ) {\n	var conv2, current, conv, tmp,\n		converters = {},\n		i = 0,\n		// Work with a copy of dataTypes in case we need to modify it for conversion\n		dataTypes = s.dataTypes.slice(),\n		prev = dataTypes[ 0 ];\n\n	// Apply the dataFilter if provided\n	if ( s.dataFilter ) {\n		response = s.dataFilter( response, s.dataType );\n	}\n\n	// Create converters map with lowercased keys\n	if ( dataTypes[ 1 ] ) {\n		for ( conv in s.converters ) {\n			converters[ conv.toLowerCase() ] = s.converters[ conv ];\n		}\n	}\n\n	// Convert to each sequential dataType, tolerating list modification\n	for ( ; (current = dataTypes[++i]); ) {\n\n		// There\'s only work to do if current dataType is non-auto\n		if ( current !== "*" ) {\n\n			// Convert response if prev dataType is non-auto and differs from current\n			if ( prev !== "*" && prev !== current ) {\n\n				// Seek a direct converter\n				conv = converters[ prev + " " + current ] || converters[ "* " + current ];\n\n				// If none found, seek a pair\n				if ( !conv ) {\n					for ( conv2 in converters ) {\n\n						// If conv2 outputs current\n						tmp = conv2.split(" ");\n						if ( tmp[ 1 ] === current ) {\n\n							// If prev can be converted to accepted input\n							conv = converters[ prev + " " + tmp[ 0 ] ] ||\n								converters[ "* " + tmp[ 0 ] ];\n							if ( conv ) {\n								// Condense equivalence converters\n								if ( conv === true ) {\n									conv = converters[ conv2 ];\n\n								// Otherwise, insert the intermediate dataType\n								} else if ( converters[ conv2 ] !== true ) {\n									current = tmp[ 0 ];\n									dataTypes.splice( i--, 0, current );\n								}\n\n								break;\n							}\n						}\n					}\n				}\n\n				// Apply converter (if not an equivalence)\n				if ( conv !== true ) {\n\n					// Unless errors are allowed to bubble, catch and return them\n					if ( conv && s["throws"] ) {\n						response = conv( response );\n					} else {\n						try {\n							response = conv( response );\n						} catch ( e ) {\n							return { state: "parsererror", error: conv ? e : "No conversion from " + prev + " to " + current };\n						}\n					}\n				}\n			}\n\n			// Update prev for next iteration\n			prev = current;\n		}\n	}\n\n	return { state: "success", data: response };\n}\n// Install script dataType\njQuery.ajaxSetup({\n	accepts: {\n		script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"\n	},\n	contents: {\n		script: /(?:java|ecma)script/\n	},\n	converters: {\n		"text script": function( text ) {\n			jQuery.globalEval( text );\n			return text;\n		}\n	}\n});\n\n// Handle cache\'s special case and global\njQuery.ajaxPrefilter( "script", function( s ) {\n	if ( s.cache === undefined ) {\n		s.cache = false;\n	}\n	if ( s.crossDomain ) {\n		s.type = "GET";\n		s.global = false;\n	}\n});\n\n// Bind script tag hack transport\njQuery.ajaxTransport( "script", function(s) {\n\n	// This transport only deals with cross domain requests\n	if ( s.crossDomain ) {\n\n		var script,\n			head = document.head || jQuery("head")[0] || document.documentElement;\n\n		return {\n\n			send: function( _, callback ) {\n\n				script = document.createElement("script");\n\n				script.async = true;\n\n				if ( s.scriptCharset ) {\n					script.charset = s.scriptCharset;\n				}\n\n				script.src = s.url;\n\n				// Attach handlers for all browsers\n				script.onload = script.onreadystatechange = function( _, isAbort ) {\n\n					if ( isAbort || !script.readyState || /loaded|complete/.test( script.readyState ) ) {\n\n						// Handle memory leak in IE\n						script.onload = script.onreadystatechange = null;\n\n						// Remove the script\n						if ( script.parentNode ) {\n							script.parentNode.removeChild( script );\n						}\n\n						// Dereference the script\n						script = null;\n\n						// Callback if not abort\n						if ( !isAbort ) {\n							callback( 200, "success" );\n						}\n					}\n				};\n\n				// Circumvent IE6 bugs with base elements (#2709 and #4378) by prepending\n				// Use native DOM manipulation to avoid our domManip AJAX trickery\n				head.insertBefore( script, head.firstChild );\n			},\n\n			abort: function() {\n				if ( script ) {\n					script.onload( undefined, true );\n				}\n			}\n		};\n	}\n});\nvar oldCallbacks = [],\n	rjsonp = /(=)\\?(?=&|$)|\\?\\?/;\n\n// Default jsonp settings\njQuery.ajaxSetup({\n	jsonp: "callback",\n	jsonpCallback: function() {\n		var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( ajax_nonce++ ) );\n		this[ callback ] = true;\n		return callback;\n	}\n});\n\n// Detect, normalize options and install callbacks for jsonp requests\njQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {\n\n	var callbackName, overwritten, responseContainer,\n		jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?\n			"url" :\n			typeof s.data === "string" && !( s.contentType || "" ).indexOf("application/x-www-form-urlencoded") && rjsonp.test( s.data ) && "data"\n		);\n\n	// Handle iff the expected data type is "jsonp" or we have a parameter to set\n	if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {\n\n		// Get callback name, remembering preexisting value associated with it\n		callbackName = s.jsonpCallback = jQuery.isFunction( s.jsonpCallback ) ?\n			s.jsonpCallback() :\n			s.jsonpCallback;\n\n		// Insert callback into url or form data\n		if ( jsonProp ) {\n			s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );\n		} else if ( s.jsonp !== false ) {\n			s.url += ( ajax_rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;\n		}\n\n		// Use data converter to retrieve json after script execution\n		s.converters["script json"] = function() {\n			if ( !responseContainer ) {\n				jQuery.error( callbackName + " was not called" );\n			}\n			return responseContainer[ 0 ];\n		};\n\n		// force json dataType\n		s.dataTypes[ 0 ] = "json";\n\n		// Install callback\n		overwritten = window[ callbackName ];\n		window[ callbackName ] = function() {\n			responseContainer = arguments;\n		};\n\n		// Clean-up function (fires after converters)\n		jqXHR.always(function() {\n			// Restore preexisting value\n			window[ callbackName ] = overwritten;\n\n			// Save back as free\n			if ( s[ callbackName ] ) {\n				// make sure that re-using the options doesn\'t screw things around\n				s.jsonpCallback = originalSettings.jsonpCallback;\n\n				// save the callback name for future use\n				oldCallbacks.push( callbackName );\n			}\n\n			// Call if it was a function and we have a response\n			if ( responseContainer && jQuery.isFunction( overwritten ) ) {\n				overwritten( responseContainer[ 0 ] );\n			}\n\n			responseContainer = overwritten = undefined;\n		});\n\n		// Delegate to script\n		return "script";\n	}\n});\nvar xhrCallbacks, xhrSupported,\n	xhrId = 0,\n	// #5280: Internet Explorer will keep connections alive if we don\'t abort on unload\n	xhrOnUnloadAbort = window.ActiveXObject && function() {\n		// Abort all pending requests\n		var key;\n		for ( key in xhrCallbacks ) {\n			xhrCallbacks[ key ]( undefined, true );\n		}\n	};\n\n// Functions to create xhrs\nfunction createStandardXHR() {\n	try {\n		return new window.XMLHttpRequest();\n	} catch( e ) {}\n}\n\nfunction createActiveXHR() {\n	try {\n		return new window.ActiveXObject("Microsoft.XMLHTTP");\n	} catch( e ) {}\n}\n\n// Create the request object\n// (This is still attached to ajaxSettings for backward compatibility)\njQuery.ajaxSettings.xhr = window.ActiveXObject ?\n	/* Microsoft failed to properly\n	 * implement the XMLHttpRequest in IE7 (can\'t request local files),\n	 * so we use the ActiveXObject when it is available\n	 * Additionally XMLHttpRequest can be disabled in IE7/IE8 so\n	 * we need a fallback.\n	 */\n	function() {\n		return !this.isLocal && createStandardXHR() || createActiveXHR();\n	} :\n	// For all other browsers, use the standard XMLHttpRequest object\n	createStandardXHR;\n\n// Determine support properties\nxhrSupported = jQuery.ajaxSettings.xhr();\njQuery.support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );\nxhrSupported = jQuery.support.ajax = !!xhrSupported;\n\n// Create transport if the browser can provide an xhr\nif ( xhrSupported ) {\n\n	jQuery.ajaxTransport(function( s ) {\n		// Cross domain only allowed if supported through XMLHttpRequest\n		if ( !s.crossDomain || jQuery.support.cors ) {\n\n			var callback;\n\n			return {\n				send: function( headers, complete ) {\n\n					// Get a new xhr\n					var handle, i,\n						xhr = s.xhr();\n\n					// Open the socket\n					// Passing null username, generates a login popup on Opera (#2865)\n					if ( s.username ) {\n						xhr.open( s.type, s.url, s.async, s.username, s.password );\n					} else {\n						xhr.open( s.type, s.url, s.async );\n					}\n\n					// Apply custom fields if provided\n					if ( s.xhrFields ) {\n						for ( i in s.xhrFields ) {\n							xhr[ i ] = s.xhrFields[ i ];\n						}\n					}\n\n					// Override mime type if needed\n					if ( s.mimeType && xhr.overrideMimeType ) {\n						xhr.overrideMimeType( s.mimeType );\n					}\n\n					// X-Requested-With header\n					// For cross-domain requests, seeing as conditions for a preflight are\n					// akin to a jigsaw puzzle, we simply never set it to be sure.\n					// (it can always be set on a per-request basis or even using ajaxSetup)\n					// For same-domain requests, won\'t change header if already provided.\n					if ( !s.crossDomain && !headers["X-Requested-With"] ) {\n						headers["X-Requested-With"] = "XMLHttpRequest";\n					}\n\n					// Need an extra try/catch for cross domain requests in Firefox 3\n					try {\n						for ( i in headers ) {\n							xhr.setRequestHeader( i, headers[ i ] );\n						}\n					} catch( err ) {}\n\n					// Do send the request\n					// This may raise an exception which is actually\n					// handled in jQuery.ajax (so no try/catch here)\n					xhr.send( ( s.hasContent && s.data ) || null );\n\n					// Listener\n					callback = function( _, isAbort ) {\n						var status, responseHeaders, statusText, responses;\n\n						// Firefox throws exceptions when accessing properties\n						// of an xhr when a network error occurred\n						// http://helpful.knobs-dials.com/index.php/Component_returned_failure_code:_0x80040111_(NS_ERROR_NOT_AVAILABLE)\n						try {\n\n							// Was never called and is aborted or complete\n							if ( callback && ( isAbort || xhr.readyState === 4 ) ) {\n\n								// Only called once\n								callback = undefined;\n\n								// Do not keep as active anymore\n								if ( handle ) {\n									xhr.onreadystatechange = jQuery.noop;\n									if ( xhrOnUnloadAbort ) {\n										delete xhrCallbacks[ handle ];\n									}\n								}\n\n								// If it\'s an abort\n								if ( isAbort ) {\n									// Abort it manually if needed\n									if ( xhr.readyState !== 4 ) {\n										xhr.abort();\n									}\n								} else {\n									responses = {};\n									status = xhr.status;\n									responseHeaders = xhr.getAllResponseHeaders();\n\n									// When requesting binary data, IE6-9 will throw an exception\n									// on any attempt to access responseText (#11426)\n									if ( typeof xhr.responseText === "string" ) {\n										responses.text = xhr.responseText;\n									}\n\n									// Firefox throws an exception when accessing\n									// statusText for faulty cross-domain requests\n									try {\n										statusText = xhr.statusText;\n									} catch( e ) {\n										// We normalize with Webkit giving an empty statusText\n										statusText = "";\n									}\n\n									// Filter status for non standard behaviors\n\n									// If the request is local and we have data: assume a success\n									// (success with no data won\'t get notified, that\'s the best we\n									// can do given current implementations)\n									if ( !status && s.isLocal && !s.crossDomain ) {\n										status = responses.text ? 200 : 404;\n									// IE - #1450: sometimes returns 1223 when it should be 204\n									} else if ( status === 1223 ) {\n										status = 204;\n									}\n								}\n							}\n						} catch( firefoxAccessException ) {\n							if ( !isAbort ) {\n								complete( -1, firefoxAccessException );\n							}\n						}\n\n						// Call complete if needed\n						if ( responses ) {\n							complete( status, statusText, responses, responseHeaders );\n						}\n					};\n\n					if ( !s.async ) {\n						// if we\'re in sync mode we fire the callback\n						callback();\n					} else if ( xhr.readyState === 4 ) {\n						// (IE6 & IE7) if it\'s in cache and has been\n						// retrieved directly we need to fire the callback\n						setTimeout( callback );\n					} else {\n						handle = ++xhrId;\n						if ( xhrOnUnloadAbort ) {\n							// Create the active xhrs callbacks list if needed\n							// and attach the unload handler\n							if ( !xhrCallbacks ) {\n								xhrCallbacks = {};\n								jQuery( window ).unload( xhrOnUnloadAbort );\n							}\n							// Add to list of active xhrs callbacks\n							xhrCallbacks[ handle ] = callback;\n						}\n						xhr.onreadystatechange = callback;\n					}\n				},\n\n				abort: function() {\n					if ( callback ) {\n						callback( undefined, true );\n					}\n				}\n			};\n		}\n	});\n}\nvar fxNow, timerId,\n	rfxtypes = /^(?:toggle|show|hide)$/,\n	rfxnum = new RegExp( "^(?:([+-])=|)(" + core_pnum + ")([a-z%]*)$", "i" ),\n	rrun = /queueHooks$/,\n	animationPrefilters = [ defaultPrefilter ],\n	tweeners = {\n		"*": [function( prop, value ) {\n			var end, unit,\n				tween = this.createTween( prop, value ),\n				parts = rfxnum.exec( value ),\n				target = tween.cur(),\n				start = +target || 0,\n				scale = 1,\n				maxIterations = 20;\n\n			if ( parts ) {\n				end = +parts[2];\n				unit = parts[3] || ( jQuery.cssNumber[ prop ] ? "" : "px" );\n\n				// We need to compute starting value\n				if ( unit !== "px" && start ) {\n					// Iteratively approximate from a nonzero starting point\n					// Prefer the current property, because this process will be trivial if it uses the same units\n					// Fallback to end or a simple constant\n					start = jQuery.css( tween.elem, prop, true ) || end || 1;\n\n					do {\n						// If previous iteration zeroed out, double until we get *something*\n						// Use a string for doubling factor so we don\'t accidentally see scale as unchanged below\n						scale = scale || ".5";\n\n						// Adjust and apply\n						start = start / scale;\n						jQuery.style( tween.elem, prop, start + unit );\n\n					// Update scale, tolerating zero or NaN from tween.cur()\n					// And breaking the loop if scale is unchanged or perfect, or if we\'ve just had enough\n					} while ( scale !== (scale = tween.cur() / target) && scale !== 1 && --maxIterations );\n				}\n\n				tween.unit = unit;\n				tween.start = start;\n				// If a +=/-= token was provided, we\'re doing a relative animation\n				tween.end = parts[1] ? start + ( parts[1] + 1 ) * end : end;\n			}\n			return tween;\n		}]\n	};\n\n// Animations created synchronously will run synchronously\nfunction createFxNow() {\n	setTimeout(function() {\n		fxNow = undefined;\n	});\n	return ( fxNow = jQuery.now() );\n}\n\nfunction createTweens( animation, props ) {\n	jQuery.each( props, function( prop, value ) {\n		var collection = ( tweeners[ prop ] || [] ).concat( tweeners[ "*" ] ),\n			index = 0,\n			length = collection.length;\n		for ( ; index < length; index++ ) {\n			if ( collection[ index ].call( animation, prop, value ) ) {\n\n				// we\'re done with this property\n				return;\n			}\n		}\n	});\n}\n\nfunction Animation( elem, properties, options ) {\n	var result,\n		stopped,\n		index = 0,\n		length = animationPrefilters.length,\n		deferred = jQuery.Deferred().always( function() {\n			// don\'t match elem in the :animated selector\n			delete tick.elem;\n		}),\n		tick = function() {\n			if ( stopped ) {\n				return false;\n			}\n			var currentTime = fxNow || createFxNow(),\n				remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),\n				// archaic crash bug won\'t allow us to use 1 - ( 0.5 || 0 ) (#12497)\n				temp = remaining / animation.duration || 0,\n				percent = 1 - temp,\n				index = 0,\n				length = animation.tweens.length;\n\n			for ( ; index < length ; index++ ) {\n				animation.tweens[ index ].run( percent );\n			}\n\n			deferred.notifyWith( elem, [ animation, percent, remaining ]);\n\n			if ( percent < 1 && length ) {\n				return remaining;\n			} else {\n				deferred.resolveWith( elem, [ animation ] );\n				return false;\n			}\n		},\n		animation = deferred.promise({\n			elem: elem,\n			props: jQuery.extend( {}, properties ),\n			opts: jQuery.extend( true, { specialEasing: {} }, options ),\n			originalProperties: properties,\n			originalOptions: options,\n			startTime: fxNow || createFxNow(),\n			duration: options.duration,\n			tweens: [],\n			createTween: function( prop, end ) {\n				var tween = jQuery.Tween( elem, animation.opts, prop, end,\n						animation.opts.specialEasing[ prop ] || animation.opts.easing );\n				animation.tweens.push( tween );\n				return tween;\n			},\n			stop: function( gotoEnd ) {\n				var index = 0,\n					// if we are going to the end, we want to run all the tweens\n					// otherwise we skip this part\n					length = gotoEnd ? animation.tweens.length : 0;\n				if ( stopped ) {\n					return this;\n				}\n				stopped = true;\n				for ( ; index < length ; index++ ) {\n					animation.tweens[ index ].run( 1 );\n				}\n\n				// resolve when we played the last frame\n				// otherwise, reject\n				if ( gotoEnd ) {\n					deferred.resolveWith( elem, [ animation, gotoEnd ] );\n				} else {\n					deferred.rejectWith( elem, [ animation, gotoEnd ] );\n				}\n				return this;\n			}\n		}),\n		props = animation.props;\n\n	propFilter( props, animation.opts.specialEasing );\n\n	for ( ; index < length ; index++ ) {\n		result = animationPrefilters[ index ].call( animation, elem, props, animation.opts );\n		if ( result ) {\n			return result;\n		}\n	}\n\n	createTweens( animation, props );\n\n	if ( jQuery.isFunction( animation.opts.start ) ) {\n		animation.opts.start.call( elem, animation );\n	}\n\n	jQuery.fx.timer(\n		jQuery.extend( tick, {\n			elem: elem,\n			anim: animation,\n			queue: animation.opts.queue\n		})\n	);\n\n	// attach callbacks from options\n	return animation.progress( animation.opts.progress )\n		.done( animation.opts.done, animation.opts.complete )\n		.fail( animation.opts.fail )\n		.always( animation.opts.always );\n}\n\nfunction propFilter( props, specialEasing ) {\n	var value, name, index, easing, hooks;\n\n	// camelCase, specialEasing and expand cssHook pass\n	for ( index in props ) {\n		name = jQuery.camelCase( index );\n		easing = specialEasing[ name ];\n		value = props[ index ];\n		if ( jQuery.isArray( value ) ) {\n			easing = value[ 1 ];\n			value = props[ index ] = value[ 0 ];\n		}\n\n		if ( index !== name ) {\n			props[ name ] = value;\n			delete props[ index ];\n		}\n\n		hooks = jQuery.cssHooks[ name ];\n		if ( hooks && "expand" in hooks ) {\n			value = hooks.expand( value );\n			delete props[ name ];\n\n			// not quite $.extend, this wont overwrite keys already present.\n			// also - reusing \'index\' from above because we have the correct "name"\n			for ( index in value ) {\n				if ( !( index in props ) ) {\n					props[ index ] = value[ index ];\n					specialEasing[ index ] = easing;\n				}\n			}\n		} else {\n			specialEasing[ name ] = easing;\n		}\n	}\n}\n\njQuery.Animation = jQuery.extend( Animation, {\n\n	tweener: function( props, callback ) {\n		if ( jQuery.isFunction( props ) ) {\n			callback = props;\n			props = [ "*" ];\n		} else {\n			props = props.split(" ");\n		}\n\n		var prop,\n			index = 0,\n			length = props.length;\n\n		for ( ; index < length ; index++ ) {\n			prop = props[ index ];\n			tweeners[ prop ] = tweeners[ prop ] || [];\n			tweeners[ prop ].unshift( callback );\n		}\n	},\n\n	prefilter: function( callback, prepend ) {\n		if ( prepend ) {\n			animationPrefilters.unshift( callback );\n		} else {\n			animationPrefilters.push( callback );\n		}\n	}\n});\n\nfunction defaultPrefilter( elem, props, opts ) {\n	/*jshint validthis:true */\n	var prop, index, length,\n		value, dataShow, toggle,\n		tween, hooks, oldfire,\n		anim = this,\n		style = elem.style,\n		orig = {},\n		handled = [],\n		hidden = elem.nodeType && isHidden( elem );\n\n	// handle queue: false promises\n	if ( !opts.queue ) {\n		hooks = jQuery._queueHooks( elem, "fx" );\n		if ( hooks.unqueued == null ) {\n			hooks.unqueued = 0;\n			oldfire = hooks.empty.fire;\n			hooks.empty.fire = function() {\n				if ( !hooks.unqueued ) {\n					oldfire();\n				}\n			};\n		}\n		hooks.unqueued++;\n\n		anim.always(function() {\n			// doing this makes sure that the complete handler will be called\n			// before this completes\n			anim.always(function() {\n				hooks.unqueued--;\n				if ( !jQuery.queue( elem, "fx" ).length ) {\n					hooks.empty.fire();\n				}\n			});\n		});\n	}\n\n	// height/width overflow pass\n	if ( elem.nodeType === 1 && ( "height" in props || "width" in props ) ) {\n		// Make sure that nothing sneaks out\n		// Record all 3 overflow attributes because IE does not\n		// change the overflow attribute when overflowX and\n		// overflowY are set to the same value\n		opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];\n\n		// Set display property to inline-block for height/width\n		// animations on inline elements that are having width/height animated\n		if ( jQuery.css( elem, "display" ) === "inline" &&\n				jQuery.css( elem, "float" ) === "none" ) {\n\n			// inline-level elements accept inline-block;\n			// block-level elements need to be inline with layout\n			if ( !jQuery.support.inlineBlockNeedsLayout || css_defaultDisplay( elem.nodeName ) === "inline" ) {\n				style.display = "inline-block";\n\n			} else {\n				style.zoom = 1;\n			}\n		}\n	}\n\n	if ( opts.overflow ) {\n		style.overflow = "hidden";\n		if ( !jQuery.support.shrinkWrapBlocks ) {\n			anim.always(function() {\n				style.overflow = opts.overflow[ 0 ];\n				style.overflowX = opts.overflow[ 1 ];\n				style.overflowY = opts.overflow[ 2 ];\n			});\n		}\n	}\n\n\n	// show/hide pass\n	for ( index in props ) {\n		value = props[ index ];\n		if ( rfxtypes.exec( value ) ) {\n			delete props[ index ];\n			toggle = toggle || value === "toggle";\n			if ( value === ( hidden ? "hide" : "show" ) ) {\n				continue;\n			}\n			handled.push( index );\n		}\n	}\n\n	length = handled.length;\n	if ( length ) {\n		dataShow = jQuery._data( elem, "fxshow" ) || jQuery._data( elem, "fxshow", {} );\n		if ( "hidden" in dataShow ) {\n			hidden = dataShow.hidden;\n		}\n\n		// store state if its toggle - enables .stop().toggle() to "reverse"\n		if ( toggle ) {\n			dataShow.hidden = !hidden;\n		}\n		if ( hidden ) {\n			jQuery( elem ).show();\n		} else {\n			anim.done(function() {\n				jQuery( elem ).hide();\n			});\n		}\n		anim.done(function() {\n			var prop;\n			jQuery._removeData( elem, "fxshow" );\n			for ( prop in orig ) {\n				jQuery.style( elem, prop, orig[ prop ] );\n			}\n		});\n		for ( index = 0 ; index < length ; index++ ) {\n			prop = handled[ index ];\n			tween = anim.createTween( prop, hidden ? dataShow[ prop ] : 0 );\n			orig[ prop ] = dataShow[ prop ] || jQuery.style( elem, prop );\n\n			if ( !( prop in dataShow ) ) {\n				dataShow[ prop ] = tween.start;\n				if ( hidden ) {\n					tween.end = tween.start;\n					tween.start = prop === "width" || prop === "height" ? 1 : 0;\n				}\n			}\n		}\n	}\n}\n\nfunction Tween( elem, options, prop, end, easing ) {\n	return new Tween.prototype.init( elem, options, prop, end, easing );\n}\njQuery.Tween = Tween;\n\nTween.prototype = {\n	constructor: Tween,\n	init: function( elem, options, prop, end, easing, unit ) {\n		this.elem = elem;\n		this.prop = prop;\n		this.easing = easing || "swing";\n		this.options = options;\n		this.start = this.now = this.cur();\n		this.end = end;\n		this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );\n	},\n	cur: function() {\n		var hooks = Tween.propHooks[ this.prop ];\n\n		return hooks && hooks.get ?\n			hooks.get( this ) :\n			Tween.propHooks._default.get( this );\n	},\n	run: function( percent ) {\n		var eased,\n			hooks = Tween.propHooks[ this.prop ];\n\n		if ( this.options.duration ) {\n			this.pos = eased = jQuery.easing[ this.easing ](\n				percent, this.options.duration * percent, 0, 1, this.options.duration\n			);\n		} else {\n			this.pos = eased = percent;\n		}\n		this.now = ( this.end - this.start ) * eased + this.start;\n\n		if ( this.options.step ) {\n			this.options.step.call( this.elem, this.now, this );\n		}\n\n		if ( hooks && hooks.set ) {\n			hooks.set( this );\n		} else {\n			Tween.propHooks._default.set( this );\n		}\n		return this;\n	}\n};\n\nTween.prototype.init.prototype = Tween.prototype;\n\nTween.propHooks = {\n	_default: {\n		get: function( tween ) {\n			var result;\n\n			if ( tween.elem[ tween.prop ] != null &&\n				(!tween.elem.style || tween.elem.style[ tween.prop ] == null) ) {\n				return tween.elem[ tween.prop ];\n			}\n\n			// passing an empty string as a 3rd parameter to .css will automatically\n			// attempt a parseFloat and fallback to a string if the parse fails\n			// so, simple values such as "10px" are parsed to Float.\n			// complex values such as "rotate(1rad)" are returned as is.\n			result = jQuery.css( tween.elem, tween.prop, "" );\n			// Empty strings, null, undefined and "auto" are converted to 0.\n			return !result || result === "auto" ? 0 : result;\n		},\n		set: function( tween ) {\n			// use step hook for back compat - use cssHook if its there - use .style if its\n			// available and use plain properties where available\n			if ( jQuery.fx.step[ tween.prop ] ) {\n				jQuery.fx.step[ tween.prop ]( tween );\n			} else if ( tween.elem.style && ( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null || jQuery.cssHooks[ tween.prop ] ) ) {\n				jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );\n			} else {\n				tween.elem[ tween.prop ] = tween.now;\n			}\n		}\n	}\n};\n\n// Remove in 2.0 - this supports IE8\'s panic based approach\n// to setting things on disconnected nodes\n\nTween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {\n	set: function( tween ) {\n		if ( tween.elem.nodeType && tween.elem.parentNode ) {\n			tween.elem[ tween.prop ] = tween.now;\n		}\n	}\n};\n\njQuery.each([ "toggle", "show", "hide" ], function( i, name ) {\n	var cssFn = jQuery.fn[ name ];\n	jQuery.fn[ name ] = function( speed, easing, callback ) {\n		return speed == null || typeof speed === "boolean" ?\n			cssFn.apply( this, arguments ) :\n			this.animate( genFx( name, true ), speed, easing, callback );\n	};\n});\n\njQuery.fn.extend({\n	fadeTo: function( speed, to, easing, callback ) {\n\n		// show any hidden elements after setting opacity to 0\n		return this.filter( isHidden ).css( "opacity", 0 ).show()\n\n			// animate to the value specified\n			.end().animate({ opacity: to }, speed, easing, callback );\n	},\n	animate: function( prop, speed, easing, callback ) {\n		var empty = jQuery.isEmptyObject( prop ),\n			optall = jQuery.speed( speed, easing, callback ),\n			doAnimation = function() {\n				// Operate on a copy of prop so per-property easing won\'t be lost\n				var anim = Animation( this, jQuery.extend( {}, prop ), optall );\n				doAnimation.finish = function() {\n					anim.stop( true );\n				};\n				// Empty animations, or finishing resolves immediately\n				if ( empty || jQuery._data( this, "finish" ) ) {\n					anim.stop( true );\n				}\n			};\n			doAnimation.finish = doAnimation;\n\n		return empty || optall.queue === false ?\n			this.each( doAnimation ) :\n			this.queue( optall.queue, doAnimation );\n	},\n	stop: function( type, clearQueue, gotoEnd ) {\n		var stopQueue = function( hooks ) {\n			var stop = hooks.stop;\n			delete hooks.stop;\n			stop( gotoEnd );\n		};\n\n		if ( typeof type !== "string" ) {\n			gotoEnd = clearQueue;\n			clearQueue = type;\n			type = undefined;\n		}\n		if ( clearQueue && type !== false ) {\n			this.queue( type || "fx", [] );\n		}\n\n		return this.each(function() {\n			var dequeue = true,\n				index = type != null && type + "queueHooks",\n				timers = jQuery.timers,\n				data = jQuery._data( this );\n\n			if ( index ) {\n				if ( data[ index ] && data[ index ].stop ) {\n					stopQueue( data[ index ] );\n				}\n			} else {\n				for ( index in data ) {\n					if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {\n						stopQueue( data[ index ] );\n					}\n				}\n			}\n\n			for ( index = timers.length; index--; ) {\n				if ( timers[ index ].elem === this && (type == null || timers[ index ].queue === type) ) {\n					timers[ index ].anim.stop( gotoEnd );\n					dequeue = false;\n					timers.splice( index, 1 );\n				}\n			}\n\n			// start the next in the queue if the last step wasn\'t forced\n			// timers currently will call their complete callbacks, which will dequeue\n			// but only if they were gotoEnd\n			if ( dequeue || !gotoEnd ) {\n				jQuery.dequeue( this, type );\n			}\n		});\n	},\n	finish: function( type ) {\n		if ( type !== false ) {\n			type = type || "fx";\n		}\n		return this.each(function() {\n			var index,\n				data = jQuery._data( this ),\n				queue = data[ type + "queue" ],\n				hooks = data[ type + "queueHooks" ],\n				timers = jQuery.timers,\n				length = queue ? queue.length : 0;\n\n			// enable finishing flag on private data\n			data.finish = true;\n\n			// empty the queue first\n			jQuery.queue( this, type, [] );\n\n			if ( hooks && hooks.cur && hooks.cur.finish ) {\n				hooks.cur.finish.call( this );\n			}\n\n			// look for any active animations, and finish them\n			for ( index = timers.length; index--; ) {\n				if ( timers[ index ].elem === this && timers[ index ].queue === type ) {\n					timers[ index ].anim.stop( true );\n					timers.splice( index, 1 );\n				}\n			}\n\n			// look for any animations in the old queue and finish them\n			for ( index = 0; index < length; index++ ) {\n				if ( queue[ index ] && queue[ index ].finish ) {\n					queue[ index ].finish.call( this );\n				}\n			}\n\n			// turn off finishing flag\n			delete data.finish;\n		});\n	}\n});\n\n// Generate parameters to create a standard animation\nfunction genFx( type, includeWidth ) {\n	var which,\n		attrs = { height: type },\n		i = 0;\n\n	// if we include width, step value is 1 to do all cssExpand values,\n	// if we don\'t include width, step value is 2 to skip over Left and Right\n	includeWidth = includeWidth? 1 : 0;\n	for( ; i < 4 ; i += 2 - includeWidth ) {\n		which = cssExpand[ i ];\n		attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;\n	}\n\n	if ( includeWidth ) {\n		attrs.opacity = attrs.width = type;\n	}\n\n	return attrs;\n}\n\n// Generate shortcuts for custom animations\njQuery.each({\n	slideDown: genFx("show"),\n	slideUp: genFx("hide"),\n	slideToggle: genFx("toggle"),\n	fadeIn: { opacity: "show" },\n	fadeOut: { opacity: "hide" },\n	fadeToggle: { opacity: "toggle" }\n}, function( name, props ) {\n	jQuery.fn[ name ] = function( speed, easing, callback ) {\n		return this.animate( props, speed, easing, callback );\n	};\n});\n\njQuery.speed = function( speed, easing, fn ) {\n	var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {\n		complete: fn || !fn && easing ||\n			jQuery.isFunction( speed ) && speed,\n		duration: speed,\n		easing: fn && easing || easing && !jQuery.isFunction( easing ) && easing\n	};\n\n	opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ? opt.duration :\n		opt.duration in jQuery.fx.speeds ? jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;\n\n	// normalize opt.queue - true/undefined/null -> "fx"\n	if ( opt.queue == null || opt.queue === true ) {\n		opt.queue = "fx";\n	}\n\n	// Queueing\n	opt.old = opt.complete;\n\n	opt.complete = function() {\n		if ( jQuery.isFunction( opt.old ) ) {\n			opt.old.call( this );\n		}\n\n		if ( opt.queue ) {\n			jQuery.dequeue( this, opt.queue );\n		}\n	};\n\n	return opt;\n};\n\njQuery.easing = {\n	linear: function( p ) {\n		return p;\n	},\n	swing: function( p ) {\n		return 0.5 - Math.cos( p*Math.PI ) / 2;\n	}\n};\n\njQuery.timers = [];\njQuery.fx = Tween.prototype.init;\njQuery.fx.tick = function() {\n	var timer,\n		timers = jQuery.timers,\n		i = 0;\n\n	fxNow = jQuery.now();\n\n	for ( ; i < timers.length; i++ ) {\n		timer = timers[ i ];\n		// Checks the timer has not already been removed\n		if ( !timer() && timers[ i ] === timer ) {\n			timers.splice( i--, 1 );\n		}\n	}\n\n	if ( !timers.length ) {\n		jQuery.fx.stop();\n	}\n	fxNow = undefined;\n};\n\njQuery.fx.timer = function( timer ) {\n	if ( timer() && jQuery.timers.push( timer ) ) {\n		jQuery.fx.start();\n	}\n};\n\njQuery.fx.interval = 13;\n\njQuery.fx.start = function() {\n	if ( !timerId ) {\n		timerId = setInterval( jQuery.fx.tick, jQuery.fx.interval );\n	}\n};\n\njQuery.fx.stop = function() {\n	clearInterval( timerId );\n	timerId = null;\n};\n\njQuery.fx.speeds = {\n	slow: 600,\n	fast: 200,\n	// Default speed\n	_default: 400\n};\n\n// Back Compat <1.8 extension point\njQuery.fx.step = {};\n\nif ( jQuery.expr && jQuery.expr.filters ) {\n	jQuery.expr.filters.animated = function( elem ) {\n		return jQuery.grep(jQuery.timers, function( fn ) {\n			return elem === fn.elem;\n		}).length;\n	};\n}\njQuery.fn.offset = function( options ) {\n	if ( arguments.length ) {\n		return options === undefined ?\n			this :\n			this.each(function( i ) {\n				jQuery.offset.setOffset( this, options, i );\n			});\n	}\n\n	var docElem, win,\n		box = { top: 0, left: 0 },\n		elem = this[ 0 ],\n		doc = elem && elem.ownerDocument;\n\n	if ( !doc ) {\n		return;\n	}\n\n	docElem = doc.documentElement;\n\n	// Make sure it\'s not a disconnected DOM node\n	if ( !jQuery.contains( docElem, elem ) ) {\n		return box;\n	}\n\n	// If we don\'t have gBCR, just use 0,0 rather than error\n	// BlackBerry 5, iOS 3 (original iPhone)\n	if ( typeof elem.getBoundingClientRect !== core_strundefined ) {\n		box = elem.getBoundingClientRect();\n	}\n	win = getWindow( doc );\n	return {\n		top: box.top  + ( win.pageYOffset || docElem.scrollTop )  - ( docElem.clientTop  || 0 ),\n		left: box.left + ( win.pageXOffset || docElem.scrollLeft ) - ( docElem.clientLeft || 0 )\n	};\n};\n\njQuery.offset = {\n\n	setOffset: function( elem, options, i ) {\n		var position = jQuery.css( elem, "position" );\n\n		// set position first, in-case top/left are set even on static elem\n		if ( position === "static" ) {\n			elem.style.position = "relative";\n		}\n\n		var curElem = jQuery( elem ),\n			curOffset = curElem.offset(),\n			curCSSTop = jQuery.css( elem, "top" ),\n			curCSSLeft = jQuery.css( elem, "left" ),\n			calculatePosition = ( position === "absolute" || position === "fixed" ) && jQuery.inArray("auto", [curCSSTop, curCSSLeft]) > -1,\n			props = {}, curPosition = {}, curTop, curLeft;\n\n		// need to be able to calculate position if either top or left is auto and position is either absolute or fixed\n		if ( calculatePosition ) {\n			curPosition = curElem.position();\n			curTop = curPosition.top;\n			curLeft = curPosition.left;\n		} else {\n			curTop = parseFloat( curCSSTop ) || 0;\n			curLeft = parseFloat( curCSSLeft ) || 0;\n		}\n\n		if ( jQuery.isFunction( options ) ) {\n			options = options.call( elem, i, curOffset );\n		}\n\n		if ( options.top != null ) {\n			props.top = ( options.top - curOffset.top ) + curTop;\n		}\n		if ( options.left != null ) {\n			props.left = ( options.left - curOffset.left ) + curLeft;\n		}\n\n		if ( "using" in options ) {\n			options.using.call( elem, props );\n		} else {\n			curElem.css( props );\n		}\n	}\n};\n\n\njQuery.fn.extend({\n\n	position: function() {\n		if ( !this[ 0 ] ) {\n			return;\n		}\n\n		var offsetParent, offset,\n			parentOffset = { top: 0, left: 0 },\n			elem = this[ 0 ];\n\n		// fixed elements are offset from window (parentOffset = {top:0, left: 0}, because it is it\'s only offset parent\n		if ( jQuery.css( elem, "position" ) === "fixed" ) {\n			// we assume that getBoundingClientRect is available when computed position is fixed\n			offset = elem.getBoundingClientRect();\n		} else {\n			// Get *real* offsetParent\n			offsetParent = this.offsetParent();\n\n			// Get correct offsets\n			offset = this.offset();\n			if ( !jQuery.nodeName( offsetParent[ 0 ], "html" ) ) {\n				parentOffset = offsetParent.offset();\n			}\n\n			// Add offsetParent borders\n			parentOffset.top  += jQuery.css( offsetParent[ 0 ], "borderTopWidth", true );\n			parentOffset.left += jQuery.css( offsetParent[ 0 ], "borderLeftWidth", true );\n		}\n\n		// Subtract parent offsets and element margins\n		// note: when an element has margin: auto the offsetLeft and marginLeft\n		// are the same in Safari causing offset.left to incorrectly be 0\n		return {\n			top:  offset.top  - parentOffset.top - jQuery.css( elem, "marginTop", true ),\n			left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true)\n		};\n	},\n\n	offsetParent: function() {\n		return this.map(function() {\n			var offsetParent = this.offsetParent || document.documentElement;\n			while ( offsetParent && ( !jQuery.nodeName( offsetParent, "html" ) && jQuery.css( offsetParent, "position") === "static" ) ) {\n				offsetParent = offsetParent.offsetParent;\n			}\n			return offsetParent || document.documentElement;\n		});\n	}\n});\n\n\n// Create scrollLeft and scrollTop methods\njQuery.each( {scrollLeft: "pageXOffset", scrollTop: "pageYOffset"}, function( method, prop ) {\n	var top = /Y/.test( prop );\n\n	jQuery.fn[ method ] = function( val ) {\n		return jQuery.access( this, function( elem, method, val ) {\n			var win = getWindow( elem );\n\n			if ( val === undefined ) {\n				return win ? (prop in win) ? win[ prop ] :\n					win.document.documentElement[ method ] :\n					elem[ method ];\n			}\n\n			if ( win ) {\n				win.scrollTo(\n					!top ? val : jQuery( win ).scrollLeft(),\n					top ? val : jQuery( win ).scrollTop()\n				);\n\n			} else {\n				elem[ method ] = val;\n			}\n		}, method, val, arguments.length, null );\n	};\n});\n\nfunction getWindow( elem ) {\n	return jQuery.isWindow( elem ) ?\n		elem :\n		elem.nodeType === 9 ?\n			elem.defaultView || elem.parentWindow :\n			false;\n}\n// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods\njQuery.each( { Height: "height", Width: "width" }, function( name, type ) {\n	jQuery.each( { padding: "inner" + name, content: type, "": "outer" + name }, function( defaultExtra, funcName ) {\n		// margin is only for outerHeight, outerWidth\n		jQuery.fn[ funcName ] = function( margin, value ) {\n			var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),\n				extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );\n\n			return jQuery.access( this, function( elem, type, value ) {\n				var doc;\n\n				if ( jQuery.isWindow( elem ) ) {\n					// As of 5/8/2012 this will yield incorrect results for Mobile Safari, but there\n					// isn\'t a whole lot we can do. See pull request at this URL for discussion:\n					// https://github.com/jquery/jquery/pull/764\n					return elem.document.documentElement[ "client" + name ];\n				}\n\n				// Get document width or height\n				if ( elem.nodeType === 9 ) {\n					doc = elem.documentElement;\n\n					// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height], whichever is greatest\n					// unfortunately, this causes bug #3838 in IE6/8 only, but there is currently no good, small way to fix it.\n					return Math.max(\n						elem.body[ "scroll" + name ], doc[ "scroll" + name ],\n						elem.body[ "offset" + name ], doc[ "offset" + name ],\n						doc[ "client" + name ]\n					);\n				}\n\n				return value === undefined ?\n					// Get width or height on the element, requesting but not forcing parseFloat\n					jQuery.css( elem, type, extra ) :\n\n					// Set width or height on the element\n					jQuery.style( elem, type, value, extra );\n			}, type, chainable ? margin : undefined, chainable, null );\n		};\n	});\n});\n// Limit scope pollution from any deprecated API\n// (function() {\n\n// })();\n// Expose jQuery to the global object\nwindow.jQuery = window.$ = jQuery;\n\n// Expose jQuery as an AMD module, but only for AMD loaders that\n// understand the issues with loading multiple versions of jQuery\n// in a page that all might call define(). The loader will indicate\n// they have special allowances for multiple jQuery versions by\n// specifying define.amd.jQuery = true. Register as a named module,\n// since jQuery can be concatenated with other files that may use define,\n// but not use a proper concatenation script that understands anonymous\n// AMD modules. A named AMD is safest and most robust way to register.\n// Lowercase jquery is used because AMD module names are derived from\n// file names, and jQuery is normally delivered in a lowercase file name.\n// Do this after creating the global so that if an AMD module wants to call\n// noConflict to hide this version of jQuery, it will work.\nif ( typeof define === "function" && define.amd && define.amd.jQuery ) {\n	define( "jquery", [], function () { return jQuery; } );\n}\n\n})( window );');

/*can/util/can*/
define("can/util/can",[],function(){var e;"undefined"!=typeof window?(e=window.can||{},("undefined"==typeof GLOBALCAN||GLOBALCAN!==!1)&&(window.can=e)):e={},e.k=function(){},e.isDeferred=function(e){var o=this.isFunction;return e&&o(e.then)&&o(e.pipe)};var o=0;return e.cid=function(e,n){return e._cid||(o++,e._cid=(n||"")+o),e._cid},e.VERSION="@EDGE",e.simpleExtend=function(e,o){for(var n in o)e[n]=o[n];return e},e.frag=function(o){var n;return o&&"string"!=typeof o?11===o.nodeType?o:"number"==typeof o.nodeType?(n=document.createDocumentFragment(),n.appendChild(o),n):"number"==typeof o.length?(n=document.createDocumentFragment(),e.each(o,function(o){n.appendChild(e.frag(o))}),n):(n=e.buildFragment(""+o,document.body),n.childNodes.length||n.appendChild(document.createTextNode("")),n):(n=e.buildFragment(null==o?"":""+o,document.body),n.childNodes.length||n.appendChild(document.createTextNode("")),n)},e.__reading=function(){},e.dev={warnTimeout:5e3,logLevel:0,warn:function(e){var o=this.logLevel;2>o&&(Array.prototype.unshift.call(arguments,"WARN:"),window.console&&console.warn?this._logger("warn",Array.prototype.slice.call(arguments)):window.console&&console.log?this._logger("log",Array.prototype.slice.call(arguments)):window.opera&&window.opera.postError&&window.opera.postError("steal.js WARNING: "+e))},log:function(e){var o=this.logLevel;1>o&&(window.console&&console.log?(Array.prototype.unshift.call(arguments,"Info:"),this._logger("log",Array.prototype.slice.call(arguments))):window.opera&&window.opera.postError&&window.opera.postError("steal.js INFO: "+e))},_logger:function(e,o){console.log.apply?console[e].apply(console,o):console[e](o)}},e});
/*can/util/attr/attr*/
define("can/util/attr/attr",["can/util/can"],function(t){var e="undefined"!=typeof window&&window.setImmediate||function(t){return setTimeout(t,0)},n={MutationObserver:"undefined"!=typeof window&&(window.MutationObserver||window.WebKitMutationObserver||window.MozMutationObserver),map:{"class":"className",value:"value",innerText:"innerText",textContent:"textContent",checked:!0,disabled:!0,readonly:!0,required:!0,src:function(t,e){return null==e||""===e?(t.removeAttribute("src"),null):(t.setAttribute("src",e),e)},style:function(t,e){return t.style.cssText=e||""}},defaultValue:["input","textarea"],set:function(e,r,u){var i;n.MutationObserver||(i=n.get(e,r));var a,o=e.nodeName.toString().toLowerCase(),d=n.map[r];"function"==typeof d?a=d(e,u):d===!0?(a=e[r]=!0,"checked"===r&&"radio"===e.type&&t.inArray(o,n.defaultValue)>=0&&(e.defaultChecked=!0)):d?(a=e[d]=u,"value"===d&&t.inArray(o,n.defaultValue)>=0&&(e.defaultValue=u)):(e.setAttribute(r,u),a=u),n.MutationObserver||a===i||n.trigger(e,r,i)},trigger:function(n,r,u){return t.data(t.$(n),"canHasAttributesBindings")?e(function(){t.trigger(n,{type:"attributes",attributeName:r,target:n,oldValue:u,bubbles:!1},[])}):void 0},get:function(t,e){var r=n.map[e];return"string"==typeof r&&t[r]?t[r]:t.getAttribute(e)},remove:function(t,e){var r;n.MutationObserver||(r=n.get(t,e));var u=n.map[e];"function"==typeof u&&u(t,void 0),u===!0?t[e]=!1:"string"==typeof u?t[u]="":t.removeAttribute(e),n.MutationObserver||null==r||n.trigger(t,e,r)},has:function(){if("undefined"!=typeof document){var t=document.createElement("div");return t.hasAttribute?function(t,e){return t.hasAttribute(e)}:function(t,e){return null!==t.getAttribute(e)}}return function(){}}()};return n});
/*can/event/event*/
define("can/event/event",["can/util/can"],function(n){return n.addEvent=function(n,e){var t=this.__bindEvents||(this.__bindEvents={}),i=t[n]||(t[n]=[]);return i.push({handler:e,name:n}),this},n.listenTo=function(e,t,i){var s=this.__listenToEvents;s||(s=this.__listenToEvents={});var r=n.cid(e),v=s[r];v||(v=s[r]={obj:e,events:{}});var a=v.events[t];a||(a=v.events[t]=[]),a.push(i),n.bind.call(e,t,i)},n.stopListening=function(e,t,i){var s=this.__listenToEvents,r=s,v=0;if(!s)return this;if(e){var a=n.cid(e);if((r={})[a]=s[a],!s[a])return this}for(var o in r){var d,l=r[o];e=s[o].obj,t?(d={})[t]=l.events[t]:d=l.events;for(var c in d){var h=d[c]||[];for(v=0;v<h.length;)i&&i===h[v]||!i?(n.unbind.call(e,c,h[v]),h.splice(v,1)):v++;h.length||delete l.events[c]}n.isEmptyObject(l.events)&&delete s[o]}return this},n.removeEvent=function(n,e,t){if(!this.__bindEvents)return this;for(var i,s=this.__bindEvents[n]||[],r=0,v="function"==typeof e;r<s.length;)i=s[r],(t?t(i,n,e):v&&i.handler===e||!v&&(i.cid===e||!e))?s.splice(r,1):r++;return this},n.dispatch=function(n,e){var t=this.__bindEvents;if(t){"string"==typeof n&&(n={type:n});var i=n.type,s=(t[i]||[]).slice(0);e=[n].concat(e||[]);for(var r=0,v=s.length;v>r;r++)s[r].handler.apply(this,e);return n}},n.one=function(e,t){var i=function(){return n.unbind.call(this,e,i),t.apply(this,arguments)};return n.bind.call(this,e,i),this},n.event={on:n.addEvent,off:n.removeEvent,bind:n.addEvent,unbind:n.removeEvent,delegate:function(e,t,i){return n.addEvent.call(t,i)},undelegate:function(e,t,i){return n.removeEvent.call(t,i)},trigger:n.dispatch,one:n.one,addEvent:n.addEvent,removeEvent:n.removeEvent,listenTo:n.listenTo,stopListening:n.stopListening,dispatch:n.dispatch},n.event});
/*can/util/array/each*/
define("can/util/array/each",["can/util/can"],function(t){var e=function(t){var e=t.length;return"function"!=typeof arr&&(0===e||"number"==typeof e&&e>0&&e-1 in t)};return t.each=function(n,a,r){var f,i,l,c=0;if(n)if(e(n))if(t.List&&n instanceof t.List)for(i=n.attr("length");i>c&&(l=n.attr(c),a.call(r||l,l,c,n)!==!1);c++);else for(i=n.length;i>c&&(l=n[c],a.call(r||l,l,c,n)!==!1);c++);else if("object"==typeof n)if(t.Map&&n instanceof t.Map||n===t.route){var o=t.Map.keys(n);for(c=0,i=o.length;i>c&&(f=o[c],l=n.attr(f),a.call(r||l,l,f,n)!==!1);c++);}else for(f in n)if(n.hasOwnProperty(f)&&a.call(r||n[f],n[f],f,n)===!1)break;return n},t});
/*can/util/inserted/inserted*/
define("can/util/inserted/inserted",["can/util/can"],function(e){e.inserted=function(n){n=e.makeArray(n);for(var r,i,t=!1,a=e.$(document.contains?document:document.body),d=0;void 0!==(i=n[d]);d++){if(!t){if(!i.getElementsByTagName)continue;if(!e.has(a,i).length)return;t=!0}if(t&&i.getElementsByTagName){r=e.makeArray(i.getElementsByTagName("*")),e.trigger(i,"inserted",[],!1);for(var o,s=0;void 0!==(o=r[s]);s++)e.trigger(o,"inserted",[],!1)}}},e.appendChild=function(n,r){var i;i=11===r.nodeType?e.makeArray(r.childNodes):[r],n.appendChild(r),e.inserted(i)},e.insertBefore=function(n,r,i){var t;t=11===r.nodeType?e.makeArray(r.childNodes):[r],n.insertBefore(r,i),e.inserted(t)}});
/*can/util/jquery/jquery*/
define("can/util/jquery/jquery",["jquery/jquery","can/util/can","can/util/attr/attr","can/event/event","can/util/array/each","can/util/inserted/inserted"],function(t,e,n){var r=function(t){return t.nodeName&&(1===t.nodeType||9===t.nodeType)||t==window};t.extend(e,t,{trigger:function(n,a,i,u){r(n)?t.event.trigger(a,i,n,!u):n.trigger?n.trigger(a,i):("string"==typeof a&&(a={type:a}),a.target=a.target||n,e.dispatch.call(n,a,i))},event:e.event,addEvent:e.addEvent,removeEvent:e.removeEvent,buildFragment:function(e,n){var r;return e=[e],n=n||document,n=!n.nodeType&&n[0]||n,n=n.ownerDocument||n,r=t.buildFragment(e,n),r.cacheable?t.clone(r.fragment):r.fragment||r},$:t,each:e.each,bind:function(n,a){return this.bind&&this.bind!==e.bind?this.bind(n,a):r(this)?t.event.add(this,n,a):e.addEvent.call(this,n,a),this},unbind:function(n,a){return this.unbind&&this.unbind!==e.unbind?this.unbind(n,a):r(this)?t.event.remove(this,n,a):e.removeEvent.call(this,n,a),this},delegate:function(n,a,i){return this.delegate?this.delegate(n,a,i):r(this)?t(this).delegate(n,a,i):e.bind.call(this,a,i),this},undelegate:function(n,a,i){return this.undelegate?this.undelegate(n,a,i):r(this)?t(this).undelegate(n,a,i):e.unbind.call(this,a,i),this},proxy:function(t,e){return function(){return t.apply(e,arguments)}},attr:n}),e.on=e.bind,e.off=e.unbind,t.each(["append","filter","addClass","remove","data","get","has"],function(t,n){e[n]=function(t){return t[n].apply(t,e.makeArray(arguments).slice(1))}});var a=t.cleanData;t.cleanData=function(n){t.each(n,function(t,n){n&&e.trigger(n,"removed",[],!1)}),a(n)};var i,u=t.fn.domManip;if(t.fn.domManip=function(){for(var t=1;t<arguments.length;t++)if("function"==typeof arguments[t]){i=t;break}return u.apply(this,arguments)},t(document.createElement("div")).append(document.createElement("div")),t.fn.domManip=2===i?function(t,n,r){return u.call(this,t,n,function(t){var n;11===t.nodeType&&(n=e.makeArray(t.childNodes));var a=r.apply(this,arguments);return e.inserted(n?n:[t]),a})}:function(t,n){return u.call(this,t,function(t){var r;11===t.nodeType&&(r=e.makeArray(t.childNodes));var a=n.apply(this,arguments);return e.inserted(r?r:[t]),a})},e.attr.MutationObserver)t.event.special.attributes={setup:function(){var t=this,n=new e.attr.MutationObserver(function(n){n.forEach(function(n){var r=e.simpleExtend({},n);e.trigger(t,r,[])})});n.observe(this,{attributes:!0,attributeOldValue:!0}),e.data(e.$(this),"canAttributesObserver",n)},teardown:function(){e.data(e.$(this),"canAttributesObserver").disconnect(),t.removeData(this,"canAttributesObserver")}};else{var s=t.attr;t.attr=function(t,n){var r,a;arguments.length>=3&&(r=s.call(this,t,n));var i=s.apply(this,arguments);return arguments.length>=3&&(a=s.call(this,t,n)),a!==r&&e.attr.trigger(t,n,r),i};var d=t.removeAttr;t.removeAttr=function(t,n){var r=s.call(this,t,n),a=d.apply(this,arguments);return null!=r&&e.attr.trigger(t,n,r),a},t.event.special.attributes={setup:function(){e.data(e.$(this),"canHasAttributesBindings",!0)},teardown:function(){t.removeData(this,"canHasAttributesBindings")}}}return function(){var t="<-\n>",n=e.buildFragment(t,document);if(t!==n.childNodes[0].nodeValue){var r=e.buildFragment;e.buildFragment=function(t,e){var n=r(t,e);return 1===n.childNodes.length&&3===n.childNodes[0].nodeType&&(n.childNodes[0].nodeValue=t),n}}}(),t.event.special.inserted={},t.event.special.removed={},e});
/*can/util/bind/bind*/
define("can/util/bind/bind",["can/util/util"],function(i){return i.bindAndSetup=function(){return i.addEvent.apply(this,arguments),this._init||(this._bindings?this._bindings++:(this._bindings=1,this._bindsetup&&this._bindsetup())),this},i.unbindAndTeardown=function(){return i.removeEvent.apply(this,arguments),null===this._bindings?this._bindings=0:this._bindings--,!this._bindings&&this._bindteardown&&this._bindteardown(),this},i});
/*can/map/bubble*/
define("can/map/bubble",["can/util/util"],function(n){var e=n.bubble={event:function(n,e){return n.constructor._bubbleRule(e,n)},childrenOf:function(n,i){n._each(function(b,t){b&&b.bind&&e.toParent(b,n,t,i)})},teardownChildrenFrom:function(n,i){n._each(function(b){e.teardownFromParent(n,b,i)})},toParent:function(e,i,b,t){n.listenTo.call(i,e,t,function(){var t=n.makeArray(arguments),r=t.shift();t[0]=(n.List&&i instanceof n.List?i.indexOf(e):b)+(t[0]?"."+t[0]:""),r.triggeredNS=r.triggeredNS||{},r.triggeredNS[i._cid]||(r.triggeredNS[i._cid]=!0,n.trigger(i,r,t))})},teardownFromParent:function(e,i,b){i&&i.unbind&&n.stopListening.call(e,i,b)},bind:function(n,i){if(!n._init){var b=e.event(n,i);b&&(n._bubbleBindings||(n._bubbleBindings={}),n._bubbleBindings[b]?n._bubbleBindings[b]++:(n._bubbleBindings[b]=1,e.childrenOf(n,b)))}},unbind:function(i,b){var t=e.event(i,b);t&&(i._bubbleBindings&&i._bubbleBindings[t]--,i._bubbleBindings[t]||(delete i._bubbleBindings[t],e.teardownChildrenFrom(i,t),n.isEmptyObject(i._bubbleBindings)&&delete i._bubbleBindings))},add:function(i,b,t){if(b instanceof n.Map&&i._bubbleBindings)for(var r in i._bubbleBindings)i._bubbleBindings[r]&&(e.teardownFromParent(i,b,r),e.toParent(b,i,t,r))},removeMany:function(n,i){for(var b=0,t=i.length;t>b;b++)e.remove(n,i[b])},remove:function(i,b){if(b instanceof n.Map&&i._bubbleBindings)for(var t in i._bubbleBindings)i._bubbleBindings[t]&&e.teardownFromParent(i,b,t)},set:function(i,b,t,r){return n.Map.helpers.isObservable(t)&&e.add(i,t,b),n.Map.helpers.isObservable(r)&&e.remove(i,r),t}};return e});
/*can/util/string/string*/
define("can/util/string/string",["can/util/util"],function(e){var r=/_|-/,n=/\=\=/,t=/([A-Z]+)([A-Z][a-z])/g,a=/([a-z\d])([A-Z])/g,u=/([a-z\d])([A-Z])/g,i=/\{([^\}]+)\}/g,c=/"/g,o=/'/g,l=/-+(.)?/g,p=/[a-z][A-Z]/g,f=function(e,r,n){var t=e[r];return void 0===t&&n===!0&&(t=e[r]={}),t},g=function(e){return/^f|^o/.test(typeof e)},d=function(e){var r=null===e||void 0===e||isNaN(e)&&""+e=="NaN";return""+(r?"":e)};return e.extend(e,{esc:function(e){return d(e).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(c,"&#34;").replace(o,"&#39;")},getObject:function(r,n,t){var a,u,i,c,o=r?r.split("."):[],l=o.length,p=0;if(n=e.isArray(n)?n:[n||window],c=n.length,!l)return n[0];for(p;c>p;p++){for(a=n[p],i=void 0,u=0;l>u&&g(a);u++)i=a,a=f(i,o[u]);if(void 0!==i&&void 0!==a)break}if(t===!1&&void 0!==a&&delete i[o[u-1]],t===!0&&void 0===a)for(a=n[0],u=0;l>u&&g(a);u++)a=f(a,o[u],!0);return a},capitalize:function(e){return e.charAt(0).toUpperCase()+e.slice(1)},camelize:function(e){return d(e).replace(l,function(e,r){return r?r.toUpperCase():""})},hyphenate:function(e){return d(e).replace(p,function(e){return e.charAt(0)+"-"+e.charAt(1).toLowerCase()})},underscore:function(e){return e.replace(n,"/").replace(t,"$1_$2").replace(a,"$1_$2").replace(u,"_").toLowerCase()},sub:function(r,n,t){var a=[];return r=r||"",a.push(r.replace(i,function(r,u){var i=e.getObject(u,n,t===!0?!1:void 0);return void 0===i||null===i?(a=null,""):g(i)&&a?(a.push(i),""):""+i})),null===a?a:a.length<=1?a[0]:a},replacer:i,undHash:r}),e});
/*can/construct/construct*/
define("can/construct/construct",["can/util/string/string"],function(t){var n=0;return t.Construct=function(){return arguments.length?t.Construct.extend.apply(t.Construct,arguments):void 0},t.extend(t.Construct,{constructorExtends:!0,newInstance:function(){var t,n=this.instance();return n.setup&&(t=n.setup.apply(n,arguments)),n.init&&n.init.apply(n,t||arguments),n},_inherit:function(n,e,r){t.extend(r||n,n||{})},_overwrite:function(t,n,e,r){t[e]=r},setup:function(n){this.defaults=t.extend(!0,{},n.defaults,this.defaults)},instance:function(){n=1;var t=new this;return n=0,t},extend:function(e,r,s){function o(){return n?void 0:(this.constructor!==o&&arguments.length&&o.constructorExtends&&t.dev.warn("can/construct/construct.js: extending a can.Construct without calling extend"),this.constructor!==o&&arguments.length&&o.constructorExtends?o.extend.apply(o,arguments):o.newInstance.apply(o,arguments))}"string"!=typeof e&&(s=r,r=e,e=null),s||(s=r,r=null),s=s||{};var c,u,i,a,p,l,d,h,g=this,f=this.prototype;h=this.instance(),t.Construct._inherit(s,f,h);for(p in g)g.hasOwnProperty(p)&&(o[p]=g[p]);t.Construct._inherit(r,g,o),e&&(c=e.split("."),l=c.pop(),u=t.getObject(c.join("."),window,!0),d=u,i=t.underscore(e.replace(/\./g,"_")),a=t.underscore(l),u[l]&&t.dev.warn("can/construct/construct.js: There's already something called "+e),u[l]=o),t.extend(o,{constructor:o,prototype:h,namespace:d,_shortName:a,fullName:e,_fullName:i}),void 0!==l&&(o.shortName=l),o.prototype.constructor=o;var m=[g].concat(t.makeArray(arguments)),y=o.setup.apply(o,m);return o.init&&o.init.apply(o,y||m),o}}),t.Construct.prototype.setup=function(){},t.Construct.prototype.init=function(){},t.Construct});
/*can/util/batch/batch*/
define("can/util/batch/batch",["can/util/can"],function(t){var i=1,n=0,r=[],c=[];t.batch={start:function(t){n++,t&&c.push(t)},stop:function(a,e){if(a?n=0:n--,0===n){var f,g,h=r.slice(0),u=c.slice(0);for(r=[],c=[],i++,e&&t.batch.start(),f=0,g=h.length;g>f;f++)t.trigger.apply(t,h[f]);for(f=0,g=u.length;f<u.length;f++)u[f]()}},trigger:function(c,a,e){if(!c._init){if(0===n)return t.trigger(c,a,e);a="string"==typeof a?{type:a}:a,a.batchNum=i,r.push([c,a,e])}}}});
/*can/map/map*/
define("can/map/map",["can/util/util","can/util/bind/bind","./bubble","can/construct/construct","can/util/batch/batch"],function(t,e,i){var n=null,r=function(){for(var t in n)n[t].added&&delete n[t].obj._cid;n=null},s=function(t){return n&&n[t._cid]&&n[t._cid].instance},a=null,u=t.Map=t.Construct.extend({setup:function(){if(t.Construct.setup.apply(this,arguments),t.Map){this.defaults||(this.defaults={}),this._computes=[];for(var e in this.prototype)"define"!==e&&"function"!=typeof this.prototype[e]?this.defaults[e]=this.prototype[e]:this.prototype[e].isComputed&&this._computes.push(e);this.helpers.define(this)}!t.List||this.prototype instanceof t.List||(this.List=u.List.extend({Map:this},{}))},_bubble:i,_bubbleRule:function(t){return("change"===t||t.indexOf(".")>=0)&&"change"},_computes:[],bind:t.bindAndSetup,on:t.bindAndSetup,unbind:t.unbindAndTeardown,off:t.unbindAndTeardown,id:"id",helpers:{define:function(){},attrParts:function(e,i){return i?[e]:t.isArray(e)?e:(""+e).split(".")},addToMap:function(e,i){var s;n||(s=r,n={});var a=e._cid,u=t.cid(e);return n[u]||(n[u]={obj:e,instance:i,added:!a}),s},isObservable:function(e){return e instanceof t.Map||e&&e===t.route},canMakeObserve:function(e){return e&&!t.isDeferred(e)&&(t.isArray(e)||t.isPlainObject(e))},serialize:function(e,i,n){var r=t.cid(e),s=!1;return a||(s=!0,a={attr:{},serialize:{}}),a[i][r]=n,e.each(function(r,s){var o,h=u.helpers.isObservable(r),c=h&&a[i][t.cid(r)];o=c?c:"serialize"===i?u.helpers._serialize(e,s,r):u.helpers._getValue(e,s,r,i),void 0!==o&&(n[s]=o)}),t.__reading(e,"__keys"),s&&(a=null),n},_serialize:function(t,e,i){return u.helpers._getValue(t,e,i,"serialize")},_getValue:function(t,e,i,n){return u.helpers.isObservable(i)?i[n]():i}},keys:function(e){var i=[];t.__reading(e,"__keys");for(var n in e._data)i.push(n);return i}},{setup:function(e){this._data={},t.cid(this,".map"),this._init=1;var i=this._setupDefaults();this._setupComputes(i);var n=e&&t.Map.helpers.addToMap(e,this),r=t.extend(t.extend(!0,{},i),e);this.attr(r),n&&n(),this.bind("change",t.proxy(this._changes,this)),delete this._init},_setupComputes:function(){var t=this.constructor._computes;this._computedBindings={};for(var e,i=0,n=t.length;n>i;i++)e=t[i],this[e]=this[e].clone(this),this._computedBindings[e]={count:0}},_setupDefaults:function(){return this.constructor.defaults||{}},_bindsetup:function(){},_bindteardown:function(){},_changes:function(e,i,n,r,s){t.batch.trigger(this,{type:i,batchNum:e.batchNum},[r,s]),("remove"===n||"add"===n)&&t.batch.trigger(this,{type:"__keys",batchNum:e.batchNum})},_triggerChange:function(){t.batch.trigger(this,"change",t.makeArray(arguments))},_each:function(t){var e=this.__get();for(var i in e)e.hasOwnProperty(i)&&t(e[i],i)},attr:function(e,i){var n=typeof e;return"string"!==n&&"number"!==n?this._attrs(e,i):1===arguments.length?(t.__reading(this,e),this._get(e)):(this._set(e,i),this)},each:function(){return t.each.apply(void 0,[this].concat(t.makeArray(arguments)))},removeAttr:function(e){var i=t.List&&this instanceof t.List,n=t.Map.helpers.attrParts(e),r=n.shift(),s=i?this[r]:this._data[r];return n.length&&s?s.removeAttr(n):("string"==typeof e&&~e.indexOf(".")&&(r=e),this._remove(r,s),s)},_remove:function(t,e){t in this._data&&(delete this._data[t],t in this.constructor.prototype||delete this[t],this._triggerChange(t,"remove",void 0,e))},_get:function(e){var i;if("string"==typeof e&&~e.indexOf(".")&&(i=this.__get(e),void 0!==i))return i;var n=t.Map.helpers.attrParts(e),r=this.__get(n.shift());return n.length?r?r._get(n):void 0:r},__get:function(t){return t?this._computedBindings[t]?this[t]():this._data[t]:this._data},__type:function(e){if(!(e instanceof t.Map)&&t.Map.helpers.canMakeObserve(e)){var i=s(e);if(i)return i;if(t.isArray(e)){var n=t.List;return new n(e)}var r=this.constructor.Map||t.Map;return new r(e)}return e},_set:function(e,i,n){var r=t.Map.helpers.attrParts(e,n),s=r.shift(),a=this._init?void 0:this.__get(s);if(r.length&&u.helpers.isObservable(a))a._set(r,i);else{if(r.length)throw"can.Map: Object does not exist";this.__convert&&(i=this.__convert(s,i)),this.__set(s,this.__type(i,s),a)}},__set:function(t,e,i){if(e!==i){var n=this.__get().hasOwnProperty(t)?"set":"add";this.___set(t,this.constructor._bubble.set(this,t,e,i)),this._triggerChange(t,n,e,i),i&&this.constructor._bubble.teardownFromParent(this,i)}},___set:function(e,i){this._computedBindings[e]?this[e](i):this._data[e]=i,t.isFunction(this.constructor.prototype[e])||this._computedBindings[e]||(this[e]=i)},bind:function(e){var i=this._computedBindings&&this._computedBindings[e];if(i)if(i.count)i.count++;else{i.count=1;var n=this;i.handler=function(i,r,s){t.batch.trigger(n,{type:e,batchNum:i.batchNum},[r,s])},this[e].bind("change",i.handler)}return this.constructor._bubble.bind(this,e),t.bindAndSetup.apply(this,arguments)},unbind:function(e){var i=this._computedBindings&&this._computedBindings[e];return i&&(1===i.count?(i.count=0,this[e].unbind("change",i.handler),delete i.handler):i.count--),this.constructor._bubble.unbind(this,e),t.unbindAndTeardown.apply(this,arguments)},serialize:function(){return t.Map.helpers.serialize(this,"serialize",{})},_attrs:function(e,i){if(void 0===e)return u.helpers.serialize(this,"attr",{});e=t.simpleExtend({},e);var n,r,s=this;t.batch.start(),this.each(function(t,n){if("_cid"!==n){if(r=e[n],void 0===r)return void(i&&s.removeAttr(n));s.__convert&&(r=s.__convert(n,r)),u.helpers.isObservable(r)?s.__set(n,s.__type(r,n),t):u.helpers.isObservable(t)&&u.helpers.canMakeObserve(r)?t.attr(r,i):t!==r&&s.__set(n,s.__type(r,n),t),delete e[n]}});for(n in e)"_cid"!==n&&(r=e[n],this._set(n,r,!0));return t.batch.stop(),this},compute:function(e){if(t.isFunction(this.constructor.prototype[e]))return t.compute(this[e],this);var i=e.split("."),n=i.length-1,r={args:[]};return t.compute(function(e){return arguments.length?void t.compute.read(this,i.slice(0,n)).value.attr(i[n],e):t.compute.read(this,i,r).value},this)}});return u.prototype.on=u.prototype.bind,u.prototype.off=u.prototype.unbind,u});
/*can/list/list*/
define("can/list/list",["can/util/util","can/map/map","can/map/bubble"],function(t,e,i){var r=[].splice,s=function(){var t={0:"a",length:1};return r.call(t,0,1),!t[0]}(),h=e.extend({Map:e},{setup:function(e,i){this.length=0,t.cid(this,".map"),this._init=1,this._setupComputes(),e=e||[];var r;t.isDeferred(e)?this.replace(e):(r=e.length&&t.Map.helpers.addToMap(e,this),this.push.apply(this,t.makeArray(e||[]))),r&&r(),this.bind("change",t.proxy(this._changes,this)),t.simpleExtend(this,i),delete this._init},_triggerChange:function(i,r,s,h){e.prototype._triggerChange.apply(this,arguments);var n=+i;~i.indexOf(".")||isNaN(n)||("add"===r?(t.batch.trigger(this,r,[s,n]),t.batch.trigger(this,"length",[this.length])):"remove"===r?(t.batch.trigger(this,r,[h,n]),t.batch.trigger(this,"length",[this.length])):t.batch.trigger(this,r,[s,n]))},__get:function(e){return e?this[e]&&this[e].isComputed&&t.isFunction(this.constructor.prototype[e])?this[e]():this[e]:this},___set:function(t,e){this[t]=e,+t>=this.length&&(this.length=+t+1)},_remove:function(t,e){isNaN(+t)?(delete this[t],this._triggerChange(t,"remove",void 0,e)):this.splice(t,1)},_each:function(t){for(var e=this.__get(),i=0;i<e.length;i++)t(e[i],i)},serialize:function(){return e.helpers.serialize(this,"serialize",[])},splice:function(e,h){var n,a=t.makeArray(arguments);for(n=2;n<a.length;n++)a[n]=i.set(this,n,this.__type(a[n],n));void 0===h&&(h=a[1]=this.length-e);var l=r.apply(this,a);if(!s)for(n=this.length;n<l.length+this.length;n++)delete this[n];return t.batch.start(),h>0&&(this._triggerChange(""+e,"remove",void 0,l),i.removeMany(this,l)),a.length>2&&this._triggerChange(""+e,"add",a.slice(2),l),t.batch.stop(),l},_attrs:function(i,r){return void 0===i?e.helpers.serialize(this,"attr",[]):(i=t.makeArray(i),t.batch.start(),this._updateAttrs(i,r),void t.batch.stop())},_updateAttrs:function(t,i){for(var r=Math.min(t.length,this.length),s=0;r>s;s++){var h=this[s],n=t[s];e.helpers.isObservable(h)&&e.helpers.canMakeObserve(n)?h.attr(n,i):h!==n&&this._set(s,n)}t.length>this.length?this.push.apply(this,t.slice(this.length)):t.length<this.length&&i&&this.splice(t.length)}}),n=function(e){return e[0]&&t.isArray(e[0])?e[0]:t.makeArray(e)};return t.each({push:"length",unshift:0},function(t,e){var r=[][e];h.prototype[e]=function(){for(var e,s,h=[],n=t?this.length:0,a=arguments.length;a--;)s=arguments[a],h[a]=i.set(this,a,this.__type(s,a));return e=r.apply(this,h),(!this.comparator||h.length)&&this._triggerChange(""+n,"add",h,void 0),e}}),t.each({pop:"length",shift:0},function(t,e){h.prototype[e]=function(){var r=n(arguments),s=t&&this.length?this.length-1:0,h=[][e].apply(this,r);return this._triggerChange(""+s,"remove",void 0,[h]),h&&h.unbind&&i.remove(this,h),h}}),t.extend(h.prototype,{indexOf:function(e,i){return this.attr("length"),t.inArray(e,this,i)},join:function(){return[].join.apply(this.attr(),arguments)},reverse:function(){var e=t.makeArray([].reverse.call(this));this.replace(e)},slice:function(){var t=Array.prototype.slice.apply(this,arguments);return new this.constructor(t)},concat:function(){var e=[];return t.each(t.makeArray(arguments),function(i,r){e[r]=i instanceof t.List?i.serialize():i}),new this.constructor(Array.prototype.concat.apply(this.serialize(),e))},forEach:function(e,i){return t.each(this,e,i||this)},replace:function(e){return t.isDeferred(e)?e.then(t.proxy(this.replace,this)):this.splice.apply(this,[0,this.length].concat(t.makeArray(e||[]))),this},filter:function(e,i){var r,s=new t.List,h=this;return this.each(function(t,n){r=e.call(i|h,t,n,h),r&&s.push(t)}),s}}),t.List=e.List=h,t.List});
/*can/util/string/deparam/deparam*/
define("can/util/string/deparam/deparam",["can/util/util","can/util/string/string"],function(t){var n=/^\d+$/,e=/([^\[\]]+)|(\[\])/g,r=/([^?#]*)(#.*)?$/,i=function(t){return decodeURIComponent(t.replace(/\+/g," "))};return t.extend(t,{deparam:function(a){var u,c,o={};return a&&r.test(a)&&(u=a.split("&"),t.each(u,function(t){var r=t.split("="),a=i(r.shift()),u=i(r.join("=")),p=o;if(a){r=a.match(e);for(var s=0,d=r.length-1;d>s;s++)p[r[s]]||(p[r[s]]=n.test(r[s+1])||"[]"===r[s+1]?[]:{}),p=p[r[s]];c=r.pop(),"[]"===c?p.push(u):p[c]=u}})),o}}),t});
/*can/route/route*/
define("can/route/route",["can/util/util","can/map/map","can/list/list","can/util/string/deparam/deparam"],function(t){var e,r,n,a,u=/\:([\w\.]+)/g,o=/^(?:&[^=]+=[^&]*)+/,i=function(e){var r=[];return t.each(e,function(e,n){

return r},l=window.location,s=function(t){return(t+"").replace(/([.?*+\^$\[\]\\(){}|\-])/g,"\\$1")},d=t.each,f=t.extend,p=function(e){return e&&"object"==typeof e?(e=e instanceof t.Map?e.attr():t.isFunction(e.slice)?e.slice():t.extend({},e),t.each(e,function(t,r){e[r]=p(t)})):void 0!==e&&null!==e&&t.isFunction(e.toString)&&(e=e.toString()),e},h=function(t){return t.replace(/\\/g,"")},g=function(){a=1,clearTimeout(e),e=setTimeout(function(){a=0;var e=t.route.data.serialize(),r=t.route.param(e,!0);t.route._call("setURL",r),n=r},10)};t.route=function(e,r){var n=t.route._call("root");n.lastIndexOf("/")===n.length-1&&0===e.indexOf("/")&&(e=e.substr(1)),r=r||{};for(var a,o,i=[],c="",l=u.lastIndex=0,d=t.route._call("querySeparator");a=u.exec(e);)i.push(a[1]),c+=h(e.substring(l,u.lastIndex-a[0].length)),o="\\"+(h(e.substr(u.lastIndex,1))||d),c+="([^"+o+"]"+(r[a[1]]?"*":"+")+")",l=u.lastIndex;return c+=e.substr(l).replace("\\",""),t.route.routes[e]={test:new RegExp("^"+c+"($|"+s(d)+")"),route:e,names:i,defaults:r,length:e.split("/").length},t.route},f(t.route,{param:function(e,r){var n,a,o=0,i=e.route,l=0;if(delete e.route,d(e,function(){l++}),d(t.route.routes,function(t){return a=c(t,e),a>o&&(n=t,o=a),a>=l?!1:void 0}),t.route.routes[i]&&c(t.route.routes[i],e)===o&&(n=t.route.routes[i]),n){var s,p=f({},e),h=n.route.replace(u,function(t,r){return delete p[r],e[r]===n.defaults[r]?"":encodeURIComponent(e[r])}).replace("\\","");return d(n.defaults,function(t,e){p[e]===t&&delete p[e]}),s=t.param(p),r&&t.route.attr("route",n.route),h+(s?t.route._call("querySeparator")+s:"")}return t.isEmptyObject(e)?"":t.route._call("querySeparator")+t.param(e)},deparam:function(e){var r=t.route._call("root");r.lastIndexOf("/")===r.length-1&&0===e.indexOf("/")&&(e=e.substr(1));var n={length:-1},a=t.route._call("querySeparator"),u=t.route._call("paramsMatcher");if(d(t.route.routes,function(t){t.test.test(e)&&t.length>n.length&&(n=t)}),n.length>-1){var o=e.match(n.test),i=o.shift(),c=e.substr(i.length-(o[o.length-1]===a?1:0)),l=c&&u.test(c)?t.deparam(c.slice(1)):{};return l=f(!0,{},n.defaults,l),d(o,function(t,e){t&&t!==a&&(l[n.names[e]]=decodeURIComponent(t))}),l.route=n.route,l}return e.charAt(0)!==a&&(e=a+e),u.test(e)?t.deparam(e.slice(1)):{}},data:new t.Map({}),map:function(e){var r;e instanceof t.Map?r=e:e.prototype instanceof t.Map&&(r=new e),t.route.data=r},routes:{},ready:function(e){return e!==!0&&(t.route._setup(),t.route.setState()),t.route},url:function(e,r){return r&&(e=t.extend({},t.route.deparam(t.route._call("matchingPartOfURL")),e)),t.route._call("root")+t.route.param(e)},link:function(e,r,n,a){return"<a "+i(f({href:t.route.url(r,a)},n))+">"+e+"</a>"},current:function(e){return this._call("matchingPartOfURL")===t.route.param(e)},bindings:{hashchange:{paramsMatcher:o,querySeparator:"&",bind:function(){t.bind.call(window,"hashchange",m)},unbind:function(){t.unbind.call(window,"hashchange",m)},matchingPartOfURL:function(){return l.href.split(/#!?/)[1]||""},setURL:function(t){return l.hash="#!"+t,t},root:"#!"}},defaultBinding:"hashchange",currentBinding:null,_setup:function(){t.route.currentBinding||(t.route._call("bind"),t.route.bind("change",g),t.route.currentBinding=t.route.defaultBinding)},_teardown:function(){t.route.currentBinding&&(t.route._call("unbind"),t.route.unbind("change",g),t.route.currentBinding=null),clearTimeout(e),a=0},_call:function(){var e=t.makeArray(arguments),r=e.shift(),n=t.route.bindings[t.route.currentBinding||t.route.defaultBinding],a=n[r];return a.apply?a.apply(n,e):a}}),d(["bind","unbind","on","off","delegate","undelegate","removeAttr","compute","_get","__get"],function(e){t.route[e]=function(){return t.route.data[e]?t.route.data[e].apply(t.route.data,arguments):void 0}}),t.route.attr=function(e,r){var n,a=typeof e;return n=void 0===r?arguments:"string"!==a&&"number"!==a?[p(e),r]:[e,p(r)],t.route.data.attr.apply(t.route.data,n)};var m=t.route.setState=function(){var e=t.route._call("matchingPartOfURL"),u=r;if(r=t.route.deparam(e),!a||e!==n){t.batch.start();for(var o in u)r[o]||t.route.removeAttr(o);t.route.attr(r),t.batch.stop()}};return t.route});
/*can/control/control*/
define("can/control/control",["can/util/util","can/construct/construct"],function(t){var n,e=function(n,e,o){return t.bind.call(n,e,o),function(){t.unbind.call(n,e,o)}},o=t.isFunction,s=t.extend,r=t.each,i=[].slice,c=/\{([^\}]+)\}/g,u=t.getObject("$.event.special",[t])||{},l=function(n,e,o,s){return t.delegate.call(n,e,o,s),function(){t.undelegate.call(n,e,o,s)}},a=function(n,o,s,r){return r?l(n,t.trim(r),o,s):e(n,o,s)},h=t.Control=t.Construct({setup:function(){if(t.Construct.setup.apply(this,arguments),t.Control){var n,e=this;e.actions={};for(n in e.prototype)e._isAction(n)&&(e.actions[n]=e._action(n))}},_shifter:function(n,e){var s="string"==typeof e?n[e]:e;return o(s)||(s=n[s]),function(){return n.called=e,s.apply(n,[this.nodeName?t.$(this):this].concat(i.call(arguments,0)))}},_isAction:function(t){var n=this.prototype[t],e=typeof n;return"constructor"!==t&&("function"===e||"string"===e&&o(this.prototype[n]))&&!!(u[t]||f[t]||/[^\w]/.test(t))},_action:function(e,o){if(c.lastIndex=0,o||!c.test(e)){var s=o?t.sub(e,this._lookup(o)):e;if(!s)return t.dev.log("can/control/control.js: No property found for handling "+e),null;var r=t.isArray(s),i=r?s[1]:s,u=i.split(/\s+/g),l=u.pop();return{processor:f[l]||n,parts:[i,u.join(" "),l],delegate:r?s[0]:void 0}}},_lookup:function(t){return[t,window]},processors:{},defaults:{}},{setup:function(n,e){var o,r=this.constructor,i=r.pluginName||r._fullName;return this.element=t.$(n),i&&"can_control"!==i&&this.element.addClass(i),o=t.data(this.element,"controls"),o||(o=[],t.data(this.element,"controls",o)),o.push(this),this.options=s({},r.defaults,e),this.on(),[this.element,this.options]},on:function(n,e,o,s){if(!n){this.off();var r,i,c=this.constructor,u=this._bindings,l=c.actions,h=this.element,f=t.Control._shifter(this,"destroy");for(r in l)l.hasOwnProperty(r)&&(i=l[r]||c._action(r,this.options,this),i&&(u.control[r]=i.processor(i.delegate||h,i.parts[2],i.parts[1],r,this)));return t.bind.call(h,"removed",f),u.user.push(function(n){t.unbind.call(n,"removed",f)}),u.user.length}return"string"==typeof n&&(s=o,o=e,e=n,n=this.element),void 0===s&&(s=o,o=e,e=null),"string"==typeof s&&(s=t.Control._shifter(this,s)),this._bindings.user.push(a(n,o,s,e)),this._bindings.user.length},off:function(){var t=this.element[0],n=this._bindings;n&&(r(n.user||[],function(n){n(t)}),r(n.control||{},function(n){n(t)})),this._bindings={user:[],control:{}}},destroy:function(){if(null===this.element)return void t.dev.warn("can/control/control.js: Control already destroyed");var n,e=this.constructor,o=e.pluginName||e._fullName;this.off(),o&&"can_control"!==o&&this.element.removeClass(o),n=t.data(this.element,"controls"),n.splice(t.inArray(this,n),1),t.trigger(this,"destroyed"),this.element=null}}),f=t.Control.processors;return n=function(n,e,o,s,r){return a(n,e,t.Control._shifter(r,s),o)},r(["change","click","contextmenu","dblclick","keydown","keyup","keypress","mousedown","mousemove","mouseout","mouseover","mouseup","reset","resize","scroll","select","submit","focusin","focusout","mouseenter","mouseleave","touchstart","touchmove","touchcancel","touchend","touchleave"],function(t){f[t]=n}),h});
/*can/control/route/route*/
define("can/control/route/route",["can/util/util","can/route/route","can/control/control"],function(t){return t.Control.processors.route=function(o,r,u,n,e){u=u||"",t.route.routes[u]||("/"===u[0]&&(u=u.substring(1)),t.route(u));var c,i=function(o){if(t.route.attr("route")===u&&(void 0===o.batchNum||o.batchNum!==c)){c=o.batchNum;var r=t.route.attr();delete r.route,t.isFunction(e[n])?e[n](r):e[e[n]](r)}};return t.route.bind("change",i),function(){t.route.unbind("change",i)}},t});
/*can/model/model*/
define("can/model/model",["can/util/util","can/map/map","can/list/list"],function(t){var e=function(e,r,i){var n=new t.Deferred;return e.then(function(){var e=t.makeArray(arguments),s=!0;try{e[0]=i.apply(r,e)}catch(o){s=!1,n.rejectWith(n,[o].concat(e))}s&&n.resolveWith(n,e)},function(){n.rejectWith(this,arguments)}),"function"==typeof e.abort&&(n.abort=function(){return e.abort()}),n},r=0,i=function(e){return t.__reading(e,e.constructor.id),e.__get(e.constructor.id)},n=function(e,r,i,n,s,o){var a={};if("string"==typeof e){var u=e.split(/\s+/);a.url=u.pop(),u.length&&(a.type=u.pop())}else t.extend(a,e);return a.data="object"!=typeof r||t.isArray(r)?r:t.extend(a.data||{},r),a.url=t.sub(a.url,a.data,!0),t.ajax(t.extend({type:i||"post",dataType:n||"json",success:s,error:o},a))},s=function(r,n,s,o,a){var u;t.isArray(r)?(u=r[1],r=r[0]):u=r.serialize(),u=[u];var c,d,l=r.constructor;return("update"===n||"destroy"===n)&&u.unshift(i(r)),d=l[n].apply(l,u),c=e(d,r,function(t){return r[a||n+"d"](t,d),r}),d.abort&&(c.abort=function(){d.abort()}),c.then(s,o),c},o={models:function(e){return function(r,i){if(t.Model._reqs++,r){if(r instanceof this.List)return r;var n=this,s=[],o=n.List||y,a=i instanceof t.List?i:new o,u=t.isArray(r),c=r instanceof y,d=u?r:c?r.serialize():t.getObject(e||"data",r);if("undefined"==typeof d)throw new Error("Could not get any raw data while converting using .models");return d.length||t.dev.warn("model.js models has no data."),a.length&&a.splice(0),t.each(d,function(t){s.push(n.model(t))}),a.push.apply(a,s),u||t.each(r,function(t,e){"data"!==e&&a.attr(e,t)}),setTimeout(t.proxy(this._clean,this),1),a}}},model:function(e){return function(r){if(r){"function"==typeof r.serialize&&(r=r.serialize()),this.parseModel?r=this.parseModel.apply(this,arguments):e&&(r=t.getObject(e||"data",r));var i=r[this.id],n=(i||0===i)&&this.store[i]?this.store[i].attr(r,this.removeAttr||!1):new this(r);return n}}}},a=function(e){return function(r){return e?t.getObject(e||"data",r):r}},u={parseModel:a,parseModels:a},c={create:{url:"_shortName",type:"post"},update:{data:function(e,r){r=r||{};var i=this.id;return r[i]&&r[i]!==e&&(r["new"+t.capitalize(e)]=r[i],delete r[i]),r[i]=e,r},type:"put"},destroy:{type:"delete",data:function(t,e){return e=e||{},e.id=e[this.id]=t,e}},findAll:{url:"_shortName"},findOne:{}},d=function(t,e){return function(r){return r=t.data?t.data.apply(this,arguments):r,n(e||this[t.url||"_url"],r,t.type||"get")}},l=function(t,e){if(t.resource){var r=t.resource.replace(/\/+$/,"");return"findAll"===e||"create"===e?r:r+"/{"+t.id+"}"}};t.Model=t.Map.extend({fullName:"can.Model",_reqs:0,setup:function(e,i,n,s){if("string"!==i&&(s=n,n=i),s||(s=n),this.store={},t.Map.setup.apply(this,arguments),t.Model){n&&n.List?(this.List=n.List,this.List.Map=this):this.List=e.List.extend({Map:this},{});var a=this,p=t.proxy(this._clean,a);t.each(c,function(r,i){if(t.isFunction(a[i])||(a[i]=d(r,a[i]?a[i]:l(a,i))),a["make"+t.capitalize(i)]){var n=a["make"+t.capitalize(i)](a[i]);t.Construct._overwrite(a,e,i,function(){t.Model._reqs++;var e=n.apply(this,arguments),r=e.then(p,p);return r.abort=e.abort,r})}}),t.each(o,function(r,i){var n="parse"+t.capitalize(i),o=a[i];"string"==typeof o?(t.Construct._overwrite(a,e,n,u[n](o)),t.Construct._overwrite(a,e,i,r(o))):s&&(s[i]||s[n])||t.Construct._overwrite(a,e,n,u[n]())}),t.each(u,function(r,i){"string"==typeof a[i]&&t.Construct._overwrite(a,e,i,r(a[i]))}),"can.Model"!==a.fullName&&a.fullName||(a.fullName="Model"+ ++r),t.Model._reqs=0,this._url=this._shortName+"/{"+this.id+"}"}},_ajax:d,_makeRequest:s,_clean:function(){if(t.Model._reqs--,!t.Model._reqs)for(var e in this.store)this.store[e]._bindings||delete this.store[e];return arguments[0]},models:o.models("data"),model:o.model()},{setup:function(e){var r=e&&e[this.constructor.id];t.Model._reqs&&null!=r&&(this.constructor.store[r]=this),t.Map.prototype.setup.apply(this,arguments)},isNew:function(){var t=i(this);return!(t||0===t)},save:function(t,e){return s(this,this.isNew()?"create":"update",t,e)},destroy:function(e,r){if(this.isNew()){var i=this,n=t.Deferred();return n.then(e,r),n.done(function(t){i.destroyed(t)}).resolve(i)}return s(this,"destroy",e,r,"destroyed")},_bindsetup:function(){return this.constructor.store[this.__get(this.constructor.id)]=this,t.Map.prototype._bindsetup.apply(this,arguments)},_bindteardown:function(){return delete this.constructor.store[i(this)],t.Map.prototype._bindteardown.apply(this,arguments)},___set:function(e,r){t.Map.prototype.___set.call(this,e,r),e===this.constructor.id&&this._bindings&&(this.constructor.store[i(this)]=this)}});var p=function(e){var r="parse"+t.capitalize(e);return function(t){return this[r]&&(t=this[r].apply(this,arguments)),this[e](t)}},h=function(t){return this.parseModel?this.parseModel.apply(this,arguments):this.model(t)},f={makeFindAll:p("models"),makeFindOne:p("model"),makeCreate:h,makeUpdate:h};t.each(f,function(r,i){t.Model[i]=function(i){return function(){var n=t.makeArray(arguments),s=t.isFunction(n[1])?n.splice(0,1):n.splice(0,2),o=e(i.apply(this,s),this,r);return o.then(n[0],n[1]),o}}}),t.each(["created","updated","destroyed"],function(e){t.Model.prototype[e]=function(r){var i,n=this.constructor;i=r&&"object"==typeof r&&this.attr(r.attr?r.attr():r),t.trigger(this,"change",e),t.dev.log("Model.js - "+n.shortName+" "+e),t.trigger(n,e,this)}});var y=t.Model.List=t.List.extend({_bubbleRule:function(e,r){return t.List._bubbleRule(e,r)||"destroyed"}},{setup:function(e){t.isPlainObject(e)&&!t.isArray(e)?(t.List.prototype.setup.apply(this),this.replace(this.constructor.Map.findAll(e))):t.List.prototype.setup.apply(this,arguments),this._init=1,this.bind("destroyed",t.proxy(this._destroyed,this)),delete this._init},_destroyed:function(t,e){if(/\w+/.test(e))for(var r;(r=this.indexOf(t.target))>-1;)this.splice(r,1)}});return t.Model});
/*can/view/view*/
define("can/view/view",["can/util/util"],function(e){var r=e.isFunction,n=e.makeArray,t=1,i=function(e){var r=function(){return c.frag(e.apply(this,arguments))};return r.render=function(){return e.apply(e,arguments)},r},a=function(r,n){if(!r.length)throw e.dev.log("can/view/view.js: There is no template or an empty template at "+n),"can.view: No template or empty template:"+n},o=function(r,n){var t,i,o,d="string"==typeof r?r:r.url,u=r.engine&&"."+r.engine||d.match(/\.[\w\d]+$/);if(d.match(/^#/)&&(d=d.substr(1)),(i=document.getElementById(d))&&(u="."+i.type.match(/\/(x\-)?(.+)/)[2]),u||c.cached[d]||(d+=u=c.ext),e.isArray(u)&&(u=u[0]),o=c.toId(d),d.match(/^\/\//)&&(d=d.substr(2),d="undefined"!=typeof window&&window.steal?steal.config().root.mapJoin(""+steal.id(d)):d),window.require&&require.toUrl&&(d=require.toUrl(d)),t=c.types[u],c.cached[o])return c.cached[o];if(i)return c.registerView(o,i.innerHTML,t);var s=new e.Deferred;return e.ajax({async:n,url:d,dataType:"text",error:function(e){a("",d),s.reject(e)},success:function(e){a(e,d),c.registerView(o,e,t,s)}}),s},d=function(r){var n=[];if(e.isDeferred(r))return[r];for(var t in r)e.isDeferred(r[t])&&n.push(r[t]);return n},u=function(r){return e.isArray(r)&&"success"===r[1]?r[0]:r},c=e.view=e.template=function(e,n,t,i){r(t)&&(i=t,t=void 0);var a;return a=r(e)?e(n,t,i):c.renderAs("fragment",e,n,t,i)};return e.extend(c,{frag:function(e,r){return c.hookup(c.fragment(e),r)},fragment:function(r){if("string"!=typeof r&&11===r.nodeType)return r;var n=e.buildFragment(r,document.body);return n.childNodes.length||n.appendChild(document.createTextNode("")),n},toId:function(r){return e.map(r.toString().split(/\/|\./g),function(e){return e?e:void 0}).join("_")},toStr:function(e){return null==e?"":""+e},hookup:function(r,n){var t,i,a=[];return e.each(r.childNodes?e.makeArray(r.childNodes):r,function(r){1===r.nodeType&&(a.push(r),a.push.apply(a,e.makeArray(r.getElementsByTagName("*"))))}),e.each(a,function(e){e.getAttribute&&(t=e.getAttribute("data-view-id"))&&(i=c.hookups[t])&&(i(e,n,t),delete c.hookups[t],e.removeAttribute("data-view-id"))}),r},hookups:{},hook:function(e){return c.hookups[++t]=e," data-view-id='"+t+"'"},cached:{},cachedRenderers:{},cache:!0,register:function(r){this.types["."+r.suffix]=r,"undefined"!=typeof window&&window.steal&&steal.type&&steal.type(r.suffix+" view js",function(e,r){var n=c.types["."+e.type],t=c.toId(e.id+"");e.text=n.script(t,e.text),r()}),e[r.suffix]=c[r.suffix]=function(e,n){return n?r.fragRenderer?c.preload(e,r.fragRenderer(e,n)):c.preloadStringRenderer(e,r.renderer(e,n)):r.fragRenderer?r.fragRenderer(null,e):i(r.renderer(null,e))}},types:{},ext:".ejs",registerScript:function(e,r,n){return"can.view.preloadStringRenderer('"+r+"',"+c.types["."+e].script(r,n)+");"},preload:function(r,n){var t=c.cached[r]=(new e.Deferred).resolve(function(e,r){return n.call(e,e,r)});return t.__view_id=r,c.cachedRenderers[r]=n,n},preloadStringRenderer:function(e,r){return this.preload(e,i(r))},render:function(r,n,t,i){return e.view.renderAs("string",r,n,t,i)},renderTo:function(e,r,n,t){return("string"===e&&r.render?r.render:r)(n,t)},renderAs:function(t,i,a,s,f){r(s)&&(f=s,s=void 0);var p,l,v,w,g,h=d(a);if(h.length)return l=new e.Deferred,v=e.extend({},a),h.push(o(i,!0)),e.when.apply(e,h).then(function(r){var i,o=n(arguments),d=o.pop();if(e.isDeferred(a))v=u(r);else for(var c in a)e.isDeferred(a[c])&&(v[c]=u(o.shift()));i=e.view.renderTo(t,d,v,s),l.resolve(i,v),f&&f(i,v)},function(){l.reject.apply(l,arguments)}),l;if(p=e.__clearReading(),w=r(f),l=o(i,w),p&&e.__setReading(p),w)g=l,l.then(function(r){f(a?e.view.renderTo(t,r,a,s):r)});else{if("resolved"===l.state()&&l.__view_id){var y=c.cachedRenderers[l.__view_id];return a?e.view.renderTo(t,y,a,s):y}l.then(function(r){g=a?e.view.renderTo(t,r,a,s):r})}return g},registerView:function(r,n,t,a){var o,d="object"==typeof t?t:c.types[t||c.ext];return o=d.fragRenderer?d.fragRenderer(r,n):i(d.renderer(r,n)),a=a||new e.Deferred,c.cache&&(c.cached[r]=a,a.__view_id=r,c.cachedRenderers[r]=o),a.resolve(o)}}),"undefined"!=typeof window&&window.steal&&steal.type&&steal.type("view js",function(e,r){var n=c.types["."+e.type],t=c.toId(e.id),i=n.plugin||"can/view/"+e.type,a=n.fragRenderer?"preload":"preloadStringRenderer";e.text="steal('can/view','"+i+"',function(can){return can.view."+a+"('"+t+"',"+e.text+");\n})",r()}),e});
/*can/compute/compute*/
define("can/compute/compute",["can/util/util","can/util/bind/bind","can/util/batch/batch"],function(n){var e=[];n.__read=function(n,t){e.push({});var o=n.call(t);return{value:o,observed:e.pop()}},n.__reading=function(n,t){e.length&&(e[e.length-1][n._cid+"|"+t]={obj:n,event:t+""})},n.__clearReading=function(){if(e.length){var n=e[e.length-1];return e[e.length-1]={},n}},n.__setReading=function(n){e.length&&(e[e.length-1]=n)},n.__addReading=function(t){e.length&&n.simpleExtend(e[e.length-1],t)};var t=function(e,t,o,u){var r,i,c=n.__read(e,t),a=c.observed;for(i in a)o[i]?delete o[i]:(r=a[i],r.obj.bind(r.event,u));for(i in o)r=o[i],r.obj.unbind(r.event,u);return c},o=function(e,t,o,u){t!==o&&n.batch.trigger(e,u?{type:"change",batchNum:u}:"change",[t,o])},u=function(e,o,u,r){var i,c,a;return{on:function(f){c||(c=function(n){if(e.bound&&(void 0===n.batchNum||n.batchNum!==a)){var r=i.value;i=t(o,u,i.observed,c),f(i.value,r,n.batchNum),a=a=n.batchNum}}),i=t(o,u,{},c),r(i.value),e.hasDependencies=!n.isEmptyObject(i.observed)},off:function(){for(var n in i.observed){var e=i.observed[n];e.obj.unbind(e.event,c)}}}},r=function(e){return e instanceof n.Map||e&&e.__get},i=function(){};n.compute=function(t,r,c){if(t&&t.isComputed)return t;var a,f,l,d=i,s=i,b=function(){return f},p=function(n){f=n},v=p,h=n.makeArray(arguments),g=function(n,e,t){v(n),o(a,n,e,t)};if(a=function(t){if(arguments.length){var u=f,i=p.call(r,t,u);return a.hasDependencies?b.call(r):(f=void 0===i?b.call(r):i,o(a,f,u),f)}return e.length&&a.canReadForChangeEvent!==!1&&(n.__reading(a,"change"),a.bound||n.compute.temporarilyBind(a)),a.bound?f:b.call(r)},"function"==typeof t){p=t,b=t,a.canReadForChangeEvent=c===!1?!1:!0;var m=u(a,t,r||this,v);d=m.on,s=m.off}else if(r)if("string"==typeof r){var y=r,_=t instanceof n.Map;_&&(a.hasDependencies=!0),b=function(){return _?t.attr(y):t[y]},p=function(n){_?t.attr(y,n):t[y]=n};var C;d=function(e){C=function(){e(b(),f)},n.bind.call(t,c||y,C),f=n.__read(b).value},s=function(){n.unbind.call(t,c||y,C)}}else if("function"==typeof r)f=t,p=r,r=c,l="setter";else{f=t;var x=r,O=g;if(r=x.context||x,b=x.get||b,p=x.set||function(){return f},x.fn){var E,R=x.fn;b=function(){return R.call(r,f)},0===R.length?E=u(a,R,r,v):1===R.length?E=u(a,function(){return R.call(r,f)},r,v):(g=function(n){void 0!==n&&O(n,f)},E=u(a,function(){var n=R.call(r,f,function(n){O(n,f)});return void 0!==n?n:f},r,v)),d=E.on,s=E.off}else g=function(){var n=b.call(r);O(n,f)};d=x.on||d,s=x.off||s}else f=t;return n.cid(a,"compute"),n.simpleExtend(a,{isComputed:!0,_bindsetup:function(){this.bound=!0;var e=n.__clearReading();d.call(this,g),n.__setReading(e)},_bindteardown:function(){s.call(this,g),this.bound=!1},bind:n.bindAndSetup,unbind:n.unbindAndTeardown,clone:function(e){return e&&("setter"===l?h[2]=e:h[1]=e),n.compute.apply(n,h)}})};var c,a=function(){for(var n=0,e=c.length;e>n;n++)c[n].unbind("change",i);c=null};return n.compute.temporarilyBind=function(n){n.bind("change",i),c||(c=[],setTimeout(a,10)),c.push(n)},n.compute.truthy=function(e){return n.compute(function(){var n=e();return"function"==typeof n&&(n=n()),!!n})},n.compute.async=function(e,t,o){return n.compute(e,{fn:t,context:o})},n.compute.read=function(e,t,o){o=o||{};for(var u,i,c,a=e,f=0,l=t.length;l>f;f++)if(i=a,i&&i.isComputed&&(o.foundObservable&&o.foundObservable(i,f),i=i()),r(i)?(!c&&o.foundObservable&&o.foundObservable(i,f),c=1,a="function"==typeof i[t[f]]&&i.constructor.prototype[t[f]]===i[t[f]]?o.returnObserveMethods?a[t[f]]:"constructor"===t[f]&&i instanceof n.Construct?i[t[f]]:i[t[f]].apply(i,o.args||[]):a.attr(t[f])):a=i[t[f]],u=typeof a,a&&a.isComputed&&!o.isArgument&&l-1>f?(!c&&o.foundObservable&&o.foundObservable(i,f+1),a=a()):f<t.length-1&&"function"===u&&o.executeAnonymousFunctions&&!(n.Construct&&a.prototype instanceof n.Construct)&&(a=a()),f<t.length-1&&(null===a||"function"!==u&&"object"!==u))return o.earlyExit&&o.earlyExit(i,f,a),{value:void 0,parent:i};return"function"!=typeof a||n.Construct&&a.prototype instanceof n.Construct||(o.isArgument?a.isComputed||o.proxyMethods===!1||(a=n.proxy(a,i)):(a.isComputed&&!c&&o.foundObservable&&o.foundObservable(a,f),a=a.call(i))),void 0===a&&o.earlyExit&&o.earlyExit(i,f-1),{value:a,parent:i}},n.compute});
/*can/view/scope/scope*/
define("can/view/scope/scope",["can/util/util","can/construct/construct","can/map/map","can/list/list","can/view/view","can/compute/compute"],function(e){var t=/(\\)?\./g,n=/\\\./g,r=function(e){var r=[],a=0;return e.replace(t,function(t,u,i){u||(r.push(e.slice(a,i).replace(n,".")),a=i+t.length)}),r.push(e.slice(a).replace(n,".")),r},a=e.Construct.extend({read:e.compute.read},{init:function(e,t){this._context=e,this._parent=t,this.__cache={}},attr:function(t){var n=e.__clearReading(),r=this.read(t,{isArgument:!0,returnObserveMethods:!0,proxyMethods:!1}).value;return e.__setReading(n),r},add:function(e){return e!==this._context?new this.constructor(e,this):this},computeData:function(t,n){n=n||{args:[]};var r,a,u=this,i={compute:e.compute(function(c){if(!arguments.length){if(r)return e.compute.read(r,a,n).value;var s=u.read(t,n);return r=s.rootObserve,a=s.reads,i.scope=s.scope,i.initialValue=s.value,s.value}if(r.isComputed&&!a.length)r(c);else{var o=a.length-1;e.compute.read(r,a.slice(0,o)).value.attr(a[o],c)}})};return i},compute:function(e,t){return this.computeData(e,t).compute},read:function(t,n){var a;if("./"===t.substr(0,2))a=!0,t=t.substr(2);else{if("../"===t.substr(0,3))return this._parent.read(t.substr(3),n);if(".."===t)return{value:this._parent._context};if("."===t||"this"===t)return{value:this._context}}for(var u,i,c,s,o,l,p=-1===t.indexOf("\\.")?t.split("."):r(t),v=this,d=[],f=-1;v;){if(u=v._context,null!==u){var h=e.compute.read(u,p,e.simpleExtend({foundObservable:function(e,t){o=e,l=p.slice(t)},earlyExit:function(t,n){n>f&&(i=o,d=l,f=n,s=v,c=e.__clearReading())},executeAnonymousFunctions:!0},n));if(void 0!==h.value)return{scope:v,rootObserve:o,value:h.value,reads:l}}e.__clearReading(),v=a?null:v._parent}return i?(e.__setReading(c),{scope:s,rootObserve:i,reads:d,value:void 0}):{names:p,value:void 0}}});return e.view.Scope=a,a});
/*can/view/elements*/
define("can/view/elements",["can/util/util","can/view/view"],function(t){var e="undefined"!=typeof document?document:null,n=e&&function(){return 1===t.$(document.createComment("~")).length}(),o={tagToContentPropMap:{option:e&&"textContent"in document.createElement("option")?"textContent":"innerText",textarea:"value"},attrMap:t.attr.map,attrReg:/([^\s=]+)[\s]*=[\s]*/,defaultValue:t.attr.defaultValue,tagMap:{"":"span",table:"tbody",tr:"td",ol:"li",ul:"li",tbody:"tr",thead:"tr",tfoot:"tr",select:"option",optgroup:"option"},reverseTagMap:{tr:"tbody",option:"select",td:"tr",th:"tr",li:"ul"},getParentNode:function(t,e){return e&&11===t.parentNode.nodeType?e:t.parentNode},setAttr:t.attr.set,getAttr:t.attr.get,removeAttr:t.attr.remove,contentText:function(t){return"string"==typeof t?t:t||0===t?""+t:""},after:function(e,n){var o=e[e.length-1];o.nextSibling?t.insertBefore(o.parentNode,n,o.nextSibling):t.appendChild(o.parentNode,n)},replace:function(e,r){o.after(e,r),t.remove(t.$(e)).length<e.length&&!n&&t.each(e,function(t){8===t.nodeType&&t.parentNode.removeChild(t)})}};return t.view.elements=o,o});
/*can/view/callbacks/callbacks*/
define("can/view/callbacks/callbacks",["can/util/util","can/view/view"],function(t){var e=t.view.attr=function(t,e){if(!e){var i=a[t];if(!i)for(var r=0,o=n.length;o>r;r++){var s=n[r];if(s.match.test(t)){i=s.handler;break}}return i}"string"==typeof t?a[t]=e:n.push({match:t,handler:e})},a={},n=[],i=/[-\:]/,r=t.view.tag=function(t,e){if(!e){var a=o[t.toLowerCase()];return!a&&i.test(t)&&(a=function(){}),a}"undefined"!=typeof window&&window.html5&&(window.html5.elements+=" "+t,window.html5.shivDocument()),o[t.toLowerCase()]=e},o={};return t.view.callbacks={_tags:o,_attributes:a,_regExpAttributes:n,tag:r,attr:e,tagHandler:function(e,a,n){var i=n.options.read("tags."+a,{isArgument:!0,proxyMethods:!1}).value,r=i||o[a],s=n.scope,c=r?r(e,n):s;if(r||t.dev.warn("can/view/scanner.js: No custom element found for "+a),c&&n.subtemplate){s!==c&&(s=s.add(c));var l=n.subtemplate(s,n.options),u="string"==typeof l?t.view.frag(l):l;t.appendChild(e,u)}}},t.view.callbacks});
/*can/view/scanner*/
define("can/view/scanner",["can/view/view","./elements","can/view/callbacks/callbacks"],function(can,elements,viewCallbacks){var newLine=/(\r|\n)+/g,notEndTag=/\//,clean=function(t){return t.split("\\").join("\\\\").split("\n").join("\\n").split('"').join('\\"').split("	").join("\\t")},getTag=function(t,e,n){if(t)return t;for(;n<e.length;){if("<"===e[n]&&!notEndTag.test(e[n+1]))return elements.reverseTagMap[e[n+1]]||"span";n++}return""},bracketNum=function(t){return--t.split("{").length- --t.split("}").length},myEval=function(script){eval(script)},attrReg=/([^\s]+)[\s]*=[\s]*$/,startTxt="var ___v1ew = [];",finishTxt="return ___v1ew.join('')",put_cmd="___v1ew.push(\n",insert_cmd=put_cmd,htmlTag=null,quote=null,beforeQuote=null,rescan=null,getAttrName=function(){var t=beforeQuote.match(attrReg);return t&&t[1]},status=function(){return quote?"'"+getAttrName()+"'":htmlTag?1:0},top=function(t){return t[t.length-1]},Scanner;return can.view.Scanner=Scanner=function(t){can.extend(this,{text:{},tokens:[]},t),this.text.options=this.text.options||"",this.tokenReg=[],this.tokenSimple={"<":"<",">":">",'"':'"',"'":"'"},this.tokenComplex=[],this.tokenMap={};for(var e,n=0;e=this.tokens[n];n++)e[2]?(this.tokenReg.push(e[2]),this.tokenComplex.push({abbr:e[1],re:new RegExp(e[2]),rescan:e[3]})):(this.tokenReg.push(e[1]),this.tokenSimple[e[1]]=e[0]),this.tokenMap[e[0]]=e[1];this.tokenReg=new RegExp("("+this.tokenReg.slice(0).concat(["<",">",'"',"'"]).join("|")+")","g")},Scanner.prototype={helpers:[],scan:function(t,e){var n=[],s=0,a=this.tokenSimple,r=this.tokenComplex;t=t.replace(newLine,"\n"),this.transform&&(t=this.transform(t)),t.replace(this.tokenReg,function(e,i){var o=arguments[arguments.length-2];if(o>s&&n.push(t.substring(s,o)),a[e])n.push(e);else for(var u,c=0;u=r[c];c++)if(u.re.test(e)){n.push(u.abbr),u.rescan&&n.push(u.rescan(i));break}s=o+i.length}),s<t.length&&n.push(t.substr(s));var i,o,u,c,l="",p=[startTxt+(this.text.start||"")],h=function(t,e){p.push(put_cmd,'"',clean(t),'"'+(e||"")+");")},g=[],f=null,m=!1,k={attributeHookups:[],tagHookups:[],lastTagHookup:""},b=function(){k.lastTagHookup=k.tagHookups.pop()+k.tagHookups.length},v="",x=[],T=!1,w=!1,d=0,_=this.tokenMap;for(htmlTag=quote=beforeQuote=null;void 0!==(u=n[d++]);){if(null===f)switch(u){case _.left:case _.escapeLeft:case _.returnLeft:m=htmlTag&&1;case _.commentLeft:f=u,l.length&&h(l),l="";break;case _.escapeFull:m=htmlTag&&1,rescan=1,f=_.escapeLeft,l.length&&h(l),rescan=n[d++],l=rescan.content||rescan,rescan.before&&h(rescan.before),n.splice(d,0,_.right);break;case _.commentFull:break;case _.templateLeft:l+=_.left;break;case"<":0!==n[d].indexOf("!--")&&(htmlTag=1,m=0),l+=u;break;case">":htmlTag=0;var H="/"===l.substr(l.length-1)||"--"===l.substr(l.length-2),N="";if(k.attributeHookups.length&&(N="attrs: ['"+k.attributeHookups.join("','")+"'], ",k.attributeHookups=[]),v+k.tagHookups.length!==k.lastTagHookup&&v===top(k.tagHookups))H&&(l=l.substr(0,l.length-1)),p.push(put_cmd,'"',clean(l),'"',",can.view.pending({tagName:'"+v+"',"+N+"scope: "+(this.text.scope||"this")+this.text.options),H?(p.push("}));"),l="/>",b()):"<"===n[d]&&n[d+1]==="/"+v?(p.push("}));"),l=u,b()):(p.push(",subtemplate: function("+this.text.argNames+"){\n"+startTxt+(this.text.start||"")),l="");else if(m||!T&&elements.tagToContentPropMap[x[x.length-1]]||N){var R=",can.view.pending({"+N+"scope: "+(this.text.scope||"this")+this.text.options+'}),"';H?h(l.substr(0,l.length-1),R+'/>"'):h(l,R+'>"'),l="",m=0}else l+=u;(H||T)&&(x.pop(),v=x[x.length-1],T=!1),k.attributeHookups=[];break;case"'":case'"':if(htmlTag)if(quote&&quote===u){quote=null;var L=getAttrName();if(viewCallbacks.attr(L)&&k.attributeHookups.push(L),w){l+=u,h(l),p.push(finishTxt,"}));\n"),l="",w=!1;break}}else if(null===quote&&(quote=u,beforeQuote=i,c=getAttrName(),"img"===v&&"src"===c||"style"===c)){h(l.replace(attrReg,"")),l="",w=!0,p.push(insert_cmd,"can.view.txt(2,'"+getTag(v,n,d)+"',"+status()+",this,function(){",startTxt),h(c+"="+u);break}default:if("<"===i){v="!--"===u.substr(0,3)?"!--":u.split(/\s/)[0];var S,y=!1;0===v.indexOf("/")&&(y=!0,S=v.substr(1)),y?(top(x)===S&&(v=S,T=!0),top(k.tagHookups)===S&&(h(l.substr(0,l.length-1)),p.push(finishTxt+"}}) );"),l="><",b())):(v.lastIndexOf("/")===v.length-1&&(v=v.substr(0,v.length-1)),"!--"!==v&&viewCallbacks.tag(v)&&("content"===v&&elements.tagMap[top(x)]&&(u=u.replace("content",elements.tagMap[top(x)])),k.tagHookups.push(v)),x.push(v))}l+=u}else switch(u){case _.right:case _.returnRight:switch(f){case _.left:o=bracketNum(l),1===o?(p.push(insert_cmd,"can.view.txt(0,'"+getTag(v,n,d)+"',"+status()+",this,function(){",startTxt,l),g.push({before:"",after:finishTxt+"}));\n"})):(s=g.length&&-1===o?g.pop():{after:";"},s.before&&p.push(s.before),p.push(l,";",s.after));break;case _.escapeLeft:case _.returnLeft:o=bracketNum(l),o&&g.push({before:finishTxt,after:"}));\n"});for(var j=f===_.escapeLeft?1:0,C={insert:insert_cmd,tagName:getTag(v,n,d),status:status(),specialAttribute:w},q=0;q<this.helpers.length;q++){var E=this.helpers[q];if(E.name.test(l)){l=E.fn(l,C),E.name.source===/^>[\s]*\w*/.source&&(j=0);break}}"object"==typeof l?l.startTxt&&l.end&&w?p.push(insert_cmd,"can.view.toStr( ",l.content,"() ) );"):(l.startTxt?p.push(insert_cmd,"can.view.txt(\n"+("string"==typeof status()||(null!=l.escaped?l.escaped:j))+",\n'"+v+"',\n"+status()+",\nthis,\n"):l.startOnlyTxt&&p.push(insert_cmd,"can.view.onlytxt(this,\n"),p.push(l.content),l.end&&p.push("));")):w?p.push(insert_cmd,l,");"):p.push(insert_cmd,"can.view.txt(\n"+("string"==typeof status()||j)+",\n'"+v+"',\n"+status()+",\nthis,\nfunction(){ "+(this.text.escape||"")+"return ",l,o?startTxt:"}));\n"),rescan&&rescan.after&&rescan.after.length&&(h(rescan.after.length),rescan=null)}f=null,l="";break;case _.templateLeft:l+=_.left;break;default:l+=u}i=u}l.length&&h(l),p.push(";");var M=p.join(""),A={out:(this.text.outStart||"")+M+" "+finishTxt+(this.text.outEnd||"")};return myEval.call(A,"this.fn = (function("+this.text.argNames+"){"+A.out+"});\r\n//# sourceURL="+e+".js"),A}},can.view.pending=function(t){var e=can.view.getHooks();return can.view.hook(function(n){can.each(e,function(t){t(n)}),t.templateType="legacy",t.tagName&&viewCallbacks.tagHandler(n,t.tagName,t),can.each(t&&t.attrs||[],function(e){t.attributeName=e;var s=viewCallbacks.attr(e);s&&s(n,t)})})},can.view.tag("content",function(t,e){return e.scope}),can.view.Scanner=Scanner,Scanner});
/*can/view/node_lists/node_lists*/
define("can/view/node_lists/node_lists",["can/util/util","can/view/elements"],function(e){var n=!0;try{document.createTextNode("")._=0}catch(r){n=!1}var t={},i={},u="ejs_"+Math.random(),a=0,s=function(e){if(n||3!==e.nodeType)return e[u]?e[u]:(++a,e[u]=(e.nodeName?"element_":"obj_")+a);for(var r in i)if(i[r]===e)return r;return++a,i["text_"+a]=e,"text_"+a},o=[].splice,d=[].push,c=function(e){for(var n=0,r=0,t=e.length;t>r;r++){var i=e[r];i.nodeType?n++:n+=c(i)}return n},l={id:s,update:function(n,r){var t=l.unregisterChildren(n);r=e.makeArray(r);var i=n.length;return o.apply(n,[0,i].concat(r)),l.nestList(n),t},nestList:function(e){for(var n=0;n<e.length;){var r=e[n],i=t[s(r)];i?i!==e&&e.splice(n,c(i),i):t[s(r)]=e,n++}},last:function(e){var n=e[e.length-1];return n.nodeType?n:l.last(n)},first:function(e){var n=e[0];return n.nodeType?n:l.first(n)},register:function(e,n){return e.unregistered=n,l.nestList(e),e},unregisterChildren:function(n){var r=[];return e.each(n,function(e){e.nodeType?(delete t[s(e)],r.push(e)):d.apply(r,l.unregister(e))}),r},unregister:function(e){var n=l.unregisterChildren(e);if(e.unregistered){var r=e.unregistered;delete e.unregistered,r()}return n},nodeMap:t};return e.view.nodeLists=l,l});
/*can/view/parser/parser*/
define("can/view/parser/parser",["can/view/view"],function(e){function t(e){for(var t={},r=e.split(","),a=0;a<r.length;a++)t[r[a]]=!0;return t}var r="-A-Za-z0-9_",a="[a-zA-Z_:]["+r+":.]+",s="\\s*=\\s*",n='"((?:\\\\.|[^"])*)"',i="'((?:\\\\.|[^'])*)'",l="(?:"+s+"(?:(?:\"[^\"]*\")|(?:'[^']*')|[^>\\s]+))?",o="\\{\\{[^\\}]*\\}\\}\\}?",c="\\{\\{([^\\}]*)\\}\\}\\}?",d=new RegExp("^<(["+r+"]+)((?:\\s*(?:(?:(?:"+a+")?"+l+")|(?:"+o+")+))*)\\s*(\\/?)>"),p=new RegExp("^<\\/(["+r+"]+)[^>]*>"),u=new RegExp("(?:(?:("+a+")|"+c+")(?:"+s+"(?:(?:"+n+")|(?:"+i+")|([^>\\s]+)))?)","g"),f=new RegExp(c,"g"),g=/<|\{\{/,h=t

function r(e,r,s,n){if(r=r.toLowerCase(),m[r])for(;o.last()&&b[o.last()];)a("",o.last());x[r]&&o.last()===r&&a("",r),n=h[r]||!!n,t.start(r,n),n||o.push(r),E.parseAttrs(s,t),t.end(r,n)}function a(e,r){var a;if(r)for(a=o.length-1;a>=0&&o[a]!==r;a--);else a=0;if(a>=0){for(var s=o.length-1;s>=a;s--)t.close&&t.close(o[s]);o.length=a}}function s(e,r){t.special&&t.special(r)}var n,i,l,o=[],c=e;

),a("",o.last());else if(0===e.indexOf("<!--")?(n=e.indexOf("-->"),n

n=e.search(g);var u=0>n?e:e.substring(0,n);e=0>n?"":e.substring(n),t.chars&&u&&t.chars(u)}if(e===c)throw"Parse Error: "+e;c=e}a(),t.done()};return E.parseAttrs=function(e,t){(null!=e?e:"").replace(u,function(e,r,a,s,n,i){if(a&&t.special(a),r||s||n||i){var l=arguments[3]?arguments[3]:arguments[4]?arguments[4]:arguments[5]?arguments[5]:v[r.toLowerCase()]?r:"";t.attrStart(r||"");for(var o,c=f.lastIndex=0,d=f.exec(l);d;)o=l.substring(c,f.lastIndex-d[0].length),o.length&&t.attrValue(o),t.special(d[1]),c=f.lastIndex,d=f.exec(l);o=l.substr(c,l.length),o&&t.attrValue(o),t.attrEnd(r||"")}})},e.view.parser=E,E});
/*can/view/live/live*/
define("can/view/live/live",["can/util/util","can/view/elements","can/view/view","can/view/node_lists/node_lists","can/view/parser/parser"],function(e,t,n,r,i){t=t||e.view.elements,r=r||e.view.NodeLists,i=i||e.view.parser;var a=function(t,n,r){var i=!1,a=function(){return i||(i=!0,r(o),e.unbind.call(t,"removed",a)),!0},o={teardownCheck:function(e){return e?!1:a()}};return e.bind.call(t,"removed",a),n(o),o},o=function(e,t,n){return a(e,function(){t.bind("change",n)},function(e){t.unbind("change",n),e.nodeList&&r.unregister(e.nodeList)})},c=function(e){var t,n={};return i.parseAttrs(e,{attrStart:function(e){n[e]="",t=e},attrValue:function(e){n[t]+=e},attrEnd:function(){}}),n},u=[].splice,d=function(e){return e&&e.nodeType},s=function(e){e.childNodes.length||e.appendChild(document.createTextNode(""))},l={list:function(n,i,o,c,d){var s,f=[n],p=[],v=function(n,i,a){var d=document.createDocumentFragment(),s=[],l=[];e.each(i,function(t,n){var i=e.compute(n+a),u=o.call(c,t,i),f="string"==typeof u,p=e.frag(u);p=f?e.view.hookup(p):p;var v=e.makeArray(p.childNodes);s.push(r.register(v)),d.appendChild(p),l.push(i)});var v=a+1;if(f[v]){var h=r.first(f[v]);e.insertBefore(h.parentNode,d,h)}else t.after(1===v?[g]:[r.last(f[v-1])],d);u.apply(f,[v,0].concat(s)),u.apply(p,[a,0].concat(l));for(var b=a+l.length,m=p.length;m>b;b++)p[b](b)},h=function(t,n,i,a,o){if(a||!w.teardownCheck(g.parentNode)){var c=f.splice(i+1,n.length),u=[];e.each(c,function(e){var t=r.unregister(e);[].push.apply(u,t)}),p.splice(i,n.length);for(var d=i,s=p.length;s>d;d++)p[d](d);o||e.remove(e.$(u))}},g=document.createTextNode(""),b=function(e){s&&s.unbind&&s.unbind("add",v).unbind("remove",h),h({},{length:f.length-1},0,!0,e)},m=function(e,t){b(),s=t||[],s.bind&&s.bind("add",v).bind("remove",h),v({},s,0)};d=t.getParentNode(n,d);var w=a(d,function(){e.isFunction(i)&&i.bind("change",m)},function(){e.isFunction(i)&&i.unbind("change",m),b(!0)});l.replace(f,g,w.teardownCheck),m({},e.isFunction(i)?i():i)},html:function(n,i,a){var c;a=t.getParentNode(n,a),c=o(a,i,function(e,t){var n=r.first(u).parentNode;n&&l(t),c.teardownCheck(r.first(u).parentNode)});var u=[n],l=function(n){var i=!d(n),o=e.frag(n),c=e.makeArray(u);s(o),i&&(o=e.view.hookup(o,a)),c=r.update(u,o.childNodes),t.replace(c,o)};c.nodeList=u,r.register(u,c.teardownCheck),l(i())},replace:function(n,i,a){var o=n.slice(0),c=e.frag(i);return r.register(n,a),"string"==typeof i&&(c=e.view.hookup(c,n[0].parentNode)),r.update(n,c.childNodes),t.replace(o,c),n},text:function(n,r,i){var a=t.getParentNode(n,i),c=o(a,r,function(t,n){"unknown"!=typeof u.nodeValue&&(u.nodeValue=e.view.toStr(n)),c.teardownCheck(u.parentNode)}),u=document.createTextNode(e.view.toStr(r()));c.nodeList=l.replace([n],u,c.teardownCheck)},setAttributes:function(t,n){var r=c(n);for(var i in r)e.attr.set(t,i,r[i])},attributes:function(n,r,i){var a={},u=function(r){var i,o=c(r);for(i in o){var u=o[i],d=a[i];u!==d&&e.attr.set(n,i,u),delete a[i]}for(i in a)t.removeAttr(n,i);a=o};o(n,r,function(e,t){u(t)}),arguments.length>=3?a=c(i):u(r())},attributePlaceholder:"__!!__",attributeReplace:/__!!__/g,attribute:function(n,r,i){o(n,i,function(){t.setAttr(n,r,u.render())});var a,c=e.$(n);a=e.data(c,"hooks"),a||e.data(c,"hooks",a={});var u,d=t.getAttr(n,r),s=d.split(l.attributePlaceholder),f=[];f.push(s.shift(),s.join(l.attributePlaceholder)),a[r]?a[r].computes.push(i):a[r]={render:function(){var e=0,n=d?d.replace(l.attributeReplace,function(){return t.contentText(u.computes[e++]())}):t.contentText(u.computes[e++]());return n},computes:[i],batchNum:void 0},u=a[r],f.splice(1,0,i()),t.setAttr(n,r,f.join(""))},specialAttribute:function(e,n,r){o(e,r,function(r,i){t.setAttr(e,n,p(i))}),t.setAttr(e,n,p(r()))},simpleAttribute:function(e,n,r){o(e,r,function(r,i){t.setAttr(e,n,i)}),t.setAttr(e,n,r())}};l.attr=l.simpleAttribute,l.attrs=l.attributes;var f=/(\r|\n)+/g,p=function(e){var n=/^["'].*["']$/;return e=e.replace(t.attrReg,"").replace(f,""),n.test(e)?e.substr(1,e.length-2):e};return e.view.live=l,l});
/*can/view/render*/
define("can/view/render",["can/view/view","./elements","can/view/live/live","can/util/string/string"],function(t,n,e){var i,r=[],u=function(t){var e=n.tagMap[t]||"span";return"span"===e?"@@!!@@":"<"+e+">"+u(e)+"</"+e+">"},o=function(n,e){if("string"==typeof n)return n;if(!n&&0!==n)return"";var i=n.hookup&&function(t,e){n.hookup.call(n,t,e)}||"function"==typeof n&&n;return i?e?"<"+e+" "+t.view.hook(i)+"></"+e+">":(r.push(i),""):""+n},c=function(n,e){return"string"==typeof n||"number"==typeof n?t.esc(n):o(n,e)},s=!1,a=function(){};return t.extend(t.view,{live:e,setupLists:function(){var n,e=t.view.lists;return t.view.lists=function(t,e){return n={list:t,renderer:e},Math.random()},function(){return t.view.lists=e,n}},getHooks:function(){var t=r.slice(0);return i=t,r=[],t},onlytxt:function(t,n){return c(n.call(t))},txt:function(f,p,l,v,h){var w,g,d,b,y=n.tagMap[p]||"span",k=!1,m=a;if(s)w=h.call(v);else{("string"==typeof l||1===l)&&(s=!0);var x=t.view.setupLists();m=function(){d.unbind("change",a)},d=t.compute(h,v,!1),d.bind("change",a),g=x(),w=d(),s=!1,k=d.hasDependencies}if(g)return m(),"<"+y+t.view.hook(function(t,n){e.list(t,g.list,g.renderer,v,n)})+"></"+y+">";if(!k||"function"==typeof w)return m(),(s||2===f||!f?o:c)(w,0===l&&y);var M=n.tagToContentPropMap[p];return 0!==l||M?1===l?(r.push(function(t){e.attributes(t,d,d()),m()}),d()):2===f?(b=l,r.push(function(t){e.specialAttribute(t,b,d),m()}),d()):(b=0===l?M:l,(0===l?i:r).push(function(t){e.attribute(t,b,d),m()}),e.attributePlaceholder):"<"+y+t.view.hook(f&&"object"!=typeof w?function(t,n){e.text(t,d,n),m()}:function(t,n){e.html(t,d,n),m()})+">"+u(y)+"</"+y+">"}}),t});
/*can/view/mustache/mustache*/
define("can/view/mustache/mustache",["can/util/util","can/view/scope/scope","can/view/view","can/view/scanner","can/compute/compute","can/view/render"],function(e){e.view.ext=".mustache";var t="scope",n="___h4sh",r="{scope:"+t+",options:options}",i="{scope:"+t+",options:options, special: true}",s=t+",options",o=/((([^\s]+?=)?('.*?'|".*?"))|.*?)\s/g,a=/^(('.*?'|".*?"|[0-9]+\.?[0-9]*|true|false|null|undefined)|((.+?)=(('.*?'|".*?"|[0-9]+\.?[0-9]*|true|false)|(.+))))$/,c=function(e){return'{get:"'+e.replace(/"/g,'\\"')+'"}'},u=function(e){return e&&"string"==typeof e.get},f=function(t){return t instanceof e.Map||t&&!!t._get},p=function(e){return e&&e.splice&&"number"==typeof e.length},l=function(t,n,r){var i=function(e,r){return t(e||n,r)};return function(t,s){return void 0===t||t instanceof e.view.Scope||(t=n.add(t)),void 0===s||s instanceof e.view.Options||(s=r.add(s)),i(t,s||r)}},h=function(t){if(this.constructor!==h){var n=new h(t);return function(e,t){return n.render(e,t)}}return"function"==typeof t?void(this.template={fn:t}):(e.extend(this,t),void(this.template=this.scanner.scan(this.text,this.name)))};e.Mustache=h,"undefined"!=typeof window&&(window.Mustache=h),h.prototype.render=function(t,n){return t instanceof e.view.Scope||(t=new e.view.Scope(t||{})),n instanceof e.view.Options||(n=new e.view.Options(n||{})),n=n||{},this.template.fn.call(t,t,n)},e.extend(h.prototype,{scanner:new e.view.Scanner({text:{start:"",scope:t,options:",options: options",argNames:s},tokens:[["returnLeft","{{{","{{[{&]"],["commentFull","{{!}}","^[\\s\\t]*{{!.+?}}\\n"],["commentLeft","{{!","(\\n[\\s\\t]*{{!|{{!)"],["escapeFull","{{}}","(^[\\s\\t]*{{[#/^][^}]+?}}\\n|\\n[\\s\\t]*{{[#/^][^}]+?}}\\n|\\n[\\s\\t]*{{[#/^][^}]+?}}$)",function(e){return{before:/^\n.+?\n$/.test(e)?"\n":"",content:e.match(/\{\{(.+?)\}\}/)[1]||""}}],["escapeLeft","{{"],["returnRight","}}}"],["right","}}"]],helpers:[{name:/^>[\s]*\w*/,fn:function(t){var n=e.trim(t.replace(/^>\s?/,"")).replace(/["|']/g,"");return"can.Mustache.renderPartial('"+n+"',"+s+")"}},{name:/^\s*data\s/,fn:function(e){var n=e.match(/["|'](.*)["|']/)[1];return"can.proxy(function(__){can.data(can.$(__),'"+n+"', this.attr('.')); }, "+t+")"}},{name:/\s*\(([\$\w]+)\)\s*->([^\n]*)/,fn:function(e){var n=/\s*\(([\$\w]+)\)\s*->([^\n]*)/,r=e.match(n);return"can.proxy(function(__){var "+r[1]+"=can.$(__);with("+t+".attr('.')){"+r[2]+"}}, this);"}},{name:/^.*$/,fn:function(t,u){var f=!1,p={content:"",startTxt:!1,startOnlyTxt:!1,end:!1};if(t=e.trim(t),t.length&&(f=t.match(/^([#^/]|else$)/))){switch(f=f[0]){case"#":case"^":u.specialAttribute?p.startOnlyTxt=!0:(p.startTxt=!0,p.escaped=0);break;case"/":return p.end=!0,p.content+='return ___v1ew.join("");}}])',p}t=t.substring(1)}if("else"!==f){var l,h=[],v=[],d=0;p.content+="can.Mustache.txt(\n"+(u.specialAttribute?i:r)+",\n"+(f?'"'+f+'"':"null")+",",(e.trim(t)+" ").replace(o,function(e,t){d&&(l=t.match(a))?l[2]?h.push(l[0]):v.push(l[4]+":"+(l[6]?l[6]:c(l[5]))):h.push(c(t)),d++}),p.content+=h.join(","),v.length&&(p.content+=",{"+n+":{"+v.join(",")+"}}")}switch(f&&"else"!==f&&(p.content+=",[\n\n"),f){case"^":case"#":p.content+="{fn:function("+s+"){var ___v1ew = [];";break;case"else":p.content+='return ___v1ew.join("");}},\n{inverse:function('+s+"){\nvar ___v1ew = [];";break;default:p.content+=")"}return f||(p.startTxt=!0,p.end=!0),p}}]})});for(var v=e.view.Scanner.prototype.helpers,d=0;d<v.length;d++)h.prototype.scanner.helpers.unshift(v[d]);return h.txt=function(t,r,i){for(var s,o,a=t.scope,c=t.options,v=[],d={fn:function(){},inverse:function(){}},g=a.attr("."),m=!0,w=3;w<arguments.length;w++){var x=arguments[w];if(r&&e.isArray(x))d=e.extend.apply(e,[d].concat(x));else if(x&&x[n]){s=x[n];for(var _ in s)u(s[_])&&(s[_]=h.get(s[_].get,t,!1,!0))}else v.push(x&&u(x)?h.get(x.get,t,!1,!0):x)}if(u(i)){var y=i.get;i=h.get(i.get,t,v.length,!1),m=y===i}if(d.fn=l(d.fn,a,c),d.inverse=l(d.inverse,a,c),"^"===r){var b=d.fn;d.fn=d.inverse,d.inverse=b}return(o=m&&"string"==typeof i&&h.getHelper(i,c)||e.isFunction(i)&&!i.isComputed&&{fn:i})?(e.extend(d,{context:g,scope:a,contexts:a,hash:s}),v.push(d),function(){return o.fn.apply(g,v)||""}):function(){var t;t=e.isFunction(i)&&i.isComputed?i():i;var n,s,o,a=v.length?v:[t],c=!0,u=[];if(r)for(n=0;n<a.length;n++)o=a[n],s="undefined"!=typeof o&&f(o),p(o)?"#"===r?c=c&&!!(s?o.attr("length"):o.length):"^"===r&&(c=c&&!(s?o.attr("length"):o.length)):c="#"===r?c&&!!o:"^"===r?c&&!o:c;if(c){if("#"===r){if(p(t)){var l=f(t);for(n=0;n<t.length;n++)u.push(d.fn(l?t.attr(""+n):t[n]));return u.join("")}return d.fn(t||{})||""}return"^"===r?d.inverse(t||{})||"":""+(null!=t?t:"")}return""}},h.get=function(t,n,r,i){var s=n.scope.attr("."),o=n.options||{};if(r){if(h.getHelper(t,o))return t;if(n.scope&&e.isFunction(s[t]))return s[t];e.dev.warn('can/view/mustache/mustache.js: Unable to find helper "'+t+'".')}var a=n.scope.computeData(t,{isArgument:i,args:[s,n.scope]}),c=a.compute;e.compute.temporarilyBind(c);var u=a.initialValue;return void 0!==u||r||e.dev.warn('can/view/mustache/mustache.js: Unable to find key "'+t+'".'),void 0!==u&&a.scope===n.scope||!h.getHelper(t,o)?c.hasDependencies?c:u:t},h.resolve=function(t){return f(t)&&p(t)&&t.attr("length")?t:e.isFunction(t)?t():t},e.view.Options=e.view.Scope.extend({init:function(t){t.helpers||t.partials||t.tags||(t={helpers:t}),e.view.Scope.prototype.init.apply(this,arguments)}}),h._helpers={},h.registerHelper=function(e,t){this._helpers[e]={name:e,fn:t}},h.getHelper=function(e,t){var n=t.attr("helpers."+e);return n?{fn:n}:this._helpers[e]},h.render=function(t,n){if(!e.view.cached[t]){var r=e.__clearReading();n.attr("partial")&&(t=n.attr("partial")),e.__setReading(r)}return e.view.render(t,n)},h.safeString=function(e){return{toString:function(){return e}}},h.renderPartial=function(t,n,r){var i=r.attr("partials."+t);return i?i.render?i.render(n,r):i(n,r):e.Mustache.render(t,n,r)},e.each({"if":function(t,n){var r;return r=e.isFunction(t)?e.compute.truthy(t)():!!h.resolve(t),r?n.fn(n.contexts||this):n.inverse(n.contexts||this)},unless:function(e,t){return h.resolve(e)?void 0:t.fn(t.contexts||this)},each:function(t,n){var r,i,s,o=h.resolve(t),a=[];if(e.view.lists&&(o instanceof e.List||t&&t.isComputed&&void 0===o))return e.view.lists(t,function(e,t){return n.fn(n.scope.add({"@index":t}).add(e))});if(t=o,t&&p(t)){for(s=0;s<t.length;s++)a.push(n.fn(n.scope.add({"@index":s}).add(t[s])));return a.join("")}if(f(t)){for(r=e.Map.keys(t),s=0;s<r.length;s++)i=r[s],a.push(n.fn(n.scope.add({"@key":i}).add(t[i])));return a.join("")}if(t instanceof Object){for(i in t)a.push(n.fn(n.scope.add({"@key":i}).add(t[i])));return a.join("")}},"with":function(e,t){var n=e;return e=h.resolve(e),e?t.fn(n):void 0},log:function(e,t){"undefined"!=typeof console&&console.log&&(t?console.log(e,t.context):console.log(e.context))}},function(e,t){h.registerHelper(t,e)}),e.view.register({suffix:"mustache",contentType:"x-mustache-template",script:function(e,t){return"can.Mustache(function("+s+") { "+new h({text:t,name:e}).template.out+" })"},renderer:function(e,t){return h({text:t,name:e})}}),e.mustache.registerHelper=e.proxy(e.Mustache.registerHelper,e.Mustache),e.mustache.safeString=e.Mustache.safeString,e});
/*can/observe/observe*/
define("can/observe/observe",["can/util/util","can/map/map","can/list/list","can/compute/compute"],function(t){return t.Observe=t.Map,t.Observe.startBatch=t.batch.start,t.Observe.stopBatch=t.batch.stop,t.Observe.triggerBatch=t.batch.trigger,t});
/*can/view/bindings/bindings*/
define("can/view/bindings/bindings",["can/util/util","can/view/mustache/mustache","can/control/control"],function(e){var t=function(){var e={"":!0,"true":!0,"false":!1},t=function(t){if(t&&t.getAttribute){var n=t.getAttribute("contenteditable");return e[n]}};return function(e){var n=t(e);return"boolean"==typeof n?n:!!t(e.parentNode)}}(),n=function(e){return"{"===e[0]&&"}"===e[e.length-1]?e.substr(1,e.length-2):e};e.view.attr("can-value",function(i,l){var r,c,h=n(i.getAttribute("can-value")),v=l.scope.computeData(h,{args:[]}).compute;return"input"===i.nodeName.toLowerCase()&&("checkbox"===i.type&&(r=e.attr.has(i,"can-true-value")?i.getAttribute("can-true-value"):!0,c=e.attr.has(i,"can-false-value")?i.getAttribute("can-false-value"):!1),"checkbox"===i.type||"radio"===i.type)?void new a(i,{value:v,trueValue:r,falseValue:c}):"select"===i.nodeName.toLowerCase()&&i.multiple?void new o(i,{value:v}):t(i)?void new u(i,{value:v}):void new s(i,{value:v})});var i={enter:function(e,t,n){return{event:"keyup",handler:function(e){return 13===e.keyCode?n.call(this,e):void 0}}}};e.view.attr(/can-[\w\.]+/,function(t,s){var a=s.attributeName,o=a.substr("can-".length),u=function(i){var o=n(t.getAttribute(a)),u=s.scope.read(o,{returnObserveMethods:!0,isArgument:!0});return u.value.call(u.parent,s.scope._context,e.$(this),i)};if(i[o]){var l=i[o](s,t,u);u=l.handler,o=l.event}e.bind.call(t,o,u)});var s=e.Control.extend({init:function(){"SELECT"===this.element[0].nodeName.toUpperCase()?setTimeout(e.proxy(this.set,this),1):this.set()},"{value} change":"set",set:function(){if(this.element){var e=this.options.value();this.element[0].value=null==e?"":e}},change:function(){this.element&&this.options.value(this.element[0].value)}}),a=e.Control.extend({init:function(){this.isCheckbox="checkbox"===this.element[0].type.toLowerCase(),this.check()},"{value} change":"check",check:function(){if(this.isCheckbox){var t=this.options.value(),n=this.options.trueValue||!0;this.element[0].checked=t===n}else{var i=this.options.value()==this.element[0].value?"set":"remove";e.attr[i](this.element[0],"checked",!0)}},change:function(){this.isCheckbox?this.options.value(this.element[0].checked?this.options.trueValue:this.options.falseValue):this.element[0].checked&&this.options.value(this.element[0].value)}}),o=s.extend({init:function(){this.delimiter=";",this.set()},set:function(){var t=this.options.value();"string"==typeof t?(t=t.split(this.delimiter),this.isString=!0):t&&(t=e.makeArray(t));var n={};e.each(t,function(e){n[e]=!0}),e.each(this.element[0].childNodes,function(e){e.value&&(e.selected=!!n[e.value])})},get:function(){var t=[],n=this.element[0].childNodes;return e.each(n,function(e){e.selected&&e.value&&t.push(e.value)}),t},change:function(){var t=this.get(),n=this.options.value();this.isString||"string"==typeof n?(this.isString=!0,this.options.value(t.join(this.delimiter))):n instanceof e.List?n.attr(t,!0):this.options.value(t)}}),u=e.Control.extend({init:function(){this.set(),this.on("blur","setValue")},"{value} change":"set",set:function(){var e=this.options.value();this.element[0].innerHTML="undefined"==typeof e?"":e},setValue:function(){this.options.value(this.element[0].innerHTML)}})});
/*can/component/component*/
define("can/component/component",["can/util/util","can/view/callbacks/callbacks","can/control/control","can/observe/observe","can/view/mustache/mustache","can/view/bindings/bindings"],function(t,e){var n=/^(dataViewId|class|id)$/i,o=/\{([^\}]+)\}/g,s=t.Component=t.Construct.extend({setup:function(){if(t.Construct.setup.apply(this,arguments),t.Component){var e=this,n=this.prototype.scope;if(this.Control=i.extend(this.prototype.events),n&&("object"!=typeof n||n instanceof t.Map)?n.prototype instanceof t.Map&&(this.Map=n):this.Map=t.Map.extend(n||{}),this.attributeScopeMappings={},t.each(this.Map?this.Map.defaults:{},function(t,n){"@"===t&&(e.attributeScopeMappings[n]=n)}),this.prototype.template)if("function"==typeof this.prototype.template){var o=this.prototype.template;this.renderer=function(){return t.view.frag(o.apply(null,arguments))}}else this.renderer=t.view.mustache(this.prototype.template);t.view.tag(this.prototype.tag,function(t,n){new e(t,n)})}}},{setup:function(o,s){var i,a,c,p={},r=this,u={};if(t.each(this.constructor.attributeScopeMappings,function(e,n){p[n]=o.getAttribute(t.hyphenate(e))}),t.each(t.makeArray(o.attributes),function(c){var l=t.camelize(c.nodeName.toLowerCase()),h=c.value;if(!(r.constructor.attributeScopeMappings[l]||n.test(l)||e.attr(c.nodeName))){if("{"===h[0]&&"}"===h[h.length-1])h=h.substr(1,h.length-2);else if("legacy"!==s.templateType)return void(p[l]=h);var d=s.scope.computeData(h,{args:[]}),f=d.compute,m=function(t,e){i=l,a.attr(l,e),i=null};f.bind("change",m),p[l]=f(),f.hasDependencies?(t.bind.call(o,"removed",function(){f.unbind("change",m)}),u[l]=d):f.unbind("change",m)}}),this.constructor.Map)a=new this.constructor.Map(p);else if(this.scope instanceof t.Map)a=this.scope;else if(t.isFunction(this.scope)){var l=this.scope(p,s.scope,o);a=l instanceof t.Map?l:l.prototype instanceof t.Map?new l(p):new(t.Map.extend(l))(p)}var h={};t.each(u,function(t,e){h[e]=function(n,o){i!==e&&t.compute(o)},a.bind(e,h[e])}),t.bind.call(o,"removed",function(){t.each(h,function(t,e){a.unbind(e,h[e])})}),t.isEmptyObject(this.constructor.attributeScopeMappings)&&"legacy"===s.templateType||t.bind.call(o,"attributes",function(e){var n=t.camelize(e.attributeName);u[n]||a.attr(n,o.getAttribute(e.attributeName))}),this.scope=a,t.data(t.$(o),"scope",this.scope);var d=s.scope.add(this.scope),f={helpers:{}};t.each(this.helpers||{},function(e,n){t.isFunction(e)&&(f.helpers[n]=function(){return e.apply(a,arguments)})}),this._control=new this.constructor.Control(o,{scope:this.scope}),this.constructor.renderer?(f.tags||(f.tags={}),f.tags.content=function m(e,n){var o=s.subtemplate||n.subtemplate;o&&(delete f.tags.content,t.view.live.replace([e],o(n.scope,n.options)),f.tags.content=m)},c=this.constructor.renderer(d,s.options.add(f))):c="legacy"===s.templateType?t.view.frag(s.subtemplate?s.subtemplate(d,s.options.add(f)):""):s.subtemplate?s.subtemplate(d,s.options.add(f)):document.createDocumentFragment(),t.appendChild(o,c)}}),i=t.Control.extend({_lookup:function(t){return[t.scope,t,window]},_action:function(e,n,s){var i,a;if(o.lastIndex=0,i=o.test(e),s||!i){if(i){a=t.compute(function(){var s,i=e.replace(o,function(e,o){var i;return"scope"===o?(s=n.scope,""):(o=o.replace(/^scope\./,""),i=t.compute.read(n.scope,o.split("."),{isArgument:!0}).value,void 0===i&&(i=t.getObject(o)),"string"==typeof i?i:(s=i,""))}),a=i.split(/\s+/g),c=a.pop();return{processor:this.processors[c]||this.processors.click,parts:[i,a.join(" "),c],delegate:s||void 0}},this);var c=function(t,n){s._bindings.control[e](s.element),s._bindings.control[e]=n.processor(n.delegate||s.element,n.parts[2],n.parts[1],e,s)};return a.bind("change",c),s._bindings.readyComputes[e]={compute:a,handler:c},a()}return t.Control._action.apply(this,arguments)}}},{setup:function(e,n){return this.scope=n.scope,t.Control.prototype.setup.call(this,e,n)},off:function(){this._bindings&&t.each(this._bindings.readyComputes||{},function(t){t.compute.unbind("change",t.handler)}),t.Control.prototype.off.apply(this,arguments),this._bindings.readyComputes={}}});return window.$&&$.fn&&($.fn.scope=function(t){return t?this.data("scope").attr(t):this.data("scope")}),t.scope=function(e,n){return e=t.$(e),n?t.data(e,"scope").attr(n):t.data(e,"scope")},s});
/*can/can*/
define("can/can",["can/util/util","can/control/route/route","can/model/model","can/view/mustache/mustache","can/component/component"],function(n){return n});
/*can/view/mustache*/
System.set('can/view/mustache', Module({}));
/*can/view/mustache/system*/
System.set('can/view/mustache/system', Module({}));
/*bithub/homepage/homepage.mustache!can/view/mustache/system*/
define("bithub/homepage/homepage.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/homepage/homepage.mustache",e.Mustache(function(){var e=[];return e.push('<div id="main-container" class="span8 left-content"></div>\n<div id="sidebar-container" class="span4"></div>\n'),e.join("")}))});
/*bithub/homepage/event_list/views/init.mustache!can/view/mustache/system*/
define("bithub/homepage/event_list/views/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(i){return i.view.preloadStringRenderer("bithub/homepage/event_list/views/init.mustache",i.Mustache(function(s,e){var t=[];return t.push('<div class="filters">\n	<div id="issue-state-filter"></div>\n	<div id="timespan-filter"></div>\n</div>\n<div class="event-list-container" class="span8 left-content">\n	<div class="spinner-top">\n		<img '),t.push(i.view.txt(2,"img","src",this,function(){var i=[];return i.push('src="'),i.push('/assets/images/ajax-loader-sm-blue.gif"'),i.join("")})),t.push(' alt="loading...">\n	</div>\n	<div class="event-list">\n		<div class="events-list-wrapper"></div>\n		<div class="spinner-bottom">\n			<img '),t.push(i.view.txt(2,"img","src",this,function(){var i=[];return i.push('src="'),i.push('/assets/images/ajax-loader-sm-blue.gif"'),i.join("")})),t.push(' alt="loading...">\n		</div>'),t.push("\n"),t.push(i.view.txt(0,"/div",0,this,i.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"doneLoading"},[{fn:function(){var i=[];return i.push('			<div class="noMoreContent">... that\'s all there is ...</div>'),i.push("\n"),i.join("")}}]))),t.push("	</div>  \n</div>"),t.join("")}))});
/*bithub/homepage/event_list/views/latest.mustache!can/view/mustache/system*/
define("bithub/homepage/event_list/views/latest.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/homepage/event_list/views/latest.mustache",t.Mustache(function(s,e){var i=[];return i.push('<div class="events-group latest-group">'),i.push("\n"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"data.eventList"},[{fn:function(s,e){var i=[];return i.push('		<div class="day">'),i.push("\n"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"hasEvents"},[{fn:function(s,e){var i=[];return i.push('				<div class="sep-wrapper wrapper" '),i.push(t.view.txt(2,"div","style",this,function(){var t=[];return t.push('style="'),t.push('height: 42px;"'),t.join("")})),i.push('>\n					<div class="sep blue">\n					<h4 class="flag blue">\n						'),i.push(t.view.txt(0,"h4",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"stopDate"},[{fn:function(s,e){var i=[];return i.push(t.view.txt(1,"h4",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"prettifyTs"},{get:"stopDate"},"date"))),i.push(" - "),i.join("")}}]))),i.push("\n						"),i.push(t.view.txt(1,"h4",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"prettifyTs"},{get:"date"},"date"))),i.push("\n						</h4>\n					</div>\n				</div>"),i.push("\n"),i.join("")}}]))),i.push("			"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"hasDigest"},[{fn:function(s,e){var i=[];return i.push('\n				<bh-digest-list digest="digest"',t.view.pending({tagName:"bh-digest-list",scope:s,options:e})),i.push("></bh-digest-list>"),i.push("\n"),i.join("")}}]))),i.push("			"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"dailyCategories"},[{fn:function(s,e){var i=[];return i.push('\n				<div class="category-'),i.push(t.view.txt(!0,"div","class",this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),i.push('"',t.view.pending({scope:s,options:e}),">"),i.push("\n"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"hasCategoryFilter"},[{fn:function(s,e){var i=[];return i.push('						<h3 class="section-title"><i class="icon-'),i.push(t.view.txt(!0,"i","class",this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),i.push('"',t.view.pending({scope:s,options:e}),">"),i.push("</i>"),i.push(t.view.txt(1,"h3",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),i.push("</h3>"),i.push("\n"),i.join("")}}]))),i.push("					"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"entityComponent"},{get:"events"},{get:"date"}))),i.push("\n				</div>"),i.push("\n"),i.join("")}}]))),i.push("			"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"types.code"},[{fn:function(s,e){var i=[];return i.push('\n				<div class="category-code">'),i.push("\n"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"hasCategoryFilter"},[{fn:function(){var t=[];return t.push('						<h3 class="section-title"><i class="icon-code"></i>Code</h3>'),t.push("\n"),t.join("")}}]))),i.push('					<bh-code events="code" currentdate="date"',t.view.pending({tagName:"bh-code",scope:s,options:e})),i.push("></bh-code>\n				</div>"),i.push("\n"),i.join("")}}]))),i.push("			"),i.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"types.chat"},[{fn:function(s,e){var i=[];return i.push('\n				<bh-chat messages="types.chat"',t.view.pending({tagName:"bh-chat",scope:s,options:e})),i.push("></bh-chat>"),i.push("\n"),i.join("")}}]))),i.push("			\n		</div>"),i.push("\n"),i.join("")}}]))),i.push("</div>"),i.join("")}))});
/*bithub/homepage/event_list/views/greatest.mustache!can/view/mustache/system*/
define("bithub/homepage/event_list/views/greatest.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/homepage/event_list/views/greatest.mustache",e.Mustache(function(t,s){var n=[];return n.push('<div class="events-group greatest-group">\n	'),n.push(e.view.txt(0,"div",0,this,e.Mustache.txt({scope:t,options:s},null,{get:"entityComponent"},{get:"data.eventList"}))),n.push("\n</div>\n"),n.join("")}))});
/*bithub/homepage/event_list/determine_event_partial*/
define("bithub/homepage/event_list/determine_event_partial",[],function(){var e=[{template:"bh-push",tags:["push"]},{template:"bh-issue",tags:["bug","feature"]},{template:"bh-twitter",tags:["tweet"]},{template:"bh-event",tags:["event"]}];return function(t){var a="bh-default",n=0;return can.each(e,function(e){var u=0;can.each(t,function(t){e.tags.indexOf(t)>=0&&u++}),u>n&&(a=e.template)}),a}});
/*can/construct/proxy/proxy*/
define("can/construct/proxy/proxy",["can/util/util","can/construct/construct"],function(t){var r=t.isFunction,n=t.isArray,o=t.makeArray,a=function(t){var a,s=o(arguments);t=s.shift(),n(t)||(t=[t]),a=this;for(var e=0;e<t.length;e++)if("string"==typeof t[e]&&!r(this[t[e]]))throw"class.js "+(this.fullName||this.Class.fullName)+" does not have a "+t[e]+"method!";return function(){for(var r,e,c=s.concat(o(arguments)),u=t.length,i=0;u>i;i++)e=t[i],e&&(r="string"==typeof e,c=(r?a[e]:e).apply(a,c||[]),u-1>i&&(c=!n(c)||c._use_call?[c]:c));return c}};t.Construct.proxy=t.Construct.prototype.proxy=a;for(var s=[t.Map,t.Control,t.Model],e=0;e<s.length;e++)s[e]&&(s[e].proxy=a);return t});
/*can/construct/super/super*/
define("can/construct/super/super",["can/util/util","can/construct/construct"],function(t){var n=t.isFunction,r=/xyz/.test(function(){return this.xyz})?/\b_super\b/:/.*/;return t.Construct._overwrite=function(t,u,i,s){t[i]=n(s)&&n(u[i])&&r.test(s)?function(t,n){return function(){var r,i=this._super;return this._super=u[t],r=n.apply(this,arguments),this._super=i,r}}(i,s):s},t.Construct._inherit=function(n,r,u){u=u||n;for(var i in n)t.Construct._overwrite(u,r,i,n[i])},t});
/*bithub/models/upvote*/
define("bithub/models/upvote",["can/can","can/model/model","can/construct/proxy/proxy","can/construct/super/super"],function(t,e){return e("Bithub.Models.Upvote",{update:"POST /api/v1/events/{eventId}/upvote",destroy:"DELETE /api/v1/events/{eventId}/upvote"},{init:function(){var t=this.attr("event");t&&this.attr("eventId",t.attr("id"))},upvote:function(){var t=this;return this.sumUpvotes(),this.save().fail(function(){t.sumUpvotes(-1)})},unvote:function(){var t=this;return this.sumUpvotes(-1),this.destroy().fail(function(){t.sumUpvotes(1)})},sumUpvotes:function(t){var e,n=window.CURRENT_USER.attr("upvoted_events"),s=this.event.attr("id");t=t||1,this.event.attr("upvotes",this.event.attr("upvotes")+t),t>0?n.push(s):(e=n.indexOf(s),e>-1&&n.splice(e,1))},isNew:function(){return!1}})});
/*bithub/models/award*/
define("bithub/models/award",["can/can","can/construct/proxy/proxy","can/construct/super/super"],function(t){return t.Model("Bithub.Models.Award",{init:function(){},create:"POST /api/v1/events/{eventId}/award"},{init:function(){this.attr("eventId",this.attr("event.id"))},award:function(){return this.save().done(this.proxy("awardEvent"))},awardEvent:function(r){var a=this.attr("event"),e=a.attr("parent_id"),n=a.constructor.store&&a.constructor.store[e];a.attr("props").attr({awarded_value:r.value,thread_awarded:!0}),n&&t.map(n.attr("children"),function(t){t.attr("props.thread_awarded",!0)})}})});
/*bithub/editpost/editpost.mustache!can/view/mustache/system*/
define("bithub/editpost/editpost.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/editpost/editpost.mustache",t.Mustache(function(e,s){var n=[];return n.push('<div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3>Edit event</h3> \n</div>\n<div class="modal-body">\n	<div class="row">\n		<div class="span8 newpost-form">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"if"},{get:"currentEvent"},[{fn:function(e,s){var n=[];return n.push('				<bh-postform event="currentEvent"',t.view.pending({tagName:"bh-postform",scope:e,options:s})),n.push("></bh-postform>"),n.push("\n"),n.join("")}}]))),n.push("		</div>\n	</div>\n</div>"),n.join("")}))});
/*bithub/postform/postform.mustache!can/view/mustache/system*/
define("bithub/postform/postform.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/postform/postform.mustache",t.Mustache(function(s,e){var n=[];return n.push('<form id="newpost-form" class="form-horizontal" action="'),n.push(t.view.txt(!0,"form","action",this,t.Mustache.txt({scope:s,options:e},null,{get:"formAction"}))),n.push('" method="post" enctype="multipart/form-data" accept-charset="UTF-8"',t.view.pending({scope:s,options:e}),">"),n.push('\n	<fieldset>\n		<div class="row">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"serverError"},[{fn:function(s,e){var n=[];return n.push('				<div class="alert alert-error post-error">\n					<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"this"},[{fn:function(s,e){var n=[];return n.push("							<li>"),n.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</li>"),n.push("\n"),n.join("")}}]))),n.push("					</ul>\n				</div>"),n.push("\n"),n.join("")}}]))),n.push('			<div class="span left">\n				<div class="image-uploader">\n					<span>Add An <br>Image</span>'),n.push("\n"),n.push(t.view.txt(0,"/span",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"hasImage"},[{fn:function(){var t=[];return t.push('						<div class="remove-image" can-click="removeImage">&times;</div>'),t.push("\n"),t.join("")}}]))),n.push('					<div class="image-preview">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"event.postImageUrl"},[{fn:function(s,e){var n=[];return n.push("							<img "),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push('"'),n.join("")})),n.push(" "),n.push(t.view.txt(1,"img",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"removeBrokenPreviewImage"}))),n.push("",t.view.pending({scope:s,options:e}),">"),n.push("\n"),n.join("")}}]))),n.push("					</div>"),n.push("\n"),n.push(t.view.txt(0,"/div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"imageUploadProgress"},[{fn:function(s,e){var n=[];return n.push('						<div class="progress">\n							<div class="bar" '),n.push(t.view.txt(2,"div","style",this,function(){var n=[];return n.push('style="'),n.push("width: "),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"imageUploadProgress"})())),n.push('%;"'),n.join("")})),n.push("",t.view.pending({scope:s,options:e}),">"),n.push("</div>\n						</div>"),n.push("\n"),n.join("")}}]))),n.push("					"),n.push(t.view.txt(0,"img",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"imageUploadError"},[{fn:function(){var t=[];return t.push('\n						<div class="text-error">Unsupported filetype!</div>'),t.push("\n"),t.join("")}}]))),n.push("					"),n.push(t.view.txt(0,"img",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"unless"},{get:"hasImage"},[{fn:function(s,e){var n=[];return n.push('\n						<input type="file" name="event[image]" size="1" '),n.push(t.view.txt(1,"input",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"fileupload"}))),n.push(" ",t.view.pending({scope:s,options:e}),"/>"),n.push("\n"),n.join("")}}]))),n.push('				</div>\n				<div class="control-group" id="newpost-form-project">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"project",[{fn:function(s,e){var n=[];return n.push('						<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push('					<div class="select-menu">\n						<select can-value="event.project">\n							<option value=""',t.view.pending({scope:s,options:e}),">"),n.push("Pick a Project</option>"),n.push("\n"),n.push(t.view.txt(0,"select",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"eventProjects"},[{fn:function(s,e){var n=[];return n.push('								<option value="'),n.push(t.view.txt(!0,"option","value",this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"option",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"display_name"}))),n.push("</option>"),n.push("\n"),n.join("")}}]))),n.push('						</select>\n					</div>\n				</div>\n				<div class="control-group" '),n.push(t.view.txt(2,"div","style",this,function(){var t=[];return t.push('style="'),t.push('margin-left: 0px;"'),t.join("")})),n.push(' id="newpost-form-category">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"category",[{fn:function(s,e){var n=[];return n.push('						<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push('					<div class="select-menu">\n						<select can-value="event.category">\n							<option value=""',t.view.pending({scope:s,options:e}),">"),n.push("Pick a Category</option>"),n.push("\n"),n.push(t.view.txt(0,"select",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"eventCategories"},[{fn:function(s,e){var n=[];return n.push('								<option value="'),n.push(t.view.txt(!0,"option","value",this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"option",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"display_name"}))),n.push("</option>"),n.push("\n"),n.join("")}}]))),n.push("						</select>\n					</div>\n				</div>"),n.push("\n"),n.push(t.view.txt(0,"img",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"event.isEvent"},[{fn:function(s,e){var n=[];return n.push('					<div class="btn-group control-group date" id="newpost-form-date" '),n.push(t.view.txt(2,"div","style",this,function(){var t=[];return t.push('style="'),t.push('margin-left: 0px;"'),t.join("")})),n.push('>\n						<div class="input-append date newpost-datepicker" '),n.push(t.view.txt(1,"div",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"datepicker"}))),n.push("",t.view.pending({scope:s,options:e}),">"),n.push('\n							<input class="span2" type="text" can-value="event.date" readonly>\n							<span class="add-on"><i class="icon-calendar"></i></span>\n						</div>\n					</div>\n					<div class="btn-group control-group time" id="newpost-form-time" '),n.push(t.view.txt(2,"div","style",this,function(){var t=[];return t.push('style="'),t.push('margin-left: 0px;"'),t.join("")})),n.push('>\n						<div class="input-append time newpost-timepicker">\n							<input class="span2 event-time" type="text" placeholder="" value="'),n.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:s,options:e},null,{get:"event.time"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push('\n							<span class="add-on"><i class="icon-time"></i></span>\n						</div>\n					</div>'),n.push("\n"),n.join("")}}]))),n.push('			</div>\n			<div class="span right">\n				<div class="control-group title" id="newpost-form-title">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"title",[{fn:function(s,e){var n=[];return n.push('						<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push('					<input can-value="event.title" type="text" class="title span3" data-toggle="toggle" placeholder="Title:"/>\n				</div>\n				<div class="control-group url" id="newpost-form-url">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"url",[{fn:function(s,e){var n=[];return n.push('						<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push('					<input can-value="event.url" type="text" class="url span3" placeholder="URL:"/>\n				</div>'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"event.isEvent"},[{fn:function(s,e){var n=[];return n.push('					<div class="control-group location" id="newpost-form-location">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"location",[{fn:function(s,e){var n=[];return n.push('							<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push('						<input can-value="event.location" type="text" class="url span3" placeholder="Location:"/>\n					</div>'),n.push("\n"),n.join("")}}]))),n.push('				<div class="control-group body" id="newpost-form-body">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"errorsFor"},"body",[{fn:function(s,e){var n=[];return n.push('						<p class="text-error">'),n.push(t.view.txt(0,"p",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push("</p>"),n.push("\n"),n.join("")}}]))),n.push("					"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"event.isNew"},[{fn:function(s,e){var n=[];return n.push('\n						<textarea can-value="event.body" rows="6" class="body span5" placeholder="Write something:"',t.view.pending({scope:s,options:e}),">"),n.push("</textarea>\n					"),n.join("")}},{inverse:function(s,e){var n=[];return n.push('\n						<textarea can-value="event.source_body" rows="6" class="body span5" placeholder="Write something:"',t.view.pending({scope:s,options:e}),">"),n.push("</textarea>"),n.push("\n"),n.join("")}}]))),n.push("				</div>"),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"currentUser.isAdmin"},[{fn:function(s,e){var n=[];return n.push("					"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"unless"},{get:"event.isNew"},[{fn:function(s,e){var n=[];return n.push('\n						<div class="control-group clearfix tag-control-group">\n							<div class="available-tags">\n								<h5>Available Tags</h5>\n								<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"availableTags"},[{fn:function(s,e){var n=[];return n.push('										<li can-click="addTag">'),n.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push('<span class="tag-icon">&#9656;</span></li>'),n.push("\n"),n.join("")}}]))),n.push('								</ul>\n							</div>\n							<div class="event-tags">\n								<h5>Event Tags</h5>\n								<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"eventTags"},[{fn:function(s,e){var n=[];return n.push('										<li can-click="removeTag">'),n.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"."}))),n.push('<span class="tag-icon">&times;</span></li>'),n.push("\n"),n.join("")}}]))),n.push("								</ul>\n							</div>\n						</div>"),n.push("\n"),n.join("")}}]))),n.push('					<div class="control-group post-as" id="newpost-form-post-as">\n						<label '),n.push(t.view.txt(2,"label","style",this,function(){var t=[];return t.push('style="'),t.push('float: left; width: 50px;"'),t.join("")})),n.push('>\n							<input type="radio" can-value="event.origin_author_feed" class="origin-feed" value="github"/>GH\n						</label>\n						<label '),n.push(t.view.txt(2,"label","style",this,function(){var t=[];return t.push('style="'),t.push('float: left; width: 50px;"'),t.join("")})),n.push('>\n							<input type="radio" can-value="event.origin_author_feed" class="origin-feed" value="twitter"/>TW\n						</label>\n						<input name="postas" type="text" autocomplete="off" class="title span1 typeahead" data-toggle="toggle" placeholder="'),n.push(t.view.txt(!0,"input","placeholder",this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"event.isNew"},[{fn:function(){var t=[];return t.push("Post as"),t.join("")}},{inverse:function(){var t=[];return t.push("Transfer ownership"),t.join("")}}]))),n.push(':" '),n.push(t.view.txt(1,"input",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"typeahead"}))),n.push("",t.view.pending({scope:s,options:e}),"/>"),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"postAsAvatar"},[{fn:function(s,e){var n=[];return n.push('							<img class="postas avatar float-right" '),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push('"'),n.join("")})),n.push(' alt="" ',t.view.pending({scope:s,options:e}),"/>"),n.push("\n"),n.join("")}}]))),n.push("					</div>"),n.push("\n"),n.join("")}}]))),n.push('				<div class="control-group">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"isLoading"},[{fn:function(){var s=[];return s.push('						<div class="spinner-top">\n							<img '),s.push(t.view.txt(2,"img","src",this,function(){var t=[];return t.push('src="'),t.push('/assets/images/ajax-loader-sm-blue.gif"'),t.join("")})),s.push(' alt="loading...">\n						</div>\n					'),s.join("")}},{inverse:function(){var t=[];return t.push('\n						<button id="newpost-form-submit" type="submit" class="btn-bigass light">\n							Submit\n						</button>'),t.push("\n"),t.join("")}}]))),n.push("				</div>\n			</div>\n		</div>\n	</fieldset>\n</form>\n"),n.join("")}))});
/*vendor/moment/moment.src*/
(function(t){function e(t,e){

return r+i}function f(t){return t?ie[t]||t.toLowerCase().replace(/(.)s$/,"$1"):t}function l(t,e){return e.abbr=t,x[t]||(x[t]=new s),x[t].set(e),x[t]}function _(t){if(!t)return H.fn._lang;if(!x[t]&&A)try{require("./lang/"+t)}catch(e){return H.fn._lang}return x[t]}function m(t){return t.match(/\[.*\]/)?t.replace(/^\[|\]$/g,""):t.replace(/\\/g,"")}

null==e&&(n._isValid=!1)}function Y(t){var e,n,s=[];if(!t._d){for(e=0;7>e;e++)t._a[e]=s[e]=null==t._a[e]?2===e?1:0:t._a[e];s[3]+=~~((t._tzm||0)/60),s[4]+=~~((t._tzm||0)%60),

for(;oe.length;)P=oe.pop(),ue[P+P]=e(ue[P],2);for(ue.DDDD=e(ue.DDD,3),s.prototype={

var t=this.year();return t%4===0&&t%100!==0||t%400===0},isDST:function(){return this.zone()<this.clone().month(0).zone()||this.zone()<this.clone().month(5).zone()},day:function(t){var e=this._isUTC?this._d.getUTCDay():this._d.getDay();return null!=t?"string"==typeof t&&(t=this.lang().weekdaysParse(t),"number"!=typeof t)?this:this.add({d:t-e}):e},month:function(t){var e,n=this._isUTC?"UTC":"";return null!=t?"string"==typeof t&&(t=this.lang().monthsParse(t),"number"!=typeof t)?this:(

return t+n}}),A&&(module.exports=H),"undefined"==typeof ender&&(this.moment=H),"function"==typeof define&&define.amd&&define("moment",[],function(){return H})}).call(this);
/*vendor/moment/moment*/
define("vendor/moment/moment",["vendor/moment/moment.src"]);
/*can/map/validations/validations*/
define("can/map/validations/validations",["can/util/util","can/map/map"],function(t){var n=function(n,i,e){if(e||(e=i,i={}),i=i||{},n="string"==typeof n?[n]:t.makeArray(n),!i.testIf||i.testIf.call(this)){var a=this;t.each(n,function(t){a.validations[t]||(a.validations[t]=[]),a.validations[t].push(function(n){var a=e.call(this,n,t);return void 0===a?void 0:i.message||a})})}},i=t.Map.prototype.__set;return t.Map.prototype.__set=function(n,e,a,o,s){var r=this,l=r.constructor.validations,u=function(i){var e=s&&s.call(r,i);return e!==!1&&t.trigger(r,"error",[n,i],!0),!1};if(i.call(r,n,e,a,o,u),l&&l[n]){var c=r.errors(n);c&&u(c)}return this},t.each([t.Map,t.Model],function(i){if(void 0!==i){var e=i.setup;t.extend(i,{setup:function(t){e.apply(this,arguments),this.validations&&t.validations!==this.validations||(this.validations={})},validate:n,validationMessages:{format:"is invalid",inclusion:"is not a valid option (perhaps out of range)",lengthShort:"is too short",lengthLong:"is too long",presence:"can't be empty",range:"is out of range",numericality:"must be a number"},validateFormatOf:function(t,i,e){n.call(this,t,e,function(t){return"undefined"!=typeof t&&null!==t&&""!==t&&null===String(t).match(i)?this.constructor.validationMessages.format:void 0})},validateInclusionOf:function(t,i,e){n.call(this,t,e,function(t){if("undefined"!=typeof t){for(var n=0;n<i.length;n++)if(i[n]===t)return;return this.constructor.validationMessages.inclusion}})},validateLengthOf:function(t,i,e,a){n.call(this,t,a,function(t){return("undefined"==typeof t||null===t)&&i>0||"undefined"!=typeof t&&null!==t&&t.length<i?this.constructor.validationMessages.lengthShort+" (min="+i+")":"undefined"!=typeof t&&null!==t&&t.length>e?this.constructor.validationMessages.lengthLong+" (max="+e+")":void 0})},validatePresenceOf:function(t,i){n.call(this,t,i,function(t){return"undefined"==typeof t||""===t||null===t?this.constructor.validationMessages.presence:void 0})},validateRangeOf:function(t,i,e,a){n.call(this,t,a,function(t){return("undefined"==typeof t||null===t)&&i>0||"undefined"!=typeof t&&null!==t&&(i>t||t>e)?this.constructor.validationMessages.range+" ["+i+","+e+"]":void 0})},validatesNumericalityOf:function(t){n.call(this,t,function(t){var n=!isNaN(parseFloat(t))&&isFinite(t);return n?void 0:this.constructor.validationMessages.numericality})}})}}),t.extend(t.Map.prototype,{errors:function(n,i){n&&(n=t.isArray(n)?n:[n]);var e={},a=this,o=function(n,o){t.each(o,function(t){var o=t.call(a,r?a.__convert?a.__convert(n,i):i:a.attr(n));o&&(e[n]||(e[n]=[]),e[n].push(o))})},s=this.constructor.validations,r=n&&1===n.length&&2===arguments.length;return t.each(n||s||{},function(t,n){"number"==typeof n&&(n=t,t=s[n]),o(n,t||[])}),t.isEmptyObject(e)?null:r?e[n[0]]:e}}),t.Map});
/*can/map/attributes/attributes*/
define("can/map/attributes/attributes",["can/util/util","can/map/map","can/list/list"],function(t){t.dev.warn("can/map/attributes is a deprecated plugin and will be removed in a future release. can/map/define provides the same functionality in a more complete API."),t.each([t.Map,t.Model],function(e){if(void 0!==e){var n=function(t){return"object"==typeof t&&null!==t&&t};t.extend(e,{attributes:{},convert:{date:function(t){var e=typeof t;return"string"===e?(t=Date.parse(t),isNaN(t)?null:new Date(t)):"number"===e?new Date(t):t},number:function(t){return parseFloat(t)},"boolean":function(t){return"false"!==t&&"0"!==t&&t?!0:!1},"default":function(e,n,r,i){if(t.Map.prototype.isPrototypeOf(i.prototype)&&"function"==typeof i.model&&"function"==typeof i.models)return i[t.isArray(e)?"models":"model"](e);if(t.Map.prototype.isPrototypeOf(i.prototype))return t.isArray(e)&&"function"==typeof i.List?new i.List(e):new i(e);if("function"==typeof i)return i(e,n);var a,o=t.getObject(i),u=window;return i.indexOf(".")>=0&&(a=i.substring(0,i.lastIndexOf(".")),u=t.getObject(a)),"function"==typeof o?o.call(u,e,n):e}},serialize:{"default":function(t){return n(t)&&t.serialize?t.serialize():t},date:function(t){return t&&t.getTime()}}});var r=e.setup;e.setup=function(e,n,i){var a=this;r.call(a,e,n,i),t.each(["attributes"],function(t){a[t]&&e[t]!==a[t]||(a[t]={})}),t.each(["convert","serialize"],function(n){e[n]!==a[n]&&(a[n]=t.extend({},e[n],a[n]))})}}}),t.Map.prototype.__convert=function(t,e){var n,r,i=this.constructor,a=this.__get(t);return i.attributes&&(n=i.attributes[t],r=i.convert[n]||i.convert["default"]),null!==e&&n?r.call(i,e,a,function(){},n):e};var e=t.Map.helpers._serialize;t.Map.helpers._serialize=function(t,n,r){var i=t.constructor,a=i.attributes?i.attributes[n]:0,o=i.serialize?i.serialize[a]:0;return r&&"function"==typeof r.serialize?e.apply(this,arguments):o?o(r,a):e.apply(this,arguments)};var n=t.Map.prototype.serialize;return t.Map.prototype.serialize=function(t){var e=n.apply(this,arguments);return t?e[t]:e},t.Map});
/*can/map/setter/setter*/
define("can/map/setter/setter",["can/util/util","can/map/map"],function(e){e.classize=function(t,a){for(var r=t.split(e.undHash),n=0;n<r.length;n++)r[n]=e.capitalize(r[n]);return r.join(a||"")};var t=e.classize,a=e.Map.prototype,r=a.__set;return a.__set=function(a,n,i,c,l){var s;e.dev.warn("can/map/setter is a deprecated plugin and will be removed in a future release. can/map/define provides the same functionality in a more complete API.");var o=t(a),u="set"+o,p=function(t){clearTimeout(s);var r=l&&l.call(d,t);return r!==!1&&e.trigger(d,"error",[a,t],!0),!1},d=this;return this[u]?(e.batch.start(),n=this[u](n,function(e){r.call(d,a,e,i,c,p),clearTimeout(s)},p),void 0===n?(s=setTimeout(function(){e.dev.warn("can/map/setter.js: Setter "+u+" did not return a value or call the setter callback.")},e.dev.warnTimeout),void e.batch.stop()):(r.call(d,a,n,i,c,p),e.batch.stop(),this)):(r.call(d,a,n,i,c,p),this)},e.Map});
/*bithub/models/event*/
define("bithub/models/event",["can/can","./upvote","./award","vendor/moment/moment","can/list/list","can/map/validations/validations","can/map/attributes/attributes","can/map/setter/setter","can/construct/super/super"],function(t,e,i){var r=function(e,i){return i?t.sub("{prefix}[{key}]",{prefix:i,key:e}):e},n=function(t,e){var i=t.split("/");return i[i.length-1]=e+"_"+i[i.length-1],i.join("/")},a=["title","body","category","project","type","tags","url","location","datetime","origin_author_feed","origin_author_id","origin_author_name","feed","id"],o=t.Model("Bithub.Models.Event",{findAll:"GET /api/v1/events",findOne:"GET /api/v1/events/{id}",create:"POST /api/v1/events",update:"PUT /api/v1/events/{id}",destroy:"DELETE /api/v1/events/{id}",findGreatest:function(){return this.findAll.apply(this,arguments)},findLatest:function(e,i){var r;return t.isFunction(e.thread_updated_date)&&(e.thread_updated_date=e.thread_updated_date(),"undefined"==typeof e.thread_updated_date)?(r=$.Deferred(),i&&r.done(i),r.resolve(o.models({data:[]})),r):this.findAll.apply(this,arguments)},attributes:{children:"Bithub.Models.Event.models"},init:function(){this.validate("title",function(t){return t?void 0:"Please add a title"}),this.validate("body",function(){var e=this.attr(this.isNew()?"body":"source_body"),i=this.attr("category"),r=Bithub.Models.Tag.allowedCategoriesForNewPost;return t.inArray(i,r)>-1&&!e?"Please write something about this":void 0}),this.validate("category",function(t){var e;return t?(e=this.isNew()?"NewPost":"ExistingPost",Bithub.Models.Tag["allowedCategoriesFor"+e].indexOf(t)<0?"Picked category doesn't exist":void 0):"Please choose a category"}),this.validate("project",function(t){return t?Bithub.Models.Tag.allowedProjectsForNewPost.indexOf(t)<0?"Picked project doesn't exist":void 0:"Please choose a project"}),this.validate("url",function(t){return t&&!t.match("^http[s]?:\\/\\/(www\\.)?")?"Invalid URL (don't forget 'http[s]://')":void 0}),this.validate("location",function(){var e=t.trim(location||"");return this.isEvent()&&""===e?"Events should have a location":void 0})},model:function(t){return t.id&&t.props&&t.props.location&&(t.location=t.props.location),t.body&&(t.body=t.body.replace(/<!/g,"&lt;!")),this._super(t)}},{upvote:function(){return new e({event:this}).upvote()},unvote:function(){return new e({event:this}).unvote()},upvoteOrUnvote:function(){var t=CURRENT_USER.hasVotedFor(this)?"unvote":"upvote";this[t]()},award:function(){return new i({event:this}).award()},isPush:function(){return this.attr("tags").indexOf("pull_request")>=0},isPullRequest:function(){return this.attr("tags").indexOf("pull_request")>=0},isPushOrPullReq:function(){return this.isPush()||this.isPullRequest()},isIssue:function(){return this.attr("tags").indexOf("issues")>=0},isAwardable:function(){return this.attr("tags").indexOf("issue_comment")>=0},isEvent:function(){return"event"===this.attr("category")},isOrCanBeAwarded:function(){return this.attr("props.thread_awarded")||this.attr("upvotes")>0},hasAwardValue:function(){return this.attr("props.awarded_value")&&this.attr("props.awarded_value")>=0},authorName:function(){return this.attr("author.name")||this.attr("props.origin_author_name")},serializeToArray:function(t){var e=this.serialize().event,i=[];for(var n in e)e.hasOwnProperty(n)&&i.push({name:r(n,t),value:e[n]});return i},serialize:function(){var t=this._super.apply(this,arguments);if("undefined"!=typeof t.datetime&&(t.scheduled_at=moment(t.datetime).utc().format(),delete t.datetime),"undefined"==typeof t.feed&&(t.feed="bithub"),!this.isNew()){t.body=t.source_body;for(var e in t)-1===a.indexOf(e)&&delete t[e]}return"undefined"==typeof t.origin_author_id&&(delete t.origin_author_feed,delete t.origin_author_name),t.tags=t.tags||[],t.tags.push.apply(t.tags,[t.feed,t.category,t.project]),t.tags=_.uniq(t.tags),{event:t}},setDatetime:function(t){return"string"==typeof t?moment(t).toDate():t},date:function(){return moment(this.attr("datetime")).format("MM/DD/YYYY")},time:function(){return moment(this.attr("datetime")).format("hh:mmA")},awardedValue:function(){for(var t,e=this.attr("children")||[],i=e.attr?e.attr("length"):e.length,r=0;i>r;r++)if(t=e[r].attr("props.awarded_value"))return t;return 0},postImageUrl:function(){var t=this.attr("original_image_url");return t?n(t,"140x110"):null}});return t.Model.List("Bithub.Models.Event.List",{sortByOriginTS:function(){return[].sort.call(this,function(t,e){var i=moment(t.attr("origin_ts")).toDate().getTime(),r=moment(e.attr("origin_ts")).toDate().getTime();return r>i?-1:i>r?1:0})}}),o});
/*bithub/models/tag*/
define("bithub/models/tag",["can/can"],function(e){return e.Model("Bithub.Models.Tag",{findAll:"GET /api/v1/tags",findOne:"GET /api/v1/tags/{id}",create:"POST /api/v1/tags",update:"PUT /api/v1/tags/{id}",destroy:"DELETE /api/v1/tags/{id}",allowedCategoriesForNewPost:["article","app","plugin","event"],allowedCategoriesForExistingPost:["article","app","plugin","event","twitter","bug","feature","question","comment","document_js"],allowedProjectsForNewPost:["canjs","jquerypp","donejs","javascriptmvc","funcunit","stealjs","canui","document_js","testee","documentjs"],allowedProjectsForExistingPost:["canjs","jquerypp","donejs","javascriptmvc","funcunit","stealjs","canui","document_js","testee","documentjs"]},{})});
/*bithub/models/postas_user*/
define("bithub/models/postas_user",["can/can","can/construct/super/super"],function(e){var r=e.Model("Bithub.Models.PostasUser",{findAll:"GET /api/v1/users/{feed}",model:function(e){return e.from=e.screen_name?"twitter":"github",e.username=e.screen_name||e.username||e.login,this._super(e)}},{fullName:function(){var e=[this.username];return this.name&&e.push(this.name),e.join(" / ")},profileImageUrl:function(){var e=function(e){return"https://www.gravatar.com/avatar/"+e+"?s=48"};return"github"===this.from?e(this.gravatar_id):this.profile_image_url}});return r});
/*fileupload-load-image*/
System.define("fileupload-load-image","/*\n * JavaScript Load Image 1.5\n * https://github.com/blueimp/JavaScript-Load-Image\n *\n * Copyright 2011, Sebastian Tschan\n * https://blueimp.net\n *\n * iOS image scaling fixes based on\n * https://github.com/stomita/ios-imagefile-megapixel\n *\n * Licensed under the MIT license:\n * http://www.opensource.org/licenses/MIT\n */\n\n/*jslint nomen: true, bitwise: true */\n/*global window, document, URL, webkitURL, Blob, File, FileReader, define */\n\n(function ($) {\n    'use strict';\n\n    // Loads an image for a given File object.\n    // Invokes the callback with an img or optional canvas\n    // element (if supported by the browser) as parameter:\n    var loadImage = function (file, callback, options) {\n            var img = document.createElement('img'),\n                url,\n                oUrl;\n            img.onerror = callback;\n            img.onload = function () {\n                if (oUrl && !(options && options.noRevoke)) {\n                    loadImage.revokeObjectURL(oUrl);\n                }\n                if (callback) {\n                    callback(loadImage.scale(img, options));\n                }\n            };\n            if (loadImage.isInstanceOf('Blob', file) ||\n                    // Files are also Blob instances, but some browsers\n                    // (Firefox 3.6) support the File API but not Blobs:\n                    loadImage.isInstanceOf('File', file)) {\n                url = oUrl = loadImage.createObjectURL(file);\n                // Store the file type for resize processing:\n                img._type = file.type;\n            } else if (typeof file === 'string') {\n                url = file;\n                if (options && options.crossOrigin) {\n                    img.crossOrigin = options.crossOrigin;\n                }\n            } else {\n                return false;\n            }\n            if (url) {\n                img.src = url;\n                return img;\n            }\n            return loadImage.readFile(file, function (e) {\n                var target = e.target;\n                if (target && target.result) {\n                    img.src = target.result;\n                } else {\n                    if (callback) {\n                        callback(e);\n                    }\n                }\n            });\n        },\n        // The check for URL.revokeObjectURL fixes an issue with Opera 12,\n        // which provides URL.createObjectURL but doesn't properly implement it:\n        urlAPI = (window.createObjectURL && window) ||\n            (window.URL && URL.revokeObjectURL && URL) ||\n            (window.webkitURL && webkitURL);\n\n    loadImage.isInstanceOf = function (type, obj) {\n        // Cross-frame instanceof check\n        return Object.prototype.toString.call(obj) === '[object ' + type + ']';\n    };\n\n    // Detects subsampling in JPEG images:\n    loadImage.detectSubsampling = function (img) {\n        var canvas,\n            context;\n        if (img.width * img.height > 1024 * 1024) { // only consider mexapixel images\n            canvas = document.createElement('canvas');\n            canvas.width = canvas.height = 1;\n            context = canvas.getContext('2d');\n            context.drawImage(img, -img.width + 1, 0);\n            // subsampled image becomes half smaller in rendering size.\n            // check alpha channel value to confirm image is covering edge pixel or not.\n            // if alpha value is 0 image is not covering, hence subsampled.\n            return context.getImageData(0, 0, 1, 1).data[3] === 0;\n        }\n        return false;\n    };\n\n    // Detects vertical squash in JPEG images:\n    loadImage.detectVerticalSquash = function (img, correctedHeight) {\n        var canvas = document.createElement('canvas'),\n            context = canvas.getContext('2d'),\n            data,\n            sy,\n            ey,\n            py,\n            alpha;\n        canvas.width = 1;\n        canvas.height = correctedHeight;\n        context.drawImage(img, 0, 0);\n        data = context.getImageData(0, 0, 1, correctedHeight).data;\n        // search image edge pixel position in case it is squashed vertically:\n        sy = 0;\n        ey = correctedHeight;\n        py = correctedHeight;\n        while (py > sy) {\n            alpha = data[(py - 1) * 4 + 3];\n            if (alpha === 0) {\n                ey = py;\n            } else {\n                sy = py;\n            }\n            py = (ey + sy) >> 1;\n        }\n        return (py / correctedHeight) || 1;\n    };\n\n    // Renders image to canvas while working around iOS image scaling bugs:\n    // https://github.com/blueimp/JavaScript-Load-Image/issues/13\n    loadImage.renderImageToCanvas = function (\n        canvas,\n        img,\n        sourceX,\n        sourceY,\n        sourceWidth,\n        sourceHeight,\n        destX,\n        destY,\n        destWidth,\n        destHeight\n    ) {\n        var context = canvas.getContext('2d'),\n            tmpCanvas = document.createElement('canvas'),\n            tileSize = tmpCanvas.width = tmpCanvas.height = 1024,\n            tmpContext = tmpCanvas.getContext('2d'),\n            vertSquashRatio,\n            tileX,\n            tileY;\n        context.save();\n        if (loadImage.detectSubsampling(img)) {\n            sourceWidth /= 2;\n            sourceHeight /= 2;\n        }\n        vertSquashRatio = loadImage.detectVerticalSquash(img, sourceHeight);\n        destWidth = Math.ceil(tileSize * destWidth / sourceWidth);\n        destHeight = Math.ceil(\n            tileSize * destHeight / sourceHeight / vertSquashRatio\n        );\n        destY = 0;\n        tileY = 0;\n        while (tileY < sourceHeight) {\n            destX = 0;\n            tileX = 0;\n            while (tileX < sourceWidth) {\n                tmpContext.clearRect(0, 0, tileSize, tileSize);\n                tmpContext.drawImage(\n                    img,\n                    sourceX,\n                    sourceY,\n                    sourceWidth,\n                    sourceHeight,\n                    -tileX,\n                    -tileY,\n                    sourceWidth,\n                    sourceHeight\n                );\n                context.drawImage(\n                    tmpCanvas,\n                    0,\n                    0,\n                    tileSize,\n                    tileSize,\n                    destX,\n                    destY,\n                    destWidth,\n                    destHeight\n                );\n                tileX += tileSize;\n                destX += destWidth;\n            }\n            tileY += tileSize;\n            destY += destHeight;\n        }\n        context.restore();\n    };\n\n    // Scales the given image (img or canvas HTML element)\n    // using the given options.\n    // Returns a canvas object if the browser supports canvas\n    // and the canvas or crop option is true or a canvas object\n    // is passed as image, else the scaled image:\n    loadImage.scale = function (img, options) {\n        options = options || {};\n        var canvas = document.createElement('canvas'),\n            useCanvas = img.getContext ||\n                ((options.canvas || options.crop) && canvas.getContext),\n            width = img.width,\n            height = img.height,\n            maxWidth = options.maxWidth,\n            maxHeight = options.maxHeight,\n            sourceWidth = width,\n            sourceHeight = height,\n            sourceX = 0,\n            sourceY = 0,\n            destX = 0,\n            destY = 0,\n            destWidth,\n            destHeight,\n            scale;\n        if (useCanvas && maxWidth && maxHeight && options.crop) {\n            destWidth = maxWidth;\n            destHeight = maxHeight;\n            if (width / height < maxWidth / maxHeight) {\n                sourceHeight = maxHeight * width / maxWidth;\n                sourceY = (height - sourceHeight) / 2;\n            } else {\n                sourceWidth = maxWidth * height / maxHeight;\n                sourceX = (width - sourceWidth) / 2;\n            }\n        } else {\n            destWidth = width;\n            destHeight = height;\n            scale = Math.max(\n                (options.minWidth || destWidth) / destWidth,\n                (options.minHeight || destHeight) / destHeight\n            );\n            if (scale > 1) {\n                destWidth = Math.ceil(destWidth * scale);\n                destHeight = Math.ceil(destHeight * scale);\n            }\n            scale = Math.min(\n                (maxWidth || destWidth) / destWidth,\n                (maxHeight || destHeight) / destHeight\n            );\n            if (scale < 1) {\n                destWidth = Math.ceil(destWidth * scale);\n                destHeight = Math.ceil(destHeight * scale);\n            }\n        }\n        if (useCanvas) {\n            canvas.width = destWidth;\n            canvas.height = destHeight;\n            if (img._type === 'image/jpeg') {\n                loadImage.renderImageToCanvas(\n                    canvas,\n                    img,\n                    sourceX,\n                    sourceY,\n                    sourceWidth,\n                    sourceHeight,\n                    destX,\n                    destY,\n                    destWidth,\n                    destHeight\n                );\n            } else {\n                canvas.getContext('2d').drawImage(\n                    img,\n                    sourceX,\n                    sourceY,\n                    sourceWidth,\n                    sourceHeight,\n                    destX,\n                    destY,\n                    destWidth,\n                    destHeight\n                );\n            }\n            return canvas;\n        }\n        img.width = destWidth;\n        img.height = destHeight;\n        return img;\n    };\n\n    loadImage.createObjectURL = function (file) {\n        return urlAPI ? urlAPI.createObjectURL(file) : false;\n    };\n\n    loadImage.revokeObjectURL = function (url) {\n        return urlAPI ? urlAPI.revokeObjectURL(url) : false;\n    };\n\n    // Loads a given File object via FileReader interface,\n    // invokes the callback with the event object (load or error).\n    // The result can be read via event.target.result:\n    loadImage.readFile = function (file, callback) {\n        if (window.FileReader && FileReader.prototype.readAsDataURL) {\n            var fileReader = new FileReader();\n            fileReader.onload = fileReader.onerror = callback;\n            fileReader.readAsDataURL(file);\n            return fileReader;\n        }\n        return false;\n    };\n\n    if (typeof define === 'function' && define.amd) {\n        define(function () {\n            return loadImage;\n        });\n    } else {\n        $.loadImage = loadImage;\n    }\n}(this));\n");
/*jquery-widget*/
System.define("jquery-widget",'/*\n * jQuery UI Widget 1.10.1+amd\n * https://github.com/blueimp/jQuery-File-Upload\n *\n * Copyright 2013 jQuery Foundation and other contributors\n * Released under the MIT license.\n * http://jquery.org/license\n *\n * http://api.jqueryui.com/jQuery.widget/\n */\n\n(function (factory) {\n    if (typeof define === "function" && define.amd) {\n        // Register as an anonymous AMD module:\n        define(["jquery"], factory);\n    } else {\n        // Browser globals:\n        factory(jQuery);\n    }\n}(function( $, undefined ) {\n\nvar uuid = 0,\n	slice = Array.prototype.slice,\n	_cleanData = $.cleanData;\n$.cleanData = function( elems ) {\n	for ( var i = 0, elem; (elem = elems[i]) != null; i++ ) {\n		try {\n			$( elem ).triggerHandler( "remove" );\n		// http://bugs.jquery.com/ticket/8235\n		} catch( e ) {}\n	}\n	_cleanData( elems );\n};\n\n$.widget = function( name, base, prototype ) {\n	var fullName, existingConstructor, constructor, basePrototype,\n		// proxiedPrototype allows the provided prototype to remain unmodified\n		// so that it can be used as a mixin for multiple widgets (#8876)\n		proxiedPrototype = {},\n		namespace = name.split( "." )[ 0 ];\n\n	name = name.split( "." )[ 1 ];\n	fullName = namespace + "-" + name;\n\n	if ( !prototype ) {\n		prototype = base;\n		base = $.Widget;\n	}\n\n	// create selector for plugin\n	$.expr[ ":" ][ fullName.toLowerCase() ] = function( elem ) {\n		return !!$.data( elem, fullName );\n	};\n\n	$[ namespace ] = $[ namespace ] || {};\n	existingConstructor = $[ namespace ][ name ];\n	constructor = $[ namespace ][ name ] = function( options, element ) {\n		// allow instantiation without "new" keyword\n		if ( !this._createWidget ) {\n			return new constructor( options, element );\n		}\n\n		// allow instantiation without initializing for simple inheritance\n		// must use "new" keyword (the code above always passes args)\n		if ( arguments.length ) {\n			this._createWidget( options, element );\n		}\n	};\n	// extend with the existing constructor to carry over any static properties\n	$.extend( constructor, existingConstructor, {\n		version: prototype.version,\n		// copy the object used to create the prototype in case we need to\n		// redefine the widget later\n		_proto: $.extend( {}, prototype ),\n		// track widgets that inherit from this widget in case this widget is\n		// redefined after a widget inherits from it\n		_childConstructors: []\n	});\n\n	basePrototype = new base();\n	// we need to make the options hash a property directly on the new instance\n	// otherwise we\'ll modify the options hash on the prototype that we\'re\n	// inheriting from\n	basePrototype.options = $.widget.extend( {}, basePrototype.options );\n	$.each( prototype, function( prop, value ) {\n		if ( !$.isFunction( value ) ) {\n			proxiedPrototype[ prop ] = value;\n			return;\n		}\n		proxiedPrototype[ prop ] = (function() {\n			var _super = function() {\n					return base.prototype[ prop ].apply( this, arguments );\n				},\n				_superApply = function( args ) {\n					return base.prototype[ prop ].apply( this, args );\n				};\n			return function() {\n				var __super = this._super,\n					__superApply = this._superApply,\n					returnValue;\n\n				this._super = _super;\n				this._superApply = _superApply;\n\n				returnValue = value.apply( this, arguments );\n\n				this._super = __super;\n				this._superApply = __superApply;\n\n				return returnValue;\n			};\n		})();\n	});\n	constructor.prototype = $.widget.extend( basePrototype, {\n		// TODO: remove support for widgetEventPrefix\n		// always use the name + a colon as the prefix, e.g., draggable:start\n		// don\'t prefix for widgets that aren\'t DOM-based\n		widgetEventPrefix: existingConstructor ? basePrototype.widgetEventPrefix : name\n	}, proxiedPrototype, {\n		constructor: constructor,\n		namespace: namespace,\n		widgetName: name,\n		widgetFullName: fullName\n	});\n\n	// If this widget is being redefined then we need to find all widgets that\n	// are inheriting from it and redefine all of them so that they inherit from\n	// the new version of this widget. We\'re essentially trying to replace one\n	// level in the prototype chain.\n	if ( existingConstructor ) {\n		$.each( existingConstructor._childConstructors, function( i, child ) {\n			var childPrototype = child.prototype;\n\n			// redefine the child widget using the same prototype that was\n			// originally used, but inherit from the new version of the base\n			$.widget( childPrototype.namespace + "." + childPrototype.widgetName, constructor, child._proto );\n		});\n		// remove the list of existing child constructors from the old constructor\n		// so the old child constructors can be garbage collected\n		delete existingConstructor._childConstructors;\n	} else {\n		base._childConstructors.push( constructor );\n	}\n\n	$.widget.bridge( name, constructor );\n};\n\n$.widget.extend = function( target ) {\n	var input = slice.call( arguments, 1 ),\n		inputIndex = 0,\n		inputLength = input.length,\n		key,\n		value;\n	for ( ; inputIndex < inputLength; inputIndex++ ) {\n		for ( key in input[ inputIndex ] ) {\n			value = input[ inputIndex ][ key ];\n			if ( input[ inputIndex ].hasOwnProperty( key ) && value !== undefined ) {\n				// Clone objects\n				if ( $.isPlainObject( value ) ) {\n					target[ key ] = $.isPlainObject( target[ key ] ) ?\n						$.widget.extend( {}, target[ key ], value ) :\n						// Don\'t extend strings, arrays, etc. with objects\n						$.widget.extend( {}, value );\n				// Copy everything else by reference\n				} else {\n					target[ key ] = value;\n				}\n			}\n		}\n	}\n	return target;\n};\n\n$.widget.bridge = function( name, object ) {\n	var fullName = object.prototype.widgetFullName || name;\n	$.fn[ name ] = function( options ) {\n		var isMethodCall = typeof options === "string",\n			args = slice.call( arguments, 1 ),\n			returnValue = this;\n\n		// allow multiple hashes to be passed on init\n		options = !isMethodCall && args.length ?\n			$.widget.extend.apply( null, [ options ].concat(args) ) :\n			options;\n\n		if ( isMethodCall ) {\n			this.each(function() {\n				var methodValue,\n					instance = $.data( this, fullName );\n				if ( !instance ) {\n					return $.error( "cannot call methods on " + name + " prior to initialization; " +\n						"attempted to call method \'" + options + "\'" );\n				}\n				if ( !$.isFunction( instance[options] ) || options.charAt( 0 ) === "_" ) {\n					return $.error( "no such method \'" + options + "\' for " + name + " widget instance" );\n				}\n				methodValue = instance[ options ].apply( instance, args );\n				if ( methodValue !== instance && methodValue !== undefined ) {\n					returnValue = methodValue && methodValue.jquery ?\n						returnValue.pushStack( methodValue.get() ) :\n						methodValue;\n					return false;\n				}\n			});\n		} else {\n			this.each(function() {\n				var instance = $.data( this, fullName );\n				if ( instance ) {\n					instance.option( options || {} )._init();\n				} else {\n					$.data( this, fullName, new object( options, this ) );\n				}\n			});\n		}\n\n		return returnValue;\n	};\n};\n\n$.Widget = function( /* options, element */ ) {};\n$.Widget._childConstructors = [];\n\n$.Widget.prototype = {\n	widgetName: "widget",\n	widgetEventPrefix: "",\n	defaultElement: "<div>",\n	options: {\n		disabled: false,\n\n		// callbacks\n		create: null\n	},\n	_createWidget: function( options, element ) {\n		element = $( element || this.defaultElement || this )[ 0 ];\n		this.element = $( element );\n		this.uuid = uuid++;\n		this.eventNamespace = "." + this.widgetName + this.uuid;\n		this.options = $.widget.extend( {},\n			this.options,\n			this._getCreateOptions(),\n			options );\n\n		this.bindings = $();\n		this.hoverable = $();\n		this.focusable = $();\n\n		if ( element !== this ) {\n			$.data( element, this.widgetFullName, this );\n			this._on( true, this.element, {\n				remove: function( event ) {\n					if ( event.target === element ) {\n						this.destroy();\n					}\n				}\n			});\n			this.document = $( element.style ?\n				// element within the document\n				element.ownerDocument :\n				// element is window or document\n				element.document || element );\n			this.window = $( this.document[0].defaultView || this.document[0].parentWindow );\n		}\n\n		this._create();\n		this._trigger( "create", null, this._getCreateEventData() );\n		this._init();\n	},\n	_getCreateOptions: $.noop,\n	_getCreateEventData: $.noop,\n	_create: $.noop,\n	_init: $.noop,\n\n	destroy: function() {\n		this._destroy();\n		// we can probably remove the unbind calls in 2.0\n		// all event bindings should go through this._on()\n		this.element\n			.unbind( this.eventNamespace )\n			// 1.9 BC for #7810\n			// TODO remove dual storage\n			.removeData( this.widgetName )\n			.removeData( this.widgetFullName )\n			// support: jquery <1.6.3\n			// http://bugs.jquery.com/ticket/9413\n			.removeData( $.camelCase( this.widgetFullName ) );\n		this.widget()\n			.unbind( this.eventNamespace )\n			.removeAttr( "aria-disabled" )\n			.removeClass(\n				this.widgetFullName + "-disabled " +\n				"ui-state-disabled" );\n\n		// clean up events and states\n		this.bindings.unbind( this.eventNamespace );\n		this.hoverable.removeClass( "ui-state-hover" );\n		this.focusable.removeClass( "ui-state-focus" );\n	},\n	_destroy: $.noop,\n\n	widget: function() {\n		return this.element;\n	},\n\n	option: function( key, value ) {\n		var options = key,\n			parts,\n			curOption,\n			i;\n\n		if ( arguments.length === 0 ) {\n			// don\'t return a reference to the internal hash\n			return $.widget.extend( {}, this.options );\n		}\n\n		if ( typeof key === "string" ) {\n			// handle nested keys, e.g., "foo.bar" => { foo: { bar: ___ } }\n			options = {};\n			parts = key.split( "." );\n			key = parts.shift();\n			if ( parts.length ) {\n				curOption = options[ key ] = $.widget.extend( {}, this.options[ key ] );\n				for ( i = 0; i < parts.length - 1; i++ ) {\n					curOption[ parts[ i ] ] = curOption[ parts[ i ] ] || {};\n					curOption = curOption[ parts[ i ] ];\n				}\n				key = parts.pop();\n				if ( value === undefined ) {\n					return curOption[ key ] === undefined ? null : curOption[ key ];\n				}\n				curOption[ key ] = value;\n			} else {\n				if ( value === undefined ) {\n					return this.options[ key ] === undefined ? null : this.options[ key ];\n				}\n				options[ key ] = value;\n			}\n		}\n\n		this._setOptions( options );\n\n		return this;\n	},\n	_setOptions: function( options ) {\n		var key;\n\n		for ( key in options ) {\n			this._setOption( key, options[ key ] );\n		}\n\n		return this;\n	},\n	_setOption: function( key, value ) {\n		this.options[ key ] = value;\n\n		if ( key === "disabled" ) {\n			this.widget()\n				.toggleClass( this.widgetFullName + "-disabled ui-state-disabled", !!value )\n				.attr( "aria-disabled", value );\n			this.hoverable.removeClass( "ui-state-hover" );\n			this.focusable.removeClass( "ui-state-focus" );\n		}\n\n		return this;\n	},\n\n	enable: function() {\n		return this._setOption( "disabled", false );\n	},\n	disable: function() {\n		return this._setOption( "disabled", true );\n	},\n\n	_on: function( suppressDisabledCheck, element, handlers ) {\n		var delegateElement,\n			instance = this;\n\n		// no suppressDisabledCheck flag, shuffle arguments\n		if ( typeof suppressDisabledCheck !== "boolean" ) {\n			handlers = element;\n			element = suppressDisabledCheck;\n			suppressDisabledCheck = false;\n		}\n\n		// no element argument, shuffle and use this.element\n		if ( !handlers ) {\n			handlers = element;\n			element = this.element;\n			delegateElement = this.widget();\n		} else {\n			// accept selectors, DOM elements\n			element = delegateElement = $( element );\n			this.bindings = this.bindings.add( element );\n		}\n\n		$.each( handlers, function( event, handler ) {\n			function handlerProxy() {\n				// allow widgets to customize the disabled handling\n				// - disabled as an array instead of boolean\n				// - disabled class as method for disabling individual parts\n				if ( !suppressDisabledCheck &&\n						( instance.options.disabled === true ||\n							$( this ).hasClass( "ui-state-disabled" ) ) ) {\n					return;\n				}\n				return ( typeof handler === "string" ? instance[ handler ] : handler )\n					.apply( instance, arguments );\n			}\n\n			// copy the guid so direct unbinding works\n			if ( typeof handler !== "string" ) {\n				handlerProxy.guid = handler.guid =\n					handler.guid || handlerProxy.guid || $.guid++;\n			}\n\n			var match = event.match( /^(\\w+)\\s*(.*)$/ ),\n				eventName = match[1] + instance.eventNamespace,\n				selector = match[2];\n			if ( selector ) {\n				delegateElement.delegate( selector, eventName, handlerProxy );\n			} else {\n				element.bind( eventName, handlerProxy );\n			}\n		});\n	},\n\n	_off: function( element, eventName ) {\n		eventName = (eventName || "").split( " " ).join( this.eventNamespace + " " ) + this.eventNamespace;\n		element.unbind( eventName ).undelegate( eventName );\n	},\n\n	_delay: function( handler, delay ) {\n		function handlerProxy() {\n			return ( typeof handler === "string" ? instance[ handler ] : handler )\n				.apply( instance, arguments );\n		}\n		var instance = this;\n		return setTimeout( handlerProxy, delay || 0 );\n	},\n\n	_hoverable: function( element ) {\n		this.hoverable = this.hoverable.add( element );\n		this._on( element, {\n			mouseenter: function( event ) {\n				$( event.currentTarget ).addClass( "ui-state-hover" );\n			},\n			mouseleave: function( event ) {\n				$( event.currentTarget ).removeClass( "ui-state-hover" );\n			}\n		});\n	},\n\n	_focusable: function( element ) {\n		this.focusable = this.focusable.add( element );\n		this._on( element, {\n			focusin: function( event ) {\n				$( event.currentTarget ).addClass( "ui-state-focus" );\n			},\n			focusout: function( event ) {\n				$( event.currentTarget ).removeClass( "ui-state-focus" );\n			}\n		});\n	},\n\n	_trigger: function( type, event, data ) {\n		var prop, orig,\n			callback = this.options[ type ];\n\n		data = data || {};\n		event = $.Event( event );\n		event.type = ( type === this.widgetEventPrefix ?\n			type :\n			this.widgetEventPrefix + type ).toLowerCase();\n		// the original event may come from any element\n		// so we need to reset the target on the new event\n		event.target = this.element[ 0 ];\n\n		// copy original event properties over to the new event\n		orig = event.originalEvent;\n		if ( orig ) {\n			for ( prop in orig ) {\n				if ( !( prop in event ) ) {\n					event[ prop ] = orig[ prop ];\n				}\n			}\n		}\n\n		this.element.trigger( event, data );\n		return !( $.isFunction( callback ) &&\n			callback.apply( this.element[0], [ event ].concat( data ) ) === false ||\n			event.isDefaultPrevented() );\n	}\n};\n\n$.each( { show: "fadeIn", hide: "fadeOut" }, function( method, defaultEffect ) {\n	$.Widget.prototype[ "_" + method ] = function( element, options, callback ) {\n		if ( typeof options === "string" ) {\n			options = { effect: options };\n		}\n		var hasOptions,\n			effectName = !options ?\n				method :\n				options === true || typeof options === "number" ?\n					defaultEffect :\n					options.effect || defaultEffect;\n		options = options || {};\n		if ( typeof options === "number" ) {\n			options = { duration: options };\n		}\n		hasOptions = !$.isEmptyObject( options );\n		options.complete = callback;\n		if ( options.delay ) {\n			element.delay( options.delay );\n		}\n		if ( hasOptions && $.effects && $.effects.effect[ effectName ] ) {\n			element[ method ]( options );\n		} else if ( effectName !== method && element[ effectName ] ) {\n			element[ effectName ]( options.duration, options.easing, callback );\n		} else {\n			element.queue(function( next ) {\n				$( this )[ method ]();\n				if ( callback ) {\n					callback.call( element[ 0 ] );\n				}\n				next();\n			});\n		}\n	};\n});\n\n}));\n');
/*fileupload*/
System.define("fileupload","/*\n * jQuery File Upload Plugin 5.26\n * https://github.com/blueimp/jQuery-File-Upload\n *\n * Copyright 2010, Sebastian Tschan\n * https://blueimp.net\n *\n * Licensed under the MIT license:\n * http://www.opensource.org/licenses/MIT\n */\n\n/*jslint nomen: true, unparam: true, regexp: true */\n/*global define, window, document, File, Blob, FormData, location */\n\n(function (factory) {\n    'use strict';\n    if (typeof define === 'function' && define.amd) {\n        // Register as an anonymous AMD module:\n        define([\n            'jquery',\n            'jquery.ui.widget'\n        ], factory);\n    } else {\n        // Browser globals:\n        factory(window.jQuery);\n    }\n}(function ($) {\n    'use strict';\n\n    // The FileReader API is not actually used, but works as feature detection,\n    // as e.g. Safari supports XHR file uploads via the FormData API,\n    // but not non-multipart XHR file uploads:\n    $.support.xhrFileUpload = !!(window.XMLHttpRequestUpload && window.FileReader);\n    $.support.xhrFormDataFileUpload = !!window.FormData;\n\n    // The fileupload widget listens for change events on file input fields defined\n    // via fileInput setting and paste or drop events of the given dropZone.\n    // In addition to the default jQuery Widget methods, the fileupload widget\n    // exposes the \"add\" and \"send\" methods, to add or directly send files using\n    // the fileupload API.\n    // By default, files added via file input selection, paste, drag & drop or\n    // \"add\" method are uploaded immediately, but it is possible to override\n    // the \"add\" callback option to queue file uploads.\n    $.widget('blueimp.fileupload', {\n\n        options: {\n            // The drop target element(s), by the default the complete document.\n            // Set to null to disable drag & drop support:\n            dropZone: $(document),\n            // The paste target element(s), by the default the complete document.\n            // Set to null to disable paste support:\n            pasteZone: $(document),\n            // The file input field(s), that are listened to for change events.\n            // If undefined, it is set to the file input fields inside\n            // of the widget element on plugin initialization.\n            // Set to null to disable the change listener.\n            fileInput: undefined,\n            // By default, the file input field is replaced with a clone after\n            // each input field change event. This is required for iframe transport\n            // queues and allows change events to be fired for the same file\n            // selection, but can be disabled by setting the following option to false:\n            replaceFileInput: true,\n            // The parameter name for the file form data (the request argument name).\n            // If undefined or empty, the name property of the file input field is\n            // used, or \"files[]\" if the file input name property is also empty,\n            // can be a string or an array of strings:\n            paramName: undefined,\n            // By default, each file of a selection is uploaded using an individual\n            // request for XHR type uploads. Set to false to upload file\n            // selections in one request each:\n            singleFileUploads: true,\n            // To limit the number of files uploaded with one XHR request,\n            // set the following option to an integer greater than 0:\n            limitMultiFileUploads: undefined,\n            // Set the following option to true to issue all file upload requests\n            // in a sequential order:\n            sequentialUploads: false,\n            // To limit the number of concurrent uploads,\n            // set the following option to an integer greater than 0:\n            limitConcurrentUploads: undefined,\n            // Set the following option to true to force iframe transport uploads:\n            forceIframeTransport: false,\n            // Set the following option to the location of a redirect url on the\n            // origin server, for cross-domain iframe transport uploads:\n            redirect: undefined,\n            // The parameter name for the redirect url, sent as part of the form\n            // data and set to 'redirect' if this option is empty:\n            redirectParamName: undefined,\n            // Set the following option to the location of a postMessage window,\n            // to enable postMessage transport uploads:\n            postMessage: undefined,\n            // By default, XHR file uploads are sent as multipart/form-data.\n            // The iframe transport is always using multipart/form-data.\n            // Set to false to enable non-multipart XHR uploads:\n            multipart: true,\n            // To upload large files in smaller chunks, set the following option\n            // to a preferred maximum chunk size. If set to 0, null or undefined,\n            // or the browser does not support the required Blob API, files will\n            // be uploaded as a whole.\n            maxChunkSize: undefined,\n            // When a non-multipart upload or a chunked multipart upload has been\n            // aborted, this option can be used to resume the upload by setting\n            // it to the size of the already uploaded bytes. This option is most\n            // useful when modifying the options object inside of the \"add\" or\n            // \"send\" callbacks, as the options are cloned for each file upload.\n            uploadedBytes: undefined,\n            // By default, failed (abort or error) file uploads are removed from the\n            // global progress calculation. Set the following option to false to\n            // prevent recalculating the global progress data:\n            recalculateProgress: true,\n            // Interval in milliseconds to calculate and trigger progress events:\n            progressInterval: 100,\n            // Interval in milliseconds to calculate progress bitrate:\n            bitrateInterval: 500,\n            // By default, uploads are started automatically when adding files:\n            autoUpload: true,\n\n            // Additional form data to be sent along with the file uploads can be set\n            // using this option, which accepts an array of objects with name and\n            // value properties, a function returning such an array, a FormData\n            // object (for XHR file uploads), or a simple object.\n            // The form of the first fileInput is given as parameter to the function:\n            formData: function (form) {\n                return form.serializeArray();\n            },\n\n            // The add callback is invoked as soon as files are added to the fileupload\n            // widget (via file input selection, drag & drop, paste or add API call).\n            // If the singleFileUploads option is enabled, this callback will be\n            // called once for each file in the selection for XHR file uplaods, else\n            // once for each file selection.\n            // The upload starts when the submit method is invoked on the data parameter.\n            // The data object contains a files property holding the added files\n            // and allows to override plugin options as well as define ajax settings.\n            // Listeners for this callback can also be bound the following way:\n            // .bind('fileuploadadd', func);\n            // data.submit() returns a Promise object and allows to attach additional\n            // handlers using jQuery's Deferred callbacks:\n            // data.submit().done(func).fail(func).always(func);\n            add: function (e, data) {\n                if (data.autoUpload || (data.autoUpload !== false &&\n                        ($(this).data('blueimp-fileupload') ||\n                        $(this).data('fileupload')).options.autoUpload)) {\n                    data.submit();\n                }\n            },\n\n            // Other callbacks:\n\n            // Callback for the submit event of each file upload:\n            // submit: function (e, data) {}, // .bind('fileuploadsubmit', func);\n\n            // Callback for the start of each file upload request:\n            // send: function (e, data) {}, // .bind('fileuploadsend', func);\n\n            // Callback for successful uploads:\n            // done: function (e, data) {}, // .bind('fileuploaddone', func);\n\n            // Callback for failed (abort or error) uploads:\n            // fail: function (e, data) {}, // .bind('fileuploadfail', func);\n\n            // Callback for completed (success, abort or error) requests:\n            // always: function (e, data) {}, // .bind('fileuploadalways', func);\n\n            // Callback for upload progress events:\n            // progress: function (e, data) {}, // .bind('fileuploadprogress', func);\n\n            // Callback for global upload progress events:\n            // progressall: function (e, data) {}, // .bind('fileuploadprogressall', func);\n\n            // Callback for uploads start, equivalent to the global ajaxStart event:\n            // start: function (e) {}, // .bind('fileuploadstart', func);\n\n            // Callback for uploads stop, equivalent to the global ajaxStop event:\n            // stop: function (e) {}, // .bind('fileuploadstop', func);\n\n            // Callback for change events of the fileInput(s):\n            // change: function (e, data) {}, // .bind('fileuploadchange', func);\n\n            // Callback for paste events to the pasteZone(s):\n            // paste: function (e, data) {}, // .bind('fileuploadpaste', func);\n\n            // Callback for drop events of the dropZone(s):\n            // drop: function (e, data) {}, // .bind('fileuploaddrop', func);\n\n            // Callback for dragover events of the dropZone(s):\n            // dragover: function (e) {}, // .bind('fileuploaddragover', func);\n\n            // Callback for the start of each chunk upload request:\n            // chunksend: function (e, data) {}, // .bind('fileuploadchunksend', func);\n\n            // Callback for successful chunk uploads:\n            // chunkdone: function (e, data) {}, // .bind('fileuploadchunkdone', func);\n\n            // Callback for failed (abort or error) chunk uploads:\n            // chunkfail: function (e, data) {}, // .bind('fileuploadchunkfail', func);\n\n            // Callback for completed (success, abort or error) chunk upload requests:\n            // chunkalways: function (e, data) {}, // .bind('fileuploadchunkalways', func);\n\n            // The plugin options are used as settings object for the ajax calls.\n            // The following are jQuery ajax settings required for the file uploads:\n            processData: false,\n            contentType: false,\n            cache: false\n        },\n\n        // A list of options that require a refresh after assigning a new value:\n        _refreshOptionsList: [\n            'fileInput',\n            'dropZone',\n            'pasteZone',\n            'multipart',\n            'forceIframeTransport'\n        ],\n\n        _BitrateTimer: function () {\n            this.timestamp = +(new Date());\n            this.loaded = 0;\n            this.bitrate = 0;\n            this.getBitrate = function (now, loaded, interval) {\n                var timeDiff = now - this.timestamp;\n                if (!this.bitrate || !interval || timeDiff > interval) {\n                    this.bitrate = (loaded - this.loaded) * (1000 / timeDiff) * 8;\n                    this.loaded = loaded;\n                    this.timestamp = now;\n                }\n                return this.bitrate;\n            };\n        },\n\n        _isXHRUpload: function (options) {\n            return !options.forceIframeTransport &&\n                ((!options.multipart && $.support.xhrFileUpload) ||\n                $.support.xhrFormDataFileUpload);\n        },\n\n        _getFormData: function (options) {\n            var formData;\n            if (typeof options.formData === 'function') {\n                return options.formData(options.form);\n            }\n            if ($.isArray(options.formData)) {\n                return options.formData;\n            }\n            if (options.formData) {\n                formData = [];\n                $.each(options.formData, function (name, value) {\n                    formData.push({name: name, value: value});\n                });\n                return formData;\n            }\n            return [];\n        },\n\n        _getTotal: function (files) {\n            var total = 0;\n            $.each(files, function (index, file) {\n                total += file.size || 1;\n            });\n            return total;\n        },\n\n        _initProgressObject: function (obj) {\n            obj._progress = {\n                loaded: 0,\n                total: 0,\n                bitrate: 0\n            };\n        },\n\n        _onProgress: function (e, data) {\n            if (e.lengthComputable) {\n                var now = +(new Date()),\n                    loaded;\n                if (data._time && data.progressInterval &&\n                        (now - data._time < data.progressInterval) &&\n                        e.loaded !== e.total) {\n                    return;\n                }\n                data._time = now;\n                loaded = Math.floor(\n                    e.loaded / e.total * (data.chunkSize || data._progress.total)\n                ) + (data.uploadedBytes || 0);\n                // Add the difference from the previously loaded state\n                // to the global loaded counter:\n                this._progress.loaded += (loaded - data._progress.loaded);\n                this._progress.bitrate = this._bitrateTimer.getBitrate(\n                    now,\n                    this._progress.loaded,\n                    data.bitrateInterval\n                );\n                data._progress.loaded = data.loaded = loaded;\n                data._progress.bitrate = data.bitrate = data._bitrateTimer.getBitrate(\n                    now,\n                    loaded,\n                    data.bitrateInterval\n                );\n                // Trigger a custom progress event with a total data property set\n                // to the file size(s) of the current upload and a loaded data\n                // property calculated accordingly:\n                this._trigger('progress', e, data);\n                // Trigger a global progress event for all current file uploads,\n                // including ajax calls queued for sequential file uploads:\n                this._trigger('progressall', e, this._progress);\n            }\n        },\n\n        _initProgressListener: function (options) {\n            var that = this,\n                xhr = options.xhr ? options.xhr() : $.ajaxSettings.xhr();\n            // Accesss to the native XHR object is required to add event listeners\n            // for the upload progress event:\n            if (xhr.upload) {\n                $(xhr.upload).bind('progress', function (e) {\n                    var oe = e.originalEvent;\n                    // Make sure the progress event properties get copied over:\n                    e.lengthComputable = oe.lengthComputable;\n                    e.loaded = oe.loaded;\n                    e.total = oe.total;\n                    that._onProgress(e, options);\n                });\n                options.xhr = function () {\n                    return xhr;\n                };\n            }\n        },\n\n        _initXHRData: function (options) {\n            var formData,\n                file = options.files[0],\n                // Ignore non-multipart setting if not supported:\n                multipart = options.multipart || !$.support.xhrFileUpload,\n                paramName = options.paramName[0];\n            options.headers = options.headers || {};\n            if (options.contentRange) {\n                options.headers['Content-Range'] = options.contentRange;\n            }\n            if (!multipart) {\n                options.headers['Content-Disposition'] = 'attachment; filename=\"' +\n                    encodeURI(file.name) + '\"';\n                options.contentType = file.type;\n                options.data = options.blob || file;\n            } else if ($.support.xhrFormDataFileUpload) {\n                if (options.postMessage) {\n                    // window.postMessage does not allow sending FormData\n                    // objects, so we just add the File/Blob objects to\n                    // the formData array and let the postMessage window\n                    // create the FormData object out of this array:\n                    formData = this._getFormData(options);\n                    if (options.blob) {\n                        formData.push({\n                            name: paramName,\n                            value: options.blob\n                        });\n                    } else {\n                        $.each(options.files, function (index, file) {\n                            formData.push({\n                                name: options.paramName[index] || paramName,\n                                value: file\n                            });\n                        });\n                    }\n                } else {\n                    if (options.formData instanceof FormData) {\n                        formData = options.formData;\n                    } else {\n                        formData = new FormData();\n                        $.each(this._getFormData(options), function (index, field) {\n                            formData.append(field.name, field.value);\n                        });\n                    }\n                    if (options.blob) {\n                        options.headers['Content-Disposition'] = 'attachment; filename=\"' +\n                            encodeURI(file.name) + '\"';\n                        formData.append(paramName, options.blob, file.name);\n                    } else {\n                        $.each(options.files, function (index, file) {\n                            // Files are also Blob instances, but some browsers\n                            // (Firefox 3.6) support the File API but not Blobs.\n                            // This check allows the tests to run with\n                            // dummy objects:\n                            if ((window.Blob && file instanceof Blob) ||\n                                    (window.File && file instanceof File)) {\n                                formData.append(\n                                    options.paramName[index] || paramName,\n                                    file,\n                                    file.name\n                                );\n                            }\n                        });\n                    }\n                }\n                options.data = formData;\n            }\n            // Blob reference is not needed anymore, free memory:\n            options.blob = null;\n        },\n\n        _initIframeSettings: function (options) {\n            // Setting the dataType to iframe enables the iframe transport:\n            options.dataType = 'iframe ' + (options.dataType || '');\n            // The iframe transport accepts a serialized array as form data:\n            options.formData = this._getFormData(options);\n            // Add redirect url to form data on cross-domain uploads:\n            if (options.redirect && $('<a></a>').prop('href', options.url)\n                    .prop('host') !== location.host) {\n                options.formData.push({\n                    name: options.redirectParamName || 'redirect',\n                    value: options.redirect\n                });\n            }\n        },\n\n        _initDataSettings: function (options) {\n            if (this._isXHRUpload(options)) {\n                if (!this._chunkedUpload(options, true)) {\n                    if (!options.data) {\n                        this._initXHRData(options);\n                    }\n                    this._initProgressListener(options);\n                }\n                if (options.postMessage) {\n                    // Setting the dataType to postmessage enables the\n                    // postMessage transport:\n                    options.dataType = 'postmessage ' + (options.dataType || '');\n                }\n            } else {\n                this._initIframeSettings(options, 'iframe');\n            }\n        },\n\n        _getParamName: function (options) {\n            var fileInput = $(options.fileInput),\n                paramName = options.paramName;\n            if (!paramName) {\n                paramName = [];\n                fileInput.each(function () {\n                    var input = $(this),\n                        name = input.prop('name') || 'files[]',\n                        i = (input.prop('files') || [1]).length;\n                    while (i) {\n                        paramName.push(name);\n                        i -= 1;\n                    }\n                });\n                if (!paramName.length) {\n                    paramName = [fileInput.prop('name') || 'files[]'];\n                }\n            } else if (!$.isArray(paramName)) {\n                paramName = [paramName];\n            }\n            return paramName;\n        },\n\n        _initFormSettings: function (options) {\n            // Retrieve missing options from the input field and the\n            // associated form, if available:\n            if (!options.form || !options.form.length) {\n                options.form = $(options.fileInput.prop('form'));\n                // If the given file input doesn't have an associated form,\n                // use the default widget file input's form:\n                if (!options.form.length) {\n                    options.form = $(this.options.fileInput.prop('form'));\n                }\n            }\n            options.paramName = this._getParamName(options);\n            if (!options.url) {\n                options.url = options.form.prop('action') || location.href;\n            }\n            // The HTTP request method must be \"POST\" or \"PUT\":\n            options.type = (options.type || options.form.prop('method') || '')\n                .toUpperCase();\n            if (options.type !== 'POST' && options.type !== 'PUT' &&\n                    options.type !== 'PATCH') {\n                options.type = 'POST';\n            }\n            if (!options.formAcceptCharset) {\n                options.formAcceptCharset = options.form.attr('accept-charset');\n            }\n        },\n\n        _getAJAXSettings: function (data) {\n            var options = $.extend({}, this.options, data);\n            this._initFormSettings(options);\n            this._initDataSettings(options);\n            return options;\n        },\n\n        // jQuery 1.6 doesn't provide .state(),\n        // while jQuery 1.8+ removed .isRejected() and .isResolved():\n        _getDeferredState: function (deferred) {\n            if (deferred.state) {\n                return deferred.state();\n            }\n            if (deferred.isResolved()) {\n                return 'resolved';\n            }\n            if (deferred.isRejected()) {\n                return 'rejected';\n            }\n            return 'pending';\n        },\n\n        // Maps jqXHR callbacks to the equivalent\n        // methods of the given Promise object:\n        _enhancePromise: function (promise) {\n            promise.success = promise.done;\n            promise.error = promise.fail;\n            promise.complete = promise.always;\n            return promise;\n        },\n\n        // Creates and returns a Promise object enhanced with\n        // the jqXHR methods abort, success, error and complete:\n        _getXHRPromise: function (resolveOrReject, context, args) {\n            var dfd = $.Deferred(),\n                promise = dfd.promise();\n            context = context || this.options.context || promise;\n            if (resolveOrReject === true) {\n                dfd.resolveWith(context, args);\n            } else if (resolveOrReject === false) {\n                dfd.rejectWith(context, args);\n            }\n            promise.abort = dfd.promise;\n            return this._enhancePromise(promise);\n        },\n\n        // Adds convenience methods to the callback arguments:\n        _addConvenienceMethods: function (e, data) {\n            var that = this;\n            data.submit = function () {\n                if (this.state() !== 'pending') {\n                    data.jqXHR = this.jqXHR =\n                        (that._trigger('submit', e, this) !== false) &&\n                        that._onSend(e, this);\n                }\n                return this.jqXHR || that._getXHRPromise();\n            };\n            data.abort = function () {\n                if (this.jqXHR) {\n                    return this.jqXHR.abort();\n                }\n                return this._getXHRPromise();\n            };\n            data.state = function () {\n                if (this.jqXHR) {\n                    return that._getDeferredState(this.jqXHR);\n                }\n            };\n            data.progress = function () {\n                return this._progress;\n            };\n        },\n\n        // Parses the Range header from the server response\n        // and returns the uploaded bytes:\n        _getUploadedBytes: function (jqXHR) {\n            var range = jqXHR.getResponseHeader('Range'),\n                parts = range && range.split('-'),\n                upperBytesPos = parts && parts.length > 1 &&\n                    parseInt(parts[1], 10);\n            return upperBytesPos && upperBytesPos + 1;\n        },\n\n        // Uploads a file in multiple, sequential requests\n        // by splitting the file up in multiple blob chunks.\n        // If the second parameter is true, only tests if the file\n        // should be uploaded in chunks, but does not invoke any\n        // upload requests:\n        _chunkedUpload: function (options, testOnly) {\n            var that = this,\n                file = options.files[0],\n                fs = file.size,\n                ub = options.uploadedBytes = options.uploadedBytes || 0,\n                mcs = options.maxChunkSize || fs,\n                slice = file.slice || file.webkitSlice || file.mozSlice,\n                dfd = $.Deferred(),\n                promise = dfd.promise(),\n                jqXHR,\n                upload;\n            if (!(this._isXHRUpload(options) && slice && (ub || mcs < fs)) ||\n                    options.data) {\n                return false;\n            }\n            if (testOnly) {\n                return true;\n            }\n            if (ub >= fs) {\n                file.error = 'Uploaded bytes exceed file size';\n                return this._getXHRPromise(\n                    false,\n                    options.context,\n                    [null, 'error', file.error]\n                );\n            }\n            // The chunk upload method:\n            upload = function () {\n                // Clone the options object for each chunk upload:\n                var o = $.extend({}, options),\n                    currentLoaded = o._progress.loaded;\n                o.blob = slice.call(\n                    file,\n                    ub,\n                    ub + mcs,\n                    file.type\n                );\n                // Store the current chunk size, as the blob itself\n                // will be dereferenced after data processing:\n                o.chunkSize = o.blob.size;\n                // Expose the chunk bytes position range:\n                o.contentRange = 'bytes ' + ub + '-' +\n                    (ub + o.chunkSize - 1) + '/' + fs;\n                // Process the upload data (the blob and potential form data):\n                that._initXHRData(o);\n                // Add progress listeners for this chunk upload:\n                that._initProgressListener(o);\n                jqXHR = ((that._trigger('chunksend', null, o) !== false && $.ajax(o)) ||\n                        that._getXHRPromise(false, o.context))\n                    .done(function (result, textStatus, jqXHR) {\n                        ub = that._getUploadedBytes(jqXHR) ||\n                            (ub + o.chunkSize);\n                        // Create a progress event if no final progress event\n                        // with loaded equaling total has been triggered\n                        // for this chunk:\n                        if (o._progress.loaded === currentLoaded) {\n                            that._onProgress($.Event('progress', {\n                                lengthComputable: true,\n                                loaded: ub - o.uploadedBytes,\n                                total: ub - o.uploadedBytes\n                            }), o);\n                        }\n                        options.uploadedBytes = o.uploadedBytes = ub;\n                        o.result = result;\n                        o.textStatus = textStatus;\n                        o.jqXHR = jqXHR;\n                        that._trigger('chunkdone', null, o);\n                        that._trigger('chunkalways', null, o);\n                        if (ub < fs) {\n                            // File upload not yet complete,\n                            // continue with the next chunk:\n                            upload();\n                        } else {\n                            dfd.resolveWith(\n                                o.context,\n                                [result, textStatus, jqXHR]\n                            );\n                        }\n                    })\n                    .fail(function (jqXHR, textStatus, errorThrown) {\n                        o.jqXHR = jqXHR;\n                        o.textStatus = textStatus;\n                        o.errorThrown = errorThrown;\n                        that._trigger('chunkfail', null, o);\n                        that._trigger('chunkalways', null, o);\n                        dfd.rejectWith(\n                            o.context,\n                            [jqXHR, textStatus, errorThrown]\n                        );\n                    });\n            };\n            this._enhancePromise(promise);\n            promise.abort = function () {\n                return jqXHR.abort();\n            };\n            upload();\n            return promise;\n        },\n\n        _beforeSend: function (e, data) {\n            if (this._active === 0) {\n                // the start callback is triggered when an upload starts\n                // and no other uploads are currently running,\n                // equivalent to the global ajaxStart event:\n                this._trigger('start');\n                // Set timer for global bitrate progress calculation:\n                this._bitrateTimer = new this._BitrateTimer();\n                // Reset the global progress values:\n                this._progress.loaded = this._progress.total = 0;\n                this._progress.bitrate = 0;\n            }\n            if (!data._progress) {\n                data._progress = {};\n            }\n            data._progress.loaded = data.loaded = data.uploadedBytes || 0;\n            data._progress.total = data.total = this._getTotal(data.files) || 1;\n            data._progress.bitrate = data.bitrate = 0;\n            this._active += 1;\n            // Initialize the global progress values:\n            this._progress.loaded += data.loaded;\n            this._progress.total += data.total;\n        },\n\n        _onDone: function (result, textStatus, jqXHR, options) {\n            var total = options._progress.total;\n            if (options._progress.loaded < total) {\n                // Create a progress event if no final progress event\n                // with loaded equaling total has been triggered:\n                this._onProgress($.Event('progress', {\n                    lengthComputable: true,\n                    loaded: total,\n                    total: total\n                }), options);\n            }\n            options.result = result;\n            options.textStatus = textStatus;\n            options.jqXHR = jqXHR;\n            this._trigger('done', null, options);\n        },\n\n        _onFail: function (jqXHR, textStatus, errorThrown, options) {\n            options.jqXHR = jqXHR;\n            options.textStatus = textStatus;\n            options.errorThrown = errorThrown;\n            this._trigger('fail', null, options);\n            if (options.recalculateProgress) {\n                // Remove the failed (error or abort) file upload from\n                // the global progress calculation:\n                this._progress.loaded -= options._progress.loaded;\n                this._progress.total -= options._progress.total;\n            }\n        },\n\n        _onAlways: function (jqXHRorResult, textStatus, jqXHRorError, options) {\n            // jqXHRorResult, textStatus and jqXHRorError are added to the\n            // options object via done and fail callbacks\n            this._active -= 1;\n            this._trigger('always', null, options);\n            if (this._active === 0) {\n                // The stop callback is triggered when all uploads have\n                // been completed, equivalent to the global ajaxStop event:\n                this._trigger('stop');\n            }\n        },\n\n        _onSend: function (e, data) {\n            if (!data.submit) {\n                this._addConvenienceMethods(e, data);\n            }\n            var that = this,\n                jqXHR,\n                aborted,\n                slot,\n                pipe,\n                options = that._getAJAXSettings(data),\n                send = function () {\n                    that._sending += 1;\n                    // Set timer for bitrate progress calculation:\n                    options._bitrateTimer = new that._BitrateTimer();\n                    jqXHR = jqXHR || (\n                        ((aborted || that._trigger('send', e, options) === false) &&\n                        that._getXHRPromise(false, options.context, aborted)) ||\n                        that._chunkedUpload(options) || $.ajax(options)\n                    ).done(function (result, textStatus, jqXHR) {\n                        that._onDone(result, textStatus, jqXHR, options);\n                    }).fail(function (jqXHR, textStatus, errorThrown) {\n                        that._onFail(jqXHR, textStatus, errorThrown, options);\n                    }).always(function (jqXHRorResult, textStatus, jqXHRorError) {\n                        that._sending -= 1;\n                        that._onAlways(\n                            jqXHRorResult,\n                            textStatus,\n                            jqXHRorError,\n                            options\n                        );\n                        if (options.limitConcurrentUploads &&\n                                options.limitConcurrentUploads > that._sending) {\n                            // Start the next queued upload,\n                            // that has not been aborted:\n                            var nextSlot = that._slots.shift();\n                            while (nextSlot) {\n                                if (that._getDeferredState(nextSlot) === 'pending') {\n                                    nextSlot.resolve();\n                                    break;\n                                }\n                                nextSlot = that._slots.shift();\n                            }\n                        }\n                    });\n                    return jqXHR;\n                };\n            this._beforeSend(e, options);\n            if (this.options.sequentialUploads ||\n                    (this.options.limitConcurrentUploads &&\n                    this.options.limitConcurrentUploads <= this._sending)) {\n                if (this.options.limitConcurrentUploads > 1) {\n                    slot = $.Deferred();\n                    this._slots.push(slot);\n                    pipe = slot.pipe(send);\n                } else {\n                    pipe = (this._sequence = this._sequence.pipe(send, send));\n                }\n                // Return the piped Promise object, enhanced with an abort method,\n                // which is delegated to the jqXHR object of the current upload,\n                // and jqXHR callbacks mapped to the equivalent Promise methods:\n                pipe.abort = function () {\n                    aborted = [undefined, 'abort', 'abort'];\n                    if (!jqXHR) {\n                        if (slot) {\n                            slot.rejectWith(options.context, aborted);\n                        }\n                        return send();\n                    }\n                    return jqXHR.abort();\n                };\n                return this._enhancePromise(pipe);\n            }\n            return send();\n        },\n\n        _onAdd: function (e, data) {\n            var that = this,\n                result = true,\n                options = $.extend({}, this.options, data),\n                limit = options.limitMultiFileUploads,\n                paramName = this._getParamName(options),\n                paramNameSet,\n                paramNameSlice,\n                fileSet,\n                i;\n            if (!(options.singleFileUploads || limit) ||\n                    !this._isXHRUpload(options)) {\n                fileSet = [data.files];\n                paramNameSet = [paramName];\n            } else if (!options.singleFileUploads && limit) {\n                fileSet = [];\n                paramNameSet = [];\n                for (i = 0; i < data.files.length; i += limit) {\n                    fileSet.push(data.files.slice(i, i + limit));\n                    paramNameSlice = paramName.slice(i, i + limit);\n                    if (!paramNameSlice.length) {\n                        paramNameSlice = paramName;\n                    }\n                    paramNameSet.push(paramNameSlice);\n                }\n            } else {\n                paramNameSet = paramName;\n            }\n            data.originalFiles = data.files;\n            $.each(fileSet || data.files, function (index, element) {\n                var newData = $.extend({}, data);\n                newData.files = fileSet ? element : [element];\n                newData.paramName = paramNameSet[index];\n                that._initProgressObject(newData);\n                that._addConvenienceMethods(e, newData);\n                result = that._trigger('add', e, newData);\n                return result;\n            });\n            return result;\n        },\n\n        _replaceFileInput: function (input) {\n            var inputClone = input.clone(true);\n            $('<form></form>').append(inputClone)[0].reset();\n            // Detaching allows to insert the fileInput on another form\n            // without loosing the file input value:\n            input.after(inputClone).detach();\n            // Avoid memory leaks with the detached file input:\n            $.cleanData(input.unbind('remove'));\n            // Replace the original file input element in the fileInput\n            // elements set with the clone, which has been copied including\n            // event handlers:\n            this.options.fileInput = this.options.fileInput.map(function (i, el) {\n                if (el === input[0]) {\n                    return inputClone[0];\n                }\n                return el;\n            });\n            // If the widget has been initialized on the file input itself,\n            // override this.element with the file input clone:\n            if (input[0] === this.element[0]) {\n                this.element = inputClone;\n            }\n        },\n\n        _handleFileTreeEntry: function (entry, path) {\n            var that = this,\n                dfd = $.Deferred(),\n                errorHandler = function (e) {\n                    if (e && !e.entry) {\n                        e.entry = entry;\n                    }\n                    // Since $.when returns immediately if one\n                    // Deferred is rejected, we use resolve instead.\n                    // This allows valid files and invalid items\n                    // to be returned together in one set:\n                    dfd.resolve([e]);\n                },\n                dirReader;\n            path = path || '';\n            if (entry.isFile) {\n                if (entry._file) {\n                    // Workaround for Chrome bug #149735\n                    entry._file.relativePath = path;\n                    dfd.resolve(entry._file);\n                } else {\n                    entry.file(function (file) {\n                        file.relativePath = path;\n                        dfd.resolve(file);\n                    }, errorHandler);\n                }\n            } else if (entry.isDirectory) {\n                dirReader = entry.createReader();\n                dirReader.readEntries(function (entries) {\n                    that._handleFileTreeEntries(\n                        entries,\n                        path + entry.name + '/'\n                    ).done(function (files) {\n                        dfd.resolve(files);\n                    }).fail(errorHandler);\n                }, errorHandler);\n            } else {\n                // Return an empy list for file system items\n                // other than files or directories:\n                dfd.resolve([]);\n            }\n            return dfd.promise();\n        },\n\n        _handleFileTreeEntries: function (entries, path) {\n            var that = this;\n            return $.when.apply(\n                $,\n                $.map(entries, function (entry) {\n                    return that._handleFileTreeEntry(entry, path);\n                })\n            ).pipe(function () {\n                return Array.prototype.concat.apply(\n                    [],\n                    arguments\n                );\n            });\n        },\n\n        _getDroppedFiles: function (dataTransfer) {\n            dataTransfer = dataTransfer || {};\n            var items = dataTransfer.items;\n            if (items && items.length && (items[0].webkitGetAsEntry ||\n                    items[0].getAsEntry)) {\n                return this._handleFileTreeEntries(\n                    $.map(items, function (item) {\n                        var entry;\n                        if (item.webkitGetAsEntry) {\n                            entry = item.webkitGetAsEntry();\n                            if (entry) {\n                                // Workaround for Chrome bug #149735:\n                                entry._file = item.getAsFile();\n                            }\n                            return entry;\n                        }\n                        return item.getAsEntry();\n                    })\n                );\n            }\n            return $.Deferred().resolve(\n                $.makeArray(dataTransfer.files)\n            ).promise();\n        },\n\n        _getSingleFileInputFiles: function (fileInput) {\n            fileInput = $(fileInput);\n            var entries = fileInput.prop('webkitEntries') ||\n                    fileInput.prop('entries'),\n                files,\n                value;\n            if (entries && entries.length) {\n                return this._handleFileTreeEntries(entries);\n            }\n            files = $.makeArray(fileInput.prop('files'));\n            if (!files.length) {\n                value = fileInput.prop('value');\n                if (!value) {\n                    return $.Deferred().resolve([]).promise();\n                }\n                // If the files property is not available, the browser does not\n                // support the File API and we add a pseudo File object with\n                // the input value as name with path information removed:\n                files = [{name: value.replace(/^.*\\\\/, '')}];\n            } else if (files[0].name === undefined && files[0].fileName) {\n                // File normalization for Safari 4 and Firefox 3:\n                $.each(files, function (index, file) {\n                    file.name = file.fileName;\n                    file.size = file.fileSize;\n                });\n            }\n            return $.Deferred().resolve(files).promise();\n        },\n\n        _getFileInputFiles: function (fileInput) {\n            if (!(fileInput instanceof $) || fileInput.length === 1) {\n                return this._getSingleFileInputFiles(fileInput);\n            }\n            return $.when.apply(\n                $,\n                $.map(fileInput, this._getSingleFileInputFiles)\n            ).pipe(function () {\n                return Array.prototype.concat.apply(\n                    [],\n                    arguments\n                );\n            });\n        },\n\n        _onChange: function (e) {\n            var that = this,\n                data = {\n                    fileInput: $(e.target),\n                    form: $(e.target.form)\n                };\n            this._getFileInputFiles(data.fileInput).always(function (files) {\n                data.files = files;\n                if (that.options.replaceFileInput) {\n                    that._replaceFileInput(data.fileInput);\n                }\n                if (that._trigger('change', e, data) !== false) {\n                    that._onAdd(e, data);\n                }\n            });\n        },\n\n        _onPaste: function (e) {\n            var cbd = e.originalEvent.clipboardData,\n                items = (cbd && cbd.items) || [],\n                data = {files: []};\n            $.each(items, function (index, item) {\n                var file = item.getAsFile && item.getAsFile();\n                if (file) {\n                    data.files.push(file);\n                }\n            });\n            if (this._trigger('paste', e, data) === false ||\n                    this._onAdd(e, data) === false) {\n                return false;\n            }\n        },\n\n        _onDrop: function (e) {\n            var that = this,\n                dataTransfer = e.dataTransfer = e.originalEvent.dataTransfer,\n                data = {};\n            if (dataTransfer && dataTransfer.files && dataTransfer.files.length) {\n                e.preventDefault();\n            }\n            this._getDroppedFiles(dataTransfer).always(function (files) {\n                data.files = files;\n                if (that._trigger('drop', e, data) !== false) {\n                    that._onAdd(e, data);\n                }\n            });\n        },\n\n        _onDragOver: function (e) {\n            var dataTransfer = e.dataTransfer = e.originalEvent.dataTransfer;\n            if (this._trigger('dragover', e) === false) {\n                return false;\n            }\n            if (dataTransfer && $.inArray('Files', dataTransfer.types) !== -1) {\n                dataTransfer.dropEffect = 'copy';\n                e.preventDefault();\n            }\n        },\n\n        _initEventHandlers: function () {\n            if (this._isXHRUpload(this.options)) {\n                this._on(this.options.dropZone, {\n                    dragover: this._onDragOver,\n                    drop: this._onDrop\n                });\n                this._on(this.options.pasteZone, {\n                    paste: this._onPaste\n                });\n            }\n            this._on(this.options.fileInput, {\n                change: this._onChange\n            });\n        },\n\n        _destroyEventHandlers: function () {\n            this._off(this.options.dropZone, 'dragover drop');\n            this._off(this.options.pasteZone, 'paste');\n            this._off(this.options.fileInput, 'change');\n        },\n\n        _setOption: function (key, value) {\n            var refresh = $.inArray(key, this._refreshOptionsList) !== -1;\n            if (refresh) {\n                this._destroyEventHandlers();\n            }\n            this._super(key, value);\n            if (refresh) {\n                this._initSpecialOptions();\n                this._initEventHandlers();\n            }\n        },\n\n        _initSpecialOptions: function () {\n            var options = this.options;\n            if (options.fileInput === undefined) {\n                options.fileInput = this.element.is('input[type=\"file\"]') ?\n                        this.element : this.element.find('input[type=\"file\"]');\n            } else if (!(options.fileInput instanceof $)) {\n                options.fileInput = $(options.fileInput);\n            }\n            if (!(options.dropZone instanceof $)) {\n                options.dropZone = $(options.dropZone);\n            }\n            if (!(options.pasteZone instanceof $)) {\n                options.pasteZone = $(options.pasteZone);\n            }\n        },\n\n        _create: function () {\n            var options = this.options;\n            // Initialize options set via HTML5 data-attributes:\n            $.extend(options, $(this.element[0].cloneNode(false)).data());\n            this._initSpecialOptions();\n            this._slots = [];\n            this._sequence = this._getXHRPromise(true);\n            this._sending = this._active = 0;\n            this._initProgressObject(this);\n            this._initEventHandlers();\n        },\n\n        // This method is exposed to the widget API and allows to query\n        // the widget upload progress.\n        // It returns an object with loaded, total and bitrate properties\n        // for the running uploads:\n        progress: function () {\n            return this._progress;\n        },\n\n        // This method is exposed to the widget API and allows adding files\n        // using the fileupload API. The data parameter accepts an object which\n        // must have a files property and can contain additional options:\n        // .fileupload('add', {files: filesList});\n        add: function (data) {\n            var that = this;\n            if (!data || this.options.disabled) {\n                return;\n            }\n            if (data.fileInput && !data.files) {\n                this._getFileInputFiles(data.fileInput).always(function (files) {\n                    data.files = files;\n                    that._onAdd(null, data);\n                });\n            } else {\n                data.files = $.makeArray(data.files);\n                this._onAdd(null, data);\n            }\n        },\n\n        // This method is exposed to the widget API and allows sending files\n        // using the fileupload API. The data parameter accepts an object which\n        // must have a files or fileInput property and can contain additional options:\n        // .fileupload('send', {files: filesList});\n        // The method returns a Promise object for the file upload call.\n        send: function (data) {\n            if (data && !this.options.disabled) {\n                if (data.fileInput && !data.files) {\n                    var that = this,\n                        dfd = $.Deferred(),\n                        promise = dfd.promise(),\n                        jqXHR,\n                        aborted;\n                    promise.abort = function () {\n                        aborted = true;\n                        if (jqXHR) {\n                            return jqXHR.abort();\n                        }\n                        dfd.reject(null, 'abort', 'abort');\n                        return promise;\n                    };\n                    this._getFileInputFiles(data.fileInput).always(\n                        function (files) {\n                            if (aborted) {\n                                return;\n                            }\n                            data.files = files;\n                            jqXHR = that._onSend(null, data).then(\n                                function (result, textStatus, jqXHR) {\n                                    dfd.resolve(result, textStatus, jqXHR);\n                                },\n                                function (jqXHR, textStatus, errorThrown) {\n                                    dfd.reject(jqXHR, textStatus, errorThrown);\n                                }\n                            );\n                        }\n                    );\n                    return this._enhancePromise(promise);\n                }\n                data.files = $.makeArray(data.files);\n                if (data.files.length) {\n                    return this._onSend(null, data);\n                }\n            }\n            return this._getXHRPromise(false, data && data.context);\n        }\n\n    });\n\n}));\n");

/*fileupload-processing*/
System.define("fileupload-processing","/*\n * jQuery File Upload File Processing Plugin 1.2.3\n * https://github.com/blueimp/jQuery-File-Upload\n *\n * Copyright 2012, Sebastian Tschan\n * https://blueimp.net\n *\n * Licensed under the MIT license:\n * http://www.opensource.org/licenses/MIT\n */\n\n/*jslint nomen: true, unparam: true, regexp: true */\n/*global define, window, document */\n\n(function (factory) {\n    'use strict';\n    if (typeof define === 'function' && define.amd) {\n        // Register as an anonymous AMD module:\n        define([\n            'jquery',\n            'load-image',\n            'canvas-to-blob',\n            './jquery.fileupload'\n        ], factory);\n    } else {\n        // Browser globals:\n        factory(\n            window.jQuery,\n            window.loadImage\n        );\n    }\n}(function ($, loadImage) {\n    'use strict';\n\n    // The File Upload FP version extends the fileupload widget\n    // with file processing functionality:\n    $.widget('blueimp.fileupload', $.blueimp.fileupload, {\n\n        options: {\n            // The list of file processing actions:\n            process: [\n            /*\n                {\n                    action: 'load',\n                    fileTypes: /^image\\/(gif|jpeg|png)$/,\n                    maxFileSize: 20000000 // 20MB\n                },\n                {\n                    action: 'resize',\n                    maxWidth: 1920,\n                    maxHeight: 1200,\n                    minWidth: 800,\n                    minHeight: 600\n                },\n                {\n                    action: 'save'\n                }\n            */\n            ],\n\n            // The add callback is invoked as soon as files are added to the\n            // fileupload widget (via file input selection, drag & drop or add\n            // API call). See the basic file upload widget for more information:\n            add: function (e, data) {\n                if (data.autoUpload || (data.autoUpload !== false &&\n                        ($(this).data('blueimp-fileupload') ||\n                        $(this).data('fileupload')).options.autoUpload)) {\n                    $(this).fileupload('process', data).done(function () {\n                        data.submit();\n                    });\n                }\n            }\n        },\n\n        processActions: {\n            // Loads the image given via data.files and data.index\n            // as img element if the browser supports canvas.\n            // Accepts the options fileTypes (regular expression)\n            // and maxFileSize (integer) to limit the files to load:\n            load: function (data, options) {\n                var that = this,\n                    file = data.files[data.index],\n                    dfd = $.Deferred();\n                if (window.HTMLCanvasElement &&\n                        window.HTMLCanvasElement.prototype.toBlob &&\n                        ($.type(options.maxFileSize) !== 'number' ||\n                            file.size < options.maxFileSize) &&\n                        (!options.fileTypes ||\n                            options.fileTypes.test(file.type))) {\n                    loadImage(\n                        file,\n                        function (img) {\n                            if (!img.src) {\n                                return dfd.rejectWith(that, [data]);\n                            }\n                            data.img = img;\n                            dfd.resolveWith(that, [data]);\n                        }\n                    );\n                } else {\n                    dfd.rejectWith(that, [data]);\n                }\n                return dfd.promise();\n            },\n            // Resizes the image given as data.img and updates\n            // data.canvas with the resized image as canvas element.\n            // Accepts the options maxWidth, maxHeight, minWidth and\n            // minHeight to scale the given image:\n            resize: function (data, options) {\n                var img = data.img,\n                    canvas;\n                options = $.extend({canvas: true}, options);\n                if (img) {\n                    canvas = loadImage.scale(img, options);\n                    if (canvas.width !== img.width ||\n                            canvas.height !== img.height) {\n                        data.canvas = canvas;\n                    }\n                }\n                return data;\n            },\n            // Saves the processed image given as data.canvas\n            // inplace at data.index of data.files:\n            save: function (data, options) {\n                // Do nothing if no processing has happened:\n                if (!data.canvas) {\n                    return data;\n                }\n                var that = this,\n                    file = data.files[data.index],\n                    name = file.name,\n                    dfd = $.Deferred(),\n                    callback = function (blob) {\n                        if (!blob.name) {\n                            if (file.type === blob.type) {\n                                blob.name = file.name;\n                            } else if (file.name) {\n                                blob.name = file.name.replace(\n                                    /\\..+$/,\n                                    '.' + blob.type.substr(6)\n                                );\n                            }\n                        }\n                        // Store the created blob at the position\n                        // of the original file in the files list:\n                        data.files[data.index] = blob;\n                        dfd.resolveWith(that, [data]);\n                    };\n                // Use canvas.mozGetAsFile directly, to retain the filename, as\n                // Gecko doesn't support the filename option for FormData.append:\n                if (data.canvas.mozGetAsFile) {\n                    callback(data.canvas.mozGetAsFile(\n                        (/^image\\/(jpeg|png)$/.test(file.type) && name) ||\n                            ((name && name.replace(/\\..+$/, '')) ||\n                                'blob') + '.png',\n                        file.type\n                    ));\n                } else {\n                    data.canvas.toBlob(callback, file.type);\n                }\n                return dfd.promise();\n            }\n        },\n\n        // Resizes the file at the given index and stores the created blob at\n        // the original position of the files list, returns a Promise object:\n        _processFile: function (files, index, options) {\n            var that = this,\n                dfd = $.Deferred().resolveWith(that, [{\n                    files: files,\n                    index: index\n                }]),\n                chain = dfd.promise();\n            that._processing += 1;\n            $.each(options.process, function (i, settings) {\n                chain = chain.pipe(function (data) {\n                    return that.processActions[settings.action]\n                        .call(this, data, settings);\n                });\n            });\n            chain.always(function () {\n                that._processing -= 1;\n                if (that._processing === 0) {\n                    that.element\n                        .removeClass('fileupload-processing');\n                }\n            });\n            if (that._processing === 1) {\n                that.element.addClass('fileupload-processing');\n            }\n            return chain;\n        },\n\n        // Processes the files given as files property of the data parameter,\n        // returns a Promise object that allows to bind a done handler, which\n        // will be invoked after processing all files (inplace) is done:\n        process: function (data) {\n            var that = this,\n                options = $.extend({}, this.options, data);\n            if (options.process && options.process.length &&\n                    this._isXHRUpload(options)) {\n                $.each(data.files, function (index, file) {\n                    that._processingQueue = that._processingQueue.pipe(\n                        function () {\n                            var dfd = $.Deferred();\n                            that._processFile(data.files, index, options)\n                                .always(function () {\n                                    dfd.resolveWith(that);\n                                });\n                            return dfd.promise();\n                        }\n                    );\n                });\n            }\n            return this._processingQueue;\n        },\n\n        _create: function () {\n            this._super();\n            this._processing = 0;\n            this._processingQueue = $.Deferred().resolveWith(this)\n                .promise();\n        }\n\n    });\n\n}));\n");
/*bithub/postform/postform*/
define("bithub/postform/postform",["can/component/component","./postform.mustache!","bithub/models/event","bithub/models/tag","bithub/models/postas_user","fileupload-processing"],function(t,e,r,a,n){var i;return t.extend({tag:"bh-postform",template:e,scope:{event:null,imageUploadProgress:0,imageUploadError:!1,__showAllErrors:!1,postAsAvatar:null,serverError:null,hasImage:!1,init:function(){null===this.attr("event")&&this.attr("event",new r),this.attr("event").isNew()||(this.__oldEvent=this.attr("event"),this.attr("event",new r(this.attr("event").attr())),this.attr("event.project",this.getProjectForEvent())),this.attr("__dirtyAttrs",[]),this.attr("__errors",{})},eventCategories:function(){var t=[],e=this.attr("event").isNew()?"NewPost":"ExistingPost";return window.CATEGORIES.each(function(r){a["allowedCategoriesFor"+e].indexOf(r.attr("name"))>-1&&t.push(r)}),t},eventProjects:function(){var t=[],e=this.attr("event").isNew()?"NewPost":"ExistingPost";return window.PROJECTS.each(function(r){a["allowedProjectsFor"+e].indexOf(r.attr("name"))>-1&&t.push(r)}),t},currentUser:function(){return window.CURRENT_USER},availableTags:function(){var t=this.attr("event.tags"),e=this.attr("event.category"),r=this.attr("event.project");return window.VISIBLE_TAGS.attr("length"),t.attr("length"),can.grep(window.VISIBLE_TAGS,function(a){var n=a.attr("name"),i=-1===t.indexOf(n);return i=i&&n!==e,i=i&&n!==r}).sort(function(t,e){return t.attr("name")>e.attr("name")?1:t.attr("name")<e.attr("name")?-1:0})},eventTags:function(){var t=this.attr("event"),e=t.attr("category"),r=t.attr("project"),a=t.attr("tags");return a.attr("length"),_.uniq(can.grep(a,function(t){return t!==r&&t!==e}))},getProjectForEvent:function(){for(var t=this.attr("event"),e=t.attr("tags").attr(),r=a.allowedProjectsForNewPost,n=0;n<e.length;n++)if(can.inArray(e[n],r)>-1)return e[n]},formAction:function(){var t=this.attr("event");return t.isNew()?"/api/v1/events":can.sub("/api/v1/events/{id}",t)},removeTag:function(t){var e=this.attr("event.tags"),r=e.indexOf(t);r>-1&&e.splice(r,1)},addTag:function(t){this.attr("event.tags").push(t.attr("name"))},removeImage:function(){this.attr("hasImage",!1)}},helpers:{errorsFor:function(t,e){var r=this.attr("__errors."+t),a=this.attr("__dirtyAttrs");return(this.attr("__showAllErrors")||a.indexOf(t)>-1)&&r?e.fn(r.join("<br>")):void 0},datepicker:function(){return function(t){var e=$(t);e.datepicker({format:"mm/dd/yyyy",weekStart:0})}},fileupload:function(){var t=this;return function(e){$(e).fileupload({datatype:"json",limitMultiFileUploads:1,add:$.noop,type:t.attr("event").isNew()?"POST":"PUT",replaceFileInput:!1})}},typeahead:function(){var t=this;return this.attr("event.origin_author_feed")||this.attr("event.origin_author_feed","github"),function(e){var r=$(e);r.typeahead({minLength:3,source:function(e,r){var a=t.attr("event.origin_author_feed");clearTimeout(this.timeout),this.timeout=setTimeout(function(){n.findAll({user:e,feed:a}).then(function(t){i=t,r(can.map(t,function(t){return t.fullName()}))})},200)},matcher:function(t){return t&&t.toLowerCase().indexOf(this.query.trim().toLowerCase())>-1},updater:function(e){var r,a=e.split("/")[0].trim();return r=can.grep(i,function(t){return t.username===a})[0],t.attr("event.origin_author_id",r.id),t.attr("event.origin_author_feed",r.from),t.attr("event.origin_author_name",r.fullName()),t.attr("postAsAvatar",r.profileImageUrl()),e}})}},removeBrokenPreviewImage:function(){return function(t){var e=$(t);e.on("error",function(){e.remove()})}}},events:{" fileuploadadd":function(t,e,r){for(var a=0;a<r.files.length;a++)if(!r.files[a].name.match(/(\.|\/)(gif|jpe?g|png)$/i))return this.scope.attr({imageUploadError:!0,hasImage:!1}),t.find(".image-uploader .image-preview img").remove(),void delete this.__filedata;this.scope.attr({imageUploadError:!1,hasImage:!0}),this.__fileData=r,window.loadImage(r.files[0],function(e){t.find(".image-uploader .image-preview").html(e)},{maxWidth:140,maxHeight:110,noRevoke:!0})}," fileuploadprogress":function(t,e,r){this.scope.attr("imageUploadProgress",parseInt(r.loaded/r.total*100,10))},"{scope} hasImage":function(t,e,r){var a;r===!1&&(delete this.__fileData,this.element.find(".image-uploader .image-preview img").remove(),a=this.scope.attr("event").postImageUrl(),a&&this.element.find(".image-uploader .image-preview").html('<img src="'+a+'">'))},"{scope.event} change":function(t,e,r,a,n){var i=this.scope.attr("__dirtyAttrs");-1===i.indexOf(r)&&i.push(r),this.scope.attr("__errors").attr(t.errors(),!0),"category"===r&&("event"===n?this.scope.attr("event.datetime",moment().toDate()):this.scope.removeAttr("event.datetime"))},".event-time change":function(t){var e,r=can.trim(t.val()).toUpperCase(),a=this.scope.attr("event.datetime"),n=moment(r,["H","HH","H:m","H:mm","HH:m","HH:mm","hA","hhA","h:mA","h:mmA","hh:mA","hh:mmA"]);n&&n.isValid()&&(e=moment(a),e.hour(n.hour()),e.minute(n.minute()),this.scope.attr("event.datetime",e.toDate())),t.val(this.scope.attr("event").time())},".newpost-datepicker changeDate":function(t){var e=moment(this.scope.attr("event.datetime")),r=moment(t.find("input").val(),"MM/DD/YYYY");e.date(r.date()),e.month(r.month()),e.year(r.year()),this.scope.attr("event.datetime",e.toDate()),t.datepicker("hide")},"form submit":function(t,e){var r,a=this.scope.attr("event"),n=a.errors();return e.preventDefault(),this.scope.attr("serverError",null),n?(can.batch.start(),this.scope.attr("__errors").attr(n,!0),this.scope.attr("__showAllErrors",!0),void can.batch.stop()):(this.__fileData?(this.__fileData.formData=a.serializeToArray("event"),r=this.__fileData.submit()):r=a.save(),this.scope.attr("isLoading",!0),r.done(this.proxy("eventSaved")),void r.fail(this.proxy("eventErrored")))},eventSaved:function(t){t instanceof r||(t=r.model(t)),this.scope.__oldEvent&&(this.scope.__oldEvent.attr(t.attr()),can.trigger(this.scope.__oldEvent,"updated")),this.element.trigger("event.saved")},eventErrored:function(t){var e=JSON.parse(t.responseText||"{}");this.scope.attr("serverError",e.errors||null),this.scope.attr("isLoading",!1)}}})});
/*bithub/editpost/editpost*/
define("bithub/editpost/editpost",["can/control/control","./editpost.mustache!","bithub/postform/postform"],function(t,e){return t.extend({open:function(t){var e=$(".edit-post-modal");0===e.length&&(e=$("<div></div>").addClass("edit-post-modal modal hide fade"),new this(e),$("#container").append(e)),e.trigger("openModal",[t])}},{init:function(){this.currentEvent=can.compute(null),this.element.html(e({currentEvent:this.currentEvent})).modal()}," openModal":function(t,e,n){this.currentEvent(null),this.currentEvent(n),this.element.modal("show")}," event.saved":function(){this.element.modal("hide")}})});
/*bithub/homepage/event_list/handlers/handlers*/
define("bithub/homepage/event_list/handlers/handlers",["can/control/control","bithub/models/upvote","bithub/models/award","bithub/editpost/editpost"],function(e,t,n,a){return e.extend({".event .votes .voteup-btn click":"upvote",".event .votes .count click":"upvote",".event .votes .uparrow click":"upvote",".replies .reply-event .vote-btn click":"upvote",upvote:function(e,t){var n=can.data(e.closest(".reply-event, .event"),"eventObj"),a=this.options.currentUser;a.isLoggedIn()?n.upvoteOrUnvote():(this.options.modals.showLogin(),a.delayedActions(function(){n.upvoteOrUnvote()})),t.stopImmediatePropagation()},".replies .reply-event .award-btn click":function(e,t){t.preventDefault();var n=can.data(e.closest(".reply-event"),"eventObj");n.award(),can.data(e.closest(".event"),"eventObj").attr("props.thread_awarded",!0)},".event-metadata a click":function(e){window.location=e.attr("href")},".expand-manage-bar click":function(e,t){t.preventDefault(),e.closest(".event").find(".manage-bar").slideToggle()},".manage-bar .tag-action click":function(e,t){t.preventDefault();var n=can.data(e.closest(".event"),"eventObj"),a=e.data("tag"),o=n.attr("tags");o.indexOf(a)>=0?o.splice(o.indexOf(a),1):o.push(a),n.save()},".manage-bar .category-action click":function(e,t){t.preventDefault();var n=can.data(e.closest(".event"),"eventObj");n.attr("category",e.data("category")),n.save()},".delete-event click":function(e,t){t.preventDefault();var n=can.data(e.closest(".event.list-element"),"eventObj");n.destroy(function(){e.closest(".event.list-element").fadeOut()})},".edit-event click":function(e,t){t.preventDefault();var n=can.data(e.closest(".event.list-element"),"eventObj");a.open(n)}})});
/*ui/html_select/init.mustache!can/view/mustache/system*/
define("ui/html_select/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("ui/html_select/init.mustache",t.Mustache(function(e,s){var i=[];return i.push(t.view.txt(0,"",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"if"},{get:"show"},[{fn:function(e,s){var i=[];return i.push('<div class="select-menu">\n  <select name="'),i.push(t.view.txt(!0,"select","name",this,t.Mustache.txt({scope:e,options:s},null,{get:"elemName"}))),i.push('" ',t.view.pending({scope:e,options:s}),">"),i.push("\n"),i.push(t.view.txt(0,"select",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"items"},[{fn:function(e,s){var i=[];return i.push("	<option "),i.push(t.view.txt(1,"option",1,this,t.Mustache.txt({scope:e,options:s},null,{get:"isSelected"}))),i.push(' value="'),i.push(t.view.txt(!0,"option","value",this,t.Mustache.txt({scope:e,options:s},null,{get:"value"}))),i.push('"',t.view.pending({scope:e,options:s}),">"),i.push(t.view.txt(1,"option",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"display"}))),i.push("</option>"),i.push("\n"),i.join("")}}]))),i.push("  </select>\n</div>"),i.push("\n"),i.join("")}}]))),i.join("")}))});
/*ui/html_select/html_select*/
define("ui/html_select/html_select",["can/can","./init.mustache!"],function(e,t){return e.Control.extend({defaults:{}},{init:function(e,n){var i=this;this.element.html(t({items:n.items||[],show:n.show||!0,elemName:n.elemName||""},{isSelected:function(){return this.value==i.options.currentValue()?'selected="selected"':""}}))},"select change":function(e,t){t.preventDefault(),this.options.onChange(e.val())}})});
/*bithub/homepage/event_list/spinner/spinner*/
define("bithub/homepage/event_list/spinner/spinner",["can/control/control"],function(n){return n.extend({"{spinnerTop} change":function(n,e,t){var i=this.element,o=i.css("height");t?($(document).scrollTop(0),i.css("height",o),i.find(".event-list").hide(),i.find(".spinner-top").show()):(i.css("height","auto"),i.find(".spinner-top").hide(),i.find(".event-list").show())},"{spinnerBottom} change":function(n,e,t){var i=this.element.find(".spinner-bottom");t?i.show():i.hide()}})});
/*bithub/homepage/event_list/flagsnapper/flagsnapper*/
define("bithub/homepage/event_list/flagsnapper/flagsnapper",["can/can","can/construct/proxy/proxy"],function(o){return o.Control.extend({defaults:{offset:70}},{init:function(){this.on(document,"scroll",this.proxy("onscroll"))},onscroll:function(){var o=this,t=$(window).scrollTop();$(".sep.blue").each(function(n,s){var i=$(s),e=i.offset().top-o.options.offset,f=i.css("position");"fixed"!=f&&t>e&&(i.data("fromTop",e),i.css({position:"fixed",top:o.options.offset+"px","z-index":1e3+n})),"fixed"===f&&i.data("fromTop")>=t&&i.css({position:"static"})})}})});
/*bithub/homepage/event_list/post_render/post_render*/
define("bithub/homepage/event_list/post_render/post_render",["can/control/control","bithub/homepage/event_list/flagsnapper/flagsnapper"],function(e,n){return e.extend({init:function(){this.FlagSnapper=new n(this.element,{})}," rendered":function(){var e=this;setTimeout(function(){e.FlagSnapper.onscroll()},0)}})});
/*bithub/homepage/event_list/latest_events_sorter*/
define("bithub/homepage/event_list/latest_events_sorter",["can/map/map","can/list/list","can/construct/super/super"],function(){var t=["follow","watch","fork"],r=function(r){for(var e,a=r.attr("tags"),s=0;s<t.length;s++)if(e=t[s],can.inArray(e,a)>-1)return"watch"===e?"wtch":e},e=["app","article","bug","chat","code","comment","digest","event","feature","plugin","question","twitter"],a=["follow","watch","fork"],s=function(t,r,e){return[t,r,e.attr("props.origin_author_name")].join(":")};return can.Map({hasEvents:!1,init:function(){this._super.apply(this,arguments),this.__existingDigests=[],this.attr("types",{})},appendEvents:function(t){var r,s,n;if(t.attr("length")){s=moment(t[0].attr("thread_updated_ts")).format("YYYY-MM-DD"),n=moment(t[t.length-1].attr("thread_updated_ts")).format("YYYY-MM-DD"),this.attr({date:s,stopDate:n==s?void 0:n});for(var i=0;i<t.length;i++)r=t[i],_.contains(e,r.attr("category"))&&("digest"!==r.attr("category")||0!=_.intersection(a,r.attr("tags")).length)&&this.addEvent(r)}},addEvent:function(t){var r=(this.attr("types"),t.attr("category")),e="chat"===r?"unshift":"push";this.attr("types."+r)||this.attr("types").attr(r,[]),this.attr("types."+r)[e](t),this.attr("hasEvents",(this.attr("hasEvents")||0)+1)},hasDigest:function(){return!!this.attr("types.digest")},digest:function(){for(var t,e,a,n,i=this.attr("types.digest"),o=i.attr("length"),h={follow:{},wtch:{},fork:{}},p=0;o>p;p++)t=i[p],a=r(t),"follow"===a?e=t.attr("props.target")||t.attr("props.target_screen_name"):"wtch"===a?e=t.attr("props.repo_name"):"fork"===a&&(e=t.attr("props.repo_name")),a&&(h[a][e]=h[a][e]||[],n=s(a,e,t),-1===this.__existingDigests.indexOf(n)&&(this.__existingDigests.push(n),h[a][e].push(t)));return h},code:function(){for(var t,r,e,a,s,n=this.attr("types.code"),i=n.attr("length"),o=["push","create","pull_request"],h={},p=0;i>p;p++)t=n[p],a=t.attr("title"),r=t.attr("props.origin_author_name"),s=t.attr("props.repo_name"),e=_.find(t.attr("tags"),function(t){return _.contains(o,t)}),e&&("push"!=e||0!=t.attr("children").length)?(h[s]||(h[s]={}),h[s][r]||(h[s][r]={push:[],create:[],pull_request:[],"delete":[],authorName:t.attr("author.name")||t.attr("actor")||r,tags:n[p].attr("tags").serialize()}),h[s][r][e].push(t)):this.attr("hasEvents",(this.attr("hasEvents")||1)-1);var u=can.map(h,function(t,r){return{repo:r,userEvents:can.map(t,function(t){return t})}});return u}})});
/*vendor/lodash/lodash.src*/
!function(n){function e(n,e,t){for(var r=(t||0)-1,u=n.length;++r<u;)if(n[r]===e)return r;return-1}function t(n,t){var r=typeof t;if(n=n.cache,"boolean"==r||null==t)return n[t];"number"!=r&&"string"!=r&&(r="object");var u="number"==r?t:d+t;return n=n[r]||(n[r]={}),"object"==r?n[u]&&e(n[u],t)>-1?0:-1:n[u]?0:-1}function r(n){var e=this.cache,t=typeof n;if("boolean"==t||null==n)e[n]=!0;else{"number"!=t&&"string"!=t&&(t="object");var r="number"==t?n:d+n,u=e[t]||(e[t]={});"object"==t?(u[r]||(u[r]=[])).push(n)==this.array.length&&(e[t]=!1):u[r]=!0}}function u(n){return n.charCodeAt(0)}function a(n,e){var t=n.index,r=e.index;if(n=n.criteria,e=e.criteria,n!==e){if(n>e||"undefined"==typeof n)return 1;if(e>n||"undefined"==typeof e)return-1}return r>t?-1:1}function o(n){var e=-1,t=n.length,u=l();u["false"]=u["null"]=u["true"]=u.undefined=!1;var a=l();for(a.array=n,a.cache=u,a.push=r;++e<t;)a.push(n[e]);return u.object===!1?(p(a),null):a}function i(n){return"\\"+H[n]}function f(){return h.pop()||[]}function l(){return y.pop()||{array:null,cache:null,criteria:null,"false":!1,index:0,leading:!1,maxWait:0,"null":!1,number:null,object:null,push:null,string:null,trailing:!1,"true":!1,undefined:!1,value:null}

}function ne(n){var e,t;return n&&pr.call(n)==K&&(e=n.constructor,!he(e)||e instanceof e)?(Br(n,function(n,e){t=e}),t===g||ir.call(n,t)):!1}function ee(n){return Nr[n]}function te(n){return pr.call(n)==q}function re(n,e,t,r,u,a){var o=n;if("boolean"!=typeof e&&null!=e&&(r=t,t=e,e=!1),"function"==typeof t){if(t="undefined"==typeof r?t:h.createCallback(t,r,1),o=t(o),"undefined"!=typeof o)return o;o=n}var i=ye(o);if(i){var l=pr.call(o);if(!V[l])return o;var p=Er(o)}if(!i||!e)return i?p?s(o):Rr({},o):o;var v=xr[l];

var d=!u;u||(u=f()),a||(a=f());for(var _=u.length;_--;)if(u[_]==n)return a[_]==e;var k=0;

function ge(n){return hr(n)&&!yr(parseFloat(n))}function he(n){return"function"==typeof n}function ye(n){return!(!n||!G[typeof n])}function me(n){return de(n)&&n!=+n}function be(n){return null===n}function de(n){return"number"==typeof n||pr.call(n)==U}function _e(n){return n?"object"==typeof n&&pr.call(n)==L:!1}function ke(n){return"string"==typeof n||pr.call(n)==M}function je(n){return"undefined"==typeof n}function we(n,e,t){var r=arguments,u=0,a=2;if(!ye(n))return n;if(t===b)var o=r[3],i=r[4],l=r[5];else{var p=!0;i=f(),l=f(),"number"!=typeof t&&(a=r.length),a>3&&"function"==typeof r[a-2]?o=h.createCallback(r[--a-1],r[a--],2):a>2&&"function"==typeof r[a-1]&&(o=r[--a])}for(;++u<a;)(Er(r[u])?Fe:Fr)(r[u],function(e,t){var r,u,a=e,f=n[t];if(e&&((u=Er(e))||Tr(e))){for(var c=i.length;c--;)if(r=i[c]==e){f=l[c];break}if(!r){var p;o&&(a=o(f,e),(p="undefined"!=typeof a)&&(f=a)),p||(f=u?Er(f)?f:[]:Tr(f)?f:{}),i.push(e),l.push(f),p||(f=we(f,e,b,o,i,l))}}else o&&(a=o(f,e),"undefined"==typeof a&&(a=e)),"undefined"!=typeof a&&(f=a);n[t]=f});return p&&(c(i),c(l)),n}function Ce(n,e,t){var r=X(),u="function"==typeof e,a={};if(u)e=h.createCallback(e,t);else var o=rr.apply(Xt,jr.call(arguments,1));return Br(n,function(n,t,i){(u?!e(n,t,i):r(o,t)<0)&&(a[t]=n)}),a}function xe(n){for(var e=-1,t=Sr(n),r=t.length,u=Pt(r);++e<r;){var a=t[e];u[e]=[a,n[a]]}

);return r}function Ee(n,e,t,r){var u=Er(n);if(e=h.createCallback(e,r,4),null==t)if(u)t=[];else{var a=n&&n.constructor,o=a&&a.prototype;t=H(o)}return(u?Fe:Fr)(n,function(n,r,u){return e(t,n,r,u)}),t}function Ie(n){for(var e=-1,t=Sr(n),r=t.length,u=Pt(r);++e<r;)u[e]=n[t[e]];return u}function Se(n){for(var e=-1,t=rr.apply(Xt,jr.call(arguments,1)),r=t.length,u=Pt(r);++e<r;)u[e]=n[t[e]];return u}function Ae(n,e,t){var r=-1,u=X(),a=n?n.length:0,o=!1;return t=(0>t?br(0,a+t):t)||0,a&&"number"==typeof a?o=(ke(n)?n.indexOf(e,t):u(n,e,t))>-1:Fr(n,function(n){return++r>=t?!(o=n===e):void 0}),o}function Ne(n,e,t){var r={};return e=h.createCallback(e,t),Fe(n,function(n,t,u){t=Jt(e(n,t,u)),ir.call(r,t)?r[t]++:r[t]=1}),r}function Re(n,e,t){var r=!0;e=h.createCallback(e,t);

),o}function De(n,e,t){var r=-1,u=n?n.length:0;if(e=h.createCallback(e,t),"number"==typeof u)for(var a=Pt(u);++r<u;)a[r]=e(n[r],r,n);else a=[],Fr(n,function(n,t,u){

if("number"!=typeof a){var i=Sr(n);a=i.length}return e=h.createCallback(e,r,4),Fe(n,function(n,r,f){r=i?i[--a]:--a,t=o?(o=!1,u[r]):e(t,u[r],r,f)}),t}function Le(n,e,t){

if(l){var c=o(i);c?(u=t,i=c):l=!1}for(;++r<a;){var s=n[r];u(i,s)<0&&f.push(s)}

else if(r){var a=it(n,t);return n[a]===t?a:-1}return n?e(n,t,r):-1}function et(n,e,t){if(!n)return[];var r=0,u=n.length;if("number"!=typeof e&&null!=e){var a=u;for(e=h.createCallback(e,t);a--&&e(n[a],a,n);)r++}else r=null==e||t?1:e||r;return s(n,0,dr(br(0,u-r),u))}function tt(n){for(var r=arguments,u=r.length,a=-1,i=f(),l=-1,s=X(),v=n?n.length:0,g=[],h=f();++a<u;){var y=r[a];i[a]=s===e&&(y?y.length:0)>=_&&o(a?r[a]:h)}n:for(;++l<v;){var m=i[0];if(y=n[l],(m?t(m,y):s(h,y))<0){for(a=u,(m||h).push(y);--a;)if(m=i[a],(m?t(m,y):s(r[a],y))<0)continue n;g.push(y)}}for(;u--;)m=i[u],m&&p(m);return c(i),c(h),g}function rt(n,e,t){

var u=0,a=n?n.length:u;for(t=t?h.createCallback(t,r,1):St,e=t(e);a>u;){var o=u+a>>>1;t(n[o])<e?u=o+1:a=o}return u}function ft(n){return Er(n)||(arguments[0]=n?jr.call(n):Xt),zr(rr.apply(Xt,arguments))}function lt(n){for(var e=-1,t=n?ze(Pe(n,"length")):0,r=Pt(0>t?0:t);++e<t;)r[e]=Pe(n,e);return r}function ct(n){return Xe(n,jr.call(arguments,1))}function pt(n){return n?lt(arguments):[]}function st(n,e){for(var t=-1,r=n?n.length:0,u={};++t<r;){var a=n[t];e?u[a]=e[t]:u[a[0]]=a[1]}return u}function vt(n,e){return 1>n?e():function(){return--n<1?e.apply(this,arguments):void 0}}function gt(n,e){return Or.fastBind||sr&&arguments.length>2?sr.call.apply(sr,arguments):k(n,e,jr.call(arguments,2))}function ht(n){for(var e=arguments.length>1?rr.apply(Xt,jr.call(arguments,1)):oe(n),t=-1,r=e.length;++t<r;){var u=e[t];n[u]=gt(n[u],n)}return n}function yt(n,e){return k(n,e,jr.call(arguments,2),b)}function mt(){

);return r}}return"undefined"==typeof e||S&&!S.test(ar.call(n))?n:1===t?function(t){return n.call(e,t)}:2===t?function(t,r){return n.call(e,t,r)}:4===t?function(t,r,u,a){return n.call(e,t,r,u,a)}:function(t,r,u){return n.call(e,t,r,u)}}function dt(n,e,t){function r(){tr(s),tr(v),l=0,s=v=null}function u(){var e=g&&(!h||l>1);r(),e&&(p!==!1&&(c=new Kt),i=n.apply(f,o))}function a(){r(),(g||p!==e)&&(c=new Kt,i=n.apply(f,o))}var o,i,f,l=0,c=0,p=!1,s=null,v=null,g=!0;if(e=br(0,e||0),t===!0){var h=!0;g=!1}else ye(t)&&(h=t.leading,p="maxWait"in t&&br(e,t.maxWait||0),g="trailing"in t?t.trailing:g);return function(){if(o=arguments,f=this,l++,tr(v),p===!1)h&&2>l&&(i=n.apply(f,o));else{var t=new Kt;s||h||(c=t);var r=p-(t-c);0>=r?(tr(s),s=null,c=t,i=n.apply(f,o)):s||(s=cr(a,r))}return e!==p&&(v=cr(u,e)),i}}function _t(n){var e=jr.call(arguments,1);return cr(function(){n.apply(g,e)},1)}function kt(n,e){var t=jr.call(arguments,2);return cr(function(){n.apply(g,t)},e)}function jt(n,e){function t(){var r=t.cache,u=d+(e?e.apply(this,arguments):arguments[0]);return ir.call(r,u)?r[u]:r[u]=n.apply(this,arguments)}return t.cache={},t}function wt(n){var e,t;return function(){return e?t:(e=!0,t=n.apply(this,arguments),n=null,t)}}function Ct(n){return k(n,jr.call(arguments,1))}function xt(n){return k(n,jr.call(arguments,1),null,b)}function Ot(n,e,t){var r=!0,u=!0;t===!1?r=!1:ye(t)&&(r="leading"in t?t.leading:r,u="trailing"in t?t.trailing:u),t=l(),t.leading=r,t.maxWait=e,t.trailing=u;var a=dt(n,e,t);return p(t),a}function Et(n,e){

})}function Nt(){return r._=Zt,this}function Rt(n,e){null==n&&null==e&&(e=1),n=+n||0,null==e?(e=n,n=0):e=+e||0;var t=kr();return n%1||e%1?n+dr(t*(e-n+parseFloat("1e-"+((t+"").length-1))),e):n+ur(t*(e-n+1))}function $t(n,e){var t=n?n[e]:g;return he(t)?n[e]():t}function Bt(n,e,t){var r=h.templateSettings;n||(n=""),t=$r({},t,r);var u,a=$r({},t.

}()|[\]\\]/g,"\\$&").replace(/valueOf|for [^\]]+/g,".+?")+"$"),er=Mt.ceil,tr=r.clearTimeout,rr=Xt.concat,ur=Mt.floor,ar=Lt.prototype.toString,or=nr.test(or=Gt.getPrototypeOf)&&or,ir=Yt.hasOwnProperty,fr=Xt.push,lr=(Yt.propertyIsEnumerable,r.setImmediate

++o<f;)if(r=i[o],e(u[r],r,n)===!1)return a;return a},Tr=function(n){if(!n||pr.call(n)!=K)return!1;var e=n.valueOf,t="function"==typeof e&&(t=or(e))&&or(t);return t?n==t||or(n)==t:ne(n)},qr=$e,Dr=Z(function Pr(n,e,t){for(var r=-1,u=n?n.length:0,a=[];++r<u;){var o=n[r];t&&(o=t(o,r,n)),Er(o)?fr.apply(a,e?o:Pr(o)):a.push(o)}return a}),zr=Z(function(n,r,u){var a=-1,i=X(),l=n?n.length:0,s=[],v=!r&&l>=_&&i===e,g=u||v?f():s;if(v){var h=o(g);h?(i=t,g=h):(v=!1,g=u?g:(c(g),s))}for(;++a<l;){var y=n[a],m=u?u(y,a,n):y;(r?!a||g[g.length-1]!==m:i(g,m)<0)&&((u||v)&&g.push(m),s.push(y))}

var g,h=[],y=[],m=0,b={},d=+new Date+"",_=75,k=40,j=/\b__p \+= '';/g,w=/\b(__p \+=) '' \+/g,C=/(__e\(.*?\)|\b__t\)) \+\n'';/g,x=/&(?:amp|lt|gt|quot|#39);/g,O=/\$\{([^\\}]*(?:\\.[^\\}]*)*)\}/g,E=/\w*$/,I=/<%=([\s\S]+?)%>/g,S=(S=/\bthis\b/)&&S.test(v)&&S,A=" 	\f ﻿\n\r\u2028\u2029 ᠎             　",N=RegExp("^["+A+"]*0+(?=.$)"),R=/($^)/,$=/[&<>"']/g,B=/

,J=G[typeof exports]&&exports,Q=G[typeof module]&&module&&module.exports==J&&module,X=G[typeof global]&&global;

/*vendor/lodash/lodash*/
define("vendor/lodash/lodash",["vendor/lodash/lodash.src"]);
/*bithub/helpers/fun_helpers*/
define("bithub/helpers/fun_helpers",["vendor/lodash/lodash"],function(n){return{checker:function(){var r=n.toArray(arguments);return function(e){return n.reduce(r,function(r,u){return u(e)?r:n(r).push(u.message).value()},[])}},validator:function(n,r){var e=function(){return r.apply(r,arguments)};return e.message=n,e},complement:function(){return function(){return!pred.apply(null,n.toArray(arguments))}}}});
/*bithub/helpers/mustacheHelpers*/
define("bithub/helpers/mustacheHelpers",["can/view/mustache/mustache","can/control/control","vendor/moment/moment"],function(e,t){can.Mustache.registerHelper("prettifyTs",function(e,t){var n;e=moment(can.isFunction(e)?e():e),t=can.isFunction(t)?t():t;var a={date:{today:"[Today]",nextDay:"[Tomorrow]",yesterday:"[Yesterday]",thisweek:"dddd",lastweek:"[Last] dddd",nextWeek:"dddd",_default:"L"},time:{today:"LT",yesterday:"LT",thisweek:"LT",lastweek:"LT",_default:"LT"},datetime:{today:"[Today at] LT",yesterday:"[Yesterday at] LT",nextDay:"[Tomorrow at] LT",thisweek:"dddd [at] LT",lastweek:"[Last] dddd [at] LT",nextWeek:"dddd [at] LT",_default:"L [at] LT"}},r=moment().second(0).minute(0).hour(0).diff(moment(e).second(0).minute(0).hour(0),"days",!0);return n=0>r?a[t]._default:1>r?a[t].today:2>r?a[t].yesterday:7>r?a[t].thisweek:14>r?a[t].lastweek:a[t]._default,can.__clearReading(),e.local().format(n)}),can.Mustache.registerHelper("loop",function(e,t){var n="",a=t.hash&&t.hash.begin||0,r=t.hash&&t.hash.length||e.length,s=t.hash&&t.hash.iOffset||0,i=t.hash&&t.hash.reverse||!1;if(e="function"==typeof e?e():e,!e)return n;if(e.attr("length")>0)if(a="function"==typeof a?a():a,r="function"==typeof r?r():r,s="function"==typeof s?s():s,i)for(var c=a+r-1;c>=a&&c>=0;c--)e[c].attr("__index",c+s+1),n+=t.fn(e[c]);else for(var c=a;a+r>c&&c<e.length;c++)e[c].attr("__index",c+s+1),n+=t.fn(e[c]);return n}),can.Mustache.registerHelper("ifequal",function(e,t,n){return e="function"==typeof e?e():e,t="function"==typeof t?t():t,e===t?n.fn(this):n.inverse(this)}),can.Mustache.registerHelper("substring",function(e,t,n){return e="function"==typeof e?e():e,can.__clearReading(),(e||"").substr(t,n)}),can.Mustache.registerHelper("capitalize",function(e){return can.capitalize("function"==typeof e?e():e)}),can.Mustache.registerHelper("logObj",function(e){console.log(e)}),can.Mustache.registerHelper("stripHtml",function(e){return e="function"==typeof e?e():e,$("<div>"+e+"</div>").text()}),can.Mustache.registerHelper("applyMore",function(){return function(e){$(e).addClass("no-more")}});var n=can.view.mustache('<img src="{{ src }}" alt="{{ alt }}" class="{{ klass }}" {{imgErrorHandler}}>'),a=t.extend({" load":function(){this.destroy()}," error":function(){this.element.attr("src","/assets/images/fallback.png"),this.destroy()}});can.Mustache.registerHelper("imgErrorHandler",function(){return function(e){new a(e)}}),can.Mustache.registerHelper("img",function(e){var t="",a="";return e=can.isFunction(e)?e():e,arguments.length>2&&(t=arguments[1],t=can.isFunction(t)?t():t),arguments.length>3&&(a=arguments[2],a=can.isFunction(a)?a():a),can.__clearReading(),e?n.render({src:e,alt:t,klass:a}):""}),can.Mustache.registerHelper("v",function(e){var t=can.isFunction(e)?e():e;return can.__clearReading(),t}),can.Mustache.registerHelper("when",function(e,t){var n=can.isFunction(e)?e():e;return can.__clearReading(),n?t.fn(t.scope):t.inverse(t.scope)})});
/*jquerypp/dom/compare/compare*/
define("jquerypp/dom/compare/compare",["jquery/jquery"],function(e){return e.fn.compare=function(e){try{e=e.jquery?e[0]:e}catch(t){return null}if(window.HTMLElement){var n=HTMLElement.prototype.toString.call(e);if("[xpconnect wrapped native prototype]"==n||"[object XULElement]"==n||"[object Window]"===n)return null}if(this[0].compareDocumentPosition)return this[0].compareDocumentPosition(e);if(this[0]==document&&e!=document)return 8;var o=(this[0]!==e&&this[0].contains(e)&&16)+(this[0]!=e&&e.contains(this[0])&&8),r=document.documentElement;return this[0].sourceIndex&&(o+=this[0].sourceIndex<e.sourceIndex&&4,o+=this[0].sourceIndex>e.sourceIndex&&2,o+=(this[0].ownerDocument!==e.ownerDocument||this[0]!=r&&this[0].sourceIndex<=0||e!=r&&e.sourceIndex<=0)&&1),o},e});
/*jquerypp/dom/range/range*/
define("jquerypp/dom/range/range",["jquery/jquery","jquerypp/dom/compare/compare"],function(e){e.fn.range=function(){return e.Range(this[0])};var t=function(e){return e.replace(/([a-z])([a-z]+)/gi,function(e,t,n){return t+n.toLowerCase()}).replace(/_/g,"")},n=function(e){return e.replace(/^([a-z]+)_TO_([a-z]+)/i,function(e,t,n){return n+"_TO_"+t})},r=function(e){return e?e.ownerDocument.defaultView||e.ownerDocument.parentWindow:window},i={};e.Range=function(t){return this.constructor!==e.Range?new e.Range(t):(t&&t.jquery&&(t=t[0]),void(!t||t.nodeType?(this.win=r(t),this.win.document.createRange?this.range=this.win.document.createRange():this.win&&this.win.document.body&&this.win.document.body.createTextRange&&(this.range=this.win.document.body.createTextRange()),t&&this.select(t)):null!=t.clientX||null!=t.pageX||null!=t.left?this.moveToPoint(t):t.originalEvent&&t.originalEvent.touches&&t.originalEvent.touches.length?this.moveToPoint(t.originalEvent.touches[0]):t.originalEvent&&t.originalEvent.changedTouches&&t.originalEvent.changedTouches.length?this.moveToPoint(t.originalEvent.changedTouches[0]):this.range=t))},e.Range.current=function(t){var n,i=r(t);return i.getSelection?(n=i.getSelection(),new e.Range(n.rangeCount?n.getRangeAt(0):i.document.createRange())):new e.Range(i.document.selection.createRange())},e.extend(e.Range.prototype,{moveToPoint:function(t){var n=t.clientX,r=t.clientY;if(!n){var a=d();n=(t.pageX||t.left||0)-a.left,r=(t.pageY||t.top||0)-a.top}if(i.moveToPoint)return this.range=e.Range().range,this.range.moveToPoint(n,r),this;for(var s=document.elementFromPoint(n,r),c=0;c<s.childNodes.length;c++){var g=s.childNodes[c];if(3===g.nodeType||4===g.nodeType)for(var l=e.Range(g),h=l.toString().length,f=1;h+1>f;f++){var u=l.end(f).rect();if(u.left<=n&&u.left+u.width>=n&&u.top<=r&&u.top+u.height>=r)return l.start(f-1),this.range=l.range,this}}var p;o(s.childNodes,function(n){var r=e.Range(n);return r.rect().top>t.clientY?!1:void(p=r)}),p?(p.start(p.toString().length),this.range=p.range):this.range=e.Range(s).range},window:function(){return this.win||window},overlaps:function(t){t.nodeType&&(t=e.Range(t).select(t));var n=this.compare("START_TO_START",t),r=this.compare("END_TO_END",t);return 0>=n&&r>=0?!0:n>=0&&this.compare("START_TO_END",t)<=0?!0:this.compare("END_TO_START",t)>=0&&0>=r?!0:!1},collapse:function(e){return this.range.collapse(void 0===e?!0:e),this},toString:function(){return"string"==typeof this.range.text?this.range.text:this.range.toString()},start:function(t){if(void 0===t){if(this.range.startContainer)return{container:this.range.startContainer,offset:this.range.startOffset};var n=this.clone().collapse().parent(),r=e.Range(n).select(n).collapse();return r.move("END_TO_START",this),{container:n,offset:r.toString().length}}if(this.range.setStart)if("number"==typeof t)this.range.setStart(this.range.startContainer,t);else if("string"==typeof t){var i=l(this.range.startContainer,this.range.startOffset,parseInt(t,10));this.range.setStart(i.node,i.offset)}else this.range.setStart(t.container,t.offset);else if("string"==typeof t)this.range.moveStart("character",parseInt(t,10));else{var o,a=this.start().container;"number"==typeof t?o=t:(a=t.container,o=t.offset);var s=e.Range(a).collapse();s.range.move(o),this.move("START_TO_START",s)}return this},end:function(t){if(void 0===t){if(this.range.startContainer)return{container:this.range.endContainer,offset:this.range.endOffset};var n=this.clone().collapse(!1).parent(),r=e.Range(n).select(n).collapse();return r.move("END_TO_END",this),{container:n,offset:r.toString().length}}if(this.range.setEnd)if("number"==typeof t)this.range.setEnd(this.range.endContainer,t);else if("string"==typeof t){var i=l(this.range.endContainer,this.range.endOffset,parseInt(t,10));this.range.setEnd(i.node,i.offset)}else this.range.setEnd(t.container,t.offset);else if("string"==typeof t)this.range.moveEnd("character",parseInt(t,10));else{var o,a=this.end().container;"number"==typeof t?o=t:(a=t.container,o=t.offset);var s=e.Range(a).collapse();s.range.move(o),this.move("END_TO_START",s)}return this},parent:function(){if(this.range.commonAncestorContainer)return this.range.commonAncestorContainer;var t=this.range.parentElement(),n=this.range;return o(t.childNodes,function(r){return e.Range(r).range.inRange(n)?(t=r,!1):void 0}),t},rect:function(t){var n=this.range.getBoundingClientRect();if(n.height||n.width||(n=this.range.getClientRects()[0]),"page"===t){var r=d();n=e.extend({},n),n.top+=r.top,n.left+=r.left}return n},rects:function(t){var n,r=e.map(e.makeArray(this.range.getClientRects()).sort(function(e,t){return t.width*t.height-e.width*e.height}),function(t){return e.extend({},t)}),i=0;for(r.length;i<r.length;){var o=r[i],a=!1;for(n=i+1;n<r.length;)if(u(o,r[n])){if(r[n].width){a=r[n];break}r.splice(n,1)}else n++;a?r.splice(i,1):i++}if("page"==t){var s=d();return e.each(r,function(e,t){t.top+=s.top,t.left+=s.left})}return r}}),function(){var r=e.Range.prototype,i=e.Range().range;r.compare=i.compareBoundaryPoints?function(e,t){return this.range.compareBoundaryPoints(this.window().Range[n(e)],t.range)}:function(e,n){return this.range.compareEndPoints(t(e),n.range)},r.move=i.setStart?function(e,t){var n=t.range;switch(e){case"START_TO_END":this.range.setStart(n.endContainer,n.endOffset);break;case"START_TO_START":this.range.setStart(n.startContainer,n.startOffset);break;case"END_TO_END":this.range.setEnd(n.endContainer,n.endOffset);break;case"END_TO_START":this.range.setEnd(n.startContainer,n.startOffset)}return this}:function(e,n){return this.range.setEndPoint(t(e),n.range),this};{var a=i.cloneRange?"cloneRange":"duplicate";i.selectNodeContents?"selectNodeContents":"moveToElementText"}r.clone=function(){return e.Range(this.range[a]())},r.select=i.selectNodeContents?function(e){if(e)this.range.selectNodeContents(e);else{var t=this.window().getSelection();t.removeAllRanges(),t.addRange(this.range)}return this}:function(e){if(e)if(3===e.nodeType){var t,n=e.parentNode,r=0;o(n.childNodes,function(n){return n===e?(t=r+n.nodeValue.length,!1):void(r+=n.nodeValue.length)}),this.range.moveToElementText(n),this.range.moveEnd("character",t-this.range.text.length),this.range.moveStart("character",r)}else this.range.moveToElementText(e);else this.range.select();return this}}();var o=function(e,t){for(var n,r=0;e[r];r++)if(n=e[r],3===n.nodeType||4===n.nodeType){if(t(n)===!1)return!1}else if(8!==n.nodeType&&o(n.childNodes,t)===!1)return!1},a=function(e){return 3===e.nodeType||4===e.nodeType},s=function(e,t){return function(n,r){return n[e]&&!r?a(n[e])?n[e]:arguments.callee(n[e]):n[t]?a(n[t])?n[t]:arguments.callee(n[t]):n.parentNode?arguments.callee(n.parentNode,!0):void 0}},c=s("firstChild","nextSibling"),g=s("lastChild","previousSibling"),l=function(e,t,n){var r=0>n?g:c;return a(e)?0>t+n?h(r(e),t+n):h(e,t+n):(e=e.childNodes[t]?e.childNodes[t]:e.lastChild,a(e)||(e=r(e)),h(e,n))},h=function(e,t){var n=0>t?g:c;for(t=Math.abs(t);e&&t>=e.nodeValue.length;)t-=e.nodeValue.length,e=n(e);return{node:e,offset:n===c?t:e.nodeValue.length-t}},f=function(e,t){return e.left<=t.clientX&&e.left+e.width>=t.clientX&&e.top<=t.clientY&&e.top+e.height>=t.clientY},u=function(e,t){return f(e,{clientX:t.left,clientY:t.top})&&f(e,{clientX:t.left+t.width,clientY:t.top})&&f(e,{clientX:t.left,clientY:t.top+t.height})&&f(e,{clientX:t.left+t.width,clientY:t.top+t.height})},d=function(e){var e=e||window;return doc=e.document.documentElement,body=e.document.body,{left:(doc&&doc.scrollLeft||body&&body.scrollLeft||0)+(doc.clientLeft||0),top:(doc&&doc.scrollTop||body&&body.scrollTop||0)+(doc.clientTop||0)}};return i.moveToPoint=!!e.Range().range.moveToPoint,e});
/*more*/
System.define("more","(function(){\n\n	/**\n	 * $('.body').more({\n	 *   moreHTML: \"<a href='javascript://' class='more'>...</a>\",\n	 *   moreWidth: 50,\n	 *   lessHTML: \" <a href='javascript://' class='less'>less</a>\",\n	 *   lines: 2\n	 * })\n	 */\n	\n\n	var stripHTML = function(html){ \n		var tmp = document.createElement(\"DIV\"); \n		tmp.innerHTML = html; \n		return tmp.textContent || tmp.innerText || \"\";\n	}\n\n	$.fn.more = function(options){\n		//return this\n		// setup defaults\n			options = $.extend({\n				lessHTML: \" <a href='javascript://' class='less'>-</a>\",\n				moreHTML: \" <a href='javascript://' class='more'>+</a>\",\n				moreWidth: 50,\n				lines: 2\n			}, options||{});\n			\n		this.each(function(el){\n			var $el = $(this), elementHTML, processedHTML, newHTML;\n\n			// skip if there is no content\n			if ( can.trim($el.text()).length == 0 ) {\n				return;\n			}\n			\n			// save current HTML for later\n			$el.data('originalHTML', $el.html())\n\n			/*$el.find('li').append('<span>[br]</span>');\n\n			$el.find('p, div, ul, ol, br').after('<span>[br]</span>');\n\n			newHTML = can.trim(stripHTML($el.text()).replace(/\\[br\\]/g, '<br>'));\n			newHTML = newHTML.replace(/&nbsp;/g, '').replace(/^<br>/, '').replace(/<br>[\\s\\S]*?<br>/g, '<br>');\n\n			$el.html('<p>' + newHTML + '</p>');*/\n\n			elementHTML = $el.html();\n\n			// the active range we will be moving around\n			var range = $el.range(),\n				// the end of the body's text for comparison\n				end = range.clone().collapse(false).start(\"-1\"),\n				// the start of the body's text for comparison\n				start = nextChar(range.collapse().end(\"+1\"),end).clone(),\n				// the current line's first character's coordinates\n				prevRect = start.rect(),\n				// how many lines we've come across\n				lines = 0,\n				prevStart;\n\n			// go until we reach the end of the body \n			while(range.compare(\"START_TO_START\",end) != 0){\n				range.end(\"+1\").start(\"+1\");\n				\n				var rect = range.rect();\n				// if the charcter is on a new line\n				if( rect && (rect.top -prevRect.top  > 4) ) {\n					lines++;\n					// quit on second line\n					if(lines == options.lines){\n						break;\n					}\n					prevStart = range.clone()\n					prevRect = rect;\n				}\n			}\n			\n			if(lines === options.lines){\n				// backup to previous line\n				range.end('-1').start('-1');\n			}\n			\n\n			// get the last visible text element\n			prevChar(range, start)\n			\n			var movedLeft = false,\n				offset = $el.offset(),\n				width = $el.width(),\n				rangeRect;\n			\n			// keep moving back until there is room for more\n			while(range.compare(\"START_TO_START\",start) != -1 ){\n				rangeRect = range.rect(true);\n				if(!rangeRect || rangeRect.left <= (offset.left+width-options.moreWidth) ) {\n					break;\n				}\n				movedLeft = true;\n				range.end(\"-1\").start(\"-1\")\n			}\n			// exit if we don't need to add more button\n			if(!movedLeft && (lines < options.lines ) ) {\n				return\n			}\n			\n			var parent = range.start().container;\n			// remove remaining text\n			if( parent.nodeType === Node.TEXT_NODE ||\n				parent.nodeType === Node.CDATA_SECTION_NODE ) {\n				parent.nodeValue = parent.nodeValue.slice(0,range.start().offset+1)\n			}\n			var removeAfter =  parent;\n			// remove everything after\n			while(removeAfter !== this && removeAfter){\n				var parentEl, childNodes, index;\n				parentEl = removeAfter.parentNode;\n				if(parentEl){\n					childNodes = parentEl.childNodes;\n					index = $.inArray(removeAfter,childNodes );\n					\n					for(var i = parentEl.childNodes.length-1; i > index; i--){\n						parentEl.removeChild( childNodes[i] );\n					}\n\n					\n				}\n\n				removeAfter = parentEl;\n			}\n			\n			// add more after parent\n			if( parent.nodeType === Node.TEXT_NODE ||\n				parent.nodeType === Node.CDATA_SECTION_NODE ) {\n				 parent = parent.parentElement\n			}\n			\n			$el.find('li').append('<span>[br]</span>');\n\n			$el.find('p, div, ul, ol, br').after('<span>[br]</span>');\n\n			newHTML = can.trim(stripHTML($el.text()).replace(/\\[br\\]/g, '<br>'));\n			newHTML = newHTML.replace(/&nbsp;/g, '')\n							 .replace(/^<br>/, '')\n							 .replace(/<br>$/, '')\n							 .replace(/<br>[\\s\\S]*?<br>/g, '<br>');\n\n			$el.html('<p>' + newHTML + options.moreHTML + '</p>');\n\n			$el.data('shortenedHTML',$el.html())\n				// show more / hide listeners\n				.on(\"click\",\"a.more\",function(){\n					$el.html($el.data('originalHTML')+options.lessHTML)\n				})\n				.on(\"click\",\"a.less\",function(){\n					$el.html($el.data('shortenedHTML'))\n				});\n		})\n	};\n	\n	// Moves the range until it hits something other than a space\n	var nextChar = function(range, boundary){\n		while(/[\\s\\n]/.test(range) && range.compare(\"END_TO_END\",boundary) != 1){\n			range.start(\"+1\").end(\"+1\")\n		}\n		return range;\n	};\n	// Moves the range until it hits something other than a space\n	var prevChar = function(range, boundary){\n		while(/[\\s\\n]/.test(range) && range.compare(\"START_TO_START\",boundary) != -1){\n			range.start(\"-1\").end(\"-1\")\n		}\n		return range;\n	}\n	\n})();\n");
/*can/map/delegate/delegate*/
define("can/map/delegate/delegate",["can/util/util","can/map/map"],function(t){var e=function(t,e){var a,r=t.length,l=0,s=[];for(l;r>l;l++){if(a=e[l],"string"!=typeof a)return null;if("**"===t[l])return e.join(".");if("*"===t[l])s.push(a);else{if(a!==t[l])return null;s.push(a)}}return s.join(".")},a=function(a,r,l,s,n){var i,u,c,h,p,o=r.split("."),g=(this._observe_delegates||[]).slice(0);a.attr=r,a.lastAttr=o[o.length-1];for(var d=0;i=g[d++];)if(!(a.batchNum&&i.batchNum===a.batchNum||i.undelegated)){h=void 0,p=!0;for(var v=0;v<i.attrs.length;v++)u=i.attrs[v],c=e(u.parts,o),c&&(h=c),u.value&&p?p=u.value===""+this.attr(u.attr):p&&i.attrs.length>1&&(p=void 0!==this.attr(u.attr));if(h&&p){var b=r.replace(h+".","");a.batchNum&&(i.batchNum=a.batchNum),"change"===i.event?(r=b,a.curAttr=h,i.callback.apply(this.attr(h),t.makeArray(arguments))):i.event===l?i.callback.apply(this.attr(h),[a,s,n,b]):"set"===i.event&&"add"===l&&i.callback.apply(this.attr(h),[a,s,n,b])}}};return t.extend(t.Map.prototype,{delegate:function(e,r,l){e=t.trim(e);for(var s,n=this._observe_delegates||(this._observe_delegates=[]),i=[],u=/([^\s=,]+)(?:=("[^",]*"|'[^',]*'|[^\s"',]*))?(,?)\s*/g;null!==(s=u.exec(e));)s[2]&&t.inArray(s[2].substr(0,1),['"',"'"])>=0&&(s[2]=s[2].substr(1,-1)),i.push({attr:s[1],parts:s[1].split("."),value:s[2],or:","===s[3]});return n.push({selector:e,attrs:i,callback:l,event:r}),1===n.length&&this.bind("change",a),this},undelegate:function(e,r,l){e=e&&t.trim(e);var s,n=0,i=this._observe_delegates||[];if(e)for(;n<i.length;)s=i[n],s.callback===l||!l&&s.selector===e?(s.undelegated=!0,i.splice(n,1)):n++;else i=[];return i.length||this.unbind("change",a),this}}),t.Map.prototype.delegate.matches=e,t.Map});
/*bithub/entities/entities*/
System.define("bithub/entities/entities","steal.apply(steal, (function(){\n	var files = ['chat', 'code', 'default', 'digest-list', 'event', 'issue', 'push', 'twitter'];\n	for(var i = 0; i < files.length; i++){\n		files[i] = './' + files[i] + '/' + files[i] + '.js';\n	}\n	return files;\n})())");
/*bithub/homepage/event_list/event_list*/
define("bithub/homepage/event_list/event_list",["can/can","bithub/homepage/event_list/views/init.mustache!","bithub/homepage/event_list/views/latest.mustache!","bithub/homepage/event_list/views/greatest.mustache!","bithub/homepage/event_list/determine_event_partial","bithub/homepage/event_list/handlers/handlers","ui/html_select/html_select","bithub/homepage/event_list/spinner/spinner","bithub/homepage/event_list/post_render/post_render","bithub/homepage/event_list/latest_events_sorter","bithub/models/event","bithub/models/upvote","bithub/models/award","bithub/helpers/fun_helpers","can/construct/proxy/proxy","bithub/helpers/mustacheHelpers","more/more","can/map/delegate/delegate","bithub/entities/entities"],function(e,t,n,i,o,r,a,s,u,l,d,p,h,c){var m=(_.compose(_.isEmpty,c.complement),["event","twitter","bug","comment","feature","question","article","plugin","app","uncategorized"]),v={},f=(e.Control.extend({init:function(){},"{day} types.chat add":function(){setTimeout(this.proxy("adjustScroll"),0)},adjustScroll:function(){this.element[0].scrollTop=this.element[0].scrollHeight}}),function(){return"latest"===e.route.attr("view")});return e.Control.extend({defaults:{}},{init:function(){var n=this;this.spinnerTop=e.compute(!1),this.spinnerBottom=e.compute(!1),this._canLoad=e.compute(!0),this._paginatorReady=e.compute(!0),this.__updateCounter=0,this.data={user:this.options.currentUser},this.element.html(t({doneLoading:function(){var e=!n._canLoad();return e&&!n.spinnerTop()&&!n.spinnerBottom()}})),new r(this.element,{currentUser:this.options.currentUser,modals:this.options.modals}),new s(this.element,{spinnerTop:this.spinnerTop,spinnerBottom:this.spinnerBottom}),new u(this.element),new a(this.element.find("#timespan-filter"),{items:[{value:"day",display:"Today"},{value:"week",display:"This Week"},{value:"month",display:"This Month"},{value:"all",display:"All Time"}],currentValue:function(){return e.route.attr("timespan")},onChange:function(t){e.route.attr("timespan",t)},show:function(){return"greatest"==e.route.attr("view")}}),new a(this.element.find("#issue-state-filter"),{items:[{value:"open",display:"Open"},{value:"closed",display:"Closed"},{value:"both",display:function(){return"All "+e.route.attr("category")+"s"}}],defaultValue:"open",currentValue:function(){return e.route.attr("state")||"open"},onChange:function(t){e.route.attr("state",t)},show:function(){return["bug","feature"].indexOf(e.route.attr("category"))>=0?(void 0==e.route.attr("state")&&e.route.attr("state","open"),!0):void 0}}),this.spinnerTop(!0)},"{preloadedEvents} add":function(){this.updateEvents(this.options.preloadedEvents)},"{can.route} view":"reload","{can.route} project":"reload","{can.route} category":"reload","{can.route} timespan":"reload","{can.route} state":"reload","{Bithub.Models.Event} reload":"reload",reload:function(){var e=this;this._loader&&(this._loader.abort&&this._loader.abort(),delete this._loader),window.scrollTo(0,0),this.setCanLoad(!0),this._paginatorReady(!1),this.spinnerTop(!0),this.__cleanup=function(){e.element.find(".events-list-wrapper").empty()},this.options.queryTracker.reset(function(){e._paginatorReady(!0),e.load(!0)})},"{window} onbottom":function(){return this._paginatorReady()&&!this._loader?(this.options.queryTracker.next(),this.options.queryTracker._onEndOfList()?void this.setCanLoad(!1):void(this._canLoad()&&(this.spinnerBottom(!0),this.load(!0)))):void 0},fillDocumentHeight:function(){var e=this;setTimeout(function(){var t=e.element.find(".events-list-wrapper").innerHeight(),n=$(window).height();e._canLoad()&&n+n/2>t&&$(window).trigger("onbottom")},1)},load:function(e){var t=this,n=e?"current":"next",i=f()?"Latest":"Greatest";window.EVENTS_PRELOADED&&(clearTimeout(this.loadTimeout),this.loadTimeout=setTimeout(function(){t.__cleanup&&(t.__cleanup(),delete t.__cleanup),t.spinnerBottom(!0),t._loader=d["find"+i](t.options.queryTracker[n](),t.proxy("updateEvents"))},10))},updateEvents:function(t){var r,a,s,u=this,d=e.extend({},this.data),p=new l,h=f()?n:i,c=[],g=!0;delete this._loader,(f()||0!==t.length)&&(this.options.queryTracker.hasNext()||0!==t.length)?0===t.length&&$(window).trigger("resetOnBottom"):this.setCanLoad(!1),f()&&p.appendEvents(t),e.extend(d,{eventList:f()?p:t}),console.time("renderEvents"),r=h({data:d,hasCategoryFilter:function(){return"all"!==e.route.attr("category")}},{dailyCategories:function(t){var n=t.context.attr("date");return e.map(m,function(e){var i=t.context.attr("types."+e);return i&&i.length?t.fn({currentCategory:e,events:i,date:n}):""}).join("")},entityComponent:function(t,n){var i=0;return"undefined"==typeof t.length&&(t=[t]),e.map(t,function(t){var r,a=o(t.attr("tags")),s='<{c} currentdate="date" event="event" inited="inited" user="user"></{c}>';return i%2===0&&(i=0,c.push(e.compute(!g))),"undefined"==typeof v[a]&&(v[a]=e.view.mustache(e.sub(s,{c:a}))),r=v[a].render({date:n,event:t,inited:c[c.length-1],user:u.options.currentUser}),i++,r}).join("")}}),a=function(){c.shift()(!0),c.length?setTimeout(a,1):setTimeout(s,1)},s=function(e){return function(){u.element&&e===u.__updateCounter&&(u.element.find((f()?".greatest":".latest")+"-group").remove(),u.element.find(".events-list-wrapper").append(r),console.timeEnd("renderEvents"),u.spinnerTop(!1),u.spinnerBottom(!1),u.postRendering(),u.fillDocumentHeight(),u.__updateCounter++)}}(this.__updateCounter),g=!1,c.length?a():s()},setCanLoad:function(e){this._canLoad(e)},postRendering:function(){this.element.trigger("rendered")}})});
/*bithub/event_details/event_details.mustache!can/view/mustache/system*/
define("bithub/event_details/event_details.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/event_details/event_details.mustache",e.Mustache(function(t,n){var s=[];return s.push('<div class="span9 offset1 clearfix">\n	'),s.push(e.view.txt(0,"div",0,this,e.Mustache.txt({scope:t,options:n},null,{get:"entityComponent"},{get:"event"}))),s.push("\n</div>\n"),s.join("")}))});
/*bithub/event_details/event_details*/
define("bithub/event_details/event_details",["can/can","bithub/models/event","./event_details.mustache!","bithub/homepage/event_list/determine_event_partial","bithub/homepage/event_list/handlers/handlers","bithub/helpers/mustacheHelpers"],function(e,t,n,a,i){return e.Control.extend({defaults:{}},{init:function(r,s){var d={user:s.currentUser,projects:s.projects,categories:s.categories,visibleTags:s.visibleTags};t.findOne({id:e.route.attr("id")},function(t){r.html(n({event:t,data:d},{entityComponent:function(t,n){return"undefined"==typeof t.length&&(t=[t]),e.map(t,function(t){var i=a(t.attr("tags")),r='<{c} currentdate="date" event="event" data="data"></{c}>';return e.view.mustache(e.sub(r,{c:i})).render({data:d,date:n,event:t})}).join("")}}))}),new i(this.element,s)}})});
/*bithub/homepage/sidebar/sidebar.mustache!can/view/mustache/system*/
define("bithub/homepage/sidebar/sidebar.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/homepage/sidebar/sidebar.mustache",t.Mustache(function(s,e){var n=[];return n.push('<div class="sidebar">\n  '),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"user.loggedInDelayed"},[{fn:function(s,e){var n=[];return n.push(' \n  <div clss="scorings logged-in">\n	<h4>Earn More Points</h4>\n	<div  class="well">\n	  <ul>'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"accomplishments.profile"},[{fn:function(s,e){var n=[];return n.push("		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"if"},{get:"twitter"},[{fn:function(){var t=[];return t.push('<li><a href="" id="connect-twitter">Connect your Twitter account</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"if"},{get:"github"},[{fn:function(){var t=[];return t.push('<li><a href="" id="connect-github">Connect your GitHub account</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"if"},{get:"completed"},[{fn:function(){var t=[];return t.push('<li><a href="/profile">Complete your profile</a></li>'),t.join("")}}]))),n.push("\n"),n.join("")}}]))),n.push("		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"pickUndone"},{get:"accomplishments.githubWatch"},[{fn:function(s,e){var n=[];return n.push('\n		<li><a href="http://github.com/bitovi/'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"picked"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push("Watch "),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"picked"}))),n.push(" on GitHub</a></li>"),n.push("\n"),n.join("")}}]))),n.push("		"),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"pickUndone"},{get:"accomplishments.twitterFollow"},[{fn:function(s,e){var n=[];return n.push('		<li><a href="http://twitter.com/'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"picked"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push("Follow "),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"picked"}))),n.push(" on Twitter</a></li>"),n.push("\n"),n.join("")}}]))),n.push('	  </ul>\n	  <p>\n		Get points and votes for content you post, bug fixes, for sharing your apps or plugins and for attending events. <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"routes.earnpoints"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push('Details &raquo;</a>\n	  </p>\n	</div>\n  </div>\n  <div class="awards free-stuff logged-in">\n	<h4><a href="/rewards">Rewards</a></h4>\n	<div class="rewards">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"iterRewards"},[{fn:function(s,e){var n=[];return n.push('	  <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"rewardsUrl"}))),n.push('" class="item '),n.push(t.view.txt(!0,"a","class",this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.status_cssClass"}))),n.push('" '),n.push(t.view.txt(0,"a",1,this,t.Mustache.txt({scope:s,options:e},"#",{get:"reward.thumbUrl"},[{fn:function(s,e){var n=[];return n.push(""),n.push(t.view.txt(2,"a","style",this,function(){var n=[];return n.push('style="'),n.push("background-image: url("),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push(')"'),n.join("")})),n.join("")}}]))),n.push("",t.view.pending({scope:s,options:e}),">"),n.push('\n		<span class="points">'),n.push(t.view.txt(0,"span",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"reward.display_point_minimum"},[{fn:function(s,e){var n=[];return n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.display_point_minimum"}))),n.join("")}},{inverse:function(s,e){var n=[];return n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.point_minimum"}))),n.join("")}}]))),n.push("</span>\n		"),n.push(t.view.txt(0,"a",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"reward.status_message"},[{fn:function(s,e){var n=[];return n.push('<span class="message">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.status_message"}))),n.push("</span>"),n.join("")}}]))),n.push("\n	  </a>"),n.push("\n"),n.join("")}}]))),n.push("	</div>\n  </div>\n  \n  "),n.join("")}},{inverse:function(s,e){var n=[];return n.push('\n  \n  <a href="/earnpoints" class="scorings logged-out">\n	<h4 class="handwriting leftalign"><span class="leftarrow">&darr;</span> Earn Points</h4>\n	<div class="well">\n	  <ul class="">\n		<li>Login &amp; create an account <span class="badge">+1</span></li>\n		<li>Follow us on Twitter/Github <span class="badge">+1 each</span></li>\n		<li>Post content & get votes <span class="badge">+2 + votes</span></li>\n	  </ul>\n	  <p>Get points for content, bug fixes, and sharing your apps or plugins.</p>\n	</div>\n  </a>\n  <div class="awards free-stuff logged-out">\n	<h4 class="handwriting rightalign"><a href="/rewards">Get Free Stuff <span class="rightarrow"></span></a></h4>\n	<div class="rewards">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"iterRewards"},[{fn:function(s,e){var n=[];return n.push('	  <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"rewardsUrl"}))),n.push('" class="item" '),n.push(t.view.txt(0,"a",1,this,t.Mustache.txt({scope:s,options:e},"#",{get:"reward.thumbUrl"},[{fn:function(s,e){var n=[];return n.push(""),n.push(t.view.txt(2,"a","style",this,function(){var n=[];return n.push('style="'),n.push("background-image: url("),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push(')"'),n.join("")})),n.join("")}}]))),n.push("",t.view.pending({scope:s,options:e}),">"),n.push('\n		<span class="points">'),n.push(t.view.txt(0,"span",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"reward.display_point_minimum"},[{fn:function(s,e){var n=[];return n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.display_point_minimum"}))),n.join("")}},{inverse:function(s,e){var n=[];return n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"reward.point_minimum"}))),n.join("")}}]))),n.push("</span>\n	  </a>"),n.push("\n"),n.join("")}}]))),n.push('	</div>\n	<div class="btn-wrapper logged-out"><button href="#login-modal" role="button" data-toggle="modal" class="btn-signup">Sign Up Now</button></div>\n  </div>'),n.push("\n"),n.join("")}}]))),n.push('  <div id="leaderboard" class="leaderboard logged-out logged-in"></div>'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"meetups.length"},[{fn:function(s,e){var n=[];return n.push('		<div class="visit-meetups">\n			<h4><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"routes.meetups"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push('Upcoming Events</a></h4>\n			<div>\n				<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"meetups"},[{fn:function(s,e){var n=[];return n.push('						<li class="clearfix">\n							<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"url"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push('\n								<span class="event-date-wrap">\n									'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"prettifyTs"},{get:"props.scheduled_at"},"date"))),n.push("\n								</span>\n								<strong>\n									"),n.push(t.view.txt(0,"strong",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"v"},{get:"title"}))),n.push("\n								</strong>\n							</a>\n							<br>\n							"),n.push(t.view.txt(1,"br",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"v"},{get:"props.location"}))),n.push("\n						</li>"),n.push("\n"),n.join("")}}]))),n.push("				</ul>\n			</div>\n		</div>"),n.push("\n"),n.join("")}}]))),n.push("</div>"),n.join("")}))});
/*bithub/homepage/sidebar/leaderboard/leaderboard.mustache!can/view/mustache/system*/
define("bithub/homepage/sidebar/leaderboard/leaderboard.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/homepage/sidebar/leaderboard/leaderboard.mustache",t.Mustache(function(s,e){var n=[];return n.push('<h4><a href="/leaderboard">Leaderboard</a></h4>\n<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"loop"},{get:"users"},{___h4sh:{length:{get:"topLength"}}},[{fn:function(s,e){var n=[];return n.push('	<li class="'),n.push(t.view.txt(!0,"li","class",this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"loggedIn"},[{fn:function(){var t=[];return t.push("active"),t.join("")}}]))),n.push(' clearfix"',t.view.pending({scope:s,options:e}),">"),n.push('\n	<span class="position pull-left">#'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"__index"}))),n.push("</span>\n	"),n.push(t.view.txt(0,"li",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"avatarUrl"},[{fn:function(s,e){var n=[];return n.push('<img class="avatar pull-left" '),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push('?size=22"'),n.join("")})),n.push(' alt="avatar" '),n.push(t.view.txt(1,"img",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"imgErrorHandler"}))),n.push("",t.view.pending({scope:s,options:e}),">"),n.join("")}}]))),n.push('\n	<span class="leaderboard-name pull-left">\n		<a href="/profile/'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"id"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push('</a>\n	</span>\n	<span class="points pull-right">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"score"}))),n.push("</span>\n	</li>"),n.push("\n"),n.join("")}}]))),n.push("</ul>"),n.push("\n"),n.push(t.view.txt(0,"/ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"breakAt"},[{fn:function(s,e){var n=[];return n.push('	<ul class="unstyled"><li class="lightning-divider"></li></ul>\n	<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"loop"},{get:"users"},{___h4sh:{begin:{get:"breakAt"},length:3}},[{fn:function(s,e){var n=[];return n.push('	<li class="'),n.push(t.view.txt(!0,"li","class",this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"loggedIn"},[{fn:function(){var t=[];return t.push("active"),t.join("")}}]))),n.push(' clearfix"',t.view.pending({scope:s,options:e}),">"),n.push('\n		<span class="position pull-left">#'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"__index"}))),n.push("</span>\n		"),n.push(t.view.txt(0,"li",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"avatarUrl"},[{fn:function(s,e){var n=[];return n.push('<img class="avatar pull-left" '),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push('?size=22"'),n.join("")})),n.push(' alt="avatar" '),n.push(t.view.txt(1,"img",1,this,t.Mustache.txt({scope:s,options:e},null,{get:"imgErrorHandler"}))),n.push("",t.view.pending({scope:s,options:e}),">"),n.join("")}}]))),n.push('\n		<span class="leaderboard-name pull-left">\n			<a href="/profile/'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"id"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push('</a>\n		</span>\n		<span class="points pull-right">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"score"}))),n.push("</span>\n	</li>\n	"),n.join("")}}]))),n.push("  \n	</ul>"),n.join("")}}]))),n.join("")}))});
/*bithub/models/identity*/
define("bithub/models/identity",["can/can"],function(i){var t=i.Model("Bithub.Models.Identity",{destroy:"DELETE /api/auth/unlink_identity/{uid}"},{identifier:function(){var i=this.attr("source_data");return i.nickname||i.name||i.email}});return t});
/*bithub/helpers/auth*/
define("bithub/helpers/auth",[],function(){return{login:function(t){var e=this,o=t.windowPropsStr?t.windowPropsStr:"width=600,height=300,scrollbars=yes",n=t.title?t.title:"Oauth login",i=t.url?t.url:"/api/auth/github";oauthWindow=window.open(i,n,o),oauthWindowSweeper=window.setInterval(function(){oauthWindow.closed&&(window.clearInterval(oauthWindowSweeper),e.fromSession())},500)},logout:function(){var t=this,e=["loggedInDelayed","authStatus"];$.get("/api/auth/logout",function(){setTimeout(function(){for(var o in t.attr())e.indexOf(o)<0&&t.removeAttr(o)},500),"homepage"!=can.route.attr("page")&&can.route.attr({page:"homepage",view:"latest"})})}}});
/*bithub/helpers/github*/
define("bithub/helpers/github",[],function(){var n=function(n){return n?_.map(n.split(","),function(n){var e=n.split(";"),r={url:e.shift().trim().slice(1,-1)};return _.each(e,function(n){var e=n.split("="),i=e[0].trim(),t=e[1].trim().slice(1,-1);r[i]=t}),r}):void 0},e=function(r,i,t){$.ajax({url:r,headers:{},success:function(r,u,a){var c=n(a.getResponseHeader("Link")),o=_.find(c,function(n){return"next"==n.rel?!0:void 0});i.push.apply(i,_.map(r,function(n){return{id:n.id,name:n.name,full_name:n.full_name}})),o?e(o.url,i,t):t(i)}})},r=function(n){var e=_.chain(n.split(" ")).map(function(n){return n.match(/(.+)\/(.+)/i)}).compact().first();return e?{org:e[1].replace(".","_"),repo:e[2].replace(".","_")}:void 0};return{query:function(n,r){e(n,[],r)},matchRepo:r}});
/*bithub/helpers/data*/
define("bithub/helpers/data",["vendor/lodash/lodash"],function(){var n=function(n){return null===n||void 0===n},e=function(e){return _.each(_.keys(e),function(u){n(e[u])&&(e[u]="")}),e};return{cleanup:e}});
/*bithub/models/user*/
define("bithub/models/user",["can/can","./identity","../helpers/auth","../helpers/github","../helpers/data","can/map/delegate/delegate","can/map/attributes/attributes"],function(t,i,e,n,a){var r={twitter:{url:"/api/auth/twitter"},github:{url:"/api/auth/github"},meetup:{url:"/api/auth/meetup"}},s=function(t,i,e,n){var a=this,r=300,n=this.loggedIn();n===!0?$(".logged-out").fadeOut(r):$(".logged-in").fadeOut(r),setTimeout(function(){a.attr("loggedInDelayed",n),n===!0?$(".logged-in").fadeIn(r):$(".logged-out").fadeIn(r)},r-10)},o=t.Model.extend("Bithub.Models.User",{findAll:"GET  /api/v1/users",findOne:"GET  /api/v1/users/{id}",update:"PUT  /api/v1/users/{id}",destroy:"DELETE  /api/v1/users/{id}",attributes:{identities:i}},{serialize:function(){var t=this._super();return delete t.achievements,delete t.identities,delete t.activites,t},fromSession:function(){var i=this;this.delegate("authStatus","change",t.proxy(s,this)),this.loadSession(function(t){i.attr(a.cleanup(t)),i.attr("authStatus","loggedIn"),i.executeDelayedActions()},function(t){i.attr("authStatus","loggedOut"),console.error(t)})},refreshSession:function(){var t=this;this.loadSession(function(i){t.attr(a.cleanup(i))})},loadSession:function(i,e){t.ajax({url:"/api/auth/session",type:"GET"}).done(i).fail(e)},loggedIn:function(t){return void 0==t?"loggedIn"==this.attr("authStatus"):void(["twitter","github","meetup"].indexOf(t)>=0?this.login(t):0==t&&this.logout())},delayedActions:function(i){return"function"==typeof i&&(t.isArray(this.attr("delayedActions"))?this.attr("delayedActions").push(i):this.attr("delayedActions",[i])),this.attr("delayedActions")},executeDelayedActions:function(){if(this.attr("delayedActions"))for(;this.attr("delayedActions").length;)this.attr("delayedActions").shift()()},loggingIn:function(){return"loggingIn"==this.attr("authStatus")},isAdmin:function(){return _.include(this.attr("roles"),"admin")},hasEmail:function(){return!!this.attr("email")},hasAddress:function(){return!!(this.attr("address")&&this.attr("postal")&&this.attr("country")&&this.attr("city"))},isCompleted:function(){return this.hasEmail()&&this.hasAddress()},getIdentity:function(t){return _.first(_.filter(this.attr("identities"),function(i){return i.attr("provider")==t}))},login:function(t){this.attr("authStatus","loggingIn"),e.login.call(this,r[t])},logout:function(){this.attr("authStatus","loggedOut"),e.logout.apply(this)},manageRoles:function(i,e){var n=this;t.ajax({url:"/api/users/"+n.id+"/"+i+"role",data:{role:e},type:"PUT"}).done(function(t){n.attr("roles",t.roles)})},rolesCsv:function(){return(this.attr("roles")||[]).join(", ")},filterActivities:function(t,i){var e=_.filter(this.attr("activities"),t);return i?_.pluck(e,i):e},watchedRepos:function(){return t.map(t.grep(this.attr("activities")||[],function(t){var i=t.attr("title");return i&&0==i.indexOf("started watching bitovi/")}),function(t){return t.attr("title").match(/.*bitovi\/(.*).*/i)[1]})},followedAccounts:function(){var t=this.filterActivities(function(t){return t.attr("title")&&0==t.attr("title").indexOf("followed @")?t:void 0},"title");return _.map(t,function(t){return t.split("@")[1]})},isLoggedIn:function(){return"loggedIn"===this.attr("authStatus")},hasVotedFor:function(t){return t?(this.attr("upvoted_events")||[]).indexOf(t.attr("id"))>-1:!1},loadActivities:function(){var t=this;Bithub.Models.Activity.findAll({userId:this.attr("id"),limit:1e5},function(i){t.attr("activities",t.attr("activities")||[]),t.attr("activities").replace(i)})},avatarUrl:function(){var t,i,e="/assets/images/icon-user.png",n=this.attr("avatar_url")||e;if(n===e){t=this.attr("identities")||[];for(var a=0;a<t.length;a++)if(i=t[a].source_data||{},n=i.image||i.avatar_url||i.profile_image_url||e,n!==e)return n}return n}});return t.Model.List("Bithub.Models.User.List",{topUser:function(){if(0!=this.attr("length")){var t=this.attr(0);return this.each(function(i){i.attr("score")>t.attr("score")&&(t=i)}),t}}}),o});
/*bithub/homepage/sidebar/leaderboard/leaderboard*/
define("bithub/homepage/sidebar/leaderboard/leaderboard",["can/can","./leaderboard.mustache!","bithub/models/user","can/map/delegate/delegate","bithub/helpers/mustacheHelpers"],function(e,t){var r=e.compute(function(e){var t=0;return e&&(t=e),t}),n=e.compute(function(){return r()>7?r()-1:0}),s=e.compute(function(){return r()<6?6:r()<8?r():5});return e.Control.extend({defaults:{}},{init:function(e,r){this.users=new Bithub.Models.User.List,this.setFilteredUsers(),this.element.html(t({users:this.users,currentUser:r.currentUser,breakAt:n,topLength:s}))},"{currentUser} loggedInDelayed change":function(e,t,n,s,i){1==i?this.determineRank():r(0)},"{users} length":function(){this.setFilteredUsers(),this.determineRank()},setFilteredUsers:function(){this.users.replace(e.grep(this.options.users,function(e){return!e.attr("roles")||0===e.attr("roles").length}))},determineRank:function(){var e=this;this.users.each(function(t,n){e.options.currentUser.attr("id")===t.attr("id")&&(t.attr("loggedIn",!0),r(n))})}})});
/*bithub/models/reward*/
define("bithub/models/reward",["can/can","can/list/list","vendor/moment/moment"],function(t){REWARD_STATUSES={ACHIEVED:1,SHIPPED:2};var a=function(t,a){var r=t.split("/");return r[r.length-1]=a+"_"+r[r.length-1],r.join("/")},r=t.Model("Bithub.Models.Reward",{findAll:"GET /api/v1/rewards",findOne:"GET /api/v1/rewards/{id}",create:"POST /api/v1/rewards",update:"PUT /api/v1/rewards/{id}",destroy:"DELETE /api/v1/rewards/{id}"},{init:function(){this.attr("imageUrlBasedOnShipping",this.imageUrl())},imageUrl:function(){return a(this.attr("original_image_url"),"240x240")},thumbUrl:function(){return a(this.attr("original_image_url"),"124x124")},greyscaleImageUrl:function(){return a(this.attr("original_image_url"),"240x240,greyscale")}});return t.Model.List("Bithub.Models.Reward.List",{matchAchievements:function(a,r){var e=this,i=-1,s=a.attr("score"),n=a.attr("achievements")||new t.List,l=REWARD_STATUSES,d={},o={};n.each(function(t){var a=t.attr("shipped_at");a?(d[t.reward_id]=l.SHIPPED,o[t.reward_id]=moment(a).format("MM/DD/YY")):d[t.reward_id]=l.ACHIEVED}),_.each(e,function(t,a){var r=d[t.attr("id")],e=t.attr("point_minimum");r===l.SHIPPED?(i=a,t.attr({status_cssClass:"achieved",status_inlineMessage:"Shipped "+o[t.id],imageUrlBasedOnShipping:t.greyscaleImageUrl()})):r===l.ACHIEVED||s>=e?(i=a,t.attr({status_cssClass:"shipping",status_message:"Shipping soon!",imageUrlBasedOnShipping:t.imageUrl()})):t.attr({imageUrlBasedOnShipping:t.imageUrl()})}),i<e.attr("length")-1&&(e[i+1].attr({status_cssClass:"almost",status_message:e.attr(i+1).attr("point_minimum")-a.attr("score")+" points to go!"}),i+1==e.attr("length")-1&&r.attr("length")&&e.attr(i+1+".status_message",r.topUser().attr("score")-a.attr("score")+" points to go!"))},nextRewardIdx:function(t){if(!t||!t.attr("length"))return 0;var a=this,r=0,e=_.last(t).attr("reward_id");return _.each(a,function(t,a){t.attr("id")==e&&(r=a)}),r==t.attr("length")-1?r:r+1}}),r});
/*bithub/models/accomplishment*/
define("bithub/models/accomplishment",["can/can","can/list/list","vendor/moment/moment"],function(t){var i=t.Model("Bithub.Models.Accomplishment",{findAll:"GET /api/v1/users/{userId}/accomplishments"},{});return i.List=i.List.extend({},{filterActivities:function(t,i){var e=_.filter(this,t);return i?_.pluck(e,i):e},watchedRepos:function(){return t.map(t.grep(this||[],function(t){var i=t.attr("title");return i&&0==i.indexOf("started watching bitovi/")}),function(t){return t.attr("title").match(/.*bitovi\/(.*).*/i)[1]})},followedAccounts:function(){var t=this.filterActivities(function(t){return t.attr("title")&&0==t.attr("title").indexOf("followed @")?t:void 0},"title");return _.map(t,function(t){return t.split("@")[1]})}}),i});
/*bithub/homepage/sidebar/sidebar*/
define("bithub/homepage/sidebar/sidebar",["can/can","./sidebar.mustache!","bithub/homepage/sidebar/leaderboard/leaderboard","bithub/models/reward","bithub/models/accomplishment","bithub/helpers/mustacheHelpers"],function(t,e,r,n,i){var s=new t.Observe({profile:{github:!1,twitter:!1,completed:!1},twitterFollow:{bitovi:!1,canjs:!1,jquerypp:!1,javascriptmvc:!1,funcunit:!1},githubWatch:{javascriptmvc:!1,canjs:!1,jquerypp:!1,funcunit:!1,documentjs:!1,steal:!1}});return t.Control.extend({defaults:{rewards:new Bithub.Models.Reward.List}},{init:function(i,o){var a=this,u=this.options.rewards,c=this.options.currentUser;this.meetupList=new t.List,n.findAll({order:"point_minimum"},function(t){var e=_.filter(t,function(t){return t.disabled_ts?void 0:t});u.replace(e),a.onLogin()}),i.html(e({user:c,rewards:u,accomplishments:s,meetups:this.meetupList,routes:{earnpoints:t.route.url({page:"earnpoints"},!1),rewards:t.route.url({page:"rewards"},!1),meetups:t.route.url({category:"event"},!0)}},{helpers:{pickUndone:function(e,r){e="function"==typeof e?e():e;var n=_.filter(_.keys(e.attr()),function(t){return e.attr(t)?void 0:t});return n.length>0?r.fn(t.extend(this,{picked:n[_.random(0,n.length-1)]})):r.inverse(this)},iterRewards:function(e){var r="",n=t.route.url({page:"rewards"}),i=0,s=2;c.loggedIn()?(i=u.nextRewardIdx(c.attr("achievements")),i>=u.attr("length")-1&&i--,s=i+2):u.attr("length")>2&&(i=_.random(0,u.attr("length")-2),s=i+2),u.attr("length");for(var o=i;s>o;o++)r+=e.fn({reward:u.attr(o),rewardsUrl:n});return r}}})),o.currentUser.loggedIn()&&this.matchRewards(),new r(i.find("#leaderboard"),o),Bithub.Models.Event.findLatest({category:"event",limit:3,order:"thread_updated_ts:asc",in_future:!0}).then(this.proxy("fillEvents"))},fillEvents:function(t){this.meetupList.replace(t)},"{currentUser} authStatus":function(){this.options.currentUser.loggedIn()&&this.onLogin()},"{rewards} length":"onLogin","{users} length":"onLogin",onLogin:function(){this.matchRewards(),i.findAll({userId:this.options.currentUser.id},this.proxy("updateAccomplishments"))},matchRewards:function(){this.options.rewards.matchAchievements(this.options.currentUser,this.options.users)},updateAccomplishments:function(t){var e=this.options.currentUser;s.attr("profile.twitter",e.getIdentity("twitter")?!0:!1),s.attr("profile.github",e.getIdentity("github")?!0:!1),s.attr("profile.completed",e.isCompleted()),_.each(t.followedAccounts(),function(t){s.attr("twitterFollow."+t,!0)}),_.each(t.watchedRepos(),function(t){s.attr("githubWatch."+t,!0)})},"#twitter-modal-btn click":function(t,e){e.preventDefault(),this.options.modals.showTwitter()},"#show-new-post-form-btn click":function(t,e){e.preventDefault(),this.options.newpostVisibility(!0)},"#connect-github click":function(t,e){e.preventDefault(),this.options.currentUser.login("github")},"#connect-twitter click":function(t,e){e.preventDefault(),this.options.currentUser.login("twitter")}})});
/*bithub/homepage/homepage*/
define("bithub/homepage/homepage",["can/can","bithub/homepage/homepage.mustache!","bithub/homepage/event_list/event_list","bithub/event_details/event_details","bithub/homepage/sidebar/sidebar","bithub/helpers/fun_helpers"],function(e,n,t,i,a,o){var r,h=_.compose(_.isUndefined,o.complement),s=function(e){return"details"===e?i:t};return e.Control.extend({pluginName:"homepage",defaults:{buffers:{}}},{init:function(t,i){this.element.html(n()),this.initControl(e.route.attr("view")),new a(t.find("#sidebar-container"),this.options);var o=i.currentUser.loggedIn();h(o)&&i.currentUser._triggerChange("loggedIn","change",o,o)},"{can.route} view":function(e,n,t){this.initControl(t)},"{can.route} page":function(e,n,t){" homepage"!=t&&(r=null)},initControl:function(e){var n=s(e);if(r!=n){r=n;{var t=$("<div/>").addClass("clean");this.element.find("#main-container").html(t)}new n(this.element.find("#main-container > div"),this.options)}}})});
/*bithub/profile/earnpoints/init.mustache!can/view/mustache/system*/
define("bithub/profile/earnpoints/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/profile/earnpoints/init.mustache",t.Mustache(function(s,e){var n=[];return n.push('<div class="row">\n	<div class="span8 earn-points">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"if"},{get:"user.isLoggedIn"},[{fn:function(){var t=[];return t.push("	<h1>Support open source. Get free stuff.</h1>\n	<p>\n		Contribute to our open source community and be awarded with free stuff. Tweet, comment, fix bugs. The community votes for you and you get awesome rewards for helping.\n	</p>"),t.push("\n"),t.join("")}}]))),n.push('	<h2>Earn Points</h2>\n	<div class="points-wrapper">\n		<h3><i class="icon-user"></i> Your Account <span class="badge">+1 each</span></h3>'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"hasProvider"},"twitter",[{fn:function(){var t=[];return t.push('		<p class="complete"><i class="icon-check"></i> Connect your Twitter account</p>\n		'),t.join("")}},{inverse:function(){var t=[];return t.push('\n		<p><a href="javascript://" id="connect-twitter-link">Connect your Twitter account</a></p>'),t.push("\n"),t.join("")}}]))),n.push("		"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"hasProvider"},"github",[{fn:function(){var t=[];return t.push('\n		<p class="complete"><i class="icon-check"></i> Connect your GitHub account</p>\n		'),t.join("")}},{inverse:function(){var t=[];return t.push('\n		<p><a href="javascript://" id="connect-github-link">Connect your GitHub account</a></p>'),t.push("\n"),t.join("")}}]))),n.push("		"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"hasProvider"},"meetup",[{fn:function(){var t=[];return t.push('\n		<p class="complete"><i class="icon-check"></i> Connect your Meetup account</p>\n		'),t.join("")}},{inverse:function(){var t=[];return t.push('\n		<p><a href="javascript://" id="connect-meetup-link">Connect your Meetup account</a></p>'),t.push("\n"),t.join("")}}]))),n.push("		"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"user.isCompleted"},[{fn:function(){var t=[];return t.push('\n		<p class="complete"><i class="icon-check"></i> Complete your profile</p>\n		'),t.join("")}},{inverse:function(){var t=[];return t.push('\n		<p><a href="/profile">Complete your profile</a></p>'),t.push("\n"),t.join("")}}]))),n.push('	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-heart"></i> Follow Us <span class="badge">+ 1 each</span></h3>\n		<p>Follow these Twitter accounts:</p>\n		<div class="twitter-accounts">\n		<a href="https://twitter.com/bitovi" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @bitovi</a>\n		<a href="https://twitter.com/canjs" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @canjs</a>\n		<a href="https://twitter.com/jquerypp" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @jquerypp</a>\n		<a href="https://twitter.com/javascriptmvc" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @javascriptmvc</a>\n		<a href="https://twitter.com/funcunit" class="twitter-follow-button" data-show-count="false">Follow @funcunit</a>\n		</div>\n		<script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?\'http\':\'https\';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+\'://platform.twitter.com/widgets.js\';fjs.parentNode.insertBefore(js,fjs);}}(document, \'script\', \'twitter-wjs\');</script>\n	</div>\n	<div class="points-wrapper">\n		<p>Watch these repos on Github:</p>\n		<ul class="repos">\n		'),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"javascriptmvc",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> javascriptmvc</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/javascriptmvc">javascriptmvc</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"canjs",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> canjs</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/canjs">canjs</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"jquerypp",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> jquerypp</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/jquerypp">jquerypp</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"funcunit",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> funcunit</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/funcunit">funcunit</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"documentjs",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> documentjs</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/documentjs">documentjs</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"steal",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> steal</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/steal">steal</a></li>'),t.join("")}}]))),n.push("\n		"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"isWatched"},"testee_js",[{fn:function(){var t=[];return t.push('<li class="complete"><i class="icon-check"></i> testee</li>'),t.join("")}},{inverse:function(){var t=[];return t.push('<li>&bull; <a href="https://github.com/bitovi/testee.js">testee</a></li>'),t.join("")}}]))),n.push('\n		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-twitter"></i> Twitter <span class="badge">+ 1 + votes</span></h3>\n		<p><a href="https://twitter.com/intent/tweet?screen_name=bitovi" target="_blank">Tweet about us &raquo</a></p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.twitter"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-bug"></i> Issues <span class="badge">+ 2 + votes</span></h3>\n		<p><a href="https://github.com/bitovi">Report issues on GitHub</a> that are tagged by an admin as a Bug or Feature</p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.bug"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-code"></i> Code <span class="badge">+ votes x2</span></h3>\n		<p><a href="https://github.com/bitovi">Request a pull on GitHub</a> for fixing a bug or adding a feature that\'s merged by and admin</p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.code"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-article"></i> Articles <span class="badge">+ 2 + votes</span></h3>\n		<p>Share you tips, tricks, and JavaScript advice with the community. <a href="javascript://" id="submit-article-link">Submit an article &raquo;</a></p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.article"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-app"></i> Apps <span class="badge">+ 2 + votes</span></h3>\n		<p>Post your projects, apps, and websites that user our stuff. <a href="javascript://" id="submit-app-link">Post your app &raquo;</a></p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.app"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-plugin"></i> Plugins <span class="badge">+ 2 + votes</span></h3>\n		<p>Share extended functionally with everyone. <a href="javascript://" id="submit-plugin-link">Share your plugin &raquo;</a></p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.plugin"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	<div class="points-wrapper">\n		<h3><i class="icon-plugin"></i> Events <span class="badge">+ 2 + votes</span></h3>\n		<p>Host an event for the community. <a href="javascript://" id="submit-event-link">Create an event &raquo;</a></p>\n		<ul class="toprated unstyled clearfix">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"events.event"},[{fn:function(s,e){var n=[];return n.push('		<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n	</div>\n	</div>\n	<div class="span4">\n	<div class="sidebar">\n		<div class="widget">\n		<h2>Get Votes</h2>\n		<p>All the content you post can be upvoted by community.</p>'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"^",{get:"if"},{get:"user.isLoggedIn"},[{fn:function(){var t=[];return t.push('		<div class="btn-wrapper logged-out">\n		<button href="#login-modal" role="button" data-toggle="modal" class="btn-signup">Sign Up Now</button>\n		</div>'),t.push("\n"),t.join("")}}]))),n.push('			</div>\n		<div class="toprated widget clearfix">\n		<h4>'),n.push(t.view.txt(0,"h4",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"user.isLoggedIn"},[{fn:function(){var t=[];return t.push("Your"),t.join("")}}]))),n.push(' Top-Rated Posts</h4>\n		<ul class="unstyled">'),n.push("\n"),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"posts"},[{fn:function(s,e){var n=[];return n.push('			<li class="clearfix"><span class="points">'),n.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"upvotes"}))),n.push('</span> <a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"eventDetailsUrl"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"stripHtml"},{get:"title"}))),n.push("</a></li>"),n.push("\n"),n.join("")}}]))),n.push('		</ul>\n		</div>\n		<div class="leaderboard"></div>\n	</div>\n	</div>\n</div>'),n.join("")}))});
/*bithub/profile/earnpoints/earnpoints*/
define("bithub/profile/earnpoints/earnpoints",["can/can","./init.mustache!","bithub/homepage/sidebar/leaderboard/leaderboard"],function(t,e,i){var n=new t.List,s=(new t.List,{twitter:new t.List,bug:new t.List,feature:new t.List,code:new t.List,article:new t.List,app:new t.List,plugin:new t.List,event:new t.List}),r=new t.List,o={order:["upvotes:desc","thread_updated_ts:desc"],limit:3},u={order:["upvotes:desc","thread_updated_ts:desc"],limit:5};return t.Control.extend({defaults:{loaded:t.compute(_.keys(s).length)}},{init:function(o,u){var a=u.currentUser;a.isLoggedIn()&&a.loadActivities(),this.element.html(e({user:a,events:s,posts:r},{helpers:{hasProvider:function(t,e){return a.getIdentity(t)?e.fn(this):e.inverse(this)},isWatched:function(t,e){return n.attr("length"),_.filter(n,function(e){return e==t}).length?e.fn(this):e.inverse(this)},eventDetailsUrl:function(){return t.route.url({page:"eventdetails",id:this.attr("id")})}},partials:{}})),new i(o.find(".leaderboard"),{currentUser:u.currentUser,users:u.users}),this.loadTopPosts(),a.isLoggedIn()&&(this.loadGithub(),this.loadEvents())},"{loaded} change":function(t,e,i){var n=this.options.loaded;0==i&&(this.mergeList(s.bug,s.feature,function(t,e){return t.attr("upvotes")-e.attr("upvotes")}),n(_.keys(s).length))},"{currentUser} authStatus":function(){this.options.currentUser.isLoggedIn()&&this.options.currentUser.loadActivities()},"{currentUser} activities":function(){var t=this;setTimeout(function(){t.loadGithub(),t.loadEvents(),t.loadTopPosts()},1)},loadGithub:function(){n.replace(this.options.currentUser.watchedRepos())},loadEvents:function(){var e=this.options.loaded;o.author_id=this.options.currentUser.attr("id"),_.each(_.keys(s),function(i){var n=t.extend({category:i},o);"event"===i&&(n.order="thread_updated_ts:asc",n.host_id=n.author_id,delete n.author_id),Bithub.Models.Event.findAll(n,function(t){s[i].replace(t),e(e()-1)})})},mergeList:function(e,i,n){i.forEach(function(i){-1===t.inArray(i,e)&&e.push(i)}),[].sort.call(e,n)},loadTopPosts:function(){var e=this.options.currentUser,i=t.extend({},u);e.isLoggedIn()&&(i.author_id=e.attr("id")),Bithub.Models.Event.findAll(i,function(t){r.replace(t)})},"#connect-github-link click":function(t,e){e.preventDefault(),this.options.currentUser.login("github")},"#connect-twitter-link click":function(t,e){e.preventDefault(),this.options.currentUser.login("twitter")},"#connect-meetup-link click":function(t,e){e.preventDefault(),this.options.currentUser.login("meetup")},"#submit-article-link click":function(e,i){i.preventDefault(),t.route.attr("newpost_c","article"),this.options.newpostVisibility(!0)},"#submit-app-link click":function(e,i){i.preventDefault(),t.route.attr("newpost_c","app"),this.options.newpostVisibility(!0)},"#submit-plugin-link click":function(e,i){i.preventDefault(),t.route.attr("newpost_c","plugin"),this.options.newpostVisibility(!0)},"#submit-event-link click":function(e,i){i.preventDefault(),t.route.attr("newpost_c","event"),this.options.newpostVisibility(!0)},"{window} userLinkError":function(t,e,i){alert(i)}})});
/*bithub/profile/rewards/init.mustache!can/view/mustache/system*/
define("bithub/profile/rewards/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(s){return s.view.preloadStringRenderer("bithub/profile/rewards/init.mustache",s.Mustache(function(t,e){var n=[];return n.push('<div class="rewards-wrapper">\n	<div class="getSwagContainer">\n		<h3>Support open source. Get free stuff.</h3>'),n.push("\n"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"user.isLoggedIn"},[{fn:function(t,e){var n=[];return n.push("		"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"^",{get:"if"},{get:"user.isCompleted"},[{fn:function(t,e){var n=[];return n.push('<div class="alert alert-danger alert-block"><p>We can\'t send you stuff because you haven\'t completed your profile! <a href="'),n.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"routes.profile"}))),n.push('" class="pull-right btn btn-small btn-danger"',s.view.pending({scope:t,options:e}),">"),n.push("Complete Profile</a><br /></p></div>"),n.join("")}}]))),n.push("\n		<p>When you reach these point levels, we'll send you this stuff. We send swag to US addresses every month and to non-US addresses every six months.</p>\n		"),n.join("")}},{inverse:function(){var s=[];return s.push('\n		<div class="btn-wrapper logged-out pull-right">\n		<button href="#login-modal" role="button" data-toggle="modal" class="btn-signup">Sign Up Now</button>\n		</div>\n		<p>When you reach these point levels, we\'ll send you stuff.<br></p>'),s.push("\n"),s.join("")}}]))),n.push('		<div class="rewards">'),n.push("\n"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"user.isLoggedIn"},[{fn:function(t,e){var n=[];return n.push("			"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"rewards"},[{fn:function(t,e){var n=[];return n.push(' <!-- css classes: achieved, shipping, almost -->\n				<div class="item '),n.push(s.view.txt(!0,"div","class",this,s.Mustache.txt({scope:t,options:e},null,{get:"status_cssClass"}))),n.push('" '),n.push(s.view.txt(2,"div","style",this,function(){var n=[];return n.push('style="'),n.push("background-image: url("),n.push(s.view.toStr(s.Mustache.txt({scope:t,options:e,special:!0},null,{get:"imageUrlBasedOnShipping"})())),n.push(')"'),n.join("")})),n.push("",s.view.pending({scope:t,options:e}),">"),n.push('\n					<span class="points">'),n.push("\n"),n.push(s.view.txt(0,"span",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"display_point_minimum"},[{fn:function(t,e){var n=[];return n.push("							"),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"display_point_minimum"}))),n.push("\n						"),n.join("")}},{inverse:function(t,e){var n=[];return n.push("\n							"),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"point_minimum"}))),n.push("\n"),n.join("")}}]))),n.push("					</span>"),n.push("\n"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"status_message"},[{fn:function(t,e){var n=[];return n.push('						<span class="message">'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"status_message"}))),n.push("</span>"),n.push("\n"),n.join("")}}]))),n.push('					<span class="title">\n						'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"title"}))),n.push("\n"),n.push(s.view.txt(0,"span",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"status_inlineMessage"},[{fn:function(t,e){var n=[];return n.push('							<span class="message">\n								'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"status_inlineMessage"}))),n.push("\n							</span>"),n.push("\n"),n.join("")}}]))),n.push("					</span>\n				</div>"),n.push("\n"),n.join("")}}]))),n.push("		"),n.join("")}},{inverse:function(t,e){var n=[];return n.push("\n		"),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"rewards"},[{fn:function(t,e){var n=[];return n.push(' <!-- css classes: achieved, shipping, almost -->\n			<div class="item" '),n.push(s.view.txt(2,"div","style",this,function(){var n=[];return n.push('style="'),n.push("background-image: url("),n.push(s.view.toStr(s.Mustache.txt({scope:t,options:e,special:!0},null,{get:"imageUrl"})())),n.push(')"'),n.join("")})),n.push("",s.view.pending({scope:t,options:e}),">"),n.push('\n				<span class="points">'),n.push(s.view.txt(0,"span",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"display_point_minimum"},[{fn:function(t,e){var n=[];return n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"display_point_minimum"}))),n.join("")}},{inverse:function(t,e){var n=[];return n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"point_minimum"}))),n.join("")}}]))),n.push('</span>\n				<span class="title">'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"title"}))),n.push("</span>\n			</div>"),n.push("\n"),n.join("")}}]))),n.push("		"),n.join("")}}]))),n.push("\n		</div>\n	</div>\n</div>\n"),n.join("")}))});
/*bithub/profile/rewards/rewards*/
define("bithub/profile/rewards/rewards",["can/can","./init.mustache!","bithub/models/reward"],function(e,r,t){return e.Control.extend({defaults:{rewards:new Bithub.Models.Reward.List}},{init:function(s,i){var n=this.options.rewards;t.findAll({order:"point_minimum"},function(e){var r=_.filter(e,function(e){return e.disabled_ts?void 0:e});n.replace(r)}),this.element.html(r({user:i.currentUser,rewards:n,routes:{profile:e.route.url({page:"profile",view:"info"},!1)}})),i.currentUser.isLoggedIn()&&this.matchRewards()},"{currentUser} authStatus":function(){this.options.currentUser.isLoggedIn()&&this.matchRewards()},"{rewards} length":"matchRewards","{users} length":"matchRewards",matchRewards:function(){this.options.rewards.matchAchievements(this.options.currentUser,this.options.users)}})});
/*bithub/profile/init.mustache!can/view/mustache/system*/
define("bithub/profile/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/profile/init.mustache",e.Mustache(function(){var e=[];return e.push('<div class="profileContainer"></div>\n'),e.join("")}))});
/*bithub/profile/info/info.mustache!can/view/mustache/system*/
define("bithub/profile/info/info.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/profile/info/info.mustache",t.Mustache(function(n,s){var e=[];return e.push('<div class="profile-content">\n<div class="row">\n	<div class="span5 offset3">\n	<form id="edit-profile-form">\n		\n		<div class="control-group">\n		<label class="wrapped-label-input">\n			<span>Name:</span>\n			<input type="text" name="name" value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.name"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		</div>\n		<div class="control-group">\n		<label class="wrapped-label-input">\n			<span>Email:</span>\n			<input type="text" name="email" value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.email"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		</div>\n		<p class="mailing-addr-caption">Mailing Address (so we can send you swag):</p>\n		<div class="control-group">\n		<label class="wrapped-label-input">\n			<span>Street:</span>\n			<input type="text" name="address" '),e.push(t.view.txt(2,"input","style",this,function(){var t=[];return t.push('style="'),t.push('width: 330px;"'),t.join("")})),e.push(' value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.address"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		</div>\n		<div class="controls controls-row">\n		<label class="wrapped-label-input">\n			<span>City:</span>\n			<input type="text" name="city" value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.city"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		<label class="wrapped-label-input">\n			<span>State/Province:</span>\n			<input type="text" name="state" value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.state"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		</div>\n		<div class="controls controls-row">\n		<label class="wrapped-label-input">\n			<span>Postal Code:</span>\n			<input type="text" name="postal" value="'),e.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"user.postal"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push('\n		</label>\n		\n		<select id="countryISO" name="countryISO">'),e.push("\n"),e.push(t.view.txt(0,"select",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"countries"},[{fn:function(n,s){var e=[];return e.push('			<option value="'),e.push(t.view.txt(!0,"option","value",this,t.Mustache.txt({scope:n,options:s},null,{get:"iso"}))),e.push('"',t.view.pending({scope:n,options:s}),">"),e.push(t.view.txt(1,"option",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"name"}))),e.push("</option>"),e.push("\n"),e.join("")}}]))),e.push("		</select>\n		</div>"),e.push("\n"),e.push(t.view.txt(0,"/div",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"unless"},{get:"isEditing"},[{fn:function(){var t=[];return t.push('			<div class="control-group">\n			<button type="submit" name="update" class="btn-bigass light" data-loading-text="Saving...">Update</button>\n			<div class="form-status">\n				<p class="success">Saved!</p>\n				<p class="error">Something went wrong!</p>\n			</div>\n			</div>'),t.join("")}}]))),e.push('	</form>\n	\n	</div>\n	<div class="span3">'),e.push("\n"),e.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"if"},{get:"user.avatarUrl"},[{fn:function(n,s){var e=[];return e.push('		<div class="avatar">\n			'),e.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"user.avatarUrl"},[{fn:function(n,s){var e=[];return e.push("<img "),e.push(t.view.txt(2,"img","src",this,function(){var e=[];return e.push('src="'),e.push(t.view.toStr(t.Mustache.txt({scope:n,options:s,special:!0},null,{get:"."})())),e.push('"'),e.join("")})),e.push(' alt="avatar"',t.view.pending({scope:n,options:s}),">"),e.join("")}}]))),e.push("\n		</div>"),e.push("\n"),e.join("")}}]))),e.push('	<ul class="providers">\n		<li class="github">'),e.push("\n"),e.push(t.view.txt(0,"li",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"hasProvider"},"github",[{fn:function(n,s){var e=[];return e.push('			<span class="login-github"></span>Github ('),e.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"identifier"}))),e.push(') <a href="javascript://" can-click="unlinkIdentity">&times;</a>\n		'),e.join("")}},{inverse:function(){var t=[];return t.push('\n			<a id="login-github-link" href="/api/auth/github" class="btn connect"><span class="login-github"></span>Connect Github</a>'),t.push("\n"),t.join("")}}]))),e.push('		</li>\n		<li class="twitter">'),e.push("\n"),e.push(t.view.txt(0,"li",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"hasProvider"},"twitter",[{fn:function(n,s){var e=[];return e.push('			<span class="login-twitter"></span>Twitter ('),e.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"identifier"}))),e.push(') <a href="javascript://" can-click="unlinkIdentity">&times;</a>\n		'),e.join("")}},{inverse:function(){var t=[];return t.push('\n			<a id="login-twitter-link" href="/api/auth/twitter" class="btn connect"><span class="login-twitter"></span>Connect Twitter</a>'),t.push("\n"),t.join("")}}]))),e.push('		</li>\n		<li class="meetup">'),e.push("\n"),e.push(t.view.txt(0,"li",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"hasProvider"},"meetup",[{fn:function(n,s){var e=[];return e.push('			<span class="login-meetup"></span>Meetup ('),e.push(t.view.txt(1,"li",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"identifier"}))),e.push(') <a href="javascript://" can-click="unlinkIdentity">&times;</a>\n		'),e.join("")}},{inverse:function(){var t=[];return t.push('\n			<a id="login-meetup-link" href="/api/auth/meetup" class="btn connect"><span class="login-meetup"></span>Connect Meetup</a>'),t.push("\n"),t.join("")}}]))),e.push("		</li>\n		\n	</ul>\n	</div>\n</div>\n</div>\n"),e.join("")}))});
/*ui/bootstrap_dropdown/init.mustache!can/view/mustache/system*/
define("ui/bootstrap_dropdown/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("ui/bootstrap_dropdown/init.mustache",t.Mustache(function(n,s){var e=[];return e.push("<div "),e.push(t.view.txt(0,"div",1,this,t.Mustache.txt({scope:n,options:s},"#",{get:"if"},{get:"htmlId"},[{fn:function(n,s){var e=[];return e.push('id="'),e.push(t.view.txt(!0,"div","id",this,t.Mustache.txt({scope:n,options:s},null,{get:"htmlId"}))),e.push('"'),e.join("")}}]))),e.push(' class="btn-group"',t.view.pending({scope:n,options:s}),">"),e.push('\n  <button class="btn select">'),e.push(t.view.txt(1,"button",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"selectedValue"}))),e.push('</button>\n  <button class="btn dropdown-toggle" data-toggle="dropdown">\n	<span class="caret"></span>\n  </button>\n  <ul class="dropdown-menu">'),e.push("\n"),e.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:n,options:s},"#",{get:"items"},[{fn:function(n,s){var e=[];return e.push("	<li><a "),e.push(t.view.txt(1,"a",1,this,function(){return t.proxy(function(n){t.data(t.$(n),"option",this.attr("."))},n)})),e.push("",t.view.pending({scope:n,options:s}),">"),e.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:n,options:s},null,{get:"value"}))),e.push("</a></li>"),e.push("\n"),e.join("")}}]))),e.push("  </ul>\n</div>\n"),e.join("")}))});
/*ui/bootstrap_dropdown/bootstrap_dropdown*/
define("ui/bootstrap_dropdown/bootstrap_dropdown",["can/can","./init.mustache!"],function(t,e){return t.Control({defaults:{}},{init:function(t,n){this.element.html(e({htmlId:n.htmlId,items:n.items},{selectedValue:function(){var t="";return n.items.each(function(e){e.attr("key")===n.selected()&&(t=e.attr("value"))}),t}}))},"a click":function(e,n){n.preventDefault(),this.options.selected(t.data(e,"option").key)},"button click":function(t,e){e.preventDefault()}})});
/*bithub/models/country*/
define("bithub/models/country",["can/can"],function(n){var e=n.Model("Bithub.Models.Country",{findAll:"GET /api/v1/countries"},{});return e});
/*jquerypp/dom/form_params/form_params*/
define("jquerypp/dom/form_params/form_params",["jquery/jquery"],function(a){var r=/[^\[\]]+/g,t=function(r){return a.isNumeric(r)?parseFloat(r):"true"===r?!0:"false"===r?!1:""===r||null===r?void 0:r},e=function(r,t,n,i,s,c,u){var o=i.shift();if(u=u?u+"."+o:o,i.length)n[o]||(n[o]={}),e(r,t,n[o],i,s,c,u);else{if(u in c&&"radio"!=t&&!a.isArray(n[o])?n[o]=o in n?[n[o]]:[]:c[u]=!0,("radio"==t||"checkbox"==t)&&!r.is(":checked"))return;n[o]?n[o].push(s):n[o]=s}};return a.fn.extend({formParams:function(a){var r;return!!a===a&&(r=a,a=null),a?this.setParams(a):this.getParams(r)},setParams:function(r){this.find("[name]").each(function(){var t=a(this),e=r[t.attr("name")];void 0!==e&&(t.is(":radio")?t.val()==e&&t.attr("checked",!0):t.is(":checkbox")?(e=a.isArray(e)?e:[e],a.inArray(t.val(),e)>-1&&t.attr("checked",!0)):t.val(e))})},getParams:function(n){var i={},s={};return this.find("[name]:not(:disabled)").each(function(){var c,u=a(this),o=u.attr("type"),f=u.attr("name"),h=u.val();"submit"!=o&&f&&(c=f.match(r),c.length||(c=[f]),n&&(h=t(h)),e(u,o,i,c,h,s))}),i}}),a});
/*bithub/profile/info/info*/
define("bithub/profile/info/info",["can/can","./info.mustache!","ui/bootstrap_dropdown/bootstrap_dropdown","bithub/models/country","can/list/list","jquerypp/dom/form_params/form_params"],function(t,n,i,e){var r={order:["priority:desc","name"],limit:500};return t.Control.extend({pluginName:"profile-info",defaults:{isEditing:!1}},{init:function(){var t=this,i=this.loadCountries();this.element.html(n({countries:i,user:this.options.currentUser,isEditing:this.options.isEditing,unlinkIdentity:this.proxy("unlinkIdentity")},{helpers:{hasProvider:function(n,i){var e=t.options.currentUser.getIdentity(n);return e?i.fn(e):i.inverse(this)}},partials:{}})),this.options.isEditing&&this.element.find(":input").prop("disabled",!0).addClass("disabled")},loadCountries:function(){var n=this,i={iso:"",name:"--",display_name:"--"},o=new t.Observe.List([i]);return e.findAll(r,function(t){o.push.apply(o,t),n.element&&n.element.find("#countryISO").val(n.options.currentUser.attr("country.iso"))}),o},unlinkIdentity:function(t){var n=this;confirm("Are you sure you want to unlink this identity?")&&t.destroy(function(){n.options.currentUser.refreshSession()})}," submit":function(t,n){var i=this.element.find("button");n.preventDefault(),i.button("loading"),this.options.currentUser.attr(t.formParams()).save(function(){i.button("reset"),t.find(".form-status .success").show().delay(1e3).fadeOut()},function(){i.button("reset"),t.find(".form-status .error").show().delay(1e3).fadeOut()})},"#login-github-link click":function(t,n){n.preventDefault(),this.options.currentUser.login("github")},"#login-twitter-link click":function(t,n){n.preventDefault(),this.options.currentUser.login("twitter")},"#login-meetup-link click":function(t,n){n.preventDefault(),this.options.currentUser.login("meetup")},"{window} userLinkError":function(t,n,i){alert(i)}})});
/*bithub/models/activity*/
define("bithub/models/activity",["can/can"],function(i){var e=i.Model.extend("Bithub.Models.Activity",{findAll:"GET /api/v1/users/{userId}/activities"},{});return e});
/*bithub/profile/activities/activities.mustache!can/view/mustache/system*/
define("bithub/profile/activities/activities.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/profile/activities/activities.mustache",t.Mustache(function(s,i){var e=[];return e.push('<!-- ACTIVITIES -->\n<div id="activities" class="span12">'),e.push("\n"),e.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"if"},{get:"isLoggedIn"},[{fn:function(s,i){var e=[];return e.push("		<h3>Activity</h3>"),e.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"if"},{get:"activities.length"},[{fn:function(s,i){var e=[];return e.push('			<ul id="activity-list">'),e.push("\n"),e.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"each"},{get:"activities"},[{fn:function(s,i){var e=[];return e.push('					<li>\n						<span class="badge">'),e.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:i},null,{get:"addPlus"},{get:"value"}))),e.push('</span>\n						<span class="type">'),e.push(t.view.txt(1,"span",0,this,t.Mustache.txt({scope:s,options:i},null,{get:"type"}))),e.push('</span>\n						<a href="'),e.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"eventUrl"}))),e.push('"',t.view.pending({scope:s,options:i}),">"),e.push(t.view.txt(0,"a",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"if"},{get:"title"},[{fn:function(s,i){var e=[];return e.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:i},null,{get:"title"}))),e.join("")}},{inverse:function(s,i){var e=[];return e.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:i},null,{get:"comment"}))),e.join("")}}]))),e.push("</a>\n						<small>("),e.push(t.view.txt(1,"small",0,this,t.Mustache.txt({scope:s,options:i},null,{get:"prettifyTs"},{get:"created_at"},"datetime"))),e.push(")</small>\n					</li>"),e.push("\n"),e.join("")}}]))),e.push("			\n			</ul>"),e.push("\n"),e.join("")}}]))),e.push("		"),e.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"if"},{get:"isLoading"},[{fn:function(){var s=[];return s.push('\n			<div class="spinner-bottom">\n				<img '),s.push(t.view.txt(2,"img","src",this,function(){var t=[];return t.push('src="'),t.push('/assets/images/ajax-loader-sm-blue.gif"'),t.join("")})),s.push(' alt="loading...">\n			</div>'),s.push("\n"),s.join("")}}]))),e.push("	"),e.join("")}},{inverse:function(){var t=[];return t.push('\n		<div class="alert notice">You must be logged in to see this page.</div>'),t.push("\n"),t.join("")}}]))),e.push("</div>"),e.join("")}))});
/*bithub/profile/activities/activities*/
define("bithub/profile/activities/activities",["can/can","bithub/models/activity","./activities.mustache!"],function(i,t,s){return i.Control.extend({pluginName:"profile-activities",defaults:{}},{init:function(e,a){var n=a.currentUser,o=a.routes;this.params={limit:200,offset:0},this.canLoad=!0,this.isLoading=i.compute(!1),this.activities=new t.List,e.html(s({activities:this.activities,routes:o,isLoading:this.isLoading,isLoggedIn:function(){return n.isLoggedIn()}},{helpers:{addPlus:function(t){return t=i.isFunction(t)?t():t,t>0?"+"+t:void 0},eventUrl:function(){var t;return"author"==this.attr("type")?t=this.attr("id"):"award"==this.attr("type")&&(t=this.attr("event_id")),t?i.route.url({page:"eventdetails",id:t}):"javascript://"}},partials:{}})),n.isLoggedIn()&&this.loadActivities()},"{currentUser} authStatus":function(i,t,s){"loggedIn"===s&&this.loadActivities()},"{window} onbottom":"loadActivities",loadActivities:function(){this.canLoad&&(this.isLoading(!0),t.findAll(i.extend({userId:this.options.currentUser.attr("id")},this.params),this.proxy("updateActivities")),this.params.offset=this.params.offset+this.params.limit)},updateActivities:function(i){this.activities.push.apply(this.activities,i),this.isLoading(!1),i.length<this.params.limit&&(this.canLoad=!1)}})});
/*bithub/profile/profile*/
define("bithub/profile/profile",["can/can","./init.mustache!","bithub/profile/info/info","bithub/profile/activities/activities"],function(e,t,i,r){var n;return e.Control.extend({pluginName:"profile",defaults:{views:{info:i,activities:r}}},{init:function(t,i){this.initControl(e.route.attr("view"),i),this.reloadUser()},"{can.route} view":function(e,t,i){this.initControl(i)},"{can.route} page":function(e,t,i){" profile"!=i&&(n=null)},"{currentUser} loggedIn":function(t,i,r){0==r&&e.route.attr({page:"homepage",view:"latest"})},reloadUser:function(){var e=this.options.currentUser,t=this;e.isLoggedIn()?this.__currentReq=e.constructor.findOne({id:e.id},function(){delete t.__currentReq,t.__refreshTimeout=setTimeout(t.proxy("reloadUser"),6e4)}):t.__refreshTimeout=setTimeout(t.proxy("reloadUser"),6e4)},initControl:function(e){var t=this.options.views[e];if(n!=t){n=t;var i=$("<div/>");new t(i,this.options),this.element.html(i)}},destroy:function(){this._super(),this.__currentReq&&this.__currentReq.abort(),clearTimeout(this.__refreshTimeout)}})});
/*bithub/admin/users/users.mustache!can/view/mustache/system*/
define("bithub/admin/users/users.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/admin/users/users.mustache",t.Mustache(function(s,e){var n=[];return n.push('<table class="table table-stripped">\n	<thead>\n		<tr>\n			<th>Name</th>\n			<th>Email</th>\n			<th>Roles</th>\n			<th>Points</th>\n			<th></th>\n		</tr>\n	</thead>\n	<tbody>'),n.push("\n"),n.push(t.view.txt(0,"tbody",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"users"},[{fn:function(s,e){var n=[];return n.push("		\n		<tr "),n.push(t.view.txt(1,"tr",1,this,function(){return t.proxy(function(s){t.data(t.$(s),"user",this.attr("."))},s)})),n.push("",t.view.pending({scope:s,options:e}),">"),n.push('\n			<td><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"editUserUrl"},{get:"this"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push("</a></td>\n			<td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"email"}))),n.push("</td>\n			<td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"rolesCsv"}))),n.push("</td>\n			<td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"score"}))),n.push('</td>\n			<td><button class="delete-user close">&times;</button></td>\n		</tr>'),n.join("")}}]))),n.push('	</tbody>\n</table>\n<div class="pagination pull-right">\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"prevOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:s,options:e}),">"),n.push('Prev page</a>\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:e},null,{get:"nextOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:s,options:e}),">"),n.push("Next page</a>\n</div>\n"),n.join("")}))});
/*bithub/admin/users/form/form.mustache!can/view/mustache/system*/
define("bithub/admin/users/form/form.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(s){return s.view.preloadStringRenderer("bithub/admin/users/form/form.mustache",s.Mustache(function(t,n){var e=[];return e.push('<div class="user-info"></div>\n<div class="span7 offset3 user-roles">\n	'),e.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:n},"#",{get:"if"},{get:"user.roles.length"},[{fn:function(t,n){var e=[];return e.push('	\n		<div class="remove-role row">\n			<strong>User roles</strong><br>\n			<ul class="roles unstyled">'),e.push("\n"),e.push(s.view.txt(0,"ul",0,this,s.Mustache.txt({scope:t,options:n},"#",{get:"user.roles"},[{fn:function(t,n){var e=[];return e.push("					<li "),e.push(s.view.txt(1,"li",1,this,function(){return s.proxy(function(t){s.data(s.$(t),"role",this.attr("."))},t)})),e.push(' class="clearfix"',s.view.pending({scope:t,options:n}),">"),e.push("\n					"),e.push(s.view.txt(1,"li",0,this,s.Mustache.txt({scope:t,options:n},null,{get:"."}))),e.push(' <button class="btn btn-mini btn-danger remove-role">&times;</button>\n				</li>'),e.push("\n"),e.join("")}}]))),e.push("			</ul>\n		</div>"),e.push("\n"),e.join("")}}]))),e.push('	<div id="add-role" class="row">\n		<form class="form-horizontal">\n			<div class="control-group">\n				<input type="text" class="input-medium" name="role" placeholder="Add role">\n				<button type="submit" class="btn">Add</button>\n			</div>\n		</form>\n	</div>\n</div>'),e.join("")}))});
/*bithub/admin/users/form/form*/
define("bithub/admin/users/form/form",["can/can","./form.mustache!","bithub/models/user","bithub/profile/info/info"],function(e,n,t,i){return e.Control.extend({defaults:{User:t}},{init:function(){this.element.html(n({user:this.options.user})),new i(this.element.find(".user-info"),{currentUser:this.options.user,isEditing:!0})},"{can.route} action":function(){},"form submit":function(e,n){n.preventDefault();var t=e.find("input[name=role]").val();this.options.user.manageRoles("add",t),this.element.find("input[name=role]").val("")},"li button.remove-role click":function(e,n){if(n.preventDefault(),confirm("Are you sure?")){var t=e.closest("li").data("role");this.options.user.manageRoles("remove",t)}}})});
/*bithub/admin/paginator*/
define("bithub/admin/paginator",["can/map/map"],function(t){function e(e){s(e)?_.isObject(e)&&!_.isEmpty(e)?this.state=new t({offset:_.isNumber(e.offset)?e.offset:i(e.offset),limit:_.isNumber(e.limit)?e.limit:i(e.limit)}):(_.isNumber(e)||_.isString(e))&&(this.state=new t(_.merge(f,{offset:_.isNumber(e)?e:i(e)}))):this.state=new t(f)}var i=function(t){return parseInt(t,10)},s=function(t){return null!=t&&void 0!=t},f={limit:20,offset:0};return e.prototype={limit:function(){return this.state.attr("limit")},offset:function(){return this.state.attr("offset")},currentState:function(){return this.state.attr()},prevOffset:function(){var t=this.state.attr("offset")-this.state.attr("limit");return t>=0?t:0},nextOffset:function(){return this.state.attr("offset")+this.state.attr("limit")},updateOffset:function(t){s(t)&&_.isString(t)?this.state.attr("offset",i(t)):this.state.attr("offset",f.offset)}},e});
/*bithub/admin/users/users*/
define("bithub/admin/users/users",["can/can","./users.mustache!","bithub/models/user","./form/form","../paginator"],function(t,e,n,r,i){var u,o=function(t){return"edit"===t||"new"===t},s={page:"admin",view:"users",action:"list"},a=function(e){return t.route.url(t.extend({offset:e},s))};return t.Control.extend({pluginName:"admin-users",defaults:{}},{init:function(){u=new i(t.route.attr("offset")),o(t.route.attr("action"))?this.loadForm():this.loadList()},loadForm:function(){var e=this;t.route.attr("id")?n.findOne({id:t.route.attr("id")},function(t){new r(e.element,{user:t})}):new r(e.element,{user:new n})},loadList:function(){var r=this;n.findAll(t.extend(u.currentState(),{order:"name:asc"}),function(n){r.element&&r.element.html(e({users:n,prevOffsetUrl:function(){return a(u.prevOffset())},nextOffsetUrl:function(){return a(u.nextOffset())}},{editUserUrl:function(e){return t.route.url({page:"admin",view:"users",action:"edit",id:e.id})}}))})},"{can.route} offset":function(t,e,n){n&&(u.updateOffset(n),this.loadList())},"{can.route} action":function(t,e,n){o(n)?this.loadForm():this.loadList()},".delete-user click":function(t){var e=t.closest("tr").data("user");confirm("Are you sure?")&&e.destroy()}})});
/*bithub/admin/rewards/rewards.mustache!can/view/mustache/system*/
define("bithub/admin/rewards/rewards.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/admin/rewards/rewards.mustache",t.Mustache(function(e,s){var n=[];return n.push('<table class="table table-stripped">\n	<thead>\n		<tr>\n			<th>Title</th>\n			<th>Point minumum [Display text]</th>\n			<th>Disabled at</th>\n			<th>Image</th>\n			<th></th>\n		</tr>\n	</thead>\n	<tbody>'),n.push("\n"),n.push(t.view.txt(0,"tbody",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"rewards"},[{fn:function(e,s){var n=[];return n.push("		<tr "),n.push(t.view.txt(1,"tr",1,this,function(){return t.proxy(function(e){t.data(t.$(e),"reward",this.attr("."))},e)})),n.push("",t.view.pending({scope:e,options:s}),">"),n.push('\n			<td><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"editRewardUrl"},{get:"this"}))),n.push('"',t.view.pending({scope:e,options:s}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"title"}))),n.push("</a></td>\n			<td>\n				"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"point_minimum"}))),n.push("\n				"),n.push(t.view.txt(0,"td",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"if"},{get:"display_point_minimum"},[{fn:function(e,s){var n=[];return n.push("["),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"display_point_minimum"}))),n.push("]"),n.join("")}}]))),n.push("\n			</td>\n			<td>"),n.push(t.view.txt(0,"td",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"if"},{get:"disabled_ts"},[{fn:function(e,s){var n=[];return n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"prettifyTs"},{get:"disabled_ts"},"date"))),n.join("")}}]))),n.push("</td>\n			<td><img "),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:e,options:s,special:!0},null,{get:"original_image_url"})())),n.push('"'),n.join("")})),n.push("",t.view.pending({scope:e,options:s}),"/>"),n.push('</td>\n			<td><button class="delete-reward close">&times;</button></td>\n		</tr>'),n.push("\n"),n.join("")}}]))),n.push('	</tbody>\n</table>\n<div class="actions pull-left">\n	<a class="btn" href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"newRewardUrl"}))),n.push('"',t.view.pending({scope:e,options:s}),">"),n.push('New reward</a>\n</div>\n<div class="pagination pull-right">\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"prevOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:e,options:s}),">"),n.push('Prev page</a>\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"nextOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:e,options:s}),">"),n.push("Next page</a>\n</div>\n"),n.join("")}))});
/*bithub/admin/rewards/form/form.mustache!can/view/mustache/system*/
define("bithub/admin/rewards/form/form.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/admin/rewards/form/form.mustache",e.Mustache(function(i,t){var n=[];return n.push('<div id="new-reward" class="row">\n  <form class="form-horizontal">\n	<input type="hidden" id="reward-id" name="reward[id]" value="'),n.push(e.view.txt(!0,"input","value",this,e.Mustache.txt({scope:i,options:t},null,{get:"reward.id"}))),n.push('" ',e.view.pending({scope:i,options:t}),"/>"),n.push('\n	<div class="control-group">\n	  <label class="control-label" for="reward-title">Title</label>\n	  <div class="controls">\n		<input type="text" id="reward-title" name="reward[title]" placeholder="Title" value="'),n.push(e.view.txt(!0,"input","value",this,e.Mustache.txt({scope:i,options:t},null,{get:"reward.title"}))),n.push('" ',e.view.pending({scope:i,options:t}),"/>"),n.push('\n	  </div>\n	</div>\n	<div class="control-group">\n	  <label class="control-label" for="reward-point-mimimum">Point minimum</label>\n	  <div class="controls">\n		<input type="text" id="reward-point-mimimum" name="reward[point_minimum]" placeholder="Point minimum" value="'),n.push(e.view.txt(!0,"input","value",this,e.Mustache.txt({scope:i,options:t},null,{get:"reward.point_minimum"}))),n.push('" ',e.view.pending({scope:i,options:t}),"/>"),n.push('\n	  </div>\n	</div>\n	<div class="control-group">\n	  <label class="control-label" for="reward-display-point-mimimum">Point minimum text</label>\n	  <div class="controls">\n		<input type="text" id="reward-point-mimimum" name="reward[display_point_minimum]" placeholder="Display point text" value="'),n.push(e.view.txt(!0,"input","value",this,e.Mustache.txt({scope:i,options:t},null,{get:"reward.display_point_minimum"}))),n.push('" ',e.view.pending({scope:i,options:t}),"/>"),n.push('\n	  </div>\n	</div>\n	<div class="control-group">\n	  <label class="control-label" for="reward-description">Description</label>\n	  <div class="controls">\n		<textarea id="reward-description" name="reward[description]" placeholder="Description..."',e.view.pending({scope:i,options:t}),">"),n.push('</textarea>\n	  </div>\n	</div>\n	<div class="control-group">\n	  <label class="control-label" for="reward-disabled-ts">Disabled at</label>\n	  <div class="controls">\n		<input type="text" id="reward-disabled-ts" name="reward[disabled_ts]" placeholder="Disabled at" value="'),n.push(e.view.txt(!0,"input","value",this,e.Mustache.txt({scope:i,options:t},null,{get:"formatTs"},{get:"reward.disabled_ts"}))),n.push('" '),n.push(e.view.txt(1,"input",1,this,e.Mustache.txt({scope:i,options:t},null,{get:"datepicker"}))),n.push(" ",e.view.pending({scope:i,options:t}),"/>"),n.push('\n	  </div>\n	</div>\n	<div class="control-group">\n	  <label class="control-label" for="reward-image">Image</label>\n	  <div class="controls">\n		<input type="file" id="reward-image" name="reward[image]" />\n	  </div>\n	</div>\n	<div class="control-group">\n	  <div class="controls">\n		<button type="submit" name="submit" class="btn">Submit</button>\n	  </div>\n	</div>\n	\n  </form>\n</div>\n'),n.join("")}))});
/*bootstrap-datepicker*/
System.define("bootstrap-datepicker","/* =========================================================\n * bootstrap-datepicker.js\n * http://www.eyecon.ro/bootstrap-datepicker\n * =========================================================\n * Copyright 2012 Stefan Petre\n * Improvements by Andrew Rowls\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n	function UTCDate(){\n		return new Date(Date.UTC.apply(Date, arguments));\n	}\n	function UTCToday(){\n		var today = new Date();\n		return UTCDate(today.getUTCFullYear(), today.getUTCMonth(), today.getUTCDate());\n	}\n\n	// Picker object\n\n	var Datepicker = function(element, options) {\n		var that = this;\n\n		this._process_options(options);\n\n		this.element = $(element);\n		this.isInline = false;\n		this.isInput = this.element.is('input');\n		this.component = this.element.is('.date') ? this.element.find('.add-on, .btn') : false;\n		this.hasInput = this.component && this.element.find('input').length;\n		if(this.component && this.component.length === 0)\n			this.component = false;\n\n		this.picker = $(DPGlobal.template);\n		this._buildEvents();\n		this._attachEvents();\n\n		if(this.isInline) {\n			this.picker.addClass('datepicker-inline').appendTo(this.element);\n		} else {\n			this.picker.addClass('datepicker-dropdown dropdown-menu');\n		}\n\n		if (this.o.rtl){\n			this.picker.addClass('datepicker-rtl');\n			this.picker.find('.prev i, .next i')\n						.toggleClass('icon-arrow-left icon-arrow-right');\n		}\n\n\n		this.viewMode = this.o.startView;\n\n		if (this.o.calendarWeeks)\n			this.picker.find('tfoot th.today')\n						.attr('colspan', function(i, val){\n							return parseInt(val) + 1;\n						});\n\n		this._allow_update = false;\n\n		this.setStartDate(this.o.startDate);\n		this.setEndDate(this.o.endDate);\n		this.setDaysOfWeekDisabled(this.o.daysOfWeekDisabled);\n\n		this.fillDow();\n		this.fillMonths();\n\n		this._allow_update = true;\n\n		this.update();\n		this.showMode();\n\n		if(this.isInline) {\n			this.show();\n		}\n	};\n\n	Datepicker.prototype = {\n		constructor: Datepicker,\n\n		_process_options: function(opts){\n			// Store raw options for reference\n			this._o = $.extend({}, this._o, opts);\n			// Processed options\n			var o = this.o = $.extend({}, this._o);\n\n			// Check if \"de-DE\" style date is available, if not language should\n			// fallback to 2 letter code eg \"de\"\n			var lang = o.language;\n			if (!dates[lang]) {\n				lang = lang.split('-')[0];\n				if (!dates[lang])\n					lang = $.fn.datepicker.defaults.language;\n			}\n			o.language = lang;\n\n			switch(o.startView){\n				case 2:\n				case 'decade':\n					o.startView = 2;\n					break;\n				case 1:\n				case 'year':\n					o.startView = 1;\n					break;\n				default:\n					o.startView = 0;\n			}\n\n			switch (o.minViewMode) {\n				case 1:\n				case 'months':\n					o.minViewMode = 1;\n					break;\n				case 2:\n				case 'years':\n					o.minViewMode = 2;\n					break;\n				default:\n					o.minViewMode = 0;\n			}\n\n			o.startView = Math.max(o.startView, o.minViewMode);\n\n			o.weekStart %= 7;\n			o.weekEnd = ((o.weekStart + 6) % 7);\n\n			var format = DPGlobal.parseFormat(o.format)\n			if (o.startDate !== -Infinity) {\n				o.startDate = DPGlobal.parseDate(o.startDate, format, o.language);\n			}\n			if (o.endDate !== Infinity) {\n				o.endDate = DPGlobal.parseDate(o.endDate, format, o.language);\n			}\n\n			o.daysOfWeekDisabled = o.daysOfWeekDisabled||[];\n			if (!$.isArray(o.daysOfWeekDisabled))\n				o.daysOfWeekDisabled = o.daysOfWeekDisabled.split(/[,\\s]*/);\n			o.daysOfWeekDisabled = $.map(o.daysOfWeekDisabled, function (d) {\n				return parseInt(d, 10);\n			});\n		},\n		_events: [],\n		_secondaryEvents: [],\n		_applyEvents: function(evs){\n			for (var i=0, el, ev; i<evs.length; i++){\n				el = evs[i][0];\n				ev = evs[i][1];\n				el.on(ev);\n			}\n		},\n		_unapplyEvents: function(evs){\n			for (var i=0, el, ev; i<evs.length; i++){\n				el = evs[i][0];\n				ev = evs[i][1];\n				el.off(ev);\n			}\n		},\n		_buildEvents: function(){\n			if (this.isInput) { // single input\n				this._events = [\n					[this.element, {\n						focus: $.proxy(this.show, this),\n						keyup: $.proxy(this.update, this),\n						keydown: $.proxy(this.keydown, this)\n					}]\n				];\n			}\n			else if (this.component && this.hasInput){ // component: input + button\n				this._events = [\n					// For components that are not readonly, allow keyboard nav\n					[this.element.find('input'), {\n						focus: $.proxy(this.show, this),\n						keyup: $.proxy(this.update, this),\n						keydown: $.proxy(this.keydown, this)\n					}],\n					[this.component, {\n						click: $.proxy(this.show, this)\n					}]\n				];\n			}\n			else if (this.element.is('div')) {  // inline datepicker\n				this.isInline = true;\n			}\n			else {\n				this._events = [\n					[this.element, {\n						click: $.proxy(this.show, this)\n					}]\n				];\n			}\n\n			this._secondaryEvents = [\n				[this.picker, {\n					click: $.proxy(this.click, this)\n				}],\n				[$(window), {\n					resize: $.proxy(this.place, this)\n				}],\n				[$(document), {\n					mousedown: $.proxy(function (e) {\n						// Clicked outside the datepicker, hide it\n						if (!(\n							this.element.is(e.target) ||\n							this.element.find(e.target).size() ||\n							this.picker.is(e.target) ||\n							this.picker.find(e.target).size()\n						)) {\n							this.hide();\n						}\n					}, this)\n				}]\n			];\n		},\n		_attachEvents: function(){\n			this._detachEvents();\n			this._applyEvents(this._events);\n		},\n		_detachEvents: function(){\n			this._unapplyEvents(this._events);\n		},\n		_attachSecondaryEvents: function(){\n			this._detachSecondaryEvents();\n			this._applyEvents(this._secondaryEvents);\n		},\n		_detachSecondaryEvents: function(){\n			this._unapplyEvents(this._secondaryEvents);\n		},\n		_trigger: function(event, altdate){\n			var date = altdate || this.date,\n				local_date = new Date(date.getTime() + (date.getTimezoneOffset()*60000));\n\n			this.element.trigger({\n				type: event,\n				date: local_date,\n				format: $.proxy(function(altformat){\n					var format = altformat || this.o.format;\n					return DPGlobal.formatDate(date, format, this.o.language);\n				}, this)\n			});\n		},\n\n		show: function(e) {\n			if (!this.isInline)\n				this.picker.appendTo('body');\n			this.picker.show();\n			this.height = this.component ? this.component.outerHeight() : this.element.outerHeight();\n			this.place();\n			this._attachSecondaryEvents();\n			if (e) {\n				e.preventDefault();\n			}\n			this._trigger('show');\n		},\n\n		hide: function(e){\n			if(this.isInline) return;\n			if (!this.picker.is(':visible')) return;\n			this.picker.hide().detach();\n			this._detachSecondaryEvents();\n			this.viewMode = this.o.startView;\n			this.showMode();\n\n			if (\n				this.o.forceParse &&\n				(\n					this.isInput && this.element.val() ||\n					this.hasInput && this.element.find('input').val()\n				)\n			)\n				this.setValue();\n			this._trigger('hide');\n		},\n\n		remove: function() {\n			this.hide();\n			this._detachEvents();\n			this._detachSecondaryEvents();\n			this.picker.remove();\n			delete this.element.data().datepicker;\n			if (!this.isInput) {\n				delete this.element.data().date;\n			}\n		},\n\n		getDate: function() {\n			var d = this.getUTCDate();\n			return new Date(d.getTime() + (d.getTimezoneOffset()*60000));\n		},\n\n		getUTCDate: function() {\n			return this.date;\n		},\n\n		setDate: function(d) {\n			this.setUTCDate(new Date(d.getTime() - (d.getTimezoneOffset()*60000)));\n		},\n\n		setUTCDate: function(d) {\n			this.date = d;\n			this.setValue();\n		},\n\n		setValue: function() {\n			var formatted = this.getFormattedDate();\n			if (!this.isInput) {\n				if (this.component){\n					this.element.find('input').val(formatted);\n				}\n			} else {\n				this.element.val(formatted);\n			}\n		},\n\n		getFormattedDate: function(format) {\n			if (format === undefined)\n				format = this.o.format;\n			return DPGlobal.formatDate(this.date, format, this.o.language);\n		},\n\n		setStartDate: function(startDate){\n			this._process_options({startDate: startDate});\n			this.update();\n			this.updateNavArrows();\n		},\n\n		setEndDate: function(endDate){\n			this._process_options({endDate: endDate});\n			this.update();\n			this.updateNavArrows();\n		},\n\n		setDaysOfWeekDisabled: function(daysOfWeekDisabled){\n			this._process_options({daysOfWeekDisabled: daysOfWeekDisabled});\n			this.update();\n			this.updateNavArrows();\n		},\n\n		place: function(){\n						if(this.isInline) return;\n			var zIndex = parseInt(this.element.parents().filter(function() {\n							return $(this).css('z-index') != 'auto';\n						}).first().css('z-index'))+10;\n			var offset = this.component ? this.component.parent().offset() : this.element.offset();\n			var height = this.component ? this.component.outerHeight(true) : this.element.outerHeight(true);\n			this.picker.css({\n				top: offset.top + height,\n				left: offset.left,\n				zIndex: zIndex\n			});\n		},\n\n		_allow_update: true,\n		update: function(){\n			if (!this._allow_update) return;\n\n			var date, fromArgs = false;\n			if(arguments && arguments.length && (typeof arguments[0] === 'string' || arguments[0] instanceof Date)) {\n				date = arguments[0];\n				fromArgs = true;\n			} else {\n				date = this.isInput ? this.element.val() : this.element.data('date') || this.element.find('input').val();\n				delete this.element.data().date;\n			}\n\n			this.date = DPGlobal.parseDate(date, this.o.format, this.o.language);\n\n			if(fromArgs) this.setValue();\n\n			if (this.date < this.o.startDate) {\n				this.viewDate = new Date(this.o.startDate);\n			} else if (this.date > this.o.endDate) {\n				this.viewDate = new Date(this.o.endDate);\n			} else {\n				this.viewDate = new Date(this.date);\n			}\n			this.fill();\n		},\n\n		fillDow: function(){\n			var dowCnt = this.o.weekStart,\n			html = '<tr>';\n			if(this.o.calendarWeeks){\n				var cell = '<th class=\"cw\">&nbsp;</th>';\n				html += cell;\n				this.picker.find('.datepicker-days thead tr:first-child').prepend(cell);\n			}\n			while (dowCnt < this.o.weekStart + 7) {\n				html += '<th class=\"dow\">'+dates[this.o.language].daysMin[(dowCnt++)%7]+'</th>';\n			}\n			html += '</tr>';\n			this.picker.find('.datepicker-days thead').append(html);\n		},\n\n		fillMonths: function(){\n			var html = '',\n			i = 0;\n			while (i < 12) {\n				html += '<span class=\"month\">'+dates[this.o.language].monthsShort[i++]+'</span>';\n			}\n			this.picker.find('.datepicker-months td').html(html);\n		},\n\n		setRange: function(range){\n			if (!range || !range.length)\n				delete this.range;\n			else\n				this.range = $.map(range, function(d){ return d.valueOf(); });\n			this.fill();\n		},\n\n		getClassNames: function(date){\n			var cls = [],\n				year = this.viewDate.getUTCFullYear(),\n				month = this.viewDate.getUTCMonth(),\n				currentDate = this.date.valueOf(),\n				today = new Date();\n			if (date.getUTCFullYear() < year || (date.getUTCFullYear() == year && date.getUTCMonth() < month)) {\n				cls.push('old');\n			} else if (date.getUTCFullYear() > year || (date.getUTCFullYear() == year && date.getUTCMonth() > month)) {\n				cls.push('new');\n			}\n			// Compare internal UTC date with local today, not UTC today\n			if (this.o.todayHighlight &&\n				date.getUTCFullYear() == today.getFullYear() &&\n				date.getUTCMonth() == today.getMonth() &&\n				date.getUTCDate() == today.getDate()) {\n				cls.push('today');\n			}\n			if (currentDate && date.valueOf() == currentDate) {\n				cls.push('active');\n			}\n			if (date.valueOf() < this.o.startDate || date.valueOf() > this.o.endDate ||\n				$.inArray(date.getUTCDay(), this.o.daysOfWeekDisabled) !== -1) {\n				cls.push('disabled');\n			}\n			if (this.range){\n				if (date > this.range[0] && date < this.range[this.range.length-1]){\n					cls.push('range');\n				}\n				if ($.inArray(date.valueOf(), this.range) != -1){\n					cls.push('selected');\n				}\n			}\n			return cls;\n		},\n\n		fill: function() {\n			var d = new Date(this.viewDate),\n				year = d.getUTCFullYear(),\n				month = d.getUTCMonth(),\n				startYear = this.o.startDate !== -Infinity ? this.o.startDate.getUTCFullYear() : -Infinity,\n				startMonth = this.o.startDate !== -Infinity ? this.o.startDate.getUTCMonth() : -Infinity,\n				endYear = this.o.endDate !== Infinity ? this.o.endDate.getUTCFullYear() : Infinity,\n				endMonth = this.o.endDate !== Infinity ? this.o.endDate.getUTCMonth() : Infinity,\n				currentDate = this.date && this.date.valueOf(),\n				tooltip;\n			this.picker.find('.datepicker-days thead th.datepicker-switch')\n						.text(dates[this.o.language].months[month]+' '+year);\n			this.picker.find('tfoot th.today')\n						.text(dates[this.o.language].today)\n						.toggle(this.o.todayBtn !== false);\n			this.picker.find('tfoot th.clear')\n						.text(dates[this.o.language].clear)\n						.toggle(this.o.clearBtn !== false);\n			this.updateNavArrows();\n			this.fillMonths();\n			var prevMonth = UTCDate(year, month-1, 28,0,0,0,0),\n				day = DPGlobal.getDaysInMonth(prevMonth.getUTCFullYear(), prevMonth.getUTCMonth());\n			prevMonth.setUTCDate(day);\n			prevMonth.setUTCDate(day - (prevMonth.getUTCDay() - this.o.weekStart + 7)%7);\n			var nextMonth = new Date(prevMonth);\n			nextMonth.setUTCDate(nextMonth.getUTCDate() + 42);\n			nextMonth = nextMonth.valueOf();\n			var html = [];\n			var clsName;\n			while(prevMonth.valueOf() < nextMonth) {\n				if (prevMonth.getUTCDay() == this.o.weekStart) {\n					html.push('<tr>');\n					if(this.o.calendarWeeks){\n						// ISO 8601: First week contains first thursday.\n						// ISO also states week starts on Monday, but we can be more abstract here.\n						var\n							// Start of current week: based on weekstart/current date\n							ws = new Date(+prevMonth + (this.o.weekStart - prevMonth.getUTCDay() - 7) % 7 * 864e5),\n							// Thursday of this week\n							th = new Date(+ws + (7 + 4 - ws.getUTCDay()) % 7 * 864e5),\n							// First Thursday of year, year from thursday\n							yth = new Date(+(yth = UTCDate(th.getUTCFullYear(), 0, 1)) + (7 + 4 - yth.getUTCDay())%7*864e5),\n							// Calendar week: ms between thursdays, div ms per day, div 7 days\n							calWeek =  (th - yth) / 864e5 / 7 + 1;\n						html.push('<td class=\"cw\">'+ calWeek +'</td>');\n\n					}\n				}\n				clsName = this.getClassNames(prevMonth);\n				clsName.push('day');\n\n				var before = this.o.beforeShowDay(prevMonth);\n				if (before === undefined)\n					before = {};\n				else if (typeof(before) === 'boolean')\n					before = {enabled: before};\n				else if (typeof(before) === 'string')\n					before = {classes: before};\n				if (before.enabled === false)\n					clsName.push('disabled');\n				if (before.classes)\n					clsName = clsName.concat(before.classes.split(/\\s+/));\n				if (before.tooltip)\n					tooltip = before.tooltip;\n\n				clsName = $.unique(clsName);\n				html.push('<td class=\"'+clsName.join(' ')+'\"' + (tooltip ? ' title=\"'+tooltip+'\"' : '') + '>'+prevMonth.getUTCDate() + '</td>');\n				if (prevMonth.getUTCDay() == this.o.weekEnd) {\n					html.push('</tr>');\n				}\n				prevMonth.setUTCDate(prevMonth.getUTCDate()+1);\n			}\n			this.picker.find('.datepicker-days tbody').empty().append(html.join(''));\n			var currentYear = this.date && this.date.getUTCFullYear();\n\n			var months = this.picker.find('.datepicker-months')\n						.find('th:eq(1)')\n							.text(year)\n							.end()\n						.find('span').removeClass('active');\n			if (currentYear && currentYear == year) {\n				months.eq(this.date.getUTCMonth()).addClass('active');\n			}\n			if (year < startYear || year > endYear) {\n				months.addClass('disabled');\n			}\n			if (year == startYear) {\n				months.slice(0, startMonth).addClass('disabled');\n			}\n			if (year == endYear) {\n				months.slice(endMonth+1).addClass('disabled');\n			}\n\n			html = '';\n			year = parseInt(year/10, 10) * 10;\n			var yearCont = this.picker.find('.datepicker-years')\n								.find('th:eq(1)')\n									.text(year + '-' + (year + 9))\n									.end()\n								.find('td');\n			year -= 1;\n			for (var i = -1; i < 11; i++) {\n				html += '<span class=\"year'+(i == -1 ? ' old' : i == 10 ? ' new' : '')+(currentYear == year ? ' active' : '')+(year < startYear || year > endYear ? ' disabled' : '')+'\">'+year+'</span>';\n				year += 1;\n			}\n			yearCont.html(html);\n		},\n\n		updateNavArrows: function() {\n			if (!this._allow_update) return;\n\n			var d = new Date(this.viewDate),\n				year = d.getUTCFullYear(),\n				month = d.getUTCMonth();\n			switch (this.viewMode) {\n				case 0:\n					if (this.o.startDate !== -Infinity && year <= this.o.startDate.getUTCFullYear() && month <= this.o.startDate.getUTCMonth()) {\n						this.picker.find('.prev').css({visibility: 'hidden'});\n					} else {\n						this.picker.find('.prev').css({visibility: 'visible'});\n					}\n					if (this.o.endDate !== Infinity && year >= this.o.endDate.getUTCFullYear() && month >= this.o.endDate.getUTCMonth()) {\n						this.picker.find('.next').css({visibility: 'hidden'});\n					} else {\n						this.picker.find('.next').css({visibility: 'visible'});\n					}\n					break;\n				case 1:\n				case 2:\n					if (this.o.startDate !== -Infinity && year <= this.o.startDate.getUTCFullYear()) {\n						this.picker.find('.prev').css({visibility: 'hidden'});\n					} else {\n						this.picker.find('.prev').css({visibility: 'visible'});\n					}\n					if (this.o.endDate !== Infinity && year >= this.o.endDate.getUTCFullYear()) {\n						this.picker.find('.next').css({visibility: 'hidden'});\n					} else {\n						this.picker.find('.next').css({visibility: 'visible'});\n					}\n					break;\n			}\n		},\n\n		click: function(e) {\n			e.preventDefault();\n			var target = $(e.target).closest('span, td, th');\n			if (target.length == 1) {\n				switch(target[0].nodeName.toLowerCase()) {\n					case 'th':\n						switch(target[0].className) {\n							case 'datepicker-switch':\n								this.showMode(1);\n								break;\n							case 'prev':\n							case 'next':\n								var dir = DPGlobal.modes[this.viewMode].navStep * (target[0].className == 'prev' ? -1 : 1);\n								switch(this.viewMode){\n									case 0:\n										this.viewDate = this.moveMonth(this.viewDate, dir);\n										break;\n									case 1:\n									case 2:\n										this.viewDate = this.moveYear(this.viewDate, dir);\n										break;\n								}\n								this.fill();\n								break;\n							case 'today':\n								var date = new Date();\n								date = UTCDate(date.getFullYear(), date.getMonth(), date.getDate(), 0, 0, 0);\n\n								this.showMode(-2);\n								var which = this.o.todayBtn == 'linked' ? null : 'view';\n								this._setDate(date, which);\n								break;\n							case 'clear':\n								var element;\n								if (this.isInput)\n									element = this.element;\n								else if (this.component)\n									element = this.element.find('input');\n								if (element)\n									element.val(\"\").change();\n								this._trigger('changeDate');\n								this.update();\n								if (this.o.autoclose)\n									this.hide();\n								break;\n						}\n						break;\n					case 'span':\n						if (!target.is('.disabled')) {\n							this.viewDate.setUTCDate(1);\n							if (target.is('.month')) {\n								var day = 1;\n								var month = target.parent().find('span').index(target);\n								var year = this.viewDate.getUTCFullYear();\n								this.viewDate.setUTCMonth(month);\n								this._trigger('changeMonth', this.viewDate);\n								if (this.o.minViewMode === 1) {\n									this._setDate(UTCDate(year, month, day,0,0,0,0));\n								}\n							} else {\n								var year = parseInt(target.text(), 10)||0;\n								var day = 1;\n								var month = 0;\n								this.viewDate.setUTCFullYear(year);\n								this._trigger('changeYear', this.viewDate);\n								if (this.o.minViewMode === 2) {\n									this._setDate(UTCDate(year, month, day,0,0,0,0));\n								}\n							}\n							this.showMode(-1);\n							this.fill();\n						}\n						break;\n					case 'td':\n						if (target.is('.day') && !target.is('.disabled')){\n							var day = parseInt(target.text(), 10)||1;\n							var year = this.viewDate.getUTCFullYear(),\n								month = this.viewDate.getUTCMonth();\n							if (target.is('.old')) {\n								if (month === 0) {\n									month = 11;\n									year -= 1;\n								} else {\n									month -= 1;\n								}\n							} else if (target.is('.new')) {\n								if (month == 11) {\n									month = 0;\n									year += 1;\n								} else {\n									month += 1;\n								}\n							}\n							this._setDate(UTCDate(year, month, day,0,0,0,0));\n						}\n						break;\n				}\n			}\n		},\n\n		_setDate: function(date, which){\n			if (!which || which == 'date')\n				this.date = new Date(date);\n			if (!which || which  == 'view')\n				this.viewDate = new Date(date);\n			this.fill();\n			this.setValue();\n			this._trigger('changeDate');\n			var element;\n			if (this.isInput) {\n				element = this.element;\n			} else if (this.component){\n				element = this.element.find('input');\n			}\n			if (element) {\n				element.change();\n				if (this.o.autoclose && (!which || which == 'date')) {\n					this.hide();\n				}\n			}\n		},\n\n		moveMonth: function(date, dir){\n			if (!dir) return date;\n			var new_date = new Date(date.valueOf()),\n				day = new_date.getUTCDate(),\n				month = new_date.getUTCMonth(),\n				mag = Math.abs(dir),\n				new_month, test;\n			dir = dir > 0 ? 1 : -1;\n			if (mag == 1){\n				test = dir == -1\n					// If going back one month, make sure month is not current month\n					// (eg, Mar 31 -> Feb 31 == Feb 28, not Mar 02)\n					? function(){ return new_date.getUTCMonth() == month; }\n					// If going forward one month, make sure month is as expected\n					// (eg, Jan 31 -> Feb 31 == Feb 28, not Mar 02)\n					: function(){ return new_date.getUTCMonth() != new_month; };\n				new_month = month + dir;\n				new_date.setUTCMonth(new_month);\n				// Dec -> Jan (12) or Jan -> Dec (-1) -- limit expected date to 0-11\n				if (new_month < 0 || new_month > 11)\n					new_month = (new_month + 12) % 12;\n			} else {\n				// For magnitudes >1, move one month at a time...\n				for (var i=0; i<mag; i++)\n					// ...which might decrease the day (eg, Jan 31 to Feb 28, etc)...\n					new_date = this.moveMonth(new_date, dir);\n				// ...then reset the day, keeping it in the new month\n				new_month = new_date.getUTCMonth();\n				new_date.setUTCDate(day);\n				test = function(){ return new_month != new_date.getUTCMonth(); };\n			}\n			// Common date-resetting loop -- if date is beyond end of month, make it\n			// end of month\n			while (test()){\n				new_date.setUTCDate(--day);\n				new_date.setUTCMonth(new_month);\n			}\n			return new_date;\n		},\n\n		moveYear: function(date, dir){\n			return this.moveMonth(date, dir*12);\n		},\n\n		dateWithinRange: function(date){\n			return date >= this.o.startDate && date <= this.o.endDate;\n		},\n\n		keydown: function(e){\n			if (this.picker.is(':not(:visible)')){\n				if (e.keyCode == 27) // allow escape to hide and re-show picker\n					this.show();\n				return;\n			}\n			var dateChanged = false,\n				dir, day, month,\n				newDate, newViewDate;\n			switch(e.keyCode){\n				case 27: // escape\n					this.hide();\n					e.preventDefault();\n					break;\n				case 37: // left\n				case 39: // right\n					if (!this.o.keyboardNavigation) break;\n					dir = e.keyCode == 37 ? -1 : 1;\n					if (e.ctrlKey){\n						newDate = this.moveYear(this.date, dir);\n						newViewDate = this.moveYear(this.viewDate, dir);\n					} else if (e.shiftKey){\n						newDate = this.moveMonth(this.date, dir);\n						newViewDate = this.moveMonth(this.viewDate, dir);\n					} else {\n						newDate = new Date(this.date);\n						newDate.setUTCDate(this.date.getUTCDate() + dir);\n						newViewDate = new Date(this.viewDate);\n						newViewDate.setUTCDate(this.viewDate.getUTCDate() + dir);\n					}\n					if (this.dateWithinRange(newDate)){\n						this.date = newDate;\n						this.viewDate = newViewDate;\n						this.setValue();\n						this.update();\n						e.preventDefault();\n						dateChanged = true;\n					}\n					break;\n				case 38: // up\n				case 40: // down\n					if (!this.o.keyboardNavigation) break;\n					dir = e.keyCode == 38 ? -1 : 1;\n					if (e.ctrlKey){\n						newDate = this.moveYear(this.date, dir);\n						newViewDate = this.moveYear(this.viewDate, dir);\n					} else if (e.shiftKey){\n						newDate = this.moveMonth(this.date, dir);\n						newViewDate = this.moveMonth(this.viewDate, dir);\n					} else {\n						newDate = new Date(this.date);\n						newDate.setUTCDate(this.date.getUTCDate() + dir * 7);\n						newViewDate = new Date(this.viewDate);\n						newViewDate.setUTCDate(this.viewDate.getUTCDate() + dir * 7);\n					}\n					if (this.dateWithinRange(newDate)){\n						this.date = newDate;\n						this.viewDate = newViewDate;\n						this.setValue();\n						this.update();\n						e.preventDefault();\n						dateChanged = true;\n					}\n					break;\n				case 13: // enter\n					this.hide();\n					e.preventDefault();\n					break;\n				case 9: // tab\n					this.hide();\n					break;\n			}\n			if (dateChanged){\n				this._trigger('changeDate');\n				var element;\n				if (this.isInput) {\n					element = this.element;\n				} else if (this.component){\n					element = this.element.find('input');\n				}\n				if (element) {\n					element.change();\n				}\n			}\n		},\n\n		showMode: function(dir) {\n			if (dir) {\n				this.viewMode = Math.max(this.o.minViewMode, Math.min(2, this.viewMode + dir));\n			}\n			/*\n				vitalets: fixing bug of very special conditions:\n				jquery 1.7.1 + webkit + show inline datepicker in bootstrap popover.\n				Method show() does not set display css correctly and datepicker is not shown.\n				Changed to .css('display', 'block') solve the problem.\n				See https://github.com/vitalets/x-editable/issues/37\n\n				In jquery 1.7.2+ everything works fine.\n			*/\n			//this.picker.find('>div').hide().filter('.datepicker-'+DPGlobal.modes[this.viewMode].clsName).show();\n			this.picker.find('>div').hide().filter('.datepicker-'+DPGlobal.modes[this.viewMode].clsName).css('display', 'block');\n			this.updateNavArrows();\n		}\n	};\n\n	var DateRangePicker = function(element, options){\n		this.element = $(element);\n		this.inputs = $.map(options.inputs, function(i){ return i.jquery ? i[0] : i; });\n		delete options.inputs;\n\n		$(this.inputs)\n			.datepicker(options)\n			.bind('changeDate', $.proxy(this.dateUpdated, this));\n\n		this.pickers = $.map(this.inputs, function(i){ return $(i).data('datepicker'); });\n		this.updateDates();\n	};\n	DateRangePicker.prototype = {\n		updateDates: function(){\n			this.dates = $.map(this.pickers, function(i){ return i.date; });\n			this.updateRanges();\n		},\n		updateRanges: function(){\n			var range = $.map(this.dates, function(d){ return d.valueOf(); });\n			$.each(this.pickers, function(i, p){\n				p.setRange(range);\n			});\n		},\n		dateUpdated: function(e){\n			var dp = $(e.target).data('datepicker'),\n				new_date = dp.getUTCDate(),\n				i = $.inArray(e.target, this.inputs),\n				l = this.inputs.length;\n			if (i == -1) return;\n\n			if (new_date < this.dates[i]){\n				// Date being moved earlier/left\n				while (i>=0 && new_date < this.dates[i]){\n					this.pickers[i--].setUTCDate(new_date);\n				}\n			}\n			else if (new_date > this.dates[i]){\n				// Date being moved later/right\n				while (i<l && new_date > this.dates[i]){\n					this.pickers[i++].setUTCDate(new_date);\n				}\n			}\n			this.updateDates();\n		},\n		remove: function(){\n			$.map(this.pickers, function(p){ p.remove(); });\n			delete this.element.data().datepicker;\n		}\n	};\n\n	function opts_from_el(el, prefix){\n		// Derive options from element data-attrs\n		var data = $(el).data(),\n			out = {}, inkey,\n			replace = new RegExp('^' + prefix.toLowerCase() + '([A-Z])'),\n			prefix = new RegExp('^' + prefix.toLowerCase());\n		for (var key in data)\n			if (prefix.test(key)){\n				inkey = key.replace(replace, function(_,a){ return a.toLowerCase(); });\n				out[inkey] = data[key];\n			}\n		return out;\n	}\n\n	function opts_from_locale(lang){\n		// Derive options from locale plugins\n		var out = {};\n		// Check if \"de-DE\" style date is available, if not language should\n		// fallback to 2 letter code eg \"de\"\n		if (!dates[lang]) {\n			lang = lang.split('-')[0]\n			if (!dates[lang])\n				return;\n		}\n		var d = dates[lang];\n		$.each($.fn.datepicker.locale_opts, function(i,k){\n			if (k in d)\n				out[k] = d[k];\n		});\n		return out;\n	}\n\n	var old = $.fn.datepicker;\n	$.fn.datepicker = function ( option ) {\n		var args = Array.apply(null, arguments);\n		args.shift();\n		var internal_return,\n			this_return;\n		this.each(function () {\n			var $this = $(this),\n				data = $this.data('datepicker'),\n				options = typeof option == 'object' && option;\n			if (!data) {\n				var elopts = opts_from_el(this, 'date'),\n					// Preliminary otions\n					xopts = $.extend({}, $.fn.datepicker.defaults, elopts, options),\n					locopts = opts_from_locale(xopts.language),\n					// Options priority: js args, data-attrs, locales, defaults\n					opts = $.extend({}, $.fn.datepicker.defaults, locopts, elopts, options);\n				if ($this.is('.input-daterange') || opts.inputs){\n					var ropts = {\n						inputs: opts.inputs || $this.find('input').toArray()\n					};\n					$this.data('datepicker', (data = new DateRangePicker(this, $.extend(opts, ropts))));\n				}\n				else{\n					$this.data('datepicker', (data = new Datepicker(this, opts)));\n				}\n			}\n			if (typeof option == 'string' && typeof data[option] == 'function') {\n				internal_return = data[option].apply(data, args);\n				if (internal_return !== undefined)\n					return false;\n			}\n		});\n		if (internal_return !== undefined)\n			return internal_return;\n		else\n			return this;\n	};\n\n	$.fn.datepicker.defaults = {\n		autoclose: false,\n		beforeShowDay: $.noop,\n		calendarWeeks: false,\n		clearBtn: false,\n		daysOfWeekDisabled: [],\n		endDate: Infinity,\n		forceParse: true,\n		format: 'mm/dd/yyyy',\n		keyboardNavigation: true,\n		language: 'en',\n		minViewMode: 0,\n		rtl: false,\n		startDate: -Infinity,\n		startView: 0,\n		todayBtn: false,\n		todayHighlight: false,\n		weekStart: 0\n	};\n	$.fn.datepicker.locale_opts = [\n		'format',\n		'rtl',\n		'weekStart'\n	];\n	$.fn.datepicker.Constructor = Datepicker;\n	var dates = $.fn.datepicker.dates = {\n		en: {\n			days: [\"Sunday\", \"Monday\", \"Tuesday\", \"Wednesday\", \"Thursday\", \"Friday\", \"Saturday\", \"Sunday\"],\n			daysShort: [\"Sun\", \"Mon\", \"Tue\", \"Wed\", \"Thu\", \"Fri\", \"Sat\", \"Sun\"],\n			daysMin: [\"Su\", \"Mo\", \"Tu\", \"We\", \"Th\", \"Fr\", \"Sa\", \"Su\"],\n			months: [\"January\", \"February\", \"March\", \"April\", \"May\", \"June\", \"July\", \"August\", \"September\", \"October\", \"November\", \"December\"],\n			monthsShort: [\"Jan\", \"Feb\", \"Mar\", \"Apr\", \"May\", \"Jun\", \"Jul\", \"Aug\", \"Sep\", \"Oct\", \"Nov\", \"Dec\"],\n			today: \"Today\",\n			clear: \"Clear\"\n		}\n	};\n\n	var DPGlobal = {\n		modes: [\n			{\n				clsName: 'days',\n				navFnc: 'Month',\n				navStep: 1\n			},\n			{\n				clsName: 'months',\n				navFnc: 'FullYear',\n				navStep: 1\n			},\n			{\n				clsName: 'years',\n				navFnc: 'FullYear',\n				navStep: 10\n		}],\n		isLeapYear: function (year) {\n			return (((year % 4 === 0) && (year % 100 !== 0)) || (year % 400 === 0));\n		},\n		getDaysInMonth: function (year, month) {\n			return [31, (DPGlobal.isLeapYear(year) ? 29 : 28), 31, 30, 31, 30, 31, 31, 30, 31, 30, 31][month];\n		},\n		validParts: /dd?|DD?|mm?|MM?|yy(?:yy)?/g,\n		nonpunctuation: /[^ -\\/:-@\\[\\u3400-\\u9fff-`{-~\\t\\n\\r]+/g,\n		parseFormat: function(format){\n			// IE treats \\0 as a string end in inputs (truncating the value),\n			// so it's a bad format delimiter, anyway\n			var separators = format.replace(this.validParts, '\\0').split('\\0'),\n				parts = format.match(this.validParts);\n			if (!separators || !separators.length || !parts || parts.length === 0){\n				throw new Error(\"Invalid date format.\");\n			}\n			return {separators: separators, parts: parts};\n		},\n		parseDate: function(date, format, language) {\n			if (date instanceof Date) return date;\n			if (typeof format === 'string')\n				format = DPGlobal.parseFormat(format);\n			if (/^[\\-+]\\d+[dmwy]([\\s,]+[\\-+]\\d+[dmwy])*$/.test(date)) {\n				var part_re = /([\\-+]\\d+)([dmwy])/,\n					parts = date.match(/([\\-+]\\d+)([dmwy])/g),\n					part, dir;\n				date = new Date();\n				for (var i=0; i<parts.length; i++) {\n					part = part_re.exec(parts[i]);\n					dir = parseInt(part[1]);\n					switch(part[2]){\n						case 'd':\n							date.setUTCDate(date.getUTCDate() + dir);\n							break;\n						case 'm':\n							date = Datepicker.prototype.moveMonth.call(Datepicker.prototype, date, dir);\n							break;\n						case 'w':\n							date.setUTCDate(date.getUTCDate() + dir * 7);\n							break;\n						case 'y':\n							date = Datepicker.prototype.moveYear.call(Datepicker.prototype, date, dir);\n							break;\n					}\n				}\n				return UTCDate(date.getUTCFullYear(), date.getUTCMonth(), date.getUTCDate(), 0, 0, 0);\n			}\n			var parts = date && date.match(this.nonpunctuation) || [],\n				date = new Date(),\n				parsed = {},\n				setters_order = ['yyyy', 'yy', 'M', 'MM', 'm', 'mm', 'd', 'dd'],\n				setters_map = {\n					yyyy: function(d,v){ return d.setUTCFullYear(v); },\n					yy: function(d,v){ return d.setUTCFullYear(2000+v); },\n					m: function(d,v){\n						v -= 1;\n						while (v<0) v += 12;\n						v %= 12;\n						d.setUTCMonth(v);\n						while (d.getUTCMonth() != v)\n							d.setUTCDate(d.getUTCDate()-1);\n						return d;\n					},\n					d: function(d,v){ return d.setUTCDate(v); }\n				},\n				val, filtered, part;\n			setters_map['M'] = setters_map['MM'] = setters_map['mm'] = setters_map['m'];\n			setters_map['dd'] = setters_map['d'];\n			date = UTCDate(date.getFullYear(), date.getMonth(), date.getDate(), 0, 0, 0);\n			var fparts = format.parts.slice();\n			// Remove noop parts\n			if (parts.length != fparts.length) {\n				fparts = $(fparts).filter(function(i,p){\n					return $.inArray(p, setters_order) !== -1;\n				}).toArray();\n			}\n			// Process remainder\n			if (parts.length == fparts.length) {\n				for (var i=0, cnt = fparts.length; i < cnt; i++) {\n					val = parseInt(parts[i], 10);\n					part = fparts[i];\n					if (isNaN(val)) {\n						switch(part) {\n							case 'MM':\n								filtered = $(dates[language].months).filter(function(){\n									var m = this.slice(0, parts[i].length),\n										p = parts[i].slice(0, m.length);\n									return m == p;\n								});\n								val = $.inArray(filtered[0], dates[language].months) + 1;\n								break;\n							case 'M':\n								filtered = $(dates[language].monthsShort).filter(function(){\n									var m = this.slice(0, parts[i].length),\n										p = parts[i].slice(0, m.length);\n									return m == p;\n								});\n								val = $.inArray(filtered[0], dates[language].monthsShort) + 1;\n								break;\n						}\n					}\n					parsed[part] = val;\n				}\n				for (var i=0, s; i<setters_order.length; i++){\n					s = setters_order[i];\n					if (s in parsed && !isNaN(parsed[s]))\n						setters_map[s](date, parsed[s]);\n				}\n			}\n			return date;\n		},\n		formatDate: function(date, format, language){\n			if (typeof format === 'string')\n				format = DPGlobal.parseFormat(format);\n			var val = {\n				d: date.getUTCDate(),\n				D: dates[language].daysShort[date.getUTCDay()],\n				DD: dates[language].days[date.getUTCDay()],\n				m: date.getUTCMonth() + 1,\n				M: dates[language].monthsShort[date.getUTCMonth()],\n				MM: dates[language].months[date.getUTCMonth()],\n				yy: date.getUTCFullYear().toString().substring(2),\n				yyyy: date.getUTCFullYear()\n			};\n			val.dd = (val.d < 10 ? '0' : '') + val.d;\n			val.mm = (val.m < 10 ? '0' : '') + val.m;\n			var date = [],\n				seps = $.extend([], format.separators);\n			for (var i=0, cnt = format.parts.length; i <= cnt; i++) {\n				if (seps.length)\n					date.push(seps.shift());\n				date.push(val[format.parts[i]]);\n			}\n			return date.join('');\n		},\n		headTemplate: '<thead>'+\n							'<tr>'+\n								'<th class=\"prev\"><i class=\"icon-arrow-left\"/></th>'+\n								'<th colspan=\"5\" class=\"datepicker-switch\"></th>'+\n								'<th class=\"next\"><i class=\"icon-arrow-right\"/></th>'+\n							'</tr>'+\n						'</thead>',\n		contTemplate: '<tbody><tr><td colspan=\"7\"></td></tr></tbody>',\n		footTemplate: '<tfoot><tr><th colspan=\"7\" class=\"today\"></th></tr><tr><th colspan=\"7\" class=\"clear\"></th></tr></tfoot>'\n	};\n	DPGlobal.template = '<div class=\"datepicker\">'+\n							'<div class=\"datepicker-days\">'+\n								'<table class=\" table-condensed\">'+\n									DPGlobal.headTemplate+\n									'<tbody></tbody>'+\n									DPGlobal.footTemplate+\n								'</table>'+\n							'</div>'+\n							'<div class=\"datepicker-months\">'+\n								'<table class=\"table-condensed\">'+\n									DPGlobal.headTemplate+\n									DPGlobal.contTemplate+\n									DPGlobal.footTemplate+\n								'</table>'+\n							'</div>'+\n							'<div class=\"datepicker-years\">'+\n								'<table class=\"table-condensed\">'+\n									DPGlobal.headTemplate+\n									DPGlobal.contTemplate+\n									DPGlobal.footTemplate+\n								'</table>'+\n							'</div>'+\n						'</div>';\n\n	$.fn.datepicker.DPGlobal = DPGlobal;\n\n\n	/* DATEPICKER NO CONFLICT\n	* =================== */\n\n	$.fn.datepicker.noConflict = function(){\n		$.fn.datepicker = old;\n		return this;\n	};\n\n\n	/* DATEPICKER DATA-API\n	* ================== */\n\n	$(document).on(\n		'focus.datepicker.data-api click.datepicker.data-api',\n		'[data-provide=\"datepicker\"]',\n		function(e){\n			var $this = $(this);\n			if ($this.data('datepicker')) return;\n			e.preventDefault();\n			// component click requires us to explicitly show it\n			$this.datepicker('show');\n		}\n	);\n	$(function(){\n		$('[data-provide=\"datepicker-inline\"]').datepicker();\n	});\n\n}( window.jQuery ));\n");

/*bithub/admin/rewards/form/form*/
define("bithub/admin/rewards/form/form",["can/can","./form.mustache!","bithub/models/reward","fileupload-processing","jquerypp/dom/form_params/form_params","bootstrap-datepicker/bootstrap-datepicker"],function(t,e,a){return t.Control.extend({defaults:{Reward:a}},{init:function(t,a){t.html(e({reward:a.reward},{datepicker:function(){return function(t){$(t).datepicker({format:"yyyy-mm-dd",weekStart:0})}},formatTs:function(t){return t="function"==typeof t?t():t,t?moment(t).format("YYYY-MM-DD"):""}}));var i=this;t.fileupload({url:"/api/rewards",datatype:"json",limitMultiFileUploads:1,replaceFileInput:!1,acceptFileTypes:/(\.|\/)(gif|jpe?g|png)$/i,add:function(t,e){i.options.fileData=e}})}," fileuploaddone":function(){t.route.attr({page:"admin",view:"rewards",action:"list"})}," submit":function(t,e){if(e.preventDefault(),this.options.fileData){var a=this.options.reward.attr("id");a&&this.element.fileupload("option",{type:"PUT",url:"/api/rewards/"+a}),this.options.fileData.submit()}else{var i=this.options.reward.attr(this.element.formParams());i.save()}},"{Reward} created":"gotoList","{Reward} updated":"gotoList",gotoList:function(){t.route.attr({page:"admin",view:"rewards",action:"list"})}})});
/*bithub/admin/rewards/rewards*/
define("bithub/admin/rewards/rewards",["can/can","bithub/models/reward","./rewards.mustache!","./form/form","../paginator"],function(t,e,r,n,i){var a=function(t){return"edit"===t||"new"===t},o={order:"point_minimum"},d={page:"admin",view:"rewards",action:"list"},u=function(e){return t.route.url(t.extend({offset:e},d))};return t.Control({pluginName:"admin-rewards",defaults:{Reward:e}},{init:function(){paginator=new i(t.route.attr("offset")),a(t.route.attr("action"))?this.loadForm():this.loadList()},loadForm:function(){var r=this,i=$("<div/>");t.route.attr("id")?e.findOne({id:t.route.attr("id")},function(t){new n(i,{reward:t})}):new n(i,{reward:new e}),r.element.html(i)},loadList:function(){var n=this;e.findAll(t.extend({},paginator.currentState(),o),function(e){n.element.html(r({rewards:e,prevOffsetUrl:function(){return u(paginator.prevOffset())},nextOffsetUrl:function(){return u(paginator.nextOffset())}},{editRewardUrl:function(e){return t.route.url({page:"admin",view:"rewards",action:"edit",id:e.id})},newRewardUrl:function(){return t.route.url({page:"admin",view:"rewards",action:"new"})}}))})},"{can.route} offset":function(t,e,r){paginator.updateOffset(r),this.loadList()},"{can.route} action":function(t,e,r){a(r)?this.loadForm():this.loadList()},"td .delete-reward click":function(t){confirm("Are you sure?")&&t.closest("tr").data("reward").destroy()},"{Reward} destroyed":function(){}})});
/*bithub/admin/tags/tags.mustache!can/view/mustache/system*/
define("bithub/admin/tags/tags.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/admin/tags/tags.mustache",t.Mustache(function(e,s){var n=[];return n.push('<table class="table table-stripped">\n	<thead>\n		<tr>\n			<th>Name</th>\n			<th>Display Name</th>\n		</tr>\n	</thead>\n	<tbody>'),n.push("\n"),n.push(t.view.txt(0,"tbody",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"tags"},[{fn:function(e,s){var n=[];return n.push("		\n		<tr "),n.push(t.view.txt(1,"tr",1,this,function(){return t.proxy(function(e){t.data(t.$(e),"tag",this.attr("."))},e)})),n.push("",t.view.pending({scope:e,options:s}),">"),n.push('\n			<td><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"editTagUrl"},{get:"this"}))),n.push('"',t.view.pending({scope:e,options:s}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"name"}))),n.push("</a></td>\n			<td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"display_name"}))),n.push('</td>\n			<td><button class="delete-tag close">&times;</button></td>\n		</tr>'),n.join("")}}]))),n.push('	</tbody>\n</table>\n<div class="actions pull-left">\n	<a class="btn" href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"newTagUrl"}))),n.push('"',t.view.pending({scope:e,options:s}),">"),n.push('New reward</a>\n</div>\n<div class="pagination pull-right">\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"prevOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:e,options:s}),">"),n.push('Prev page</a>\n	<a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"nextOffsetUrl"}))),n.push('" class="btn"',t.view.pending({scope:e,options:s}),">"),n.push("Next page</a>\n</div>\n"),n.join("")}))});
/*bithub/admin/tags/form/form.mustache!can/view/mustache/system*/
define("bithub/admin/tags/form/form.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(n){return n.view.preloadStringRenderer("bithub/admin/tags/form/form.mustache",n.Mustache(function(t,e){var a=[];return a.push('<div id="new-tag" class="row">\n	<form class="form-horizontal">\n		<div class="control-group">\n			<label class="control-label" for="name">Name</label>\n			<div class="controls">\n				<input type="text" name="tag[name]" placeholder="Name" value="'),a.push(n.view.txt(!0,"input","value",this,n.Mustache.txt({scope:t,options:e},null,{get:"tag.name"}))),a.push('" ',n.view.pending({scope:t,options:e}),"/>"),a.push('\n			</div>\n		</div>\n		<div class="control-group">\n			<label class="control-label" for="display_name">Display name</label>\n			<div class="controls">\n				<input type="text" name="tag[display_name]" placeholder="Display name" value="'),a.push(n.view.txt(!0,"input","value",this,n.Mustache.txt({scope:t,options:e},null,{get:"tag.display_name"}))),a.push('" ',n.view.pending({scope:t,options:e}),"/>"),a.push('\n			</div>\n		</div>\n		<div class="control-group">\n			<div class="controls">\n				<button type="submit" name="submit" class="btn">Submit</button>\n			</div>\n		</div>\n	</form>\n</div>\n'),a.join("")}))});
/*bithub/admin/tags/form/form*/
define("bithub/admin/tags/form/form",["can/can","./form.mustache!","bithub/models/tag","jquerypp/dom/form_params/form_params"],function(t,a,n){return t.Control.extend({defaults:{Tag:n}},{init:function(t,n){t.html(a({tag:n.tag}))}," submit":function(t,a){a.preventDefault(),this.options.tag.attr(this.element.formParams()),this.options.tag.save()},"{Tag} created":function(){t.route.attr({page:"admin",view:"tags",action:"list"})},"{Tag} updated":function(){t.route.attr({page:"admin",view:"tags",action:"list"})}})});
/*bithub/admin/tags/tags*/
define("bithub/admin/tags/tags",["can/can","bithub/models/tag","./tags.mustache!","bithub/admin/tags/form/form","../paginator"],function(t,n,e,i,a){var o,r=function(t){return"edit"===t||"new"===t},u={page:"admin",view:"tags",action:"list"},f=function(n){return t.route.url(t.extend({offset:n},u))};return t.Control.extend({pluginName:"admin-tags",defaults:{Tag:n}},{init:function(){o=new a(t.route.attr("offset")),r(t.route.attr("action"))?this.loadForm():this.loadList()},loadForm:function(){var e=this;t.route.attr("id")?n.findOne({id:t.route.attr("id")},function(t){new i(e.element,{tag:t})}):new i(e.element,{tag:new n})},loadList:function(){var i=this;n.findAll(o.currentState(),function(n){i.element.html(e({tags:n,prevOffsetUrl:function(){return f(o.prevOffset())},nextOffsetUrl:function(){return f(o.nextOffset())}},{editTagUrl:function(n){return t.route.url({page:"admin",view:"tags",action:"edit",id:n.id})},newTagUrl:function(){return t.route.url({page:"admin",view:"tags",action:"new"})}}))})},"{can.route} offset":function(t,n,e){o.updateOffset(e),this.loadList()},"{can.route} action":function(t,n,e){r(e)?this.loadForm():this.loadList()},"td .delete-tag click":function(t){confirm("Are you sure?")&&t.closest("tr").data("tag").destroy()},"{Tag} destroyed":function(){}})});
/*bithub/admin/achievements/init.mustache!can/view/mustache/system*/
define("bithub/admin/achievements/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/admin/achievements/init.mustache",t.Mustache(function(e,s){var n=[];return n.push('<table class="table table-stripped">\n  <thead>\n	<tr>\n	  <th>User</th>\n	  <th>Address</th>\n	  <th>Reward</th>\n	  <th>Achieved</th>\n	  <th>Shipped</th>\n	  <th></th>\n	</tr>\n  </thead>\n  <tbody>'),n.push("\n"),n.push(t.view.txt(0,"tbody",0,this,t.Mustache.txt({scope:e,options:s},"#",{get:"achievements"},[{fn:function(e,s){var n=[];return n.push("	<tr "),n.push(t.view.txt(1,"tr",1,this,function(){return t.proxy(function(e){t.data(t.$(e),"achievement",this.attr("."))},e)})),n.push("",t.view.pending({scope:e,options:s}),">"),n.push("\n	  <td>\n		"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.name"}))),n.push('<br>\n		<a href="mailto:'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:e,options:s},null,{get:"user.email"}))),n.push('"',t.view.pending({scope:e,options:s}),">"),n.push(t.view.txt(1,"a",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.email"}))),n.push("</a>\n	  </td>\n	  <td>\n		"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.address"}))),n.push("<br>\n		"),n.push(t.view.txt(1,"br",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.city"}))),n.push(" "),n.push(t.view.txt(1,"br",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.state"}))),n.push(" "),n.push(t.view.txt(1,"br",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.postal"}))),n.push("<br>\n		"),n.push(t.view.txt(1,"br",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"user.country.name"}))),n.push("\n	  </td>\n	  <td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"reward.title"}))),n.push("</td>\n	  <td>"),n.push(t.view.txt(1,"td",0,this,t.Mustache.txt({scope:e,options:s},null,{get:"formatTs"},{get:"achieved_at"}))),n.push('</td>\n	  <td><input name="shipped_at" type="text" value="'),n.push(t.view.txt(!0,"input","value",this,t.Mustache.txt({scope:e,options:s},null,{get:"formatTs"},{get:"shipped_at"}))),n.push('" '),n.push(t.view.txt(1,"input",1,this,t.Mustache.txt({scope:e,options:s},null,{get:"datepicker"}))),n.push(" ",t.view.pending({scope:e,options:s}),"/>"),n.push('</td>\n	  <td>\n		<button class="btn btn-primary save">Save</button>\n	  </td>\n	</tr>'),n.push("\n"),n.join("")}}]))),n.push("  </tbody>\n</table>\n"),n.join("")}))});
/*bithub/models/achievement*/
define("bithub/models/achievement",["can/can","can/list/list","vendor/moment/moment"],function(e){var i=e.Model("Bithub.Models.Achievement",{findAll:"GET /api/v1/achievements",findOne:"GET /api/v1/achievements/{id}",create:"POST /api/v1/achievements",update:"PUT /api/v1/achievements/{id}",destroy:"DELETE /api/v1/achievements/{id}"},{});return i});
/*bithub/admin/achievements/achievements*/
define("bithub/admin/achievements/achievements",["can/can","./init.mustache!","bithub/models/achievement","bootstrap-datepicker/bootstrap-datepicker","vendor/moment/moment"],function(e,t,n){return e.Control.extend({defaults:{achievements:new e.Observe.List}},{init:function(){var e=this.options.achievements;n.findAll({},function(t){e.replace(t)}),this.element.html(t({achievements:e},{datepicker:function(){return function(e){$(e).datepicker({format:"yyyy-mm-dd",weekStart:0})}},formatTs:function(e){return e="function"==typeof e?e():e,e?moment(e).format("YYYY-MM-DD"):""}}))},".btn.save click":function(e,t){t.preventDefault();var n=e.closest("tr").data("achievement"),a=e.closest("tr").find("input[name=shipped_at]").val();n.attr("shipped_at",a&&moment(a).format()),n.save(function(){e.removeClass("btn-primary").addClass("btn-success"),setTimeout(function(){e.removeClass("btn-success").addClass("btn-primary")},3e3)},function(){e.removeClass("btn-primary").addClass("btn-danger"),setTimeout(function(){e.removeClass("btn-danger").addClass("btn-primary")},3e3)})}})});
/*bithub/admin/admin*/
define("bithub/admin/admin",["can/can","bithub/helpers/fun_helpers","bithub/admin/users/users","bithub/admin/rewards/rewards","bithub/admin/tags/tags","bithub/admin/achievements/achievements"],function(i,n,t,e,s,r){return i.Control.extend("Bithub.Controls.Admin",{requiredPermissions:["admin"],defaults:{views:{users:t,rewards:e,tags:s,achievements:r}},pluginName:"admin"},{init:function(){this.initControl(i.route.attr("view"))},"{can.route} view":function(i,n,t){this.initControl(t)},initControl:function(i){var n=this.options.views[i],t=$("<div/>");new n(t,this.options),this.element.html(t)}})});
/*bithub/leaderboard/leaderboard.mustache!can/view/mustache/system*/
define("bithub/leaderboard/leaderboard.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(s){return s.view.preloadStringRenderer("bithub/leaderboard/leaderboard.mustache",s.Mustache(function(t,e){var n=[];return n.push('<div class="leaderboard-wrap">\n	<h4>Leaderboard</h4>'),n.push(s.view.txt(0,"div",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"if"},{get:"isAdmin"},[{fn:function(t,e){var n=[];return n.push('		<select class="include-admins">\n			<option value="filter-admins"',s.view.pending({scope:t,options:e}),">"),n.push('Filter Admins</option>\n			<option value="include-admins"',s.view.pending({scope:t,options:e}),">"),n.push("Include Admins</option>\n		</select>\n		<hr>"),n.push("\n"),n.join("")}}]))),n.push('	<ul class="unstyled">'),n.push("\n"),n.push(s.view.txt(0,"ul",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"each"},{get:"users"},[{fn:function(t,e){var n=[];return n.push('			<li class="clearfix">\n				<span class="position pull-left">#'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"calcIndex"},{get:"@index"}))),n.push("</span>\n				"),n.push(s.view.txt(0,"li",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"avatarUrl"},[{fn:function(t,e){var n=[];return n.push('<img class="avatar pull-left" '),n.push(s.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(s.view.toStr(s.Mustache.txt({scope:t,options:e,special:!0},null,{get:"."})())),n.push('?size=22"'),n.join("")})),n.push(' alt="avatar" '),n.push(s.view.txt(1,"img",1,this,s.Mustache.txt({scope:t,options:e},null,{get:"imgErrorHandler"}))),n.push("",s.view.pending({scope:t,options:e}),">"),n.join("")}}]))),n.push('\n			<span class="leaderboard-name pull-left">\n				<a href="/profile/'),n.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"id"}))),n.push('"',s.view.pending({scope:t,options:e}),">"),n.push(s.view.txt(1,"a",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"name"}))),n.push('</a>\n			</span>\n			<span class="points pull-right">'),n.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"score"}))),n.push("</span>\n			</li>"),n.push("\n"),n.join("")}}]))),n.push("	</ul>\n</div>"),n.join("")}))});
/*bithub/leaderboard/leaderboard*/
define("bithub/leaderboard/leaderboard",["can/control/control","./leaderboard.mustache!"],function(e,s){var t=function(e){return can.grep(e,function(e){return!e.attr("roles")||0===e.attr("roles").length})};return e.extend({init:function(){var e=this;this.users=new Bithub.Models.User.List(t(this.options.users)),this.element.html(s({users:this.users,isAdmin:function(){return e.options.currentUser.isAdmin()}},{calcIndex:function(e){var s=parseInt(can.isFunction(e)?e():e,10)+1;return 10>s?s="00"+s:100>s&&(s="0"+s),s}})),this.__updatedUsers=!1},"{users} length":function(){this.__updatedUsers||(this.replaceUsers(this.element.find(".include-admins")),this.__updatedUsers=!0)},".include-admins change":"replaceUsers",replaceUsers:function(e){var s=e.val(),n="include-admins"===s?this.options.users:t(this.options.users),r=this,i=function(){r.users.push.apply(r.users,n.splice(0,20)),n.length>0&&(r.__appendUsers=setTimeout(i,1))};clearTimeout(this.__appendUsers),this.users.splice(0),i()}})});
/*bithub/user_profile/user_profile.mustache!can/view/mustache/system*/
define("bithub/user_profile/user_profile.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/user_profile/user_profile.mustache",t.Mustache(function(s,e){var n=[];return n.push('<div class="profile-content user-profile-page">\n	<h3 class="user-profile-header span10 offset1">\n		User profile\n		<a href="/leaderboard" class="leaderboard">Leaderboard</a>\n	</h3>\n	<div class="span2 user-profile-wrapper offset1">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"user"},[{fn:function(s,e){var n=[];return n.push("			"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"avatarUrl"},[{fn:function(s,e){var n=[];return n.push('\n				<div class="avatar">\n					'),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"avatarUrl"},[{fn:function(s,e){var n=[];return n.push("<img "),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:e,special:!0},null,{get:"."})())),n.push('"'),n.join("")})),n.push(' alt="avatar"',t.view.pending({scope:s,options:e}),">"),n.join("")}}]))),n.push("\n				</div>"),n.push("\n"),n.join("")}}]))),n.push('			<h4 class="username">'),n.push(t.view.txt(1,"h4",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"name"}))),n.push("</h4>\n			"),n.push(t.view.txt(1,"img",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"score"}))),n.push(" "),n.push(t.view.txt(1,"img",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"pointInflector"},{get:"score"}))),n.push("\n			<hr>\n			"),n.push(t.view.txt(0,"hr",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"userUrls"},{get:"this"}))),n.push("\n"),n.join("")}}]))),n.push('	</div>\n	<div class="span8">\n		\n		<div class="events-group latest-group">'),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"if"},{get:"loadedEvents"},[{fn:function(s,e){var n=[];return n.push("				"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"data"},[{fn:function(s,e){var n=[];return n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"hasDigest"},[{fn:function(s,e){var n=[];return n.push('						<bh-digest-list digest="digest"',t.view.pending({tagName:"bh-digest-list",scope:s,options:e})),n.push("></bh-digest-list>"),n.push("\n"),n.join("")}}]))),n.push("					"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"dailyCategories"},[{fn:function(s,e){var n=[];return n.push('\n						<div class="category-'),n.push(t.view.txt(!0,"div","class",this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push('\n							<h3 class="section-title"><i class="icon-'),n.push(t.view.txt(!0,"i","class",this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),n.push('"',t.view.pending({scope:s,options:e}),">"),n.push("</i>"),n.push(t.view.txt(1,"h3",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"currentCategory"}))),n.push("</h3>\n							"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},null,{get:"entityComponent"},{get:"events"}))),n.push("\n						</div>"),n.push("\n"),n.join("")}}]))),n.push("					"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"when"},{get:"types.code"},[{fn:function(s,e){var n=[];return n.push('\n						<div class="category-code">\n							<h3 class="section-title"><i class="icon-code"></i>Code</h3>\n							<bh-code events="code" currentdate="date"',t.view.pending({tagName:"bh-code",scope:s,options:e})),n.push("></bh-code>\n						</div>"),n.push("\n"),n.join("")}}]))),n.push("				"),n.join("")}}]))),n.push("\n"),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:e},"#",{get:"unless"},{get:"hasEvents"},[{fn:function(){var t=[];return t.push('					<div class="alert alert-notice">\n						User has no events.\n					</div>'),t.push("\n"),t.join("")}}]))),n.push("			"),n.join("")}},{inverse:function(){var s=[];return s.push('\n				<div class="alert alert-info">\n					<img '),s.push(t.view.txt(2,"img","src",this,function(){var t=[];return t.push('src="'),t.push('/assets/images/ajax-loader-sm-blue.gif"'),t.join("")})),s.push(' alt="loading...">\n				</div>'),s.push("\n"),s.join("")}}]))),n.push("		</div>\n	</div>\n</div>"),n.join("")}))});
/*bithub/user_profile/user_profile*/
define("bithub/user_profile/user_profile",["can/can","./user_profile.mustache!","bithub/models/user","bithub/models/activity","bithub/homepage/event_list/determine_event_partial","bithub/homepage/event_list/latest_events_sorter","bithub/homepage/event_list/handlers/handlers","bithub/entities/entities"],function(t,e,i,n,r,s,a){var o={},u=["event","twitter","bug","comment","feature","question","article","plugin","app","uncategorized"];return t.Control.extend({pluginName:"user_profile"},{init:function(){var e=t.route.attr("id"),r=this;this.params={limit:200,offset:0},this.canLoad=!0,this.isLoading=t.compute(!1),this.activities=new n.List,this.user=t.compute(null),this.events=new s,i.findOne({id:e},function(t){r.user(t)}),new a(this.element,{currentUser:this.options.currentUser,modals:this.options.modals}),this.hasEvents=t.compute(!1),this.loadedEvents=t.compute(!1),this.loadActivities(),this.initControl()},initControl:function(){var i=this;this.element.html(e({user:this.user,isLoading:this.isLoading,data:this.events,loadedEvents:function(){return i.loadedEvents()&&i.user()},hasEvents:this.hasEvents,isLoggedIn:function(){return user.isLoggedIn()}},{addPlus:function(e){return e=t.isFunction(e)?e():e,e>0?"+"+e:void 0},eventUrl:function(){var e;return"author"==this.attr("type")?e=this.attr("id"):"award"==this.attr("type")&&(e=this.attr("event_id")),e?t.route.url({page:"eventdetails",id:e}):"javascript://"},userUrls:function(e){return t.map(e.attr("reduced_identities"),function(e){return t.sub('<a href="{url}" class="clearfix identity"><span class="login-{provider}"></span> {name}</a>',{url:e.attr("profile_url"),name:e.attr("username")||e.attr("name"),provider:e.attr("provider")})}).join("")},pointInflector:function(e){return e=t.isFunction(e)?e():e,1===e?"point":"points"},dailyCategories:function(e){return t.map(u,function(t){var i=e.context.attr("types").attr(t);return i&&i.length?e.fn({currentCategory:t,events:i}):""}).join("")},entityComponent:function(e){return"undefined"==typeof e.length&&(e=[e]),t.map(e,function(e){var n,s=r(e.attr("tags")),a='<{c} event="event" inited="inited" user="user"></{c}>';return"undefined"==typeof o[s]&&(o[s]=t.view.mustache(t.sub(a,{c:s}))),n=o[s].render({event:e,inited:!0,user:i.user})}).join("")}}))},loadActivities:function(){var e=t.route.attr("id");this.canLoad&&Bithub.Models.Event.findAll({author_id:e,order:"thread_updated_ts:desc",limit:500},this.proxy("updateActivities"))},updateActivities:function(t){this.events.appendEvents(t),this.loadedEvents(!0),this.hasEvents(t.length>0)}})});
/*bithub/pageswitcher/no_permissions.mustache!can/view/mustache/system*/
define("bithub/pageswitcher/no_permissions.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(e){return e.view.preloadStringRenderer("bithub/pageswitcher/no_permissions.mustache",e.Mustache(function(){var e=[];return e.push('<div class="alert alert-error alert-flag">\n  <strong>Sorry!</strong> You don\'t have permission to access this page. Please <a href="/?login=true">login</a> or return to <a href="/">home</a>. \n</div>'),e.join("")}))});
/*bithub/helpers/permission_checker*/
define("bithub/helpers/permission_checker",["vendor/lodash/lodash"],function(){return{needsPermissions:function(s){return _.has(s,"requiredPermissions")&&!_.isEmpty(s.requiredPermissions)},hasPermissions:function(s,e){var i;return this.needsPermissions(e)?(i=s.attr("roles")||[],_.isEqual(_.intersection(e.requiredPermissions,i),e.requiredPermissions)):!0}}});
/*bithub/pageswitcher/pageswitcher*/
define("bithub/pageswitcher/pageswitcher",["can/can","bithub/homepage/homepage","bithub/event_details/event_details","bithub/profile/earnpoints/earnpoints","bithub/profile/rewards/rewards","bithub/profile/profile","bithub/admin/admin","bithub/leaderboard/leaderboard","bithub/user_profile/user_profile","./no_permissions.mustache!","bithub/helpers/permission_checker"],function(e,t,i,r,n,o,s,a,u,h,l){return e.Control.extend({defaults:{pages:{homepage:t,eventdetails:i,earnpoints:r,rewards:n,profile:o,admin:s,user_profile:u,leaderboard:a}}},{init:function(){this.initControl(e.route.attr("page"))},"{can.route} page":function(e,t,i){window.scrollTo(0,0),this.initControl(i)},initControl:function(e){var t=this.options.pages[e],i=$("<div/>");l.hasPermissions(this.options.currentUser,t)?_.isEqual(e,this._currentPage)||(new t(i,this.options),this.element.html(i),this._currentPage=e):this.element.html(h())},"{currentUser} authStatus":function(t){if(t.isLoggedIn()){var i=this;setTimeout(function(){i.initControl(e.route.attr("page"))},0)}}})});
/*bithub/navigator/filterbar/filterbar.mustache!can/view/mustache/system*/
define("bithub/navigator/filterbar/filterbar.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(a){return a.view.preloadStringRenderer("bithub/navigator/filterbar/filterbar.mustache",a.Mustache(function(){var a=[];return a.push('<!-- GREATEST / LATEST -->\n<ul class="nav" id="viewFilter"></ul>\n<ul class="nav"><li class="divider-vertical"></li></ul>\n<!-- PROJECTS FILTER -->\n<ul class="nav" id="projectFilter"></ul>\n<ul class="nav"><li class="divider-vertical"></li></ul>\n<!-- CATEGORY FILTER -->\n<ul class="nav" id="categoryFilter"></ul>\n'),a.join("")}))});
/*can/view/ejs/ejs*/
define("can/view/ejs/ejs",["can/util/util","can/view/view","can/util/string/string","can/compute/compute","can/view/scanner","can/view/render"],function(t){var e=t.extend,n=function(t){if(this.constructor!==n){var r=new n(t);return function(t,e){return r.render(t,e)}}return"function"==typeof t?void(this.template={fn:t}):(e(this,t),void(this.template=this.scanner.scan(this.text,this.name)))};return t.EJS=n,n.prototype.render=function(t,e){return t=t||{},this.template.fn.call(t,t,new n.Helpers(t,e||{}))},e(n.prototype,{scanner:new t.view.Scanner({text:{outStart:"with(_VIEW) { with (_CONTEXT) {",outEnd:"}}",argNames:"_CONTEXT,_VIEW",context:"this"},tokens:[["templateLeft","<%%"],["templateRight","%>"],["returnLeft","<%=="],["escapeLeft","<%="],["commentLeft","<%#"],["left","<%"],["right","%>"],["returnRight","%>"]],helpers:[{name:/\s*\(([\$\w]+)\)\s*->([^\n]*)/,fn:function(t){var e=/\s*\(([\$\w]+)\)\s*->([^\n]*)/,n=t.match(e);return"can.proxy(function(__){var "+n[1]+"=can.$(__);"+n[2]+"}, this);"}}],transform:function(t){return t.replace(/<%([\s\S]+?)%>/gm,function(t,e){var n,r,i=[];e.replace(/[{}]/gm,function(t,e){i.push([t,e])});do for(n=!1,r=i.length-2;r>=0;r--)if("{"===i[r][0]&&"}"===i[r+1][0]){i.splice(r,2),n=!0;break}while(n);if(i.length>=2){var s,a=["<%"],c=0;for(r=0;s=i[r];r++)a.push(e.substring(c,c=s[1])),a.push("{"===s[0]&&r<i.length-1||"}"===s[0]&&r>0?"{"===s[0]?"{ %><% ":" %><% }":s[0]),++c;return a.push(e.substring(c),"%>"),a.join("")}return"<%"+e+"%>"})}})}),n.Helpers=function(t,n){this._data=t,this._extras=n,e(this,n)},n.Helpers.prototype={list:function(e,n){t.each(e,function(t,r){n(t,r,e)})},each:function(e,n){t.isArray(e)?this.list(e,n):t.view.lists(e,n)}},t.view.register({suffix:"ejs",script:function(t,e){return"can.EJS(function(_CONTEXT,_VIEW) { "+new n({text:e,name:t}).template.out+" })"},renderer:function(t,e){return n({text:e,name:t})}}),t.ejs.Helpers=n.Helpers,t});
/*can/view/ejs*/
System.set('can/view/ejs', Module({}));
/*can/view/ejs/system*/
System.set('can/view/ejs/system', Module({}));
/*ui/radio_selector/init.ejs!can/view/ejs/system*/
define("ui/radio_selector/init.ejs!can/view/ejs/system",["can/view/ejs/ejs"],function(can){return can.view.preloadStringRenderer("ui/radio_selector/init.ejs",can.EJS(function(_CONTEXT,_VIEW){with(_VIEW)with(_CONTEXT){var ___v1ew=[];return ___v1ew.push(can.view.txt(0,"ul",0,this,function(){var n=[];return can.each(items,function(e){n.push('\n<li>\n<a href="'),n.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(e)})),n.push('" class="'),n.push(can.view.txt(!0,"a","class",this,function(){return e.name})),n.push(" boxed item "),n.push(can.view.txt(!0,"a","class",this,function(){return isSelected(e)})),n.push('"',can.view.pending({scope:this}),">"),n.push('<span class="glyph"></span>'),n.push(can.view.txt(1,"a",0,this,function(){return e.display_name})),n.push("</a>\n</li>  \n")}),n.join("")})),___v1ew.push("\n"),___v1ew.join("")}}))});
/*ui/radio_selector/radio_selector*/
define("ui/radio_selector/radio_selector",["can/can","./init.ejs!"],function(t,e){var r=function(e){var r={category:t.route.attr("category"),project:t.route.attr("project"),view:e.name};return"bug"===t.route.attr("category")&&(r.state=t.route.attr("state")),t.route.url(r,!1)};return t.Control.extend({pluginName:"options-selector",defaults:{}},{init:function(n,i){var o=this,u=new t.Observe.List(i.items);n.html(e({items:u},{isSelected:function(t){var e=_.isFunction(t)?t():t.name;return o.options.state()===e?"active":""},itemUrl:function(t){return t.url?_.isFunction(t.url)?t.url():t.url:r(t)}}))}})});
/*ui/dropdown_selector/init.ejs!can/view/ejs/system*/
define("ui/dropdown_selector/init.ejs!can/view/ejs/system",["can/view/ejs/ejs"],function(can){return can.view.preloadStringRenderer("ui/dropdown_selector/init.ejs",can.EJS(function(_CONTEXT,_VIEW){with(_VIEW)with(_CONTEXT){var ___v1ew=[];return ___v1ew.push('<li class="dropdown active" {{#if htmlId}}id="{{htmlId}}"{{/if}}>\n  <a href="" class="dropdown-toggle" data-toggle="dropdown"><span class="title">'),___v1ew.push(can.view.txt(1,"span",0,this,function(){return selected()})),___v1ew.push('</span><b class="caret"></b></a>\n  <ul class="dropdown-menu">\n	  <li><a href="'),___v1ew.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(defaultOption)})),___v1ew.push('" class="item"',can.view.pending({scope:this}),">"),___v1ew.push(can.view.txt(1,"a",0,this,function(){return defaultOption.display_name})),___v1ew.push("</a></li>\n	  "),___v1ew.push(can.view.txt(0,"ul",0,this,function(){var e=[];return can.each(items,function(n){e.push('\n		<li><a href="'),e.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(n)})),e.push('" class="item"',can.view.pending({scope:this}),">"),e.push(can.view.txt(1,"a",0,this,function(){return n.display_name})),e.push("</a></li>	\n	  ")}),e.join("")})),___v1ew.push("\n  </ul>\n</li>\n"),___v1ew.join("")}}))});
/*bootstrap*/
System.define("bootstrap","/* ===================================================\n * bootstrap-transition.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#transitions\n * ===================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n  /* CSS TRANSITION SUPPORT (http://www.modernizr.com/)\n   * ======================================================= */\n\n  $(function () {\n\n    $.support.transition = (function () {\n\n      var transitionEnd = (function () {\n\n        var el = document.createElement('bootstrap')\n          , transEndEventNames = {\n               'WebkitTransition' : 'webkitTransitionEnd'\n            ,  'MozTransition'    : 'transitionend'\n            ,  'OTransition'      : 'oTransitionEnd otransitionend'\n            ,  'transition'       : 'transitionend'\n            }\n          , name\n\n        for (name in transEndEventNames){\n          if (el.style[name] !== undefined) {\n            return transEndEventNames[name]\n          }\n        }\n\n      }())\n\n      return transitionEnd && {\n        end: transitionEnd\n      }\n\n    })()\n\n  })\n\n}(window.jQuery);/* ==========================================================\n * bootstrap-alert.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#alerts\n * ==========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* ALERT CLASS DEFINITION\n  * ====================== */\n\n  var dismiss = '[data-dismiss=\"alert\"]'\n    , Alert = function (el) {\n        $(el).on('click', dismiss, this.close)\n      }\n\n  Alert.prototype.close = function (e) {\n    var $this = $(this)\n      , selector = $this.attr('data-target')\n      , $parent\n\n    if (!selector) {\n      selector = $this.attr('href')\n      selector = selector && selector.replace(/.*(?=#[^\\s]*$)/, '') //strip for ie7\n    }\n\n    $parent = $(selector)\n\n    e && e.preventDefault()\n\n    $parent.length || ($parent = $this.hasClass('alert') ? $this : $this.parent())\n\n    $parent.trigger(e = $.Event('close'))\n\n    if (e.isDefaultPrevented()) return\n\n    $parent.removeClass('in')\n\n    function removeElement() {\n      $parent\n        .trigger('closed')\n        .remove()\n    }\n\n    $.support.transition && $parent.hasClass('fade') ?\n      $parent.on($.support.transition.end, removeElement) :\n      removeElement()\n  }\n\n\n /* ALERT PLUGIN DEFINITION\n  * ======================= */\n\n  var old = $.fn.alert\n\n  $.fn.alert = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('alert')\n      if (!data) $this.data('alert', (data = new Alert(this)))\n      if (typeof option == 'string') data[option].call($this)\n    })\n  }\n\n  $.fn.alert.Constructor = Alert\n\n\n /* ALERT NO CONFLICT\n  * ================= */\n\n  $.fn.alert.noConflict = function () {\n    $.fn.alert = old\n    return this\n  }\n\n\n /* ALERT DATA-API\n  * ============== */\n\n  $(document).on('click.alert.data-api', dismiss, Alert.prototype.close)\n\n}(window.jQuery);/* ============================================================\n * bootstrap-button.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#buttons\n * ============================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* BUTTON PUBLIC CLASS DEFINITION\n  * ============================== */\n\n  var Button = function (element, options) {\n    this.$element = $(element)\n    this.options = $.extend({}, $.fn.button.defaults, options)\n  }\n\n  Button.prototype.setState = function (state) {\n    var d = 'disabled'\n      , $el = this.$element\n      , data = $el.data()\n      , val = $el.is('input') ? 'val' : 'html'\n\n    state = state + 'Text'\n    data.resetText || $el.data('resetText', $el[val]())\n\n    $el[val](data[state] || this.options[state])\n\n    // push to event loop to allow forms to submit\n    setTimeout(function () {\n      state == 'loadingText' ?\n        $el.addClass(d).attr(d, d) :\n        $el.removeClass(d).removeAttr(d)\n    }, 0)\n  }\n\n  Button.prototype.toggle = function () {\n    var $parent = this.$element.closest('[data-toggle=\"buttons-radio\"]')\n\n    $parent && $parent\n      .find('.active')\n      .removeClass('active')\n\n    this.$element.toggleClass('active')\n  }\n\n\n /* BUTTON PLUGIN DEFINITION\n  * ======================== */\n\n  var old = $.fn.button\n\n  $.fn.button = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('button')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('button', (data = new Button(this, options)))\n      if (option == 'toggle') data.toggle()\n      else if (option) data.setState(option)\n    })\n  }\n\n  $.fn.button.defaults = {\n    loadingText: 'loading...'\n  }\n\n  $.fn.button.Constructor = Button\n\n\n /* BUTTON NO CONFLICT\n  * ================== */\n\n  $.fn.button.noConflict = function () {\n    $.fn.button = old\n    return this\n  }\n\n\n /* BUTTON DATA-API\n  * =============== */\n\n  $(document).on('click.button.data-api', '[data-toggle^=button]', function (e) {\n    var $btn = $(e.target)\n    if (!$btn.hasClass('btn')) $btn = $btn.closest('.btn')\n    $btn.button('toggle')\n  })\n\n}(window.jQuery);/* ==========================================================\n * bootstrap-carousel.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#carousel\n * ==========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* CAROUSEL CLASS DEFINITION\n  * ========================= */\n\n  var Carousel = function (element, options) {\n    this.$element = $(element)\n    this.$indicators = this.$element.find('.carousel-indicators')\n    this.options = options\n    this.options.pause == 'hover' && this.$element\n      .on('mouseenter', $.proxy(this.pause, this))\n      .on('mouseleave', $.proxy(this.cycle, this))\n  }\n\n  Carousel.prototype = {\n\n    cycle: function (e) {\n      if (!e) this.paused = false\n      if (this.interval) clearInterval(this.interval);\n      this.options.interval\n        && !this.paused\n        && (this.interval = setInterval($.proxy(this.next, this), this.options.interval))\n      return this\n    }\n\n  , getActiveIndex: function () {\n      this.$active = this.$element.find('.item.active')\n      this.$items = this.$active.parent().children()\n      return this.$items.index(this.$active)\n    }\n\n  , to: function (pos) {\n      var activeIndex = this.getActiveIndex()\n        , that = this\n\n      if (pos > (this.$items.length - 1) || pos < 0) return\n\n      if (this.sliding) {\n        return this.$element.one('slid', function () {\n          that.to(pos)\n        })\n      }\n\n      if (activeIndex == pos) {\n        return this.pause().cycle()\n      }\n\n      return this.slide(pos > activeIndex ? 'next' : 'prev', $(this.$items[pos]))\n    }\n\n  , pause: function (e) {\n      if (!e) this.paused = true\n      if (this.$element.find('.next, .prev').length && $.support.transition.end) {\n        this.$element.trigger($.support.transition.end)\n        this.cycle(true)\n      }\n      clearInterval(this.interval)\n      this.interval = null\n      return this\n    }\n\n  , next: function () {\n      if (this.sliding) return\n      return this.slide('next')\n    }\n\n  , prev: function () {\n      if (this.sliding) return\n      return this.slide('prev')\n    }\n\n  , slide: function (type, next) {\n      var $active = this.$element.find('.item.active')\n        , $next = next || $active[type]()\n        , isCycling = this.interval\n        , direction = type == 'next' ? 'left' : 'right'\n        , fallback  = type == 'next' ? 'first' : 'last'\n        , that = this\n        , e\n\n      this.sliding = true\n\n      isCycling && this.pause()\n\n      $next = $next.length ? $next : this.$element.find('.item')[fallback]()\n\n      e = $.Event('slide', {\n        relatedTarget: $next[0]\n      , direction: direction\n      })\n\n      if ($next.hasClass('active')) return\n\n      if (this.$indicators.length) {\n        this.$indicators.find('.active').removeClass('active')\n        this.$element.one('slid', function () {\n          var $nextIndicator = $(that.$indicators.children()[that.getActiveIndex()])\n          $nextIndicator && $nextIndicator.addClass('active')\n        })\n      }\n\n      if ($.support.transition && this.$element.hasClass('slide')) {\n        this.$element.trigger(e)\n        if (e.isDefaultPrevented()) return\n        $next.addClass(type)\n        $next[0].offsetWidth // force reflow\n        $active.addClass(direction)\n        $next.addClass(direction)\n        this.$element.one($.support.transition.end, function () {\n          $next.removeClass([type, direction].join(' ')).addClass('active')\n          $active.removeClass(['active', direction].join(' '))\n          that.sliding = false\n          setTimeout(function () { that.$element.trigger('slid') }, 0)\n        })\n      } else {\n        this.$element.trigger(e)\n        if (e.isDefaultPrevented()) return\n        $active.removeClass('active')\n        $next.addClass('active')\n        this.sliding = false\n        this.$element.trigger('slid')\n      }\n\n      isCycling && this.cycle()\n\n      return this\n    }\n\n  }\n\n\n /* CAROUSEL PLUGIN DEFINITION\n  * ========================== */\n\n  var old = $.fn.carousel\n\n  $.fn.carousel = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('carousel')\n        , options = $.extend({}, $.fn.carousel.defaults, typeof option == 'object' && option)\n        , action = typeof option == 'string' ? option : options.slide\n      if (!data) $this.data('carousel', (data = new Carousel(this, options)))\n      if (typeof option == 'number') data.to(option)\n      else if (action) data[action]()\n      else if (options.interval) data.pause().cycle()\n    })\n  }\n\n  $.fn.carousel.defaults = {\n    interval: 5000\n  , pause: 'hover'\n  }\n\n  $.fn.carousel.Constructor = Carousel\n\n\n /* CAROUSEL NO CONFLICT\n  * ==================== */\n\n  $.fn.carousel.noConflict = function () {\n    $.fn.carousel = old\n    return this\n  }\n\n /* CAROUSEL DATA-API\n  * ================= */\n\n  $(document).on('click.carousel.data-api', '[data-slide], [data-slide-to]', function (e) {\n    var $this = $(this), href\n      , $target = $($this.attr('data-target') || (href = $this.attr('href')) && href.replace(/.*(?=#[^\\s]+$)/, '')) //strip for ie7\n      , options = $.extend({}, $target.data(), $this.data())\n      , slideIndex\n\n    $target.carousel(options)\n\n    if (slideIndex = $this.attr('data-slide-to')) {\n      $target.data('carousel').pause().to(slideIndex).cycle()\n    }\n\n    e.preventDefault()\n  })\n\n}(window.jQuery);/* =============================================================\n * bootstrap-collapse.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#collapse\n * =============================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* COLLAPSE PUBLIC CLASS DEFINITION\n  * ================================ */\n\n  var Collapse = function (element, options) {\n    this.$element = $(element)\n    this.options = $.extend({}, $.fn.collapse.defaults, options)\n\n    if (this.options.parent) {\n      this.$parent = $(this.options.parent)\n    }\n\n    this.options.toggle && this.toggle()\n  }\n\n  Collapse.prototype = {\n\n    constructor: Collapse\n\n  , dimension: function () {\n      var hasWidth = this.$element.hasClass('width')\n      return hasWidth ? 'width' : 'height'\n    }\n\n  , show: function () {\n      var dimension\n        , scroll\n        , actives\n        , hasData\n\n      if (this.transitioning || this.$element.hasClass('in')) return\n\n      dimension = this.dimension()\n      scroll = $.camelCase(['scroll', dimension].join('-'))\n      actives = this.$parent && this.$parent.find('> .accordion-group > .in')\n\n      if (actives && actives.length) {\n        hasData = actives.data('collapse')\n        if (hasData && hasData.transitioning) return\n        actives.collapse('hide')\n        hasData || actives.data('collapse', null)\n      }\n\n      this.$element[dimension](0)\n      this.transition('addClass', $.Event('show'), 'shown')\n      $.support.transition && this.$element[dimension](this.$element[0][scroll])\n    }\n\n  , hide: function () {\n      var dimension\n      if (this.transitioning || !this.$element.hasClass('in')) return\n      dimension = this.dimension()\n      this.reset(this.$element[dimension]())\n      this.transition('removeClass', $.Event('hide'), 'hidden')\n      this.$element[dimension](0)\n    }\n\n  , reset: function (size) {\n      var dimension = this.dimension()\n\n      this.$element\n        .removeClass('collapse')\n        [dimension](size || 'auto')\n        [0].offsetWidth\n\n      this.$element[size !== null ? 'addClass' : 'removeClass']('collapse')\n\n      return this\n    }\n\n  , transition: function (method, startEvent, completeEvent) {\n      var that = this\n        , complete = function () {\n            if (startEvent.type == 'show') that.reset()\n            that.transitioning = 0\n            that.$element.trigger(completeEvent)\n          }\n\n      this.$element.trigger(startEvent)\n\n      if (startEvent.isDefaultPrevented()) return\n\n      this.transitioning = 1\n\n      this.$element[method]('in')\n\n      $.support.transition && this.$element.hasClass('collapse') ?\n        this.$element.one($.support.transition.end, complete) :\n        complete()\n    }\n\n  , toggle: function () {\n      this[this.$element.hasClass('in') ? 'hide' : 'show']()\n    }\n\n  }\n\n\n /* COLLAPSE PLUGIN DEFINITION\n  * ========================== */\n\n  var old = $.fn.collapse\n\n  $.fn.collapse = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('collapse')\n        , options = $.extend({}, $.fn.collapse.defaults, $this.data(), typeof option == 'object' && option)\n      if (!data) $this.data('collapse', (data = new Collapse(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.collapse.defaults = {\n    toggle: true\n  }\n\n  $.fn.collapse.Constructor = Collapse\n\n\n /* COLLAPSE NO CONFLICT\n  * ==================== */\n\n  $.fn.collapse.noConflict = function () {\n    $.fn.collapse = old\n    return this\n  }\n\n\n /* COLLAPSE DATA-API\n  * ================= */\n\n  $(document).on('click.collapse.data-api', '[data-toggle=collapse]', function (e) {\n    var $this = $(this), href\n      , target = $this.attr('data-target')\n        || e.preventDefault()\n        || (href = $this.attr('href')) && href.replace(/.*(?=#[^\\s]+$)/, '') //strip for ie7\n      , option = $(target).data('collapse') ? 'toggle' : $this.data()\n    $this[$(target).hasClass('in') ? 'addClass' : 'removeClass']('collapsed')\n    $(target).collapse(option)\n  })\n\n}(window.jQuery);/* ============================================================\n * bootstrap-dropdown.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#dropdowns\n * ============================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* DROPDOWN CLASS DEFINITION\n  * ========================= */\n\n  var toggle = '[data-toggle=dropdown]'\n    , Dropdown = function (element) {\n        var $el = $(element).on('click.dropdown.data-api', this.toggle)\n        $('html').on('click.dropdown.data-api', function () {\n          $el.parent().removeClass('open')\n        })\n      }\n\n  Dropdown.prototype = {\n\n    constructor: Dropdown\n\n  , toggle: function (e) {\n      var $this = $(this)\n        , $parent\n        , isActive\n\n      if ($this.is('.disabled, :disabled')) return\n\n      $parent = getParent($this)\n\n      isActive = $parent.hasClass('open')\n\n      clearMenus()\n\n      if (!isActive) {\n        if ('ontouchstart' in document.documentElement) {\n          // if mobile we we use a backdrop because click events don't delegate\n          $('<div class=\"dropdown-backdrop\"/>').insertBefore($(this)).on('click', clearMenus)\n        }\n        $parent.toggleClass('open')\n      }\n\n      $this.focus()\n\n      return false\n    }\n\n  , keydown: function (e) {\n      var $this\n        , $items\n        , $active\n        , $parent\n        , isActive\n        , index\n\n      if (!/(38|40|27)/.test(e.keyCode)) return\n\n      $this = $(this)\n\n      e.preventDefault()\n      e.stopPropagation()\n\n      if ($this.is('.disabled, :disabled')) return\n\n      $parent = getParent($this)\n\n      isActive = $parent.hasClass('open')\n\n      if (!isActive || (isActive && e.keyCode == 27)) {\n        if (e.which == 27) $parent.find(toggle).focus()\n        return $this.click()\n      }\n\n      $items = $('[role=menu] li:not(.divider):visible a', $parent)\n\n      if (!$items.length) return\n\n      index = $items.index($items.filter(':focus'))\n\n      if (e.keyCode == 38 && index > 0) index--                                        // up\n      if (e.keyCode == 40 && index < $items.length - 1) index++                        // down\n      if (!~index) index = 0\n\n      $items\n        .eq(index)\n        .focus()\n    }\n\n  }\n\n  function clearMenus() {\n    $('.dropdown-backdrop').remove()\n    $(toggle).each(function () {\n      getParent($(this)).removeClass('open')\n    })\n  }\n\n  function getParent($this) {\n    var selector = $this.attr('data-target')\n      , $parent\n\n    if (!selector) {\n      selector = $this.attr('href')\n      selector = selector && /#/.test(selector) && selector.replace(/.*(?=#[^\\s]*$)/, '') //strip for ie7\n    }\n\n    $parent = selector && $(selector)\n\n    if (!$parent || !$parent.length) $parent = $this.parent()\n\n    return $parent\n  }\n\n\n  /* DROPDOWN PLUGIN DEFINITION\n   * ========================== */\n\n  var old = $.fn.dropdown\n\n  $.fn.dropdown = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('dropdown')\n      if (!data) $this.data('dropdown', (data = new Dropdown(this)))\n      if (typeof option == 'string') data[option].call($this)\n    })\n  }\n\n  $.fn.dropdown.Constructor = Dropdown\n\n\n /* DROPDOWN NO CONFLICT\n  * ==================== */\n\n  $.fn.dropdown.noConflict = function () {\n    $.fn.dropdown = old\n    return this\n  }\n\n\n  /* APPLY TO STANDARD DROPDOWN ELEMENTS\n   * =================================== */\n\n  $(document)\n    .on('click.dropdown.data-api', clearMenus)\n    .on('click.dropdown.data-api', '.dropdown form', function (e) { e.stopPropagation() })\n    .on('click.dropdown.data-api'  , toggle, Dropdown.prototype.toggle)\n    .on('keydown.dropdown.data-api', toggle + ', [role=menu]' , Dropdown.prototype.keydown)\n\n}(window.jQuery);\n/* =========================================================\n * bootstrap-modal.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#modals\n * =========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* MODAL CLASS DEFINITION\n  * ====================== */\n\n  var Modal = function (element, options) {\n    this.options = options\n    this.$element = $(element)\n      .delegate('[data-dismiss=\"modal\"]', 'click.dismiss.modal', $.proxy(this.hide, this))\n    this.options.remote && this.$element.find('.modal-body').load(this.options.remote)\n  }\n\n  Modal.prototype = {\n\n      constructor: Modal\n\n    , toggle: function () {\n        return this[!this.isShown ? 'show' : 'hide']()\n      }\n\n    , show: function () {\n        var that = this\n          , e = $.Event('show')\n\n        this.$element.trigger(e)\n\n        if (this.isShown || e.isDefaultPrevented()) return\n\n        this.isShown = true\n\n        this.escape()\n\n        this.backdrop(function () {\n          var transition = $.support.transition && that.$element.hasClass('fade')\n\n          if (!that.$element.parent().length) {\n            that.$element.appendTo(document.body) //don't move modals dom position\n          }\n\n          that.$element.show()\n\n          if (transition) {\n            that.$element[0].offsetWidth // force reflow\n          }\n\n          that.$element\n            .addClass('in')\n            .attr('aria-hidden', false)\n\n          that.enforceFocus()\n\n          transition ?\n            that.$element.one($.support.transition.end, function () { that.$element.focus().trigger('shown') }) :\n            that.$element.focus().trigger('shown')\n\n        })\n      }\n\n    , hide: function (e) {\n        e && e.preventDefault()\n\n        var that = this\n\n        e = $.Event('hide')\n\n        this.$element.trigger(e)\n\n        if (!this.isShown || e.isDefaultPrevented()) return\n\n        this.isShown = false\n\n        this.escape()\n\n        $(document).off('focusin.modal')\n\n        this.$element\n          .removeClass('in')\n          .attr('aria-hidden', true)\n\n        $.support.transition && this.$element.hasClass('fade') ?\n          this.hideWithTransition() :\n          this.hideModal()\n      }\n\n    , enforceFocus: function () {\n        var that = this\n        $(document).on('focusin.modal', function (e) {\n          if (that.$element[0] !== e.target && !that.$element.has(e.target).length) {\n            that.$element.focus()\n          }\n        })\n      }\n\n    , escape: function () {\n        var that = this\n        if (this.isShown && this.options.keyboard) {\n          this.$element.on('keyup.dismiss.modal', function ( e ) {\n            e.which == 27 && that.hide()\n          })\n        } else if (!this.isShown) {\n          this.$element.off('keyup.dismiss.modal')\n        }\n      }\n\n    , hideWithTransition: function () {\n        var that = this\n          , timeout = setTimeout(function () {\n              that.$element.off($.support.transition.end)\n              that.hideModal()\n            }, 500)\n\n        this.$element.one($.support.transition.end, function () {\n          clearTimeout(timeout)\n          that.hideModal()\n        })\n      }\n\n    , hideModal: function () {\n        var that = this\n        this.$element.hide()\n        this.backdrop(function () {\n          that.removeBackdrop()\n          that.$element.trigger('hidden')\n        })\n      }\n\n    , removeBackdrop: function () {\n        this.$backdrop && this.$backdrop.remove()\n        this.$backdrop = null\n      }\n\n    , backdrop: function (callback) {\n        var that = this\n          , animate = this.$element.hasClass('fade') ? 'fade' : ''\n\n        if (this.isShown && this.options.backdrop) {\n          var doAnimate = $.support.transition && animate\n\n          this.$backdrop = $('<div class=\"modal-backdrop ' + animate + '\" />')\n            .appendTo(document.body)\n\n          this.$backdrop.click(\n            this.options.backdrop == 'static' ?\n              $.proxy(this.$element[0].focus, this.$element[0])\n            : $.proxy(this.hide, this)\n          )\n\n          if (doAnimate) this.$backdrop[0].offsetWidth // force reflow\n\n          this.$backdrop.addClass('in')\n\n          if (!callback) return\n\n          doAnimate ?\n            this.$backdrop.one($.support.transition.end, callback) :\n            callback()\n\n        } else if (!this.isShown && this.$backdrop) {\n          this.$backdrop.removeClass('in')\n\n          $.support.transition && this.$element.hasClass('fade')?\n            this.$backdrop.one($.support.transition.end, callback) :\n            callback()\n\n        } else if (callback) {\n          callback()\n        }\n      }\n  }\n\n\n /* MODAL PLUGIN DEFINITION\n  * ======================= */\n\n  var old = $.fn.modal\n\n  $.fn.modal = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('modal')\n        , options = $.extend({}, $.fn.modal.defaults, $this.data(), typeof option == 'object' && option)\n      if (!data) $this.data('modal', (data = new Modal(this, options)))\n      if (typeof option == 'string') data[option]()\n      else if (options.show) data.show()\n    })\n  }\n\n  $.fn.modal.defaults = {\n      backdrop: true\n    , keyboard: true\n    , show: true\n  }\n\n  $.fn.modal.Constructor = Modal\n\n\n /* MODAL NO CONFLICT\n  * ================= */\n\n  $.fn.modal.noConflict = function () {\n    $.fn.modal = old\n    return this\n  }\n\n\n /* MODAL DATA-API\n  * ============== */\n\n  $(document).on('click.modal.data-api', '[data-toggle=\"modal\"]', function (e) {\n    var $this = $(this)\n      , href = $this.attr('href')\n      , $target = $($this.attr('data-target') || (href && href.replace(/.*(?=#[^\\s]+$)/, ''))) //strip for ie7\n      , option = $target.data('modal') ? 'toggle' : $.extend({ remote:!/#/.test(href) && href }, $target.data(), $this.data())\n\n    e.preventDefault()\n\n    $target\n      .modal(option)\n      .one('hide', function () {\n        $this.focus()\n      })\n  })\n\n}(window.jQuery);\n/* ===========================================================\n * bootstrap-tooltip.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#tooltips\n * Inspired by the original jQuery.tipsy by Jason Frame\n * ===========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* TOOLTIP PUBLIC CLASS DEFINITION\n  * =============================== */\n\n  var Tooltip = function (element, options) {\n    this.init('tooltip', element, options)\n  }\n\n  Tooltip.prototype = {\n\n    constructor: Tooltip\n\n  , init: function (type, element, options) {\n      var eventIn\n        , eventOut\n        , triggers\n        , trigger\n        , i\n\n      this.type = type\n      this.$element = $(element)\n      this.options = this.getOptions(options)\n      this.enabled = true\n\n      triggers = this.options.trigger.split(' ')\n\n      for (i = triggers.length; i--;) {\n        trigger = triggers[i]\n        if (trigger == 'click') {\n          this.$element.on('click.' + this.type, this.options.selector, $.proxy(this.toggle, this))\n        } else if (trigger != 'manual') {\n          eventIn = trigger == 'hover' ? 'mouseenter' : 'focus'\n          eventOut = trigger == 'hover' ? 'mouseleave' : 'blur'\n          this.$element.on(eventIn + '.' + this.type, this.options.selector, $.proxy(this.enter, this))\n          this.$element.on(eventOut + '.' + this.type, this.options.selector, $.proxy(this.leave, this))\n        }\n      }\n\n      this.options.selector ?\n        (this._options = $.extend({}, this.options, { trigger: 'manual', selector: '' })) :\n        this.fixTitle()\n    }\n\n  , getOptions: function (options) {\n      options = $.extend({}, $.fn[this.type].defaults, this.$element.data(), options)\n\n      if (options.delay && typeof options.delay == 'number') {\n        options.delay = {\n          show: options.delay\n        , hide: options.delay\n        }\n      }\n\n      return options\n    }\n\n  , enter: function (e) {\n      var defaults = $.fn[this.type].defaults\n        , options = {}\n        , self\n\n      this._options && $.each(this._options, function (key, value) {\n        if (defaults[key] != value) options[key] = value\n      }, this)\n\n      self = $(e.currentTarget)[this.type](options).data(this.type)\n\n      if (!self.options.delay || !self.options.delay.show) return self.show()\n\n      clearTimeout(this.timeout)\n      self.hoverState = 'in'\n      this.timeout = setTimeout(function() {\n        if (self.hoverState == 'in') self.show()\n      }, self.options.delay.show)\n    }\n\n  , leave: function (e) {\n      var self = $(e.currentTarget)[this.type](this._options).data(this.type)\n\n      if (this.timeout) clearTimeout(this.timeout)\n      if (!self.options.delay || !self.options.delay.hide) return self.hide()\n\n      self.hoverState = 'out'\n      this.timeout = setTimeout(function() {\n        if (self.hoverState == 'out') self.hide()\n      }, self.options.delay.hide)\n    }\n\n  , show: function () {\n      var $tip\n        , pos\n        , actualWidth\n        , actualHeight\n        , placement\n        , tp\n        , e = $.Event('show')\n\n      if (this.hasContent() && this.enabled) {\n        this.$element.trigger(e)\n        if (e.isDefaultPrevented()) return\n        $tip = this.tip()\n        this.setContent()\n\n        if (this.options.animation) {\n          $tip.addClass('fade')\n        }\n\n        placement = typeof this.options.placement == 'function' ?\n          this.options.placement.call(this, $tip[0], this.$element[0]) :\n          this.options.placement\n\n        $tip\n          .detach()\n          .css({ top: 0, left: 0, display: 'block' })\n\n        this.options.container ? $tip.appendTo(this.options.container) : $tip.insertAfter(this.$element)\n\n        pos = this.getPosition()\n\n        actualWidth = $tip[0].offsetWidth\n        actualHeight = $tip[0].offsetHeight\n\n        switch (placement) {\n          case 'bottom':\n            tp = {top: pos.top + pos.height, left: pos.left + pos.width / 2 - actualWidth / 2}\n            break\n          case 'top':\n            tp = {top: pos.top - actualHeight, left: pos.left + pos.width / 2 - actualWidth / 2}\n            break\n          case 'left':\n            tp = {top: pos.top + pos.height / 2 - actualHeight / 2, left: pos.left - actualWidth}\n            break\n          case 'right':\n            tp = {top: pos.top + pos.height / 2 - actualHeight / 2, left: pos.left + pos.width}\n            break\n        }\n\n        this.applyPlacement(tp, placement)\n        this.$element.trigger('shown')\n      }\n    }\n\n  , applyPlacement: function(offset, placement){\n      var $tip = this.tip()\n        , width = $tip[0].offsetWidth\n        , height = $tip[0].offsetHeight\n        , actualWidth\n        , actualHeight\n        , delta\n        , replace\n\n      $tip\n        .offset(offset)\n        .addClass(placement)\n        .addClass('in')\n\n      actualWidth = $tip[0].offsetWidth\n      actualHeight = $tip[0].offsetHeight\n\n      if (placement == 'top' && actualHeight != height) {\n        offset.top = offset.top + height - actualHeight\n        replace = true\n      }\n\n      if (placement == 'bottom' || placement == 'top') {\n        delta = 0\n\n        if (offset.left < 0){\n          delta = offset.left * -2\n          offset.left = 0\n          $tip.offset(offset)\n          actualWidth = $tip[0].offsetWidth\n          actualHeight = $tip[0].offsetHeight\n        }\n\n        this.replaceArrow(delta - width + actualWidth, actualWidth, 'left')\n      } else {\n        this.replaceArrow(actualHeight - height, actualHeight, 'top')\n      }\n\n      if (replace) $tip.offset(offset)\n    }\n\n  , replaceArrow: function(delta, dimension, position){\n      this\n        .arrow()\n        .css(position, delta ? (50 * (1 - delta / dimension) + \"%\") : '')\n    }\n\n  , setContent: function () {\n      var $tip = this.tip()\n        , title = this.getTitle()\n\n      $tip.find('.tooltip-inner')[this.options.html ? 'html' : 'text'](title)\n      $tip.removeClass('fade in top bottom left right')\n    }\n\n  , hide: function () {\n      var that = this\n        , $tip = this.tip()\n        , e = $.Event('hide')\n\n      this.$element.trigger(e)\n      if (e.isDefaultPrevented()) return\n\n      $tip.removeClass('in')\n\n      function removeWithAnimation() {\n        var timeout = setTimeout(function () {\n          $tip.off($.support.transition.end).detach()\n        }, 500)\n\n        $tip.one($.support.transition.end, function () {\n          clearTimeout(timeout)\n          $tip.detach()\n        })\n      }\n\n      $.support.transition && this.$tip.hasClass('fade') ?\n        removeWithAnimation() :\n        $tip.detach()\n\n      this.$element.trigger('hidden')\n\n      return this\n    }\n\n  , fixTitle: function () {\n      var $e = this.$element\n      if ($e.attr('title') || typeof($e.attr('data-original-title')) != 'string') {\n        $e.attr('data-original-title', $e.attr('title') || '').attr('title', '')\n      }\n    }\n\n  , hasContent: function () {\n      return this.getTitle()\n    }\n\n  , getPosition: function () {\n      var el = this.$element[0]\n      return $.extend({}, (typeof el.getBoundingClientRect == 'function') ? el.getBoundingClientRect() : {\n        width: el.offsetWidth\n      , height: el.offsetHeight\n      }, this.$element.offset())\n    }\n\n  , getTitle: function () {\n      var title\n        , $e = this.$element\n        , o = this.options\n\n      title = $e.attr('data-original-title')\n        || (typeof o.title == 'function' ? o.title.call($e[0]) :  o.title)\n\n      return title\n    }\n\n  , tip: function () {\n      return this.$tip = this.$tip || $(this.options.template)\n    }\n\n  , arrow: function(){\n      return this.$arrow = this.$arrow || this.tip().find(\".tooltip-arrow\")\n    }\n\n  , validate: function () {\n      if (!this.$element[0].parentNode) {\n        this.hide()\n        this.$element = null\n        this.options = null\n      }\n    }\n\n  , enable: function () {\n      this.enabled = true\n    }\n\n  , disable: function () {\n      this.enabled = false\n    }\n\n  , toggleEnabled: function () {\n      this.enabled = !this.enabled\n    }\n\n  , toggle: function (e) {\n      var self = e ? $(e.currentTarget)[this.type](this._options).data(this.type) : this\n      self.tip().hasClass('in') ? self.hide() : self.show()\n    }\n\n  , destroy: function () {\n      this.hide().$element.off('.' + this.type).removeData(this.type)\n    }\n\n  }\n\n\n /* TOOLTIP PLUGIN DEFINITION\n  * ========================= */\n\n  var old = $.fn.tooltip\n\n  $.fn.tooltip = function ( option ) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('tooltip')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('tooltip', (data = new Tooltip(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.tooltip.Constructor = Tooltip\n\n  $.fn.tooltip.defaults = {\n    animation: true\n  , placement: 'top'\n  , selector: false\n  , template: '<div class=\"tooltip\"><div class=\"tooltip-arrow\"></div><div class=\"tooltip-inner\"></div></div>'\n  , trigger: 'hover focus'\n  , title: ''\n  , delay: 0\n  , html: false\n  , container: false\n  }\n\n\n /* TOOLTIP NO CONFLICT\n  * =================== */\n\n  $.fn.tooltip.noConflict = function () {\n    $.fn.tooltip = old\n    return this\n  }\n\n}(window.jQuery);\n/* ===========================================================\n * bootstrap-popover.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#popovers\n * ===========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* POPOVER PUBLIC CLASS DEFINITION\n  * =============================== */\n\n  var Popover = function (element, options) {\n    this.init('popover', element, options)\n  }\n\n\n  /* NOTE: POPOVER EXTENDS BOOTSTRAP-TOOLTIP.js\n     ========================================== */\n\n  Popover.prototype = $.extend({}, $.fn.tooltip.Constructor.prototype, {\n\n    constructor: Popover\n\n  , setContent: function () {\n      var $tip = this.tip()\n        , title = this.getTitle()\n        , content = this.getContent()\n\n      $tip.find('.popover-title')[this.options.html ? 'html' : 'text'](title)\n      $tip.find('.popover-content')[this.options.html ? 'html' : 'text'](content)\n\n      $tip.removeClass('fade top bottom left right in')\n    }\n\n  , hasContent: function () {\n      return this.getTitle() || this.getContent()\n    }\n\n  , getContent: function () {\n      var content\n        , $e = this.$element\n        , o = this.options\n\n      content = (typeof o.content == 'function' ? o.content.call($e[0]) :  o.content)\n        || $e.attr('data-content')\n\n      return content\n    }\n\n  , tip: function () {\n      if (!this.$tip) {\n        this.$tip = $(this.options.template)\n      }\n      return this.$tip\n    }\n\n  , destroy: function () {\n      this.hide().$element.off('.' + this.type).removeData(this.type)\n    }\n\n  })\n\n\n /* POPOVER PLUGIN DEFINITION\n  * ======================= */\n\n  var old = $.fn.popover\n\n  $.fn.popover = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('popover')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('popover', (data = new Popover(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.popover.Constructor = Popover\n\n  $.fn.popover.defaults = $.extend({} , $.fn.tooltip.defaults, {\n    placement: 'right'\n  , trigger: 'click'\n  , content: ''\n  , template: '<div class=\"popover\"><div class=\"arrow\"></div><h3 class=\"popover-title\"></h3><div class=\"popover-content\"></div></div>'\n  })\n\n\n /* POPOVER NO CONFLICT\n  * =================== */\n\n  $.fn.popover.noConflict = function () {\n    $.fn.popover = old\n    return this\n  }\n\n}(window.jQuery);\n/* =============================================================\n * bootstrap-scrollspy.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#scrollspy\n * =============================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* SCROLLSPY CLASS DEFINITION\n  * ========================== */\n\n  function ScrollSpy(element, options) {\n    var process = $.proxy(this.process, this)\n      , $element = $(element).is('body') ? $(window) : $(element)\n      , href\n    this.options = $.extend({}, $.fn.scrollspy.defaults, options)\n    this.$scrollElement = $element.on('scroll.scroll-spy.data-api', process)\n    this.selector = (this.options.target\n      || ((href = $(element).attr('href')) && href.replace(/.*(?=#[^\\s]+$)/, '')) //strip for ie7\n      || '') + ' .nav li > a'\n    this.$body = $('body')\n    this.refresh()\n    this.process()\n  }\n\n  ScrollSpy.prototype = {\n\n      constructor: ScrollSpy\n\n    , refresh: function () {\n        var self = this\n          , $targets\n\n        this.offsets = $([])\n        this.targets = $([])\n\n        $targets = this.$body\n          .find(this.selector)\n          .map(function () {\n            var $el = $(this)\n              , href = $el.data('target') || $el.attr('href')\n              , $href = /^#\\w/.test(href) && $(href)\n            return ( $href\n              && $href.length\n              && [[ $href.position().top + (!$.isWindow(self.$scrollElement.get(0)) && self.$scrollElement.scrollTop()), href ]] ) || null\n          })\n          .sort(function (a, b) { return a[0] - b[0] })\n          .each(function () {\n            self.offsets.push(this[0])\n            self.targets.push(this[1])\n          })\n      }\n\n    , process: function () {\n        var scrollTop = this.$scrollElement.scrollTop() + this.options.offset\n          , scrollHeight = this.$scrollElement[0].scrollHeight || this.$body[0].scrollHeight\n          , maxScroll = scrollHeight - this.$scrollElement.height()\n          , offsets = this.offsets\n          , targets = this.targets\n          , activeTarget = this.activeTarget\n          , i\n\n        if (scrollTop >= maxScroll) {\n          return activeTarget != (i = targets.last()[0])\n            && this.activate ( i )\n        }\n\n        for (i = offsets.length; i--;) {\n          activeTarget != targets[i]\n            && scrollTop >= offsets[i]\n            && (!offsets[i + 1] || scrollTop <= offsets[i + 1])\n            && this.activate( targets[i] )\n        }\n      }\n\n    , activate: function (target) {\n        var active\n          , selector\n\n        this.activeTarget = target\n\n        $(this.selector)\n          .parent('.active')\n          .removeClass('active')\n\n        selector = this.selector\n          + '[data-target=\"' + target + '\"],'\n          + this.selector + '[href=\"' + target + '\"]'\n\n        active = $(selector)\n          .parent('li')\n          .addClass('active')\n\n        if (active.parent('.dropdown-menu').length)  {\n          active = active.closest('li.dropdown').addClass('active')\n        }\n\n        active.trigger('activate')\n      }\n\n  }\n\n\n /* SCROLLSPY PLUGIN DEFINITION\n  * =========================== */\n\n  var old = $.fn.scrollspy\n\n  $.fn.scrollspy = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('scrollspy')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('scrollspy', (data = new ScrollSpy(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.scrollspy.Constructor = ScrollSpy\n\n  $.fn.scrollspy.defaults = {\n    offset: 10\n  }\n\n\n /* SCROLLSPY NO CONFLICT\n  * ===================== */\n\n  $.fn.scrollspy.noConflict = function () {\n    $.fn.scrollspy = old\n    return this\n  }\n\n\n /* SCROLLSPY DATA-API\n  * ================== */\n\n  $(window).on('load', function () {\n    $('[data-spy=\"scroll\"]').each(function () {\n      var $spy = $(this)\n      $spy.scrollspy($spy.data())\n    })\n  })\n\n}(window.jQuery);/* ========================================================\n * bootstrap-tab.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#tabs\n * ========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* TAB CLASS DEFINITION\n  * ==================== */\n\n  var Tab = function (element) {\n    this.element = $(element)\n  }\n\n  Tab.prototype = {\n\n    constructor: Tab\n\n  , show: function () {\n      var $this = this.element\n        , $ul = $this.closest('ul:not(.dropdown-menu)')\n        , selector = $this.attr('data-target')\n        , previous\n        , $target\n        , e\n\n      if (!selector) {\n        selector = $this.attr('href')\n        selector = selector && selector.replace(/.*(?=#[^\\s]*$)/, '') //strip for ie7\n      }\n\n      if ( $this.parent('li').hasClass('active') ) return\n\n      previous = $ul.find('.active:last a')[0]\n\n      e = $.Event('show', {\n        relatedTarget: previous\n      })\n\n      $this.trigger(e)\n\n      if (e.isDefaultPrevented()) return\n\n      $target = $(selector)\n\n      this.activate($this.parent('li'), $ul)\n      this.activate($target, $target.parent(), function () {\n        $this.trigger({\n          type: 'shown'\n        , relatedTarget: previous\n        })\n      })\n    }\n\n  , activate: function ( element, container, callback) {\n      var $active = container.find('> .active')\n        , transition = callback\n            && $.support.transition\n            && $active.hasClass('fade')\n\n      function next() {\n        $active\n          .removeClass('active')\n          .find('> .dropdown-menu > .active')\n          .removeClass('active')\n\n        element.addClass('active')\n\n        if (transition) {\n          element[0].offsetWidth // reflow for transition\n          element.addClass('in')\n        } else {\n          element.removeClass('fade')\n        }\n\n        if ( element.parent('.dropdown-menu') ) {\n          element.closest('li.dropdown').addClass('active')\n        }\n\n        callback && callback()\n      }\n\n      transition ?\n        $active.one($.support.transition.end, next) :\n        next()\n\n      $active.removeClass('in')\n    }\n  }\n\n\n /* TAB PLUGIN DEFINITION\n  * ===================== */\n\n  var old = $.fn.tab\n\n  $.fn.tab = function ( option ) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('tab')\n      if (!data) $this.data('tab', (data = new Tab(this)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.tab.Constructor = Tab\n\n\n /* TAB NO CONFLICT\n  * =============== */\n\n  $.fn.tab.noConflict = function () {\n    $.fn.tab = old\n    return this\n  }\n\n\n /* TAB DATA-API\n  * ============ */\n\n  $(document).on('click.tab.data-api', '[data-toggle=\"tab\"], [data-toggle=\"pill\"]', function (e) {\n    e.preventDefault()\n    $(this).tab('show')\n  })\n\n}(window.jQuery);/* =============================================================\n * bootstrap-typeahead.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#typeahead\n * =============================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* TYPEAHEAD PUBLIC CLASS DEFINITION\n  * ================================= */\n\n  var Typeahead = function (element, options) {\n    this.$element = $(element)\n    this.options = $.extend({}, $.fn.typeahead.defaults, options)\n    this.matcher = this.options.matcher || this.matcher\n    this.sorter = this.options.sorter || this.sorter\n    this.highlighter = this.options.highlighter || this.highlighter\n    this.updater = this.options.updater || this.updater\n    this.source = this.options.source\n    this.$menu = $(this.options.menu)\n    this.shown = false\n    this.listen()\n  }\n\n  Typeahead.prototype = {\n\n    constructor: Typeahead\n\n  , select: function () {\n      var val = this.$menu.find('.active').attr('data-value')\n      this.$element\n        .val(this.updater(val))\n        .change()\n      return this.hide()\n    }\n\n  , updater: function (item) {\n      return item\n    }\n\n  , show: function () {\n      var pos = $.extend({}, this.$element.position(), {\n        height: this.$element[0].offsetHeight\n      })\n\n      this.$menu\n        .insertAfter(this.$element)\n        .css({\n          top: pos.top + pos.height\n        , left: pos.left\n        })\n        .show()\n\n      this.shown = true\n      return this\n    }\n\n  , hide: function () {\n      this.$menu.hide()\n      this.shown = false\n      return this\n    }\n\n  , lookup: function (event) {\n      var items\n\n      this.query = this.$element.val()\n\n      if (!this.query || this.query.length < this.options.minLength) {\n        return this.shown ? this.hide() : this\n      }\n\n      items = $.isFunction(this.source) ? this.source(this.query, $.proxy(this.process, this)) : this.source\n\n      return items ? this.process(items) : this\n    }\n\n  , process: function (items) {\n      var that = this\n\n      items = $.grep(items, function (item) {\n        return that.matcher(item)\n      })\n\n      items = this.sorter(items)\n\n      if (!items.length) {\n        return this.shown ? this.hide() : this\n      }\n\n      return this.render(items.slice(0, this.options.items)).show()\n    }\n\n  , matcher: function (item) {\n      return ~item.toLowerCase().indexOf(this.query.toLowerCase())\n    }\n\n  , sorter: function (items) {\n      var beginswith = []\n        , caseSensitive = []\n        , caseInsensitive = []\n        , item\n\n      while (item = items.shift()) {\n        if (!item.toLowerCase().indexOf(this.query.toLowerCase())) beginswith.push(item)\n        else if (~item.indexOf(this.query)) caseSensitive.push(item)\n        else caseInsensitive.push(item)\n      }\n\n      return beginswith.concat(caseSensitive, caseInsensitive)\n    }\n\n  , highlighter: function (item) {\n      var query = this.query.replace(/[\\-\\[\\]{}()*+?.,\\\\\\^$|#\\s]/g, '\\\\$&')\n      return item.replace(new RegExp('(' + query + ')', 'ig'), function ($1, match) {\n        return '<strong>' + match + '</strong>'\n      })\n    }\n\n  , render: function (items) {\n      var that = this\n\n      items = $(items).map(function (i, item) {\n        i = $(that.options.item).attr('data-value', item)\n        i.find('a').html(that.highlighter(item))\n        return i[0]\n      })\n\n      items.first().addClass('active')\n      this.$menu.html(items)\n      return this\n    }\n\n  , next: function (event) {\n      var active = this.$menu.find('.active').removeClass('active')\n        , next = active.next()\n\n      if (!next.length) {\n        next = $(this.$menu.find('li')[0])\n      }\n\n      next.addClass('active')\n    }\n\n  , prev: function (event) {\n      var active = this.$menu.find('.active').removeClass('active')\n        , prev = active.prev()\n\n      if (!prev.length) {\n        prev = this.$menu.find('li').last()\n      }\n\n      prev.addClass('active')\n    }\n\n  , listen: function () {\n      this.$element\n        .on('focus',    $.proxy(this.focus, this))\n        .on('blur',     $.proxy(this.blur, this))\n        .on('keypress', $.proxy(this.keypress, this))\n        .on('keyup',    $.proxy(this.keyup, this))\n\n      if (this.eventSupported('keydown')) {\n        this.$element.on('keydown', $.proxy(this.keydown, this))\n      }\n\n      this.$menu\n        .on('click', $.proxy(this.click, this))\n        .on('mouseenter', 'li', $.proxy(this.mouseenter, this))\n        .on('mouseleave', 'li', $.proxy(this.mouseleave, this))\n    }\n\n  , eventSupported: function(eventName) {\n      var isSupported = eventName in this.$element\n      if (!isSupported) {\n        this.$element.setAttribute(eventName, 'return;')\n        isSupported = typeof this.$element[eventName] === 'function'\n      }\n      return isSupported\n    }\n\n  , move: function (e) {\n      if (!this.shown) return\n\n      switch(e.keyCode) {\n        case 9: // tab\n        case 13: // enter\n        case 27: // escape\n          e.preventDefault()\n          break\n\n        case 38: // up arrow\n          e.preventDefault()\n          this.prev()\n          break\n\n        case 40: // down arrow\n          e.preventDefault()\n          this.next()\n          break\n      }\n\n      e.stopPropagation()\n    }\n\n  , keydown: function (e) {\n      this.suppressKeyPressRepeat = ~$.inArray(e.keyCode, [40,38,9,13,27])\n      this.move(e)\n    }\n\n  , keypress: function (e) {\n      if (this.suppressKeyPressRepeat) return\n      this.move(e)\n    }\n\n  , keyup: function (e) {\n      switch(e.keyCode) {\n        case 40: // down arrow\n        case 38: // up arrow\n        case 16: // shift\n        case 17: // ctrl\n        case 18: // alt\n          break\n\n        case 9: // tab\n        case 13: // enter\n          if (!this.shown) return\n          this.select()\n          break\n\n        case 27: // escape\n          if (!this.shown) return\n          this.hide()\n          break\n\n        default:\n          this.lookup()\n      }\n\n      e.stopPropagation()\n      e.preventDefault()\n  }\n\n  , focus: function (e) {\n      this.focused = true\n    }\n\n  , blur: function (e) {\n      this.focused = false\n      if (!this.mousedover && this.shown) this.hide()\n    }\n\n  , click: function (e) {\n      e.stopPropagation()\n      e.preventDefault()\n      this.select()\n      this.$element.focus()\n    }\n\n  , mouseenter: function (e) {\n      this.mousedover = true\n      this.$menu.find('.active').removeClass('active')\n      $(e.currentTarget).addClass('active')\n    }\n\n  , mouseleave: function (e) {\n      this.mousedover = false\n      if (!this.focused && this.shown) this.hide()\n    }\n\n  }\n\n\n  /* TYPEAHEAD PLUGIN DEFINITION\n   * =========================== */\n\n  var old = $.fn.typeahead\n\n  $.fn.typeahead = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('typeahead')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('typeahead', (data = new Typeahead(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.typeahead.defaults = {\n    source: []\n  , items: 8\n  , menu: '<ul class=\"typeahead dropdown-menu\"></ul>'\n  , item: '<li><a href=\"#\"></a></li>'\n  , minLength: 1\n  }\n\n  $.fn.typeahead.Constructor = Typeahead\n\n\n /* TYPEAHEAD NO CONFLICT\n  * =================== */\n\n  $.fn.typeahead.noConflict = function () {\n    $.fn.typeahead = old\n    return this\n  }\n\n\n /* TYPEAHEAD DATA-API\n  * ================== */\n\n  $(document).on('focus.typeahead.data-api', '[data-provide=\"typeahead\"]', function (e) {\n    var $this = $(this)\n    if ($this.data('typeahead')) return\n    $this.typeahead($this.data())\n  })\n\n}(window.jQuery);\n/* ==========================================================\n * bootstrap-affix.js v2.3.2\n * http://twitter.github.com/bootstrap/javascript.html#affix\n * ==========================================================\n * Copyright 2012 Twitter, Inc.\n *\n * Licensed under the Apache License, Version 2.0 (the \"License\");

\n\n  \"use strict\"; // jshint ;_;\n\n\n /* AFFIX CLASS DEFINITION\n  * ====================== */\n\n  var Affix = function (element, options) {\n    this.options = $.extend({}, $.fn.affix.defaults, options)\n    this.$window = $(window)\n      .on('scroll.affix.data-api', $.proxy(this.checkPosition, this))\n      .on('click.affix.data-api',  $.proxy(function () { setTimeout($.proxy(this.checkPosition, this), 1) }, this))\n    this.$element = $(element)\n    this.checkPosition()\n  }\n\n  Affix.prototype.checkPosition = function () {\n    if (!this.$element.is(':visible')) return\n\n    var scrollHeight = $(document).height()\n      , scrollTop = this.$window.scrollTop()\n      , position = this.$element.offset()\n      , offset = this.options.offset\n      , offsetBottom = offset.bottom\n      , offsetTop = offset.top\n      , reset = 'affix affix-top affix-bottom'\n      , affix\n\n    if (typeof offset != 'object') offsetBottom = offsetTop = offset\n    if (typeof offsetTop == 'function') offsetTop = offset.top()\n    if (typeof offsetBottom == 'function') offsetBottom = offset.bottom()\n\n    affix = this.unpin != null && (scrollTop + this.unpin <= position.top) ?\n      false    : offsetBottom != null && (position.top + this.$element.height() >= scrollHeight - offsetBottom) ?\n      'bottom' : offsetTop != null && scrollTop <= offsetTop ?\n      'top'    : false\n\n    if (this.affixed === affix) return\n\n    this.affixed = affix\n    this.unpin = affix == 'bottom' ? position.top - scrollTop : null\n\n    this.$element.removeClass(reset).addClass('affix' + (affix ? '-' + affix : ''))\n  }\n\n\n /* AFFIX PLUGIN DEFINITION\n  * ======================= */\n\n  var old = $.fn.affix\n\n  $.fn.affix = function (option) {\n    return this.each(function () {\n      var $this = $(this)\n        , data = $this.data('affix')\n        , options = typeof option == 'object' && option\n      if (!data) $this.data('affix', (data = new Affix(this, options)))\n      if (typeof option == 'string') data[option]()\n    })\n  }\n\n  $.fn.affix.Constructor = Affix\n\n  $.fn.affix.defaults = {\n    offset: 0\n  }\n\n\n /* AFFIX NO CONFLICT\n  * ================= */\n\n  $.fn.affix.noConflict = function () {\n    $.fn.affix = old\n    return this\n  }\n\n\n /* AFFIX DATA-API\n  * ============== */\n\n  $(window).on('load', function () {\n    $('[data-spy=\"affix\"]').each(function () {\n      var $spy = $(this)\n        , data = $spy.data()\n\n      data.offset = data.offset || {}\n\n      data.offsetBottom && (data.offset.bottom = data.offsetBottom)\n      data.offsetTop && (data.offset.top = data.offsetTop)\n\n      $spy.affix(data)\n    })\n  })\n\n\n}(window.jQuery);");

/*ui/dropdown_selector/dropdown_selector*/
define("ui/dropdown_selector/dropdown_selector",["can/can","./init.ejs!","bootstrap/bootstrap"],function(t,e){var o=function(){var e=t.route.attr("page"),o=t.route.attr("view");return"homepage"===e||"admin"===e?o:"latest"},n=function(e){var n=o(),a="greatest"===n,r={project:e.name,category:t.route.attr("category"),timespan:a?t.route.attr("timespan")||"week":void 0,view:n};return"bug"===t.route.attr("category")&&(r.state=t.route.attr("state")),t.route.url(r,!1)};return t.Control.extend({defaults:{defaultOption:{name:"all",display_name:"All Projects"}}},{init:function(){var o=this;o.element.html(e({htmlId:o.options.htmlId,items:o.options.items,defaultOption:o.options.defaultOption},{selected:function(){var e=o.options.defaultOption.display_name;return t.each(o.options.items,function(t){o.options.state()===t.attr("name")&&(e=t.attr("display_name"))}),e},itemUrl:function(t){return n(t)}}))}})});
/*ui/lru_selector/init.ejs!can/view/ejs/system*/
define("ui/lru_selector/init.ejs!can/view/ejs/system",["can/view/ejs/ejs"],function(can){return can.view.preloadStringRenderer("ui/lru_selector/init.ejs",can.EJS(function(_CONTEXT,_VIEW){with(_VIEW)with(_CONTEXT){var ___v1ew=[];return ___v1ew.push('<li><a class="disabled">Filter:</a></li>\n<li><a href="'),___v1ew.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(defaultOption)})),___v1ew.push('" class="item default '),___v1ew.push(can.view.txt(!0,"a","class",this,function(){return isSelected(defaultOption)})),___v1ew.push('"',can.view.pending({scope:this}),">"),___v1ew.push(can.view.txt(1,"a",0,this,function(){return defaultOption.attr("display_name")})),___v1ew.push("</a></li>	\n"),___v1ew.push(can.view.txt(0,"span",0,this,function(){var n=[];return can.each(items,function(t,e){n.push("\n"),n.push(can.view.txt(0,"ul",0,this,function(){var n=[];return breakIdx>e&&(n.push('\n<li><a href="'),n.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(t)})),n.push('" '),n.push(can.view.txt(1,"a",1,this,function(){return can.proxy(function(n){var e=can.$(n);can.data(e,"item",t)},this)})),n.push(' class="item '),n.push(can.view.txt(!0,"a","class",this,function(){return isSelected(t)})),n.push('"',can.view.pending({scope:this}),">"),n.push(can.view.txt(1,"a",0,this,function(){return t.display_name})),n.push("</a></li>	\n")),n.join("")})),n.push("\n")}),n.join("")})),___v1ew.push('\n<li class="dropdown">\n  <a href="javascript://" class="dropdown-toggle" data-toggle="dropdown"> More <b class="caret"></b> </a>\n  <ul class="dropdown-menu">	\n	'),___v1ew.push(can.view.txt(0,"ul",0,this,function(){var n=[];return can.each(items,function(t,e){n.push("\n	"),n.push(can.view.txt(0,"ul",0,this,function(){var n=[];return e>=breakIdx&&(n.push('\n	<li><a href="'),n.push(can.view.txt(!0,"a","href",this,function(){return itemUrl(t)})),n.push('" class="item"',can.view.pending({scope:this}),">"),n.push(can.view.txt(1,"a",0,this,function(){return t.display_name})),n.push("</a></li>	\n	")),n.join("")})),n.push("\n	")}),n.join("")})),___v1ew.push("\n  </ul>\n</li>\n"),___v1ew.join("")}}))});
/*jquerypp/lang/json/json*/
define("jquerypp/lang/json/json",["jquery/jquery"],function($){$.toJSON=function(r,e,t,n){if("object"==typeof JSON&&JSON.stringify)return JSON.stringify(r,e,t);!n&&$.isFunction(e)&&(r=e("",r)),"number"==typeof t&&(t="          ".substring(0,t)),t="string"==typeof t?t.substring(0,10):"";var i=typeof r;if(null===r)return"null";if("undefined"==i||"function"==i)return void 0;if("number"==i||"boolean"==i)return r+"";if("string"==i)return $.quoteString(r);if("object"==i){if("function"==typeof r.toJSON)return $.toJSON(r.toJSON(),e,t,!0);if(r.constructor===Date){var o=r.getUTCMonth()+1;10>o&&(o="0"+o);var u=r.getUTCDate();10>u&&(u="0"+u);var f=r.getUTCFullYear(),a=r.getUTCHours();10>a&&(a="0"+a);var s=r.getUTCMinutes();10>s&&(s="0"+s);var c=r.getUTCSeconds();10>c&&(c="0"+c);var l=r.getUTCMilliseconds();return 100>l&&(l="0"+l),10>l&&(l="0"+l),'"'+f+"-"+o+"-"+u+"T"+a+":"+s+":"+c+"."+l+'Z"'}var p=$.isFunction(e)?function(r,t){return e(r,t)}:function(r,e){return e},g=t?"\n":"",S=t?" ":"";if(r.constructor===Array){for(var d=[],v=0;v<r.length;v++)d.push(($.toJSON(p(v,r[v]),e,t,!0)||"null").replace(/^/gm,t));return"["+g+d.join(","+g)+g+"]"}var y,J=[];$.isArray(e)&&(y=$.map(e,function(r){return"string"==typeof r||"number"==typeof r?r+"":null}));for(var N in r){var O,b,i=typeof N;if(!y||-1!=$.inArray(N+"",y)){if("number"==i)O='"'+N+'"';else{if("string"!=i)continue;O=$.quoteString(N)}b=$.toJSON(p(N,r[N]),e,t,!0),"undefined"!=typeof b&&J.push((O+":"+S+b).replace(/^/gm,t))}}return"{"+g+J.join(","+g)+g+"}"}},$.evalJSON=function(src){return"object"==typeof JSON&&JSON.parse?JSON.parse(src):eval("("+src+")")},$.secureEvalJSON=function(src){if("object"==typeof JSON&&JSON.parse)return JSON.parse(src);var filtered=src;if(filtered=filtered.replace(/\\["\\\/bfnrtu]/g,"@"),filtered=filtered.replace(/"[^"\\\n\r]*"|true|false|null|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?/g,"]"),filtered=filtered.replace(/(?:^|:|,)(?:\s*\[)+/g,""),/^[\],:{}\s]*$/.test(filtered))return eval("("+src+")");throw new SyntaxError("Error parsing JSON, source is not valid.")},$.quoteString=function(r){return r.match(_escapeable)?'"'+r.replace(_escapeable,function(r){var e=_meta[r];return"string"==typeof e?e:(e=r.charCodeAt(),"\\u00"+Math.floor(e/16).toString(16)+(e%16).toString(16))})+'"':'"'+r+'"'};var _escapeable=/["\\\x00-\x1f\x7f-\x9f]/g,_meta={"\b":"\\b","	":"\\t","\n":"\\n","\f":"\\f","\r":"\\r",'"':'\\"',"\\":"\\\\"};return $});
/*jquerypp/dom/cookie/cookie*/
define("jquerypp/dom/cookie/cookie",["jquery/jquery","jquerypp/lang/json/json"],function(e){return e.cookie=function(o,n,t){if("undefined"==typeof n){var i=null;if(document.cookie&&""!=document.cookie)for(var r=document.cookie.split(";"),p=0;p<r.length;p++){var c=e.trim(r[p]);if(c.substring(0,o.length+1)==o+"="){i=decodeURIComponent(c.substring(o.length+1));break}}if(e.evalJSON&&i&&i.match(/^\s*\{/))try{i=e.evalJSON(i)}catch(u){}return i}t=t||{},null===n&&(n="",t.expires=-1),"object"==typeof n&&e.toJSON&&(n=e.toJSON(n));var a="";if(t.expires&&("number"==typeof t.expires||t.expires.toUTCString)){

/*ui/lru_selector/lru_selector*/
define("ui/lru_selector/lru_selector",["can/can","./init.ejs!","jquerypp/dom/cookie/cookie","can/map/delegate/delegate"],function(e,t){var o=new e.Observe.List,n=function(){var t=e.route.attr("page"),o=e.route.attr("view");return"homepage"===t||"admin"===t?o:"latest"},i=function(t){var o=n(),i="greatest"===o;return e.route.url({category:t.name,project:e.route.attr("project")||"all",timespan:i?e.route.attr("timespan")||"week":void 0,view:o},!1)};return e.Control.extend({defaults:{orderingCookieName:"smartFilterItemsOrdering",breakIdx:3,defaultOption:{name:"all",display_name:"All"}}},{init:function(){var n=this;n.options.defaultOption=new e.Observe(n.options.defaultOption),n.element.html(t({htmlId:n.options.htmlId,defaultOption:n.options.defaultOption,items:o,breakIdx:n.options.breakIdx},{isSelected:function(e){var t="function"==typeof e?e():e.name;return n.options.state()===t?"active":""},itemUrl:function(e){return i(e)}}))},"{can.route} change":function(t,n,i,r,a){if("category"===i&&("set"===r||"add"===r)&&o.length>0){var s;e.each(o,function(e){e.name==a&&(s=e)}),o.indexOf(s)>=this.options.breakIdx&&(o.splice(o.indexOf(s),1),o.unshift(s),this.sortItems(),this.saveOrderingToCookie())}},"{categories} change":function(){this.options.categories.forEach(function(e){o.push({name:e.name,display_name:e.display_name})}),this.loadOrderingFromCookie(),this.sortItems()},sortItems:function(){var e=this.options.breakIdx,t=o.length;o.replace(o.concat(o.splice(e,t-e).sort(function(e,t){return e.name>t.name})))},saveOrderingToCookie:function(e){var t=(e||this.options.orderingCookieName,[]);o.forEach(function(e){t.push(e.attr("name"))}),$.cookie(this.options.orderingCookieName,t.join(","))},loadOrderingFromCookie:function(t){t=t||this.options.orderingCookieName,$.cookie(t)&&e.each($.cookie(t).split(","),function(e){o.forEach(function(t){return t.attr("name")===e?(o.splice(o.indexOf(t),1),void o.push(t)):void 0})})}})});
/*bithub/navigator/filterbar/filterbar*/
define("bithub/navigator/filterbar/filterbar",["can/can","./filterbar.mustache!","ui/radio_selector/radio_selector","ui/dropdown_selector/dropdown_selector","ui/lru_selector/lru_selector"],function(e,t,r,i,n){return e.Control.extend({pluginName:"filterbar",defaults:{}},{init:function(a,o){a.html(t({})),new r(a.find("#viewFilter"),{items:[{name:"latest",display_name:"Latest"},{name:"greatest",display_name:"Greatest"}],state:function(){return e.route.attr("view")}}),new i(a.find("#projectFilter"),{items:o.projects,state:function(){return e.route.attr("project")}}),new n(a.find("#categoryFilter"),{categories:o.categories,state:function(){return e.route.attr("category")}})}})});
/*bithub/navigator/admin_navbar/admin_navbar*/
define("bithub/navigator/admin_navbar/admin_navbar",["can/can","ui/radio_selector/radio_selector"],function(a,n){return a.Control.extend("Bithub.Controls.Navigator.AdminNavbar",{pluginName:"admin-navbar",requiredPermissions:["admin"],defaults:{state:function(){return a.route.attr("view")},items:[{name:"users",url:function(){return a.route.url({page:"admin",view:"users",action:"list"},!1)},display_name:"Users"},{name:"tags",url:function(){return a.route.url({page:"admin",view:"tags",action:"list"},!1)},display_name:"Tags"},{name:"rewards",url:function(){return a.route.url({page:"admin",view:"rewards",action:"list"},!1)},display_name:"Rewards"},{name:"achievements",url:function(){return a.route.url({page:"admin",view:"achievements",action:"list"},!1)},display_name:"Achievements"}]}},{init:function(a){var e=$("<ul/>").addClass("nav");new n(e,this.options),a.html(e)}})});
/*bithub/navigator/profile_navbar/init.mustache!can/view/mustache/system*/
define("bithub/navigator/profile_navbar/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(s){return s.view.preloadStringRenderer("bithub/navigator/profile_navbar/init.mustache",s.Mustache(function(t,e){var i=[];return i.push(s.view.txt(0,"",0,this,s.Mustache.txt({scope:t,options:e},"#",{get:"isShown"},[{fn:function(t,e){var i=[];return i.push('	<div class="subnav-wrapper">\n	<div class="container">\n		  <ul class="subnav">\n			\n			<li class="'),i.push(s.view.txt(!0,"li","class",this,s.Mustache.txt({scope:t,options:e},null,{get:"isActive"},{get:"routes.earnpoints"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('<a href="'),i.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"routes.earnpoints"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('Earn Points</a></li>\n			<li class="'),i.push(s.view.txt(!0,"li","class",this,s.Mustache.txt({scope:t,options:e},null,{get:"isActive"},{get:"routes.rewards"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('<a href="'),i.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"routes.rewards"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('Rewards</a></li>\n			<li class="'),i.push(s.view.txt(!0,"li","class",this,s.Mustache.txt({scope:t,options:e},null,{get:"isActive"},{get:"routes.activities"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('<a href="'),i.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"routes.activities"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('Activity</a></li>\n			<li class="'),i.push(s.view.txt(!0,"li","class",this,s.Mustache.txt({scope:t,options:e},null,{get:"isActive"},{get:"routes.info"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('<a href="'),i.push(s.view.txt(!0,"a","href",this,s.Mustache.txt({scope:t,options:e},null,{get:"routes.info"}))),i.push('"',s.view.pending({scope:t,options:e}),">"),i.push('Profile</a></li>\n			<li class="account">Your Account: <span class="userPoints">'),i.push(s.view.txt(1,"span",0,this,s.Mustache.txt({scope:t,options:e},null,{get:"user.score"}))),i.push('</span></li>\n		  </ul>\n	</div>\n	</div>\n	<div class="vert-padding"></div>'),i.push("\n"),i.join("")}}]))),i.join("")}))});
/*bithub/navigator/profile_navbar/profile_navbar*/
define("bithub/navigator/profile_navbar/profile_navbar",["can/can","./init.mustache!"],function(t,r){return t.Control({defaults:{routes:{info:function(){return t.route.url({page:"profile",view:"info"},!1)},activities:function(){return t.route.url({page:"profile",view:"activities"},!1)},rewards:function(){return t.route.url({page:"rewards"},!1)},earnpoints:function(){return t.route.url({page:"earnpoints"},!1)}}}},{init:function(e){var i=this;e.html(r({routes:this.options.routes,user:this.options.currentUser},{helpers:{isShown:function(t){return i.options.currentUser.isLoggedIn()&&i.isRoute()?t.fn(this):t.inverse(this)},isActive:function(r){return t.route.attr("view"),r().replace(/^\//,"")==t.route.attr().route?"active":""}}}))},isRoute:function(){var r=t.route.attr().route,e=this.options.routes;for(var i in e)if(e[i]().replace(/^\//,"")==r)return r}})});
/*bithub/navigator/navigator*/
define("bithub/navigator/navigator",["can/can","bithub/navigator/filterbar/filterbar","bithub/navigator/admin_navbar/admin_navbar","bithub/navigator/profile_navbar/profile_navbar","bithub/helpers/permission_checker"],function(t,i,n,r,a){return t.Control.extend("Bithub.Controls.Navigator",{defaults:{},pluginName:"navigator"},{init:function(){this.initControl(t.route.attr("page"))},"{can.route} page":function(t,i,n){this.initControl(n)},initControl:function(t){var e,o=$("<div/>");e="admin"===t?n:i,a.hasPermissions(this.options.currentUser,e)?_.isEqual(t,this._currentPage)||(new e(o,this.options),this.element.html(o),this._currentPage=t):(new i(o,this.options),this.element.html(o)),new r("#profile-navbar",this.options)},"{currentUser} loggedIn":function(){var i=this;setTimeout(function(){i.initControl(t.route.attr("page"))},0)}})});
/*bithub/login/login.mustache!can/view/mustache/system*/
define("bithub/login/login.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/login/login.mustache",t.Mustache(function(s,i){var n=[];return n.push('<li class="divider-vertical"></li>'),n.push("\n"),n.push(t.view.txt(0,"undefined",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"ifequal"},{get:"user.authStatus"},"loggedIn",[{fn:function(s,i){var n=[];return n.push('<li>\n	<button id="show-newpost-form-btn" class="'),n.push(t.view.txt(!0,"button","class",this,t.Mustache.txt({scope:s,options:i},null,{get:"postBtnActive"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span>+</span> Post</button>\n</li>\n<li id="profile-dropdown" class="dropdown">\n  <button class="dropdown-toggle" data-toggle="dropdown">\n	<div class="gradient-border-wrapper">\n	  '),n.push(t.view.txt(0,"div",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"user.avatarUrl"},[{fn:function(s,i){var n=[];return n.push('<img class="avatar" '),n.push(t.view.txt(2,"img","src",this,function(){var n=[];return n.push('src="'),n.push(t.view.toStr(t.Mustache.txt({scope:s,options:i,special:!0},null,{get:"."})())),n.push('"'),n.join("")})),n.push(' alt="gravatar" '),n.push(t.view.txt(1,"img",1,this,t.Mustache.txt({scope:s,options:i},null,{get:"imgErrorHandler"}))),n.push("",t.view.pending({scope:s,options:i}),">"),n.join("")}}]))),n.push('\n	</div>\n	<b class="caret"></b>\n  </button>\n  <ul class="dropdown-menu">\n	<li><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"routes.earnpoints"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span class="login-points profile-points"></span>Earn Points</a></li>\n	<li><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"routes.rewards"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span class="login-rewards profile-rewards"></span>Rewards</a></li>\n	<li><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"routes.activities"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span class="login-activity profile-activity"></span>Activity</a></li>\n	<li><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"routes.profile"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span class="login-profile profile-account"></span>Profile</a></li>'),n.push(t.view.txt(0,"ul",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"user.isAdmin"},[{fn:function(s,i){var n=[];return n.push('	<li><a href="'),n.push(t.view.txt(!0,"a","href",this,t.Mustache.txt({scope:s,options:i},null,{get:"routes.admin"}))),n.push('"',t.view.pending({scope:s,options:i}),">"),n.push('<span class="login-admin admin-panel"></span>Admin Panel</a></li>'),n.push("\n"),n.join("")}}]))),n.push('	\n	<li><a id="logout-btn" href="javascript://"><span class="login-logout"></span>Logout</a></li>\n  </ul>\n</li>'),n.push("\n"),n.join("")}}]))),n.push(t.view.txt(0,"/li",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"ifequal"},{get:"user.authStatus"},"loggingIn",[{fn:function(){var t=[];return t.push('\n<li>\n  <a class="disabled">Logging in...</a>\n</li>'),t.push("\n"),t.join("")}}]))),n.push(t.view.txt(0,"img",0,this,t.Mustache.txt({scope:s,options:i},"#",{get:"ifequal"},{get:"user.authStatus"},"loggedOut",[{fn:function(){var t=[];return t.push('\n<li id="login-dropdown" class="dropdown">\n  <a href="#" class="dropdown-toggle login-btn" data-toggle="dropdown">Login or Register<b class="caret"></b></a>\n  <ul class="dropdown-menu">\n	<li><a id="login-twitter-link" href="/api/auth/twitter"><span class="login-twitter"></span>Twitter</a></li>\n	<li><a id="login-github-link" href="/api/auth/github"><span class="login-github"></span>GitHub</a></li>\n	<li><a id="login-meetup-link" href="/api/auth/meetup"><span class="login-meetup"></span>Meetup</a></li>\n  </ul>\n</li>'),t.push("\n"),t.join("")}}]))),n.join("")}))});
/*bithub/login/login*/
define("bithub/login/login",["can/can","./login.mustache!"],function(t,i){return t.Control({defaults:{}},{init:function(n,e){n.html(i({user:e.currentUser,routes:{earnpoints:t.route.url({page:"earnpoints"},!1),rewards:t.route.url({page:"rewards"},!1),activities:t.route.url({page:"profile",view:"activities"},!1),profile:t.route.url({page:"profile",view:"info"},!1),admin:t.route.url({page:"admin"},!1)}},{postBtnActive:function(){return e.newpostVisibility()?"active":""}}))},"#login-github-link click":function(t,i){i.preventDefault();var n=this.options.currentUser;n.login("github")},"#login-twitter-link click":function(t,i){i.preventDefault();var n=this.options.currentUser;n.login("twitter")},"#login-meetup-link click":function(t,i){i.preventDefault();var n=this.options.currentUser;n.login("meetup")},"#show-newpost-form-btn click":function(){this.options.newpostVisibility(!this.options.newpostVisibility())},"#logout-btn click":function(){this.options.currentUser.logout()}})});
/*bithub/newpost/init.mustache!can/view/mustache/system*/
define("bithub/newpost/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(n){return n.view.preloadStringRenderer("bithub/newpost/init.mustache",n.Mustache(function(t,s){var o=[];return o.push('<div class="container">\n	<div class="row">\n	<div class="span8 newpost-form">'),o.push("\n"),o.push(n.view.txt(0,"div",0,this,n.Mustache.txt({scope:t,options:s},"#",{get:"currentEvent"},[{fn:function(t,s){var o=[];return o.push('			<bh-postform event="this"',n.view.pending({tagName:"bh-postform",scope:t,options:s})),o.push("></bh-postform>"),o.push("\n"),o.join("")}}]))),o.push('	</div>\n	<div class="span4 newpost-sidebar">\n		<button id="hide-newpost-form-btn" class="close">&times;</button>\n		<h4 class="submit-your-app">Submit Your App, <br />Plugin, or Article <span class="badge">+2 +votes</span></h4>\n		<p>Earn points by sharing something you built, wrote, or found that would be useful to the community. You get <span class="badge">+2</span> points for sharing plus points for every vote from the community.</p>\n		<hr>\n		\n		<h4>Need Help?</h4>\n		<p>If you\'re looking for help with any of our open source projects, head on over to our <a href="http://forum.javascriptmvc.com">forums</a> and post your question there.</p>\n		\n	</div>\n	</div>\n</div>\n'),o.join("")}))});
/*bithub/newpost/typeahead*/
define("bithub/newpost/typeahead",["bithub/models/postas_user"],function(e){return function(t){var n;return function(){return function(r){var i=$(r);i.typeahead({minLength:3,source:function(r,i){var u=t.find("input[name=postas_feed]:checked").val();clearTimeout(this.timeout),this.timeout=setTimeout(function(){e.findAll({user:r,feed:u}).then(function(e){n=e,i(can.map(e,function(e){return e.fullName()}))})},200)},matcher:function(e){return e&&e.toLowerCase().indexOf(this.query.trim().toLowerCase())>-1},updater:function(e){var r=e.split("/")[0].trim();return selectedUser=can.grep(n,function(e){return e.username===r})[0],t.find("input.postas_id").val(selectedUser.id),t.find("img.postas.avatar").attr("src",selectedUser.profileImageUrl()),t.find("input.postas_feed").val(selectedUser.from),e}})}}}});
/*bithub/newpost/newpost*/
define("bithub/newpost/newpost",["can/can","./init.mustache!","bithub/models/event","./typeahead","bithub/postform/postform"],function(t,e,i){return t.Control("Bithub.Newpost",{},{init:function(n){this.currentEvent=t.compute(this.options.currentEvent||new i),n.html(e({currentEvent:this.currentEvent}))},"#hide-newpost-form-btn click":function(t,e){e.preventDefault(),this.options.visibility(!this.options.visibility())},"{visibility} change":function(t){var t=this.options.visibility(),e=this.currentEvent();t?(e||this.currentEvent(new i),this.element.slideDown()):this.element.slideUp()}," event.saved":function(){var e=this,i=this.currentEvent(),n=t.route.attr();n.category===i.category&&n.project===i.project?t.trigger(Bithub.Models.Event,"reload"):t.route.attr({page:"homepage",view:"latest",category:i.category,project:i.project},!0),this.options.visibility(!1),setTimeout(function(){e.currentEvent(null)},500)},"{can.route} newpost_c":function(){},"{can.route} newpost_p":function(){}})});
/*bithub/models/pagination*/
define("bithub/models/pagination",["can/can","vendor/moment/moment","can/list/list"],function(t){var n=function(t,n,e){return t*n+(e||o)},e=function(t){return n(t,100)},r={digest:0,feature:e,bug:e,app:e,article:e,chat:function(e){return"chat"===t.route.attr("category")?n(e,30):50},code:function(t){return n(t,45)},comment:e,event:e,plugin:e,question:e,twitter:e},i=500,o=35,u=function(t,n){n=n||r;var e,i=0;for(var o in t)e=n[o],e&&(i+="function"==typeof e?e(t[o]):e);return i},a=function(t,n){n=n||{};var e,r,o,a,f=n.threshold||i,l=[],c=0,s=[];for(a=0;a<t.length;a++)e||(e=t[a].date),r=t[a].date,o=u(t[a]),c+o>=f||a===t.length-1?(s=[e],e!==r&&s.push(r),l.push(s.sort().join(":")),e=null,r=null,c=0):c+=o;return l},f={limit:30,offset:0},l=0;return t.Model.extend("Bithub.Models.Pagination",{findAll:"GET /api/v1/events/pagination",model:function(t){return t.id=++l,this._super(t)},defaultParams:function(){return t.extend({},f)},getDateSpans:function(n,e,r){r=r||{},n.tags&&1===n.tags.length&&"event"===n.tags[0]&&(n.only_future=!0,n.order="asc"),this.findAll(t.extend({},f,n),function(t){e(a(t.attr(),r))})}},{})});
/*bithub/lib/paginator*/
define("bithub/lib/paginator",["can/can","bithub/models/pagination"],function(t,i){var s=i.defaultParams(),a=function(){var i={tags:[]};if("all"!==t.route.attr("project")&&i.tags.push(t.route.attr("project")),"all"!==t.route.attr("category")&&i.tags.push(t.route.attr("category")),["bug","feature"].indexOf(t.route.attr("category"))>=0){var s=t.route.attr("state")||"open";["open","closed"].indexOf(s)>=0&&i.tags.push(s)}return i};return t.Construct.extend({init:function(i){this.idx=0,this.canLoad=!0,this.list=new t.Observe.List,this.params=t.extend({},s),this.updateList(i)},current:function(){return this.list[this.idx]},hasNext:function(){return this.idx<this.list.length-1},next:function(){return this.idx<this.list.length-1?(this.idx++,this.canLoad&&this.idx>=this.list.length-3&&(this.params.offset+=this.params.limit,this.appendList()),this.current()):!1},updateList:function(s){var e=this,n=t.extend({},this.params,a());this.canLoad&&i.getDateSpans(n,function(t){0==t.length&&(e.canLoad=!1),e.list.replace(t),s&&s()})},appendList:function(s){var e=this,n=t.extend({},this.params,a());this.canLoad&&i.getDateSpans(n,function(t){0==t.length&&(e.canLoad=!1),t.length&&-1===e.list.indexOf(t[0])&&e.list.push.apply(e.list,t),s&&s()})},reset:function(t){this.idx=0,this.params.offset=0,this.canLoad=!0,this.updateList(t)}})});
/*bithub/lib/query_tracker*/
define("bithub/lib/query_tracker",["can/can","bithub/lib/paginator","can/route/route","can/map/delegate/delegate","vendor/moment/moment"],function(t,e){var a={homepage:{latest:{order:"thread_updated_ts:desc",thread_updated_date:moment().format("YYYY-MM-DD"),offset:0,limit:1e3},greatest:{order:"upvotes:desc",offset:0,limit:15},details:{},rewards:{}},profile:{info:{},activities:{}},admin:{users:{},tags:{},rewards:{}},earnpoints:{},rewards:{},eventdetails:{},leaderboard:{},user_profile:{}},r=function(t){var e="YYYY-MM-DD",a={day:moment().format(e),week:moment().subtract("days",7).format(e)+":"+moment().format(e),month:moment().subtract("months",1).format(e)+":"+moment().format(e)};return a[t]},n=function(){var e={};return"all"!==t.route.attr("project")&&(e.tag=t.route.attr("project")),"all"!==t.route.attr("category")&&(e.category=t.route.attr("category")),"all"!==t.route.attr("timespan")&&"latest"!==t.route.attr("view")&&(e.thread_updated_date=r(t.route.attr("timespan"))),"both"!==t.route.attr("state")&&(e.state=t.route.attr("state")),"event"===e.category?(e.order="thread_updated_ts:asc",e.in_future=!0):e.without_future=!0,"feature"!==e.category&&"bug"!==e.category&&delete e.state,e},o=function(){this.paginator.next()||this._onEndOfList(!0)},i=function(){var t=this.state.homepage.greatest;this.state.attr("homepage.greatest.offset",t.offset+t.limit)};return t.Construct.extend({init:function(r,n){var o=this;a.homepage.latest.thread_updated_date=function(){return o.paginator.current()},this.state=new t.Observe(a),this.paginator=new e(n),this._onEndOfList=t.compute(!1)},reset:function(e){var r=this;this._onEndOfList(!1),"latest"==t.route.attr("view")?this._pending||(this._pending=!0,this.paginator.reset(function(){e&&e(),r._pending=!1})):(this.state.attr(a),e&&e())},current:function(e){var a=t.route.attr("page")||"homepage",r=t.route.attr("view")||"latest",o=this.state[a][r],i=t.extend({},o?o.attr():{},n()||{},e||{});return i},next:function(){return"latest"==t.route.attr("view")?o.call(this):"greatest"==t.route.attr("view")&&i.call(this),this.current()},hasNext:function(){return this.paginator.hasNext()}})});
/*bithub/modals/init.mustache!can/view/mustache/system*/
define("bithub/modals/init.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(a){return a.view.preloadStringRenderer("bithub/modals/init.mustache",a.Mustache(function(){var a=[];return a.push('<!-- LOGIN MODAL -->\n<div id="login-modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="login-modal-label" aria-hidden="true">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>\n	<h3 id="login-modal-label">Login <small>... via these services</small></h3>\n  </div>\n  <div class="modal-body">\n	<ul class="providers">\n	  <li class="twitter"><a href="/api/auth/twitter" class="btn connect"><span class="login-twitter"></span> Twitter</a></li>\n	  <li class="github"><a href="/api/auth/github" class="btn connect"><span class="login-github"></span> GitHub</a></li>\n    <li class="meetup"><a href="/api/auth/meetup" class="btn connect"><span class="login-meetup"></span> Meetup</a></li>\n	</ul>\n  </div>\n</div>\n<!-- Modal -->\n<div id="githubModal" class="modal hide fade">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3 id="myModalLabel">Watch & Fork Us on GitHub <span class="badge">+10</span></h3> \n  </div>\n  <div class="modal-body">\n	<p>Get +10 for every account you watch or fork: <a href="https://github.com/bitovi">https://github.com/bitovi</a></p>\n  </div>\n</div>\n<!-- Modal -->\n<div id="swagModal" class="modal hide fade">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3 id="myModalLabel">Redeeming Points</h3>\n  </div>\n  <div class="modal-body">\n	<p>Soon you\'ll be able to redeem your points for swag! Your points will continue to accumulate during the beta period and then you can trade them in for awesome stuff when it\'s available. Stay tuned!</p>\n  </div>\n</div>\n<!-- Modal -->\n<div id="reportModal" class="modal hide fade">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3 id="myModalLabel">Report a Bug <span class="badge">+50</span></h3> \n  </div>\n  <div class="modal-body">\n	<p>If you find a bug in one of our projects, post the issue with a breaking test case to GitHub. We\'ll flag your post as a bug, award you the points, and attach an award to it for anyone who can fix the bug.</p>\n  </div>\n</div>\n<!-- Modal -->\n<div id="bugModal" class="modal hide fade">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3 id="myModalLabel">Fix a Bug <span class="badge">+100</span></h3> \n  </div>\n  <div class="modal-body">\n	<p>Look for posts with a green point badge next to the title. This indicates that points can be awarded for providing a solution to the issue. Post your solution in the same venue as the original post. If your solution is accepted, you\'ll earn those points!</p>\n  </div>\n</div>\n'),a.join("")}))});
/*bithub/modals/_twitter_modal.mustache!can/view/mustache/system*/
define("bithub/modals/_twitter_modal.mustache!can/view/mustache/system",["can/view/mustache/mustache"],function(t){return t.view.preloadStringRenderer("bithub/modals/_twitter_modal.mustache",t.Mustache(function(){var t=[];return t.push('<!-- Modal -->\n<div id="twitterModal" class="modal hide fade">\n  <div class="modal-header">\n	<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>\n	<h3 id="myModalLabel">Follow us on Twitter <span class="badge">+4</span></h3> \n  </div>\n  <div class="modal-body">\n	<p>Get +4 for every account you follow:</p>\n	<a href="https://twitter.com/bitovi" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @bitovi</a>\n	<a href="https://twitter.com/canjs" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @canjs</a>\n	<a href="https://twitter.com/jquerypp" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @jquerypp</a>\n	<a href="https://twitter.com/javascriptmvc" class="twitter-follow-button" data-show-count="false" data-dnt="true">Follow @javascriptmvc</a>\n	<a href="https://twitter.com/funcunit" class="twitter-follow-button" data-show-count="false">Follow @funcunit</a>\n	\n	<script>!function(d,s,id){var js,fjs=d.getElementsByTagName(s)[0],p=/^http:/.test(d.location)?\'http\':\'https\';if(!d.getElementById(id)){js=d.createElement(s);js.id=id;js.src=p+\'://platform.twitter.com/widgets.js\';fjs.parentNode.insertBefore(js,fjs);}}(document, \'script\', \'twitter-wjs\');</script>\n  </div>\n</div>\n'),t.join("")}))});
/*bithub/modals/modals*/
define("bithub/modals/modals",["can/can","./init.mustache!","./_twitter_modal.mustache!"],function(t,o,e){return t.Control({defaults:{}},{init:function(t,e){this.element.html(o({})),$("#login-modal").on("show",function(t){var o=e.currentUser;(o.loggedIn()||o.loggingIn())&&t.preventDefault()})},"{currentUser} authStatus":function(o,e,n,i){!n&&!i&&t.route.attr("login")&&this.showLogin()},"#login-modal .providers .twitter a click":function(t,o){o.preventDefault(),t.closest(".modal").modal("hide"),this.options.currentUser.login("twitter")},"#login-modal .providers .github a click":function(t,o){o.preventDefault(),t.closest(".modal").modal("hide"),this.options.currentUser.login("github")},"#login-modal .providers .meetup a click":function(t,o){o.preventDefault(),t.closest(".modal").modal("hide"),this.options.currentUser.login("meetup")},showLogin:function(){this.element.find("#login-modal").modal("show")},showTwitter:function(){$("#twitterModal").length||this.element.append(e({})),$("#twitterModal").modal("show")}})});
/*bithub/helpers/loadtime*/
define("bithub/helpers/loadtime",["jquery/jquery"],function(){return function(e){var n=0,o=window.START_TIME||new Date,a=e&&e.verbose||!1;$(document).ajaxSend(function(e,o,l){n++,a&&console.log("Ajax SEND; "+l.url+"; COUNT = "+n)}).ajaxComplete(function(e,l,t){0===--n&&$("#statusbar").html("<small>"+(new Date-o)/1e3+"</small>"),a&&console.log("Ajax COMPLETE; "+t.url+"; COUNT = "+n)})}});
/*jstz*/
System.define("jstz","/**\n * This script gives you the zone info key representing your device's time zone setting.\n *\n * @name jsTimezoneDetect\n * @version 1.0.5\n * @author Jon Nylander\n * @license MIT License - http://www.opensource.org/licenses/mit-license.php\n *\n * For usage and examples, visit:\n * http://pellepim.bitbucket.org/jstz/\n *\n * Copyright (c) Jon Nylander\n */\n\n/*jslint undef: true */\n/*global console, exports*/\n\n(function(root) {\n  /**\n   * Namespace to hold all the code for timezone detection.\n   */\n  var jstz = (function () {\n      'use strict';\n      var HEMISPHERE_SOUTH = 's',\n          \n          /**\n           * Gets the offset in minutes from UTC for a certain date.\n           * @param {Date} date\n           * @returns {Number}\n           */\n          get_date_offset = function (date) {\n              var offset = -date.getTimezoneOffset();\n              return (offset !== null ? offset : 0);\n          },\n\n          get_date = function (year, month, date) {\n              var d = new Date();\n              if (year !== undefined) {\n                d.setFullYear(year);\n              }\n              d.setMonth(month);\n              d.setDate(date);\n              return d;\n          },\n\n          get_january_offset = function (year) {\n              return get_date_offset(get_date(year, 0 ,2));\n          },\n\n          get_june_offset = function (year) {\n              return get_date_offset(get_date(year, 5, 2));\n          },\n\n          /**\n           * Private method.\n           * Checks whether a given date is in daylight saving time.\n           * If the date supplied is after august, we assume that we're checking\n           * for southern hemisphere DST.\n           * @param {Date} date\n           * @returns {Boolean}\n           */\n          date_is_dst = function (date) {\n              var is_southern = date.getMonth() > 7,\n                  base_offset = is_southern ? get_june_offset(date.getFullYear()) : \n                                              get_january_offset(date.getFullYear()),\n                  date_offset = get_date_offset(date),\n                  is_west = base_offset < 0,\n                  dst_offset = base_offset - date_offset;\n                  \n              if (!is_west && !is_southern) {\n                  return dst_offset < 0;\n              }\n\n              return dst_offset !== 0;\n          },\n\n          /**\n           * This function does some basic calculations to create information about\n           * the user's timezone. It uses REFERENCE_YEAR as a solid year for which\n           * the script has been tested rather than depend on the year set by the\n           * client device.\n           *\n           * Returns a key that can be used to do lookups in jstz.olson.timezones.\n           * eg: \"720,1,2\". \n           *\n           * @returns {String}\n           */\n\n          lookup_key = function () {\n              var january_offset = get_january_offset(),\n                  june_offset = get_june_offset(),\n                  diff = january_offset - june_offset;\n\n              if (diff < 0) {\n                  return january_offset + \",1\";\n              } else if (diff > 0) {\n                  return june_offset + \",1,\" + HEMISPHERE_SOUTH;\n              }\n\n              return january_offset + \",0\";\n          },\n\n          /**\n           * Uses get_timezone_info() to formulate a key to use in the olson.timezones dictionary.\n           *\n           * Returns a primitive object on the format:\n           * {'timezone': TimeZone, 'key' : 'the key used to find the TimeZone object'}\n           *\n           * @returns Object\n           */\n          determine = function () {\n              var key = lookup_key();\n              return new jstz.TimeZone(jstz.olson.timezones[key]);\n          },\n\n          /**\n           * This object contains information on when daylight savings starts for\n           * different timezones.\n           *\n           * The list is short for a reason. Often we do not have to be very specific\n           * to single out the correct timezone. But when we do, this list comes in\n           * handy.\n           *\n           * Each value is a date denoting when daylight savings starts for that timezone.\n           */\n          dst_start_for = function (tz_name) {\n\n            var ru_pre_dst_change = new Date(2010, 6, 15, 1, 0, 0, 0), // In 2010 Russia had DST, this allows us to detect Russia :)\n                dst_starts = {\n                    'America/Denver': new Date(2011, 2, 13, 3, 0, 0, 0),\n                    'America/Mazatlan': new Date(2011, 3, 3, 3, 0, 0, 0),\n                    'America/Chicago': new Date(2011, 2, 13, 3, 0, 0, 0),\n                    'America/Mexico_City': new Date(2011, 3, 3, 3, 0, 0, 0),\n                    'America/Asuncion': new Date(2012, 9, 7, 3, 0, 0, 0),\n                    'America/Santiago': new Date(2012, 9, 3, 3, 0, 0, 0),\n                    'America/Campo_Grande': new Date(2012, 9, 21, 5, 0, 0, 0),\n                    'America/Montevideo': new Date(2011, 9, 2, 3, 0, 0, 0),\n                    'America/Sao_Paulo': new Date(2011, 9, 16, 5, 0, 0, 0),\n                    'America/Los_Angeles': new Date(2011, 2, 13, 8, 0, 0, 0),\n                    'America/Santa_Isabel': new Date(2011, 3, 5, 8, 0, 0, 0),\n                    'America/Havana': new Date(2012, 2, 10, 2, 0, 0, 0),\n                    'America/New_York': new Date(2012, 2, 10, 7, 0, 0, 0),\n                    'Europe/Helsinki': new Date(2013, 2, 31, 5, 0, 0, 0),\n                    'Pacific/Auckland': new Date(2011, 8, 26, 7, 0, 0, 0),\n                    'America/Halifax': new Date(2011, 2, 13, 6, 0, 0, 0),\n                    'America/Goose_Bay': new Date(2011, 2, 13, 2, 1, 0, 0),\n                    'America/Miquelon': new Date(2011, 2, 13, 5, 0, 0, 0),\n                    'America/Godthab': new Date(2011, 2, 27, 1, 0, 0, 0),\n                    'Europe/Moscow': ru_pre_dst_change,\n                    'Asia/Amman': new Date(2013, 2, 29, 1, 0, 0, 0),\n                    'Asia/Beirut': new Date(2013, 2, 31, 2, 0, 0, 0),\n                    'Asia/Damascus': new Date(2013, 3, 6, 2, 0, 0, 0),\n                    'Asia/Jerusalem': new Date(2013, 2, 29, 5, 0, 0, 0),\n                    'Asia/Yekaterinburg': ru_pre_dst_change,\n                    'Asia/Omsk': ru_pre_dst_change,\n                    'Asia/Krasnoyarsk': ru_pre_dst_change,\n                    'Asia/Irkutsk': ru_pre_dst_change,\n                    'Asia/Yakutsk': ru_pre_dst_change,\n                    'Asia/Vladivostok': ru_pre_dst_change,\n                    'Asia/Baku': new Date(2013, 2, 31, 4, 0, 0),\n                    'Asia/Yerevan': new Date(2013, 2, 31, 3, 0, 0),\n                    'Asia/Kamchatka': ru_pre_dst_change,\n                    'Asia/Gaza': new Date(2010, 2, 27, 4, 0, 0),\n                    'Africa/Cairo': new Date(2010, 4, 1, 3, 0, 0),\n                    'Europe/Minsk': ru_pre_dst_change,\n                    'Pacific/Apia': new Date(2010, 10, 1, 1, 0, 0, 0),\n                    'Pacific/Fiji': new Date(2010, 11, 1, 0, 0, 0),\n                    'Australia/Perth': new Date(2008, 10, 1, 1, 0, 0, 0)\n                };\n\n              return dst_starts[tz_name];\n          };\n\n      return {\n          determine: determine,\n          date_is_dst: date_is_dst,\n          dst_start_for: dst_start_for \n      };\n  }());\n\n  /**\n   * Simple object to perform ambiguity check and to return name of time zone.\n   */\n  jstz.TimeZone = function (tz_name) {\n      'use strict';\n        /**\n         * The keys in this object are timezones that we know may be ambiguous after\n         * a preliminary scan through the olson_tz object.\n         *\n         * The array of timezones to compare must be in the order that daylight savings\n         * starts for the regions.\n         */\n      var AMBIGUITIES = {\n              'America/Denver':       ['America/Denver', 'America/Mazatlan'],\n              'America/Chicago':      ['America/Chicago', 'America/Mexico_City'],\n              'America/Santiago':     ['America/Santiago', 'America/Asuncion', 'America/Campo_Grande'],\n              'America/Montevideo':   ['America/Montevideo', 'America/Sao_Paulo'],\n              'Asia/Beirut':          ['Asia/Amman', 'Asia/Jerusalem', 'Asia/Beirut', 'Europe/Helsinki','Asia/Damascus'],\n              'Pacific/Auckland':     ['Pacific/Auckland', 'Pacific/Fiji'],\n              'America/Los_Angeles':  ['America/Los_Angeles', 'America/Santa_Isabel'],\n              'America/New_York':     ['America/Havana', 'America/New_York'],\n              'America/Halifax':      ['America/Goose_Bay', 'America/Halifax'],\n              'America/Godthab':      ['America/Miquelon', 'America/Godthab'],\n              'Asia/Dubai':           ['Europe/Moscow'],\n              'Asia/Dhaka':           ['Asia/Yekaterinburg'],\n              'Asia/Jakarta':         ['Asia/Omsk'],\n              'Asia/Shanghai':        ['Asia/Krasnoyarsk', 'Australia/Perth'],\n              'Asia/Tokyo':           ['Asia/Irkutsk'],\n              'Australia/Brisbane':   ['Asia/Yakutsk'],\n              'Pacific/Noumea':       ['Asia/Vladivostok'],\n              'Pacific/Tarawa':       ['Asia/Kamchatka', 'Pacific/Fiji'],\n              'Pacific/Tongatapu':    ['Pacific/Apia'],\n              'Asia/Baghdad':         ['Europe/Minsk'],\n              'Asia/Baku':            ['Asia/Yerevan','Asia/Baku'],\n              'Africa/Johannesburg':  ['Asia/Gaza', 'Africa/Cairo']\n          },\n\n          timezone_name = tz_name,\n          \n          /**\n           * Checks if a timezone has possible ambiguities. I.e timezones that are similar.\n           *\n           * For example, if the preliminary scan determines that we're in America/Denver.\n           * We double check here that we're really there and not in America/Mazatlan.\n           *\n           * This is done by checking known dates for when daylight savings start for different\n           * timezones during 2010 and 2011.\n           */\n          ambiguity_check = function () {\n              var ambiguity_list = AMBIGUITIES[timezone_name],\n                  length = ambiguity_list.length,\n                  i = 0,\n                  tz = ambiguity_list[0];\n\n              for (; i < length; i += 1) {\n                  tz = ambiguity_list[i];\n\n                  if (jstz.date_is_dst(jstz.dst_start_for(tz))) {\n                      timezone_name = tz;\n                      return;\n                  }\n              }\n          },\n\n          /**\n           * Checks if it is possible that the timezone is ambiguous.\n           */\n          is_ambiguous = function () {\n              return typeof (AMBIGUITIES[timezone_name]) !== 'undefined';\n          };\n\n      if (is_ambiguous()) {\n          ambiguity_check();\n      }\n\n      return {\n          name: function () {\n              return timezone_name;\n          }\n      };\n  };\n\n  jstz.olson = {};\n\n  /*\n   * The keys in this dictionary are comma separated as such:\n   *\n   * First the offset compared to UTC time in minutes.\n   *\n   * Then a flag which is 0 if the timezone does not take daylight savings into account and 1 if it\n   * does.\n   *\n   * Thirdly an optional 's' signifies that the timezone is in the southern hemisphere,\n   * only interesting for timezones with DST.\n   *\n   * The mapped arrays is used for constructing the jstz.TimeZone object from within\n   * jstz.determine_timezone();\n   */\n  jstz.olson.timezones = {\n      '-720,0'   : 'Pacific/Majuro',\n      '-660,0'   : 'Pacific/Pago_Pago',\n      '-600,1'   : 'America/Adak',\n      '-600,0'   : 'Pacific/Honolulu',\n      '-570,0'   : 'Pacific/Marquesas',\n      '-540,0'   : 'Pacific/Gambier',\n      '-540,1'   : 'America/Anchorage',\n      '-480,1'   : 'America/Los_Angeles',\n      '-480,0'   : 'Pacific/Pitcairn',\n      '-420,0'   : 'America/Phoenix',\n      '-420,1'   : 'America/Denver',\n      '-360,0'   : 'America/Guatemala',\n      '-360,1'   : 'America/Chicago',\n      '-360,1,s' : 'Pacific/Easter',\n      '-300,0'   : 'America/Bogota',\n      '-300,1'   : 'America/New_York',\n      '-270,0'   : 'America/Caracas',\n      '-240,1'   : 'America/Halifax',\n      '-240,0'   : 'America/Santo_Domingo',\n      '-240,1,s' : 'America/Santiago',\n      '-210,1'   : 'America/St_Johns',\n      '-180,1'   : 'America/Godthab',\n      '-180,0'   : 'America/Argentina/Buenos_Aires',\n      '-180,1,s' : 'America/Montevideo',\n      '-120,0'   : 'America/Noronha',\n      '-120,1'   : 'America/Noronha',\n      '-60,1'    : 'Atlantic/Azores',\n      '-60,0'    : 'Atlantic/Cape_Verde',\n      '0,0'      : 'UTC',\n      '0,1'      : 'Europe/London',\n      '60,1'     : 'Europe/Berlin',\n      '60,0'     : 'Africa/Lagos',\n      '60,1,s'   : 'Africa/Windhoek',\n      '120,1'    : 'Asia/Beirut',\n      '120,0'    : 'Africa/Johannesburg',\n      '180,0'    : 'Asia/Baghdad',\n      '180,1'    : 'Europe/Moscow',\n      '210,1'    : 'Asia/Tehran',\n      '240,0'    : 'Asia/Dubai',\n      '240,1'    : 'Asia/Baku',\n      '270,0'    : 'Asia/Kabul',\n      '300,1'    : 'Asia/Yekaterinburg',\n      '300,0'    : 'Asia/Karachi',\n      '330,0'    : 'Asia/Kolkata',\n      '345,0'    : 'Asia/Kathmandu',\n      '360,0'    : 'Asia/Dhaka',\n      '360,1'    : 'Asia/Omsk',\n      '390,0'    : 'Asia/Rangoon',\n      '420,1'    : 'Asia/Krasnoyarsk',\n      '420,0'    : 'Asia/Jakarta',\n      '480,0'    : 'Asia/Shanghai',\n      '480,1'    : 'Asia/Irkutsk',\n      '525,0'    : 'Australia/Eucla',\n      '525,1,s'  : 'Australia/Eucla',\n      '540,1'    : 'Asia/Yakutsk',\n      '540,0'    : 'Asia/Tokyo',\n      '570,0'    : 'Australia/Darwin',\n      '570,1,s'  : 'Australia/Adelaide',\n      '600,0'    : 'Australia/Brisbane',\n      '600,1'    : 'Asia/Vladivostok',\n      '600,1,s'  : 'Australia/Sydney',\n      '630,1,s'  : 'Australia/Lord_Howe',\n      '660,1'    : 'Asia/Kamchatka',\n      '660,0'    : 'Pacific/Noumea',\n      '690,0'    : 'Pacific/Norfolk',\n      '720,1,s'  : 'Pacific/Auckland',\n      '720,0'    : 'Pacific/Tarawa',\n      '765,1,s'  : 'Pacific/Chatham',\n      '780,0'    : 'Pacific/Tongatapu',\n      '780,1,s'  : 'Pacific/Apia',\n      '840,0'    : 'Pacific/Kiritimati'\n  };\n\n  if (typeof exports !== 'undefined') {\n    exports.jstz = jstz;\n  } else {\n    root.jstz = jstz;\n  }\n})(this);\n");
/*ui/onbottom*/
define("ui/onbottom",["can/can","vendor/lodash/lodash","can/construct/super/super","can/construct/proxy/proxy"],function(){return can.Control("UI.Onbottom",{defaults:{threshold:50,onbottom:!1}},{setup:function(){var t=this.trigger,e=this;this.trigger=_.throttle(function(){t.call(e)},100),this._super.apply(this,arguments)},init:function(){},scroll:"trigger","{window} resize":"trigger","{window} resetOnBottom":function(){delete this.__elemHeight},trigger:function(){var t=this.element.height(),e=$(window).innerHeight(),o=$(window).scrollTop(),i=Math.abs(o-(t-e));i<this.options.threshold&&this.__elemHeight!==t&&(this.__elemHeight=t,this.element.trigger("onbottom"))}})});
/*can/route/pushstate/pushstate*/
define("can/route/pushstate/pushstate",["can/util/util","can/route/route"],function(t){"use strict";if(window.history&&history.pushState){t.route.bindings.pushstate={root:"/",paramsMatcher:/^\?(?:[^=]+=[^&]*&)*[^=]+=[^&]*/,querySeparator:"?",bind:function(){t.delegate.call(t.$(document.documentElement),"a","click",e),t.each(n,function(e){a[e]=window.history[e],window.history[e]=function(o,n,i){var r=0===i.indexOf("http"),u=window.location.search+window.location.hash;(!r&&i!==window.location.pathname+u||r&&i!==window.location.href+u)&&(a[e].apply(window.history,arguments),t.route.setState())}}),t.bind.call(window,"popstate",t.route.setState)},unbind:function(){t.undelegate.call(t.$(document.documentElement),"click","a",e),t.each(n,function(t){window.history[t]=a[t]}),t.unbind.call(window,"popstate",t.route.setState)},matchingPartOfURL:function(){var t=o(),e=location.pathname+location.search,n=e.indexOf(t);return e.substr(n+t.length)},setURL:function(e){i&&-1===e.indexOf("#")&&window.location.hash&&(e+=window.location.hash),window.history.pushState(null,null,t.route._call("root")+e)}};var e=function(e){if(!(e.isDefaultPrevented?e.isDefaultPrevented():e.defaultPrevented===!0)){var o=this._node||this,n=o.host||window.location.host;if(window.location.host===n){var a=t.route._call("root");if(0===o.pathname.indexOf(a)){var r=(o.pathname+o.search).substr(a.length),u=t.route.deparam(r);u.hasOwnProperty("route")&&(i=!0,window.history.pushState(null,null,o.href),e.preventDefault&&e.preventDefault())}}}},o=function(){var e=location.protocol+"//"+location.host,o=t.route._call("root"),n=o.indexOf(e);return 0===n?t.route.root.substr(e.length):o},n=["pushState","replaceState"],a={},i=!1;t.route.defaultBinding="pushstate"}return t});
/*bithub/bithub*/
define("bithub/bithub",["can/can","bithub/pageswitcher/pageswitcher","bithub/navigator/navigator","bithub/login/login","bithub/newpost/newpost","bithub/lib/query_tracker","bithub/modals/modals","bithub/models/event","bithub/models/tag","bithub/models/user","bithub/helpers/loadtime","jstz/jstz","ui/onbottom","can/route/pushstate/pushstate","bootstrap/bootstrap","bootstrap-datepicker/bootstrap-datepicker","vendor/lodash/lodash"],function(e,t,a,r,i,o,n,s,p,c,l,u){var d=window.location.href;"/"===d.substr(d.length-1)&&d!==window.location.origin+"/"&&(window.location.href=d.substr(0,d.length-1));if(!steal.isBuilding){l();var m=["canjs","donejs","javascriptmvc","funcunit","jquerypp","stealjs","canui","documentjs"],g=["bug","issue","twitter","question","article","comment","app","code","chat","plugin","event","feature"],w=["day","week","month","all"],h=[{params:["admin"],paramKey:"page",defaultProps:{view:"users",action:"list"},childs:[{params:["tags","users","rewards","achievements"],paramKey:"view",childs:[{params:["edit","new"],paramKey:"action"}]}]},{params:["latest"],paramKey:"view",defaultProps:{page:"homepage",view:"latest",project:"all",category:"all",timespan:"all"},childs:[{params:g,paramKey:"category"},{params:m,paramKey:"project",childs:[{params:g,paramKey:"category"}]}]},{params:["greatest"],paramKey:"view",defaultProps:{page:"homepage",view:"latest",project:"all",category:"all",timespan:"month"},childs:[{params:w,paramKey:"timespan"},{params:g,paramKey:"category",childs:[{params:w,paramKey:"timespan"}]},{params:m,paramKey:"project",childs:[{params:w,paramKey:"timespan"},{params:g,paramKey:"category",childs:[{params:w,paramKey:"timespan"}]}]}]}],f=function(e,t,a){return t=t||"/",a=a||[],(t+_.difference(e,a).join("/")).replace("//","/")},b=function(t,a,r){a=a||"/",r=r||{},_.each(t,function(t){var i=t.params,o=t.childs,n=t.paramKey,s=t.defaultProps;_.each(i,function(t){var i=s&&s[n]||[],p=f([t],a,i),c={};_.extend(c,s,r),c[n]=t,e.route(p,c),o&&b(o,p+"/",c)})})};b(h),e.route("/rewards",{page:"rewards"}),e.route("/leaderboard",{page:"leaderboard"}),e.route("/earnpoints",{page:"earnpoints"}),e.route("/profile",{page:"profile",view:"info"}),e.route("/profile/activities",{page:"profile",view:"activities"}),e.route("/profile/:id",{page:"user_profile"}),e.route("/event/:id",{page:"eventdetails"}),e.route.ready();var y=e.compute(!1),m=new e.Model.List,g=new e.Model.List,v=new e.Model.List,j=new Bithub.Models.User.List,E=new c,K=new Bithub.Models.Event.List([{}]),T=new e.Observe.List;$.ajaxSetup({beforeSend:function(e){e.setRequestHeader("clientTz",u.determine().name()||"UTC")}}),window.EVENTS_PRELOADED=!1;var L=new o({},function(t){var a="latest"===e.route.attr("view")?"Latest":"Greatest";s["find"+a](t||L.current(),function(e){window.EVENTS_PRELOADED=!0,K.replace(e),0==e.length&&K._triggerChange("length","add")})});CURRENT_USER=E,QUERY_TRACKER=L,VISIBLE_TAGS=T,CATEGORIES=g,PROJECTS=m,E.fromSession();var R=new n("#bootstrapModals",{currentUser:E});new t("#pages",{currentUser:E,preloadedEvents:K,queryTracker:L,projects:m,feeds:v,categories:g,newpostVisibility:y,modals:R,users:j,visibleTags:T}),new a("#navigator",{currentUser:E,projects:m,categories:g}),new r("#login",{currentUser:E,newpostVisibility:y}),new i("#newpost-form-container",{currentUser:E,modals:R,projects:m,categories:g,visibility:y,project:e.route.attr("newpost_p")||"",category:e.route.attr("newpost_c")||""}),e.route.attr("newpost")&&y(!0);var U=function(t,a){var r=_.each(t.attr(),function(t){e.extend(t,a)});T.push.apply(T,r)};p.findAll({type:"category",order:"priority:desc"},function(e){var t=["digest","issue","github_comment"],a=[];e.each(function(e,r){t.indexOf(e.attr("name"))>=0&&a.unshift(r)});for(var r=0;r<a.length;r++)e.splice(a[r],1);g.replace(e),U(e,{type:"category"})}),p.findAll({type:"project"},function(e){m.replace(e),U(e,{type:"project"})}),p.findAll({type:"feed"},function(e){v.replace(e)}),function S(){c.findAll({cached:!0},function(e){j.replace(e)}),setTimeout(S,6e4)}(),window.addEventListener("message",function(t){var a=t.data;window.location.origin===t.origin&&a.type&&a.message&&e.trigger(window,a.type,a.message)}),new UI.Onbottom(document,{threshold:$(window).innerHeight()/2})}});
