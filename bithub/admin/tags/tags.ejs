<table class="table table-stripped">	<thead>		<tr>			<th>Name</th>			<th>Display name</th>		</tr>	</thead>	<tbody>		<% list(tags, function( tag, idx ) { %>		<tr <%= (el) -> el.data('tag', tag) %> >			<td><a href="<%=can.route.url({page:'admin', view:'tags', action:'edit', id:tag.id})%>"><%= tag.name %></a></td>			<td><%= tag.display_name %></td>			<td><button class="delete-tag close">&times;</button></td>		</tr>		<% }) %>	</tbody></table><div class="actions pull-left">	<a class="btn" href="<%= can.route.url({page:'admin', view:'tags', action:'new'}) %>">New tag</a></div><div class="pagination pull-right"><%== can.route.link("Prev page", {page: 'admin', view: 'tags', action: 'list', offset: prevOffset }, {className:"btn"}) %><%== can.route.link("Next page", {page: 'admin', view: 'tags', action: 'list', offset: nextOffset }, {className:"btn"}) %></div>