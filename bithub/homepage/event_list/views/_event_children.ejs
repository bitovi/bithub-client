<div class="replies row">
  <span class="replies-caret"></span>
  <% list(events, function( event ) { %>

  <% if( event.attr('tags').indexOf('push_event') >= 0 ) { %>

  <% list(event.attr('children'), function( event ) { %>
  <%== partials.eventChildEvent.render( {event: event, data: data, type: 'commit'} ); %>
  <% }) %>
  
  <% } else if( event.attr('tags').indexOf('pull_request_event') >= 0 ) { %>
  
  <%== partials.eventChildEvent.render( {event: event, data: data, type: 'pull_request'} ); %>

  <% } else { %>

  <%== partials.eventChildEvent.render( {event: event, data: data, type: ''} ); %>

  <% } %>

  <% }) %>
</div> <!-- /.replies -->
