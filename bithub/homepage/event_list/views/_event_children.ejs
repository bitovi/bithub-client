<div class="replies row">
  <span class="replies-caret"></span>
  <% list(events, function( event ) { %>

  <% if( event.attr('tags').indexOf('push_event') >= 0 ) { %>

  <% list(event.attr('children'), function( event ) { %>
  <%== eventChildEventPartial.render( can.extend({event: event, eventChildEventPartial: eventChildEventPartial, type: 'commit'}, this)); %>
  <% }) %>
  
  <% } else if( event.attr('tags').indexOf('pull_request_event') >= 0 ) { %>
  
  <%== eventChildEventPartial.render( can.extend({event: event, eventChildEventPartial: eventChildEventPartial, type: 'pull_request'}, this)); %>

  <% } else { %>

  <%== eventChildEventPartial.render( can.extend({event: event, eventChildEventPartial: eventChildEventPartial, type: ''}, this)); %>

  <% } %>

  <% }) %>
</div> <!-- /.replies -->
