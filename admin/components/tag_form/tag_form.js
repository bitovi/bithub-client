steal('can/util/string', 'can/component', './tag_form.mustache', function(can, Component, tagFormView){

  return can.Component({
    tag : 'tag-form',
    template : tagFormView,
    scope : {
      
    }
  })

});