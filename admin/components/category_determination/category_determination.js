steal('can/util/string', 'can/component', './category_determination.mustache', function(can, Component, categoryDeterminationView){

  return can.Component({
    tag : 'category-determination',
    template : categoryDeterminationView,
    scope : {
      
    }
  })

});